# 2026-07-20

- 本地 `main`、`dev` 已同步至远程；从最新 `dev`（`91e16c88`）创建 `daily-2026-07-20（2）`。
- 已切换到 `daily-2026-07-20（2）`。切换前的所有未提交及未跟踪内容安全保存于 `stash@{0}`（`pre-switch-daily-2026-07-20-2`）；当前工作区干净。

- 修复：最新 `dev` 的 `scripts/run-backend.cjs` 未加载 `backend/.env`，导致后端回退连接 `127.0.0.1:5432` 的宿主机 PostgreSQL 并报 `SQLSTATE 28P01`。已恢复 `.env` / `.env.local` 加载，后端会使用本地配置的 `5433`；脚本语法、dotenv 依赖和配置包测试已通过。

- 诊断：Apollo 启动失败不是网络或数据库问题。`backend/apollo-config.yaml` 被写为 `KEY=value` 环境文件格式，但加载器将其作为 YAML 解析，需按 `apollo-config.example.yaml` 的 YAML 结构修正；当前尚未改动用户刚更新的 Apollo 配置。

- 修复：根目录缺失 `.env.development`，导致 `dev:weapp` 未注入 `TARO_APP_API_BASE_URL_DEVELOP`，登录请求退化成 `/api/app/public-config` 相对地址。已创建忽略的本地开发环境文件，指向 `http://127.0.0.1:3010`。验证被微信开发者工具自动化服务未开启（端口 3001 连接失败）阻塞；需在重启 watch 后由开发者工具重新编译。

- 更新：已确认 `backend/apollo-config.yaml` 已修正为 loader 要求的嵌套 YAML，解析校验通过，`backend/.env` 最终配置源为 `apollo`。本地小程序与后端都需重启相关 watch/进程后才能验证完整登录链路。

- 复核：开发产物已注入 `http://127.0.0.1:3010`，本地后端 health 与 `/api/app/public-config` 均返回 200。随后仍出现的 `WAServiceMainContext timeout` 属微信开发者工具 AppService 热更新后卡死，非前端 API 地址或后端不可用；自动化端口 3001 未开启，无法在当前会话抓取具体运行时堆栈。
