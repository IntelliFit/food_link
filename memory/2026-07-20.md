# 2026-07-20

## 虚拟支付开发版 UI 验证

- 用户重新登录微信开发者工具后，使用 `weapp-devtools` 在端口 `3001` 恢复自动化连接。
- 已验证首页、我的页、会员开通页的真实运行时导航与展示；会员页显示当前“进阶版·年卡”和“到期后不自动续费 · 通过微信虚拟支付开通”，无旧自动续费入口。
- 已选择“轻度版·月卡 ¥9.90”，确认订阅和微信测试环境提示后成功唤起官方虚拟支付面板，并立即关闭，没有付款。
- 全程控制台 0 errors / 0 warnings，取消后按钮恢复、返回我的页正常。证据位于 `.local-state/audits/2026-07-20-xpay-ui/`。
- 结论：开发版 XPay 运行时门槛满足，可进入正式发布预检；仍需单独核对 main 生产 XPay 配置、部署正式后端、上传体验版抽测并提交微信审核。
- 正式发布预检只读确认：Apollo `dev` 与 `main` 的 XPay `offer_id/app_key/message_token` 当前完全相同，`sandbox` 也都为 `true`。因此 main 尚未配置现网 AppKey，不能直接部署正式版。用户需把 main `wechat.xpay.app_key` 改为微信后台“现网 AppKey”、`wechat.xpay.sandbox` 改为 `false`，并确认正式消息回调 URL/Token。
- 根版本当前为 `3.0.2`；上传正式候选前需用户确认新版本号，建议 `3.0.3`。
- Chrome 中 Apollo main 页处于已登录状态，但微信公众平台管理页被浏览器安全策略禁止代理控制，不能替用户读取现网 AppKey。已把 Apollo main 页作为 handoff 保留；用户需本人复制现网 AppKey、设置 `sandbox: false` 并发布配置。

## 营养向量密钥轮换与自动更新流程

- 用户已完成新Yunwu Key创建、dev Apollo embedding配置填写和发布；临时旧Key应停用，新Key只保存在Apollo，不写源码或Git。
- 只更换API Key不需要重算线上16,553条向量；保持`text-embedding-3-large`和1024维时已有向量继续有效，只有更换模型或维度才需要全量重建。
- 用户计划稍后随开发环境其它改动一起重启。这里仅需滚动重启`foodlink-dev`后端Pod/进程，不需要重启物理服务器；可与其它对话中的dev部署合并为一次滚动更新，避免重复重启和互相覆盖。
- 现有向量召回代码无需因Key轮换修改。自动维护代码已完成：每分钟检测食物/别名变化，PostgreSQL advisory lock保证三个Pod仅一个调用外部API，新增/改名自动生成、失效/blocked自动停用；各Pod按索引版本自动刷新，每24小时强制全量对账。API失败下一分钟重试，不阻塞食物写入或用户分析。
- 定向服务、仓库、分析、App测试与go vet通过；嵌入式PostgreSQL已验证源版本查询、分布式锁互斥/释放和向量停用。提交`5b2d152c`已推送`origin/dev`，待随下一次dev新镜像构建/推送后滚动更新Pod，不需重启物理服务器；仅重启旧镜像不会获得自动维护代码。
