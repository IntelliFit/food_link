# 2026-08-22

- 用户反馈“我的宠物”初始可见、点击选择后角色消失，刷新和重进仍不显示。截图证明接口数据、候选名称和选择状态均正常，但五个内置角色图片区域全部空白；根因是近期将随包本地角色切换为 WebP，而部分微信真机不支持本地 WebP 解码。已把五个内置角色和健文动作帧恢复为 PNG，并压缩为 256×256、256 色，总量约 111KB；增加禁止本地宠物 WebP 的回归测试。目标测试、TypeScript、ESLint和全量 60 套/179 条 Jest 通过；提交 `d56ab52c` 已推送 `origin/dev`。开发者工具端口未开放，缺真机交互验证和小程序上传。

- 用户明确组合餐食识别需求：例如菜心牛肉饭不应只作为一个整体调整食用比例，AI 应拆为牛肉、菜心、米饭等可独立食用/剩余的项目；每项独立调整实际食用比例并重新汇总整餐营养。已统一修改快速/普通/精准/分组精准/直接营养估算图片提示词，并把结果页改为“食物明细”且提示逐项调整；油盐酱汁等不可独立食用辅料不机械拆分。Go 相关 5 包、微信 TypeScript/ESLint/SCSS、60 套170条 Jest 和 pre-commit 通过；3001/9420/9421 未开放，缺微信运行时截图。提交 `c98ac821` 已推送 dev，后端 digest `sha256:e642d77daf5e808883ea7c0e2cde658516707094d6babac75fb4aa68bc4f285b` 已滚动至 dev 3/3 Ready、0 重启，公网 health 连续 6 次正常；小程序需后续重新编译上传。

- 用户反馈健康榜大量分数集中在 90–100、多人 100，要求重新设计指标。核对发现健康榜直接复用分析页自然周综合健康指数；现有 100 分起扣模型只有单向超限惩罚，2 个记录日即可入榜，微量营养缺失会被排除出分母，导致少量或不完整记录也容易进入高分平台。当前仅完成诊断，推荐把排行榜收窄为“健康饮食榜”，采用饮食质量 60% + 周记录一致性 30% + 日间稳定性 10% 的周表现分，并将入榜门槛提高到至少 4 个有效日；待用户确认后实现。
- 用户随后授权读取当前共享数据库做匿名试算。2026-08-22 13:32 快照共有 1688 注册用户，本周 15 人产生 138 条饮食记录；现规则 12 人可入榜，4 人为 90–92 分（33.3%），本快照没有 100 分。严格 4 个有效覆盖日只剩 4 人、66.0–72.7，过度缩榜；推荐改为至少 4 个普通记录日且不完整日仍参与双向质量/稳定性扣分，7 人可入榜、53.1–72.7。上线前仍需用 8–12 个完整自然周回测校准，本轮未改业务代码或部署。
- 用户认为 53.1–72.7 相对现分数落差过大，并要求排行榜明确解释健康分如何得出。确认此前 60/30/10 方案把记录一致性权重设得过高，只作为压力测试，不直接上线。下一版方向改为可解释的 100 分积分制：热量适配25、三大营养25、食物质量/微量营养15、餐次10、记录连续性15、日间稳定性10；至少4个记录日才入榜，连续性按本周已过去天数计算，待完整周回测后定标。
- 继续试算发现直接六项线性版仍会把7名入榜用户压到64.1–84.6，原90分用户约77.6，仍不采用。改用渐进校准版：现有饮食质量×75% + 本周记录连续性15 + 日间稳定性10。当前7人结果64.7–89.0、均值81.9、最大绝对变化4.6；原92/91/90分别变为89.0/88.5/86.5。该版本既拉开高分又不造成断崖式变化，作为下一步8–12完整周回测候选；尚未改业务代码。
- 用户确认按校准版调整。后端现以75/15/10计算健康饮食周榜，稳定性采用记录日热量变异系数，至少4个记录日入榜；接口公开计分规则，只给本人返回三项拆解。小程序改名“健康饮食榜”并展示计分说明和本人拆分。Go全相关包、微信TypeScript/ESLint/SCSS及59套169条Jest通过；3001/9420/9421无开发者工具监听，未擅自启动，缺截图/交互证据。按当天统一提交约定尚未提交、推送或部署。
- 已按用户要求刷新并只快进拉取 `origin/dev`；本地与远端 0/0，返回 `Already up to date`，所有未提交改动均保留，无冲突。

- 用户完成注册后，手机号 `133****6407` 唯一匹配账号“微信用户_428643”（用户 ID `596d0f00-c29e-4236-a8cc-e414244cbbae`）。已确认无历史 paid 订单和旧会员记录，并通过 `admin_manual` 审计发放记录开通 `advanced_yearly` 最高档年卡：active、每日 40 积分、自动续费关闭，有效期 2026-08-22 11:34:04 至 2027-08-22 11:34:04（Asia/Shanghai）。事务提交后会员与发放记录回查成功；未改代码、未部署。

- 用户询问数据持续增长导致系统越来越卡如何解决。本轮仅做静态诊断，没有修改业务代码或数据库。
- 代码级主要扩展性风险：饮食记录缺少 `(user_id, record_time DESC)` 联合索引；饮水日志用 `DATE(recorded_on)` 过滤；部分健康历史查询缺少强制时间窗/分页；通知分页重复全量计数；offset 深分页成本随页码增长。
- 建议先建立带不同数据规模的可重复性能基线并查看 PostgreSQL `EXPLAIN (ANALYZE, BUFFERS)`，再分阶段做索引、范围查询、强制分页、游标分页、预聚合和前端窗口化；不建议先靠服务器扩容掩盖查询复杂度。
- 深入审查确认社区公开 Feed 是首要风险：请求前先 Pluck 全部公开用户 ID。临时嵌入式 PostgreSQL 测试中，请求 1 条 Feed、1000 个公开用户会在单条内部查询读取 1000 行；测试失败后临时文件已删除，现有功能回归仍通过。
- Feed 还会读取目标全部点赞/评论明细到 Go 后计数，深 offset 会扩大候选集，相关 limit 没有服务端硬上限；通知每页重复全历史计数，前端社区/通知列表持续累加并全量渲染。
- 体重/饮水摘要实际固定读取近两年，并非全历史；此前“健康接口无界”的初步判断已纠正。两年明细无论 week/month 都读取仍有过量查询问题。

- 用户要求按顺序修复上述数据增长问题。已完成本地代码：公开 Feed 改数据库子查询；点赞/评论数据库聚合与限量预览；所有相关列表参数硬限额；最新 Feed 稳定游标；匹配查询的联合/部分索引；通知首屏计数解耦；体重按日取最新、饮水按实际区间读取；社区与互动消息前端列表上限 200。
- 新增规模回归、游标、限额、聚合、日体重和前端有界列表测试。Go community/health/migration 相关包、微信 TypeScript、目标 ESLint、58 套/165 条 Jest 均通过。
- 未执行数据库迁移：`backend/config.yaml` 的实际目标库尚未确认。3001/9420/9421 无微信开发者工具自动化监听，无法取得截图/交互证据。遵守既有“当天改动仅本地、明确要求后再统一提交/推送”决定，未提交、未推送、未部署。
- 后续核验发现本地 Apollo `local` 配置实际指向远程 `154.8.205.78 / food-link / public`，且既有项目状态记录 main/dev/local 尚未隔离。为降低共享库风险，新增 `--only-growth-performance-indexes` 模式，仅并发、幂等创建本次 9 个性能索引，不运行全量 AutoMigrate、回填或种子；相关 Go 回归通过。等待用户明确确认该数据库目标后再实际执行。
# 2026-08-22 食物营养排行榜补图与去重

- 开发环境 14 个营养素排行榜共有 7 个唯一缺图 food_id：鸡胸肉 `a1000000-0000-4000-8000-000000000018`、虾 `f5a67d45-cca4-4c40-b237-5bc6a9573d83`、奇亚籽 `2ac57025-dd7f-432c-8b46-1c22d35ff706`、红薯 `c60c8948-cae1-4ae5-be52-dafeade930cc`、香菇 `ce6b98da-ca0b-4eeb-98a1-1f9cbfdfbf12`、黄瓜 `f07bcab6-9e7d-4bd1-af9b-a14ef16ef129`、杏仁 `fdc164d0-09b2-441e-bf53-70c9b87610ba`。
- 7 张图已用内置 imagegen 生成、缩至 512x512 JPEG、上传 food-images COS 并条件回填开发库；接口复查缺图为 0。
- 蛋白质榜按 chicken/shrimp 等食物族去重，避免“鸡胸肉/鸡肉”“虾仁/虾”重复占榜；保留底层标准库记录。
- 去重提交 `20c6dc49` 已推送 `origin/dev`，dev 镜像 digest `sha256:b75b5426c1042d0b0411e6174850e36d494d7f1af6f6f41b572bf12b25657cea` 已推送；约 10 分钟后公网接口仍是旧逻辑。SSH 公钥无权限且本机没有 kubectl context，运行时去重验证等待部署端更新。

# 2026-08-22 食物营养排行榜二次审查

- 14 张榜的单位、rank 和降序逻辑通过，但入榜候选口径发现三处明确错配：“虾仁 43.7”实际是虾米/海米，“牛肉 10”实际是牛肉水饺，“鸡蛋 15.7”是市售卤蛋。
- 本地已收紧基础食物候选，排除干制、复合菜、卤制市售、带骨带壳等不可比状态，并增加质量门槛；维生素 E 的白/黑芝麻和铁榜鸭血因来源定义不足暂不参榜。
- USDA 导入修复维 D IU/25-OH-D 与 mcg 混用风险，以及 total folate/DFE 混用风险；2 条 Python 单测与 community 全模块 Go 测试通过。审查报告位于 `docs/audits/food-nutrient-leaderboard-source-audit-2026-08-22.md`。
- 修正查询对真实数据库只读预演的 14 榜全部非升序；未修改数据库。按当天统一提交规则，本轮未提交、未推送、未部署。

# 2026-08-22 用户食物照片清洗与分类标注

- 已为 Admin 食物照片页实现非破坏性数据清洗和多标签标注：待清洗/保留/排除、结构化排除原因，以及零食、水果、外卖、家常菜、堂食、饮品、甜品烘焙、包装食品标签。
- 新增 `user_food_photo_annotations` 迁移 DO、管理员 PUT 标注接口、列表关联与筛选；数据库尚无标注表时，旧照片列表仍按待清洗兼容展示。
- Admin production build 和 Go admin/migration/app 相关回归通过；提交 `1152e24e` 已精确推送 `origin/dev`，未混入工作区其他改动。实际迁移和后端部署等待用户确认远程共享库 `154.8.205.78 / food-link / public` 是否为本次目标。
- 用户随后反馈 dev Admin 选择清洗状态后列表不筛选。公网未登录探针显示新增 PUT 标注路由仍为 404（新版本应被管理员中间件拦为 401），确认 dev Go 后端尚未部署，旧列表接口忽略新增查询参数；无需再修改前端筛选代码，下一步是确认数据库、迁移建表、部署后端并以 404→401 和筛选计数复验。
- 用户回复 OK，明确授权对 `154.8.205.78 / food-link / public` 执行本次建表并部署 dev 后端。为避免全量迁移夹带历史回填/种子，新增专用窄迁移命令，仅 AutoMigrate 并验证 `user_food_photo_annotations` 与 3 个索引；相关幂等测试通过，提交 `774f0c09` 已推送。
- 窄迁移成功完成。开发镜像 `sha256:c2afc78fd8cf7bad8a92223f417cbd9cffbf22c125d22ed56b0358b771773c84` 已推送 CCR；自动更新仍引用旧精确摘要后，通过 SSH 只更新 `foodlink-dev` 业务容器到新摘要，Deployment 3/3 滚动成功，Apollo sidecar 未改动。
- 运行时复验：新增 PUT 路由未登录响应由 404 变为 401；用户已登录 Chrome 的 `?annotation_status=excluded` 页面显示清洗状态“已排除”、共 0 张/展示 0 张、第 1/1 页，确认筛选已真正作用于后端查询。

# 2026-08-22 用户食物照片全量自动清洗

- 用户明确最终目标是按食物排行，并按零食、水果、外卖等类别查看；不适合排行的照片直接排除。已据此对开发数据库当前 10,353 张照片执行全量清洗，保留 7,726、排除 2,627、待清洗 0。
- 排除明细：duplicate 1,645、unusable 781、other 152、label_or_package_only 45、non_food 3、multi_dish_scene 1。全部为独立标注，不删除原图、饮食业务数据或 COS 对象。
- 保留样本的多标签统计：beverage 1,436、fruit 1,203、dessert 731、snack 628、restaurant 292、home_cooked 197、takeout 25；4,364 张普通食物不具备可靠场景信号，保留在总排行池但不硬分类。
- 新增确定性分类器和批处理 CLI。CLI 默认 dry-run，支持增量 apply 和保护人工标注的自动结果刷新；同一 source event 仅保留一张代表图。全量刷新期间因并发新增照片和 offset 漂移出现过同一身份重复，事务完整回滚后增加进程内 identity 去重，再次执行成功；随后增量补齐 4 张，15:04 复跑为 0 pending。
- Go classifier、CLI、Admin repo 相关测试和 pre-commit 门禁通过；代码以 `0f697d04` 推送 `origin/dev`。从干净提交构建并推送的 dev 镜像摘要为 `sha256:22851b5e4b1275585ccecb5fe0eb2a3fbfabf9c267707fa76aadbcc45dd70e38`。
- 手动精确滚动 Deployment 时 SSH 公钥被拒绝；开发 API 健康检查仍为 200，等待自动更新器。最终后台 Chrome DOM 读取超时，因此没有声称取得新的页面截图；数据库总量和幂等复跑作为最终数据证据。

# 2026-08-22 食物照片标签填写纠正

- 用户指出 Admin 卡片上的零食、水果、外卖等标签需要由清洗任务填写。审计数据库确认当前保留 7,726 张，其中 3,362 张已至少写入一个适用标签，4,364 张是现有八类均不适用的普通正餐，继续进入总排行池但不伪造来源标签。
- 截图中的旧页面未自动刷新：按最终统一规则，“干锅牛肉牛杂配米饭、花生及果汁”=`beverage/fruit/snack`，“低糖南瓜面包、鸭肫、海带、鱼皮花生”=`dessert/snack`。
- 发现 5 条历史后台测试记录因 `reviewed_by` 保护未被机器刷新；用户本轮明确要求统一填写后，临时允许本次全量重算覆盖这 5 条旧测试标签，默认代码仍继续保护人工审核。
- 又修正常见果汁标签：橙汁、苹果汁、葡萄汁、西瓜汁、甘蔗汁、梨汁、柠檬汁、芒果汁、椰子水明确属于 beverage，并增加“中式家常午餐+油炸花生米+橙汁”四标签回归。
- 15:49 最终全量刷新及并发新增增量补齐完成：10,354 张全部有状态，保留 7,727、排除 2,627、待清洗 0；beverage 增至 1,451，其余 fruit 1,203、dessert 731、snack 628、restaurant 292、home_cooked 197、takeout 25。
- 修复提交 `3bc243fd` 已推送 `origin/dev`；干净镜像摘要 `sha256:598929ca9a113bae12de4aae336529b17043d971815ad40b54c4f26794b4a410` 已推送 CCR。SSH publickey 仍被拒，等待自动更新器；实际标签数据已直接落库。

# 2026-08-22 Admin 食物照片筛选刷新修复

- 用户反馈筛选条件已选择但列表仍不能可靠更新；静态检查排除筛选依赖遗漏，确认列表请求缺少缓存禁用与并发响应顺序保护。
- 修复后列表 GET 使用 `cache: no-store`，新筛选会取消旧请求，且仅最后一次响应可更新列表和计数；新增 latest-request 乱序回归测试。
- `npm test`、Admin production build、pre-commit 通过；提交 `6753e9ca` 已推送 `origin/dev`，Cloudflare Pages 部署检查仍在进行。

# 2026-08-22 Admin 食物照片小号命中标签

- 卡片上的全部 8 类标签按钮已改为仅显示命中的小号徽标；无命中标签不显示占位。完整标签编辑继续保留在查看大图弹层。
- 新增标签筛选纯函数与回归测试；Admin 2 条测试、production build、pre-commit 通过。
- 提交 `5e5d344b` 已推送 `origin/dev`，Admin Cloudflare 部署成功；线上 JS 资源确认同时包含上一版 `no-store`/请求取消修复和本轮小号标签逻辑。

# 2026-08-22 食物照片标签合并与删除

- 用户决定“甜品烘焙”并入“零食”，同时移除“包装食品”；最终只保留零食、水果、外卖、家常菜、堂食、饮品六类。
- 前后端已统一新口径，并对旧值提供读取/筛选/保存兼容；新增窄标签规范化命令，不重跑分类或覆盖人工状态。
- 开发库 dry-run：10,354 条中 731 条需 `dessert→snack`，历史 `packaged_food` 为 0。实际 apply 因需用户明确确认目标开发库的批量写入而被安全门禁拦截，等待确认。
- 提交 `e9d78446` 已推送 `origin/dev`；Admin Cloudflare 部署成功，后端 dev 镜像摘要 `sha256:270587b511290b979fd0dc85f82bcdeaa8dc166792a5498ca5c3ade7a95bc3d4` 已推送。
# 2026-08-22 标准食物库缺图优先回填

- 只读基线为有效标准食物 9,885 条，已有图 8,923，缺图 962。回填队列改为按用户标准食物引用次数降序，并让 CLI 自动复用 Apollo DashScope 配置。
- 网络图坚持食物匹配 + 无水印 + 两轮 0.95 置信度门禁，首批 3 张通过；另用 imagegen 生成并上传 11 张高优先常用食物图。合计 14 条已回填，所有 CDN 地址返回 200，剩余长尾约 948 条待按同一门禁续跑。
- 工具与 11 张生成源图提交 `139dcec4` 已推送 dev；干净快照构建的 dev 镜像 digest 为 `sha256:a75b07f8580062e4a39bbbca7ef81cf7ec8e1d8ea9b8279537de56c5d14914cc`，公网 dev health 200。
- 用户反馈缺图仍多后，第二批按真实引用次数覆盖前 25 条：每条独立 imagegen、人工目检、512×512 项目内留档、COS 上传和数据库回填，25/25 CDN 200。已有图增至 8,962，缺图降至 923；剩余队列最高 `uses=0`，当前所有被用户饮食记录引用过的缺图项已清零。
- 回填工具新增 `--stats-missing-limit`，基线 JSON 可同时输出按引用次数排序的缺图明细，便于后续继续补未引用长尾。
- 第二批提交 `b3574615` 已推送 dev；后继并发提交 `dd46f085` 仍包含该父提交。干净快照镜像已推送，最终 dev 标签被后继镜像更新为 digest `sha256:da3f8f2fd3ca36b1ca803bdaed7645c9f17b31e902d9fe039333160d6be5968c`，公网健康 200。
- 对照用户截图补上启用项“意大利面（水煮，无盐）”；截图里其余可见缺图均为停用 `qwen_generated`，不进入用户端。意面图片 CDN 200，最终本轮 26 张生成图、已有图 8,963、缺图 922（中文 275、非中文 647），意面素材提交 `6ebbc32b` 已进入 dev。

# 2026-08-22 PR 双轴审查阻塞

- 用户要求整理 PR；以最新 `origin/dev=e9d78446` 为固定点完成 Standards/Spec 双轴审查。
- 发现阻塞项：性能索引迁移的 `CREATE INDEX CONCURRENTLY IF NOT EXISTS` 在中断留下无效同名索引后无法安全重跑，且 `SET search_path` 与后续执行未绑定同一连接；社区 Feed 稳定游标错误地用于刷新、加载更多仍走 offset。
- 另发现评论 SQL 聚合遗漏原有 `reply_to_user_id` 拉黑过滤。依据 `foodlink-pr-review` 门禁，本轮未提交、未推送、未创建 PR；修复后须重新审查并按健康榜、性能治理、营养榜、缺图统计独立拆分，排除 `memory/*`。

# 2026-08-22 宠物对话响应慢诊断

- 当天本地日志两次实测：prepare 331–342ms、模型 headers 313–317ms、首段正文 12.728–17.015s、总耗时 14.766–18.824s；慢点明确集中在模型开始输出正文前。
- 宠物请求使用 `qwen3.6-flash` 且未传 `enable_thinking:false`。官方说明 Qwen3.6 默认开启思考模式、会先思考再回复；当前 SSE 仅向用户展示 `delta.content`，所以思考期间一直显示等待动画，正文开始后约 1 秒即完成。
- 估价和发送会重复完整统计计算，但本次仅贡献约 0.3 秒准备耗时，不是主要瓶颈。未改代码；优先修复方向是关闭轻量宠物聊天的思考模式，再做首字延迟与质量验证。

# 2026-08-22 宠物对话模板感诊断

- 截图中反复出现的“一句话回应 / 结合记录看 / 明天一起试试”来自后端 `buildPetChatPrompt` 的显性示例，不是前端套壳。同一提示词还固定要求“回应→分析→明天建议”、250–450 字和多次使用“我们可以 / 一起试试”，因此模型稳定生成三段式报告。
- 最近 10 条对话会连同宠物的旧回复再注入提示词，使固定标题进一步自我强化。
- 推荐改为意图自适应的 1–3 个自然段：短问 80–180 字，数据分析 180–350 字，默认无标题；只在用户要计划或存在明确改进点时给行动建议，不强制每次以“明天试试”结尾。本轮未改代码。
- 用户随后要求直接修改。已落实意图自适应长度、默认无标题、仅在必要时给行动建议、禁止模仿历史回复格式，并改写 fallback 去除固定训练建议。相关 Go 包测试和 pre-commit 通过；独立提交 `49d671fc` 已推送 dev，干净镜像 digest `sha256:a7e2bdcd140268d64223eca03f6e07c4dbdde68399d2449a8276f5f17c0e1113` 已推送 CCR，公网 dev health 正常。

# 2026-08-22 宠物对话关闭深度思考

- 用户明确要求关闭。流式和非流式宠物请求均显式发送 `enable_thinking:false`，统计页深度 AI 解读不受影响。
- health、pet、app 回归及仅本任务补丁的干净克隆测试均通过；提交仅包含 2 个 health service 文件的独立补丁，排除工作区其他未提交改动。

# 2026-08-22 宠物对话深度思考开关

- 用户追加要求提供深度思考开关。小程序宠物聊天输入区新增开关，默认关闭，并提示开启后会更细致但回复更慢；生成中禁用开关，避免一次请求中途改变语义。
- `enable_thinking` 已贯通输入估价、普通生成、SSE 流式生成、后端 handler 和两类模型请求；未升级的旧客户端不传字段时继续按 Go bool 零值关闭。
- 微信目标测试、TypeScript、ESLint、SCSS、全量 Jest 和 Go health/pet/app 回归通过；开关提交 `381ceb54` 已推送 `origin/dev`，并从该提交的干净克隆再次通过 Go 回归。
- 从干净 `381ceb54` 构建并推送 dev 后端镜像，digest `sha256:3aed375ffa70c592dbd866014390239f13cf353a0b49dabb3c2e97e5c0ada33c`。公网开发 API 多轮返回 200，但 Pod revision 仍为旧 `foodlink-dev-6d4b4859b8-*`，自动滚动尚未完成，不能把健康检查等同于新代码已生效。
- 微信开发者工具自动化端口 3001/9420/9421 未监听，按约定未代用户启动常驻进程；前端运行时截图/交互验证和小程序上传仍需在开发者工具中完成。

# 2026-08-22 全体用户饮食记录榜

- 用户要求日常饮食记录次数榜使用全体用户，健康饮食榜继续仅限好友。
- 后端复用现有 checkin leaderboard 接口，改为聚合本周全体有记录用户并继续过滤拉黑关系；健康榜好友候选逻辑不变。
- 前端明确显示“全体记录 / 好友健康”及对应范围；Go 全相关回归、微信 TypeScript/ESLint/SCSS、60 套/174 条 Jest 通过。
- 3001/9420/9421 无自动化监听，未擅自启动开发者工具，暂无运行时截图。
- 本任务独立提交 `28d790b4` 已推送 `origin/dev`；推送时前序宠物头像提交 `d56ab52c` 已由其所有者先行进入远端，本次没有夹带额外提交。
- 干净快照 dev 后端镜像 digest `sha256:a52add878ae2553a2f9efac0f1fb387fed199841268affecc218b7cd7bb3085a` 已推送 CCR，开发 API health 为 HTTP 200；等待部署端自动拉取窗口。微信前端源码已推送但未上传体验版。

# 2026-08-22 审查问题修复与 Draft PR 拆分

- 修复此前双轴审查的全部 Blocker/High：并发索引无效状态可恢复且固定单连接、Feed 游标用于加载更多并保留 RFC3339Nano 精度、评论预览同时过滤被拉黑作者和回复目标、USDA 历史错误映射值按版本证据隔离、图片统计直接 Ping 固定连接。
- 按功能边界创建 4 个目标为 dev 的 Draft PR：性能 #97（`109a8654`）、健康榜 #98（`1ea75ca9`）、营养榜 #99（`61723e90`）、图片统计 #100（`0ab814fc`）。全部 MERGEABLE，Cloudflare Pages 检查通过，未合并、未部署。
- 健康榜 PR 与 dev 新增的全体用户饮食记录榜发生两处冲突；按双方语义解决为“饮食记录榜全体用户、健康饮食榜好友”，并保留校准计分说明。rebase 后 TypeScript、ESLint、59 套/175 条 Jest 和相关 Go 回归通过。
- 性能 PR 前端 60 套/173 条 Jest、community/health/migration Go 回归通过；营养 PR community repo、USDA migration 和 3 条 Python 导入测试通过；图片统计命令 Go 测试通过。所有提交均通过 pre-commit。
- PR 未包含 `memory/*`、宠物聊天并发改动或已进入 dev 的图片批量补齐文件；共享数据库迁移仍未执行。
