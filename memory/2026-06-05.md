# 2026-06-05

## 包装食物库召回 Benchmark 与优化

- 用户要求测试当前包装食物库匹配情况，并给出优化建议。
- 已新增 `backend/data/packaged_recall_benchmark.csv` 和只读命令 `backend/cmd/packaged-recall-benchmark`。
  - 命令通过当前 backend config 连接数据库。
  - 只调用 repo 层 `ResolvePackagedFood()` 和 `SearchPackagedFood(topK)`。
  - 不调用 LLM，不写库，不触发拍照识别任务。
  - 输出 JSON/CSV/中文 Markdown 到 `tmp/packaged_recall_benchmark_<timestamp>.*`。
- 首批样本 25 条：
  - 应命中 SKU：喜之郎 Cici、三得利无糖饮料、桃李豆沙小饼、雀巢咖啡、士力架、光明酸奶、TASTY ELF 小南瓜面包、乐事、卫龙等。
  - 不应直接确认 SKU：`面包`、`酸奶`、`咖啡`、`饮料`、`白米饭`、`煮玉米`。
- 优化前报告 `tmp/packaged_recall_benchmark_20260605_055635.*`：
  - `total=25`、`match_gold=15`、`no_match_gold=6`、`invalid_gold=4`。
  - `top1_accuracy=0.8000`、`top10_recall=0.8667`、`no_match_pass_rate=0.3333`。
  - `wrong_match=3`、`false_positive=4`、`near_miss=1`。
  - 主要问题：fuzzy fallback 没把输入里的品牌/口味证据一起搜；repo 层会把 `面包/酸奶/咖啡/饮料` 这类泛称直接确认成某个 SKU。
- 已做两处保守优化：
  - `ResolvePackagedFood` fuzzy fallback 改为把 `name + brand + spec + flavor + barcode` 合并搜索和打分。
  - 没有品牌/规格/口味/条码证据时，`面包/酸奶/咖啡/饮料/薯片/饼干/巧克力/奶茶/果汁/汽水/啤酒` 等泛称不再被包装 repo 直接确认成具体 SKU。
  - `packagedFoodSearchTerms` 补充 `果粒爽/原切/马铃薯片` 等关键词。
- 优化后最终报告 `tmp/packaged_recall_benchmark_20260605_060040.json` / `.csv` / `.md`：
  - `total=25`、`match_gold=18`、`no_match_gold=6`、`invalid_gold=1`。
  - `top1_accuracy=1.0000`、`top10_recall=1.0000`、`no_match_pass_rate=1.0000`。
  - `wrong_match=0`、`false_positive=0`、`near_miss=0`、`same_product_wrong_spec=0`。
  - 剩余 `invalid_gold=1` 为 `伊利巧乐兹低糖抹茶雪糕`，当前 active 包装库找不到这个具体低糖抹茶 SKU，属于库覆盖/金标复核问题。
- 验证：
  - `go test ./cmd/packaged-recall-benchmark -count=1` passed。
  - `go test ./internal/foodrecord/repo -run "TestPackagedFood(SearchTermsExtract|FuzzyQuery|SearchScore)|TestIsGenericPackagedResolveQuery" -count=1` passed。
  - `go run ./cmd/packaged-recall-benchmark --config-dir . --top-k 10` passed。
  - 相关 `git diff --check` passed，仅 LF/CRLF warning。
- 结论：
  - 首批样本下包装 repo 层表现已从“能暴露明显风险”优化到没有错 SKU/泛称误确认。
  - 但 25 条样本不能代表真实线上分布；后续应扩充脱敏真实 OCR/query 100-300 条，并加入同商品多规格、条码命中、OCR 净含量、大包装重量冲突样本。

## 标准营养库召回命中率 Benchmark v1

- 用户指出需要一个测试地方量化当前召回策略命中率，否则继续盲改 alias/fuzzy/LLM/embedding 很难判断效果。
- 已新增金标 CSV：`backend/data/nutrition_recall_benchmark.csv`，首批 170 条，覆盖米饭、玉米、鸡蛋、鸡胸、牛奶、西兰花、香蕉、苹果、燕麦、红薯、玉米肠等常见食物与风险样例。
- 已新增命令：`backend/cmd/nutrition-recall-benchmark`。
  - 只读当前配置数据库。
  - 只调用 repo 层 `ResolveFood()` 和 `SearchCandidates(topK)`。
  - 不调用 DeepSeek/LLM，不写 alias，不自动补库。
  - 输出 JSON/CSV 到 `tmp/nutrition_recall_benchmark_<timestamp>.*`。
- 最新 baseline：`tmp/nutrition_recall_benchmark_20260605_014212.json` / `.csv`。
  - `total=170`、`valid_gold=170`、`invalid_gold=0`。
  - `top1_accuracy=0.4529`、`top3_recall=0.4765`、`top5_recall=0.4882`、`top10_recall=0.4941`。
  - `wrong_match=44`、`near_miss=7`、`wrong_exact_alias=25`、`unresolved=49`。
  - 主要暴露：带计量/口语 query 大量 unresolved；部分 exact alias 指向更细 USDA/英文或细分条目，与当前人工金标目标不一致。
- 用户追问“170 条是不是都对、各层级怎么解读、benchmark 是否代表真实情况”，已明确：
  - `valid_gold=170/257` 只表示金标目标在库内有效，不表示召回正确。
  - 正确数应看 `passed` / `top1_accuracy`；失败拆成 `unresolved`、`wrong_match`、`wrong_exact_alias`、`near_miss`、`processed_mismatch`。
  - 人工 benchmark 适合做策略改动前后回归，不足以代表真实线上分布；要代表真实情况，需要继续加入脱敏真实 query 抽样并人工标注。
- 已升级 benchmark 报告和样本：
  - 金标扩展到 257 条，新增数量/单位、一碗/半根/一块、熟的/水煮/清炒、饭底/盒饭上下文、加工食品短语等更难样例。
  - 命令现在额外生成中文 Markdown 报告：`tmp/nutrition_recall_benchmark_<timestamp>.md`。
  - Markdown 会用中文指标解释含义，并按强别名错配、未召回、近失误、加工食品误命中、其它召回错输出样例和优先修复建议。
  - 最新难例 baseline：`tmp/nutrition_recall_benchmark_20260605_042227.json` / `.csv` / 中文 `.md`。
  - `total=257`、`valid_gold=257`、`invalid_gold=0`。
  - `top1_accuracy=0.3152`、`top3_recall=0.3385`、`top5_recall=0.3424`、`top10_recall=0.3463`。
  - `wrong_match=57`、`near_miss=8`、`wrong_exact_alias=33`、`unresolved=119`、`processed_mismatch=0`。
- 验证通过：
  - `go test ./cmd/nutrition-recall-benchmark -count=1`
  - `go test ./cmd/nutrition-alias-whitelist ./cmd/nutrition-recall-benchmark -count=1`
  - `go run ./cmd/nutrition-recall-benchmark --config-dir . --top-k 10`
  - 相关 `git diff --check`

## 标准营养库召回策略第一阶段落地

- 用户要求“落地，然后看看效果”。
- 已在 `backend/internal/foodrecord/repo/food_nutrition_repo.go` 落地第一阶段确定性策略：
  - `ResolveFood` 和 `SearchCandidates` 使用 `nutritionQueryVariants` 生成 query 变体。
  - 处理数量/单位/份量词：如 `鸡蛋1个`、`米饭100g`、`一碗米饭`。
  - 处理部分烹饪/状态词：如 `水煮`、`蒸`、`烤`、`去皮`。
  - 加入常见普通食物 canonical 偏好：米饭优先 `白米饭`，玉米/苞米优先 `玉米(熟)`，鸡胸优先 `禽肉（去皮鸡胸肉）`，牛奶优先 `牛奶(全脂)` 等。
  - exact alias 增加安全复核：中文 query 不被英文 USDA alias 强吃；普通食材仍不能命中加工肠/火腿/热狗类目标。
- 新增/补充测试：
  - `TestFoodNutritionRepo_ResolveFoodUsesNormalizedQueryVariants`
  - `TestFoodNutritionRepo_ResolveFoodSkipsEnglishAliasForChineseQuery`
  - `TestNutritionQueryVariants`
  - `TestIsUnsafeNutritionAliasMatch`
- 验证结果：
  - `go test ./internal/foodrecord/repo -run "TestIsUnsafeNutritionAliasMatch|TestNutritionQueryVariants" -count=1` passed。
  - `$env:CGO_ENABLED='1'; go test ./internal/foodrecord/repo ...` 因本机缺 `gcc` 无法跑 sqlite 集成；`CGO_ENABLED=0` 时 sqlite 是已知 stub。
  - `go test ./cmd/nutrition-alias-whitelist ./cmd/nutrition-recall-benchmark -count=1` passed。
  - `go run ./cmd/nutrition-recall-benchmark --config-dir . --top-k 10` passed。
- 最新效果报告：`tmp/nutrition_recall_benchmark_20260605_051233.json` / `.csv` / `.md`。
  - `total=257`、`valid_gold=257`、`passed=245`。
  - `top1_accuracy=0.9533`、`top3/top5/top10_recall=0.9533`。
  - `unresolved=7`、`wrong_match=5`、`wrong_exact_alias=0`、`processed_mismatch=0`。
  - 对比落地前中文 baseline `tmp/nutrition_recall_benchmark_20260605_042227.*`：`top1_accuracy 0.3152 -> 0.9533`、`unresolved 119 -> 7`、`wrong_exact_alias 33 -> 0`。
- 注意事项：
  - 这个提升是在当前人工 benchmark 上的结果，不能直接等价于真实线上分布。
  - 后续应加入脱敏真实 query 抽样和人工标注，作为第二套代表性 benchmark，防止规则过拟合当前 257 条人工难例。

## 标准营养库真实场景 Holdout Benchmark

- 用户要求“算法保持不变，换一个更接近真实场景的 benchmark 再测”。
- 已新增 `backend/data/nutrition_recall_realistic_holdout.csv`：
  - 127 条真实风格样本。
  - 覆盖蔬菜、肉类、水产、主食、饮品、坚果、复合菜、上下文短语、数量/单位/份量词。
  - 示例：`番茄炒蛋里的番茄`、`拍黄瓜`、`炒土豆丝`、`猪肉100g`、`鸡腿一个`、`面条一碗`、`豆浆一杯`、`盖饭底下的米饭`。
- 只改了报告展示以列出 `invalid_gold` 样例；召回算法保持上一轮落地后的版本不变。
- 运行命令：
  - `go run ./cmd/nutrition-recall-benchmark --config-dir . --input data/nutrition_recall_realistic_holdout.csv --top-k 10`
- 最新报告：
  - `tmp/nutrition_recall_benchmark_20260605_053154.json`
  - `tmp/nutrition_recall_benchmark_20260605_053154.csv`
  - `tmp/nutrition_recall_benchmark_20260605_053154.md`
- 结果：
  - `total=127`、`valid_gold=87`、`invalid_gold=40`。
  - `passed=49`、`top1_accuracy=0.5632`、`top10_recall=0.6322`。
  - `wrong_match=31`、`wrong_exact_alias=9`、`unresolved=7`、`near_miss=6`、`processed_mismatch=0`。
- 解读：
  - 这份 holdout 比原 257 条人工难例更接近真实，也更能暴露泛化问题。
  - `invalid_gold=40` 表明当前标准库 active canonical 覆盖或命名口径不足，包括 `西红柿`、`猪肉`、`鸡腿`、`鸡肉`、`面条`、`馒头`、`包子`、`面包`、`酸奶` 等常见目标。
  - 有效样本上 56.3% 说明上一轮 95.3% 不能直接当作真实线上表现。
  - 主要错误集中在 prepared dish vs ingredient 口径：例如 `拍黄瓜` 是否应归 `黄瓜`，`炒土豆丝` 是否应归 `土豆`，`煎豆腐` 是否应归 `豆腐`，需要产品上先定口径。

## 包装候选选择只针对同一商品多规格

- 用户反馈蓝山风味挂耳咖啡明显是第一候选，不应该让用户选择；用户澄清只有同一个食物不同规格才需要选择，例如薯片不同规格。
- 已修改后端多规格判断：
  - `packaged_needs_confirmation` 只在命中 SKU 的同商品候选存在不同净含量时触发。
  - 同商品判断优先 `product_family_key`，否则要求同品牌且同商品名。
  - 无关咖啡候选、同类不同商品不会再因为重量不同触发选择。
- 已修候选卡展示：
  - 后端候选图片用 `storage.ResolveReferenceURL("food-images", ...)` 转为可访问 CDN URL。
  - 前端只展示 `http/https/wxfile/cloud` 等可访问图片 URL，避免 COS key 造成空白图。
  - 候选没有营养字段时显示“营养信息待确认”，不再显示 `0 kcal/100g`。
- 验证：
  - `git diff --check -- backend/internal/analyze/service/analyze_service.go src/packageExtra/pages/result/index.tsx src/packageExtra/pages/result/index.scss src/packageExtra/pages/result/result-item-converter.ts` passed，仅 CRLF/LF warning。
  - `npx eslint src/packageExtra/pages/result/index.tsx --max-warnings 0` passed。
- 运行态仍需用户重启/部署后测试。

## 零食主动上传回归拍照识别主流程

- 用户重新确认：纯手动太麻烦，还是原来的拍照识别更靠谱；AI 自动提取信息，不够再让用户补充；关键字段没有时不应进入正式库。
- 已按新口径调整：
  - `packaged-food-edit` 默认进入拍照/相册上传，不再出现“拍照上传 / 手动填写”选择页。
  - 独立纯手动入口关闭；`mode=manual` 只作为识别后补录页，必须带识别任务、原图、已识别字段或补拍图片。
  - 直接打开补录页且没有补录上下文时，页面提示先拍照识别。
  - 识别缺字段时保留已识别内容和原图，让用户补齐后提交待审核。
  - 任务详情 blocked 文案改成“可补充”，并继续提供“补充信息”动作。
  - 补录提交仍写 `review_status=pending`，后端仍要求 `source_image_urls` 非空，避免无图或关键字段不完整的数据直接成为 active 包装库。
- 验证：
  - `npx eslint src/packageExtra/pages/packaged-food-edit/index.tsx src/packageExtra/pages/packaged-food-task-detail/index.tsx --max-warnings 0` passed。
  - `go test ./internal/foodrecord/handler -run "TestCreatePackagedFood|TestSubmitPackagedProductExtractTask" -count=1` passed。
  - `go test ./internal/foodrecord/service -run "TestCreatePackagedFoodRejectsMissingSourceImage|TestSubmitPackagedProductExtractTaskValidatesImageCount|TestEvaluatePackagedProductExtract" -count=1` passed。
  - 相关 `git diff --check` passed，仅 CRLF/LF warning。
  - `npm run typecheck -- --pretty false` 仍被仓库既有无关 TS 错误挡住；本次相关文件未出现在错误列表中。
  - weapp-devtools：已读技能说明，`mrc where --port 3001/9420/9430` 均连接失败，未能截图或交互验证。

## 包装库净含量与图片估重冲突保护

- 用户提供真实图和日志：视觉模型识别到 `光明黄桃大果粒酸奶` 约 135g、`低糖小南瓜面包` 约 50g、`巧克力味夹心点心` 约 25g；DB-first 后处理却把包装库 SKU 的 260g、1100g、130g 直接覆盖为本次重量。
- 结论：这不是视觉估重失败，而是包装库命中后过度信任 SKU 净含量。尤其 `TASTY ELF 低糖小南瓜面包 1100g` 明显是大包装/整箱 SKU，不应覆盖图中单个小面包。
- 已修改 `backend/internal/analyze/service/analyze_service.go`：缺少条码、OCR 净含量、用户规格等强证据时，如果包装库净含量明显大于视觉估重，输出 `package_match_status=matched_weight_conflict`，按视觉估重计入，`package_weight_source=ai_estimate`、`package_weight_applied=false`。
- 已新增窄测试 `TestPackagedExperimentWeightForItemKeepsVisualWeightWhenLibrarySpecIsMuchLarger`，验证 50g 小南瓜面包不会被 1100g SKU 覆盖。
- 验证：`go test ./internal/analyze/service -run "TestPackagedExperimentWeightForItemKeepsVisualWeightWhenLibrarySpecIsMuchLarger" -count=1` passed；相关 `git diff --check` passed，仅 LF/CRLF warning。

## 结果页编辑能力提示

- 用户指出识别结果页里餐食名称和营养素其实可以直接修改，但很多用户可能不知道，会直接点底部“识别有误？点击纠错”。
- 产品方向待定：结果页应更明确提示“小错误可直接改，系统性错误再纠错”，避免用户把纠错当作唯一修改入口。
- 用户进一步确认：右上角“修改/铅笔”应代表修改该食物全部属性，而不是点名称只改名称、点营养块只改营养块。
- 已实现：`src/packageExtra/pages/result/index.tsx` 的右上角编辑按钮改为打开完整食物编辑抽屉，可一次修改名称、估算重量、热量、蛋白质、碳水、脂肪、含水量；按钮旁增加“编辑”文字。
- 保存逻辑：只改重量时按比例缩放营养；用户同时修改营养时尊重用户输入；待确认包装规格项如果被用户手动编辑，会标记为用户确认来源。
- 验证：结果页 eslint passed，相关 `git diff --check` passed；typecheck 过滤本次文件无错误输出；weapp-devtools 仍因 `3001/9420/9430` 自动化端口连接失败无法截图/交互验证。

## 本地改动提交并同步主分支

- 用户要求先提交并 push 本地近期大量修改，同步到主分支；随后准备一段发到用户内测群的近期更新通知。
- 提交前确认当前分支为 `dev`，本地 `dev/main` 与 `origin/dev/origin/main` 基点一致为 `936b166 docs(memory): record branch sync`。
- 提交前清理了项目根目录 3 个调试截图：`auto-renew-audit-01-membership-top.png`、`auto-renew-current-before.png`、`auto-renew-login-blocked.png`。
- 本次同步口径：提交全部近期业务代码、测试、文档、状态文件和必要图片/测试资产；推送到 `origin dev` 与 `origin main`，完成主分支同步。
- 实际结果：
  - 本地近期改动提交为 `4682c51 feat: sync recent foodlink updates`。
  - 合并远端 `origin/dev` 标准食物图片/新手引导等 14 个提交后生成 `9fa8cd4 chore: merge latest dev updates`。
  - 已推送 `origin/dev` 与 `origin/main`，两者均指向 `9fa8cd423c0ae1b9613681549c45fc298a9a1f9a`。
  - 验证：`npx eslint src/packageExtra/pages/analyze/index.tsx src/packageExtra/pages/record-manual/index.tsx --max-warnings 0` passed；`go test ./cmd/standard-food-image-backfill ./internal/foodrecord/domain -count=1` passed；`git diff --cached --check` passed。
  - `go test ./internal/utility/repo ./internal/foodrecord/domain -count=1` 中 `internal/utility/repo` 仍因本机 `CGO_ENABLED=0` sqlite stub 失败，这是既有本地环境限制。

## 修复 merge 后 FoodNutritionDO 图片字段重复

- 用户运行 `npm run dev:backend` 时报错：`backend/internal/migration/do/schema_do.go` 中 `ImagePath` / `ImagePaths` redeclared。
- 根因：合并远端标准食物图片字段时，`FoodNutritionDO` 里同一字段被保留了两份；本地完整图片来源字段 `ImageSourceURL/ImageSourceLabel/ImageLicense` 附近已有一份正确的 `ImagePath/ImagePaths`。
- 修复：删除 `FoodNutritionDO` 前面重复的 `ImagePath/ImagePaths`，保留后面与图片来源字段同组的一份。
- 验证：`go test ./cmd/migration ./internal/migration/do -count=1` passed。
