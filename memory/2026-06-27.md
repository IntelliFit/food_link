# 2026-06-27

## 官网饮食记录分享页移动端二次优化

### 需求
- 用户提供手机浏览器截图，反馈饮食记录分享页元素间距奇怪，底部操作区过重。
- 要求底部只保留绿色「App 内打开」按钮，不要复制按钮，也不要外层白色框。

### 修改
- `website/src/pages/FoodRecordSharePage.tsx`
  - 移动端主内容收窄为 `max-w-[440px]`，顶部与卡片间距收敛。
  - 标题、描述、食物明细区的移动端字号、行高、内边距略缩小。
  - 移除复制按钮、复制 toast 和底部白色胶囊容器。
  - 底部悬浮区改为单个居中的绿色「App 内打开」按钮，保留原有 Android intent / iOS scheme 打开逻辑。

### 验证
- `cd website && npm run build` 通过。
- `cd website && npx eslint src/pages/FoodRecordSharePage.tsx` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 工作区换行提示。

## 官网分享页悬浮按钮微信内置浏览器居中修正

### 需求
- 用户反馈底部悬浮「App 内打开」按钮在 Via 浏览器正常，但微信内置浏览器里不居中；同时希望按钮整体往上移动约 20px。

### 原因
- 原实现中按钮自身使用 `mx-auto inline-flex w-full max-w-72` 居中。部分浏览器能正常处理，但微信内置浏览器/X5 内核对 `inline-flex` 和 auto margin 的组合表现不稳定，可能导致横向偏移。

### 修改
- `website/src/pages/FoodRecordSharePage.tsx`
  - 固定底部容器改为 `flex justify-center`，由外层负责稳定居中。
  - 按钮从 `inline-flex` 改为普通 `flex`，避免微信内置浏览器居中偏差。
  - 底部 padding 从 `1.05rem` 调整为 `2.3rem`，按钮整体约上移 20px，并继续兼容 `safe-area-inset-bottom`。

### 验证
- `cd website && npm run build` 通过。
- `cd website && npx eslint src/pages/FoodRecordSharePage.tsx` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 工作区换行提示。

## 官网分享页移动端满宽调整

### 需求
- 用户反馈分享内容中间框不应继续居中窄宽显示，移动端左右两侧不要留边距；顶部品牌/餐次按钮间距也显得过大。

### 修改
- `website/src/pages/FoodRecordSharePage.tsx`
  - 移动端主容器从 `max-w-[440px] px-4` 改为 `w-full px-0`，分享卡直接占满屏幕宽度。
  - 移动端分享卡取消圆角和左右边框，只保留上下边界；桌面端仍保留最大宽度、圆角和完整边框。
  - 顶部品牌/餐次行改为紧凑 `px-3 py-2`，logo 从 `size-7` 收为 `size-6`，减少顶部按钮区空隙。

### 验证
- `cd website && npm run build` 通过。
- `cd website && npx eslint src/pages/FoodRecordSharePage.tsx` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 工作区换行提示。

## 官网分享页营养指标留白压缩

### 需求
- 用户反馈营养指标里的 `g` 仍会超出边框，且指标区上下左右留白过大，让核心内容显得很窄。

### 修改
- `website/src/pages/FoodRecordSharePage.tsx`
  - 去掉营养指标网格外层浅色底和 padding。
  - 压缩指标卡片高度、左右内边距和数值/单位间距。
  - 收小数值和单位字号，避免 `kcal` / `g` 超出边框。
  - 移动端详情内容区左右 padding 从 `p-4` 收到 `px-3`，食物明细卡同步收窄左右 padding。

### 验证
- `cd website && npm run build` 通过。
- `cd website && npx eslint src/pages/FoodRecordSharePage.tsx` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 工作区换行提示。

### 后续
- 推送后等待官网部署，再用手机浏览器实测分享页按钮位置、间距和 App 拉起行为。

## 官网分享页营养指标布局修正

### 需求
- 用户反馈分享页营养指标区域的 `kcal`、`g` 字体和布局仍然奇怪，移动端只看到单位，数字被挤掉。

### 修改
- `website/src/pages/FoodRecordSharePage.tsx`
  - 营养指标区从移动端 4 列窄竖卡改为 2x2 横向卡片，桌面端仍保持 4 列。
  - `Metric` 组件改为左侧图标/指标名、右侧数值/单位的横向布局。
  - 移除数值上的 `truncate`，并让数值单位 `whitespace-nowrap`，避免窄屏下只显示 `kcal` / `g`。

### 验证
- `cd website && npm run build` 通过。
- `cd website && npx eslint src/pages/FoodRecordSharePage.tsx` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 工作区换行提示。
