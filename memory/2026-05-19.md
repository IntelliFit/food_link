# 2026-05-19

- 用户提出可借鉴“种树”类养成机制，思考是否给 `food_link` 做养宠物功能：用户吃的饭影响宠物身材、状态、情绪或发言；现有等级分存在但体现不明显，希望有更强的养成系和每日打开惊喜感。初步判断：宠物机制可作为用户健康状态的外化角色，把饮食、喝水、运动、积分、每日复盘和 AI 建议统一起来；但要避免羞辱式“吃错就宠物变胖/变丑”，应偏正向陪伴、能量、训练、成长、收藏和小任务。MVP 可先做“每日一句 + 状态变化 + 成长经验 + 小任务”，复杂动画和 AI 长对话暂缓。除宠物外，爆点候选包括每日健康盲盒、AI 饮食人格教练、连续复盘卡、可分享今日状态、好友共同养成/挑战、收藏餐食图鉴。

- 用户指出识别记录右上角“一键删除未记录”按钮和功能完全不需要，容易把本应保留等待记录的识别结果批量删掉。已移除识别记录页顶部入口和 `handleDiscardUnrecorded`，删除前端 `deleteUnrecordedAnalysisTasks` 封装；后端同步下线 `DELETE /api/analyze/tasks/unrecorded` 路由、handler/service/repo 批量删除逻辑和对应测试，只保留单条任务删除能力。验证：残留 `rg` 搜索无结果，`npx eslint src/packageExtra/pages/analyze-history/index.tsx src/utils/api.ts` 通过，`go test ./internal/analyze/handler ./internal/app -run '^$' -count=1` 和 `go test ./internal/analyze/handler -count=1` 通过，`git diff --check` 通过（仅 CRLF warning）。运行时验证受阻：`mrc where --port 9420`、`mrc where --port 3001` 均无法连接微信开发者工具自动化端口，未能截图/交互。

- 用户继续反馈：从“识别记录”里快速记录某个餐食后，列表状态会更新为“已记录”，但首页今日饮食记录不立刻出现，喝水量也没有变化。已按数据依赖和状态依赖排查：快速记录前端原本只改列表和饮食快照，首页事件又可能被缓存 TTL 短路；喝水量实际来自 body metrics summary，不会随 dashboard 快照自动更新；并且此前 500 可能已留下“饮食记录已创建但水分日志没写”的半成功记录，再次保存走 `already_saved=true` 时不会补水。已修复：快速记录成功后刷新当日云端 dashboard 快照并触发 `{ force: true }` 首页事件；首页事件监听支持 `date/force`，强制拉取 dashboard + body metrics；首页 useDidShow 判断本地快照更新时间，避免新快照被 TTL 挡住；后端 `FoodRecordService.Save()` 对已有 `source_task_id` 记录执行 `ensureExistingFoodWaterIntake()`，缺少 `ai_food_record:<record_id>` 水分日志时自动补写。验证：前端 eslint、app smoke、recipe service、foodrecord 水分纯逻辑测试、diff-check 均通过；foodrecord 幂等/回填 sqlite 单测被本机 `CGO_ENABLED=0` 和缺少 `gcc` 阻塞。注意：需重启当前后端进程，并让小程序前端使用最新编译产物后再复测。

- 用户反馈从分析结果页正常点击“记录”仍出现 `internal server error`。已定位根因：不是正常记录入口问题，而是保存饮食记录后新增的“食物含水量写入喝水统计”副作用失败。此前为避免误扣其它食物水分，把水日志来源从旧 `source_type=ai` 改为 `source_type=ai_food_record:<record_id>`，但数据库 `user_water_logs_source_type_check` 仍只允许 `manual/imported/ai`，咖啡这种带 `waterMl=900` 的记录保存后写水日志触发 check constraint，接口包装成 500。已修复代码：迁移约束允许 `^ai_food_record:[0-9a-fA-F-]{36}$`；`FoodRecordService.Save()` 和 `RecipeService.Use()` 中的食物水分入账改为派生副作用兜底，水日志写入失败只打印日志，不再让饮食记录保存失败。验证：`go test ./internal/app -run '^$' -count=1`、`go test ./internal/recipe/service -count=1`、`go test ./internal/foodrecord/service -run 'TestTotalFoodWaterIntakeMl' -count=1`、目标 `git diff --check` 均通过。用户确认后已从 `backend/` 执行 `go run ./cmd/migration -config-dir .`，返回 `migration completed: config_dir=. schema=public`；随后查询 `pg_constraint` 确认约束已允许 `ai_food_record:<uuid>`，并在事务中 probe 插入该 source_type 后回滚，确认数据库接受且未污染数据。注意：当前运行后端仍需要重启才会使用新代码。

- 用户反馈食物识别等待较久时，会先离开做别的事，之后回到“识别记录”列表点进已完成任务再点击记录，出现 `internal server error`；并指出识别前已经填过图片、餐次、运动时机等信息，历史列表应支持直接快速记录，点进结果页后也不应再次选择餐次。已修复：历史页读取任务 payload 时兼容 `meal_type/mealType`、`diet_goal/dietGoal`、`activity_timing/activityTiming`、`date/recorded_on/recordedOn`；更多菜单新增“快速记录到{餐次}”，对已完成未记录任务直接调用 `saveFoodRecord` 并带上 `source_task_id`；结果页跳转前也使用同一套兼容读取，避免丢餐次后弹选择框。后端 `FoodRecordService.Save()` 增加 `user_id + source_task_id` 幂等回查，重复保存返回同一记录并透出 `already_saved=true`；并发撞唯一约束时也回查返回，避免 500。验证：历史页 eslint 通过、目标 diff-check 通过；后端 sqlite 测试仍被本机 `CGO_ENABLED=0 / go-sqlite3 requires cgo` 阻塞；未跑 weapp-devtools，由用户手动验证。

- 用户提出：识别记录卡片右侧更多菜单里，“分享到公共食物库”不是当前最高频动作，更希望识别记录可以直接收藏到自己的餐食库/我的收藏里。已修改 `src/packageExtra/pages/analyze-history/index.tsx`：底部操作弹窗第一项新增“收藏到我的餐食”，原“分享到公共食物库”保留并下移；收藏入口复用 `createUserRecipe`，把识别结果 items、营养、水分、图片、描述、餐次保存为 `is_favorite=true` 的私人收藏餐食。`index.scss` 补收藏图标品牌色和暗色适配。按用户既有验证口径，本轮未运行构建、自动化或 weapp-devtools，由用户手动验证。

- 用户反馈：拍照识别冰淇淋后收藏为“我的餐食”，再复用该收藏生成当天饮食记录时，记录卡片标题总热量正常（截图 241.8 kcal），但展开食物明细显示 0 kcal、蛋白/碳水/脂肪也为 0。定位到复用入口 `POST /api/recipes/:recipe_id/use` 原先直接透传 `user_recipes.items` 泛 JSON；历史收藏 item 字段形态不标准时，`user_food_records.items` 强类型读取会丢营养。已修复后端：复用前规范化收藏 item，兼容 `calorie/kcal/carbohydrates/waterMl/estimatedWeightGrams` 等历史别名，并对单 item 收藏用收藏总营养补齐缺失明细。验证：`go test ./internal/recipe/service -count=1`、`go test ./internal/app -run '^$' -count=1`、目标文件 diff-check 均通过；未跑小程序 UI，按用户验证口径由用户手动验证。

- 用户复测混合零食图片，实际食物为泰国龙宫果、鹅胗、饼干和糕点；普通图片 hybrid 在 Gemini 成功调用后仍识别成新鲜无花果、牛肉馅酥脆饼干、阿胶糕、玫瑰造型点心。判断核心问题不是 Gemini 没调用，而是 Gemini prompt 可能被 Doubao 初始候选锚定，且日志缺少 OCR/识别证据，不利于判断错误来自视觉、OCR、候选仲裁还是营养库。

- 用户询问火山方舟 Chat API 文档是否支持 websearch。当前贴出的 Chat API 页面只看到通用 `tools/tool_choice` 字段，没有明确出现 `web_search`、`browsing`、`google_search` 这类可直接开启联网搜索的稳定参数；另查到火山方舟文档目录中存在 `Web Search（联网内容插件）`，但它更像插件/工具能力，需要单独确认接入协议、模型支持范围和请求格式。本轮没有向 Doubao 请求体加入未知内置 websearch 字段，避免上游 400 或静默无效。

- 已实现普通图片模式的试验版联网增强：后端在 Gemini 复核前基于用户补充文本、Doubao 描述、候选食物名和 alternativeNames 生成最多 3 个搜索 query，通过 DuckDuckGo HTML 搜索拿标题/摘要/URL，作为 `webSearchEvidence` 塞进 Gemini prompt。搜索结果只作为外部佐证，prompt 明确要求以图片、包装文字、OCR 和用户补充事实为准。

- 同步强化 Gemini 复核 prompt：必须先独立观察图片和 OCR 信息，再参考 Doubao 候选；小众水果、进口零食、包装食品、候选冲突项要显式比较 alternativeNames，不能无证据沿用 Doubao 单一候选。`hybrid_review` 新增 `web_search_status`、`web_search_queries`、`web_search_result_count`，并把 Gemini 返回的 `ocrText` 截断写入 `ocr_text` 便于线上排查。

- 验证通过：`go test ./internal/analyze/service -run 'TestAnalyzeService_AnalyzeImageStandard(HybridUsesGeminiWeightReview|HybridFallsBackToDoubaoWhenGeminiFails|IgnoresConfiguredGeminiProvider|UsesDoubaoForExplicitDoubao|ForcesDoubaoInsteadOfGemini)$|TestAnalyzeService_AnalyzeImageGeminiAliasRoutesToDoubao|TestBuildStandardImageHybridReviewPromptIncludesIndependentReviewAndSearchEvidence|TestParseDuckDuckGoHTMLResults|TestBuildStandardImageSearchQueries|TestBuildDBFirstPromptIncludesCorrectionContext' -count=1`、`go test ./internal/app -run '^$' -count=1`、目标文件 `git diff --check` 均通过；diff-check 只有 CRLF warning。

- 用户再次复测同一混合零食图片，完整日志显示：DuckDuckGo web search 连续 3 个 query 全部超时，因此 `hybrid_review.web_search_status=skipped`，本次没有真正使用到网页证据；这 3 次超时额外拖慢约 18 秒。Gemini hybrid 成功执行并 OCR 到 `Member's Mark`、`牛肉馅酥脆饼干`、`阿胶`，输出 4 个 item，其中水果从“无花果/无花果干”改成“龙功果”（接近但错字，应归一到“龙宫果/龙贡果/longkong”），但仍把鹅胗包装误判为“阿胶糕”。判断：websearch 当前调用时机太早且搜索源不可用，query 又主要来自 Doubao 的泛描述/错误候选，无法帮助推翻错误；下一步应改为先独立 OCR/视觉观察，再用 OCR/用户补充词做搜索和最终仲裁，或先禁用/强限时 websearch，避免拖慢普通模式。

- 用户澄清图片里没有“阿胶”两个字，只有“鹅胗”，但“鹅胗”字体是倒着的；因此 Gemini 的 `ocr_text:["阿胶"]` 是旋转/倒置文字 OCR 误读。已补普通图片 hybrid prompt：OCR 必须检查横向、竖排、倒置、旋转、遮挡和反光文字；疑似倒置/旋转文字要 mentally rotate 后重读，尤其区分“鹅胗/鹅肫/鹅珍”和“阿胶”；低置信度 OCR 片段不能直接当食物名，包装食品最终名称必须同时满足 OCR、包装图案、品牌/品类和可见食物逻辑一致；若 OCR 与视觉/包装图案冲突，需写入 `recognitionEvidence` 并把可能误读放入 `alternativeNames`。相关 prompt 测试和 analyze/app smoke 已通过。

- 用户希望下一次能判断“鹅胗”到底是 Doubao 首轮错了，还是 Gemini 复核改错了。已在普通图片 `hybrid_review` 中增加对照日志字段：`base_description` 和 `base_items` 表示 Doubao 首轮结果，`review_items` 表示 Gemini 复核原始 items，`final_items` 表示最终进入 db_first 的 items；每个 item 包含 name、weight_g、water_ml、confidence、recognition_evidence、weight_evidence、alternative_names 的紧凑调试字段。这样下次日志可直接对比三阶段结果，不再靠 web search query 反推。相关定向测试、analyze/app smoke 和 diff-check 已通过。

- 用户希望新增一个比当前标准模式更轻量的模式，主要测试“豆包 + 官方 websearch”。用户提供火山方舟官方 Web Search 文档，确认官方能力走 `Responses API`：`POST /api/v3/responses`，工具参数为 `tools:[{"type":"web_search","max_keyword":...}]`，支持图文输入和 VLM 判断是否搜索。已实现 `execution_mode=lite` 轻量图片模式：单次 Doubao Responses + 官方 Web Search，跳过 Gemini hybrid 和自建 DuckDuckGo 搜索，之后仍走 `db_first` 营养库回算。`hybrid_review.strategy` 为 `doubao_responses_web_search_db_first`，并记录 Responses usage / web_search 元数据。前端类型和分析页入口也已支持“轻量”模式；等待页、历史页、结果页可识别 `lite` 标签。

- 轻量模式验证：analyze service 定向测试（含 `TestDoubaoClient_AnalyzeWithImagesWebSearch_Success` 和 `TestAnalyzeService_AnalyzeImageLiteUsesDoubaoResponsesWebSearch`）、membership credit 定向测试、`go test ./internal/app -run '^$' -count=1`、相关前端 eslint 均通过。已按项目要求尝试 weapp-devtools：`mrc where --port 3001` 和 `mrc where --port 9420` 均因微信开发者工具自动化目标未连接失败，无法截图/交互验证。

- 用户要求拉取最新代码并本地合并。已在 `dev` 上先 stash 本地未提交改动，再 `git fetch origin` 并 fast-forward 到 `origin/dev` 最新提交 `35d3037`，随后 `git stash pop` 恢复本地改动。恢复时 `CURRENT_TASK.md`、`DECISIONS.md`、`memory/2026-05-18.md` 出现文档冲突，已保留远端 OTel/营养补全等新增记录和本地普通图片 hybrid/websearch 记录并清理冲突标记；本地代码改动仍在工作区，未提交未推送。

- 用户询问 `35d3037` 为什么删除 Doubao `reasoning_effort`，并认为这是奇怪的粗粒度修改，要求撤销。已在 `backend/internal/analyze/service/llm_client.go` 恢复食物分析 Doubao 请求体里的 `reasoning_effort: "minimal"`；后续如果多图请求仍因 Ark 参数组合返回 400，应按模型/图片数量/错误码做条件化兼容，而不是全局移除。

- 用户讨论喝水统计口径：拍照/文字分析食物会得到食物含水量，短期希望和手动喝水统一进入总喝水量，不先拆两套统计。代码检查结果：当前后端保存饮食记录时已通过 `FoodRecordService.recordFoodWaterIntake()` 折算 `items[].water_ml/waterMl` 并写入 `user_water_logs`，来源 `source_type=ai`；首页喝水卡读取身体指标摘要中的水日志汇总，所以食物含水量和手动喝水已统一计入总量。更新/删除饮食记录时也会按 `source_type=ai` 做差额调整。已把“短期统一总量，未来可按 source_type 拆来源”的产品口径写入 `DECISIONS.md` 和 `CURRENT_TASK.md`。

- 用户进一步复测确认：拍照后直接记录两杯咖啡会把 900ml 加入首页喝水量，但通过「我的收藏餐食」复用冰淇淋不会把 72ml 加入。定位根因：收藏复用入口 `RecipeService.Use()` 直接 `InsertFoodRecord()` 写入 `user_food_records`，绕过了 `FoodRecordService.Save()` 的食物水分入账逻辑。随后用户指出水量从 900ml 变成 828ml，等于把冰淇淋 72ml 从咖啡水分里扣掉；继续定位发现旧实现用公共 `source_type=ai` 标记所有食物水分，删除/更新未入账的收藏餐食记录时会从同日其它 `ai` 日志里扣减。已修复：`RecipeService` 增加 `WaterLogRecorder`，`app.New()` 注入 `bodyMetricsRepo`；复用收藏餐食生成饮食记录后按规范化后的 `water_ml/waterMl`、`ratio`、`intake/weight` 折算水分并写入 `user_water_logs`；食物水分来源从公共 `ai` 改为 `ai_food_record:<record_id>`，编辑/删除只调整同 record id 的水分日志，避免误扣其它饮品。验证：`go test ./internal/recipe/service -count=1`、`go test ./internal/app -run '^$' -count=1`、`go test ./internal/foodrecord/service -run 'TestTotalFoodWaterIntakeMl' -count=1` 通过；foodrecord service 涉 sqlite 的回归测试因本机 CGO/gcc 环境阻塞；未跑小程序 UI，按用户验证口径由用户手动验证。

- 用户指出 `CURRENT_TASK.md` / `DECISIONS.md` 应记录项目开发信息，但当前存在乱码和日期顺序混乱。已整理文档：`DECISIONS.md` 统一为单一标题、按日期倒序排列且检测无乱码；`CURRENT_TASK.md` 统一标题和近期任务倒序，中部不可可靠恢复的 GBK/UTF-8 串码历史块已移除，改为文末 `memory/YYYY-MM-DD.md` 历史日记索引。新增维护口径：状态文档使用 UTF-8；`CURRENT_TASK.md` 记录近期任务与交接，`DECISIONS.md` 记录长期稳定选择，完整流水继续写每日 memory。

- 用户要求把首页“目标设置”改成按日期覆盖：如果用户手动设置某一天摄入目标，那一天不应再被动态模型改动。已新增 `user_daily_nutrition_targets` 表模型和 repo，`PUT /api/user/dashboard-targets` 支持 `target_date` 后按 `user_id + target_date` upsert；首页 dashboard 优先读取当天自定义目标，来源返回 `daily_manual`，其次才是旧全局手动目标和动态模型。前端首页保存目标时传当前选中日期，标签显示“当天自定义”，成功提示改为“当天目标已更新”。饮食记录海报热量对比也接入当天覆盖目标。验证：home service 日期覆盖纯逻辑测试、user service 编译 smoke、app 编译 smoke、首页 eslint、目标 diff-check 通过；涉及 sqlite 的运行型测试仍受本机 `CGO_ENABLED=0` 阻塞；未做小程序运行时验证，按用户口径由用户手动验证。注意：新增表需要后续运行后端迁移命令。

- 用户随后明确要求直接执行数据库迁移。已在 `backend/` 目录执行 `go run ./cmd/migration -config-dir .`，终端返回 `migration completed: config_dir=. schema=public`。这意味着当前 `backend/config.yaml` 指向的远程 PostgreSQL 目标库已实际跑过迁移；此前“尚未迁移”的口径已过时，后续判断首页问题时不能再把“没跑 migration”当成现状。

- 用户反馈首页突然 toast 获取首页数据失败，且摄入热量看起来没有更新。排查 Gin 日志确认只有 GET /api/home/dashboard 返回 500，其它首页并发接口均 200；截图里剩余 1075 与 708/1783 匹配，说明前端在 dashboard 失败后继续展示上一份本地/旧快照。根因高概率是当天目标覆盖功能新增的 user_daily_nutrition_targets 表尚未在当前目标库完成迁移，HomeDashboard 查询该表时遇到 undefined_table 并让整个首页接口失败。已在 backend/internal/home/repo/home_repo.go 给 GetDailyNutritionTarget 增加缺表兜底：遇到 SQLSTATE 42P01 / relation does not exist / sqlite no such table 时当作当天无自定义目标，继续走系统动态目标，避免首页 500；新增 repo 纯单测覆盖。验证：go test ./internal/home/repo -count=1、go test ./internal/home/service -run 'TestDashboardTargets|TestBuildNutritionTargetPlan' -count=1、go test ./internal/home/repo ./internal/home/service ./internal/app -run '^$' -count=1、目标 diff-check 均通过；sqlite dashboard 运行型测试仍被本机 CGO_ENABLED=0 阻塞。后续需重启当前 Go 后端，并确认/运行 backend 迁移命令正式创建 user_daily_nutrition_targets。

- 进一步只读核验当前 backend/config.yaml 指向的 PostgreSQL（154.8.205.78 / food-link / public）：通过 Go 临时只读查询 select to_regclass('public.user_daily_nutrition_targets')::text，结果 table_exists=false。结论：当天目标覆盖功能新增的 user_daily_nutrition_targets 当前尚未正式迁移到该目标库；已有代码兜底只能避免首页 dashboard 因缺表 500，不能替代正式迁移。要让当天自定义目标完整可写可读，仍需用户确认目标库后从 backend/ 执行 go run ./cmd/migration -config-dir .，并重启当前 Go 后端。
