# 2026-06-06

## kk 蛋白粉低蛋白异常排查与修复

- 用户反馈截图中 50g 怡可纳草莓味酵母蛋白粉显示蛋白质 `1.8g`、热量 `37kcal`，怀疑 DeepSeek 生成错误。
- 只读查线上库定位到 `kk` 的任务：`924e9043-093a-4390-9f6e-1ae2b327ef33`，创建时间 `2026-06-05 19:15:48`（Asia/Shanghai），输入 `50g怡可纳草莓味酵母蛋白粉`。
- 根因不是 DeepSeek：item 是 `nutrition_source=packaged_food_library`、`resolve_status=fuzzy`，误命中 `光明 如实 关山樱花+草莓风味发酵乳 250g`，按酸奶 `3.6g protein/100g` 缩放到 50g 得到 `1.8g`。
- 已修复 `backend/internal/foodrecord/repo/food_nutrition_repo.go`：蛋白粉类查询只允许同类包装候选自动命中，避免“草莓味”等口味词把酸奶/发酵乳候选顶上来；品类判断只看商品身份字段，不看 OCR/配料/search_text。
- 验证：当前代码下 `ResolvePackagedFood("怡可纳草莓味酵母蛋白粉")` 返回 `unresolved`；repo 纯函数测试、包装 benchmark 单测和真实库包装召回 benchmark 均通过，首批 25 条包装 benchmark 仍为 `top1_accuracy=1.0000`、`false_positive=0`。
- 未自动重启/部署后端；运行态需要用户后续重启或部署才生效。本轮无前端 UI 改动，未做 weapp-devtools 验证。

## 包装食品命中算法改为证据锚点门槛

- 用户进一步指出：不能单纯为 `蛋白粉` 做特例，问题是“包装袋/SKU 命中算法”整体过度相信 fuzzy 弱相似。
- 已把修复扩展为通用 resolver 门槛：包装库 fuzzy 自动确认必须有 SKU 证据锚点，弱口味/泛品类只能参与排序，不能单独确认。
- 新增/调整的门槛包括：明确品类冲突过滤、品牌/规格/核心商品词锚点、弱词计数限制、品牌别名解析（如 `乐事（Lay's）` 可被 `乐事` 锚定）、候选池扩大以避免正确 SKU 被泛词挤掉。
- 真实库只读 spot check：`怡可纳草莓味酵母蛋白粉` 现在包装库 resolve 为 `unresolved`，包装搜索候选为 0。
- 复跑包装 benchmark：`total=25`、`top1_accuracy=1.0000`、`top10_recall=1.0000`、`false_positive=0`，乐事番茄样本已恢复正确召回。
- repo 纯函数测试 passed；sqlite 集成测试在本机 `CGO_ENABLED=0` 下仍被 go-sqlite3 stub 阻塞，测试代码已保留给 CI/带 CGO 环境。

## 包装库真实任务审计

- 用户追问是否有真实 benchmark、能否确保后续不再出现同类误命中。
- 已明确：现有 `packaged_recall_benchmark.csv` 是人工金标回归集，包含真实库 SKU 和事故样例，但不是完整线上随机抽样；没有人工标注不能称为真实正确率 benchmark。
- 新增只读命令 `backend/cmd/packaged-real-task-audit`，扫描真实 `analysis_tasks` 中历史 `packaged_food_library` item，用当前 resolver 复算并输出 JSON/CSV。
- 审计命令默认用 item name 复算，原始 `text_input` 单独记录，避免多 item 文本污染单个包装 item 的 resolver。
- 运行 `go run ./cmd/packaged-real-task-audit --config-dir . --days 14 --limit 500`，报告 `tmp/packaged_real_task_audit_20260606_045150.json/.csv`。
- 结果：45 个真实任务、53 个历史包装 item；新规则 `same_match=34`、`now_unresolved=19`、`changed_match=0`。这说明新规则把一批弱 fuzzy 历史命中降级为未确认，没有把它们改错到另一个 SKU。
- 事故中的两个真实输入变体都被新规则拦下：`50g怡可纳草莓味酵母蛋白粉` 和 `50怡可纳草莓味酵母蛋白粉` 旧命中酸奶，新规则均 unresolved。

## kk 蛋白粉历史错误结果回填

- 用户要求把已写入历史任务的错误数据改回来。
- 已定位两条错误 `analysis_tasks`：`e6e32056-6729-4900-b335-721f9123674c`（`50怡可纳草莓味酵母蛋白粉`）和 `924e9043-093a-4390-9f6e-1ae2b327ef33`（`50g怡可纳草莓味酵母蛋白粉`）。
- 查询 `user_food_records` 确认没有保存成正式饮食记录，因此只修复 `analysis_tasks.result`。
- 已将两条结果从误命中酸奶包装 SKU `3ac1cbf8-f27b-4f47-a684-e7992a30d6b0` 改为标准营养库 `酵母蛋白粉`（`d8243154-214d-4e1a-bd1a-c6ac827ba793`）。
- 50g 回填营养：热量 `193.20kcal`、蛋白质 `38.77g`、碳水 `3.16g`、脂肪 `2.71g`；清除了包装库候选、包装命中和包装重量字段。
- 旧 JSON 备份在 `tmp/kk_protein_powder_bad_result_backup_20260606_080616.json`；复查两条任务 `packaged_food_id` 均为空，`packaged_food_resolution.matched_count=0`。

## 校园食堂学校选择验证

### 验证与修复

- 使用微信开发者工具 CLI 启动自动化，`mrc where --port 9420` 可连接，当前页可重启到 `packageExtra/pages/campus-canteen/index`。
- `mrc screenshot` 在当前 Windows 环境连接成功但保存阶段卡住，没有落盘截图。
- 校园食堂页首屏存在 `.campus-hero-title`，当前页路径正确，小程序错误日志为 0。
- 初次打开选择大学弹层时，后端 `/api/location`、`/api/schools/provinces`、`/api/schools` 都返回 200，但前端省份/大学项为空。
- 根因是 `src/utils/api.ts` 中学校/定位 API 对 `unwrapResponse` 做了二次 `.data` 解包。`unwrapResponse` 已经返回后端信封里的 `data`。
- 已修复 `searchSchools`、`getSchoolProvinces`、`getUserLocation` 的解包逻辑。
- 修复后复验：选择大学弹层中 `.school-picker-locate-tip`、`.school-picker-province-tag`、`.school-picker-item` 均存在，`.school-picker-empty` 不存在。

### 注意

- `/api/schools`、`/api/schools/provinces`、`/api/location` 当前需要 JWT。小程序登录态会带 token；直接 curl 未带 token 会返回 401。
- `mrc click .school-picker-province-tag` 在当前会话里曾卡住，未完成完整点击切换截图验证；但接口日志显示后续 `/api/schools` 200 且大学列表已经渲染。

## 饭饭午餐酸奶和热狗肠 0 卡修复

- 用户提供 2026-06-06 午餐分享图：饭饭记录中每日坚果 25g 正常为 139 kcal，但 `如实酸奶` 135g 和 `双汇玉米热狗肠` 40g 均显示 0 kcal。
- 按 `jinhui-stack-debug` 的数据依赖路径先查后端数据，定位正式饮食记录 `316ccf20-a4e8-4373-af30-9dbf00cac91c`，用户 `饭饭`（`6f0b67e5-f3b1-4b38-b3fc-081d931d8ef1`），餐次 `lunch`，关联任务 `d16b6e15-3183-4910-9507-5f1be361c653`。
- 根因：记录已保存为错误 JSON。`如实酸奶` 为 `nutrition_source=unresolved` 且核心营养全 0；`双汇玉米热狗肠` 为 `candidates_only` 后未应用包装库候选营养。
- 新增 `backend/cmd/food-zero-nutrition-repair`：支持按用户/日期/餐次或 record_id 诊断；`--repair` dry-run，`--apply` 写库；写库前备份旧 JSON。
- 优化 `backend/internal/foodrecord/repo/food_nutrition_repo.go`：标准营养库 query normalizer 对“品牌/商品名 + 酸奶/酸牛奶/发酵乳”安全兜底到 `酸奶`，并排除软糖、溶豆、奶昔、饮料、蛋糕、冰淇淋、雪糕等非普通酸奶。
- 已执行 `go run ./cmd/food-zero-nutrition-repair --config-dir . --record-id 316ccf20-a4e8-4373-af30-9dbf00cac91c --repair --apply`：
  - `如实酸奶` 135g 回填为标准库 `无糖酸奶`（`be6ae888-ff6f-4f77-a418-ff590aa97515`）：`83.70 kcal / P 4.72g / C 6.35g / F 4.05g`。
  - `双汇玉米热狗肠` 40g 回填为包装库 SKU `311b59ef-05a0-427e-955f-3a4c3b05022b`：`81.17 kcal / P 4.40g / C 6.00g / F 4.40g`。
  - 整餐合计从 `138.8 kcal / P 2.83g / C 10g / F 9.8g` 更新到 `303.67 kcal / P 11.95g / C 22.35g / F 18.25g`，并同步关联 `analysis_tasks.result`。
- 旧记录备份：`tmp/food_zero_nutrition_backup_316ccf20-a4e8-4373-af30-9dbf00cac91c_20260606_194548.json`。
- 复查同一命令 dry-run 显示 `changed=false`、`skipped_reason=no_target_zero_items`，合计稳定为 `303.67 kcal`。
- 验证：`go test ./cmd/food-zero-nutrition-repair -count=1` passed；`go test ./internal/foodrecord/repo -run "TestNutritionQueryVariants|TestIsUnsafeNutritionAliasMatch|TestIsGenericPackagedResolveQuery" -count=1` passed；DB 依赖型 repo 测试本机被 `CGO_ENABLED=0` / 缺 gcc 阻塞，仅保留给 CI/带 CGO 环境。
- 本轮无前端页面/组件/样式/路由/交互改动，未触发 weapp-devtools 验证；新 normalizer 需要后端重启或部署后才会影响后续新识别。

## 食物记录主链路流程图图片版

- 用户要求把食物记录路径、具体算法流程、benchmark/优化环节直接生成一张图片。
- 已生成高清 PNG：`docs/food-record-flowchart.png`。
- 配套 Mermaid 文档仍保留：`docs/food-record-flowchart.md`。
- 图片采用 1-6 步大模块：用户发起记录、输入处理、AI/文本解析、DB-first 营养算法、结果页确认、保存与展示；底部集中列出 benchmark/优化/数据治理环节，并突出“保存前 0 营养门禁”。
- 用户进一步确认更想要 Mermaid 图本身；已补纯 Mermaid 文件 `docs/food-record-flowchart.mmd`。
- 明确算法口径：包装袋/SKU 全没命中时不能直接结束或保存 0；应先回落标准营养库，标准库也未命中再走 DeepSeek/LLM 兜底，兜底失败也必须被保存前 0 营养门禁拦住。
