# 2026-07-11

- 用户咨询新人在另一台电脑开发无法访问数据的接入方式。已确认小程序 `develop` 环境默认指向本机 `127.0.0.1:3010`，共享开发 API 为 `https://dev.api.healthymax.cn`。建议普通联调改用开发 API，而非开放生产服务器/数据库；后端开发另设隔离开发库与最小权限访问。
- 补充核对：本机后端 `CONFIG_SOURCE=local` 且本地运行配置指向远程 PostgreSQL，因此能直接读写远程数据；数据库 TCP 凭据与 SSH shell 登录独立。当前以现有密钥测试 `root@coachlink.fit` 返回 `Permission denied (publickey)`，说明此机器没有已授权的 root SSH 身份。
- 用户确认新成员需要改后端。后续接入应是独立 SSH 公钥账号配合 Apollo dev/隔离开发数据库，而非复制当前生产级数据库/运行密钥。
- 已确认本机通过 SSH config 的 `ssh 154.8.205.78` 以 `ubuntu` 用户成功访问主机 `VM-24-14-ubuntu`；该账号有免密全 sudo，可创建新人独立 SSH 账号。`root@coachlink.fit` 的 publickey 拒绝只说明未配置 root 身份。
- 运行态实测：本机 3010 正运行 Go `server.exe`，health 200，local app config 指向远程 PostgreSQL 且 worker.count=1；dev/main API 均健康，K8s 中 `foodlink-dev` 与 `foodlink-main` 各有 3 个 2/2 Running Pod。未直接读取 Apollo main/dev 配置，尚不能断言其是否与本地共用同一库。
- 用户要求筛选近期活跃用户、长期不活跃用户，并据此清理微信体验成员。已做只读活跃度统计：注册 1247 人，近 30 天活跃 285 人，31–60 天未活跃 94 人，超过 60 天无行为 868 人；明细在 `tmp/activity-report-2026-07-11.csv`。指标使用最后登录、饮食记录、分析任务、运动记录四者中的最近时间。候选中含早期高使用量老用户，不能直接全删；需先得到微信后台体验成员名单取交集并人工复核。尝试通过已登录 Chrome 只读访问 `mp.weixin.qq.com`，但被浏览器安全策略禁止；小程序自动化 3001 未连接，因此当前无法自动获取或移除成员。

## 圈子页 iOS 举报占位文字与底栏隐藏 main 修复
- 用户要求切到 `main`、拉取最新代码，并重新解决两个问题；当前工作区改动允许直接撤销。
- 已执行 `git reset --hard` / `git clean -fd`，切到 `main` 后发现本地旧网站分享页提交与远端冲突；为对齐最新 `origin/main`，rebase 时跳过该本地旧提交，当前 `main` 位于 `751fa486`。
- 修复 1：`apps/wechat/src/pages/community/components/FeedReportSheet.tsx` 在 `visible=false` 时直接 `return null`，不再渲染隐藏的举报弹层和原生 `Textarea`，避免 iOS 微信 placeholder “补充说明（选填）”穿透悬浮。
- 修复 2：默认圈子页未复现底栏无故消失；底栏设计上会在评论输入栏或筛选抽屉打开时隐藏。验证发现残留 `community_filter_drawer_visible` flag 会让 `custom-tab-bar` 隐藏，因此在 `apps/wechat/src/pages/community/index.tsx` 中新增 `feedFilterExpandedRef`，并在 `Taro.useDidShow` 里按真实评论/筛选状态清理残留隐藏 flag。
- 验证：目标文件 eslint 通过；`git diff --check` 通过，仅既有 LF/CRLF warning；`node --check apps/wechat/custom-tab-bar/index.js` 通过；WeApp DevTools 3002 默认圈子页 `tabHidden=false`、两个 flag 为空、举报输入/textarea 为 0、异常数 0；手动设置筛选残留 flag 后 `tabHidden=true`，清理后恢复 `false`；截图接口 8 秒超时。
- 限制：现有 `dev:weapp` watch 进程存在但未把本次源码刷新进 `apps/wechat/dist/common.js`，未重启/新启动本地 dev server；全量 `npm --workspace apps/wechat run typecheck` 仍失败于既有分包类型错误，非本次改动。
