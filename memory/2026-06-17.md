# 2026-06-17

## 完成：Mobile App 登录页简洁化与密码入口隐藏
- 按用户提供的知乎/小红书登录参考图，App 登录页改为白底简洁首屏，不再使用切换按钮或上下两个登录框。
- `apps/mobile/src/screens/LoginScreen.tsx` 现在只展示手机号验证码登录/注册合并和微信一键登录：手机号行、验证码行、发送验证码、主按钮「登录 / 注册」、灰色胶囊「微信一键登录」和协议勾选；移除了可见的密码登录入口、密码 tab 和长说明文案。
- 登录页不再展示手动邀请码输入框；`fi` / `invite_code` / `inviteCode` 深链邀请参数仍在后台解析并透传给短信首登或微信登录接口。
- `apps/mobile/src/navigation/RootNavigator.tsx` 在未登录栈注册 `Agreements` / `PrivacyPolicy`，登录页协议链接可在未登录状态打开。
- 验证通过：`npm --workspace apps/mobile run typecheck`；`git diff --check -- apps/mobile/src/screens/LoginScreen.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅有既有 LF/CRLF 提示；扫描确认登录页无 `密码登录`、`LOGIN_MODES`、`loginWithPassword`、`Food Link`、`欢迎回来` 等旧文案/旧入口。
- ADB + Expo Go 复验：清理第二个模拟器 `127.0.0.1:5555` 的 Expo Go 本地数据后打开 `exp://192.168.8.193:8081`，首屏显示简洁手机号验证码和微信入口，证据截图 `tmp/foodlink-login-simple.png`；协议勾选可切换，点击《用户协议》可进入未登录协议页。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有写入业务数据；只清理了第二个模拟器的 Expo Go 本地 token/cache 以进入未登录态验证。

## 完成：Mobile App 小程序迁移收尾盘点
- 继续推进微信小程序到 App 的迁移收尾；本轮按 `apps/wechat/src/app.config.ts` 已注册页面复核 App 导航覆盖。
- 结论：小程序主包、`packageExtra`、`packageStatsMetabolic`、`packageAbout`、`packageUserGroup` 的注册页面均已在 `apps/mobile/src/navigation/RootNavigator.tsx` 中有对应 App 路由或合并后的等价路由。
- 合并/兼容约定：`weight-record` / `water-record` / `exercise-record` 合并为 `BodyMetricRecord`；`weight-trend` / `water-trend` / `exercise-trend` 合并为 `TrendDetail(kind)`；`food-expiry` 对应 `Expiry`；`pro-membership` 对应 `MembershipCenter`；`campus-food-share` / `food-library-share` 对应 `PublicFoodShare`。
- 复核 `apps/wechat/src/packageExtra/pages/health-profile-edit` 只剩 `index.config.ts`，未在小程序 `app.config.ts` 注册；App 不新增残余壳路由，继续由 `HealthProfile` / `HealthProfileView` 承接。
- 最终验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（28 tests）、`go build ./cmd/server`、`git diff --check`、禁用用户文案扫描、backend health 200、Metro 8081 running、ADB 设备在线。
- MuMu + Expo Go + ADB 最终烟测：`127.0.0.1:16384` 打开 `exp://192.168.8.193:8081`，登录页正常渲染，可见「使用微信、手机验证码或手机号密码登录」和「验证码登录 / 手机号登录 / 注册手机号」。证据：`tmp/final-app-smoke.png/xml`。
- 未运行远程数据库迁移、未发送真实短信、未做真实短信登录；这些需要 Redis/腾讯云短信配置和用户确认目标库后再执行。本轮没有运行 `npm run build:weapp`，没有提交/发布版本，没有主动写入新的业务数据。

## 完成：Mobile App 手机验证码登录复查与登录页运行态验证
- 继续推进微信小程序到 App 的工具界面迁移；本轮收口 App 登录页与 SMS 验证码链路。
- `apps/mobile/src/screens/LoginScreen.tsx` 默认展示「验证码登录」，页面标题和说明改为「微信、手机验证码或手机号密码登录」；发送验证码前增加 11 位大陆手机号本地校验，验证码输入限制 6 位。
- `packages/api-client` 新增/确认 `sendSMSCode` 与 `loginWithSMSCode`，并恢复 `phone` 为主、`username` 可选旧调试兼容的密码登录/注册/设置密码入参；App UI 仍只传 `phone`。
- `packages/api-client/tests/client.test.ts` 新增短信发送与验证码登录测试，覆盖 `/api/app/sms/send-code`、`/api/app/login/sms`、`inviteCode` trim 和登录 token 存储。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（28 tests passed）、`go build ./cmd/server`、相关 `git diff --check` 和禁用/内部文案扫描。
- MuMu + Expo Go + ADB 验证：清理第二个模拟器 `127.0.0.1:5555` 的 Expo Go 本地数据后打开登录页，首屏可见验证码/手机号/注册三段；空手机号发送验证码弹出「手机号有误 / 请输入 11 位大陆手机号。」；手机号登录模式显示手机号、密码、登录；注册模式显示昵称、手机号、密码、注册并登录。证据为 `tmp/login-page.png/xml`、`tmp/login-phone-guard.png/xml`、`tmp/login-password.png/xml`、`tmp/login-register.png/xml`。
- 本轮没有发送真实短信验证码，没有点击验证码登录或注册，没有写入账号业务数据；仅清理第二个模拟器 Expo Go 本地 token/cache。后端 auth service/handler 单测仍受本机 sqlite/CGO 阻塞，错误为 `go-sqlite3 requires cgo`；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 个人主页编辑与注销确认补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦个人主页编辑和注销账号确认。
- `apps/mobile/src/screens/ProfileSettingsScreen.tsx` 的编辑资料卡补齐完整用户 ID 与「复制」入口，主页头部继续展示短 ID；座右铭输入提示为「写一句你的座右铭（最多30字）」并限制 30 字。
- 注销账号二次确认文案补齐“本地登录状态也会清空”，按钮为「再想想 / 确认注销」，承接小程序高风险确认语义。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关 `git diff --check` 和禁用文案扫描。
- MuMu + Expo Go + ADB 验证：从「我的」进入「个人主页」，展开「编辑资料」，可见昵称、座右铭、选择头像/背景图、完整用户 ID 与复制按钮；点击「注销账号」打开确认弹窗，点击「再想想」后回到编辑卡。证据为 `tmp/profile-settings-owner-page.png/xml`、`tmp/profile-edit-card.png/xml`、`tmp/profile-delete-confirm.png/xml`、`tmp/profile-delete-confirm-cancelled.xml`。
- 本轮没有点击保存资料、选择图片、复制 ID 或确认注销，没有退出登录或写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 邀请好友二维码补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「邀请好友」，补齐小程序邀请页中的“扫码也能加入”二维码卡。
- `apps/mobile` 新增纯 JS 依赖 `qrcode-generator`，在 App 本地把 `foodlink://invite?fi=邀请码` 深链生成真实二维码矩阵，不依赖微信小程序码接口。
- `InviteFriendsScreen` 展示二维码卡、说明、可点击二维码、打开二维码和复制链接入口；点击后用 App 内 Modal 放大二维码并提供关闭按钮。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关 `git diff --check` 和禁用文案扫描。
- MuMu + Expo Go + ADB 验证：从「我的」进入「邀请好友」，可见邀请码、邀请链接、「扫码也能加入」、真实二维码矩阵、「打开二维码」「复制链接」和手动填写邀请码；打开二维码后出现放大预览 Modal，关闭后回到邀请页。证据为 `tmp/invite-friends-qr-page.png/xml`、`tmp/invite-friends-qr-modal.png/xml`、`tmp/invite-friends-after-modal-close.xml`。
- 本轮没有点击系统分享、复制链接/邀请码、解析邀请码或加为好友，没有写入好友、邀请或积分业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 手机号密码登录语义补齐
- 继续推进微信小程序到 App 的迁移；本轮把 App 备用登录从旧「用户名/账号密码」语义收敛为「手机号密码登录」。
- 后端 App 密码登录/注册/设置密码接口兼容 `phone` 与旧 `username`；手机号优先使用 `weapp_user.telephone`，旧 `username` / `app-pwd:` 路径只作为兼容，不作为 App 普通用户入口展示。
- `packages/api-client` 新增 phone 入参兼容，App `AuthProvider`、登录页、账号安全页、个人页入口和隐私政策相关文案均改为手机号密码语义。
- 账号安全页展示微信登录、手机号、密码登录状态；手机号做 11 位大陆手机号校验，密码做至少 8 位与两次一致校验，保存按钮在满足条件前禁用。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（27 tests）、`go build ./cmd/server`、相关禁用/旧文案扫描和 `git diff --check`。
- MuMu + Expo Go + ADB 验证：账号安全页展示手机号密码语义；输入本地测试手机号 `13800138000` 与两次一致密码后，「保存手机号和密码」变为 enabled。证据为 `tmp/phone-account-security-page-updated.png/xml`、`tmp/phone-account-security-filled.png/xml`。
- 本轮只输入本地表单测试值，没有点击保存，没有修改当前账号手机号或密码，没有写入业务数据；后端 auth service/handler 单测仍被当前 Windows sqlite/CGO 环境阻塞，错误为 `Binary was compiled with 'CGO_ENABLED=0', go-sqlite3 requires cgo to work`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 清缓存诊断缓冲补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「清除缓存」与反馈诊断缓存一致性。
- `apps/mobile/src/api.ts` 新增 `clearRecentRequestTraces()`；「我的」页和「关于与反馈」页的清缓存入口现在会同时清理本机页面缓存、最近请求 trace 和客户端 console 日志缓冲，并保留登录 token 与 user id。
- 「关于与反馈」页清理后会刷新诊断数量显示，避免用户清理后仍看到旧 trace / 日志计数。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证清缓存入口、确认弹窗、成功弹窗和清理后仍保持登录态，证据为 `tmp/cache-profile-clear-entry.png/xml`、`tmp/cache-clear-confirm.png/xml`、`tmp/cache-clear-success.png/xml`、`tmp/cache-after-ok.png/xml`。
- 本轮只清理本机缓存和诊断缓冲，没有调用后端写接口，没有退出登录，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 关于与反馈页补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「关于与反馈」。
- `AboutFeedbackScreen` 反馈区补齐小程序同义 hero、四类反馈卡片（问题反馈 / 功能建议 / 使用体验 / 其他）、500 字内容计数、至少 5 字提示、120 字联系方式计数、最多 4 张截图说明、诊断开关和提交前禁用。
- 新增 `apps/mobile/src/diagnostics/consoleLogBuffer.ts` 并在 App 启动时安装 console 缓冲；反馈诊断开启时会随请求 trace 附带最近客户端日志，不包含 token、请求体或图片。
- `AppButton` 支持 `disabled`；协议与社群卡补齐「查看协议」「隐私政策」「用户群页」跳转，并保留用户群二维码展开/收起。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证关于卡、反馈卡片、输入计数、截图说明、联系方式、诊断开关、隐私设置、协议/社群入口和二维码展开均正常，证据为 `tmp/about-feedback-page-top.png/xml`、`tmp/about-feedback-typed.png/xml`、`tmp/about-feedback-form-bottom.png/xml`、`tmp/about-feedback-group-qr.png/xml`、`tmp/about-feedback-group-qr-bottom.png/xml`。
- 本轮没有提交真实反馈，没有上传截图，没有切换隐私开关，没有清除缓存，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 账号安全表单状态补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦 App 侧微信登录之外的备用账号密码工具页。
- `AccountSecurityScreen` 改为页面聚焦刷新；用户名补齐 3-32 位且仅字母/数字/下划线校验提示；新密码与确认密码补齐「至少 8 位」「两次一致」状态标签；保存按钮在表单满足条件前禁用。
- 新增账号注销说明卡，指明注销账号入口仍在个人主页编辑页底部并会二次确认，避免在账号安全页重复放置高风险可执行注销按钮。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证账号安全页首屏、禁用保存按钮、输入测试值后按钮启用和注销说明均正常，证据为 `tmp/account-security-page.png/xml`、`tmp/account-security-filled.png/xml`。
- 本轮没有点击保存账号密码，没有注销账号，没有退出登录，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 自动续费审核预览补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「自动续费审核」。
- `AutoRenewAuditScreen` 顶部新增审核预览说明卡，明确该页用于核对签约前说明、扣费周期、每期金额和关闭路径；页面聚焦时重新读取会员状态和会员套餐。
- 「确认开通自动续费（审核预览）」展示 App 内签约预览 Modal，包含拟开通套餐、周期、金额和“不创建订单、不调用委托代扣、不真实扣款”说明。
- 「关闭自动续费（审核预览）」展示 App 内关闭路径 Modal，包含产品内路径、微信支付/应用商店/支付渠道路径和“不执行真实解约”说明。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证页面首屏、签约前说明、勾选、签约预览 Modal 和关闭路径 Modal 均正常，证据为 `tmp/auto-renew-page-top.png/xml`、`tmp/auto-renew-sign-section.png/xml`、`tmp/auto-renew-sign-ready.png/xml`、`tmp/auto-renew-sign-modal.png/xml`、`tmp/auto-renew-cancel-modal.png/xml`。
- 本轮没有创建订单，没有调用支付渠道委托代扣，没有执行真实解约，没有写入会员或支付状态；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 协议长文补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「用户协议 / 隐私政策 / 会员协议」。
- `AgreementsScreen`、`PrivacyPolicyScreen`、`MembershipAgreementScreen` 从摘要卡补齐为分节长文；内容覆盖服务范围、AI 免责声明、会员积分、支付订阅、隐私数据、图片/报告/AI 分析、本地缓存、自动续费、退款取消等关键条款。
- 三个协议页继续沿用 App 现有 `Page/Card/Text` 结构，`LegalCard` 支持多段正文。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证用户协议、隐私政策、会员协议首屏和中后段滚动均正常，证据为 `tmp/legal-agreement-top.png/xml`、`tmp/legal-agreement-mid.png/xml`、`tmp/legal-privacy-top.png/xml`、`tmp/legal-privacy-mid.png/xml`、`tmp/legal-membership-top.png/xml`、`tmp/legal-membership-mid.png/xml`。
- 本轮没有写入业务数据，没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 身体趋势工具页补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「身体趋势 / 体重趋势 / 喝水趋势 / 运动趋势」。
- `BodyTrendsScreen` 改为页面聚焦刷新，并在体重、饮水、运动摘要卡补齐直达详情按钮。
- `TrendDetailScreen` 按三类趋势分支补齐：体重最新值/较上次/30 天趋势/历史分组；喝水目标/日均/30 天热力/日期明细；运动 30 天消耗/活跃热力/最近记录；删除入口保留二次确认。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关禁用文案扫描、`git diff --check`；MuMu + Expo Go + ADB 验证身体趋势、体重趋势、喝水趋势、运动趋势页面均正常渲染，证据为 `tmp/body-trends-page.png/xml`、`tmp/weight-trend-page.png/xml`、`tmp/water-trend-page.png/xml`、`tmp/exercise-trend-page.png/xml`。
- 本轮没有新增、删除或修改身体记录，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 用户群页补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「用户群」，把 App 从单二维码卡补齐为接近小程序用户群页的信息架构。
- `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `UserGroupScreen` 新增食探交流群标题区、群卡、永久有效标签、当前唯一用户群说明、打开二维码、复制群名和加入方式说明。
- App 侧使用 `Modal` 放大二维码；「复制群名」使用 `expo-clipboard` 写入本地剪贴板并弹出「群名已复制 / 食探用户群」。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关 `git diff --check`、禁用文案扫描；MuMu + Expo Go + ADB 验证首屏、二维码 Modal 和复制群名弹窗。
- 运行时证据：`tmp/user-group-page.png/xml`、`tmp/user-group-qr-modal.png/xml`、`tmp/user-group-copy-alert.png/xml`。
- 本轮只写本地剪贴板，没有调用后端，没有修改业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 隐私设置即时保存补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「隐私设置」，把 App 从本地改开关再点保存，调整为小程序式切换即保存、失败回滚。
- `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `PrivacySettingsScreen` 默认把缺省 `searchable` / `public_records` 视为 `true`；开关切换时单项调用 `apiClient.updateUserProfile({ [key]: value })` 保存，失败恢复原状态。
- 隐私开关文案对齐小程序为「允许在圈子中被搜索」和「公开我的饮食记录」，新增副标题和说明卡「开关切换后会立即保存；保存失败时会自动恢复到原状态」，移除单独保存按钮。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关 `git diff --check`、禁用文案扫描；MuMu + Expo Go + ADB 打开隐私设置页可见两个开启状态开关、说明文案和当前账号，无红屏。
- 运行时证据：`tmp/privacy-settings-page.png`、`tmp/privacy-settings-page.xml`。
- 本轮没有切换隐私开关，避免改变当前账号隐私状态；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 好友管理三段页补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「圈子」里的好友管理，把 App 原先的一页混排好友、搜索、收到/发出请求，补齐为接近小程序好友页的 `好友列表 / 收到请求 / 我发起的` 三段体验。
- `apps/mobile/src/screens/DetailScreens.tsx` 的 `FriendsScreen` 新增三段 tab、好友昵称本地筛选、查找新朋友搜索入口、好友头像/昵称卡片、主页/私信入口、删除好友二次确认、收到请求接受/拒绝、发出申请撤销二次确认。
- `packages/api-client/src/index.ts` 新增 `cancelSentFriendRequest(requestId)`，对接后端 `DELETE /api/friend/request/:request_id`；`packages/api-client/tests/client.test.ts` 新增好友管理接口测试，覆盖列表、搜索、发送申请、请求总览、同意、撤销和删除。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（26 tests passed）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「圈子」进入「好友」，可见好友管理卡、三段 tab、搜索好友、空态「还没有好友」和「查找新朋友」；切换「收到请求」显示「暂无好友请求」，切换「我发起的」显示「没有待处理的申请」。证据：`tmp/friends-community.png`、`tmp/friends-page.png`、`tmp/friends-received-tab.png`、`tmp/friends-sent-tab.png` 及同名 XML。
- 当前验证账号无好友和请求，本轮运行时没有搜索用户、发送申请、接受/拒绝、撤销或删除，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 私信会话与聊天体验补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「圈子」里的私信入口，把 App 从简单会话/消息列表补齐到接近小程序私信页的会话分页、未读提示、头像、聊天轮询和图片发送体验。
- `packages/api-client/src/index.ts` 的 `listConversations` 兼容旧数字参数和新对象参数 `{ limit, offset }`，并返回 `has_more`、`offset`、`limit`；`packages/api-client/tests/client.test.ts` 新增私信会话与消息分页测试。
- `apps/mobile/src/screens/MigrationScreens.tsx` 的 `ConversationsScreen` 支持页面聚焦刷新、分页查看更多、当前用户识别、会话合并、未读徽标、最后消息预览和头像兜底。
- `PrivateChatScreen` 支持进入页面后刷新并每 3 秒轮询、查看更早消息、消息合并排序、时间分隔、发送文字后本地合并和静默刷新；系统消息隐藏输入区，普通聊天保留文字和图片发送入口。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（25 tests passed）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「圈子」进入「私信」可见标题、副标题 `0 个会话` 和空态；系统消息深链可见系统聊天且隐藏输入区；普通聊天深链可见输入框、`发图片` 和 disabled 的 `发送`，输入 `hello` 后 `发送` 可用。证据：`tmp/private-chat-community.png`、`tmp/private-conversations-page.png`、`tmp/private-chat-system.png`、`tmp/private-chat-normal-empty.png`、`tmp/private-chat-normal-typed.png` 及同名 XML。
- 本轮没有发送消息、没有选择或上传图片、没有写入业务数据，没有执行数据库迁移，没有运行 `npm run build:weapp`；旧 `mobile-dev.log` 中仍有早前迁移阶段错误记录，当前私信运行未复现红屏。

## 完成：Mobile App Release 0.0.1 APK 链路跑通
- 已通过 Apollo `release-config.yaml` 读取 release COS/CDN 配置，非敏感字段为 `cos_region=ap-beijing`、`release_bucket=foodlink-releases-1370036754`、`release_cdn_base_url=https://download.healthymax.cn`。
- `scripts/release_mobile_android.py` 已支持本地构建 APK：自动发现 JDK/Android SDK，运行 `expo prebuild --platform android` 与 Gradle `assembleRelease`，使用临时 debug keystore 签名的 release APK；正式升级前仍需配置稳定生产 keystore。
- 本机已安装 Android SDK command line tools、platform-tools、Android 35 platform/build-tools、COS SDK 与腾讯云 CDN SDK；脚本会生成 Gradle Maven 镜像 init script，减少国内 Maven 下载失败。
- COS 上传显式设置 `public-read`；发布时生成 APK、`.sha256`、release `manifest.json`，并更新 `channels/beta.json` / `channels/stable.json`。首次 build 1 因上传过 debug APK 且被 CDN 缓存，实际推荐下载改为 `version=0.0.1`、`buildNumber/versionCode=2`。
- 当前 APK：`https://download.healthymax.cn/releases/android/0.0.1/2/foodlink-0.0.1-2.apk`；release manifest：`https://download.healthymax.cn/releases/android/0.0.1/2/manifest.json`；beta/stable channel 均已指向 build 2。CDN 刷新任务：`624279280955690343`。
- 验证通过：`python -m py_compile scripts/release_mobile_android.py`、`npm --workspace apps/mobile run typecheck`、`cd website && npm run build`、CDN APK HEAD 200 且大小 `76729458` bytes、MuMu ADB 安装启动成功，前台窗口为 `cn.healthymax.foodlink/.MainActivity`，最近日志无 `FATAL EXCEPTION`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App Release 图标补齐与 build 3
- 复用 `apps/wechat/src/assets/logo.png` 生成 `apps/mobile/assets/icon.png` 与 `apps/mobile/assets/adaptive-icon.png`，并在 `apps/mobile/app.json` 配置 Expo `icon` 和 Android `adaptiveIcon.foregroundImage`。
- `android.versionCode` 升至 `3`，官网下载配置同步到 build 3；当前推荐 APK 为 `https://download.healthymax.cn/releases/android/0.0.1/3/foodlink-0.0.1-3.apk`。
- 已发布 build 3 到 COS/CDN，beta/stable channel 均已指向 build 3；CDN 刷新任务 `624370450914415402`。
- 验证通过：Android `mipmap-xxxhdpi/ic_launcher.webp` 已显示食探图标；build 3 manifest 返回 `buildNumber=3`，APK HEAD 200 且大小 `77267107` bytes；MuMu ADB 安装升级成功，系统显示 `versionCode=3`、`versionName=0.0.1`，启动日志无 `FATAL EXCEPTION`。
- `npm --workspace apps/mobile run typecheck` 与 `cd website && npm run build` 均通过；本轮没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：官网 App 下载入口改为运行时读取 channel
- 官网 App 下载配置保留 build 3 作为兜底，但下载区和 Hero CTA 已改为运行时读取 `https://download.healthymax.cn/channels/stable.json` 与 `https://download.healthymax.cn/channels/beta.json`。
- 新增 `website/src/hooks/useReleaseChannel.ts`；`AppComingSoonSection` 优先使用 manifest 中的 APK/AAB URL、版本号、构建号、release manifest URL，AAB 未发布时隐藏；`AppDownloadSoonButton` 优先使用 stable APK。
- 已为 release COS bucket 配置 GET/HEAD CORS，浏览器预检返回 `Access-Control-Allow-Origin: *`。
- 本地浏览器验证：下载区和 Hero CTA 的 href 均解析为 build 3 APK，且页面不显示技术化“跟随 stable.json/beta.json”文案。
- `cd website && npm run build` 与本轮下载相关文件 `git diff --check` 均通过；以后发布 App 只需更新 channel manifest 和刷新 CDN，不需要为了下载链接重新构建官网。

## 完成：Mobile App 校园食堂入口体验补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦 `CampusCanteenScreen`，把 App 原先的学校文本框 + 简单列表，补到接近小程序校园食堂页的筛选、排序、推荐和快速记录体验。
- `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的校园食堂页新增学校、食堂、楼层、窗口、菜名/位置筛选；请求后端时使用 `type=campus`、`is_campus_food=true`、`sort_by`、`school_name`、`canteen_name` 和搜索关键词。
- 校园食堂页新增热门、高蛋白、低热量、蛋白性价比推荐横向区；列表卡片补充图片、校园位置、价格、热量、蛋白、标签、点赞/评论/收藏数。
- 校园食堂列表卡新增「一键记录」入口，复用手动记录工作台的 `quickItem` + `sourceChannel=campus`，不在列表页直接保存饮食记录。
- `packages/api-client/tests/client.test.ts` 扩展公共食物列表筛选测试，覆盖校园食堂页使用的 `canteen_name` 参数。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（24 tests passed）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「圈子」点击「校园餐」进入校园食堂页，首屏可见学校/食堂/楼层/窗口/菜名筛选、排序分段和热门推荐横向区；下滑到校园餐列表卡可见价格、热量、蛋白、标签、互动数和「一键记录」；点击「一键记录」进入手动记录工作台，顶部显示 `已选 1 项 · 1份`，来源为「校园食堂」。证据：`tmp/campus-canteen-top.png`、`tmp/campus-canteen-high-protein.png`、`tmp/campus-canteen-list.png`、`tmp/campus-canteen-quick-record.png` 及同名 XML。
- 本轮没有点击「保存为饮食记录」，没有发布、点赞、收藏、评论或删除校园餐，没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 互动消息筛选分页补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮聚焦「我的」页互动消息，把 App 从单列表一次拉取，补齐到接近小程序互动通知页的筛选和分页体验。
- `packages/api-client/src/index.ts` 的 `listCommunityNotifications` 兼容旧数字参数，同时支持对象参数 `{ limit, offset, type }`，返回 `has_more`；`markCommunityNotificationsRead()` 无 ID 时保持全量已读语义。
- `apps/mobile/src/screens/DetailScreens.tsx` 的通知页新增「全部 / 点赞 / 评论」三段筛选、分页「查看更多」、按 tab 分流空态、头像进入个人主页、全部已读按钮；首次拉到未读消息时会按小程序语义自动标记已读。
- `packages/api-client/tests/client.test.ts` 新增通知分页筛选测试，覆盖 `type=comment_received`、`offset=40`、`limit=20` 和全部标记已读 payload。
- 验证通过：`npm --workspace packages/api-client run typecheck`、`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（24 tests passed）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「我的」进入「互动消息」，首屏可见全部已读和 `全部 / 点赞 / 评论` 三段筛选；当前账号无互动通知，切换点赞和评论 tab 后分别展示对应空态，无红屏。证据：`tmp/notifications-tabs.png`、`tmp/notifications-like.png`、`tmp/notifications-comment.png` 及同名 XML。
- 当前验证账号 `unread_count=0` 且通知列表为空，本轮运行时没有触发真实已读写入、没有点赞/评论/发布或删除业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 校园餐详情聚合信息补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮把校园餐详情页从只展示基础 `item`，补到可消费后端 `/campus-detail` 的聚合信息。
- `packages/core/src/types.ts` 新增 `CampusRelatedFeedItem`，并把 `CampusFoodDetail.related_feeds` / `similar_items` 按真实后端返回放宽为可空集合，兼容 `related_feeds: null`。
- `apps/mobile/src/screens/MigrationScreens.tsx` 的校园餐详情页现在展示校园价格指标：蛋白性价比、热量价格；新增「同食堂相似菜品」和「圈子相关动态」两个非空时渲染的区块。
- 相似菜品点击继续进入校园餐详情；相关动态点击进入圈子动态详情，`targetType` 使用 `campus_food`。
- `packages/api-client/tests/client.test.ts` 新增校园餐详情聚合响应测试，覆盖 metrics、similar items、related feeds 和 campus-detail 请求路径。
- 验证通过：`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（23 tests passed）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「公共食物库」打开「咖啡汉堡套餐」详情，可见 `食物详情`、`校园餐`、`蛋白性价比 0.6g/元`、`热量价格 12元/100kcal`；真实后端该样例没有相似菜品和相关动态，页面无红屏并直接进入评论区。证据：`tmp/campus-detail-after-open.png`、`tmp/campus-detail-after-open.xml`。
- 只读扫描确认当前本地后端最多 100 条校园餐中暂无非空 `similar_items` / `related_feeds`；非空 UI 通过单测锁定。尝试创建临时校园餐样例时后端返回会员权限错误 `10005`，留下的临时草稿 `79ab71c2-7e27-44e9-b875-8fedfd1eab04` 已通过业务 DELETE 清理，复查 `codex-similar-campus-0617` 剩余 0 条。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 公共食物详情评论互动补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮补齐公共食物/校园餐详情页评论互动：主评论 + 回复层级展示、评论总数、回复目标切换、取消回复、发布回复入口。
- `packages/api-client/src/index.ts` 的 `addPublicFoodComment` 支持 `parentCommentId` / `replyToUserId` 并按后端 snake_case 发送；新增 `deletePublicFoodComment` 对接 `DELETE /api/public-food-library/:item_id/comments/:comment_id`。
- `apps/mobile/src/screens/MigrationScreens.tsx` 中只有当前用户自己的评论显示「删除」，删除评论和删除自己的公共食物/校园餐上传都需要二次确认，避免误触。
- 验证通过：`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（21 tests）、`npm --workspace apps/mobile run typecheck`、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：从「圈子」进入「公共食物」，打开「锦恢做的拿铁」详情，评论区显示 `评论 5`、主评论和回复；点击「回复」后输入区切换到「正在回复 Sky Walker / 取消回复 / 回复 Sky Walker / 发布回复」。证据：`tmp/public-food-comment-existing-comments.png`、`tmp/public-food-comment-reply-target.png` 及同名 XML。
- 本轮没有发布评论、没有删除评论、没有点赞/收藏、没有写入业务数据；仅用业务 API 做只读查询以找到有评论样例。没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 进行中：Mobile App Release 发布脚本与官网下载入口
- 用户已新建 COS 存储桶和 CDN 域名，并把 Apollo namespace 从 `app-config.yaml` 改为 `release-config.yaml`；读取成功，release 配置在 `storage` 下，非敏感字段为 `cos_region=ap-beijing`、`release_bucket=foodlink-releases-1370036754`、`release_cdn_base_url=https://download.healthymax.cn`。
- 新增 `scripts/release_mobile_android.py`，从 Apollo `release-config.yaml` 读取 COS 密钥与 release bucket/CDN，支持发布 APK/AAB 到 `releases/android/{version}/{build}/`，生成 `.sha256`、`manifest.json`，并更新 `channels/beta.json` / `channels/stable.json`。普通 push 不触发发布。
- 新增 `eas.json`，定义 Android `development` / `preview` / `production` profiles；`preview` 输出 APK，`production` 输出 AAB。`apps/mobile/package.json` 的构建命令改为 `npx eas-cli ...`，避免缺少全局 `eas`。
- `apps/mobile/app.json` 暂定首个 release 为 `version=0.0.1`、`android.versionCode=1`、`android.package=cn.healthymax.foodlink`。
- 官网 `website` 下载区已从“App 即将开放”改为真实下载面板：新增 `website/src/content/app-download.ts`，下载链接按 0.0.1 / build 1 指向 `https://download.healthymax.cn/releases/android/0.0.1/1/`；提供 Android APK/AAB 与 beta/stable manifest 入口。顶部导航新增「App 下载」，Hero 下载按钮改为直达 Android APK。
- 验证通过：`python -m py_compile scripts/release_mobile_android.py`、release dry-run（确认 bucket/CDN、beta/stable channel key）、`npm --workspace apps/mobile run typecheck`、`cd website && npm run build`、`npx eas-cli --version`。
- 本轮没有真实构建或上传 APK/AAB；仓库内未找到现成 `.apk` / `.aab`。真正发布需先确认 EAS 登录/项目/签名，或提供已构建产物后运行上传脚本。
## 完成：Mobile App 公共食物列表筛选排序补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮补齐公共食物/校园餐列表页的搜索、排序和减脂筛选能力。
- `apps/mobile/src/screens/MigrationScreens.tsx` 的公共食物列表现在支持商家/地点搜索、最新/热度/评分排序、适合减脂筛选和当前条件标签；筛选只在「全部」「校园」展示，「我的」「收藏」继续走专用接口。
- 公共食物卡片新增评论数和「适合减脂」标签；顶部 4 个 tab 局部使用紧凑布局，避免「收藏」单独换行。
- `packages/api-client/tests/client.test.ts` 新增列表筛选请求测试，覆盖 `sort_by=rating`、`merchant_name` trim、`suitable_for_fat_loss=true`、`type=campus` 和 `is_campus_food=true`。
- 验证通过：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（22 tests）、相关 `git diff --check`；禁用加载/迁移/内部错误文案扫描无命中。
- MuMu + Expo Go + ADB 验证：公共食物页首屏可见筛选卡和列表卡；点击「评分」+「适合减脂」后出现「只看适合减脂」并展示减脂条目；输入 `test` 搜索后出现「搜索：test」和匹配空态；点击「清除」后搜索条件消失；切到「我的」时筛选卡隐藏。证据：`tmp/public-food-list-filter-top.png`、`tmp/public-food-list-rating-fatloss.png`、`tmp/public-food-list-search-test.png`、`tmp/public-food-list-clear-search.png`、`tmp/public-food-list-mine-no-filter.png`、`tmp/public-food-list-tabs-compact-reload.png` 及对应 XML。
- 本轮没有上传、发布、点赞、收藏、评论、删除或保存公共食物；没有写入业务数据，没有执行数据库迁移，没有运行 `npm run build:weapp`。
# 完成：Mobile App 关注列表与个人主页头部补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮完成「关注 / 被关注」列表的 Expo Go + ADB 运行时验证，并把个人主页头部从完整 UUID 展示改为小程序式短 ID + 复制 ID + 统计行结构。
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的关注列表支持头像/昵称、关系状态、进入主页、私信、关注/取消关注、分页和空态；当前账号关注/粉丝均为 0，验证覆盖真实空态。
- `apps/mobile/src/screens/ProfileSettingsScreen.tsx` 现在展示短 ID（如 `eff3150a...3661`），内联「复制ID」仍复制完整 ID；记录天数、被关注、关注改为三栏统计行，被关注/关注可进入对应列表。
- 验证通过：`npm --workspace apps/mobile run typecheck`、相关 `git diff --check`、禁用文案扫描；backend health 200、Metro running、ADB `127.0.0.1:16384` 在线。
- 运行时证据：`tmp/follow-list-following.png/xml`、`tmp/follow-list-followers.png/xml`、`tmp/profile-short-id.png/xml`、`tmp/profile-stat-following-nav.png/xml`。
- 本轮没有点击关注/取消关注、没有发送私信、没有写入业务数据；没有执行数据库迁移，没有运行 `npm run build:weapp`。

## 完成：Mobile App 成长伙伴与积分/排行焦点刷新补齐
- 继续推进微信小程序到 App 的工具界面迁移；本轮完成「成长伙伴 / 外观实验室」运行阻塞修复和「赚积分 / 打卡排行榜 / 我的」焦点刷新语义补齐。
- App 成长伙伴页已补齐候选形象、匹配原因、今日状态、外观档案、离线小惊喜、成长解锁、首页卡片显隐、本地偏好、外观实验室积分确认和候选列表。
- 运行时发现 `/api/pet/summary` 500，根因是后端直接把 `map[string]any` 写入 PostgreSQL jsonb；已在 pet repo 对 `meta`、`score_details` 和积分流水 `meta` 统一编码为 `datatypes.JSON` 后写入。
- 运行时还发现 pet 日期归属偏到前一天；已把 pet repo/service 的 date-only 解析改为 UTC date-only，并新增测试锁定 `2026-06-17` 不会变成 `2026-06-16`。
- `RewardCenterScreen`、`CheckinLeaderboardScreen`、`ProfileScreen` 改为页面聚焦刷新，承接小程序 `useDidShow` 的刷新语义，避免返回后积分、排行和「我的」摘要旧数据。
- 验证通过：`go test ./internal/pet/... -count=1`、`go build ./cmd/server`、`npm --workspace apps/mobile run typecheck`、相关 `git diff --check` 和禁用文案扫描。
- MuMu + Expo Go + ADB 验证通过：成长伙伴页、外观实验室、赚积分页、打卡排行榜均正常渲染；证据为 `tmp/pet-home-candidates.png/xml`、`tmp/pet-home-bottom.png/xml`、`tmp/pet-lab-open.png/xml`、`tmp/reward-focus-page.png/xml`、`tmp/checkin-focus-page.png/xml`。
- 本轮没有点击领取奖励、选择候选、随机换外观、去完成积分任务或制造打卡；pet summary 接口自身会创建/更新当天 score/event，这是页面读取链路的一部分。没有执行数据库迁移，没有运行 `npm run build:weapp`。
- 本次同步完成前端与后端餐次默认逻辑统一：按用户作息自动判断餐次优先于本地时段，覆盖入口包含 `analyze`、`record-manual`、`record-text`、`result`、`result-text`、`analyze-history`；后端 `Save` 接口增加对 `meal_type=snack` 的作息映射兜底，若作息缺失回退原逻辑。
- 调查能量识别算法中的“可食/可使用比例”：`ediblePortionRatio` 在 `backend/internal/analyze/service/analyze_service.go` 的 `applyEdiblePortionRatios` 阶段生成，并落入 `analysis_tasks.result` 与用户保存后的 `user_food_records.items` JSON；没有独立数据库表或列。`suggestedRatio` 是后续建议摄入比例，来源于 `applySuggestedRatios`，语义不同。
- 修复小程序前端可食/可使用比例随重量调整动态变化的问题：`apps/wechat/src/packageExtra/pages/result/index.tsx` 的结果页重量加减与食物编辑保存不再重算 `grossWeight` / `ediblePortionRatio`，并在回写分析结果缓存时保留原始可食比例字段；`apps/wechat/src/pages/index/components/MealRecordEditModal.tsx` 的首页餐食修改弹窗重量调整也不再重算可食比例。静态检查通过；小程序运行时验证因微信开发者工具自动化端口 9420/3001 均不可连接而阻塞。

- 发现并修复一个关键边界：后端 `FoodRecordService.Save` 中对 `meal_type=snack` 的作息映射因先经过 `normalizeMealType`（会转为时段）导致永远不触发，现已改为基于原始 `meal_type` 判断，且作息字段 `wake==sleep` 时回退本地时段。
- 同步修了 `result/index.tsx`、`result-text/index.tsx`：当作息推断后的 `defaultMealType` 变化时，刷新弹窗/下拉默认 `selectedMealType`，避免使用旧的本地时段默认值。
## 确认：后端登录能力现状
- 当前后端不是只有微信登录；路由已注册 `/api/login`（小程序微信）、`/api/app/login/wechat`（App 微信）、`/api/app/login/password`（App 用户名密码登录）、`/api/app/register/password`（App 用户名密码注册）、`/api/app/account/password`（登录后设置/修改密码）。
- App 账号密码登录当前按 `weapp_user.username` 查询；绑定手机号在 `telephone` 字段，不作为登录账号查询。手机号只有在被注册为 username 时才可当用户名用。
- 当前未发现短信验证码发送、验证码校验、短信验证码登录接口或相关 SMS 服务/验证码表；后端暂不支持短信验证码登录。
- 复查实际 PostgreSQL：`weapp_user` 有 `username`、`telephone`、`password_hash` 等列，也有 `idx_weapp_user_username`；但当前 `total_users=1053` 中 `username=0`、`password_hash=0`、`telephone=164`。App 后续应面向用户使用「手机号」概念，`username` 只作为内部兼容/待迁移字段。

## 完成：App 手机号短信登录与 yaml 配置收口
- App 登录页已提供微信登录、短信验证码登录、手机号密码登录/注册；`packages/api-client` 已收紧为只传 `phone`，不再保留 `username` 入参或 fallback。
- 后端已有 `weapp_user.password_hash`、`password_set_at` 字段，支持账号安全页设置手机号密码后用手机号密码登录；当前只读统计为 total_users=1058、users_with_password=0、users_with_username=0、users_with_phone=166，因此短信验证码更适合作为首登/无密码用户路径。
- 短信验证码接口为 `POST /api/app/sms/send-code` 和 `POST /api/app/login/sms`；验证码 key 为 `sms_verification_code:{phone}`，成功消费后删除，手机号/IP 节流 key 为 `throttle:sms:{phone}` 与 `throttle:sms:ip:{clientIp}`。
- 配置按 yaml/Apollo 方式读取：Apollo `app-config.yaml` namespace 增加 `redis:` 和 `sms:` 段；已移除新增短信/Redis 配置的 env 绑定口径。
- `redis.mode` 支持 `auto` / `redis` / `memory`：本地开发无 Redis 可用内存，生产应配置 `redis.mode: "redis"` 和 `redis.url`。
- 腾讯云短信配置按当前系统 yaml key 整理，示例配置保留真实短信所需字段，移除 mock 展示，并补充 `code_ttl_minutes`、`throttle_seconds`、`ip_throttle_seconds` 注释；密钥不写入仓库或状态文件。
- 只读 SQL 确认重复手机号在 `weapp_user.telephone`：`19927404435` 对应 `5865117a-e651-4a80-97ce-55629dafe76d` 与 `d7192bdf-9ed1-4190-96d6-4287c846a719`；清理前不加唯一索引。
- 验证通过：`go build ./cmd/server`、`go test ./internal/auth/... -run '^$' -count=1`、`go test ./pkg/config -count=1`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`、`npm --workspace apps/mobile run typecheck`、`git diff --check`。
- 用户授权后已重启本地后端并做真实短信发送验证：Apollo local 加载 `sms.*`，`redis.mode=memory`；MuMu `127.0.0.1:16384` 的 Expo Go 打开 `exp://192.168.8.193:8081`，在 App 登录页向 `131****4536` 发送验证码成功，页面弹出「验证码已发送」，后端 `POST /api/app/sms/send-code` 返回 200，腾讯云 request_id 为 `7b959740-77aa-4c4f-830a-ca8e2ef2d6f6`。
- 同手机号直接重试发送返回 429，节流生效；本轮未完成 `POST /api/app/login/sms` 登录，因为无法读取用户手机收到的短信验证码。
- 修复用户反馈的验证码刚收到就过期问题：旧实现 `GetDel` 会在首次错误/旧值/未输完整验证码尝试时删除正确验证码；现改为 Redis Lua / 内存锁 compare-and-delete，只有匹配成功才删除。
- 手机验证码登录与注册已合并：后端手机号不存在时自动创建 `weapp_user` 并发 token；App 登录页改为「验证码登录/注册」+「手机号密码」两段，不再展示单独「注册手机号」tab。
- 已补 `weapp_user_last_login_method_check` 允许 `sms_code`，同时代码兼容未迁移老库，旧约束未更新时跳过 `last_login_method` 不阻塞登录；未擅自对当前远程 PostgreSQL 执行迁移。
- 用户表讨论结论：当前没有独立 `users` 主表，`weapp_user` 是实际 canonical user table，包含手机号、密码、App 微信 openid、资料、积分等字段，且大量业务外键指向它。后续如重构，优先做身份绑定表或分阶段重命名，而不是在短信登录热修中直接新建主表替换。
- 追加 UI 收口：App 登录首页已移除可见的邀请码输入框，并把「验证码 / 密码 / 微信」做成同一张登录卡片内的同级切换；默认手机号验证码登录/注册，手机号密码和微信作为同级登录方式。深链邀请码仍在后台解析并透传给首登接口，不再提供手动填写入口。
- 验证：`npm --workspace apps/mobile run typecheck`、`git diff --check -- apps/mobile/src/screens/LoginScreen.tsx` 通过；MuMu `127.0.0.1:16384` + Expo Go 验证三段切换正常，UI dump 未出现「邀请码」，截图 `tmp/foodlink-login-unified-sms.png`、`tmp/foodlink-login-unified.png`。

## 完成：App 手机号登录与短信验证码接入
- 已将 App 普通账号入口收敛为手机号：`packages/api-client` 密码登录、注册、设置密码不再接收或发送 `username`；App 登录页改为「验证码登录 / 手机号登录 / 注册手机号」三段。
- 后端 App 密码登录、注册、设置密码业务路径只解析手机号；`weapp_user.username` 字段与 repo 查询能力保留为历史兼容，但不再作为 App 普通登录方式。
- 新增 `POST /api/app/sms/send-code` 和 `POST /api/app/login/sms`，短信验证码写 Redis `sms_verification_code:{phone}`，消费后删除；手机号/IP 节流 key 为 `throttle:sms:{phone}`、`throttle:sms:ip:{clientIp}`。
- 腾讯云短信接入 Go SDK `sms/v20210111 SendSms`，模板参数为 `[验证码, 有效分钟数]`；验证码使用 crypto/rand，开发 mock 可用 `SMS_MOCK_ENABLED=true` 和 `SMS_MOCK_CODE`。
- 历史早期口径：曾按环境变量列出 Redis/腾讯云短信配置；当前实现已改为上方 yaml/Apollo 的 `redis:` 与 `sms:` 段。
- 迁移 DO 与幂等迁移已补 `weapp_user.telephone` 普通索引；当前远程库存在重复手机号 `19927404435` 两条记录，因此暂不加唯一索引，后端遇重复手机号会提示联系客服。
- 验证通过：`go build ./cmd/server`、`go test ./internal/auth/... -run '^$' -count=1`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client run test -- --runInBand`（28 tests）、`npm --workspace apps/mobile run typecheck`、`git diff --check`。
- 未运行数据库迁移，因为当前 `backend/app-config.yaml` 指向远程 PostgreSQL；需用户确认后在 `backend/` 执行 `go run ./cmd/migration -config-dir .`。完整 `go test ./internal/auth/service -count=1` 在本机仍被 sqlite/CGO 阻塞。
## 我的收藏营养编辑按钮
- 小程序“我的收藏”页面已将原详情按钮改为营养编辑按钮：`apps/wechat/src/packageExtra/pages/recipes/index.tsx` 使用 `iconfont icon-edit`，点击后从底部弹出编辑面板，可修改总热量、蛋白质、碳水、脂肪，并通过 `updateUserRecipe` 保存。
- `apps/wechat/src/packageExtra/pages/recipes/index.scss` 已补充底部弹窗样式，并让收藏卡片总热量数字与 `kcal` 单位使用同一行 nowrap 展示。
- 运行时验证未完成：9420 无微信开发者工具自动化服务，3001 是 SSH 监听/转发，`mrc where` 均无法连接。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 我的收藏营养编辑联动补充
- 按用户补充要求，收藏营养编辑弹窗已改为热量主控：总热量支持直接输入与 +/- 10 kcal 步进。
- 联动规则：输入蛋白质/碳水/脂肪任一项时，总热量按 `蛋白质*4 + 碳水*4 + 脂肪*9` 回算；输入或步进总热量时，按当前宏量营养素热量占比同比例缩放蛋白质、碳水、脂肪。
- 修改文件：`apps/wechat/src/packageExtra/pages/recipes/index.tsx`、`apps/wechat/src/packageExtra/pages/recipes/index.scss`。
- 运行时验证仍阻塞：9420/3001 均无法连接微信开发者工具自动化服务。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 我的收藏微量营养素展示与缩放
- 收藏卡片已增加微量营养素摘要展示，最多展示 4 项存在值；营养编辑弹窗展示全部存在的微量营养素。
- 保存联动规则已同步到 `user_recipes.items`：`calories/protein/carbs/fat` 按各自总量变化比例缩放；膳食纤维、糖、钠、钾、钙、铁、镁、锌、维生素等微量营养素按总热量变化比例缩放，模拟今日餐食修改弹窗里摄入比例变化对营养素的联动关系。
- 修改文件：`apps/wechat/src/packageExtra/pages/recipes/index.tsx`、`apps/wechat/src/packageExtra/pages/recipes/index.scss`。
- 运行时验证仍阻塞：9420/3001 均无法连接微信开发者工具自动化服务。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 我的收藏营养编辑折叠与滚动修复
- 收藏营养编辑弹窗已改为固定头部/底部、正文区域 `ScrollView` 可滚动，解决微量营养素过多时无法滚动的问题。
- 宏量营养素和微量营养素都新增折叠/展开开关；默认宏量展开、微量折叠。
- 修改文件：`apps/wechat/src/packageExtra/pages/recipes/index.tsx`、`apps/wechat/src/packageExtra/pages/recipes/index.scss`。
- 运行时验证仍阻塞：9420/3001 均无法连接微信开发者工具自动化服务。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 我的收藏营养编辑滚动实现调整
- 按用户要求，营养编辑弹窗内部不再使用 `ScrollView`；改为普通 `View` + `overflow-y: auto`，对齐首页今日餐食修改弹窗的滚动实现。
- 修改文件：`apps/wechat/src/packageExtra/pages/recipes/index.tsx`、`apps/wechat/src/packageExtra/pages/recipes/index.scss`。
- 运行时验证仍阻塞：9420/3001 均无法连接微信开发者工具自动化服务。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 收藏营养保存 500 修复
- 用户反馈 `PUT /api/recipes/:recipe_id` 保存收藏营养时报 500。
- 根因是后端 `RecipeService.Update` 通过 `map[string]any` 更新 `items` JSONB，GORM map update 不会自动应用 `serializer:json`，本轮新增的 `items` 更新触发失败。
- 已修复 `backend/internal/recipe/service/recipe_service.go`：更新 `items` 时先 `json.Marshal`，再用 `datatypes.JSON` 写入。
- 需要用户重启本地后端后复测；本轮未自动重启服务、未运行测试。

## 身高选择器右手操作布局
- 按用户要求，注册后引导/健康档案里的身高选择器已调整为数字显示在左侧、可滑动身高尺子在右侧。
- 修改文件：`apps/wechat/src/components/HeightRuler/index.tsx`、`apps/wechat/src/components/HeightRuler/index.scss`。
- 样式同步把刻度线、十位数字标签和绿色中心指示线镜像到右侧边缘，确保右侧轮播/尺子视觉与交互位置一致。
- 静态验证：`git diff --check -- apps/wechat/src/components/HeightRuler/index.tsx apps/wechat/src/components/HeightRuler/index.scss` 通过。
- `npm --workspace apps/wechat run typecheck` 仍失败，报错在 day-record、feedback、food-library、pet、private-chat、record-manual、result-text 等既有文件，未指向本次修改文件。
- 运行时验证仍阻塞：9420 无微信开发者工具自动化服务；3001 是 SSH 监听而非小程序自动化端口。未启动/重启开发服务，未执行 `npm run build:weapp`。

## 提交推送与 dev 后端部署
- 已将当前工作区相关改动提交到 `dev` 并推送远端，最终提交为 `f744f25 feat: optimize health profile nutrition editing`。
- 推送前远端 `dev` 有新提交，已通过 rebase 合入远端 `dcea6db`，仅 `memory/2026-06-17.md` 有冲突，已保留双方记录后继续。
- 验证通过：`go test ./internal/recipe/...`、`go build ./...`、`git diff --check`。
- `npm --workspace apps/wechat run typecheck` 仍因既有小程序 TS 错误失败，未指向本次修改文件。
- 已在 `dev` 分支执行 `npm run push-docker-ccr`，成功构建并推送后端镜像 `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev`；构建平台 `linux/amd64`，Go builder `docker.io/library/golang:1.26.1-bookworm`。
- 部署端按既有自动更新脚本预计约 5 分钟内拉取 dev 镜像并更新服务。

## dev 合并到 main
- 按用户要求将 `origin/dev` 合并到 `main`，目标是让主分支包含身高选择器右手布局、我的收藏营养编辑和后端收藏 JSONB 保存修复。
- 合并前本地 `main` 已先快进到远端 `origin/main` 最新提交 `6b1113d`；`origin/dev` 最新为 `0a44708`。
- 本次合并无代码冲突，差异范围为身高选择器、收藏页营养编辑、recipe service/test 和当天 memory。

## 2026-06-17 Mobile App UI 组件库与系统弹窗风格讨论
- 用户指出 App 登录页协议提示弹窗为 Android 原生样式，按钮显示 `OK` 且不符合食探绿色品牌风格。
- 复核发现 `apps/mobile/package.json` 未引入完整 RN UI 组件库；当前移动端主要依赖自有 `theme`、`AppButton`、`Card`、`Page`，并大量直接调用 `react-native` 的 `Alert.alert`。
- 建议优先实现项目内统一 `AppDialog` / `AppToast` / `AppActionSheet` 并逐步替换 `Alert.alert`；如要引入现成 UI 库，优先评估 `react-native-paper`。
## 完成：Admin 举报详情直接删除圈子内容
- 确认原 admin 举报详情页只能更新举报工单状态或删除举报记录，不能直接删除被举报的圈子动态；“状态”确实只是 `feed_reports.status`。
- 后端新增 `POST /api/admin/feed-reports/:report_id/delete-target`，仅支持 `target_type = circle_post`；删除时复用圈子动态业务删除路径，清理对应点赞、评论和互动通知。
- 删除目标内容后，未完结举报会自动更新为 `resolved`，默认处理说明为“已删除被举报的圈子内容。”，并沿用系统消息通知举报双方。
- Admin 详情页危险操作区新增“删除被举报的圈子内容”按钮；原“删除举报记录”保留，二者分开。
- 验证通过：`go build ./cmd/server`、`go test ./internal/admin/service -run '^$' -count=1`、`go test ./internal/admin/handler ./internal/admin/repo -run '^$' -count=1`、`npm --prefix admin run build`、`git diff --check`（仅 LF/CRLF 提示）。完整 `go test ./internal/admin/...` 仍因当前 CGO/sqlite 环境失败，与本次改动无关。
- 本轮未自动重启前后端服务，未执行数据库迁移。

## 2026-06-17 Mobile App 全局弹窗 Provider
- 新增 `apps/mobile/src/providers/DialogProvider.tsx`，提供 `useAppDialog()`、`dialog.alert(title, message, kind)`、`dialog.confirm(options)` 和底层 `showDialog(options)`。
- 弹窗类型支持 `info`、`success`、`warning`、`danger`；按钮支持单确认按钮和确认/取消双按钮；危险确认使用 `AppButton` 新增的 `danger` variant。
- `apps/mobile/App.tsx` 已在 `AuthProvider` 内挂载 `DialogProvider`；`apps/mobile/src/screens/LoginScreen.tsx` 已把登录页协议提示、手机号/验证码校验、验证码发送成功/失败和登录失败提示从系统 `Alert.alert` 切到全局弹窗。
- 验证通过：`npm --workspace apps/mobile run typecheck`；`git diff --check` 仅有既有 LF/CRLF 提示。
- MuMu + Expo Go + ADB 验证：刷新 `127.0.0.1:5555` 后点击未勾协议的微信登录，弹出 App 自定义弹窗，显示「请先同意协议」和按钮「知道了」，不再出现 Android 系统 `OK`。截图：`tmp/app-dialog-login-agreement.png`。

## 完成：Admin 举报处理奖励积分
- 确认此前 Admin 举报详情没有“举报核实后发放奖励积分”的逻辑；小程序/移动端只有赚积分、举报、反馈相关入口和文案。
- `feed_reports` 新增 `reward_credits` 与 `reward_ledger_id`，并同步到 `backend/internal/migration/do/schema_do.go` 和 `backend/internal/migration/migration.go` 的幂等迁移。
- `PATCH /api/admin/feed-reports/:report_id/status` 在处理为 `resolved` 时必须传入 `reward_credits`，允许为 `0`；大于 0 时同一事务写 `user_earned_credit_ledger`，reason 为 `feed_report_reward`，source_key 为 `feed_report:<report_id>`，并更新举报人 `earned_credits_balance`，重复请求不会重复发放。
- Admin 举报详情页新增“奖励积分”输入框，选择“已处理”时可编辑，已处理后显示最终奖励值；删除被举报圈子内容的快捷操作默认奖励为 0。
- 验证通过：`npm --prefix admin run build`、`go build ./cmd/server`、`go test ./internal/admin/service -run '^$' -count=1`、`go test ./internal/admin/handler ./internal/admin/repo -run '^$' -count=1`、`git diff --check`（仅 LF/CRLF 提示）。
- 本轮未自动启动、停止或重启本地服务，未执行数据库迁移。
