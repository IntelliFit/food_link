# 2026-06-03

## 新用户默认头像与昵称

- 新增 `scripts/prepare_default_avatar.py`：将 `src/assets/default_avatar.png` 中心裁剪并缩放为 256×256 JPEG（`src/assets/default_avatar.jpg`），可选 `--upload` 上传至 COS `user-avatars/_system/default_avatar.jpg`。
- 后端 `login_service.go` 新用户注册时写入 `Avatar=_system/default_avatar.jpg`、`Nickname=微信用户_{6位随机数字}`。
- 前端登录页在仍需完善资料弹窗时预填默认头像与昵称；保存时本地默认图会先上传 COS。微信 `chooseAvatar` 逻辑不变。
- 已上传 CDN：`http://cdn-food-user-avatars.coachlink.fit/_system/default_avatar.jpg`

## 首页今日餐食 · 手动记录缩略图

- 根因：手动记录保存未带 `image_path`；dashboard 只读记录级图片字段。
- 修复：`record-manual` 保存时汇总所选食物图；`dashboard_service.collectFoodRecordImagePaths` + `home_repo.LookupManualSourceImagePaths` 回查 `public_library`/`packaged_food`。
