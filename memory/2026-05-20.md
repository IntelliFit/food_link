
## 2026-05-20 - backend slog logging migration

- Replaced backend zap logging with `pkg/logger` based on `log/slog`.
- Added `log` config defaults and `backend/config.yaml` block: level, format, output, file_path.
- `internal/app.New` now initializes logger via `logger.Init`; Gin default `gin.Logger()` is replaced by `logger.RequestLogger()`.
- Existing zap fields in backend app/taskqueue/worker/analyze/response/expiry/health logging paths were migrated to `slog` attrs.
- Removed `go.uber.org/zap` dependency from go.mod/go.sum.
- Added AGENTS.md logging rules adapted from the referenced AgentRuler backend, but did not modify the referenced project.
- Verified relevant backend compile/tests and no zap/Gin default logger references remain.

## 2026-05-20 - backend log messages switched to Chinese

- User required all log message text to be Chinese.
- Updated current backend slog messages and standard-library command logs from English to Chinese across logger access logs, app startup/worker lifecycle, task queue, analyze service, response error logging, expiry recognizer, stats insight, migration, and nutrition backfill.
- Kept structured field names, status enums, metric labels, and machine-readable command output keys in English to preserve filtering and script compatibility.
- Verified with targeted `rg` scans for English-starting log messages and trace events, focused backend compile tests, `pkg/logger`/`pkg/config` tests, and `git diff --check`.
