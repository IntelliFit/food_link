# 2026-05-20

- 用户反馈手动记录保存后，当天饮食记录页展开明细里白米饭、水煮蛋显示 `0 kcal`，蛋白/碳水/脂肪也都是 0，但餐次总热量 `347.7 kcal` 正确。已定位根因：Go 后端 `FoodItem.UnmarshalJSON` 同时用嵌入 `Alias` 和同名 `NutrientsRaw json:"nutrients"` 解析，导致手动记录请求里的 `items[].nutrients` 没有稳定进入强类型 item 字段。已改为独立解析 `FoodItem.Nutrients` 和 nested `water_ml/waterMl`，并给旧手动记录增加读取兜底：已知常见食物按每 100g 营养补回，其他旧手动 item 按餐次总营养和重量比例兜底。验证：foodrecord domain/service 定向测试和 handler/app 编译级测试通过；`git diff --check` 通过（仅 CRLF warning）；weapp-devtools 3001/9420 仍无法连接，未能截图。

- 用户要求手动记录页顶部工作台做小调整：六个餐次不要横向滑动，改成每排 3 个、两排展示；同时移除参考产品里的 `复制记录 / 快速记录 / 扫条形码` 那排入口，因为那不是 food_link 当前能力。已将餐次区域改为 `.meal-grid` 三列网格，并清理快捷入口 DOM/样式。验证：页面 eslint、目标 diff-check 通过；`rg` 确认无上述三个文案残留；weapp-devtools 3001/9420 仍未连接，未能截图。

- 用户否定上一版手动记录页优化，明确指出搜索仍失败、“全部”只显示几十个、默认内容仍乱，要求先参考成熟 App 的手动记录 UI/UX 思路重新思考，不要直接改代码。用户给出的参考方向包括：顶部搜索与辅助入口、左侧分类导航、右侧食物列表、底部购物车式批量选择、餐次切换、克重/份量调整弹层。新的判断是：当前核心问题不是 UI 微调，而是缺少产品级 food catalog，不能把 `food_nutrition_library` 和公共食物库原始数据直接裸展示给用户。下一步应先从当前数据库筛出常用食物，做展示名、分类、别名、默认份量、排序权重和搜索词治理，再重构页面。

- 用户反馈圈子页「本周打卡排行榜」前三个头像大小不一致。已定位原因：排行榜预览项会给当前登录用户加 `is-me`，原 SCSS 把当前用户头像从 `64rpx` 放大到 `96rpx`，并放大昵称和次数字号。已改为当前用户仅用浅色描边、阴影和文字高亮，不再改变头像宽高或字号。验证：目标 `git diff --check` 通过（仅 CRLF warning）；stylelint 被项目现有 SCSS 解析问题 `src/pages/community/index.scss:3 Unknown word .home-page` 阻塞；weapp-devtools 3001/9420 均连接失败，未能截图/交互验证。

- 用户确认圈子相关资质已通过，要求恢复底部导航「圈子」入口。已撤回此前审核避险的临时 `保质期` tab：`src/app.config.ts` 恢复 `pages/community/index` / 「圈子」；`custom-tab-bar/` 第四项恢复 `community` 和 `icon-quanzi`；`MAIN_TAB_ROUTES` 恢复 `/pages/community/index`；删除临时 `src/pages/expiry/` 主包包装页；首页/我的/保质期编辑/旧 food-expiry 兼容页恢复分包保质期页面跳转；好友页空态恢复「去添加好友」并切到圈子。验证：相关 eslint、custom-tab-bar JS 语法、目标 diff-check 均通过；`src` 已是圈子 tab，但当前 `dist/app.json` 和 `dist/custom-tab-bar/index.js` 仍是旧的保质期 tab 产物，开发者工具若未重新生成 `dist` 仍会显示旧底栏。weapp-devtools 3001/9420 均连接失败，未能截图/交互验证。

- 用户继续反馈手动记录页虽然有数据了，但界面丑、没有分类、默认把随机复杂餐食和英文标准库条目排在前面，搜索“饭/米饭”也不可靠，整体需要滑很久。已重做手动记录页信息架构：前端不再按来源大块竖排，改为横向餐次胶囊、来源筛选、分类筛选和统一紧凑推荐列表；默认最多展示 18 个、搜索最多展示 40 个，减少滚动。后端 `ManualFoodResult` 新增 `category`，标准库默认排序改为中文常见食物和米饭/鸡蛋/面条/鸡胸肉等高频项优先，英文/USDA 条目降权；公共库默认限制超高热量复杂餐靠前；搜索加入最近常吃，并把“饭”扩展到“米饭/白米饭/rice/cooked rice”。验证：相关 Go 定向测试、repo/service/app 编译级测试、前端 eslint、diff-check 均通过；weapp-devtools 3001/9420 端口仍未连接，未能截图/交互验证。需要重启 Go 后端并让小程序吃到最新 dev 产物后生效。

- ??????????????????????????????????????????????????????????????? `18 kcal / 0.9g / 2.7g / 0.5g`???????????????? `0 kcal / 0g / 0g / 0g`????????1???????? `PageMeta` ? `home-page--modal-open` ??????????????? `overflow` ?? `hidden`????????? `catchMove`?2????????? food item ? item ??????????????????????????? `record.total_calories / total_protein / total_carbs / total_fat` ????? item????????????????3??? `backend/internal/foodrecord/service/food_record_service.go` ? `positiveNumberFromAny(...)` ??? `json.Number` ??????????????????????????????????????????????? `TestFillMissingNutrientsAcceptsStringNumbers` ?????????`npx eslint src/pages/index/components/MealRecordEditModal.tsx src/pages/index/index.tsx --max-warnings 0`?`go test ./internal/foodrecord/service -run "TestFillMissingNutrientsAcceptsStringNumbers" -count=1`??? `git diff --check` ????? CRLF warning????????????? weapp-devtools ?????????????????????????????????

- ?????????????????????????????1????????????????????????2?????????????? `0 kcal / 0g`?3??????????????????????????????????????????? `getFoodRecordById(recordId)` ????????????????? dashboard ????`MealRecordEditModal` ?????? `intake/weight` ???? `ratio/intake`???????? 0??????????????????????????????????????????? `catchMove` ?????????????????`npx eslint src/pages/index/components/MealRecordEditModal.tsx src/pages/index/index.tsx --max-warnings 0` ????? `git diff --check` ???? CRLF warning????????? `weapp-devtools` ???????`mrc where --port 9420` ? DevTools ?????????????????????

- 用户测试新增轻量模式时，页面报错 `doubao responses api error 404`，上游 JSON 为 `ToolNotOpen / Your account has not activated web search`。结论：轻量模式已正确调用火山方舟 Responses API + 官方 `web_search` 工具，失败点是当前火山方舟账号尚未开通 `Web Search（联网内容插件）`，不是请求格式或图片输入问题。已修复错误清洗：Doubao Responses 客户端把 `ToolNotOpen` 归一为 `doubao web search tool not activated`；worker 落库前转中文提示“轻量模式需要先在火山方舟控制台开通 Web Search 联网内容插件；开通后再重试，或先切回标准模式”；分析 loading 页也加同样兜底，兼容历史 raw 错误。验证：`go test ./internal/analyze/service -run "TestDoubaoClient_AnalyzeWithImagesWebSearch" -count=1`、`go test ./internal/worker -run "TestSanitizeTaskErrorMessage" -count=1`、`go test ./internal/app -run '^$' -count=1`、`npx eslint src/packageExtra/pages/analyze-loading/index.tsx --max-warnings 0`、目标 `git diff --check` 均通过；weapp-devtools 自动化端口 3001/9420 均未连接，未能截图/交互验证。

- 用户澄清刚提供的是火山方舟联网搜索插件 key，不是普通 Doubao key。已新增独立配置项 \\doubao_web_search_api_key\\：普通/标准 Doubao 继续使用 \\doubao_api_key\\，轻量模式 Responses Web Search 使用 \\doubao_web_search_api_key\\。本地 \\ackend/config.yaml\\ 已写入该字段且被 \\ackend/.gitignore\\ 忽略，不提交真实 key；代码和 \\config-example.yaml\\ 只保留空字段。验证：config 定向测试、analyze service 轻量模式定向测试、app smoke 和 diff-check 通过。

- 用户确认宠物养成方向：不仅分享打卡有积分，好的饮食习惯也应加分；宠物需要有实际形象，而且每个人的宠物形象不同。当前建议的最简单 MVP 是“固定物种 + 随机外观种子 + 少量状态/装扮变化”，不先做复杂 AI 生成或高成本动画。用户首次进入时生成 `pet_seed`，由 seed 决定颜色、花纹、配饰、默认性格/称呼；成长复用现有积分/等级体系，分享打卡、按时记录、蛋白达标、喝水达标、热量不过量、三餐完整、运动记录等都可加成长值。首页先展示宠物形象、一句状态话术、今日成长进度和 1-3 个小任务。
- 用户复测轻量模式后出现 `doubao responses api error 401 / AuthenticationError / API key format is incorrect`。结论：用户从 Web Search 插件页面拿到的短 key 不是 Ark API Key，不能作为 `Authorization: Bearer` 使用；官方 Web Search 仍应使用普通火山方舟 Ark API Key，只是该 Ark API Key 所属账号必须已开通联网内容插件。已清空本地 `backend/config.yaml` 的 `doubao_web_search_api_key`，让轻量模式复用 `doubao_api_key`；该字段仅保留为另一个有效 Ark API Key 的可选覆盖。同步新增 Doubao Responses 401 / AuthenticationError 的前后端错误清洗。验证：worker sanitize、app smoke、loading eslint、diff-check 通过。
- 用户确认放弃官方 Web Search 轻量通道，要求前端只保留“标准”和“精准”。已移除分析页模式切换中的“轻量”按钮；`normalizeAvailableExecutionMode` 不再把 `lite` 作为可用前端模式；loading、历史、结果页将历史 `lite` 任务按标准模式展示/处理。后端 `lite` 兼容暂未删除，避免历史任务或调试路径崩掉。本轮验证：相关前端 eslint 通过，`rg` 确认食物分析前端无“轻量模式/轻量”可见入口残留，diff-check 通过；weapp-devtools 自动化端口 3001/9420 均无法连接，未能截图/交互验证。

- 已做首页宠物原型：先用纯 CSS 绘制宠物，不引入图片资产；最初放在日期后、热量卡前，用户反馈会把“今日餐食”挤下去。随后改为右侧悬浮形态，不再占首页文档流；展开时显示宠物形象、名字、一句话、成长值和今日任务，点击“收起”后变成小圆宠物泡泡，点击泡泡可展开，收起状态保存到本地 storage。外观已从固定 4 套扩充为独立组合：颜色 8 种、体型 4 种、配饰 8 种、花纹 5 种、名字 12×12 种，理论组合约 184,320。当前好习惯分数先按热量未超、蛋白接近目标、喝水达到 60%、有运动记录粗略计算。验证：首页 eslint 通过，目标 diff-check 通过（仅 CRLF warning）；运行态按用户口径由用户手动验证。

- 已实现“宠物养成与离线惊喜 MVP”：新增 Go 后端 `backend/internal/pet/` 模块、迁移 DO 和路由 `GET /api/pet/summary?date=YYYY-MM-DD`、`POST /api/pet/events/:event_id/claim`。首次 summary 自动创建宠物档案，外观采用统一物种 + seed 组合；健康行为每日评分给宠物经验；summary 为前一天生成一次 `offline_review` 离线复盘事件；领取奖励通过现有 `weapp_user.earned_credits_balance` 和 `user_earned_credit_ledger` 发放系统池积分，source_key 为 `pet_offline_reward:<event_id>`，幂等防重复领取。首页悬浮宠物已接入云端 summary，失败时降级到本地原型；展开态展示名字、等级/成长、事件或今日状态、任务和领取入口。验证：`go test ./internal/pet/... ./internal/app -run '^$|TestSummaryCreatesStablePetAndSingleOfflineEvent|TestClaimEventIsIdempotent' -count=1`、`npx eslint src/pages/index/index.tsx src/utils/api.ts --max-warnings 0`、目标 `git diff --check` 均通过（仅 CRLF warning）。用户确认后已在 `backend/` 执行 `go run ./cmd/migration -config-dir .`，返回 `migration completed: config_dir=. schema=public`；只读核验当前项目配置连接的 `154.8.205.78:5432 / food-link / public` 确认三张宠物表、三个关键唯一索引和相关约束均存在；后端进程仍需重启才会提供新增接口。

## 2026-05-20 — 食物分析三模式实验重映射

- Task: 用户要求重新做三模式测试：普通模式直接用豆包中等思考且不混合；精准模式使用当前普通 hybrid 链路但豆包中等思考；旧精准多阶段链路改名为试验模式。
- Status: implemented_static_verified_runtime_blocked_migration_pending
- Change:
  - 后端 `standard`：普通模式，强制豆包图片识别，`reasoning_effort=medium`，不触发 Gemini hybrid review。
  - 后端 `strict`：新精准模式，走当前普通 hybrid 链路，即豆包中等思考初识别 + Gemini/Ofox 复核名称/OCR/重量 + db_first 营养回算。
  - 后端 `experimental`：旧精准多阶段链路，提交时创建 `precision_plan`，后续分项估重、聚合；继续会话也使用 `experimental`。
  - 前端分析页恢复三模式入口：普通 / 精准 / 试验；只有试验模式显示参考物设置。
  - loading、历史、结果页统一显示普通/精准/试验，避免旧精准多阶段链路继续显示为精准。
  - 会员计费：`strict` 仍需要精准模式会员权限，但按普通分析成本；`experimental` 按旧精准分析成本和权限。
  - 数据库迁移代码已允许 `weapp_user.execution_mode`、`precision_sessions.execution_mode` 写入 `experimental`，但尚未运行迁移；本机 `backend/config.yaml` 指向 `154.8.205.78`，需要用户确认目标库后再执行。
- Verification:
  - `go test ./internal/analyze/service -run "TestNormalizeExecutionMode|TestBuildPrompt|TestAnalyzeService_AnalyzeImageGeminiAliasRoutesToDoubao|TestAnalyzeService_AnalyzeImageStandardIgnoresConfiguredGeminiProvider|TestAnalyzeService_AnalyzeImageStandardUsesDoubaoForExplicitDoubao|TestAnalyzeService_AnalyzeImageStandardForcesDoubaoInsteadOfGemini|TestAnalyzeService_AnalyzeImageStandardHybridUsesGeminiWeightReview|TestAnalyzeService_AnalyzeImageStandardHybridFallsBackToDoubaoWhenGeminiFails|TestAnalyzeService_AnalyzeImageLiteUsesDoubaoResponsesWebSearch|TestBuildStandardImageHybrid|TestBuildAnalyzeResponse|TestDoubaoClient_Analyze" -count=1` passed。
  - `go test ./internal/membership/service -run "TestMembershipService_ValidateFoodAnalysisCredits" -count=1` passed。
  - `npx eslint src/utils/api.ts src/utils/execution-mode.ts src/utils/membership.ts src/packageExtra/pages/analyze/index.tsx src/packageExtra/pages/analyze-loading/index.tsx src/packageExtra/pages/analyze-history/index.tsx src/packageExtra/pages/result/index.tsx --max-warnings 0` passed。
  - `git diff --check -- ...` passed（仅 CRLF warning）。
- Runtime verification:
  - 已按 weapp-devtools 尝试 `mrc where --port 3001` 和 `mrc where --port 9420`，均因微信开发者工具自动化端口未连接失败，未能截图或交互验证。
- Next step:
  - 需要用户确认是否对当前 `backend/config.yaml` 指向的数据库执行迁移：`cd backend && go run ./cmd/migration -config-dir .`。
  - 需要重启后端和刷新/重新编译小程序后，三模式实验链路才会在本地运行态生效。

## 2026-05-20 — 普通模式补强包装食品召回

- Task: 用户反馈同一张混合零食图中实际有 4 个食物，但新普通模式（纯豆包 medium）总是只识别 3 个；新精准模式（hybrid）能识别 4 个。
- Finding:
  - 日志显示普通模式 `hybrid_review.status=skipped`、`strategy=doubao_db_first`，所以本次完全由豆包单模型决定 item 数量，db_first 只负责按已有 3 个 item 查营养库，不会新增漏掉的食物。
  - 漏项高概率来自红色包装食品被绿色包装遮挡，豆包把它当成包装/背景而不是独立食物。
- Fix:
  - `backend/internal/analyze/service/analyze_service.go` 的普通/精准 hybrid 前置 DB-first 图片 prompt 增加包装食品召回规则：包装食品、袋装食品、盒装食品、被部分遮挡但仍明显是食品包装的对象必须作为独立食物项输出；包装本身不输出，但包装代表的可食内容要输出。
  - Prompt 增加区域扫描要求：左侧、中央、右侧、下方、背景/被遮挡处逐一扫描，独立食物或独立食品包装要单独列项。
- Verification:
  - `go test ./internal/analyze/service -run "TestBuildPromptStandardMode|TestBuildPromptStrictModeUsesDBFirstPrompt|TestAnalyzeService_AnalyzeImageStandardForcesDoubaoInsteadOfGemini|TestAnalyzeService_AnalyzeImageStandardHybridUsesGeminiWeightReview" -count=1` passed。
  - `git diff --check -- backend/internal/analyze/service/analyze_service.go backend/internal/analyze/service/analyze_service_test.go` passed（仅 CRLF warning）。
- Next step:
  - 需要重启后端后，用普通模式重新测试 `零食和日常吃的混合测试案例.jpg`，观察 item_count 是否从 3 提升到 4。

## 2026-05-20 — `/api/pet/summary` 500 初步排查

- Task: 用户贴出 Gin 日志片段：`GET /api/pet/summary?date=2026-05-19` 返回 500，栈尾停在 `github.com/gin-gonic/gin.(*Engine).ServeHTTP`，随后 `/api/membership/me` 正常 200。
- Finding:
  - 用户贴出的日志缺少 Gin recovery 栈最上方的 panic message / SQL error，无法只凭栈尾确认最终异常。
  - 宠物 summary 在首次或跨日打开时会写入 `user_pets`、`user_pet_daily_scores`、`user_pet_events`，并为查询日期的前一天生成离线事件；如果并发重复请求同时创建同一天 `user_pet_daily_scores`，唯一索引 `user_id + score_date` 可能导致其中一个请求失败。
  - 本轮本地 `backend-dev.log` 是 2026-05-16 的旧日志，不包含用户 03:06 的 500 详情；需要用户提供完整 recovery 顶部日志，或在当前运行终端查看 panic 第一行。
- Fix:
  - `backend/internal/pet/repo/pet_repo.go` 的 `CreateDailyScore` 改为 `ON CONFLICT (user_id, score_date) DO NOTHING`，并返回是否真实插入。
  - `backend/internal/pet/service/pet_service.go` 在冲突未插入时回查既有 daily score，避免把并发冲突当作新建成功，也避免重复发放宠物经验。
- Verification:
  - `go test ./internal/pet/... ./internal/app -run '^$|TestSummaryCreatesStablePetAndSingleOfflineEvent|TestClaimEventIsIdempotent' -count=1` passed。
- Next step:
  - 需要重启当前 Go 后端后修复才会生效。
  - 如果重启后仍出现 500，第一优先级查看 Gin recovery 顶部的 panic/error 行，重点确认是否是缺表/约束/唯一索引/外键错误。
- 用户要求将豆包图片识别调整为高图片细节、低思考强度。已在 Doubao Chat 图像输入 image_url 中加入 detail=high，在 Responses input_image 中加入 detail=high，并将 Doubao Chat 请求体 reasoning_effort 从 medium 改为 low。验证：go test ./internal/analyze/service -run "TestDoubaoClient_Analyze" -count=1 通过；目标 diff-check 仅 CRLF warning。
- 新增 Gemini 3.5 Flash 食物识别通道：后端增加独立 `external.gemini35_api_key/base_url/model` 配置和独立客户端；新增 `execution_mode=gemini35_flash`（单次 Gemini 3.5 Flash 直接识别，2 积分）与 `execution_mode=gemini35_flash_grouped`（Gemini 3.5 Flash + 最多 2 组分组复核估重，4 积分且需标准/进阶权限）。前端分析页新增 `G3.5`、`G3.5分组` 入口，loading/历史/结果/健康档案默认模式显示已兼容。当前 `backend/config.yaml` 的 `gemini35_api_key` 仍为空，需用户填完整 key；数据库 check 约束代码已更新但未迁移。验证：相关 Go 定向测试、app smoke、前端 eslint、diff-check 通过。

- G3.5 分组模式已改为轻量版试验模式分工：第一阶段 Gemini 3.5 Flash 只做识别规划，锁定食物清单、位置、OCR、候选名称和最多 2 组分组；第二阶段基于第一阶段清单专门估可食部重量，默认不得新增、删除或改名。后端新增融合逻辑，最终名称/顺序/groupId/识别证据优先第一阶段，estimatedWeightGrams/waterMl/suggestedRatio/weightEvidence 优先第二阶段。日志 meta 新增 plan_items、weight_items、final_items、plan_groups，方便判断错误来自识别规划还是重量估计。验证：两组 analyze service 定向测试通过，目标 diff-check 通过（仅 CRLF warning）。

- 普通模式 Doubao 识别耗时偏高后，已将 Doubao 图片细节从显式 `detail=high` 回退为 API 默认细节：Chat 图片输入不再带 `image_url.detail`，Responses `input_image` 不再带 `detail`；`reasoning_effort` 仍保持 `low`。验证：`go test ./internal/analyze/service -run "TestDoubaoClient_Analyze|TestDoubaoClient_AnalyzeWithImagesWebSearch" -count=1` 通过，目标 diff-check 通过。需要重启后端后生效，复测时看 `food image analyze llm completed` 的 duration 是否下降。

- db_first 营养库未命中补全已从 Gemini 切到 DeepSeek v4 Flash：`applyDBFirstNutrition` 现在调用 `estimateNutritionWithDeepSeek`，复用现有 `DeepSeekNutritionEstimator.Estimate`；默认模型从 `deepseek-v4-pro` 改为 `deepseek-v4-flash`；补全成功后的 `nutrition_source` 和写库 source 改为 `deepseek_generated`，日志/metrics 改为 `deepseek_*`。验证：相关 analyze service/DeepSeek 定向测试和 diff-check 通过。重启后端后生效。

- 用户要求食物分析只保留普通/精准两模式：普通改为 `gemini-3.1-flash-lite`（复用 Gemini3/Ofox API），精准改为独立 API 的 `gemini-3.5-flash` 非分组；豆包和其它通道前端暂时隐藏。已实现后端路由和前端可见入口收敛：`standard` 走 Gemini 3.1 Lite，`strict` 走 Gemini 3.5 Flash 单次；旧 `gemini35_flash/gemini35_flash_grouped` 映射到精准，旧 `lite/experimental` 映射到普通；取消普通/精准主链路 Gemini 失败回退 Doubao。验证：相关 analyze service 定向测试、前端 eslint、diff-check 均通过；weapp-devtools 3001/9420 未连接，无法截图/交互。需要重启后端并让小程序吃到最新产物。

- 用户指出不能把模型名称直接写在前端。已将分析页普通/精准模式、等待页提示和积分标签中的 `Gemini 3.1 Flash Lite` / `Gemini 3.5 Flash` 等模型名改成产品文案：普通模式强调快速识别，精准模式强调更细致识别包装文字、小众食物和复杂场景；内部枚举名保留用于旧任务兼容但不展示给用户。验证：相关 rg 扫描只剩内部枚举/代码标识，eslint 和 diff-check 通过。

- 用户最新调整普通模式模型口径：普通模式首轮识别不用 `gemini-3.1-flash-lite`，改回 `gemini-3-flash-preview`；`gemini-3.1-flash-lite` 只作为图片二次纠错候选通道。已改后端路由：`standard` 首轮强制 Gemini 3 Flash，`strict` 继续 Gemini 3.5 Flash 单次，`standard` 图片纠错基于 `correctionItems/previousResult` 走 Gemini 3.1 Flash Lite；同时收紧旧 `image_provider` 偏好，避免内部指定的 Gemini 3 Flash 被历史配置改写回 Doubao。验证：相关 analyze service 定向测试、app smoke、diff-check 均通过。

- 用户建议为了提升零食识别效果，prompt 应重点看配料表和包装袋文字。已强化图片识别 prompt：普通、精准、Lite 纠错和 G3.5 分组规划都明确要求零食/预包装食品优先读取品牌、品名、口味、配料表、营养成分表、净含量、规格和独立小包数量；这些文字证据优先级高于包装正面插画或模型外观猜测。若包装文字/配料表/营养成分表与视觉图案冲突，最终名称优先采用包装文字和配料证据，并在 evidence 中说明。验证：相关 analyze service prompt/图片路由定向测试和 diff-check 通过。

- 用户进一步指出包装袋倒着拍会显著降低识别率，应该提醒用户尽量让包装文字正着拍。已在分析页模式 tips 中补充：普通模式提示“有包装袋时尽量让文字正着、清晰入镜”，精准模式提示“包装袋文字尽量正着拍，配料表清晰会更准”，旧 experimental 文案也提示“包装文字倒着拍会降低识别率”。验证：`npx eslint src/packageExtra/pages/analyze/index.tsx --max-warnings 0` 通过，diff-check 通过；weapp-devtools 3001/9420 仍未连接，无法截图/交互。

- 用户反馈饮食记录“手动输入/手动记录”页没有任何食物，希望这里利用公共食物库和 `food_nutrition_library` 数据。排查确认是前后端协议严重脱节：前端 `record-manual` 页已经按“双库模式”写好，期待 `/api/manual-food/browse` 返回 `recent_items / collected_public_library / public_library / nutrition_library / stats`，并期待 `/api/manual-food/search` 返回 `results` 且查询参数用 `q`；但当前 Go 后端 `backend/internal/utility/*` 还停留在旧 `manual_food_library` 单表实现，只回 `{items:[...]}`，并且只读 `keyword`。这导致页面把旧响应当作空数据，显示“0 条标准食物和 0 条真实餐食 / 暂无可用食物数据”。

- 已重写 `backend/internal/utility/repo/manual_food_repo.go`，不再依赖旧 `manual_food_library`，改为直接聚合 `public_food_library`、`food_nutrition_library`、`food_nutrition_aliases`、`user_food_records.items[].manual_source*`、`public_food_library_collections`。`browse` 现在会返回“最近常吃 / 收藏餐食 / 真实餐食库 / 标准食物库 / stats”；`search` 会把公共食物库和标准营养库统一混排。公共食物库图片也接入 `storageClient` 解析 CDN URL，避免页面拿到裸 key。

- 同步改了 `backend/internal/utility/service/manual_food_service.go`、`backend/internal/utility/handler/utility_handler.go` 和 `backend/internal/app/app.go`：`/api/manual-food/browse` 现在直接回前端正在使用的双库结构；`/api/manual-food/search` 同时兼容 `q` 和历史 `keyword`，返回 `results` 字段；Optional JWT 下会读取 `user_id`，已登录用户才能拿到“最近常吃/收藏餐食”。验证：`go test ./internal/utility/handler ./internal/app -run "TestManualFood|^$" -count=1` 通过，`go test ./internal/utility/repo ./internal/utility/service -run '^$' -count=1` 编译级通过，`npx eslint src/packageExtra/pages/record-manual/index.tsx src/utils/api.ts --max-warnings 0` 通过，目标 diff-check 通过（仅 CRLF warning）。sqlite 运行型测试仍被本机 `CGO_ENABLED=0` 阻塞；weapp-devtools 的 `mrc where --port 9420/3001` 依旧因 DevTools 自动化端口未连通而失败，未能截图或交互复测。需要重启当前 Go 后端后，这个修复才会在小程序手动记录页真正生效。

- 本轮按用户要求整理并提交当前累计改动，准备以单次 checkpoint commit 落库。提交前剔除了一次性数据库探针 `backend/_tmp_check_table.go` 和 `backend/tmp_check_table.go`，避免把临时查表脚本带入仓库；其余宠物 MVP、手动记录目录化、分析模式收敛、首页/饮食记录修复、状态文档更新等改动一并保留。
