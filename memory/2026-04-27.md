# 2026-04-27

- 用户要求把会员免费试用改成分层策略：前 `1000` 名注册用户免费使用 `1` 个月，之后的新用户免费使用 `3` 天，之后都按正常会员价格走。
- 已完成代码修改，未代用户运行：
  - `backend/database.py` 新增 `is_user_in_first_membership_trial_batch(user_id, limit=1000)`，按 `weapp_user` 注册顺序判定是否属于最早 1000 名注册用户；当前注册时间字段会自动识别，不再写死 `created_at`。
  - `backend/main.py` 将统一 `3` 天试用升级为双层策略：前 `1000` 名 `30` 天、其余 `3` 天；`/api/membership/me` 新增返回 `trial_days_total / trial_policy`。
  - `src/utils/api.ts` 补充 `trial_days_total / trial_policy` 类型。
  - `src/pages/pro-membership/index.tsx`、`src/pages/profile/index.tsx` 已按试用策略展示“首批用户免费 1 个月”或“新用户免费 3 天”。
  - `backend/database/membership_plan_config_seed.sql` 注释口径已同步更新。
- 同日继续实现会员奖励体系，未代用户运行：
  - 新增 `backend/sql/add_membership_reward_system.sql`，包含 `user_invite_referrals` 与 `user_credit_bonus_events`
  - `backend/main.py` 的 `/api/friend/invite/accept` 现会记录邀请绑定；`/api/membership/me` 现返回奖励积分拆分；新增 `POST /api/membership/rewards/share-poster/claim`
  - `backend/database.py` 现支持：首次有效使用激活邀请奖励、每日奖励积分汇总、海报奖励领取
  - `src/pages/record-detail/index.tsx` 现会在记录拥有者成功生成海报后自动尝试领取当日 `+1` 积分
  - `src/pages/pro-membership/index.tsx` / `src/pages/profile/index.tsx` 已补奖励文案与“奖励 +x”展示
- 当前奖励实现口径：
  - 邀请人和被邀请人：被邀请人完成 `1` 次有效使用后，连续 `3` 天每天 `+5` 积分
  - 有效使用：当前采用“成功保存 1 次饮食记录”或“成功写入 1 条运动记录”
  - 分享海报：记录拥有者成功生成海报后，每日最多 `+1` 积分
  - 月度防刷上限：当前默认 `10` 个有效邀请 / 月
- 待用户自行验证：
  - 首批用户账号访问 `/api/membership/me` 时应返回 `trial_days_total=30`
  - 非首批新用户应返回 `trial_days_total=3`
  - 会员页与“我的”页应显示对应试用文案，而不是统一“3 天试用”
  - 通过邀请码进入的新用户，在成功保存 1 次饮食记录或运动记录后，`/api/membership/me` 的 `invite_bonus_credits` 应从 `0` 变为 `5`
  - 当天第一次成功生成自己的分享海报后，`/api/membership/me` 的 `share_bonus_credits` 应变为 `1`

- 用户继续指出线上会员数据口径很乱，要求先止血：
  - 已把 `/api/dev/toggle-test-membership` 收口为默认 `404`，并从 OpenAPI schema 隐藏；只有显式设置 `ENABLE_DEV_MEMBERSHIP_TOGGLE=1` 才能恢复
  - 已把会员 `pending` 清理逻辑从“清当前用户所有 pending”修正为“只清会员类套餐 code 的 pending”，避免误伤同表里的 `points_recharge`
  - 清理旧 `pending` 时会保留原订单 `extra`，只追加 `expire_reason / expired_at / superseded_by_order_no`
  - 当前仍未做全库历史 `pending` 一次性离线清扫，这部分后续可再补 SQL 或后台脚本

- 同日继续落地会员数据治理 SQL，未代用户运行：
  - 新增 `backend/sql/review_membership_data_20260427.sql`
    - 只查不改，用于先看真实有效会员、paid 用户、试用用户、假会员候选、补偿候选、历史 pending
  - 新增 `backend/sql/fix_membership_data_20260427.sql`
    - 用于取消假会员、给“异常丢资格的 paid 用户”补 1 个月会员、把超过 1 天的历史会员 pending 批量改成 expired
  - 当前补偿口径继续按“异常 paid 用户”收口：只有最近一次 `paid` 理论上还没到期，却失去会员资格的用户，才纳入补 1 个月范围；正常自然过期用户暂不在本轮补偿里
  - 用户反馈 Supabase SQL Editor 有 `200` 行限制，已追加拆分版脚本：
    - `membership_governance_review_01_summary.sql`
    - `membership_governance_review_02_fake_memberships.sql`
    - `membership_governance_review_03_compensation_candidates.sql`
    - `membership_governance_review_04_pending_orders.sql`
    - `membership_governance_fix_01_cancel_fake_memberships.sql`
    - `membership_governance_fix_02_compensate_existing_memberships.sql`
    - `membership_governance_fix_03_compensate_missing_memberships.sql`
    - `membership_governance_fix_04_expire_stale_pending.sql`
  - 用户实跑后反馈第一个 review SQL 报错：`weapp_user.created_at` 不存在；其真实字段是 `create_time`
  - 已继续修复：
    - 后端 `backend/database.py` 与 `backend/main.py` 的首批试用判定，已补入 `create_time` 兼容
    - 治理脚本 `membership_governance_review_01/02/03` 与 `fix_01` 已直接按 `weapp_user.create_time` 收口，不再做动态探测
    - `membership_governance_debug_weapp_user_columns.sql` 仍保留为兜底诊断脚本，但当前这次无需依赖它
  - 已新增 `docs/public-schema-字段明细版-2026-04-27.md`，按业务域沉淀 public schema 的字段明细；并更新 `docs/数据库实库Schema分析报告.md` 增加入口与 `create_time` 校正说明

- `2026-04-28` 用户根据名单继续收口会员治理范围：
  - 只取消 4 个指定假会员：`ec6b7cf7... 凣凣尜尜`、`eb0d0221... 草！我要干俄挺`、`b4846126... kk`、`d8c5807f... 条条`
  - 暂不处理其余 2 个假会员候选
  - 暂不对 `ikura` 做补偿会员
  - 已新增 `backend/sql/membership_governance_fix_20260428_cancel_selected_fake_memberships.sql`
  - 已删除前端会员页 `[DEV]` 测试区块、删除 `src/utils/api.ts` 中对应测试 API 封装，并删除后端 `/api/dev/toggle-test-membership` 测试路由

- 用户继续收口食物测试后台：页面上的 `standard / strict` 并不是实际链路，真实差异来自“提示词管理”里的 Gemini 自定义提示词。
- 用户随后明确纠正：不是替换掉 `standard / strict`，而是要在保留这两种模式真实生效的前提下，额外新增 `custom` 自定义模式去绑定提示词管理。
- 已按纠正后的口径完成测试后台代码调整，未代用户运行：
  - `backend/static/test_backend/index.html`：分析体验、批量测试改成三模式并存：`standard / strict / custom`。
  - `backend/static/test_backend/app.js`：只有在 `custom` 模式下才启用 Gemini 提示词下拉，并把 `execution_mode + prompt_id` 一起传给分析/批量接口。
  - `backend/main.py`：`standard / strict` 继续走原有主链路 prompt；`custom` 才按 `prompt_id` 或当前激活提示词执行；若自定义提示词为空才回退 `backend/worker.py::_build_food_prompt`。
  - 测试后台结果区会显示本次实际使用的模式；若是 `custom`，还会额外显示提示词名称，便于做 prompt 对比实验。

- 用户继续指出：提示词管理页右上角那套“模型管理”很乱，真实模型选择应该直接以“分析体验 / 批量测试”页为准。
- 已继续清理页面口径，未代用户运行：
  - 去掉了提示词管理页内的模型切换按钮，不再让页面出现第二套模型选择入口。
  - 提示词管理页说明已改成：这里只维护 `custom` 模式的 Gemini 自定义提示词，实际使用哪个模型以分析体验/批量测试页勾选为准。

- 用户继续反馈：分析体验页勾选了 2 个模型，但下方“AI 分析结果”看起来像只有 1 个模型结果。
- 已定位并修正为前端展示问题，未代用户运行：
  - 根因：摘要区会列出多个模型，但详情区之前只拿 `firstSuccess` 去渲染，所以视觉上像只返回了一份结果。
  - 修复：`backend/static/test_backend/index.html` 与 `backend/static/test_backend/app.js` 已改成“每个模型一张完整结果卡”，会分别展示餐食描述、建议、PFC、吸收率、情境建议和食物明细。
  - 同时把摘要区模型标签从笼统的 `gemini` 改为具体模型名，避免两个摘要卡看起来像重复项。

- 用户继续明确 benchmark 方向：不能只看“总重量偏差”，而应先看食物识别是否对，再看匹配食物的重量误差，并保留总重量误差。
- 已按这个方向把测试后台评测内核升级为 V1，两阶段 benchmark 已落地，未代用户运行：
  - `backend/test_backend/utils.py`：
    - 新增 `food_weight_v2` benchmark 结构
    - 先做 deterministic 食物匹配（`exact / contain / close_equivalent / fuzzy`）
    - 新增指标：`Food Precision / Recall / F1`、`matchedWeightMaeGrams`、`matchedWeightRelativeError`、`matchedWeightScore`、`weightedFoodRecall`、`finalCompositeScore`
    - 匹配食物相对误差按 `|pred-gt| / max(gt, 50g)` 计算，避免小重量食物百分比爆炸
  - `backend/static/test_backend/app.js` + `index.html`：
    - 单图结果卡和批量详情都改成优先展示 `综合分 / Food F1 / 匹配食物 MAE / 匹配食物相对误差 / 总重量误差`
    - 批量页顶部新增按模型汇总的“平均综合分”卡片，方便直接做 prompt / model benchmark
  - 新增单元测试：`backend/tests/unit/test_test_backend_utils.py`
    - 覆盖 `total/items` 两种模式
    - 覆盖 `Food F1 + composite score`
    - 覆盖 `50g` 相对误差下限

- 用户随后明确要求：食物匹配评估“必须基于 DeepSeek”，不能只靠本地规则。
- 已继续把评估器升级为 DeepSeek 主判定，未把密钥写进仓库：
  - `backend/test_backend/utils.py`
    - 新增 `calculate_item_weight_evaluation_with_deepseek(...)`
    - `items` 模式会把 `expected_items / predicted_items` 发给 `deepseek-v4-flash`，让模型只返回结构化一对一匹配关系
    - 最终 `Food F1 / MAE / matchedWeightScore / finalCompositeScore` 仍由代码计算
    - 若 `DEEPSEEK_API_KEY` 缺失或 DeepSeek 异常，会自动退回 deterministic 规则匹配，并把 `evaluatorSource=fallback_local`、`evaluatorError` 回传给前端
  - `backend/main.py`
    - 测试后台单图/批量成功结果的评测入口已改为 `await calculate_item_weight_evaluation_with_deepseek(...)`
  - `backend/static/test_backend/app.js`
    - 结果区新增“匹配评估器”显示，能看出本次是 `DeepSeek evaluator` 还是 `本地规则兜底`
  - 本地实际冒烟已跑通：
    - 用用户会话里提供的 DeepSeek key 做了最小真实调用
    - 样例 `米饭/鸡蛋/西兰花` vs `白米饭/鸡蛋/青菜` 返回 `evaluatorSource=deepseek`，并给出 `米饭↔白米饭`、`鸡蛋↔鸡蛋`、`西兰花未匹配` 的结果
