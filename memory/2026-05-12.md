# 2026-05-12

- 用户要求提交当前代码并完成同步；已在 `backend-refactor-sync-migrate-tencent` 上 rebase 到远端最新 `f4e98c3`，解决状态文件、`TaskService`、分析服务测试和运动服务冲突。
- 已推送业务提交到远端：
  - `2a5654a fix: remove analysis notification subscription`
  - `011ae75 fix: defer credit settlement until task success`
- 合并口径：
  - 保留远端 `task_queue`/内嵌 worker/trace 逻辑。
  - 食物/文字/精准分析提交时只校验积分并写 `credit_usage`，不立即扣累计奖励积分。
  - `GetTask()` 读到 `status=done` 的任务后才用幂等 sourceKey 结算累计奖励积分。
  - 运动任务仍创建后发布 queue，但不在创建成功后立即扣累计奖励积分。
- rebase 后验证：
  - `npx eslint src/packageExtra/pages/analyze/index.tsx src/packageExtra/pages/record-text/index.tsx src/utils/api.ts src/utils/membership.ts src/pages/index/index.tsx --max-warnings 0` 通过。
  - `go test ./internal/membership/service ./internal/membership/repo ./internal/health/service ./internal/expiry/service ./internal/worker ./internal/app ./internal/analyze/handler -run Test -count=1` 通过。
  - `go test ./internal/analyze/service -run 'TestTaskService_SubmitAnalyzeTask_WithImages|TestTaskService_GetTask_SettlesCreditsOnlyForDoneTask|TestTaskService_SubmitAnalyzeTask_Success|TestTaskService_SubmitTextTask_Success|TestTaskService_GetTask$|TestTaskService_EnqueueTaskPublishesQueueMessage|TestTaskService_EnqueueTaskSkipsCompletedTask' -count=1` 通过。
  - `go test ./pkg/config ./internal/taskqueue ./pkg/trace ./pkg/logger -run Test -count=1` 通过。
  - `git diff --check` 通过。
- 小程序运行时验证：`mrc where --port 9420` 与 `mrc where --port 3001` 均无法连接，提示目标项目窗口未开启自动化服务，因此本轮没有截图或交互证据。

- 用户询问 trace 和 log 的关系，并希望关键日志能在 Jaeger 里看到；本轮确认口径：普通 zap log 不会自动进入 Jaeger，Jaeger 主要看 OTel span/event/error。需要 Jaeger 可见的诊断点要显式写 `span.AddEvent`、`span.SetAttributes`、`span.RecordError`。
- 已新增 `backend/pkg/trace/events.go`，提供 `StartSpan`、`AddEvent`、`SetAttributes`、`RecordError`、`DurationMS`；`backend/pkg/logger/logger.go` 新增 `WithTrace(ctx)`，用于给 zap 日志带 `trace_id/span_id`。
- 分析任务队列消息新增 trace context 传播：
  - `backend/internal/taskqueue.TaskMessage` 增加 `TraceContext map[string]string`。
  - memory queue `PublishTask` 注入 W3C trace context；worker delivery 处理时提取 context。
  - 新增 `TestMemoryQueuePropagatesTraceContext` 覆盖 traceparent 传播。
- 关键 Jaeger 事件已覆盖：
  - submit 与 queue publish：`analysis task submitted`、`analysis task queue publish started/completed/failed`。
  - worker delivery 与 claim：`task queue delivery received`、`analysis task claim started/claimed/skipped/failed`。
  - worker process：`task processing started/completed/failed`。
  - food/food_text：开始、LLM 调用、fallback、失败、finalized、complete task。
  - DB-first 营养库 lookup：总数、命中数、未命中数、命中率和简短 item summary。
- 用户要求取消独立 worker；已删除：
  - `backend/cmd/worker/main.go`
  - `scripts/run-worker.cjs`
  - `package.json` 的 `dev:worker`
  - `backend/Dockerfile` 中 `/out/food-link-worker` 构建和 `/app/food-link-worker` 复制。
- 当前保留的是 server 内嵌后台消费者能力，仍由 `config.yaml` 的 `worker.count` 控制；这不是独立进程，适配当前 `task_queue.driver=memory` 的进程内队列。
- 验证通过：
  - `go test ./internal/taskqueue ./pkg/trace ./pkg/logger ./pkg/config -run Test -count=1`
  - `go test ./internal/worker ./internal/app -run Test -count=1`
  - `go test ./internal/analyze/service -run "TestTaskService_EnqueueTaskPublishesQueueMessage|TestTaskService_EnqueueTaskSkipsCompletedTask|TestResolveModelConfig|TestAnalyzeService_AnalyzeImageGeminiAliasRoutesToQwenTemporarily|TestAnalyzeService_AnalyzeImageFallsBackToDashScopeOnGeminiTransientError" -count=1`
  - `go test ./internal/analyze/handler -run TestAnalyzeHandler_SubmitAnalyzeTask -count=1`
  - `go test ./internal/analyze/repo -run "^$" -count=1`
  - `go build -o $env:TEMP\food-link-server-trace-worker-delete.exe ./cmd/server`
  - `node --check scripts/run-backend.cjs`
  - `git diff --check` 仅有 CRLF warnings。

- 用户询问 `worker.poll_interval_seconds`、`worker.task_types` 和 `task_queue` 配置含义，并希望写文档。
- 已新增 `docs/backend-task-queue-worker-config.md`：
  - 说明 server 内嵌 worker、`task_queue` 和 `analysis_tasks` 状态表的职责边界。
  - 解释 `poll_interval_seconds` 当前主要控制 worker tick、保质期通知 DB job 检查和 idle 日志，不是普通分析任务消息延迟。
  - 解释全部 `task_types`，特别标明 `expiry_notification` 是保质期通知检查开关，不是 `analysis_tasks` 队列消息。
  - 解释 `task_queue.driver/buffer_size/topic/brokers/consumer_group`；当前只有 `memory` 可用，`kafka` 是 fail-fast 预留。
- 文档过程中发现并补齐老异步入口的 queue 发布：
  - `health_report`：`backend/internal/user/service/analysis_task_service.go` 创建任务后发布队列消息。
  - `exercise`：`backend/internal/health/service/exercise_service.go` 创建任务后发布队列消息，发布成功后才消费积分。
  - `public_food_library_text`：`backend/internal/publicfood/service`/`repo` 创建审核任务后发布队列消息。
  - `backend/internal/app/app.go` 将同一 `taskQueue` 注入上述服务。
- 新增/更新测试：
  - 健康报告任务创建测试断言发布 `health_report` 队列消息。
  - 运动任务创建测试断言发布 `exercise` 队列消息。
- 本轮验证：
  - `go test ./internal/health/service -run "TestExerciseService_CreateLog|TestExerciseService_ProcessExerciseTask_CreatesLogWithReasoning" -count=1` 通过。
  - `go test ./internal/publicfood/repo ./internal/publicfood/service -run Test -count=1` 通过/no test files。
  - `go test ./internal/user/service -run "^$" -count=1` 编译通过。
  - `go test ./internal/app -run Test -count=1` 通过。
  - `go test ./internal/taskqueue ./internal/worker -run Test -count=1` 通过。
  - `go build -o $env:TEMP\food-link-server-worker-doc.exe ./cmd/server` 通过。
  - `git diff --check` 通过，仅 CRLF warnings。
- 阻塞/已知：
  - `go test ./internal/user/service -run "TestAnalysisTaskService_CreateHealthReportTask|TestAnalysisTaskService_CreateHealthReportTaskNormalizesLegacyURL" -count=1` 被当前 Windows `CGO_ENABLED=0 + go-sqlite3 requires cgo` 阻塞。
- 用户要求优化积分扣除机制：
  - 前端轮询获取识别结果时需要知道当前任务类型和图片数量。
  - 积分应根据任务类型（标准/精准）和图片数量，在任务成功返回结果后扣除，而不是创建任务后立即扣除。
- 已确认：
  - `GET /api/analyze/tasks/:task_id` 返回的 `AnalysisTask` 已包含 `task_type`、`image_paths`、`payload`、`result`。
  - 前端 `analyze-loading` 已使用 task/payload 推断任务类型和执行模式；图片数量可从 `image_paths.length` 获取。
- 已实现：
  - `backend/internal/membership/service/membership_service.go`
    - `ValidateFoodAnalysisCredits()` 支持传入本次计费 units。
    - 标准食物分析成本为 `2 * units`，精准模式为 `4 * units`。
  - `backend/internal/analyze/service/task_service.go`
    - 食物图片/文字/精准提交时仍校验积分是否足够，并把 `credit_usage` 写入任务 payload。
    - 删除创建 `analysis_tasks` 成功后立即扣累计奖励积分的逻辑。
    - `GetTask()` 只在任务 `status=done` 时结算累计奖励积分，sourceKey 保持幂等避免重复扣。
  - `backend/internal/membership/repo/membership_repo.go`
    - 每日系统积分统计只统计 `status=done` 的任务。
    - 新增对可计费成功任务类型的判断，避免 `precision_plan` 这种中间任务被计费。
  - `backend/internal/worker/worker.go`
    - 精准模式把 `credit_usage` 传递到最终 `precision_aggregate`，最终聚合成功才计费。
  - `backend/internal/health/service/exercise_service.go`
    - 运动任务创建后不再立即扣累计奖励积分；成功结果由 `GetTask()` 结算。
  - `backend/internal/expiry/service/expiry_service.go`
    - 保质期识别按图片数量校验/返回成本，且只在识别成功并完成任务后扣累计奖励积分。
  - `src/utils/membership.ts`、`src/packageExtra/pages/analyze/index.tsx`
    - 前端分析页积分不足预检查按当前图片数量计算；首页入口未选图前仍按 1 张预检查。
- 当前计费口径：
  - 标准食物/保质期识别：`2 积分 * 图片数`。
  - 精准模式：`4 积分 * 图片数`。
  - 文字分析：按 1 个 unit。
  - 运动记录：1 积分。
- 验证：
  - `npx eslint src/packageExtra/pages/analyze/index.tsx src/utils/membership.ts --max-warnings 0` 通过。
  - `go test ./internal/membership/service ./internal/membership/repo ./internal/health/service ./internal/expiry/service ./internal/worker -run 'Test' -count=1` 通过。
  - `go test ./internal/analyze/service -run 'TestTaskService_SubmitAnalyzeTask_WithImages|TestTaskService_GetTask_SettlesCreditsOnlyForDoneTask|TestTaskService_SubmitAnalyzeTask_Success|TestTaskService_SubmitTextTask_Success|TestTaskService_GetTask$' -count=1` 通过。
  - `go test ./internal/analyze/handler ./internal/app ./internal/health/handler ./internal/expiry/handler -run 'Test' -count=1` 通过。
  - `git diff --check` 通过。
- 阻塞/备注：
  - `go test ./internal/analyze/service -run 'Test' -count=1` 仍被既有 `TestTaskService_ListTasks` 空指针失败阻断。
  - 微信开发者工具自动化连接 9420 与 3001 均失败，提示目标项目窗口未开启自动化服务；本轮未能完成运行时截图或交互验证。

- 用户要求查看前端目前如何执行积分扣除机制，即每日积分额度是在点击什么按钮、什么接口成功之后扣除。
- 已完成源码静态梳理：
  - 前端不直接扣积分，只调用 `GET /api/membership/me` 获取会员/积分状态并在按钮前做不足拦截。
  - 食物图片分析：
    - 首页 `RecordMenu`「拍照识别/相册上传」只做预检查和选图，不扣。
    - 分析页底部「分析 X 张图片」按钮调用 `submitAnalyzeTask()` -> `POST /api/analyze/submit`；继续精准会话调用 `continuePrecisionSession()` -> `POST /api/precision-sessions/:session_id/continue`。
    - 后端创建 `analysis_tasks` 成功后扣。
  - 食物文字分析：
    - 文字页底部「开始智能分析」按钮调用 `submitTextAnalyzeTask()` -> `POST /api/analyze-text/submit`。
    - 后端创建 `analysis_tasks` 成功后扣。
  - 纠错/重新智能分析：
    - 结果页纠错重分析仍走 `submitAnalyzeTask()` 或 `submitTextAnalyzeTask()`，会按新任务再次消耗积分。
    - 继续精准估计走 `continuePrecisionSession()`，也会消耗积分。
  - 运动记录：
    - 运动页发送按钮或输入框确认调用 `createExerciseLog()` -> `POST /api/exercise-logs`。
    - 后端创建 `analysis_tasks(task_type=exercise)` 成功后扣 1 积分。
  - 保质期拍照识别：
    - 保质期编辑页「识别并预填」调用 `recognizeManagedFoodExpiryItems()` -> `POST /api/expiry/recognize`。
    - 后端识别成功并完成任务后扣 2 积分。
  - 后端机制：
    - `MembershipService.ValidateFoodAnalysisCredits()` / `ValidateExerciseCredits()` 负责校验。
    - 标准食物分析 2 积分，精准分析 4 积分，运动记录 1 积分，保质期识别 2 积分。
    - 每日系统积分使用量主要从 `analysis_tasks.payload.credit_usage.system_by_date` 和任务类型统计，不是前端直接写扣减字段。
    - 累计奖励积分通过 `ConsumeEarnedCreditsAfterSuccess()` 写 `user_earned_credit_ledger` 并更新 `weapp_user.earned_credits_balance`。
- 本轮仅静态阅读与解释，未修改业务代码，未运行测试。

- 用户反馈食物过期通知推送失败，要求先找到推送相关前后端实现，尤其是后端。
- 已完成源码静态定位：
  - 前端入口：
    - `src/packageExtra/pages/expiry-edit/index.tsx` 的 `promptExpirySubscribe()`：保存条目后弹订阅确认，调用 `Taro.requestSubscribeMessage()`，再调用后端订阅接口。
    - `src/utils/api.ts`：`EXPIRY_SUBSCRIBE_TEMPLATE_ID`、订阅请求/响应类型、`subscribeManagedFoodExpiryItem()`。
    - `config/index.ts`：通过 `TARO_APP_EXPIRY_SUBSCRIBE_TEMPLATE_ID` 注入 `__EXPIRY_SUBSCRIBE_TEMPLATE_ID__`。
  - 后端入口与 job 创建：
    - `backend/internal/app/app.go` 注册 `POST /api/expiry/items/:item_id/subscribe`，并把 `cfg.WechatPay.ExpirySubscribeTemplateID` 配给 expiry service。
    - `backend/internal/expiry/handler/expiry_handler.go` 的 `Subscribe()` 从 JWT context 取 `userID/openID`，接收 `subscribe_status/err_msg`。
    - `backend/internal/expiry/service/expiry_service.go` 的 `SubscribeWithContext()` 校验订阅状态、openid、模板 ID；`reconcileNotificationJob()` 写 `food_expiry_notification_jobs`。
    - `backend/internal/expiry/repo/expiry_repo.go` 负责 upsert、领取、更新通知 job。
  - 后端发送：
    - `backend/internal/expiry/service/notification_worker.go` 的 `NotificationWorker` 领取 job，获取微信 access_token，并调用微信 `message/subscribe/send`。
    - `backend/internal/worker/worker.go` 与 `backend/cmd/worker/main.go` 负责独立 worker 调度；`task_types` 必须包含 `expiry_notification`。
  - 关键风险点：
    - 前端构建必须注入 `TARO_APP_EXPIRY_SUBSCRIBE_TEMPLATE_ID`；当前 `dist/common.js` 中该值为空字符串，所以当前本地产物会跳过订阅。
    - 后端需要 `APPID/SECRET` 与 `EXPIRY_SUBSCRIBE_TEMPLATE_ID` 或 YAML 对应配置。
    - Docker 镜像包含 `/app/food-link-worker`，但默认入口是 server，生产必须另起 worker 进程。
- 本轮仅定位和交接，未修改业务代码，未运行测试。

- 用户要求把页面里的「当前补录日期」改为「检测到当日能量过低，是否需要补录」，并且点击这个色块后能直接弹出点击卡路里后对应的补录对话框。
- 已实现：
  - `src/pages/index/index.tsx`
    - 旧文案「当前补录日期」已无残留。
    - 补录提示仅在可补录非今日日期且摄入低于目标 60% 时出现。
    - 点击提示色块直接打开 `RecordMenu` 记录菜单弹窗，可继续拍照识别/相册上传/文本输入/手动输入。
  - `src/pages/index/index.scss`
    - 提示块改为可点击色块，增加日期副文案和「去补录」胶囊。
  - `src/styles/fl-color-scheme-dark.scss`
    - 补齐暗色主题样式。
- 验证：
  - `rg -n "当前补录日期|正在补录" src/pages src/packageExtra/pages src/components src/styles -g '*.{tsx,ts,scss}'` 无匹配。
  - `npx eslint src/pages/index/index.tsx --max-warnings 0` 通过。
  - `git diff --check` 通过。
- 阻塞：
  - 微信开发者工具自动化连接 9420 与 3001 均失败，提示项目窗口未开启自动化服务；本轮没有运行时截图/交互验证。
  - `npx stylelint src/pages/index/index.scss src/styles/fl-color-scheme-dark.scss --allow-empty-input` 被既有 SCSS/parser 与旧样式规则问题阻断。

- 用户要求移除食物记录分析识别前的通知订阅授权功能，重点是图片分析页点击分析前不再请求权限，并要求前后端都去掉。
- 已实现：
  - `src/packageExtra/pages/analyze/index.tsx`
    - 删除分析前 `Taro.requestSubscribeMessage()`。
    - 删除分析提交 payload 的 `subscribe_status`。
  - `src/packageExtra/pages/record-text/index.tsx`
    - 同步删除文字分析入口的 `ANALYSIS_SUBSCRIBE_TEMPLATE_ID`、订阅弹窗和 `subscribe_status` 提交，避免协议残留。
  - `src/utils/api.ts`
    - 删除 `ANALYSIS_SUBSCRIBE_TEMPLATE_ID` 常量。
    - 删除 `AnalyzeTaskSubmitParams` / `AnalyzeTextTaskSubmitParams` 的 `subscribe_status`。
  - `config/index.ts`
    - 删除 `TARO_APP_ANALYSIS_SUBSCRIBE_TEMPLATE_ID` 和 `__ANALYSIS_SUBSCRIBE_TEMPLATE_ID__`。
  - `backend/internal/analyze/service/task_service.go`
    - 删除 `SubmitTaskInput.SubscribeStatus` 和 `analysis_tasks.payload.subscribe_status` 写入。
  - `backend/internal/analyze/handler/analyze_handler.go`
    - 删除精准续接接口分析订阅状态字段。
  - `backend/pkg/config/config.go`
    - 删除 `wechat_pay.analysis_subscribe_template_id` 和 `ANALYSIS_SUBSCRIBE_TEMPLATE_ID` env 绑定。
  - 分析接口文档中的请求模型也删除 `subscribe_status`。
- 保留范围：
  - 保质期过期提醒订阅链路没有删除，`EXPIRY_SUBSCRIBE_TEMPLATE_ID`、`FoodExpirySubscribeRequest.subscribe_status` 和 `expiry-edit` 页订阅授权仍保留。
- 验证：
  - 分析订阅关键字 `ANALYSIS_SUBSCRIBE / analysis_subscribe / __ANALYSIS_SUBSCRIBE_TEMPLATE_ID__ / TARO_APP_ANALYSIS` 在 `src/backend/config/package.json/types/tests/docs` 中无匹配。
  - `npx eslint src/packageExtra/pages/analyze/index.tsx src/packageExtra/pages/record-text/index.tsx src/utils/api.ts --max-warnings 0` 通过。
  - `go test ./internal/analyze/handler -run 'TestAnalyzeHandler_SubmitAnalyzeTask|TestAnalyzeHandler_SubmitTextTask' -count=1` 通过。
  - `go test ./internal/analyze/service -run 'TestTaskService_SubmitAnalyzeTask|TestTaskService_SubmitTextTask|TestTaskService_SubmitCorrectionStoresChainRoot' -count=1` 通过。
  - `go test ./internal/analyze/service -run '^$' -count=1` 通过。
  - `go test ./pkg/config -run 'Test' -count=1` 通过。
  - `git diff --check` 通过。
- 备注：
  - `go test ./internal/analyze/handler ./internal/analyze/service ./pkg/config -run 'Test' -count=1` 中 handler/config 通过，但 `internal/analyze/service` 全量测试被既有 `TestTaskService_ListTasks` 空指针失败阻断。
  - 微信开发者工具自动化尝试连接 9420 与 3001 均失败，提示项目窗口未开启自动化服务；本轮没有运行时截图或交互证据。
