# 2026-08-15

## Android 商店正式包构建复验
- 用户准备上架 Android App，询问当前能否生成完整 APK。
- 当前 `main` 工作树干净；`npm run typecheck:mobile` 通过。
- `npm run build:mobile:android:store` 实际重跑成功，Gradle 完成 `assembleRelease`、`bundleRelease`、APK 打包和 AAB 签名。
- 生成 `dist/mobile-store/foodlink-3.0.2-4-release.apk` 与 `dist/mobile-store/foodlink-3.0.2-4-release.aab`；包名 `cn.healthymax.foodlink`，版本 `3.0.2 (4)`，minSdk 24，targetSdk 36，正式 API 为 `https://api.healthymax.cn`。
- APK v2 签名验证通过；AAB 的 `jarsigner -verify` 返回 0。APK SHA-256 为 `33F6AE004FA0BB0DDE807DC493BD7745EED40FF8E49DF70149192B71D463906D`，AAB SHA-256 为 `5AA3BF0F15739527F7D49B552EFAF504BB04B13B5B3808C23BD822007A02BBAC`。
- 当前没有 ADB 设备连接，未安装正式 APK 做真机冒烟；未上传或提交任何应用商店。

## vivo 隐私自动化测试准备
- 用户已在 vivo 开放平台创建应用（管理页 ID `830646`），并打开隐私测试创建页。
- 已填写任务名 `智健食探3.0.2隐私测试`、分类 `医疗健康 / 健康养生`、隐私测试项和 `https://healthymax.cn/privacy`；尚未上传 APK 或提交。
- 官网与 App 内隐私政策已统一更新，覆盖 Android App、账号认证、相机/相册、健康资料与 AI 分析、第三方服务、权利与注销，并署名运营主体。
- App 登录页已恢复账号密码入口，正式 APK/AAB 已重新构建；APK SHA-256 为 `4333801EF976E437A8E87D6DA2F590953B8FECA82B9CA760854081D7FD534B9C`。
- 按用户要求创建全新的明显测试手机号 `16666666666`，账号为最小权限、不可搜索、记录不公开；生产密码登录接口验证通过，凭据记录于 `docs/store-review-account.md`。
- Chrome 扩展未开启“允许访问文件网址”，自动上传 APK 被阻止；已把 `dist/mobile-store/foodlink-3.0.2-4-release.apk` 路径交给用户手动上传。
- vivo 表单仍未提交；账号密码字段需在向 vivo 传输前获得即时确认，最终“提交”由用户点击。
