# 2026-07-17

## 营养库精准匹配方案确认

- 用户确认采用“稳定名称规范 + Top 5 候选 + 小模型快速判断是否完全同一种食物”的两段式匹配。
- 模糊召回本身不授予营养复用资格；只有小模型明确判断候选与食物主体、加工形态、干湿状态、品牌/规格完全一致时，才允许用于本次计算。
- 需要持续补充规范名称与经审核别名，解决视觉模型对同一食物表述不稳定导致 exact 命中率低的问题；AI 新表述不能未经审核直接进入正式别名表。
- 后端链路已实现：direct exact 收口、Top5 Qwen 3.6 Flash 四维身份门禁、`standardized_food_name`、待审 alias candidate、AI 营养 inactive 隔离、包装 fuzzy 禁止自动命中和精确多规格待确认；前端接口保持兼容。
- `go test ./internal/analyze/service -count=1`、`go test ./internal/foodrecord/repo -count=1`、相关 `go vet` 与 `git diff --check` 全部通过。未改生产数据库、未启动/部署服务，需后端部署后线上生效。
