# 2026-06-25

## 985/211 校园食堂采集数据落库完成
- 用户追问此前多子智能体采集数据的位置，并要求把已有数据全部写入数据库。
- 先执行后端迁移到当前 `backend/config.yaml` 指向的远程 PostgreSQL `154.8.205.78:5432/food-link`：`cd backend && go run ./cmd/migration -config-dir .`，创建/补齐校园目录主数据表、导入 5 所 active 样本和 4 个 pending_review 采集批次。
- 5 所 active 样本已入库：北京大学、清华大学、复旦大学、浙江大学、中国科学技术大学；共 24 个 active 校区、41 个 active 食堂。
- 已把 104 所子智能体首轮公开资料采集结果写入远程库，全部为 `pending_review`：340 个校区、1007 个食堂、1233 条来源证据。
- 总覆盖：109 所学校有食堂数据，其中 5 所 active、104 所 pending_review。
- 核验结果：pending_review 学校均至少有 1 个校区和 1 个食堂；校区重复组 0，食堂重复组 0；来源按 `school + campus + canteen + source_url` 重复组 0；来源等级 A=1024、B=176、C=33。
- 本轮只写/复用 `school_campuses`、`school_canteens`、`campus_directory_sources`；未写 `canteen_windows`，当前窗口数为 0。
- 用户端食堂选择器仍应只展示 `active` 食堂；104 所采集候选需在 Admin 后台审核通过后才上线。

## Android real-device APK crash fix
- User enabled wireless debugging on a vivo V2366HA phone. ADB paired successfully with `192.168.8.103:42267` using pairing code `230129`, then connected as `adb-10AE5M0BXY000CN-UVhJ6N._adb-tls-connect._tcp`.
- Real-device logcat reproduced the crash from the installed official APK: `FATAL EXCEPTION: mqt_v_native`, `JavascriptException: ReferenceError: Property 'isDark' doesn't exist`, located at `HomeCalorieCard` inside `HomeScreen`.
- Fixed `apps/mobile/src/screens/HomeScreen.tsx`: child components no longer reference an out-of-scope `isDark`; they use the passed `themeColors`. `HomeExpirySection` and `HomeMiniAction` now accept and apply `themeColors`.
- Verification passed: `npm --workspace apps/mobile run typecheck`, `git diff --check`, and local dry-run release APK build via `python scripts/release_mobile_android.py --build-local-apk --dry-run`.
- Installed the locally built APK `dist/mobile-release/android/beta/0.0.1/4/foodlink-0.0.1-4.apk` onto the real phone with `adb install -r`; install succeeded and preserved data.
- Real-device launch verification passed: app process stayed alive, focus was `cn.healthymax.foodlink/.MainActivity`, logcat after launch and after one homepage interaction showed no `FATAL EXCEPTION`, `JavascriptException`, `ReferenceError`, or `TypeError`.
- Screenshot evidence saved at `tmp/android-phone-foodlink-start.png`.

## PR 27 dev to main conflict resolution
- User reported GitHub PR `dev` to `main` had conflicts.
- Resolved by merging `origin/main` into local `dev`, fixing conflicts in `apps/mobile/src/screens/DetailScreens.tsx`, `apps/mobile/src/screens/HomeScreen.tsx`, and `memory/2026-06-24.md`.
- Conflict policy: keep the lazy native-module loading in `DetailScreens.tsx` so `expo-sharing` and `react-native-view-shot` are only required from user-triggered poster actions, not at App startup.
- Kept the complete `themeColors` styling for `HomeMiniAction` in `HomeScreen.tsx`.
- Verification passed: `npm --workspace apps/mobile run typecheck`, `npm --workspace packages/api-client run typecheck`, `npm --workspace packages/api-client run test -- --runInBand`, and `git diff --check`.
- Created and pushed merge commit `9d32c05` to `origin/dev`. After fetch, `git merge-base --is-ancestor origin/main origin/dev` passed, so PR 27 should no longer report merge conflicts.

## 官方反馈 `/api/friend/blocks` 404 排查
- 用户反馈官方渠道最近失败请求：`GET /api/friend/blocks status=404`，页面“不显示，崩了”。
- 已用无 token 请求复现正式环境问题：`https://api.healthymax.cn/api/friend/blocks` 返回 `404 page not found`；连续命中正式 pod `foodlink-main-7959b8d9c4-222n2/c7qh2/cxz5c` 均为 404。
- 对照检查：
  - `https://api.healthymax.cn/api/friend/list` 返回 `401 缺少 Authorization`，正式服务和老好友接口可达。
  - `https://dev.api.healthymax.cn/api/friend/blocks` 返回 `401 缺少 Authorization`，开发环境已经包含该路由。
  - 本地源码和 `main` 分支 `backend/internal/app/app.go` 已注册 `GET /api/friend/blocks` 及整组拉黑接口。
- 前端好友页初始化用 `Promise.all` 同时拉取好友列表、好友申请和拉黑列表，`friendGetBlocks()` 的 404 会导致整体加载失败，因此“不显示/崩了”是真实可解释的用户影响。
- 结论：不是前端凭空误报；正式后端部署版本缺少 `2886c4b1 feat: add user blacklist` 引入的拉黑接口。建议发布当前 `main` 后端镜像，发布后用无 token 请求复测 `/api/friend/blocks` 应返回 401 而不是 404。

## 最新代码提交推送
- 用户要求提交并推送当前最新代码。
- 提交范围包括：校园食堂三段式选择与会员门槛、校园目录后台审核能力、宠物聊天 Markdown 渲染、官方反馈 `/api/friend/blocks` 404 排查记录。
- 提交前验证通过：`git diff --check`、`cd backend && go build ./cmd/server ./cmd/migration`、`npm --prefix admin run build`、小程序目标文件 ESLint。

## 985/211 校园食堂公开资料首轮采集
- 用户要求继续用多个子智能体采集剩余 985/211 学校校区与食堂，并接入后台管理体系。
- 已按每个子智能体 2-5 所学校分批完成首轮公开资料采集；当前覆盖口径为 5 所 active 样本 seed + 104 所子智能体首轮待审采集 = 109 所。
- 本轮新增完成的末段学校包括：四川大学、西南交通大学、电子科技大学、四川农业大学、西南财经大学、贵州大学、云南大学、西藏大学、西北大学、西安交通大学、西北工业大学、西安电子科技大学、长安大学、西北农林科技大学、陕西师范大学、兰州大学、青海大学、宁夏大学、新疆大学、石河子大学。
- 采集结果全部按 `pending_review` 候选处理；公开资料中 evidence_level 为 low/medium 的学校或食堂需后台人工复核，尤其是校区归属、现营业状态、转载/采购公告来源。
- 未写入数据库：当前后端配置指向远程 PostgreSQL，执行迁移/导入前需要用户明确确认目标库。当前没有运行 `go run ./cmd/migration -config-dir .`，也没有生成完整 `campus_directory_pending_research_seed.json`。
- 已有代码层入口：迁移代码可读取 `backend/data/campus_directory_pending_research_seed.json`，导入校区、pending_review 食堂和来源证据；后台校园食堂管理页可审核草稿。

## 校园食堂当前数据库实际状态复查
- 只读检查当前 `backend/config.yaml` 指向的远程 PostgreSQL：`schools` 表存在且有 2631 条学校数据。
- 校园食堂新主数据表在当前库中尚不存在：`school_campuses`、`school_canteens`、`canteen_windows`、`campus_canteen_applications`、`campus_directory_sources`、`campus_directory_import_batches` 均不存在。
- `public_food_library` 当前仅有旧兼容字段：`is_campus_food/school_name/campus_name/canteen_name/floor/window_name`，尚无 `school_id/campus_id/canteen_id/window_id`。
- 当前库里校园相关旧文本菜品约 10 条，主要为上海交通大学宿舍食堂、中国科学技术大学 1958 咖啡厅/test，以及少量空字段或乱码记录。
- 因此数据库层面“已完成所有校区和每个食堂录入”的学校数为 0；代码侧三段式 UI、用户接口、Admin 管理接口、公共食物库 ID 入参已经完成，但当前库未迁移时不能算线上可用。

## 校园食堂三段式选择与 985/211 采集审核接入
- 已将小程序校园食堂用户端改为严格三段式选择：新增 `CampusPicker`；`CanteenPicker` 改为只接收已选学校和校区，不再默认校区或提供“全部校区”。
- `campus-canteen` 筛选栏新增独立校区选择；学校变更会清空校区/食堂，校区变更会清空食堂，列表请求继续使用 `school_id/campus_id/canteen_id`。
- `campus-food-share` 与 `food-library-share` 校园模式新增独立校区字段，提交和编辑保存前必须绑定已审核食堂，同时必须已有学校和校区。
- 后端迁移 DO 新增 `campus_directory_import_batches`；`campus_directory_sources` 新增 `batch_id/evidence_excerpt/updated_at`；`school_canteens` 新增审核备注、审核人和审核时间，并允许 `rejected` 状态。
- Admin API 新增采集批次 CRUD、食堂草稿列表、草稿通过/拒绝、重复食堂合并；合并会迁移证据来源、申请和公共食物库引用到目标食堂。
- Admin 页面新增“食堂草稿”和“采集批次”标签，证据来源可绑定批次并录入证据摘录；草稿可保存字段、通过上线、拒绝或合并到已有食堂。
- 新增 `backend/data/campus_directory_import_batches_seed.json`，迁移时创建 4 个 985/211 区域采集批次；这些批次只是待审任务，不会把未审核食堂直接上线。
- 验证通过：后端校园目录/公共食物库/迁移定向测试、后端 `cmd/server`/`cmd/migration` 编译、Admin build、小程序目标文件 ESLint、`git diff --check`。
- 小程序全量 typecheck 仍被既有旧错误阻塞：`day-record`、`feedback`、`food-library`。weapp-devtools 仍无法连接：9420 无自动化服务，3001 也无法连接微信开发者工具自动化。
- 未执行数据库迁移；落库前需确认 `backend/config.yaml`/环境变量目标库后执行 `cd backend && go run ./cmd/migration -config-dir .`。未启动/重启本地服务，未运行 `npm run build:weapp`。

## 宠物聊天 Markdown 渲染与空对话建议
- 用户要求宠物聊天页的大模型回答必须渲染 Markdown/基础语法，不再让用户看到 `##`、`-`、`**`、`<u>` 等原始标记；首轮候选话题放到空对话区域，先硬编码。
- 已新增 `apps/wechat/src/packageExtra/pages/pet-chat/pet-markdown.tsx`，作为宠物聊天专属轻量 Markdown renderer，支持标题、段落、列表、加粗、下划线、简单表格兜底和代码块 fence 清理；不使用 RichText 注入 HTML。
- `apps/wechat/src/packageExtra/pages/pet-chat/index.tsx` 中宠物/assistant 消息改为渲染组件展示，用户消息保持纯文本；历史宠物回答也会统一渲染。
- 首轮 `QUICK_QUESTIONS` 增加副标题，并迁移到空对话 starter cards；已有回答后底部只展示 `FOLLOW_UPS` 追问 chips。
- `apps/wechat/src/packageExtra/pages/pet-chat/index.scss` 新增 `.pet-chat-md-*` 与 `.pet-chat-starter-*` 样式，并补齐暗色模式。
- 验证：目标 pet-chat ESLint 通过，`git diff --check` 通过。
- weapp-devtools 运行时验证阻塞：`mrc` 可用；9420 无自动化服务；3001 被 `ssh` 监听且无法连接微信开发者工具自动化。未启动/重启本地服务，未运行 `npm run build:weapp`。

## 校园食堂用户端会员门槛
- 用户要求校园食堂板块只有会员可以使用。
- 已新增 `apps/wechat/src/components/CampusMembershipGate/` 作为会员专属提示组件，加载中仅显示 spinner，非会员展示开通会员入口。
- `apps/wechat/src/packageExtra/pages/campus-canteen/index.tsx` 现在进入页面先刷新会员状态，只有 `is_pro=true` 才加载校园食堂列表和允许筛选、搜索、上传。
- `apps/wechat/src/packageExtra/pages/campus-food-share/index.tsx` 现在非会员不能直接进入校园菜品提交/编辑页面。
- `apps/wechat/src/packageExtra/pages/food-library-share/index.tsx` 的校园模式受会员限制，普通公共食物库分享不受影响。
- 验证：目标文件 ESLint 通过；目标 diff check 通过；小程序全量 typecheck 仍只剩既有旧错误 `day-record`、`feedback`、`food-library`。
- weapp-devtools 仍无法运行时验证：`mrc` 可用，但 9420 无自动化服务，3001 无法连接微信开发者工具自动化。

## 校园食堂主数据与申请审核机制
- 已实现机制优先版本：学校 → 校区 → 食堂 → 窗口稳定 ID 主数据，`public_food_library` 新增 `school_id/campus_id/canteen_id/window_id`，旧文本字段继续保留展示和回填。
- 后端新增校园目录表、状态约束、FK、索引、首批样本 seed、旧校园菜品文本到 ID 的幂等回填；首批样本为北京大学、清华大学、复旦大学、浙江大学、中国科学技术大学。
- 用户侧新增学校校区、校区/学校食堂、食堂窗口、食堂申请接口；公共食物库创建/更新/筛选支持 ID。
- Admin 新增校园食堂管理接口和前端页面，支持学校、校区、食堂、窗口、申请审核、证据来源管理；申请通过可自动创建/绑定食堂，拒绝保存原因。
- 小程序新增 `CanteenPicker`，`campus-canteen`、`campus-food-share`、`food-library-share` 校园模式均改为选择已审核食堂；找不到食堂时提交申请，不直接进入可选列表。
- 验证通过：后端校园目录/公共食物库/迁移定向测试、后端 `cmd/server` 和 `cmd/migration` 编译、Admin build、小程序本轮目标文件 ESLint、`git diff --check`。
- 小程序全量 typecheck 仍因既有旧错误失败：`day-record`、`feedback`、`food-library`。
- 未跑数据库迁移；执行 `cd backend && go run ./cmd/migration -config-dir .` 前需确认目标库。未启动本地服务。
- weapp-devtools 尝试受阻：`mrc` 可用，但 9420 无自动化服务；3001 被 ssh 进程监听且 `mrc where -p 3001` 无法连接微信开发者工具自动化。

## 最新代码提交推送
- 用户要求提交并推送当前最新代码。
- 当前分支：`dev`。
- 提交内容范围：
  - 邀请达标奖励从奖励积分改为 7 天轻度版会员，包含后端会员赠送账本、达标结算、API/类型与小程序/官网文案同步。
  - 食物识别 AI 营养补全提示词强化，DeepSeek 与 Qwen 备用链路明确生成项目 21 项微量/扩展营养字段。
  - 记录了已授权执行的远程营养库 21 项微量全 0 记录补写结果：普通库 25 条、包装库 7 条。
- 提交前验证通过：
  - `git diff --check`
  - `cd backend && go test ./internal/analyze/service -run 'TestDeepSeekNutritionEstimator|TestQwenNutritionEstimator|FinalizeAnalyzeResponseFallsBack' -count=1`
  - `cd backend && go test ./internal/membership/service ./internal/membership/repo ./internal/migration -run 'TestMembershipService_ActivateInvite|TestMembershipService_GetRewardCenterInvite|TestMembershipGrant|TestAutoMigrate|TestMembershipRepo_GrantMembership' -count=1`
  - `cd backend && go build ./cmd/server ./cmd/migration ./cmd/nutrition-backfill`
  - `npm --workspace packages/core run typecheck`
  - `npx eslint` 针对本轮小程序改动文件通过。
- 未启动本地前后端服务，未运行 weapp-devtools；本轮是提交推送操作。

## Admin「包装食品测试」页面 + 营养精度整理
- 新增 Admin 页面「包装食品测试」（`admin/src/pages/packaged-food-test-page.tsx`）与「包装食品识别运行记录」页（`admin/src/pages/packaged-food-test-runs-page.tsx`）。
- 测试页布局与样式完全对齐现有的「包装食品库」管理页：左侧 `sku-list` + `sku-card` + `thumb-strip` 固定高度卡片，右侧 `sku-editor-panel` + `editor-header` + `detail-section` + `form-grid`。
- 默认加载列表后自动选中第一条记录，右侧直接展示详情与可编辑表单。
- 点击「运行识别」跳转到运行记录页并自动触发一次识别；运行记录页展示历次运行的 ID、状态、耗时、Input/Output/Total Tokens、模型，并支持查看完整识别结果 JSON 与上游原始元数据。
- 提供「保存到数据库」按钮通过 `PATCH /api/admin/packaged-foods/:id` 回写，不覆盖 `source_image_urls`。
- 后端新增 `packaged_food_test_runs` 表，新增接口：
  - `POST /api/admin/packaged-foods/:food_id/test-extract`（创建运行记录并执行识别）
  - `GET /api/admin/packaged-foods/:food_id/test-runs`
  - `GET /api/admin/packaged-food-test-runs/:run_id`
- `FoodNutritionService` 新增 `ExtractPackagedProductWithMeta`，Vision client 新增 `AnalyzeWithImagesAndTemperatureMeta`，可采集耗时、模型、token 等元数据。
- 在 `backend/internal/foodrecord/service/packaged_photo_import.go` 中增加营养值统一 round 逻辑：宏量 1 位、矿物质 0 位、维生素 2 位；导出 `RoundPackagedNutritionMap` 供 test-extract 返回使用。
- 验证：后端 `go build ./...`、Admin `npm run build`、新增单测与既有单测均通过。
- 修复包装食品列表缩略图容器溢出问题：为 `.thumb-strip` 增加 `min-height: 0` 和 `overflow: hidden`，为图片增加 `max-height: 100%`。
- 修复按钮文本换行问题：为全局 `button` / `.button-link` 增加 `white-space: nowrap`，「运行识别」「保存修改」等文案不再换行。
- 新增表的数据库迁移尚未执行（配置指向远程 PostgreSQL，需用户确认后执行 `go run ./cmd/migration -config-dir .`）。
- 用户选择自行启动后端与 Admin 服务进行运行时验证；未替用户启动本地常驻进程。

## Admin「包装食品测试」运行时失败/0ms 排查
- 用户反馈：选中包装食品点击「运行识别」后，运行记录页新记录显示「失败 / 耗时 0 ms / Tokens -」，但后端日志显示 `POST /test-extract` 返回 200，耗时约 34 秒，run_id 一致。
- 已补充排查手段：
  - 后端 `RunTestExtract` 成功/失败路径均已输出结构化日志；handler 返回前记录 `run_status`、`duration_ms` 与 tokens。
  - 前端运行记录页新增 `console.log('[test-run] created', data.run)`，POST 返回的 run 立即插入列表顶部并选中；状态标签区分 `成功 / 运行中 / 失败`。
- 已修改文件：
  - `backend/internal/admin/service/packaged_food_service.go`（日志）
  - `backend/internal/admin/handler/packaged_food_handler.go`（返回前日志）
  - `admin/src/pages/packaged-food-test-runs-page.tsx`（状态区分、POST 响应优先展示）
- 验证：`cd backend && go build ./...` 与 `cd admin && npm run build` 均通过。
- 待用户重新 build 并启动后端后，在 Admin 再次点击「运行识别」，核对浏览器控制台 `[test-run] created` 输出与后端 `管理员测试包装食品识别` 日志是否一致。
