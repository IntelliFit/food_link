# 2026-05-29

## 零食库试验模式喜之郎 258g 漏命中修复

- 用户用 `standard_packaged_experiment` 测试“喜之郎Cici果冻爽（橙味）”，图片净含量为 258g，且用户确认该零食已落入 `packaged_food_library`；实际结果仍保留 AI 估重 150g，并走 DeepSeek 普通营养补全。
- 日志显示：试验模式提交成功，视觉模型识别为 `type=snack`，但 DB-first 回算阶段没有出现包装库命中，直接普通库未命中并 DeepSeek 兜底。
- 修复：
  - `SearchPackagedFood` 从完整字符串 `LIKE` 改为 token 检索和排序，拆出 `喜之郎 / cici / 果冻爽 / 橙味 / 橙汁 / 饮料` 等关键词，同时查产品名、品牌、口味、规格、分类、OCR 原文、条码和别名。
  - 模糊命中增加得分阈值，避免低相关候选误套。
  - 试验模式无条码/OCR净含量/用户规格时，不再把 AI 估重作为包装库 product_key 解析的净含量证据。
  - 补充 `喜之郎/Cici/果冻爽/果汁` 触发词，保证这类商品即使模型没标 snack 也进入包装库通道。
  - 新增白盒日志和最终 item 摘要字段，便于下次区分未命中、多规格候选和已应用包装库重量。
- 验证：
  - `cd backend && go test ./internal/foodrecord/repo -run "TestPackagedFoodSearchTermsExtractBrandProductAndFlavor|TestPackagedFoodSearchScoreMatchesCiciOrangeJellyDrink" -count=1` passed。
  - `cd backend && go test ./internal/analyze/service -run "TestShouldResolvePackagedFoodForExperimentInfersCiciJellyDrink|TestModelDeclaredPackagedFood|TestNormalizeExecutionMode" -count=1` passed。
  - `cd backend && go test ./internal/foodrecord/repo -run '^$' -count=1` passed。
  - `cd backend && go test ./internal/analyze/service -run '^$' -count=1` passed。
  - `git diff --check -- backend/internal/foodrecord/repo/food_nutrition_repo.go backend/internal/foodrecord/repo/food_nutrition_repo_test.go backend/internal/analyze/service/analyze_service.go backend/internal/analyze/service/analyze_service_test.go` passed（仅 CRLF warning）。
- 后续：需用户重启 Go 后端后复测同一图片，预期 `estimatedWeightGrams/originalWeightGrams/grossWeightGrams=258` 且 `nutrition_source=packaged_food_library`。

## 喜之郎测试图实测与候选排序修正

- 已按用户要求直接用 `D:\BaiduNetdiskDownload\snack-human-collect\IMG_20260525_041142-测试案例.jpg` 走当前 3010 后端：上传成功并提交 `standard_packaged_experiment`，任务 `37935010-da6d-4933-ae4b-0ed4878e5b5e` 最终仍为 150g、`package_match_status=not_found`、`nutrition_source=deepseek_generated`。
- 用最新代码直连同一 `app-config.yaml` 配置数据库做只读集成测试，`SearchPackagedFood("喜之郎Cici果冻爽（橙味）")` 可以命中库内 `cici果粒爽 橙汁饮料`，净含量 258g。
- 修正候选排序与试验模式候选过滤：口味组成为强信号，橙味查询优先橙汁 SKU，红葡萄/黄桃等其它口味候选不再触发多规格歧义导致保留 AI 估重。
- 验证通过 repo 纯单测、service 候选过滤纯单测、repo 空跑编译测试、service 空跑编译测试，以及 gated PostgreSQL 只读集成测试。当前需要重启后端后再用同图复测运行链路。

## Gemini 3 Flash 零食库重识别工具

- 用户希望用当前 `gemini-3-flash-preview` 对 `D:\BaiduNetdiskDownload\snack-human-collect` 的零食原图重新识别，修复包装库缺重量、多规格和检索召回失败。
- 已实现：
  - `backend/cmd/packaged-photo-import` 新增 `--vision-provider`、`--vision-model`、`--apply-policy`。
  - Gemini 路径使用现有 `OfoxAIClient`，命令需显式传 `--vision-provider gemini --vision-model gemini-3-flash-preview`。
  - 默认 `apply-policy=audit` 强制 dry-run，不写库。
  - 新增 `delta.csv`，按组输出 Gemini 结果与当前包装库记录的差异、候选、冲突和建议动作。
  - 新增 `FoodNutritionRepo.PatchPackagedFood` 用于安全补齐已匹配记录的缺失字段，并重算 `product_key`。
  - `backfill-missing` 只补缺失字段；`replace-high-confidence` 需要高置信和 OCR/规格重量证据才覆盖冲突字段。
- 验证：
  - `cd backend && go test ./cmd/packaged-photo-import -count=1` passed。
  - `cd backend && go test ./internal/foodrecord/repo -run "TestPackagedFoodSearchTermsExtractBrandProductAndFlavor|TestPackagedFoodSearchScoreMatchesCiciOrangeJellyDrink|TestPackagedFoodSearchScorePrefersMatchingFlavor" -count=1` passed。
  - `cd backend && go test ./internal/foodrecord/repo -run '^$' -count=1` passed。
  - `cd backend && go test ./internal/foodrecord/service -run "TestBuildPackagedProductExtractPrompt|TestEvaluatePackagedProductExtract" -count=1` passed。
  - `cd backend && go run ./cmd/packaged-photo-import --help` passed。
- 未执行实际 Gemini 批量识别，避免未确认前产生模型调用成本；建议先跑 20 组 dry-run 审计。
