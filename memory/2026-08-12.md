# 2026-08-12

- Grafana 中文化排查：`grafana.paper2ppt.work` 的 4 个业务仪表盘及首页 Infrastructure Overview 均为 provisioned dashboard，不是 Grafana 默认面板。Chrome 中可临时 Make editable，但保存会报 `Cannot save provisioned dashboard`。已准备中文文案，待用户选择“另存中文副本”或“修改服务器 provisioning 文件并重新加载”。

- 只读检查生产服务器 `154.8.205.78` 的 Prometheus Nginx Basic Auth：入口配置引用 `/etc/nginx/auth/prometheus`，用户名为 `admin`，密码条目为 bcrypt `$2y$` 不可逆哈希，服务器侧没有可直接读取的明文密码。若用户需要恢复访问，应验证候选密码或明确授权重置，不应尝试反推哈希。

- 按用户要求执行小程序 release 构建，`.env.production` 的 `TARO_APP_API_BASE_URL_RELEASE` 为 `https://api.healthymax.cn`。
- Taro production 编译成功生成 `apps/wechat/dist`，但后续 `weapp:size` 检查失败：主包 2220.5KB，超过微信 2048KB 硬限制；当前 release 产物不可直接上传，需继续缩减主包。
- 修复 release 首页白屏：`apps/wechat/src/pages/index/index.tsx` 将 React 默认导入改为命名空间导入，避免 production 分包后默认导入为 `undefined` 导致 `React.useState` 崩溃。TypeScript 检查及 production Taro 编译通过，产物 hook 已绑定为 `taro.reactExports.useState`。
- `weapp-devtools` 的 `mrc` 命令当前不在 PATH，未能完成自动化截图与交互验证；需在微信开发者工具重新编译/清缓存后人工确认首页。
- 排查 release 首页部分 API 固定约 10 秒失败：生产服务器 `ubuntu@154.8.205.78` 负载正常，Nginx 正常，3 个 `foodlink-main` Pod 均运行且无重启；21:02:03 首批 `/api/user/health-profile`、`/api/membership/me`、`/api/membership/reward-center`、`/api/analyze/tasks` 等均返回 200，Nginx 未记录 api.healthymax.cn upstream timeout/502。
- 失败模式与首页冷启动并发请求排队一致：截图前 9 个请求快速完成，队尾重复 `membership/me`、`analyze/tasks`、重复 `health-profile` 在客户端连接层约 10 秒失败；`listAnalyzeTasks` 自身 timeout 已是 20 秒，因此截图的 10 秒不是该 API 业务超时。后续应减少首屏并发、去重相同资源请求，并延后任务列表/奖励/健康档案等非首屏依赖。
- 用户纠正生产归属：`food_link` 的生产 API 是 `https://api.healthymax.cn`；`coachlink.fit` 属于另一个项目，后续禁止将其作为 food_link 的服务器入口。2026-08-12 对 `coachlink.fit` 的 SSH 尝试在主机校验前失败，未执行任何远端命令。
