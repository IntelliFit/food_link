# 2026-08-25 需求文档优化

- 用户提供截图，确认原表准确标题为“foodAI-需求以及bug管理”，要求把自己负责的需求文档写得更详细；当前飞书页面登录账号及截图负责人均为“夏诗淇”。
- 已按截图整理 19 项需求，并统一补充目标、范围、异常态和预期验收标准；优化稿位于 Codex visualization 目录的 `需求文档优化稿-夏诗淇.md`。
- 已通过用户当前打开的 Chrome 标签定位原 Base URL（Wiki token `MRdKw34zEiHrttkQkX4cg3qKnUf`，table `tblnzt1khBv6AHIj`，view `vewMnpNgGD`）。飞书 CLI 授权身份对该资源返回 131006 无权限，但 Chrome 中的夏诗淇账号可正常打开。
- 浏览器写入属于对云端内容的修改，需在实际写入前获得一次明确确认。建议只保留并扩写“需求描述”，不提前填写用于实施后记录的“验收结果”。
- 用户将文案长度收敛为每条约 30 字；已生成 `需求文档优化稿-30字版.md`，保留原详细版作为备份。
- 用户要求降低 AI 文案感；精简版已改为日常团队表达，直接陈述问题和改法，不再堆叠抽象动词与产品术语。
- 用户确认写入，并进一步限定只修改负责人为夏诗淇的记录。CLI 当前用户确为夏诗淇，但目标 Base 的 OpenAPI 资源权限不足；Chrome 扩展链路检查正常，页面交互仍超时，下一步需获准打开新的 Chrome 窗口后按技能流程重试。
- 用户同意新开 Chrome 窗口重试。新页面可加载及截图，但切换视图和网格交互继续超时；未对 Base 写入任何内容。后续需恢复 Browser 插件控制或取得可供 OpenAPI 访问的直接 Base 权限后再执行。
- 用户回复继续后，原标签页与新建标签页仍无法稳定交互；未发生云端写入。后续需要用户提供 `/base/` 多维表格直链，才能尝试由 Base API 解析真实 token 并严格按负责人筛选。
- 对用户再次提供的 Wiki 链接采用 `lark-wiki` 解析仍返回 131006；空间列表检查定位缺少 `wiki:space:retrieve` scope，已发起最小权限授权，等待用户扫码后继续。
- 用户随后取消代写并决定自行填写；确认未修改任何飞书记录，不再继续授权流程。

# 排行榜“可用”食物照片清晰度收紧

- 为 `rankable` 增加真实原图像素审查：短边至少 600px，并检查曝光、极端裁切像素比例、对比度和拉普拉斯清晰度；远景、杂乱背景和手持包装也不再通过。
- 全量处理现有 1,844 张自动候选，保留 1,352、移除 492；普通分类和人工标注未覆盖。数据库已写回。
- 代码提交 `9adccb32` 已推送 `origin/dev`；dev 镜像已推送，digest `sha256:9dda3108b11c1d8fdd3300ca9f54dbc73debb1661df59e4c9fd1dccb8c86674d`。

# 2026-08-25 薄荷健康食物库 0001–1000 导入

- 检查用户提供的 Excel：1000 行，929 行通过核心营养完整性和单位门禁；51 行错误标题“收藏”、20 行缺热量或三大营养素被拒绝。
- development 数据库只读预演：118 行同名已有数据不覆盖，新增候选 811 行。
- 数据库最终存在 811 条 `source=boohee_ui_food_batch_001` 记录，全部为 `unreviewed + inactive`；811/811 核心营养和待审核状态复核通过。
- 新增 `backend/cmd/boohee-food-import` 及单测，命令支持 XLSX 读取、默认 dry-run、事务 apply、审计证据、来源幂等更新和写后验证。
- 后台“停用”表示记录仍在标准库，但 `is_active=false`；本批另为 `unreviewed`，审核前不参与用户端匹配。
- development 日志定位“用户待审”服务器错误：`GET /api/admin/food-nutrition-contributions` 返回 500，根因是 `food_nutrition_contributions` 表不存在（SQLSTATE 42P01）。标准食物列表接口本身返回 200，错误与本批导入数据无关；迁移模型已存在但目标库未落表。
- 用户授权正常项审核后直接通过，并要求把需其本人判断的记录划分好。保守预演与正式写入结果一致：783 条通过并启用为 `reviewed_estimate`，28 条继续停用待审，事务内 811/811 状态与审核证据复核通过。
- 待审 28 条由别名占用、搜索/标题错位、短标题歧义、标题截断和名称过短构成；本批无三大营养素总量或热量明显矛盾项。
- 生成四工作表审核文件 `薄荷健康食物审核结果_0001-1000.xlsx`，完整列出 28 条待审项、783 条已通过项、冲突目标、原因和建议处理。
- 审核功能提交 `96f63f38` 已通过 pre-commit 并推送 `origin/dev`。数据库结果已生效；Docker daemon 未运行，未推送后端 dev 镜像。
- 用户再次强调不要重复上传。复核本批 118 条标准名称重复项均已跳过，新增记录中无 `approved_exact` 别名重复；13 条相似别名只为 `candidate_only/blocked`，继续停用待审。导入器补充正式别名去重门禁，未来命中 `approved_exact` 时也直接跳过。
- 正式别名去重提交 `3a6a14c1` 已通过 pre-commit 并推送 `origin/dev`。
- 14:56:30 用户再次触发 internal server error；最新 development 日志确认仍是 `/api/admin/food-nutrition-contributions` 查询缺表（SQLSTATE 42P01），标准食物列表接口 200。代码已具备表模型和完整迁移，待用户授权对 `154.8.205.78 / food-link / public` 执行定向迁移，不能在可能共享 main 的非本地库上擅自写结构。

# 2026-08-25 食物照片“零食”标签全量复审

- 只读审查当前 1,210 张 `snack` 标注，确认旧分类器以整段文本子串命中，导致鳗鱼饭中的海苔、米线中的花生、汉堡套餐中的薯条/芝士、早餐吐司、正餐坚果酸奶等都误标为零食。
- 已将规则收紧为零食主体判定，并新增 `--audit-label` 与 `--only-label`，相关 Admin repo/service/command 回归通过。
- 最终预演会跳过 3 张人工审核记录；其余 1,207 张自动标注预计保留 425 张 `snack`、移除约 782 张误标。
- 批量 `--apply` 因共享 `public` 数据库风险被门禁拒绝；本轮数据库没有写入。需要用户明确授权这次约 1,207 条自动标注覆盖后再执行和写后复核。

# 2026-08-25 用户端宠物频繁空白诊断

- PNG 兼容修复 `d56ab52c` 只在 `origin/dev`；`origin/main` 仍引用 WebP，正式用户版若从 main 发布则从未收到修复。
- 本机现有微信产物处于半更新状态：`dist/common.js` 引用 8 个 PNG，但 `dist/assets/pets` 只有旧 WebP，8 个 PNG 全部缺失，不能直接上传。
- 前端在宠物资料未返回时主动不渲染；首页请求失败还会清空已成功加载的资料，其他宠物页面也没有最后成功缓存和重试，所以弱网/瞬时接口失败会进一步造成偶发空白。
- 已确认公共请求层会解包 Go 标准响应，不是 API envelope 问题；后端宠物 service 回归通过，正常成功路径会确保并返回宠物资料。
- 微信开发者工具自动化端口未启动，未擅自启动常驻进程，无法核实实际上传包和真机日志。本轮只诊断，无业务代码修改、提交或发布。

# 2026-08-25 用户端宠物空白前端修复（Draft PR #101）

- 已实现按用户隔离的宠物最后成功缓存、一次短延迟重试和失败保留；缓存只保存稳定形象字段，不回放日期状态、事件、奖励或候选操作，退出登录和“我的 → 清除缓存”都会移除。
- `PetAvatar` 的本地内置图片和像素分身加载失败时会退回程序化形象，资源或解码异常也不再显示空白。
- 一次性微信 build 命令新增构建后宠物 PNG 同步和产物校验，开发 watch 继续依赖现有 Taro copy；缺少/损坏任一 PNG 或残留旧 WebP 都会失败。当前 dev `dist` 已补齐 8 个有效 PNG 并清除旧 WebP。
- TypeScript、目标 ESLint、隔离分支全量 Jest 61 套/185 条和宠物资源产物校验通过；Standards / Spec 最终无 Blocker、High、Medium。dev 产物未经过 production 优化，包体积门禁仍报告 main/packageExtra 超限；遵守项目规则未运行 `npm run build:weapp`。
- 3001/9420/9421 无微信开发者工具自动化服务，未进行运行时截图和交互验证。提交 `3ec0b231` 已推送 `codex/pet-avatar-resilience`，目标为 `dev` 的 Draft PR #101 已创建且 MERGEABLE；Cloudflare 两项检查均 SUCCESS。未合并、部署或上传小程序。

# 食物照片零食复审与“可用”标签

- 用户要求再次全量复审零食误标，并新增可供未来排行榜使用的“可用”标签，标准是食物清晰可见且为单一主体，不能是一桌饭菜或杂乱多食物场景。
- 分类器已改为零食主体判断；`rankable` 已接入 Admin 选项和后端保存白名单。抽样发现并补齐三拼/套餐/便当、多配餐、多容器、包装、营养成分表、速溶条包、胶囊补剂等反例。
- 最终开发库 dry-run：10,477 张中保留 7,785、排除 2,686、跳过 6 条人工记录；用户随后明确授权对共享库执行刷新。
- 已向 `154.8.205.78 / food-link / public` 写入/覆盖 10,471 条自动标注，标注表总数 10,477。写后只读回查 `rankable=1,844`、`snack=460`；零食的 3 条差额是受保护人工标注。
- Admin tests/build 与 Go admin service/repo/cleanup CLI tests 通过。提交 `7ceb462e` 已推送 `origin/dev`；Cloudflare Preview 产物已核对包含 `rankable/可用`，开发预览站可见该功能。

# 标准食物重复与图片排序

- 用户截图中的两条“酸菜白肉”实际是 `酸菜氽白肉`（158 kcal）与 `酸菜汆白肉`（78 kcal），由“氽/汆”字形差异绕过规范化名称去重。
- 全库精确/规范化/近似名称审计后，仅该组可确定为重复；其他一字差异记录是不同食材、做法或规格。错字记录无任何引用，已删除并保留规范记录，写后审计重复组为 0。
- 后端列表和搜索查询已改为有图优先，并以 ID 作为稳定尾排序；薄荷导入和 Admin 名称规范化补充 `氽→汆`。新增安全去重 CLI 与回归测试，提交 `b0868e8f` 已推送 `origin/dev`。
- dev 后端镜像已推送，digest `sha256:dc0187ec61d691876b976cebddcf903247563f15c1fb381d3ee00698c741d4f0`；16:15 连续健康检查均为 200，旧实例后缀已退出轮询，自动滚动完成。
- 用户再次强调有图项需排在前面。复验普通列表与关键词搜索两条排序测试通过；当前 `b0868e8f` 镜像重新推送为 `sha256:0bedab4280fdda2105b4dccf61460f758a633805a9e5f036b011e2967ff3da6c`。Keel 因 Deployment 固定旧摘要未自动发现，已手动精确滚动到 revision 157，3/3 副本可用，开发 API 健康检查为 `ok`。

# 标准食物“用户待审”服务器错误修复

- 最新服务日志复现 `/api/admin/food-nutrition-contributions` 500，PostgreSQL 报 `relation "food_nutrition_contributions" does not exist (SQLSTATE 42P01)`；标准食物列表接口同期为 200。
- 新增专用 `food-nutrition-contribution-migration` 命令，默认只读，`--apply` 强制精确校验数据库 host/name/schema；迁移范围仅限待审表、3 个 check、4 个外键、5 个索引及历史 pending 回填。
- 用户授权后已对 `154.8.205.78 / food-link / public` 执行：表由不存在变为存在，3 条历史待审记录回填，7 个约束/外键及 5 个索引全部验证通过。
- 迁移后只读复检：`table_exists=true`、`contribution_rows=3`、`schema_verification_passed=true`。目标 Go 测试、`go vet` 与 diff 检查通过。
- Chrome 后台页面自动接管和新标签页均超时，无法提供 UI 截图；未尝试提取或复用浏览器管理员凭据。数据库查询路径已恢复，后续由管理员页面刷新触发真实已登录请求即可确认展示。
- 修复提交 `27a902d1` 已通过 pre-commit 并推送 `origin/dev`，只包含 4 个定向迁移新文件，未混入工作区其他改动。本机 Docker daemon 未运行，未推送后端 dev 镜像；数据库修复已生效且不要求重启。

# 薄荷健康食物库 1001–2000 导入

- 读取用户提供的 1000 条工作簿，结构与批次标记 `food_batch_002` 正常。核心字段门禁保留 827 条，拒绝 173 条：107 条营养核心字段空白、64 条采集误识别“收藏”、2 条工作簿内重复。
- 开发标准库只读预演跳过 66 条已有数据，其中 64 条标准名称重复、2 条为 `approved_exact` 正式别名（嘎啦果→Apples, gala, with skin, raw；碧根果→Nuts, pecans, halves, raw）。
- 用户授权上传后，以 `boohee_ui_food_batch_002` 事务新增 761 条，写后复核 761/761；未覆盖任何已有标准食物。
- 审核结果为 738 条正常并启用为 `reviewed_estimate`，23 条继续停用待审。待审原因计数：搜索标题错位 7、宏量折算高于标称 5、别名冲突 5、短标题歧义 4、标称热量远高于宏量折算 2。
- 结果工作簿 `薄荷健康食物审核结果_1001-2000.xlsx` 含 5 张工作表；摘要公式总数闭合为 1000，公式错误扫描 0，全部工作表视觉检查通过。本批无仓库代码修改、无需提交或部署。

# 食物营养排行榜泛化肉类与缺图修复

- 只读复现正式 API：蛋白质榜的“牛肉”“鸡肉”均为空图；开发 API 已把鸡肉合并为有图“鸡胸肉”，但“牛肉”仍为空图。根因是 repo 查询把不同具体肉类合并为宽泛 family，并硬编码为泛化展示名，代表项又不要求有图。
- 查询规则已改为排除泛化“鸡肉、牛肉、虾”，只接受鸡胸肉、鸡腿肉、牛腩、牛腱、牛里脊、猪里脊肉、虾仁等具体条目；所有食物营养排行榜候选必须有非空 `image_path`。
- 社区模块全套 Go 测试和 `go vet` 通过；未改前端页面。工作树有多个既有未提交任务，按每日统一提交约定，本轮未提交、未推送、未部署。
