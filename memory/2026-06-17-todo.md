# 2026-06-17 未验证 To Do（明日手动复核）

## 1) 本次餐次自动判断改造（待手动验证）
1. 使用微信开发者工具逐页打开并验证：
   - `/pages/analyze`（拍照入口）
   - `/pages/analyze-loading` 及提交结果跳转链路
   - `/pages/analyze-history`（识别记录进入结果页、快速记录到餐次）
   - `/pages/result`（结果页餐次弹窗默认值）
   - `/pages/result-text`（文本结果页餐次弹窗默认值）
   - `/pages/record-text`（文字记录页）
   - `/pages/record-manual`（手动记录页）
2. 逐条验证“默认逻辑”是否按用户作息（wake/sleep）而非固定时间段：
   - 设置/完善用户作息（`routine_wake_hour`、`routine_sleep_hour`）
   - 在不同时间访问上述入口，观察是否显示对应推断结果
3. 验证作息缺失回退到设备时段逻辑是否正常。

## 2) 本次后端兜底（待手动验证）
1. 调 API（或通过实际保存流程）验证 `meal_type=snack` 在有作息时映射为：
   - 起床后3小时内 → `breakfast`
   - 起床后8小时内 → `lunch`
   - 睡前4小时内 → `dinner`
   - 其余 → `afternoon_snack`
2. 验证作息缺失/无效时仍走历史时段兜底，不阻断保存。

## 3) 历史未验证遗留项（本需求上下文相关）
1. 结果页“可食比例”修复的运行时验证（前端运行效果）
   - `/pages/result` 权重编辑后可食比例保持不误改
   - `/pages/index/components/MealRecordEditModal` 调整重量时可食比例不自动重算
2. 识别记录到主页分享与快速操作链路复测
   - 快速记录后返回状态与首页变化
   - 已记录任务卡片动作正常
3. 验证未清理的历史 weapp 自动化阻塞问题是否已恢复：若 `mrc` 端口可用则优先执行截图 + 导航交互。

