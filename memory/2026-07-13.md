# 2026-07-13

- 只读排查用户提供的牛肉面截图：对应任务 `97b4cf2e-9969-4468-b7e6-7aba995adce3`，执行模式为 `strict_web_search`（精准联网）。任务只有一个“牛肉面”条目，550g 被 DB-first 的精确别名匹配为“瘦牛肉(熟)”（176kcal / 蛋白26g / 碳水0g / 脂肪7g 每100g），直接缩放后得到截图的 968kcal / 蛋白143g / 碳水0g / 脂肪38.5g。
- 根因不是前端展示：混合菜未拆分 + 单一原料别名错配 + 后端没有宏量合理性校验。`strict_web_search` 当前不具备 `strict_separate` 的强制混合菜拆分兜底。
- 最近 1000 个有结果分析任务中，75 个 item 有 `waterMl > estimatedWeightGrams`。当前上游解析和后端水分聚合函数没有统一重量封顶；前端部分路径虽封顶，无法修复服务端/历史脏数据。
- 此轮仅诊断，无业务代码变更；临时只读查询程序已清除。
- 追加只读别名审计：有效别名 11,830 条；混合菜宽口径复核候选 731 条；低碳水错配高风险候选 13 条，人工复核后至少 6 条明显/高度可疑。已确认历史错误任务 15 个（牛肉面 10、红烧牛肉面 5，均命中瘦牛肉）。exact alias 总命中 2,063 item / 1,119 任务只是使用规模，不是错误数。
- `牛肉面 -> 瘦牛肉(熟)` 的别名创建于 2026-04-17；别名表缺少来源/审核字段，无法追溯是人工还是 AI。代码允许 DeepSeek 语义复用后直接写永久 alias，后续修复应改为 AI 只提候选、人工/规则审核后启用，并增加食物层级兼容和宏量合理性门禁。

- 最新付费统计按用户指定的“订单/付费记录”口径（不按用户去重、不区分支付渠道）读取远程 PostgreSQL。初查 `status='paid'` 为 43 条；严格按实际支付完成复核（`paid_at` + 微信 `wx_transaction_id`）后为 **42 条**。多出的 1 条是 2026-05-24 的 0 元 `manual_admin / MANUAL_GRANT` 后台赠送会员，没有支付流水，应排除。42 条累计支付金额 `1232.65 元`，最近完成时间 `2026-07-11 10:39:36 +08:00`。仅执行了只读查询。

- 微信支付自动续费：用户已在商户后台创建 9 个模板（3 档会员 x 月/季/年）。已确认 `214826` 是“食探会员标准版月卡每月自动续费”，对应 `standard_monthly`，19.90 元/月，模式为预扣费通知。生产解约结果 URL `https://api.healthymax.cn/api/payment/wechat/papay/contract/terminate-notify` 于当天实测返回 HTTP 200 和 V2 `SUCCESS/OK`。
- 现有回调只是审核占位，未验签/落库/更新会员；现有小程序自动续费页面同样是审核预览，实际支付仍为普通 JSAPI 一次性购买。正式代扣需补齐纯签约、合同表、预扣费通知、扣款结果回调、解约和小程序状态管理。产品决策为一用户仅允许一份有效自动续费合同，套餐切换先解约再签约。

- 当天后续完成委托代扣的代码接入，状态为 `implemented_needs_migration_config_deployment`。9 模板服务端映射：轻度月/季/年 `214835/214837/214840`，标准 `214826/214838/214841`，高级 `214836/214839/214842`。新增 `wechat_papay_contracts` 表、有效合同局部唯一索引、V2 纯签约和验签回调、预扣费 V3 通知、V2 扣款/解约、内嵌 worker 分钟巡检、会员页开关/签约/状态/取消。
- 生产启用必须先填 Apollo `wechat.pay.api_v2_key`、`wechat.pay.papay_sign_notify_url`、`wechat.pay.papay_pay_notify_url`，确认目标数据库后运行 `go run ./cmd/migration -config-dir .`，部署后用真实测试用户验证。定向 Go/ESLint 已通过；WeApp 自动化端口 3001/9420 未连接，运行时验证受阻。
- 用户随后确认直接迁移远程 `food-link/public`。全量迁移被无关 `food_nutrition_library` 锁阻塞而停止；已新增 `-only-papay` 受限、幂等迁移入口并完成执行，实测合同表和有效合同唯一索引均存在。用户配置的 API v2 密钥、签约回调、扣款回调均经程序解析为非空；本地 `wechat_pay` 内误用的环境变量名也已做兼容。当前仅待用户部署后用真实测试账户完成签约、首扣、解约全链路验证。
# 2026-07-13

- 用户反馈万界方舟切换后宠物对话和 AI 解读报告偶发报错。日志确认 `/api/pet/chat` 和 `/api/stats/insight/generate` 都曾因 `https://maas-openapi.wanjiedata.com/api/v1/chat/completions` 的 `TLS handshake timeout` 返回 503；后续洞察和图片分析可成功，说明是上游偶发连接慢，不是 key/base URL 填错。
- 已修改 `backend/internal/health/service/stats_service.go`：文本客户端 TLS 握手 30 秒、整体 90 秒，对 TLS/网络超时与 HTTP 408/429/5xx 进行一次 300ms 退避重试；这同时覆盖统计洞察与宠物对话。`go test ./internal/health/service -count=1` 与 `go test ./internal/app -run '^$' -count=1` 通过。
- `docs/llm-apollo-migration.md` 已明确 `ai_usage_pricing` 不是 Apollo/API 迁移配置，不应添加。
- 15:34 宠物对话运行时验证：万界方舟第一次连接返回 `EOF`，`StatsService` 自动重试一次后 `POST /api/pet/chat` 于约 30.8 秒返回 200；日志显示成功后才实际扣 1 积分。同期每分钟出现的 `wechat_papay_contracts does not exist` 是未执行微信自动续费数据库迁移导致的独立错误，与大模型调用无关。

- 用户提供万界方舟临时 key，授权全库营养质检和修复。key 只经进程环境变量使用。DeepSeek V4 Pro 批量调用无响应；GPT-5.4 单条可稳定判断 `牛肉面 -> 瘦牛肉(熟)` 为 remove/0.99，但批量请求反复由服务端断连，因此没有用不稳定 LLM 直接写库。
- 营养主表全扫 13,629 条：规则发现 366 条，48 条 severity>=5、0 条 severity>=8，故没有自动修改宏量值。别名全扫 11,837 条、718 条宽候选；确定性备份并事务删除 10 条形态错误别名，回查 11,827 条、硬错误 0。备份：`tmp/nutrition-alias-deterministic-applied-20260713/`。
- 水分全扫 9,198 个分析任务和 6,760 条饮食记录，备份并事务修复 180 个任务、126 条记录、371 个超过 item 重量的字段；postcheck 两表均为 0。备份：`tmp/water-overflow-applied-20260713/`。
- 后端已加入混合主食/单品、低碳主食候选、干粉/冲调形态门禁；AI 语义重排停止写永久别名；水分在分析解析、可食部折算、DB-first、饮食保存/聚合统一封顶。定向 Go 测试和 app 编译通过；完整 SQLite 写入测试受本机无 GCC/CGO 禁用阻塞。未启动或部署服务。

- 用户要求提交当前代码并让 `main`、`dev` 保持一致。当前工作区先提交为 `3787d03f`（`feat: 完成自动续费与营养数据安全防线`），随后将最新 `origin/main` 与 `origin/dev` 合入同一最终树。
- `origin/dev` 合并涉及 194 个文件，9 个冲突已逐项解决：运动仓储测试统一使用嵌入式 PostgreSQL；统计 AI 同时保留非流式重试与宠物对话 SSE；自动续费和礼券注入并存；包装食品使用通用份量缩放并保留两组测试；两侧历史记录均保留。
- 合并语义检查发现 SSE 原实现会丢失自定义 HTTP transport 并绕过重试，已改为复用 StatsService 客户端并对可恢复错误重试一次，新增回归测试。另补齐 benchmark JSONB 更新归一化，修复原有悬空测试；支付同步测试的固定日期改为相对当前时间。
- Verification: `go test ./internal/health/repo ./internal/health/service ./internal/membership/handler ./internal/membership/service ./internal/utility/repo ./internal/app -count=1` 的冲突相关包通过（首次会员固定日期测试失败后已修复并复测通过）；`go test ./internal/admin/repo -run '^TestNormalizeBenchmarkJSONUpdates$' -count=1` 与 `go test ./internal/app -run '^$' -count=1` 通过；小程序 Jest 14 suites / 54 tests 全通过。
- 全量小程序 typecheck 仅有既有 `day-record` 的 `image_path: null` 类型错误。全后端测试因嵌入式 PostgreSQL 初始化耗时和 C 盘空间不足未能完成；已确认一个真实编译错误并修复，其余失败主要为 `There is not enough space on the disk`。微信开发者工具 3001/9420 均未连接，无法完成截图与交互验证。
