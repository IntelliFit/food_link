# 2026-08-06

## FoodLink 延迟治理部署与监控收口

- main 数据库连接池初始化修复已提交并部署：`36721dd4`，镜像 digest `sha256:1c4198aee261c7f570ced93a96faa27b8bbbf74aaca4de39588cc4b8604224e0`。
- dev 同步任务恢复修复 `43cc0b3f` 和连接池修复 `6f497e64`，镜像 digest `sha256:8b51a00623d085970d811f4cf1aba4e21324e12e41370e029ff60a08d268f58d`。
- dev Deployment 从固定旧 digest 恢复为 `foodlink:dev` 标签并完成 revision 131 滚动更新；main/dev 均 3/3 Ready。
- Prometheus 已确认 main/dev `db_max_open_connections` 聚合均为 30，Grafana DB pool utilization 恢复正常。
- dev 约 4.49 万条历史 Kafka backlog 已通过 consumer group offset reset 清理；真实未完成任务由 PostgreSQL 恢复 leader 重新投递。
- main backlog 不做 offset 跳过，避免丢弃线上有效任务，继续由 3 个 partition/3 个 Pod 消费。
- provisioned Grafana dashboard 已更新 `observability/grafana-dashboard-foodlink` ConfigMap：拆分 main/dev Kafka lag、阈值设为 500/2000、趋势图按环境显示，并在 Chrome 验证。
- main 单 Pod 的一次启动重试来自 Apollo Config Service TLS handshake timeout，随后自动恢复。
- 继续观察发现 recovery leader 仍会每 5 分钟重复发布同一批 pending。已补充 30 分钟 recovery cooldown、`updated_at` 候选过滤和成功发布后的时间戳更新：dev 提交 `781ef40f`、main 提交 `eb1ba5c3`，两分支源码及镜像均已推送。
- 既有 13 个小程序 JSX lint 缩进错误已修复，根 package 已增加 `npm run test:e2e` 并指向现有 smoke 场景；lint、typecheck、小程序单测及后端目标 test/vet/build 均通过。
- E2E 实跑仍被远程临时数据库 AutoMigrate 超时阻塞；本轮遗留的两个 `api-test-server` 子进程和对应 `food_link_e2e_*` 临时数据库已全部清理。当前机器未安装可用的微信开发者工具自动化环境，因此未完成小程序运行时截图验证。

## FoodLink 请求延迟排查与第一阶段治理

- 线上排查确认 FoodLink 已使用自建 PostgreSQL/Kafka，不依赖 Supabase。
- dev 环境 Apollo `worker.count` 已从 16 调整为 1；3 个 Pod 的 worker loop 总数从 48 降为 3，空表过期扫描约从 22.83 次/秒降至 0.07 次/秒。
- 后续定位到分析任务恢复循环存在两个放大点：查询使用 `SELECT *` 读取大 JSON/图片字段；每个 Pod 每 30 秒重复恢复同一批任务，持续制造 Kafka 重复消息。
- 源码修复：恢复查询仅选择 `id/task_type/status/lease_until`；Kafka 恢复通过 PostgreSQL session advisory lock 选举单实例执行，最短恢复间隔调整为 5 分钟，memory queue 行为保持原样。
- 针对性测试 `go test ./internal/analyze/repo ./internal/worker ./internal/app`、`go vet ./...` 和 server build 通过。
- 全量 `go test ./...` 有既有失败：本机 `CGO_ENABLED=0` 导致部分 SQLite 测试无法启动，另有 user handler 与 manual food service 的既有断言失败；本次未修改测试文件。
- 下一步：提交并推送 `main`，执行 `npm run push-docker-ccr`，观察自动滚动更新、恢复锁日志、查询耗时和 Kafka lag。
