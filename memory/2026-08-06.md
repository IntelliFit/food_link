# 2026-08-06

## FoodLink 请求延迟排查与第一阶段治理

- 线上排查确认 FoodLink 已使用自建 PostgreSQL/Kafka，不依赖 Supabase。
- dev 环境 Apollo `worker.count` 已从 16 调整为 1；3 个 Pod 的 worker loop 总数从 48 降为 3，空表过期扫描约从 22.83 次/秒降至 0.07 次/秒。
- 后续定位到分析任务恢复循环存在两个放大点：查询使用 `SELECT *` 读取大 JSON/图片字段；每个 Pod 每 30 秒重复恢复同一批任务，持续制造 Kafka 重复消息。
- 源码修复：恢复查询仅选择 `id/task_type/status/lease_until`；Kafka 恢复通过 PostgreSQL session advisory lock 选举单实例执行，最短恢复间隔调整为 5 分钟，memory queue 行为保持原样。
- 针对性测试 `go test ./internal/analyze/repo ./internal/worker ./internal/app`、`go vet ./...` 和 server build 通过。
- 全量 `go test ./...` 有既有失败：本机 `CGO_ENABLED=0` 导致部分 SQLite 测试无法启动，另有 user handler 与 manual food service 的既有断言失败；本次未修改测试文件。
- 部署完成：源码提交 `52c48009` 已推送；`npm run push-docker-ccr` 成功推送 `foodlink:main`，镜像摘要为 `sha256:7c6e2b351a8c42a149f06165294dfb4985ddb262fa57173ec8491d0fb93fa89b`。
- K8s `foodlink-main` revision 122 已完成滚动更新，3 个 Pod 均为 `2/2 Running`、0 重启；只有一个 Pod 获得恢复主锁，两个 follower 不执行恢复发布。
- 首个 5 分钟恢复周期后 Kafka log-end offset 未增加，lag 从 7365 降至 7178；集群内健康检查连续 10 次为 200，耗时约 0.4–1.0ms。
- 后续仍需单独治理 main topic 仅 1 个 partition 与历史 backlog，以及其他慢 SQL；本次仅完成恢复任务负载的第一项源码治理。

## Kafka 主环境分区扩容与 backlog 审计

- 操作前 `food-link-analysis-tasks-main` 只有 1 个 partition；消费组 lag 在审计时约 6140，只有一个 Pod 能获得分区。
- 对历史 offset 54474 起的 100 条消息做非消费式抽样：仅 65 个唯一 TaskID，重复消息 35 条，单任务最多出现 3 次。
- 数据库状态统计显示 `analysis_tasks` 仍有 2434 条 `pending`，因此明确不执行 offset 跳过或直接清空 backlog。
- 已将主 topic 从 1 个 partition 扩到 3 个；未修改 dev topic，未删除消息，partition 0 的历史 offset 原样保留。
- 扩容后生产端已自动把新消息分散到 partition 1/2；为触发 kafka-go consumer group 完整 rebalance，对 `foodlink-main` 做了同镜像滚动重启。
- 重启后 3 个 Pod 各消费一个 partition，均为 `2/2 Running`、0 重启；健康检查连续返回 200，约 0.5–0.7ms。
- 最后观测：partition 0/1/2 lag 分别约 3052/106/107，总 lag 约 3265，offset 均在前进。
- partition 1/2 的短暂停顿来自真实长任务：`precision_item_estimate` 约 51 秒完成；`precision_aggregate` 等待子项约 123 秒后超时失败并继续提交，不是 Kafka 消费组卡死。
- 后续应优化精准模式聚合等待、为 lag 增加告警；在 2434 条 pending 全部核实前，不允许跳过消费 offset。
