# 2026-06-21

## 小程序健康档案保存 500 修复
- 问题：线上 `foodlink-main` 的 `PUT /api/user/health-profile` 返回 500，日志报 `unable to encode map[string]interface {} ... into text format for unknown type (OID 0): cannot find encode plan`。
- 只读确认：`ubuntu@154.8.205.78` 上 `foodlink-main` 3 个 Pod 均 Running 且 0 重启；同一路径 `GET /api/user/health-profile` 返回 200，失败集中在 PUT 保存写库。
- 根因：`backend/internal/user/service/user_service.go` 构造 `updates["health_condition"] = healthCondition`，最终经 `UserRepo.UpdateFields` 的 `Updates(map[string]any)` 写 PostgreSQL `jsonb` 字段；裸 Go map 未包装时 pgx 无法推断 jsonb 编码。
- 修复：`backend/internal/auth/repo/user_repo.go` 新增 `normalizeUserJSONUpdates`，在 `UpdateFields` 中统一将 `health_condition` 裸 map 包装成 `datatypes.JSONMap`；这样健康档案、健康关注、报告任务状态和兼容旧库登录路径都受保护。
- 新增测试：`backend/internal/auth/repo/user_repo_json_updates_test.go` 覆盖裸 map 包装和已包装 JSONMap 兼容。
- 验证：`go test ./internal/auth/repo -run 'TestNormalizeUserJSONUpdates' -count=1`、`go test ./internal/auth/repo ./internal/user/service -run '^$' -count=1`、`go test ./pkg/config -count=1`、`go build ./cmd/server` 通过。
- 限制：完整执行型 `go test ./internal/auth/repo ./internal/user/service -count=1` 在当前 Windows `CGO_ENABLED=0` 环境被既有 SQLite stub 问题阻塞；本轮未重启本地服务、未跑迁移、未部署线上。

## Jaeger 日志关联 trace 补齐
- 用户追问：为什么 Jaeger trace 中看不到错误日志详情，但 `kubectl logs` 能看到容器日志；目标是让业务日志能直接在 Jaeger 对应 trace 里看到，并补齐关键位置日志。
- 线上只读检查：`observability/otel-collector-config` 的 `logs` pipeline 当前是 `receivers: [otlp] -> exporters: [debug]`，没有导出到 Jaeger；Jaeger UI 展示 trace/span/event，因此 stdout 日志和 OTel log signal 不会自然贴到 span 详情中。
- 修复：`backend/pkg/logger/logger.go` 的 slog handler 新增 span event 桥接，所有带有效 span context 的日志都会写入当前 span event，包含 `log.severity`、`log.message` 和结构化字段；直接 `slog.InfoContext` 也被覆盖。
- 修复：`backend/internal/app/app.go` 调整中间件顺序，让 `otelgin` 包在 `logger.RequestLogger` 外层，保证请求收尾日志在 span 结束前写入 Jaeger event。
- 修复：用 `logger.Recovery()` 替换 `gin.Recovery()`，panic 恢复仍返回 500，但 panic 日志通过项目 logger 写入 stdout、OTLP log 和当前 span event。
- 补充日志：将运行时残留的 `fmt.Printf` 失败打印改为项目 logger，覆盖健康档案报告文档/模式切换日志写入失败、饮食记录饮水补齐失败、饮食/运动邀请奖励激活失败。
- 验证：`go test ./pkg/logger ./pkg/trace -count=1`、`go build ./cmd/server`、`go build ./internal/health/service ./internal/foodrecord/service ./internal/user/service ./pkg/logger`、`go test ./internal/auth/repo ./internal/user/service ./internal/foodrecord/service -run '^$' -count=1` 通过；临时 `backend/server.exe` 已清理。
- 限制：尚未部署线上；部署后新的请求 trace 才会在 Jaeger span Events/Logs 中看到业务日志。`internal/health/service` 完整测试仍被既有 mock 接口缺方法阻塞。
## 2026-06-21 - Codex in-app browser Basic Auth limitation
- Status: diagnosed why `https://jaeger.paper2ppt.work/` shows only `401 Authorization Required` in the Codex side browser.
- Finding: the server returns `WWW-Authenticate: Basic realm="Jaeger UI Authentication"`, but the in-app browser surface did not expose the browser-native HTTP Basic Auth prompt; the visible DOM is only the nginx 401 page.
- Workaround: use the user's normal browser for Basic Auth, or switch the protected route to an app-level login / SSO / temporary trusted route when it must be used inside the Codex side browser.

## 2026-06-21 - Grafana HTTP 5xx alerting direction
- User need: avoid manually watching Grafana dashboards; notify Feishu when backend requests keep returning 500/5xx.
- Finding: the Grafana instance exposes Alerting, Contact points, and Notification policies; current contact point list has only the default email receiver.
- Recommended direction: use Prometheus metrics as the alert data source, preferably ingress/app HTTP request counters, and create Grafana/Alertmanager rules for sustained 5xx count or 5xx rate. Use Grafana Webhook contact point to Feishu if custom payload is supported; otherwise add a tiny webhook bridge or use Feishu BotBuilder/Grafana connector. Avoid writing a bespoke polling loop.

## 2026-06-21 - Jaeger 日志正文中文化与关键链路补充
- 用户要求：这轮新增日志不要用英文正文，日志消息要中文。
- 已调整：用户、饮食记录、会员支付/奖励 handler 的新增日志正文改为中文动作描述；`api.action` 仍保留稳定过滤值，结构化字段名保持英文，便于日志平台过滤聚合。
- 已补充：健康档案更新、首页营养目标更新、手机号绑定、头像/封面/健康报告图片上传、健康报告任务提交、饮食记录更新/删除/分享/上传、包装食品库保存、包装营养标签识别、偏差样本保存、会员支付创建/同步、微信支付回调、分享海报奖励领取等成功摘要日志。
- 验证：`go build ./internal/user/handler ./internal/user/service ./internal/foodrecord/handler ./internal/foodrecord/service ./internal/membership/handler ./internal/membership/service ./internal/health/service ./pkg/logger ./cmd/server` 通过；`go test ./pkg/logger ./pkg/trace -count=1` 通过；`go test ./internal/auth/repo ./internal/user/service ./internal/foodrecord/service ./internal/membership/service -run '^$' -count=1` 通过；`go test ./internal/foodrecord/handler ./internal/membership/handler -run '^$' -count=1` 通过。
- 限制：`go test ./internal/user/handler -run '^$' -count=1` 被既有测试中 `NewUserHandler` 参数数量未同步阻塞；`go build ./internal/user/handler` 已通过。尚未部署线上。

## 2026-06-21 - Jaeger 日志中文化收尾
- 已继续按用户要求把 `backend/cmd/server/main.go` 的启动前兜底 fatal 文案从 `load config` / `build app` 改成中文；它们发生在项目 logger 初始化前，不会进入 Jaeger，但 stdout 正文不再是英文。
- `backend/pkg/logger` 已补充无有效 span 的日志处理：`logger.Info/Warn/Error(context.Background())` 会创建轻量 root span 并把日志作为 span event 写入，避免 worker、启动类、后台类项目日志完全无法在 Jaeger 中检索；同时修正 OTel 日志开启时 span event 可能重复写入的问题。
- 已扫描 `backend/internal`、`backend/pkg`、`backend/cmd/server` 中英文开头的 `logger` / `slog` / `RecordError` / `AddEvent` / `fmt.Errorf` / server `log.Fatalf` 文案，未发现本轮新增运行时日志正文仍为英文。
- 验证：`go test ./pkg/logger ./pkg/trace -count=1`、`go build ./cmd/server ./internal/analyze/service ./pkg/logger`、相关 user/foodrecord/membership/health 包 `go build`、repo/service 空跑测试、foodrecord/membership handler 空跑测试均通过；`go test ./internal/user/handler -run '^$' -count=1` 仍被既有 `NewUserHandler` 测试参数未同步阻塞，和本轮日志改动无关。
- 尚未部署线上；部署后新请求和新后台日志才会在 Jaeger trace/span event 或轻量 root span 中出现。

## 2026-06-21 - Jaeger trace event 中文化补充
- 继续复扫 `apm.AddEvent` 后发现分析服务中仍有英文事件名，已把精准模式大模型选择/调用/回退、LLM 重试、文字食物识别、分析任务提交和任务入队事件改为中文。
- 复扫 `apm.AddEvent`、`logger`、`slog`、server `log.Fatalf`、`fmt.Errorf` 的英文开头正文均未再命中；结构化属性值如 `analysis.stage` 仍保留英文枚举，用于机器过滤。
- 追加验证：`go build ./cmd/server ./internal/analyze/service ./pkg/logger`、`go test ./pkg/logger ./pkg/trace -count=1`、相关业务包 `go build` 和 repo/service 空跑测试通过；`git diff --check` 仅有既有 LF/CRLF warning。

## 2026-06-21 - 分析模块微量元素评分与最终评分
- 需求：为分析模块新增默认微量元素评分，并加入最终评分；参考现有宏量/健康指数扣分机制与用户自定义目标。
- 实现：
  - 新增 `backend/internal/nutrition/micro.go`：统一 21 项微量元素配置（key/label/unit/默认值/用户目标 key/上下限类型/权重），提供 `ResolveMicroNutrientTargets`、`ResolveDailyCalorieTarget`、`MicroNutrientMetrics` 等共享函数。
  - 新增 `backend/internal/analyze/service/score.go`：实现 `ComputeMicronutrientScore`、`ComputeMacroBalanceScore`、`ComputeCalorieScore`、`ComputeFinalScore`；微量元素按餐次热量占比折算每日目标，上限型（钠/糖/饱和脂肪/胆固醇）超标扣分，下限型（膳食纤维/维生素/矿物质）不足扣分。
  - 修改 `backend/internal/analyze/service/analyze_service.go`：`AnalyzeInput` 增加 `score_enabled` 开关（默认启用）；`finalizeAnalyzeResponse` 在营养解析完成后调用评分逻辑，响应新增 `score_enabled`、`micronutrient_score`、`macro_balance_score`、`calorie_score`、`final_score`。
  - 修改 `backend/internal/home/service/dashboard_service.go`：复用 `internal/nutrition` 的共享常量，避免与分析模块口径不一致。
  - 测试：`backend/internal/nutrition/micro_test.go`、`backend/internal/analyze/service/score_test.go` 覆盖默认值、自定义目标、上限/下限扣分、宏量平衡、热量、聚合开关等场景；`go build ./...`、`go vet`、相关包测试通过。
- 限制：`backend/internal/analyze/service` 全量测试中存在一个既有失败用例 `TestAnalyzeService_AnalyzeImageStandardIgnoresExplicitDoubaoAndUsesGemini3Flash`（模型选择逻辑，与本改动无关）。未重启本地服务、未部署线上。

## 2026-06-21 - Grafana Feishu HTTP 5xx alert implemented
- Status: completed in the live `observability` namespace and Grafana UI.
- Why an adapter was needed: Grafana 11 webhook contact points can send webhook payloads/headers, but the Feishu custom bot with signing requires Feishu-specific body fields (`timestamp`, `sign`, `msg_type`, `card`). A tiny in-cluster webhook adapter avoids any polling loop and keeps alerting event-driven.
- Kubernetes resources created: Secret/ConfigMap/Deployment/Service named `grafana-feishu-webhook` in namespace `observability`; sensitive Feishu webhook/sign secret/adapter bearer token are stored only in the Kubernetes Secret, not in repo notes.
- Contact point created in Grafana: `feishu-foodlink-alerts` of type `webhook`, pointing to `http://grafana-feishu-webhook.observability.svc.cluster.local:8080/webhook` with bearer auth.
- Alert rule created in Grafana: `Food Link main HTTP 5xx detected`, folder `Food Link`, evaluation group `foodlink-1m`, interval `1m`, pending period `0s`, receiver `feishu-foodlink-alerts`.
- PromQL saved: `sum(increase(food_link_http_requests_total{service_name="food_link-backend-main",status=~"5.."}[1m])) or vector(0)`; the `or vector(0)` fallback is required so a quiet minute returns 0 instead of Grafana No Data.
- Verification: adapter Deployment is 1/1 available, Pod Running with 0 restarts; a signed adapter POST returned Feishu success; Grafana Preview returned value `0` and Normal; Grafana list shows 1 normal rule under `Food Link / foodlink-1m`; copied Grafana SQLite confirmed the saved rule, PromQL, annotations, and receiver.
- Limit: no real production 5xx was intentionally generated, so the alert firing path was not tested by causing a live 500; only the adapter notification path and Grafana rule evaluation/save were verified.

## 2026-06-21 - Grafana Feishu adapter Chinese text fix
- Status: fixed live `observability/grafana-feishu-webhook` adapter after Feishu alert cards showed `??` instead of Chinese labels.
- Cause: the original ConfigMap `app.py` already contained corrupted `??` strings such as `HTTP 5xx ??` and `**??**`; Feishu was rendering exactly what the adapter sent.
- Fix: rewrote the adapter source as ASCII-only Python with Unicode escape string constants, so Kubernetes/SSH/PowerShell transport cannot corrupt Chinese labels during ConfigMap updates.
- Verification: ConfigMap no longer contains literal `??`; Deployment rolled out successfully and is 1/1 ready; a signed adapter test POST returned Feishu success and the new Pod logged `forwarded grafana alert status=firing alerts=1`.
- Note: sensitive webhook/sign secret/bearer token remain only in Kubernetes Secret `grafana-feishu-webhook`.

## 2026-06-21 - feedback 异步日志 trace 关联补充
- 审计剩余运行时异步链路后，明确 `FeedbackService.Submit` 触发的站内信、飞书通知、feedback-bot 同步此前使用 `context.Background()`，日志可通过 root span 在 Jaeger 看到，但不会和原提交请求 trace 形成父子关系。
- 修复：`backend/internal/feedback/service/feedback_service.go` 改为把提交请求 ctx 传入异步动作，并用 `context.WithoutCancel(parentCtx)` 创建后台 child span；这样不随请求取消，业务语义不变，同时异步日志落在原请求 trace 下。
- 补充：站内信发送成功路径新增中文成功日志；通用 `backend/internal/common/handlerlog/handlerlog.go` 的错误正文改为中文模块名映射，避免出现 `food_record 接口失败` 这类半英文可读正文。
- 新增测试：`TestFeedbackServiceSubmitAsyncTraceLinkedToRequest` 验证反馈异步站内信 span 与提交请求 span 属于同一个 trace，且 parent span id 指向提交请求。
- 验证：`go test ./internal/feedback/service ./pkg/logger ./pkg/trace -count=1`、相关业务包 `go build`、轻量空跑测试通过；`go test ./internal/user/handler -run '^$' -count=1` 仍被既有 `NewUserHandler` 测试参数未同步阻塞。

## 2026-06-21 - Jaeger 中文日志桥接线上验证完成

### 状态
- 已完成后端日志进入 Jaeger 的桥接，并已部署到 main 线上后端。
- 本轮新增或改动的业务日志 `msg`、Jaeger event 名称、panic/recovery 日志、启动/后台日志正文均按中文表达；`user_id`、`api.action`、`analysis.stage`、`model`、`source` 等结构化字段名或机器过滤值继续保留英文。
- 原始健康档案保存 500 的根因修复仍在当前代码中：`UserRepo.UpdateFields` 会把 `weapp_user.health_condition` 的裸 map 包装成 `datatypes.JSONMap`，避免 pgx 报 `cannot find encode plan`。

### 线上部署
- 已从仓库根目录执行 `npm run push-docker-ccr`。
- 推送镜像：`ccr.ccs.tencentyun.com/littlehorse/foodlink:main`。
- 当前线上 foodlink-main 三副本已滚动到 manifest digest `sha256:3eaaaafbebee3f4dfe98b3adcc9023924d060ca41c430bff7158b48e24680e54`，Pod 为 `foodlink-main-5cccccc67c-*`，rollout 成功且重启数为 0。

### 验证
- 本地验证：`go test ./internal/feedback/service ./pkg/logger ./pkg/trace -count=1` 通过；`go build ./cmd/server ./internal/analyze/service ./internal/feedback/service ./internal/health/service ./pkg/logger` 通过；`go test ./internal/auth/repo -run 'TestNormalizeUserJSONUpdates' -count=1` 通过。
- 中文日志复扫：`LLM client`、`llm json parse error`、`feedback-bot`、`API Key`、`Base URL`、`内部 benchmark`、`运动热量 LLM`、`AI 营养` 等新改日志正文残留未命中。
- 线上探针：`GET https://api.healthymax.cn/api/user/profile` 未带授权，按预期返回 401，`X-Trace-Id=4461edd83184d50286c02cf542f642fb`，`X-Request-Id=ddbd08ca-6835-4db7-a9a4-60feb716e4eb`。
- Pod 日志确认该 trace 有中文请求日志：`请求完成但返回客户端错误`。
- 直接访问 Jaeger Pod `16686` API 确认同一 trace 的 span `logs` 中存在中文 event 和 `log.message=请求完成但返回客户端错误`。
- 直接查询 Jaeger API `operation=后端服务开始监听`，确认无请求 context 的启动日志也生成 `log.orphan=true` 的 root span，日志 event 为中文。

### 注意
- `observability/otel-collector-config` 的 logs pipeline 仍是 `receivers: [otlp] -> exporters: [debug]`，所以 Jaeger 不是自动展示 stdout/OTel log signal；现在能在 Jaeger 看到日志，是因为应用侧把项目日志同步写成 span event。
- 外部 Jaeger UI 和集群内 `jaeger` Service 的 UI 端口走 Basic Auth/nginx；自动化验证使用 Jaeger Pod 原始 `16686` API 完成。
## 2026-06-21 - 保质期提醒失败日志错误原因补齐

### 状态
- 用户在 Jaeger 看到 `处理保质期提醒失败` 只有标题，没有具体失败原因。
- 根因：`backend/internal/worker/worker.go` 中保质期提醒 worker 调用 `r.errorLog(ctx, "处理保质期提醒失败", nil, ...)`，第三个参数漏传了实际 `err`，导致 Jaeger span/log 里没有 `error` 字段和错误详情。
- 修复：将该调用改为 `r.errorLog(ctx, "处理保质期提醒失败", err, slog.String("worker_id", workerID))`，不改变 worker 处理逻辑，只补齐日志错误原因。

### 验证与部署
- `go build ./internal/worker ./internal/expiry/service ./cmd/server` 通过。
- `go test ./internal/expiry/service ...` 仍被当前 Windows `CGO_ENABLED=0 + go-sqlite3 requires cgo` 既有环境问题阻塞，和本次一行日志修复无关。
- 已执行 `npm run push-docker-ccr` 推送 main 镜像。
- 线上 `foodlink-main` 三副本已滚动到 `sha256:54336d6ea745435cf969c36342ae7fe6691f132cd8a1c67ccf90b3d397b238ab`，Pod 为 `foodlink-main-585d6b5c7c-*`，重启数 0。
## 2026-06-21 - /api/health 请求日志关闭

### 状态
- 用户反馈 Jaeger 列表页只显示 `请求完成`，看不到请求 URL；截图中的这类 trace 是 `/api/health` 因 otelgin 过滤后没有 HTTP span，随后被 logger 的无 span 日志桥接创建成 `log.orphan=true` 的日志 trace。Jaeger 列表页显示 operation 名称，因此只看到 `请求完成`，URL 只在日志字段 `url.path=/api/health` 中。
- 按用户要求关闭 `/api/health` 的控制台请求日志与 Jaeger 日志 trace：`backend/pkg/logger/logger.go` 的 `RequestLogger` 新增 `shouldSkipRequestLog`，对 `req.URL.Path == "/api/health"` 直接 `c.Next()` 后返回，不写 stdout，也不触发 span event/root span。
- `backend/pkg/logger/logger_test.go` 新增 `TestRequestLoggerSkipsHealth`，验证 `/api/health` 不写日志、不创建 Jaeger span。

### 验证与部署
- `go test ./pkg/logger ./pkg/trace ./internal/app -count=1` 通过。
- `go build ./cmd/server ./pkg/logger ./internal/app` 通过。
- 已执行 `npm run push-docker-ccr`，线上 `foodlink-main` 三副本已滚动到 `sha256:1380da5623c96dac694f1f6189b16342ab766996fd9b5ae1885c4cf487679e0e`，Pod 为 `foodlink-main-7999d5756c-*`。
- 线上探针 `GET https://api.healthymax.cn/api/health` 返回 200，响应头仍有项目级 `X-Trace-Id=fad79a9eaafd494c91e3386cd03f2320`，但这是 request-id middleware 生成的响应头，不代表 Jaeger trace。
- 新 Pod 近 6 分钟日志中 grep `fad79a9eaafd494c91e3386cd03f2320`、`/api/health`、`codex-health-log-off-20260621` 均无输出。
- Jaeger 直接查 `/api/traces/fad79a9eaafd494c91e3386cd03f2320` 返回 `trace not found`，确认 health 不再进入 Jaeger。
## 2026-06-21 - 后端请求与数据库阶段日志补强

### 状态
- 已完成并部署 main 后端：除 `/api/health` 外，HTTP 请求现在会在进入时记录 `收到请求`，完成时记录 `请求完成` / `请求完成但返回客户端错误` / `请求失败`。
- GORM 数据库访问已接入项目 logger：带 HTTP/trace context 的请求会记录 `开始查询数据库` / `开始写入数据库` / `数据库查询完成` / `数据库更新完成` / `数据库查询失败` 等阶段，并带 `db.operation`、`db.table`、`db.rows_affected`、`db.duration`、脱敏后的 `db.statement`。
- 为避免后台 worker 轮询刷屏，数据库 info 级详细日志只在有有效 trace context 时输出；无 trace 的后台数据库真实错误仍会输出错误日志。
- `/api/health` 仍保持静默：不写请求日志，不进 Jaeger；响应头里的 `X-Trace-Id` 只是 request-id middleware 生成的排查 ID。

### 验证
- 本地：`go test ./pkg/database -run 'TestGORMLogAdapter|TestBeforeGORM|TestSanitize|TestOperationFromSQL' -count=1` 通过。
- 本地：`go test ./pkg/logger ./pkg/trace ./internal/app -count=1` 通过。
- 本地：`go build ./cmd/server ./pkg/database ./pkg/logger ./internal/app` 通过。
- 已知限制：`go test ./pkg/database -count=1` 仍被既有 Windows `CGO_ENABLED=0 + go-sqlite3 requires cgo` 的 `TestPing_WithSQLite` 阻塞，和本次日志改动无关。
- 部署：已执行 `npm run push-docker-ccr`，最终推送 manifest `sha256:bd13f50962521e317c93f9e83819da09649b6eb4565d0bd198fb3227a1a48ca3`，线上 Pod 已滚动到 `foodlink-main-796cd76ddf-*`，3/3 Running，重启数 0。
- 线上普通请求验证：`GET /api/user/profile` 未授权请求 trace `aadf3f08c47c87146175fa87639729e7` 在 Pod 日志和 Jaeger 中均包含 `收到请求` 与 `请求完成但返回客户端错误`。
- 线上数据库阶段验证：`GET /api/membership/plans` trace `341d08f36a313763b2880d61693adfaf` 在 Pod 日志和 Jaeger 中包含 `收到请求`、`开始查询数据库`、`数据库查询完成`、`请求完成`。
- 线上 health 验证：`GET /api/health` trace header `30e3f52014fd4646b31aca425ae9f748` 在 Pod 日志无记录，Jaeger API 返回 404 not found。
## 2026-06-21 - App 饮食记录微信分享卡片
- 已实现 App 分享升级：后端新增公开 HTML 分享页 `/share/food-record/:record_id`，包含 OG/Twitter meta 和移动端可读页面；仍复用既有 `/api/food-record/share/:record_id` 的公开权限判断。
- Android 原生微信模块新增 `shareWebpage`，通过 OpenSDK `WXWebpageObject` 拉起微信会话卡片；App TypeScript wrapper 新增 `shareWebpageToWechat`，失败或不可用时回退系统分享。
- App 记录详情分享、单条当天记录分享、识别历史三点菜单“分享到微信”均接入新链路；未保存识别结果会提示先保存为饮食记录。
- 验证：`go test ./internal/foodrecord/handler`、`go test ./internal/app`、`npm --workspace packages/api-client test -- --runInBand`、`npm --workspace packages/api-client run typecheck`、Android `./gradlew.bat :app:compileDebugKotlin`、`git diff --check` 通过。
- 限制：`npm run typecheck:mobile` 被既有 `ProfileScreen.tsx` 缺少一批 lucide 图标导入阻塞；未进行真机微信发送验证，需新构建含原生模块的 App 包后验证。

## 2026-06-21 - App 饮食记录微信分享卡片验证收尾
- 已补齐移动端 typecheck 阻塞：`ProfileScreen.tsx` 的更多功能菜单补全 lucide 图标导入，`npm run typecheck:mobile` 已通过。
- Android 分享原生模块不仅通过 `:app:compileDebugKotlin`，也通过 `:app:assembleDebug` 完整打包验证。
- 当前 App 工程没有 iOS 原生目录；iOS/Expo Go 继续走系统分享公开链接兜底，Android 正式包走微信网页卡片优先。
- 生产可用还需要后端部署公开分享页，并用匹配微信开放平台包名/签名的 Android 包做真机发送验证。

## 2026-06-21 - App 饮食记录分享菜单与生产 404 修复
- 用户希望分享时像常见 App 一样先选择方式，而不是直接进入微信；已把 `DetailScreens.tsx` 的饮食记录分享改成可选菜单：微信卡片、复制链接、更多分享方式。
- Android 原生微信卡片只在 `FoodLinkWechatAuth.shareWebpage` 可用时展示；Expo Go/iOS/无原生模块时展示复制链接和系统分享，微信分享失败后可改用系统分享。
- 线上分享链接 404 的直接原因是生产后端尚未部署包含 `/share/food-record/:record_id` 的镜像；已执行 `npm run push-docker-ccr` 推送 main 镜像，后续需等待自动更新并复测分享页。
- 验证：`npm run typecheck:mobile` 通过；目标 diff check 通过，仅有 LF/CRLF warning。

## 2026-06-21 - App 分享页线上验证完成
- 已将 main 后端镜像重新推送到 CCR，对应 git SHA 815e1dc。
- 线上探测 `https://api.healthymax.cn/share/food-record/not-a-real-record` 已返回公开分享页自己的 HTML 404，`Content-Type: text/html; charset=utf-8`，页面标题为“记录不存在”；这证明此前用户看到的链接 404 是新路由尚未部署导致，部署后真实记录链接会进入分享页链路。

## 2026-06-21 - App 饮食记录分享底部抽屉
- 用户指出系统弹窗分享方式太丑，并给出参考图，要求从下面弹出抽屉选择分享到哪里或复制链接。
- 已把 `DetailScreens.tsx` 中饮食记录分享方式选择改成自定义底部抽屉：标题“分享至”、关闭按钮、圆形按钮、微信好友、朋友圈、更多方式、复制链接。
- 继续复用已有微信 OpenSDK 分享、系统分享和剪贴板逻辑；无原生微信能力时不展示微信好友/朋友圈。
- 验证：`npm run typecheck:mobile` 通过；目标 diff check 通过，仅有 LF/CRLF warning。

## 2026-06-21 - App 分享链接网页拉起 App
- 用户询问是否能像小红书一样，打开分享网页后自动请求打开 App，并定位到同一条记录。
- 已实现基础 deep link 方案：后端公开分享页 `/share/food-record/:record_id` 现在生成 `foodlink://food-record?record_id=<id>`，加入 Android/iOS App Links meta，网页移动端加载后尝试跳转，同时提供“打开 App 查看”按钮作为用户手动兜底。
- App `RootNavigator` 支持解析 `food-record` / `record-detail` deep link，登录态直接进入 `RecordDetail`，未登录时暂存 `record_id`，登录后再跳转。
- 验证：`go test ./internal/foodrecord/handler -count=1`、`go test ./internal/app -count=1`、`npm run typecheck:mobile`、`git diff --check` 通过。
- 限制：完整线上体验需要部署后端分享页，并发布包含 App deep link 路由的新 App 包；自定义 scheme 可请求拉起，但微信/系统浏览器可能拦截自动跳转。更像小红书的 HTTPS 直开 App 需要后续配置 Android App Links / iOS Universal Links。

## 2026-06-21 - App 分享网页拉起 App 后端部署完成
- 已提交并推送 `04e19fd feat: open app from shared food record page`。
- 已执行 `npm run push-docker-ccr`，推送 `ccr.ccs.tencentyun.com/littlehorse/foodlink:main`，manifest digest 为 `sha256:2a219b657ce7abb0e3f7467969000fe35cb069e99faa8891ff067568cf01eb8e`。
- 线上 `foodlink-main` 已 rollout 成功，3 个新 Pod `foodlink-main-55cd69d478-*` 均 Running，重启数 0；`GET https://api.healthymax.cn/api/health` 返回 200。
- `GET https://api.healthymax.cn/share/food-record/not-a-real-record` 命中新 Pod 并返回分享页自己的 HTML 404。成功态 deep link HTML 已由后端单测覆盖；线上成功态还需要一条真实可公开记录链接验证。
