# 2026-06-10

- 讨论“我的”页意见反馈功能：用户希望新增反馈入口和后端 API，并在反馈提交时自动关联最近几个请求的 `X-Trace-Id` 等诊断信息。
- 代码勘察结论：Go 后端已有 `X-Trace-Id` / `X-Request-Id` / `X-Host-Name` 响应头中间件；前端 `src/utils/api.ts` 已能提取 trace header，但需要新增全局最近请求诊断缓冲和反馈提交 API。
- 诊断并修复微信开发者工具打开 `dist/` 白屏、`[WXSS 文件编译错误] something must be wrong`：根因是 2.7MB 的 `assets/bg/cafeteria.png` 被 WXSS `background-image` 内联为 base64，导致首页 WXSS 约 7.4MB、校园食堂页 WXSS 约 3.6MB。已改为 JSX `<Image src="/assets/bg/cafeteria.png" mode="aspectFill" />` 绝对定位背景层，并在 `config/index.ts` 复制 `assets/bg`。`npm run build:weapp` 验证通过，相关 WXSS 降至约 119KB/17KB 且不含 `data:image`；`mrc` 不在 PATH，未能完成 weapp-devtools 截图验证。
- 实现意见反馈功能：前端新增最近请求 trace 环形缓冲、`TARO_APP_RECENT_REQUEST_TRACE_LIMIT` 配置（默认 50、最大 50）、“我的”页意见反馈入口和 `packageExtra/pages/feedback` 页面；后端新增 `POST /api/feedback` 与 `user_feedback` 迁移 DO，保存提交请求 trace/request/host 以及最近请求快照。用户确认后已对远程库 `154.8.205.78:5432/food-link` 成功执行迁移。
- 新增意见反馈 Admin 后台并按用户要求改为独立站点：根目录新增 `admin/` Vite React 项目，默认通过 `VITE_ADMIN_API_BASE_URL` 指向后端，独立域名部署时可配后端地址或反代同源；Go 后端移除 `/admin/feedback` 与 `/static/admin` 静态托管，只保留 `GET /api/admin/feedback` 和 `PATCH /api/admin/feedback/:feedback_id/status` 管理 API，并为 `/api/admin/*` 增加可选 `ADMIN_CORS_ALLOWED_ORIGINS` 跨域白名单。验证：`admin/npm run build`、`go test ./internal/admin/... ./internal/feedback/... ./internal/app` 通过。
- Cloudflare Pages 构建 `admin/` 失败：`tsconfig.node.json` 使用 `types: ["node"]`，但 `admin/package.json` 未声明 `@types/node`，本地因根目录依赖存在而未暴露。已在 `admin/` 添加 `@types/node` devDependency，`npm run build` 通过。
