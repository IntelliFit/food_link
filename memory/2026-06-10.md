# 2026-06-10

- 讨论“我的”页意见反馈功能：用户希望新增反馈入口和后端 API，并在反馈提交时自动关联最近几个请求的 `X-Trace-Id` 等诊断信息。
- 代码勘察结论：Go 后端已有 `X-Trace-Id` / `X-Request-Id` / `X-Host-Name` 响应头中间件；前端 `src/utils/api.ts` 已能提取 trace header，但需要新增全局最近请求诊断缓冲和反馈提交 API。
- 诊断并修复微信开发者工具打开 `dist/` 白屏、`[WXSS 文件编译错误] something must be wrong`：根因是 2.7MB 的 `assets/bg/cafeteria.png` 被 WXSS `background-image` 内联为 base64，导致首页 WXSS 约 7.4MB、校园食堂页 WXSS 约 3.6MB。已改为 JSX `<Image src="/assets/bg/cafeteria.png" mode="aspectFill" />` 绝对定位背景层，并在 `config/index.ts` 复制 `assets/bg`。`npm run build:weapp` 验证通过，相关 WXSS 降至约 119KB/17KB 且不含 `data:image`；`mrc` 不在 PATH，未能完成 weapp-devtools 截图验证。
