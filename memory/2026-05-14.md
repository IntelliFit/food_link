# 2026-05-14

- Implemented backend API contract E2E MVP. Added `backend/e2e-test/cmd/api-contract-test`, YAML suite `backend/e2e-test/suite.yaml`, seed SQL under `backend/e2e-test/fixtures/`, runner code under `backend/e2e-test/runner`, docs at `backend/e2e-test/README.md`, and root script `npm run test:backend:api-contract`. Runner uses fresh temporary PostgreSQL DBs, existing Go AutoMigrate, seeded fixture data, named JWT auth users, `httpexpect`, and `gjson` assertions. Verification passed: `go test ./e2e-test/runner ./e2e-test/cmd/api-contract-test -run TestDoesNotExist -count=1`; `npm run test:backend:api-contract -- --timeout 5m` with `Total: 161, Passed: 161, Failed: 0`; `git diff --check` with only CRLF warnings.

- Added full backend API E2E contract testing guide at `backend/e2e-test/README.md`, written for both humans and AI agents. It covers goals, commands, file layout, temp DB lifecycle, YAML schema, auth, fixtures, assertions, route smoke, AI maintenance rules, troubleshooting, and current baseline.

- Consolidated backend E2E test assets under `backend/e2e-test/`: moved `suite.yaml` and `fixtures/base.sql` out of `backend/testdata/api-contract/`, updated the CLI default suite path to `e2e-test/suite.yaml`, and rewrote the guide as ASCII-safe content to avoid encoding issues in tool output.

- Split backend API contract cases by route/module. `backend/e2e-test/suite.yaml` now keeps global settings and `case_files: ["cases/*/*.yaml"]`; explicit cases live under directories such as `backend/e2e-test/cases/user/profile.yaml`, `cases/body-metrics/water.yaml`, `cases/membership/me.yaml`. Runner now loads external case files before validation.

- Consolidated the backend E2E test toolchain further under `backend/e2e-test/`: moved the CLI to `backend/e2e-test/cmd/api-contract-test`, moved runner code from `backend/internal/e2e` to `backend/e2e-test/runner`, removed the separate backend/root docs copies, and kept `backend/e2e-test/README.md` as the single guide.

- Updated E2E case metadata semantics: `id` is the stable machine-readable identifier used by `--case`, `name` is a short Chinese display name, and `desc` is a detailed human-readable behavior description. CLI output now shows Chinese names first and includes `[id=...]`; route smoke cases get generated Chinese names/descriptions.

- User asked why direct execution of the food analysis loadtest failed with 20 submit-time 500s. Backend log showed `invalid input syntax for type uuid: "food-analysis-load-user-19" (SQLSTATE 22P02)`, caused by default generated non-UUID loadtest user IDs when `FOOD_ANALYSIS_LOAD_USER_IDS` or `FOOD_ANALYSIS_LOAD_TOKENS` is not set. This is a setup/user-ID failure, not a food-analysis pipeline stability failure; pass real UUID users or create temporary UUID users and set `FOOD_ANALYSIS_LOAD_USER_IDS`.

- 用户要求提交并推送代码。当前待提交内容为 2026-05-13 的分享今日饮食相关改动：当天饮食记录页和当天饮食海报按 `record_time` 升序显示；当天饮食海报顶部今日摄入改为“已摄入/目标 + 圆角进度条”；并记录了 canvas 分享卡片加入外部图标的技术调研结论。同步远端 `origin/dev` 前发现本地落后 3 个提交，已 stash 本地改动、快进到远端 `dev`、恢复改动并解决 `CURRENT_TASK.md` 交接记录冲突。提交前验证：`npx eslint src/packageExtra/pages/day-record/index.tsx src/utils/poster.ts --max-warnings 0`、`git diff --check` 通过。
# 2026-05-14 loadtest default users improvement

- Improved `backend/internal/analyze/loadtest/food_analysis_stability_test.go` so direct execution works without setting `FOOD_ANALYSIS_LOAD_USER_IDS` or `FOOD_ANALYSIS_LOAD_TOKENS`: when both are empty, it opens the DB from `backend/config.yaml`, creates temporary UUID `weapp_user` rows equal to `FOOD_ANALYSIS_LOAD_COUNT`, issues local JWTs, and cleans temporary ledger/task/user rows with `t.Cleanup`. Explicit tokens or user IDs still preserve existing behavior. Verified with `gofmt` and `go test -tags food_analysis_load ./internal/analyze/loadtest -run '^$' -count=1`.
- Verified the new default auto-user path with a 1-run smoke against the existing local backend: no user/token env vars were set, one temporary UUID user was created, result was `total=1 success=1 failed=0`, COS cleanup deleted 1 image, and DB residual check for `food-analysis-load-20260514T%` users returned 0.
