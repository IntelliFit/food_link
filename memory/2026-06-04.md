# 2026-06-04

## 手动记录页来源标签样式修复

- 修复 `record-manual` 食物列表来源标签被拉伸变形的问题：`.source-badge` / `.item-tag` 改为固定高度、紧凑胶囊、单行省略。
- 影响“常用食物/标准食物”和“包装食品”等标签，保留绿色/橙色/蓝色来源区分。
- 验证：`npx eslint src/packageExtra/pages/record-manual/index.tsx` 通过；IDE lints 无新增错误。
- 阻塞：微信开发者工具自动化 `mrc where` / `mrc exists ".source-badge"` 长时间无响应后被中断，未能生成截图。

## 包装食品空图补全

- 根据 `backend/tmp/packaged-food-empty-image-review.md` 的 32 条审核清单，将本地 `backend/tmp/<food_id>.jpg` 上传到 COS `food-images`，key 使用 `packaged-food/backfill/<food_id>/<food_id>.jpg`。
- 新增 `backend/cmd/packaged-food-image-backfill`：解析审核 Markdown，dry-run 校验图片与数据库，`--apply` 时上传 COS；最终用 `--force-update-existing` 将这 32 条 `source_image_urls` 强制改写为完整 CDN URL，避免小程序将 COS key 当成本地相对路径加载。
- 实际执行结果：32 条全部上传成功并写库，数据库值形如 `http://cdn-food-images.coachlink.fit/packaged-food/backfill/<food_id>/<food_id>.jpg`。
- `backend/tmp/packaged-food-empty-image-review.md` 已同步 CDN URL 与“已上传并写库”状态。
- 审计统计：`public_library` 51 条，缺图 0，缺图率 0.00%；`packaged_food` 250 条，缺图 0，缺图率 0.00%；图片字段来源合计 301 条，缺图 0，缺图率 0.00%。
- 验证：`go test ./cmd/packaged-food-image-backfill` 通过；新增 Go 文件 IDE lints 无错误。

## 手动记录页 UX 样式优化

- `record-manual` 保留“左侧类别 + 右侧列表”的双列结构，左侧分类栏收窄并改回中性灰白背景，右侧列表保持对应内容区。
- 页面样式统一到首页低饱和绿色：`#5cb896` 作为主点缀，`#f4faf8` 作为浅绿背景；SCSS 使用 8rpx 栅格变量收敛间距、圆角、控件高度。
- 已选食物、营养汇总、底部保存栏改为更轻的浅绿面板体系；加载更多改为滚动到底自动触发，loading 为纯 spinner。
- 左侧类别栏使用 sticky 贴顶，页面向下滚动时仍可继续选择类别。
- 验证：手动记录页 ESLint 通过；微信开发者工具自动化确认当前页面、左侧分类栏、右侧内容容器、搜索框存在，点击分类成功。
- 阻塞：`mrc screenshot` 返回 `fail to capture screenshot`，未能生成截图；全项目 `npm run typecheck` 被其他页面既有类型错误阻塞。

## 标准食物库批量回填基础设施

- 基线：`missing_backfill_queue.total = 12458`（`--stats-only` → `data/standard-food-image-backfill/baseline.json`）。
- 新增脚本：`backfill-baseline.ps1`、`backfill-phase1-dryrun.ps1`、`backfill-run-next.ps1`、`backfill-summarize-results.py`；手册 `docs/standard-food-image-backfill-runbook.md`。
- CLI：`--checkpoint-every 1` 每条写 state/results；Bing 仅 Python 逻辑（`cn.bing.com/images/search` + href `mediaurl`）。
- Phase1 dry-run 待用户配置 `tmp/kimi-api-key.local` 后执行。

## 标准食物库图片回填（Kimi + Bing + COS）

- 安装项目 skill：`.cursor/skills/kimi-code-api/`（递归子文档，相对链接已转 raw GitHub URL）。
- 表结构：`food_nutrition_library` 增加 `image_path`、`image_paths`；`migration.go` 幂等补列。
- 手动记录：`nutrition_library` 结果带出图片并走 `storage.ResolveReferenceURL`。
- 新增 CLI：`backend/cmd/standard-food-image-backfill`（`--demo`、`--dry-run`/`--apply`、断点 state/results/failed）。
- 验证：`go test ./cmd/standard-food-image-backfill` 通过；`--demo` 已输出 OAuth 链接，90s 内未完成授权则超时（可用 `KIMI_API_KEY` 绕过）。
- OAuth 与迁移已在 Apollo 指向的 `food-link` 库完成；token 存 `backend/tmp/kimi-code-oauth-token.json`（勿提交）。
- 回填 CLI 增强：Kimi 失败继续下一候选、图片解码校验、state 跳过已失败项、`--failed-only` 重试、`--local-image`/`--trust-local-image`。
- 试跑：Bing 自动搜图多为 `no_match`；`卤鸭腿` + picsum 本地图 `dry_run_match` 验证上传链路（未 `--apply` 写库）。
- 待用户确认后：小批 `--apply` 或夜间分批（约 1.2 万条缺图）。

## 标准食物图片回填 - Qwen 验证

- 暂停 `shard-0001` 后台回填进程（旧进程仍在写历史 `kimi_failed`）。
- 监控脚本 `backend/scripts/backfill-monitor.py` 已改为 DashScope / Qwen 展示，不再显示 Kimi 字样；每 10 秒刷新。
- 修复 `food_nutrition_library.image_paths` jsonb 写库：使用 `datatypes.JSON(json.Marshal([]string{key}))`，解决 `SQLSTATE 22P02`。
- 使用 `backend/tmp/qwen-verify-5` 跑 5 条真实 `--apply` 验证：5/5 `db_updated`，`run_total duration=1m14.556s`。
