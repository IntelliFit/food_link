# 2026-05-24

- 用户提出一个会员规则漏洞担忧：新用户试用到期后，若先注销账号再重新登录，是否可以反复领取新用户免费期。
  - 已核对当前实现：
    - `backend/internal/membership/service/membership_service.go` 的试用资格按 `weapp_user.created_at` 计算；
    - `backend/internal/user/service/user_service.go` 注销会直接删掉 `weapp_user`；
    - `backend/internal/auth/service/login_service.go` 删除后再次登录会按同一 `openid` 创建新用户。
  - 结论：这不是纯理论风险，当前逻辑下确实存在“删除账号后重新注册，试用期被重新计算”的漏洞。
  - 修复方向已记录到本地状态：把试用资格改为独立权益记录，优先按 `unionid`、兼容 `openid` 控制一次性领取，注销不清除该标记。
  - 当天已落代码修复：
    - 新增 `user_trial_entitlements` 独立权益表模型与迁移 DO；
    - 登录时自动创建/复用试用权益记录，并在后续补拿到 `unionid` 时更新该记录；
    - 会员试用判定改为查 `user_trial_entitlements`，不再依赖当前 `weapp_user.created_at`；
    - 迁移脚本增加历史 `weapp_user` -> `user_trial_entitlements` 幂等回填。
  - 静态验证：
    - `cd backend && go test ./internal/membership/service -count=1` 通过；
    - `cd backend && go test ./internal/auth/service -run '^$' -count=1` 通过；
    - sqlite 相关 auth repo/service 真实测试仍被当前环境 `CGO_ENABLED=0` 阻塞。

- 用户贴出 `standard_web_search` 实跑日志，要求分析流程并白盒化，随后要求实现“单视觉自适应食物分析方案”以降低成本和耗时。
  - 第一步已白盒化：`hybrid_review` 输出第一轮 item、搜索证据、逐项 diff、证据来源。
  - 第二步已把低成本联网增强改成单视觉链路：
    - 第一轮仍由当前视觉模型识别图片；
    - 然后基于第一轮结果做 `complexity_decision`；
    - 只对包装食品、饮品、品牌商品、小众商品等候选发起低成本搜索；
    - 搜索结果先做相关性过滤；
    - 仅在相关结果里能提取到 `kg/g/ml` 规格或净含量时，用规则校准 `estimatedWeightGrams` / `waterMl`；
    - 不再把搜索证据交给 Gemini 做第二次图片复核。
  - 新的白盒字段包括 `complexity_decision`、`web_search_relevance`、`calibration_method`、`stage_durations_ms`、`second_vision_skipped=true`。
  - 无关搜索结果（如“原神”“汉语词典”“原字解释”）会被过滤，最终标记 `web_search_status=no_relevant_results`，不会再进入校准。
  - 酸奶/啤酒这类包装商品如果搜到 `净含量260g` / `330ml` 等有效证据，会通过规则校准重量，并写入 `evidenceSource=search:<query>` 与 `searchEvidenceUsed`。
  - 顺手修复一个阻塞编译的问题：`backend/internal/foodrecord/repo/food_nutrition_repo.go` 中 GORM `Order` 传参改为 `gorm.Expr(...)`。
  - 验证：WebSearch 单视觉相关定向测试通过，`go test ./internal/analyze/service -run "^$" -count=1` 通过，`git diff --check` 通过（仅 CRLF warning）。

- 用户要求把食物库相关数据导出到本地电脑。
  - 先按用户贴出的 Kimi 远端操作线索检查 `ubuntu-ts`，确认远端主机可连通，但 `/home/ubuntu/food_export.tar.gz` 与 `/home/ubuntu/food_export/` 已不存在，无法直接从远端捞现成包。
  - 为避免继续卡在远端环境差异，直接使用项目当前 `backend/config.yaml` 指向的 PostgreSQL（`154.8.205.78:5432 / food-link`）在本机重导。
  - 已导出以下 4 张表到 `D:\files\downloads\food_export_20260524_203521`：
    - `food_nutrition_library.csv`：12005 行
    - `food_nutrition_aliases.csv`：10248 行
    - `packaged_food_library.csv`：34 行
    - `public_food_library.csv`：53 行
  - 同时生成：
    - `D:\files\downloads\food_export_20260524_203521.zip`
    - `D:\files\downloads\food_export_20260524_203521.tar.gz`
    - `D:\files\downloads\food_export_20260524_203521\export_summary.json`
  - 这次导出与用户想要的“食物库所有食物”口径一致，覆盖标准营养库、别名库、包装食品库和公共食物库。

- 用户继续追问食物库重复与检索策略，明确希望实现“不要 100%% 完全一致命中，而是多检索几个候选，再让 DeepSeek 判断是否复用”。
  - 已在后端落第一版：
    - `backend/internal/foodrecord/repo/food_nutrition_repo.go` 新增 `SearchCandidates(...)`，普通食物库检索从单个弱 fuzzy 升级为 canonical + alias 的 TopN 候选召回与排序。
    - `backend/internal/analyze/service/analyze_service.go` 的 `applyDBFirstNutrition(...)` 对未精确命中的 item 先召回候选，再批量调用 DeepSeek 做结构化判定；只有 `confidence >= 0.9` 才复用已有食物。
    - 新的命中结果会写 `resolve_status=semantic_rerank`、`nutrition_source=library_semantic_rerank`，并带 `resolve_reason` 方便白盒观察。
    - 如果模型高置信认为只是别名差异，还会自动补写 `food_nutrition_aliases`，让下次直接精确命中。
  - 当前这版仍保持保守：低置信候选不会自动并库，没通过判定的 item 仍继续走原有 DeepSeek 营养补全链路。
  - 验证：
    - `cd backend && go test ./internal/analyze/service -run "^$" -count=1` 通过。
    - 新增的 sqlite 定向测试代码已补，但当前环境 `CGO_ENABLED=0`，既有 sqlite 测试仍被 `go-sqlite3 requires cgo` 阻塞，尚未本机跑通完整用例。

- 用户随后要求正式实现“预包装零食采集与自动入库链路 V1”。
  - 已按方案落地：
    - 后端 `packaged_food_library` 扩展 `product_key/spec_text/barcode/flavor_text/package_category/ingredients_text/source_image_urls/ocr_raw_text/extract_confidence/field_confidence/ingest_method`
    - `packaged_food_library` 规格级唯一键改为 `product_key`
    - 新增异步任务类型 `packaged_product_extract`
    - 新增接口 `POST /api/packaged-food/extract/submit`
    - 旧 `/api/packaged-food/nutrition-label/submit` 仍保留兼容，但内部转到新多图链路
    - worker 新增 `processPackagedProductExtract(...)`
    - 自动入库通过后直接 upsert `packaged_food_library`
    - `db_first` 已重新打开 `packaged_food_library` 命中逻辑
  - 前端 `src/packageExtra/pages/packaged-food-edit/index.tsx` 已重写为采集向导：
    - 第 1 步：拍包装正面
    - 第 2 步：拍营养表 + 净含量
    - 第 3 步：仅在 `needs_more_images=['ingredients']` 时补拍配料表
    - 自动入库失败时保留预填字段，允许手动确认后保存
  - 结果页零食补库入口已重新打开，点击后走新的采集向导而不是旧单图营养表页。
  - 静态验证：
    - `cd backend && go test ./internal/foodrecord/... ./internal/worker ./internal/app -run '^$' -count=1` 通过
    - `npx eslint src/utils/api.ts src/packageExtra/pages/packaged-food-edit/index.tsx src/packageExtra/pages/result/index.tsx --max-warnings 0` 通过
    - `git diff --check` 通过（仅 CRLF warning）
  - 仍有两点未完成：
    - 微信开发者工具自动化端口 `3001/9420` 仍无法连接，没法补运行时截图和页面点击验证
  - 随后用户明确要求直接执行数据库迁移。
    - 已在 `D:\files\food_link\backend` 执行 `go run ./cmd/migration -config-dir .`
    - 目标库为 `backend/config.yaml` 指向的远程 PostgreSQL：`154.8.205.78:5432 / food-link / public`
    - 首次执行失败，原因是：
      - `packaged_food_library` 新增 `product_key` 唯一索引时，历史数据在加列后全是 `''`
      - 导致 `idx_packaged_food_library_product_key` 创建时报 `SQLSTATE 23505`
    - 已立即修正迁移代码：
      - 去掉 DO 上的自动唯一索引声明，避免 AutoMigrate 抢先创建
      - 在 `ensureIndexes(...)` 中改成显式顺序：
        1. `ADD COLUMN IF NOT EXISTS`
        2. 回填旧数据 `product_key`
        3. 创建 `idx_packaged_food_library_product_key`
        4. 删除旧 `packaged_food_library_normalized_name_key`
    - 第二次执行迁移成功，日志显示 `数据库迁移完成: schema=public`
    - 只读确认结果：
      - `analysis_tasks_task_type_check` 已包含 `packaged_product_extract`
      - `packaged_food_library` 新字段已存在
      - `idx_packaged_food_library_product_key` 与 `idx_packaged_food_library_barcode` 已存在
      - 历史数据 `product_key` 空值数量为 `0`

- 用户希望首页宠物之外，再补一个“我的宠物”独立展示页，入口放在“我的”里，并追问更换宠物是否可做成 5 积分方案。
  - 已按更稳的 MVP 落地成：
    - “我的”页新增 `我的宠物` 入口；
    - 新增独立分包页 `packageExtra/pages/pet-home/index`，展示大号宠物、等级经验、今日状态、离线事件、奖励积分与换装入口；
    - 首页继续保持轻量悬浮宠物，不把完整养成内容继续堆在首页。
  - 更换方案已明确使用“奖励积分”而不是“会员每日系统积分”：
    - 新增后端接口 `POST /api/pet/reroll-appearance`
    - 每次消耗 5 点 `earned_credits_balance`
    - 保留名字、等级、经验，只随机刷新 `color / shape / pattern / accessory`
    - 同时写入 `user_earned_credit_ledger`，`reason = pet_appearance_reroll_spend`
  - “挑选外观”这版先不做完整自由编辑器，只保留入口与“即将开放”提示，避免第一版过重。
  - 静态验证：
    - `go test ./internal/pet/... -run "^$|TestSummaryCreatesStablePetAndSingleOfflineEvent|TestClaimEventIsIdempotent|TestRerollAppearanceConsumesEarnedCredits|TestRerollAppearanceRequiresEnoughEarnedCredits" -count=1` 通过
    - `npx eslint src/pages/profile/index.tsx src/packageExtra/pages/pet-home/index.tsx src/utils/api.ts --max-warnings 0` 通过
  - 当前阻塞：
    - 运行中的后端未重启，`/api/pet/reroll-appearance` 需要重启后才会生效
    - `mrc where --port 3001` / `9420` 仍无法连接，没法补小程序运行时截图
    - `go test ./internal/app` 被项目既有问题挡住：`backend/internal/analyze/service/task_service.go` 与 `analyze_service.go` 中 `boolFromAny` 重复定义，不是本次宠物改动引入

- 用户晚间继续反馈：第一次上传报告正常，但在“体检/病例识别结果”弹窗里点击“上传新报告”再上传 2 张新照片后，体感上“没有反应”，分不清是真在处理还是根本没处理。
  - 先按日志核实后端事实：任务 `d586020b-e0d0-41f7-97b8-3559bfff930f` 于 `2026-05-24 22:08:15` 提交并被 worker 认领，`2026-05-24 22:08:29` 已处理完成，所以这次不是后端没跑。
  - 问题落在前端交互层：查看页弹窗在提交新报告后仍停留旧识别结果，没有立刻切到“识别中”，也没有自动轮询并刷新新结果；用户自然会觉得“没反应”。
  - 已修复 `src/packageExtra/pages/health-profile-view/index.tsx`：
    - 提交新报告 / 重新识别成功后，立即本地写入 `_status=processing` 和本次 `_image_urls`；
    - 基于 `getAnalyzeTask(taskId)` 轮询任务状态；
    - 任务结束后自动刷新 `getHealthProfile()`；
    - 弹窗内新增“新报告识别中 / 识别失败”状态卡；
    - 报告弹窗提交时右上角显示 spinner，文案由 `确定` 改为 `完成`。
  - 样式同步补到 `src/packageExtra/pages/health-profile-view/index.scss`。
  - 验证：`npx eslint src/packageExtra/pages/health-profile-view/index.tsx --max-warnings 0` 通过。
  - 微信开发者工具自动化仍无法连接：`mrc where --port 9420` / `3001` 均失败，因此这次还是没法补运行时截图。
