- 商业计划书交付：用户希望将原始《food_link 商业计划书》改成更美化、精简、适合对外沟通的版本，内容不要太多，抓住核心痛点即可。用户强调目前尚未正式进行付费验证，已有付费更像自愿行为，因此对外版本不要重点提付费数据。
- 已交付文件：
  - `docs/食探-商业计划书-精简版.md`
  - `docs/shitan-business-plan-brief.pdf`
  - `docs/食探-商业计划书-精简版.pdf`
- 对外命名口径：统一使用 `食探（智健食探）`；`食探` 为品牌简称，`智健食探` 为小程序正式名称建议。
- 文案处理口径：弱化“付费几乎为零/尚未验证”的内部表达，改为“需求已验证，进入订阅会员与留存验证阶段”；保留真实使用数据作为产品验证，不把早期自愿付费样本作为核心卖点。
- 生成与验证：首次 PDF 使用 ReportLab CID 字体生成，用户反馈在浏览器查看时内容空白/乱码。已返工为 `scripts/generate_shitan_bp_pdf.py`，用 PIL + `NotoSansSC-VF.ttf` 先渲染页面 PNG，再封装成 PDF；`docs/business_plan_preview/page-01.png`、`page-05.png`、`page-07.png` 已视觉抽查，中文可读。新 PDF 文件大小约 686 KB，路径为 `D:\files\food_link\docs\食探-商业计划书-精简版.pdf` 和 `D:\files\food_link\docs\shitan-business-plan-brief.pdf`。当前 PDF 为图片页格式，正文不可复制检索，后续编辑以 Markdown 为准。未做小程序前端改动，因此未运行 `weapp-devtools`。
- 用户继续反馈：PDF 版内容太少、排版不够紧密，建议改成 doc。已生成 Word 文档 `D:\files\food_link\docs\食探（智健食探）商业计划书.docx`，脚本为 `scripts/generate_shitan_bp_docx.py`。该版本扩充为 9 个章节、12 张表格，仍保持对外精简口径，但信息密度更高且可编辑。已用 `python-docx` 校验文档包含 36 个段落、12 张表格，文件约 43 KB。
- 用户给出正式会员订阅方案：轻度版 `9.9/月、27.9/季、99/年`，每天 `8` 积分；标准版 `19.9/月、56.9/季、199/年`，每天 `20` 积分；进阶版 `29.9/月、84.9/季、299/年`，每天 `40` 积分。新用户免费体验 3 天，每天 8 积分。积分当天有效、不累计。运动记录 1 积分/次，基础记录/基础分析 2 积分/次。超额后可等次日恢复、升级会员或邀请好友获取额外积分。邀请双方连续 3 天每天 +5 积分，需被邀请人完成 1 次有效使用后生效，且设置月度上限防刷。分享海报奖励 1 积分，建议每日上限 1 次。已把该方案写入 `DECISIONS.md` 并更新 Word 文档；新版 docx 校验为 42 个段落、14 张表格，包含 `轻度版/标准版/进阶版/免费 3 天`。

- 定价策略整改（一期）落地：
  - 数据库
    - 新增 `backend/sql/add_tiered_membership_pricing.sql`：`membership_plan_config` 增加 `tier / period / daily_credits / original_amount / sort_order`；`user_pro_memberships` 增加 `daily_credits` 快照字段
    - 重写 `backend/database/membership_plan_config_seed.sql`：停用旧 `pro_monthly`，插入 9 档（light/standard/advanced × monthly/quarterly/yearly），并设置 `daily_credits` 与 `sort_order`
  - 后端
    - `backend/database.py`：`list_active_membership_plans` 按 `sort_order` 排序；新增 `get_today_exercise_log_count`
    - `backend/main.py`：
      - `MembershipPlanResponse` / `MembershipStatusResponse` 扩展 tier/period/积分/试用字段
      - 新增 `_credits_reset_time_iso / _is_user_in_trial / _compute_daily_credits_status`
      - `/api/membership/plans` 按新结构返回并算 `savings`
      - `/api/membership/me` 合并积分状态 + 新用户 3 天试用判定（按 `weapp_user.created_at`）
      - `/api/payment/wechat/notify/membership` 成功回调写入 `daily_credits` 快照
      - 积分消耗口径：食物分析 2 / 运动记录 1；当日有效、次日清零
  - 前端
    - `src/utils/api.ts`：补全 `MembershipTier / MembershipPeriod / MembershipPlan / MembershipStatus` 新字段
    - `src/pages/pro-membership/index.tsx + index.scss`：完整重做（Hero 积分胶囊、3 档卡片、3 周期 tab、立省¥、选中套餐大价格卡、能力对比、积分消耗说明、当前状态）
    - `src/pages/profile/index.tsx`：会员卡改为「今日积分 x/y」，非会员区分试用 / 未开通，服务入口描述同步
  - 已 `ReadLints` 无新增报错；尚未跑 `npm run dev:weapp` 重编译，也没用微信开发者工具截图验证
  - 线上数据库 SQL 尚未执行；需在部署时 apply `add_tiered_membership_pricing.sql` 并重跑 seed（`ON CONFLICT DO UPDATE`）
  - 一期仅做展示 + 试算，**还没做真正的积分扣减 enforcement**；邀请/分享奖励、自动续费、`user_membership_payments.pending` 清理推迟到后续阶段
- 交接给后续会话：
  - 下一步优先做「分析提交前按 `daily_credits_remaining` 扣减与拦截」：食物图片/文字分析和运动记录的提交端点要接入积分检查，不足则 402 拦截并提示升级
  - 微信支付 `pending` 堆积问题需要单独排查一次（可能与通知回调幂等/订单关闭有关）
  - 邀请奖励与分享奖励需要新表（邀请码 / 邀请记录 / 每日分享上限），本轮完全没做
  - 自动续费需要申请微信支付对应能力并重构订单创建链路
