# 2026-06-22

## App 微信 APP 支付配置命名收敛
- 用户确认将微信 APP 支付 AppID 配置改成更明确的字段，避免和小程序 `external.appid`、App 登录 `app_auth.wechat_app_id` 混淆。
- 已新增主配置字段 `wechat_pay.app_pay_app_id`，环境变量 `WECHAT_PAY_APP_PAY_APP_ID`；旧字段 `wechat_pay.app_id` / `WECHAT_PAY_APP_ID` 保留为兼容别名。
- 会员支付服务读取顺序：`wechat_pay.app_pay_app_id` -> `wechat_pay.app_id` -> `app_auth.wechat_app_id`。
- Apollo 需要在 `app-config.yaml` 的 `wechat_pay:` 下新增 `app_pay_app_id: "wx62da2262ae3ff06c"`；不要填小程序 AppID `wxbb1ce739d73a66e4`。
- 验证：`go test ./internal/membership/service ./internal/membership/handler ./pkg/config -count=1` 通过；`go build ./cmd/server` 通过；`git diff --check` 通过，仅有既有 LF/CRLF warning。

## 微信配置统一收敛到 wechat: 块
- 已将微信相关配置的新版主格式收敛为 `wechat.mini_program`、`wechat.mobile_app`、`wechat.pay` 三段：小程序 AppID/Secret、移动应用 AppID/Secret、商户支付参数分别归位，避免继续散落在 `external`、`app_auth`、`wechat_pay`。
- 旧字段仍兼容读取：小程序回退 `external.appid` / `external.secret`，移动应用回退 `app_auth.wechat_app_id` / `app_auth.wechat_app_secret`，支付回退 `wechat_pay.*`；业务代码应通过 `Config` helper 读取，不直接读旧字段。
- 已把用户创建的 `temp.dev.yaml`、`temp.local.yaml`、`temp.main.yaml` 转成新版格式：删除旧 `app_auth` / `wechat_pay` / `external.appid` / `external.secret`，保留 `wechat.pay.expiry_subscribe_template_id` 与 `wechat.pay.analysis_subscribe_template_id`。
- 配置结构体已补齐 `analysis_subscribe_template_id` 的读取、trim、环境变量绑定和旧字段回退，避免新版 YAML 中有该项但程序读不到。
- 验证：三份 temp YAML 均可解析且包含新版 `wechat` 块；`go test ./pkg/config ./internal/membership/service ./internal/membership/handler ./internal/app -count=1` 通过；`go build ./cmd/server` 通过；`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck`、`npm --workspace packages/api-client test -- --runInBand` 通过；`git diff --check` 仅有既有 CRLF warning。

## 本地 APP 微信支付验证与一分钱测试套餐
- 本地后端使用 `CONFIG_SOURCE=apollo`、cluster `local`、namespace `app-config.yaml` 启动成功，`GET /api/health` 返回 200，`GET /api/membership/plans` 返回套餐列表。
- Apollo 当前返回的 local 配置仍主要是旧键：`external.*`、`app_auth.*`、`wechat_pay.*`；仅看到 `wechat.mobile_app.development_mock_code` 新键。代码兼容旧键，所以不影响本轮验证，但新版 `wechat:` 块疑似尚未完整发布到当前 local namespace。
- 使用 App 微信登录 development mock code `expo-go-dev-wechat-code` 成功换到 token，测试用户为既有 mock 用户。
- 已在当前 local 配置指向的数据库 upsert 一条测试套餐：`test_one_cent_monthly`，名称 `Pay Test - 0.01 CNY`，金额 `0.01`，有效 1 个月，`is_active=true`，`sort_order=1`。
- 调用 APP 支付链路 `POST /api/membership/pay/create`，body 为 `plan_code=test_one_cent_monthly`、`pay_channel=wechat`、`trade_type=APP`、`client=mobile_app`，后端确实请求了微信下单接口，但微信返回：商户号该产品权限未开通，需要去商户平台产品中心开通 APP 支付；接口返回 502。
- 调用当前 App 旧默认链路（不传 `trade_type=APP`）会走小程序 JSAPI，下单返回 `无效的openid`，这解释了当前 App 点击后只创建/尝试订单但无法跳微信支付的表现。
- ADB 检查到模拟器 `emulator-5554` 在线，已安装微信和 Expo Go，但未安装正式 `cn.healthymax.foodlink` 包；当前 Android 原生模块只有微信登录/分享，没有微信支付 PayReq 调起方法。
- 本轮启动的临时后端 3010 已停止。

## App 支付暂缓与 main 后端重新部署
- 按短期策略暂缓 App 支付：pps/mobile/src/screens/MigrationScreens.tsx 中会员中心支付点击改为直接提示 App 支付暂未开放，说明当前只能到微信小程序完成会员开通或续费，不再创建订单。
- App API client 统一增加 X-Food-Link-Client: mobile_app，后端 handler 会读取该平台头作为 CreateMembershipPaymentInput.Client。
- 后端会员支付服务保留微信 APP 支付/支付宝预留接口与新版 wechat: 配置读取，但对 App 支付请求返回 501；同时拦截 pp-wx: / pp-phone: / pp-pwd: App-only openid 默认 JSAPI 下单，避免旧 App 用户继续生成无用小程序支付订单。
- 验证通过：go test ./internal/membership/service ./internal/membership/handler ./pkg/config ./internal/app -count=1、go build ./cmd/server、
pm run typecheck:mobile、
pm --workspace packages/core run typecheck、
pm --workspace packages/api-client run typecheck、
pm --workspace packages/api-client test -- --runInBand、git diff --check（仅 LF/CRLF warning）。
- 已执行 
pm run push-docker-ccr 推送 main 后端镜像，manifest digest sha256:d3238a4ae777bc1c6740e9686336550e085c92abd1f907254a5eb30ac2d47cf2；随后通过 kubectl rollout restart deployment/foodlink-main 滚动重启，线上 oodlink-main-5cc6478547-* 三副本 Running，https://api.healthymax.cn/api/health 返回 200 且命中新 Pod。
- 线上 Apollo main 当前仍以旧 external.* / pp_auth.* / wechat_pay.* 键为主，代码已兼容新旧格式；用户后续会把配置中心改成新版 wechat: 块。
- 本轮未发布 Android App 包；移动端前端弹窗需要下一次 App 包发布后用户才能看到，后端兜底已线上生效。

## main/dev 后端部署与新版 wechat 配置验收
- 已检查集群运行态：`foodlink-main` 与 `foodlink-dev` 均 3/3 Running，rollout 成功，后端容器 ready 且重启数为 0。
- main 后端实际镜像 digest：`sha256:d3238a4ae777bc1c6740e9686336550e085c92abd1f907254a5eb30ac2d47cf2`；dev 后端实际镜像 digest：`sha256:8b49308b700593d0416f2a917b99ae85f9dfcd24ade5e87a44828ed8305a8156`。
- 集群 Apollo bootstrap：main 使用 `cluster: main`，dev 使用 `cluster: dev`，namespace 均为 `app-config.yaml`。
- 已直接读取 Apollo 已发布配置并脱敏检查：main/dev 均包含新版 `wechat.mini_program`、`wechat.mobile_app`、`wechat.pay`；微信必需配置无缺失；旧 `app_auth` 与 `wechat_pay` 顶层块已移除；`external` 仅保留 LLM、Supabase/COS、地图等非微信外部服务键。
- Pod 启动日志确认 main/dev 后端均把 `wechat.mini_program.*`、`wechat.mobile_app.*`、`wechat.pay.*` 映射为后端配置键；`api.healthymax.cn` 与 `dev.api.healthymax.cn` 的 health 和 `/api/membership/plans` 均返回 200。

## 2026-06-22 - Admin-managed one-cent membership payment test
- Implemented a hidden membership payment test plan `test_one_cent_monthly` and DB-backed control tables `membership_payment_test_settings` and `membership_payment_test_users`.
- Admin now has a `/payment-test` page for toggling payment tests, searching users, and adding/removing test users; test users and enable state no longer belong in config-center YAML.
- Normal `/api/membership/plans` output excludes hidden test plans; attempting to purchase the hidden test plan is blocked unless the switch is enabled and the current user is allowlisted.
- Verification passed: membership/admin/app compile checks, migration package test/build, Admin build, and `git diff --check`. Repo sqlite behavior tests remain blocked by the local Windows `CGO_ENABLED=0` sqlite limitation.
- Migration was not executed because local config points at a remote PostgreSQL target. Run `cd backend && go run ./cmd/migration -config-dir .` only after confirming the target DB.
## 2026-06-22 - Payment test completion audit
- Re-audited the Admin-managed one-cent membership payment test implementation after goal continuation.
- Current evidence: Admin route `/payment-test` is wired; backend Admin routes `/api/admin/payment-test/*` are registered; migration DOs include payment test tables; membership purchase gate checks DB-backed enable/user allowlist for `test_one_cent_monthly`.
- Verification passed again: `go test ./internal/membership/service ...`, empty compile tests for membership/admin/app packages, `go test ./internal/migration/...`, temp-output `go build ./cmd/server`, temp-output `go build ./cmd/migration`, `npm --prefix admin run build`, and `git diff --check`.
- Source encoding check confirmed `admin/src/pages/payment-test-page.tsx` contains real Chinese text and no mojibake marker; PowerShell console output was the source of displayed乱码.
- Repo sqlite behavior tests for `TestMembershipRepo_ListActivePlans` and `TestMembershipRepo_GetPaymentTestAccess` are still blocked locally: default CGO disabled hits the go-sqlite3 stub; enabling CGO fails because `gcc` is not in PATH.
- Goal is code-complete, but live database migration remains pending because current backend config points at a remote PostgreSQL target and project rules require explicit user confirmation before running migration there.
## 2026-06-22 02:13:09 - Admin-managed payment test migration executed
- After explicit confirmation, executed go run ./cmd/migration -config-dir . from backend/ against the current Apollo local PostgreSQL target.
- Migration succeeded.
- Verified via read-only SQL: payment test settings/users tables exist; hidden/test columns exist on membership_plan_config; membership_payment_test_settings.default.enabled=false; hidden test plan 	est_one_cent_monthly exists at amount .01, duration_months 1, active 	rue, visible alse, test_plan 	rue; allowlist count is currently .

## 2026-06-22 - Payment test plan visibility fix
- The reason an allowlisted user still could not see the one-cent test plan in the mini program was not Admin config: the plans API was public and the mini program did not send Authorization, so the backend could not identify the current user and could only return visible normal plans.
- Backend change: `/api/membership/plans` now uses optional JWT and appends `test_one_cent_monthly` only when the DB payment-test switch is enabled and the current user is allowlisted.
- Mini program change: membership plan requests now include the stored access token and the membership page renders a separate payment-test card for the returned hidden test plan.
- Live verification with user `d5f7b38d-f4c6-4319-80d9-5e3f3ffb50bc` confirmed both `https://api.healthymax.cn` and `https://dev.api.healthymax.cn` return `test_one_cent_monthly`, amount `0.01`, `is_test_plan=true` when called with a valid token.
- Backend deployed: main code commit `940c271` and dev code commit `1382bd3`; both rollouts and public health checks passed.
- Important operational note: the already-published mini program package still cannot display the test card because it has old frontend code. A new mini program upload/release is required for users to see the one-cent test plan in WeChat.

## 2026-06-22 - Payment test card visibility in WeChat DevTools
- User confirmed the local mini program request to `http://192.168.8.193:3010/api/membership/plans` already returned `test_one_cent_monthly`, but the card was still not visible in the page.
- WeApp DevTools automation was enabled through `D:\IDE\微信web开发者工具\cli.bat auto --project K:\code\little-horse\food_link\apps\wechat\dist --auto-port 9420`.
- Diagnosis: backend and token path were correct; the card existed in the rendered tree but was too easy to miss in the lower purchase area.
- Frontend fix: moved the `payment-test-card` above the tier selector so an allowlisted test account sees `支付测试套餐` and `¥0.01` immediately after the membership hero.
- Runtime verification screenshot `tmp-payment-membership-target-area.png` showed the card above `选择档位` and the selected plan card changed to `Pay Test - 0.01 CNY` with amount `¥0.01`.

## 2026-06-22 - Payment test order must not affect membership
- User clarified the one-cent plan is only for real payment-link testing and must not switch, renew, downgrade, upgrade, or otherwise affect the user's real membership package.
- Frontend fix: when `test_one_cent_monthly` is returned, the membership page defaults to selecting it even for existing Pro members; the action button becomes `支付测试套餐 · ¥0.01`; age checks and package-switch restrictions are bypassed only for the test plan.
- Backend fix: test plan orders use fixed `payment_test` terms at the plan amount, do not supersede normal pending membership orders, and paid test orders update payment status only without calling `SaveMembership`.
- Added targeted service tests for creating a test payment while an active yearly membership exists and for verifying test payment activation does not mutate membership.

## 2026-06-22 - 餐次推荐逻辑后移与按记录推演
- 将小程序端默认餐次推断迁移到 Go 后端，新增 `/api/food-record/recommend-meal-type`。
- 后端推理先按作息/本地时间推断基础餐次，再查询当天记录；若目标正餐已存在且与最新记录间隔 > 1 小时，则按 `breakfast → lunch → dinner → evening_snack` 顺延。
- 前端新增 `getRecommendMealType` 和 `getRecommendedMealTypeWithFallback`，已替换分析页、手动记录页、文字记录页、结果页、文字结果页的默认餐次初始化。
- 验证：`go build ./...`、新增 service/handler 测试、`npx eslint` 改动文件通过；`npm run typecheck` 仅剩余既有错误。
- 微信开发者工具自动化端口 9420 当前未连接，运行时验证待后续补做。

## 2026-06-22 - AI 建议比例 UI 整合优化
- 结果页 `result/index.tsx` 中，将原本分散在「实际摄入」标签下方和进度条最右侧的 AI 建议比例提示，整合为进度条下方一行的「AI识别比例 X% · 一键应用」。
- 点击「一键应用」后，当前食物比例设为 AI 建议值，整行隐藏；手动调整比例后按现有逻辑重新判断是否显示。
- 验证：`npx eslint src/packageExtra/pages/result/index.tsx` 通过；`npm run typecheck` 仅剩余既有错误。
- 运行时验证待 `npm run dev:weapp` 编译并启动微信开发者工具后补做。

## 2026-06-22 - 移除「本餐评分」模块
- 后端删除 `backend/internal/analyze/service/score.go` 与 `score_test.go`。
- 后端 `analyze_service.go` 移除 `AnalyzeInput.ScoreEnabled` 字段、`applyScores`、`dashboardTargetsAsAnyMap` 函数及 `nutrition` 包引用；`finalizeAnalyzeResponse` 不再写入评分字段。
- 前端删除 `result/index.tsx` 与 `result-text/index.tsx` 中的 `ScoreTone`/`scoreToTone`/`scoreToLabel`、评分状态、结果解析赋值、本餐评分卡片 JSX。
- 前端删除 `result/index.scss` 与 `result-text/index.scss` 中全部 `.score-overview-*` / `.score-breakdown-*` 样式。
- 前端 `api.ts` 中 `AnalyzeResponse` 移除 `score_enabled`/`micronutrient_score`/`macro_balance_score`/`calorie_score`/`final_score`。
- 全局搜索确认 `apps/wechat/src` 已无「本餐评分」、score-overview 及相关字段残留。
- 验证：`go build ./...` 通过；`go test ./internal/analyze/service/...` 编译通过，7 个失败用例为既有模型选择问题，与本次改动无关；`npm run typecheck` 未新增本次改动导致的错误；SQLite 行为测试仍受本地 `CGO_ENABLED=0` / 无 gcc 限制无法运行。
- 微信开发者工具运行时验证需用户自行启动 `npm run dev:weapp` 后补做。

## 2026-06-22 - AI 饮食透视卡片支持折叠并全局记忆状态
- 新增 `apps/wechat/src/utils/ai-insight-collapsed.ts`，使用 `Taro.setStorageSync`/`getStorageSync` 以 `ai_insight_collapsed_v1` 为 key 持久化折叠状态。
- `result/index.tsx` 与 `result-text/index.tsx` 的「AI 饮食分析/透视」卡片标题栏改为可点击，右侧显示「▼ 收起」/「▶ 展开」。
- 点击标题栏切换折叠状态，同步写入本地缓存；页面挂载时读取缓存初始化状态，保证跨识别会话全局一致。
- 折叠时隐藏卡片内所有 insight-item，仅保留标题栏。
- 在 `result/index.scss` 与 `result-text/index.scss` 中添加 `.insight-card-header-toggle`、`.insight-toggle-affordance`、`.insight-toggle-icon`、`.insight-toggle-text` 样式，折叠时箭头旋转 -90°。
- 验证：`npx eslint` 改动文件通过；`npm run typecheck` 未新增本次改动导致的错误；`go build ./...` 通过。
- 微信开发者工具运行时验证待 `npm run dev:weapp` 编译并启动后补做。

## 2026-06-22 - WeApp build size diagnosis
- User pasted a production `npm run build:weapp` log where Taro compilation succeeded, but the post-build `scripts/check-weapp-package-size.mjs` guard failed.
- Failure is the main mini-program package only: `main 2062.4KB > 2048KB` by about 14.4KB. Total package size is safe at `5069.5KB`; `packageExtra` is close but still below the hard limit in that log at `1998.8KB`.
- Main-package largest files from the pasted build are `vendors.js`, `common.js`, `taro.js`, `app-origin.wxss`, `pages/index/index.wxss`, and Tab page JS/WXSS.
- Best first fix is to reduce global iconfont/style payload before moving more routes: `apps/wechat/src/assets/iconfont/iconfont.css` currently declares `woff2`, `woff`, `ttf`, and missing `svg`; Taro inlines those font files into `app-origin.wxss`. Keeping only `woff2`, or moving the icon font to the existing icon CDN path, should save more than the 14.4KB needed.
- Longer-term follow-up: split the nearly-full `packageExtra` into smaller domain subpackages and keep dark-theme/page-specific styles out of global app styles where possible.

## 2026-06-22 - Full-flow one-cent membership payment test with Admin restore
- User changed the test strategy back to full end-to-end membership activation: `test_one_cent_monthly` should activate membership after payment, not just validate the payment link.
- Implemented Admin-only, test-user-only cancel/restore membership operations under `/api/admin/payment-test/users/:user_id/cancel-membership` and `/restore-membership`.
- Cancellation snapshots the current `user_pro_memberships` row into `membership_payment_test_users.membership_snapshot`, clears the membership to inactive, and blocks deleting that allowlist entry until restore.
- Restore writes the saved snapshot back to `user_pro_memberships`, clears the pending snapshot, and leaves audit fields for cancelled/restored timestamps/operators.
- Excluded test paid orders from latest-paid-order reconciliation and first-paid-user-rank so restoring the original membership is stable and one-cent tests do not pollute paid-user benefit ranking.
- Verification passed: membership service targeted tests, membership handler tests, admin/app/migration compile checks, `go build ./cmd/server`, `npm --prefix admin run build`, and `git diff --check`; full membership repo behavior tests are still blocked by the known local sqlite CGO limitation.
- Pending before deploy: run backend migration against the target database to add the new `membership_payment_test_users` snapshot/audit columns.

## 2026-06-22 15:18 - Full-flow payment test migrated and deployed
- Executed `cd backend && go run ./cmd/migration -config-dir .` against the current Apollo local PostgreSQL target; migration completed successfully.
- Pushed code commit `91584ed` to `origin/dev`; cherry-picked as `ab30538` and pushed to `origin/main`.
- Pushed backend images: `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev` digest `sha256:ce013b4d28f9a281eddfe1d1343be52bdc0e49da9655c85f7decd8e05b58b447`; `:main` digest `sha256:9b2d776bc92d344613765a8870769ec10c3e1dd47ebf488c926fbd310af2a7e5`.
- Verified Kubernetes business containers for `foodlink-dev` and `foodlink-main` are running those digests; public health checks returned 200 and the new Admin cancel-membership endpoint returns 401 without auth on dev/main, confirming route deployment.
- Admin frontend should auto-deploy from the pushed branches; mini program frontend was not changed in this round.
## 2026-06-22 - 修复食物库列表能量与实际记录能量不一致
- 问题：手动记录页食物列表显示能量（如白米饭 178g/227kcal）与点击加号后实际记录能量（206kcal）不一致。
- 根因：后端 `ManualFoodRepo.mergeNutritionLibraryIntoManualFoodResult` 为最近记录/高频食物补齐微量元素时，直接用营养库完整的 `nutrients_per_100g` 覆盖原有值，导致宏量营养素被营养库数据（白米饭 116kcal/100g）覆盖，与 `total_calories` 不一致。
- 后端修复：新增 `mergeManualFoodNutrients` helper，保留现有宏量营养素（calories/protein/carbs/fat），仅在缺失时用营养库兜底；微量元素仍从营养库补齐。
- 前端修复：`record-manual/index.tsx` 的 `buildNutrientsFromWeight` 改为优先以 `baseNutrients`（即列表显示能量）按重量比例缩放，避免 `nutrients_per_100g` 与 `total_calories` 不一致时再次出现差异。
- UI：为 `record-manual/index.scss` 的 `.food-list` 增加 `padding-top: 8rpx`，保持列表与上方筛选区的上间距。
- 验证：`go build ./...` 通过；新增 `TestMergeManualFoodNutrients_*` 单元测试通过；`npx eslint` 与 `npm run typecheck` 无新增错误；依赖 SQLite 的 repo 测试仍受本地 CGO 限制无法运行。

## 2026-06-22 - 登录页隐藏测试手机号密码注册入口
- 在小程序登录页增加长按顶部「智健食探」图标/标题 3 秒后触发的小字「手机号密码注册」，用于测试邀请好友链路。
- 只允许注册固定手机号 `13511679220`；密码用户输入；昵称自动生成 `测试用户_YYYYMMDDHH`。
- 后端新增 Apollo/本地配置 `app.allow_debug_register` 控制开关，并新增公开接口 `GET /api/app/public-config`。
- 后端 `POST /api/app/register/password` 增加兜底校验：开关关闭或非指定手机号均拒绝。
- 前端新增 `getPublicConfig()` 和 `registerWithPassword()`；登录页复用现有 `handleLoginSuccess` + `finishLoginFlow` 处理登录态与邀请跳转。
- 验证：`go build ./...` 通过；`npx eslint` 改动文件通过；`npm run typecheck` 中本次改动文件无新增错误；依赖 SQLite 的后端行为测试仍受本地 `CGO_ENABLED=0` 限制无法运行；微信开发者工具端口 9420 未监听，运行时验证待后续补做。

## 2026-06-22 - 分享卡片二维码邀请链路修复
- 用户确认希望扫描分享卡片右下角二维码进入小程序的用户，在注册成功后能触发邀请关系写入数据库并进入奖励流程。
- 修复 `apps/wechat/src/app.ts`：原先只在 `useLaunch` 处理扫码 scene，app 热启动时扫码不生效；现在抽离 `handleInviteScene` 并增加 `Taro.onAppShow` 监听，覆盖冷启动和热启动。
- 修复 `backend/internal/auth/repo/user_repo.go`：`CreateInviteReferralBinding` 原写入 `status = "pending"`，与数据库 check 约束和奖励状态机不匹配，改为 `"pending_qualified"`。
- 当前奖励仍为「注册后 7 天内完成 2 个自然日有效记录，双方各得 15 积分」；如需改为注册即奖励，需要额外改动。
- 验证：`go build ./...` 通过；`npx eslint src/app.ts ...` 通过；`npm run typecheck` 本次改动文件无新增错误；后端测试仍受本地 CGO/SQLite 限制无法运行。

## 2026-06-22 - 分享卡片二维码按运行环境跳转
- 为方便测试，将分享卡片/邀请页二维码的 `env_version` 从固定 `'release'` 改为随当前运行环境自动选择。
- 新增 `apps/wechat/src/utils/api.ts#getWeappEnvVersion()`，基于 `Taro.getAccountInfoSync().miniProgram.envVersion` 返回 `develop` / `trial` / `release`。
- 修改 5 处调用：`packageExtra/pages/day-record/index.tsx`、`packageExtra/pages/record-detail/index.tsx`、`packageExtra/pages/invite-friends/index.tsx`、`pages/index/index.tsx`、`pages/index/components/MealRecordPosterModal.tsx`。
- 验证：`go build ./...` 通过；`npx eslint` 改动文件通过；`npm run typecheck` 本次改动文件无新增错误（`day-record` 既有类型错误除外）。

## 2026-06-22 - 登录页显示开发版/体验版标识
- 为便于区分运行环境，登录页「智健食探」名称右侧在开发版和体验版时显示对应标签：「（开发版）」或「（体验版）」，正式版保持原样。
- 通过 `getWeappEnvVersion()` 读取 `Taro.getAccountInfoSync().miniProgram.envVersion`。
- 验证：`npx eslint` 与 `npm run typecheck` 改动文件通过。

## 2026-06-22 - 登录页环境标识排查
- 用户反馈在开发者工具模拟器中运行 `build:weapp:release` 后登录页未显示开发版标签。
- 将登录页「智健食探」标签实现从嵌套 `<Text>` 改为同一 `<Text>` 内字符串拼接，减少渲染异常可能。
- 在 `getWeappEnvVersion()` 中增加 `globalThis.wx.getAccountInfoSync` 兜底调用。
- 在登录页 `useEffect` 中增加 `console.log('[login-env] 当前小程序环境版本:', version)`，便于在开发者工具控制台确认实际取值。
- 验证：`npx eslint` 与 `npm run typecheck` 改动文件通过。
## 2026-06-22 - WeApp build size iconfont slimming
- Build-size failure was the main package only: `main 2062.4KB > 2048KB`; total package size was safe and `packageExtra` was close but under the limit.
- No packed image files were found in the main package. The pasted `packageExtra/pages/health-profile/index.wxss` was not a static-resource issue: it had no `data:` or `url(...)` references.
- Cause: global iconfont CSS referenced `woff2`, `woff`, `ttf`, and `svg`; Taro inlined the available `woff2`, `woff`, and `ttf` into `app-origin.wxss`.
- Fix: keep only `iconfont.woff2`, delete unreferenced `iconfont.woff` and `iconfont.ttf`, and make `scripts/update-icon.py` preserve woff2-only iconfont output.
- Verification: `python -m py_compile scripts/update-icon.py`; projected main package after rebuild about `2006.7KB`, saving about `55.7KB`; `git diff --check` passed with existing LF/CRLF warnings.

### WeApp DevTools verification addendum
- Connected to existing WeChat DevTools automation service on port 9420 via `miniprogram-automator`; `mrc` CLI was not available in PATH.
- Navigation check passed: switched to `pages/profile/index`, confirmed a root node exists, then switched back to `pages/index/index`.
- Screenshot attempt via `miniProgram.screenshot` timed out in the current automation environment, so no screenshot artifact was produced.
- Runtime verification of the new slimmed CSS itself is blocked until the mini-program dist is regenerated; the currently loaded `apps/wechat/dist/app-origin.wxss` still contains the old inlined fonts.

## 2026-06-22 - Payment test sync failure fixed and deployed
- Trial mini-program one-cent payment symptom: WeChat payment completed, but membership still appeared inactive.
- Root cause: dev API created order `PM2026062215563039BA8F3E` and WeChat active query returned `SUCCESS`, but `/api/membership/pay/sync` failed while writing raw `map[string]any` into payment JSONB field `notify_payload`; the payment row stayed `pending`, so membership activation did not run.
- Repair: replayed sync through the fixed repo path; the affected user now has active `test_one_cent_monthly` membership expiring `2026-07-22T15:56:37+08:00`.
- Code fix: `UpdatePaymentByOrderNo` now normalizes `notify_payload` and `extra` map values to `datatypes.JSONMap`; migration constraint now allows `expired` payment status; mini-program test payment copy and sync result handling now reflect full membership activation.
- Deployed: dev commit `bf49e8f` with image digest `sha256:b7ebe08702a5630add52164dd25507e0544097e83c20145269d1b49a06bd26ab`; main commit `3f8c245` with image digest `sha256:dbe189921fd7ad5fb2711d426a21259d0e4606ce80c6375189faf9e370369964`; both rollouts are 3/3 ready and health checks returned 200.
- Verification limits: WeApp runtime automation was blocked because `mrc` is not available in PATH; `npm --workspace apps/wechat run typecheck` remains blocked by unrelated pre-existing TypeScript errors in other pages.

## 2026-06-22 - Admin feedback direct link and sidebar grouping
- User asked whether Admin feedback can be opened directly by feedback ID for a Feishu bot button, and also asked to make the left menu less flat.
- Confirmed the existing feedback-bot Admin URL builder already uses `/feedback?feedback_id=<id>`, but Admin frontend did not yet honor that parameter.
- Implemented Admin support for `/feedback?feedback_id=<id>` and `/feedback?id=<id>`: the page applies all-status/all-category filters, searches by ID, and selects the matching feedback.
- Backend Admin feedback list search now includes `user_feedback.id`, allowing the existing `/api/admin/feedback` endpoint to resolve the ID without adding a new route.
- Feedback detail now has a "复制直达链接" button.
- Admin sidebar grouping is now 工作台 / 用户声音 / 内容数据 / 实验工具 / 系统.
- Verification passed: Admin build, backend admin compile, admin lightweight tests, and targeted diff check. No local server was started.

## 2026-06-22 - Admin feedback direct link deployed to dev
- Committed Admin feedback direct-link/sidebar grouping change on `dev` as `45f31e1` and pushed `origin/dev`; Admin frontend should auto-deploy from the pushed branch.
- Backend deploy was required because `/api/admin/feedback` search now includes `user_feedback.id`; ran `npm run push-docker-ccr` from repo root.
- Pushed `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev` with manifest digest `sha256:7f7298bc3cfa5d83b77c518b6b69f654cf474288c5571737232e8401e4c8c0ea`.
- Verified `foodlink-dev` rollout completed: 3/3 ready, all business containers use the new digest, restart counts 0.
- Public dev health returned 200 and unauthenticated `/api/admin/feedback?...` returned 401, confirming the Admin feedback route is live.
- Note: the final pushed commit contains only the three target code files; an initial local commit attempt was corrected before push because the pre-commit hook runs `git add -u` and would have mixed unrelated iconfont/package-size changes into the deployment commit.

## 2026-06-22 - Admin feedback direct link deployed to main
- User clarified Admin uses the main backend, not dev, so the same Admin feedback direct-link/sidebar grouping change was promoted to `main`.
- Used a temporary clean main worktree to avoid mixing unrelated dirty dev working-tree files; cherry-picked dev commit `45f31e1` as main commit `fb2ccbb` and pushed `origin/main`.
- Verification on main worktree passed: Admin build, backend admin compile, admin lightweight tests, and `git diff --check HEAD~1..HEAD`.
- Ran `npm run push-docker-ccr` on main and pushed `ccr.ccs.tencentyun.com/littlehorse/foodlink:main` manifest digest `sha256:cc05fcb5b98fae1d723daa618f727452ecf1ccb03e600dfc7a3675dda2005e4c`.
- Verified `foodlink-main` rollout completed: 3/3 ready, all business containers use the new digest, restart counts 0.
- Public main health returned 200 and unauthenticated `/api/admin/feedback?...` returned 401, confirming the route is live on main.
- Removed the temporary main worktree after verification.b50be3655adcd33e5c7cc9c5ae9c1006bcb350fa

## 2026-06-22 - 登录页测试入口与邀请链路修复部署到 dev
- 将 main 分支上的登录页测试注册入口、分享卡片二维码环境适配、邀请链路状态修复等改动合并到 dev 分支（commit `7227be76`）并推送 `origin/dev`。
- 执行 `npm run push-docker-ccr` 成功推送 dev 后端镜像。
- 验证 `https://dev.api.healthymax.cn/api/health` 返回 `{"status":"ok"}`，dev 后端已更新。

## 2026-06-22 - 邀请绑定未写入问题排查与加固
- 用户反馈用体验版账号分享卡片二维码，退出后扫描并注册新账号，但数据库中没有邀请记录，积分奖励也不会生效。
- 数据库验证：最新 `user_invite_referrals` 记录停留在 2026-05-03；新注册账号 `referred_by_user_id` 为 NULL。
- 可能原因：登录页注册时邀请码未成功透传。
- 修复 `apps/wechat/src/packageExtra/pages/login/index.tsx`：邀请码优先取 URL 参数，空时回退 `pending_friend_invite_code` 本地存储，并打印日志。
- 修复 `backend/internal/auth/service/login_service.go`：在密码注册、微信登录、App 微信登录三条新用户路径增加邀请码与绑定日志；`bindInviteReferral` 细化每一步日志与错误返回。
- 已提交 dev（`23f47762`）并推送，dev 后端镜像重新构建部署中。

## 2026-06-22 - 邀请绑定日志版后端重新部署
- 推送 dev 后端镜像完成，`https://dev.api.healthymax.cn/api/health` 返回 `{"status":"ok"}`。
- 现在可以重新测试分享卡片扫码注册，通过前后端日志定位邀请码是否透传、绑定是否成功。

## 2026-06-22 - 会员卡片下方显示邀请奖励进度
- 在会员页 `plan-card` 下方新增圆角矩形提示容器：被邀请人显示「再记录 X 天，即可获得邀请奖励 15 积分」；邀请人显示「你邀请的好友 xxx 再记录 X 天，你们各得 15 积分」。
- 后端新增 `GET /api/membership/invite-reward-status`，按 `pending_qualified` 状态、7 天窗口、首次有效记录计算 `records_needed`。
- 前端在 `pro-membership/index.tsx` 加载时请求新接口，条件渲染 `.invite-reward-hint-card`，并在 `index.scss` 中新增对应样式与 dark 模式。
- 验证：`go build ./...` 通过；`npx eslint` 改动文件通过；`npm run typecheck` 无新增本次改动导致的错误；编译产物已包含新容器与接口调用。
- 后端已部署 dev：`npm run push-docker-ccr` 成功；新接口未登录返回 401，确认路由上线。
- 运行时验证尝试：启动了 `npm run dev:weapp` 与微信开发者工具自动化（端口 9420），但会员页需登录态；尝试 `mrc mock` 拦截接口返回数据时提示 `mp.mockJson is not a function`，未能完成带数据截图，待提供已登录测试账号后补做。

## 2026-06-22 - 注册流程在选择性别前增加头像昵称步骤
- 在健康档案注册流程的 `PROFILE_STEPS` 最前面新增 `'profile'` 步骤，原「性别」步骤后移。
- 新增头像选择（`chooseAvatar`）、昵称输入（`type='nickname'`）和「一键使用微信头像和昵称」按钮（`Taro.getUserProfile`）。
- 保存健康档案前先把头像昵称通过 `updateUserInfo` 写入用户资料，头像本地路径先上传 COS。
- 验证：`npx eslint src/packageExtra/pages/health-profile/index.tsx` 通过；`npm run typecheck` 本次改动文件无新增错误。

## 2026-06-22 - 我的页昵称单行省略与个人主页箭头调整
- `apps/wechat/src/pages/profile/index.tsx`：删除 `ProfileChevron` 组件，「个人主页」右侧改用 `iconfont icon-right`。
- `apps/wechat/src/pages/profile/index.scss`：`.user-name` 增加 `white-space: nowrap`、`text-overflow: ellipsis`、`overflow: hidden`，保证昵称单行省略。
- 验证：`npx eslint src/pages/profile/index.tsx` 通过；`npm run typecheck` 本次改动文件无新增错误。
