# 2026-06-19

## 用户“小渝儿～”0 热量识别记录排查

- 用户 ID：`e479caa5-e781-4f7b-b0ba-65f7d11fb54f`。
- 异常任务：`90086254-92d5-47f4-98bd-95b72b98cb1b`，`food/done`，创建于 `2026-06-19 01:34:19+08`。
- 任务结果为 `items=[]`、`description=无法获取描述`，对应保存记录 `e999ce68-1da9-4fde-b736-0a6295399fd5` 落为 `total_calories=0`。
- 用同一组三张 CDN 图片调用本机同步 `/api/analyze`：
  - 不带用户身份：返回 4 个 item，总热量约 `776.15 kcal`，未复现空 items。
  - 带该用户调试 JWT：客户端 90 秒超时，但服务端日志显示最终也返回 4 个 item、总热量非 0；末尾建议比例生成因客户端断开取消。
- 判断：原始问题不是当前稳定可复现的图片/数据问题，更像是当时 Gemini 返回空 `items` 后，后端把空识别结果当成功保存。后续应在任务完成和保存饮食记录前拦截空 items。

## 比例生成失败默认 100%

- 用户要求建议比例和可食用比例生成失败时不要直接错误，默认给出 `100%`。
- 已修改 `backend/internal/analyze/service/analyze_service.go`：
  - DeepSeek 可食比例生成失败时，每个 item 默认 `ediblePortionRatio=100`，状态 `edible_portion_status=fallback`、`edible_portion_fallback_reason=generation_failed`。
  - DeepSeek 未配置时也按 100% 可食比例兜底，不再沿用旧推导比例。
  - Gemini 建议比例生成失败时，每个 item 默认 `suggestedRatio=100`，状态 `suggest_ratio_status=fallback`、`suggest_ratio_fallback_reason=generation_failed`。
- 已补单测覆盖失败兜底：`TestAnalyzeService_ApplyEdiblePortionRatiosFailureDefaultsTo100`、`TestAnalyzeService_ApplySuggestedRatiosFailureDefaultsTo100`。
- 验证：定向 `go test ./internal/analyze/service -run ... -count=1` 通过，`git diff --check` 通过。完整 `go test ./internal/analyze/service -count=1` 仍有既有失败，非本次修改引入。
