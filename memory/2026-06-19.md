# 2026-06-19

## 2026-06-19 - 拉取远端最新代码并合并到本地

- 用户要求拉取最新代码并合并到本地。
- 拉取前本地 `main` 有 3 个未提交改动：`apps/wechat/src/components/FlPageThemeRoot.tsx`、`apps/wechat/src/styles/fl-color-scheme-dark.scss`、`memory/2026-06-18.md`。
- 已用 `git stash push -m "codex-before-pull-20260619"` 保护本地改动，再执行 `git fetch --all --prune`。
- 远端 `origin/main` / `origin/dev` 从 `1b9d9d77` 更新到 `8fc04751`。
- 本地 `main` 已 fast-forward 合并 `origin/main` 到 `8fc04751`。
- 已 `git stash pop` 恢复本地改动，无冲突；本地仍保留上述 3 个未提交改动。
- 本地 `dev` 指针已同步到 `origin/dev`。

## 2026-06-19 - 宠物聊天首页入口被回退原因定位
- 用户反馈首页宠物聊天入口消失。
- Git 记录定位：`0aec20c7 feat: add pet chat history and specialty analysis` 由 SmallHorseBrother 在 2026-06-18 10:30:39 添加首页 `pet-chat` 入口。
- 后续 `7ca325b4 revert(weapp): 将首页主要代码回退到一天前版本` 由 LSTM-Kirigaya 在 2026-06-18 23:37:11 回退 `apps/wechat/src/pages/index/index.tsx` / `index.scss`，删除了 `openPetChat` 和 `.pet-companion-card` 上的 `onClick={petCollapsed ? togglePetCollapsed : openPetChat}`，导致首页入口消失。
- 当前已在未提交改动中恢复：展开态点宠物卡进入 `packageExtra/pages/pet-chat/index`，收起态点宠物展开，并给领取/收起按钮 stopPropagation。
- WeApp 自动化验证：`mrc where --port 9420` 可连接；点击 `.pet-companion-card` 后当前页面为 `packageExtra/pages/pet-chat/index`。截图命令因 DevTools 内部路径错误失败，未取得截图证据。

## 2026-06-19 - 撤销首页整体回退 7ca325b4
- 用户要求撤销 `7ca325b4 revert(weapp): 将首页主要代码回退到一天前版本`，并明确此前提交的改动必须全部保留。
- 已先 `git stash push --include-untracked -m "codex-before-revert-7ca325b4"` 保护当前未提交改动。
- 已执行 `git revert --no-commit 7ca325b4`，无冲突；恢复了该回退删除的首页改动，包括 `MicrosSection`、首页营养区改动、宠物聊天入口 `openPetChat` 与 `.pet-companion-card` 跳转。
- 已从 stash 恢复非首页本地改动：`FlPageThemeRoot.tsx`、`fl-color-scheme-dark.scss`、`memory/2026-06-18.md`、`memory/2026-06-19.md`。
- 当前撤销结果尚未提交；`stash@{0}` 仍保留为安全备份，避免误删此前未提交内容。

## 2026-06-19 - 鹅鸭鸡专线 timeout 与模型透传修复

- 用户贴出专线日志：一次 `/api/analyze/goose-duck-chicken` 约 90s 后 `context deadline exceeded` 返回 500，另一次约 19.9s 成功；小程序端同时出现请求 timeout、HTTP 图片协议告警和 console log 写入超限。
- 根因排查：
  - 专线前端和后端同步调用都卡在 90s deadline 附近，外部模型慢请求会造成前后端同时失败。
  - 通用精确 JSON 调用此前只把 `modelName` 用于 provider/日志，不会覆盖 `OfoxAIClient` / `DoubaoClient` 请求体里的 `model` 字段；因此 `gpt-4.1:stable` 可能没有真正传到上游。
  - `authenticatedRequest` 的 `[DEBUG AUTH]` 高频 console 会放大小程序日志/诊断缓存压力。
- 已修复：
  - `backend/internal/analyze/service/llm_client.go` 增加 `AnalyzeWithImagesAndTemperatureModel`，支持单次调用覆盖模型名。
  - `backend/internal/analyze/service/analyze_service.go` 将解析后的具体模型名透传到 client 请求体，并把 LLM 指标里的 model 从空值改为真实解析模型。
  - `backend/internal/analyze/service/goose_duck_chicken_service.go` 改为主模型 `gpt-4.1:stable` 55s + 备用 `gpt-4.1-mini:stable` 45s；瞬时类错误最终返回 504 和中文可读错误。
  - `apps/wechat/src/utils/api.ts` 专线 timeout 调整为 125s，并移除认证请求开始/完成的 debug console。
  - 本地后端配置文件当前 CDN base URL 为 HTTPS；这些配置文件不在 Git HEAD 中，若运行态仍返回 HTTP，需检查运行服务是否重启以及外部配置源是否仍为 HTTP。
- 验证：
  - `go test ./internal/analyze/service -run "TestOfoxAIClient_AnalyzeWithImagesAndTemperatureModel_OverridesRequestModel|TestAnalyzeService_RunPrecisionJSONFallsBackToDoubaoOnGeminiTransientError|TestAnalyzeService_RunPrecisionJSONNoFallbackKeepsGeminiError|TestResolveModelConfig"` 通过。
  - `go test ./internal/analyze/handler -run "TestAnalyzeHandler_ClassifyGooseDuckChicken"` 通过。
  - scoped `git diff --check` 通过。
  - WeApp DevTools 9420 可连接；已 relaunch 到 `packageExtra/pages/goose-duck-chicken/index`，textarea 存在且输入成功，错误日志 0 条。
  - 截图命令仍失败：`mrc screenshot ...` 返回 `fail to capture screenshot`。
- 后续：需要重启后端服务后，这次模型透传、fallback 和 timeout 修复才会影响运行态；如 HTTP 图片 URL 仍出现，更新实际配置源的 CDN base URL 为 HTTPS。
## 2026-06-19 - 提交当前代码并同步 main/dev

- 用户要求提交当前代码，并保持开发分支和主分支一致。
- 本次提交包含：首页微量营养展开与主包瘦身相关改动、鹅鸭鸡专线请求超时和 `gpt-4.1` / `gpt-4.1-mini` fallback、OpenAI-compatible client model override 支持，以及当天/昨天 memory 记录。
- 提交前验证：`go test ./internal/analyze/service -run "TestOfoxAIClient_AnalyzeWithImagesAndTemperatureModel_OverridesRequestModel|TestResolveModelConfig|TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode"` 通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 后续将把最终同一个 HEAD 推送到 `origin/main` 和 `origin/dev`。

## App 首页、图标与图片上传体验修复

- 用户反馈 Android 桌面图标被裁切、App 名称仍是 `Food Link`、首页顶部运营位只有单个不可滑动、首页“今天吃什么”在真机竖排，以及选图提交时报 `Unsupported FormDataPart implementation`。
- 已修复：Android 展示名改为 `食探`；重新生成带安全边距的 launcher/adaptive icon；首页 banner 改为 4 个可横向滑动入口并带分页圆点；记录入口改为稳定 2x2 网格；图片上传从 `fetch(FormData)` 改为 `XMLHttpRequest` multipart。
- 已验证：按用户授权重启本地后端 `3010` 和 Metro `8081`，ADB/Expo Go 打开 `exp://192.168.8.193:8081`，首页轮播和 2x2 记录入口截图通过；选图后 `/api/upload-analyze-image-file` 返回 200，不再出现 FormDataPart 错误。
- 当前上传后的分析提交因测试账号食物分析积分不足返回 402：`食物分析积分不足，本次需要 2 积分，当前可用 0 积分`。这说明 multipart 上传问题已越过，后续如需完整分析成功需给测试账号补足积分或换有积分账号。
- 注意：Expo Go 只能验证 JS 页面和上传链路；Android 桌面图标与 App 名称需要重新构建/安装 APK 后才能在启动器中体现。

## 小程序宠物聊天功能复查

- 用户询问微信小程序宠物是否还有聊天功能，怀疑同事之前写过但后来被覆盖，因为 App 侧有同类功能且当时参考过小程序。
- 复查结论：小程序宠物聊天当前仍在代码和产物中。页面位于 `apps/wechat/src/packageExtra/pages/pet-chat/`，路由在 `apps/wechat/src/app.config.ts` 注册为 `packageExtra/pages/pet-chat/index`，当前 `apps/wechat/dist/app.json` 与 `dist/packageExtra/pages/pet-chat/` 也包含该页面。
- 当前入口：小程序首页悬浮宠物展开态点击进入 `extraPkgUrl('/pages/pet-chat/index')`；宠物主页 `apps/wechat/src/packageExtra/pages/pet-home/index.tsx` 的“去问问它”也进入同一路由。
- 历史证据：`0aec20c feat: add pet chat history and specialty analysis` 同时新增小程序 pet-chat 页面、首页入口、pet-home 入口、路由和 API；该提交已包含在 `origin/main` 与 `origin/dev`。未发现后续提交删除小程序 pet-chat。
- 可能误判原因：`home_pet_companion_hidden_v1` 会让首页悬浮宠物完全隐藏，`home_pet_companion_collapsed_v1` 会让它收起成小气泡；若用户隐藏/收起过，首页会像“宠物聊天入口没了”。当前“我的 -> 清除缓存”只移除 collapsed 与 float position，没有移除 hidden。

## 小程序“我的”页白屏与 7ca325b 回退复查

- 用户反馈小程序点进“我的”页白屏，并提供日志：`ProfilePage` 抛出 `Minified React error #130`，参数为 `undefined`。
- 复查 `7ca325b revert(weapp): 将首页主要代码回退到一天前版本`：该提交只修改 `apps/wechat/src/pages/index/*` 相关文件，删除 `MicrosSection`，回退首页 banner、营养概览展开、首页宠物点击聊天入口等；没有修改 `apps/wechat/src/pages/profile/index.tsx`。`7ca325b^` 与 `7ca325b` 中 profile 页均已存在 `Navigator` 快捷入口，因此它不是“我的”页白屏的直接引入提交。
- `7ca325b` 确实回退/删除了部分新加首页功能：`MicrosSection` 微量营养组件、鹅鸭鸡专线首页 banner、首页宠物点击进入 pet-chat、首页 banner 跳转失败 toast/容错、iconfont CDN canvas 字体加载等。
- 已修复“我的”页一个明确崩溃点：将 `apps/wechat/src/pages/profile/index.tsx` 中登录后快捷入口从 `Navigator` 改为 `View` + 既有 `handleQuickActionClick(extraPkgUrl(...))`，避免当前 Taro/微信运行时渲染 undefined 组件触发 React #130。
- 验证：`npx eslint apps/wechat/src/pages/profile/index.tsx` 通过；`apps/wechat/dist/pages/profile/index.js` 已随 watch 更新且不再包含 `taro.Navigator`；`npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有分包类型错误阻塞，未指向 `profile/index.tsx`。
- weapp-devtools：`cli.bat auto --project K:\code\little-horse\food_link\apps\wechat --auto-port 9420 --trust-project` 成功，`miniprogram-automator` 可连接并切到 `pages/profile/index`；页面 data 中已有用户信息和快捷入口节点。截图仍只显示底部 tabBar，疑似当前 DevTools/Taro 模板刷新会话异常；连续 tab 截图脚本超时，未完成可视层最终确认。本轮未运行 `npm run build:weapp`，也未启动/重启本地前后端。

## 2026-06-19 App 真机布局、验证码倒计时与关于/反馈拆分

- 用户反馈真机上 App 整体字号和卡片偏大、部分文字超宽；同时验证码发送按钮没有倒计时，意见反馈页不应混入关于信息。
- 已修复：App 全局限制 Text/TextInput 最大字体放大倍率为 1.12；移动端 Page/Card/Button/Login/Home/Community/TabBar 做密度收敛；首页统计、banner、记录入口、圈子搜索和动态标题增加行数/自适应约束。
- 已修复：验证码发送成功后优先按后端 `cooldown_seconds` 或 `retry_after_seconds` 倒计时；旧接口未返回时用当前默认节流值 30 秒兜底，倒计时期间按钮禁用。
- 已修复：新增独立 `About` 路由和 `AboutScreen`；“我的”页中“意见反馈”进入纯反馈表单，“关于我们”进入关于页；反馈页不再显示关于卡、隐私设置和协议社群。
- 验证通过：`npm --workspace apps/mobile run typecheck`；scoped `git diff --check` 仅有既有 LF/CRLF 提示。
- ADB/Expo Go 验证通过：登录页密度收敛、首页 2x2 与轮播正常、圈子按钮不溢出、反馈页纯反馈、关于页独立显示且长文本未溢出。截图：`tmp/mobile-layout-after-open.png`、`tmp/mobile-after-login-attempt.png`、`tmp/mobile-community-density.png`、`tmp/mobile-feedback-only.png`、`tmp/mobile-profile-about-visible.png`、`tmp/mobile-about-page.png`。
- 注意：本轮未运行 `npm run build:weapp`；Expo Go 可验证 JS 页面，用户已安装 APK 需重新构建安装后才能看到最新改动。

## 微信登录账号平台配置澄清

- 复核现有后端实现：小程序登录走 `external.appid` / `external.secret`，调用微信小程序 `jscode2session`；App 侧微信登录走 `app_auth.wechat_app_id` / `app_auth.wechat_app_secret`，调用微信开放平台 OAuth `sns/oauth2/access_token`。
- 因此 App 点“微信一键登录”若提示缺少 `app_auth.wechat_app_id / app_auth.wechat_app_secret`，需要配置微信开放平台“移动应用”的 AppID/AppSecret；不能直接拿公众平台小程序 AppID/AppSecret 当作原生 App 登录凭证。
- 现有公众平台小程序账号仍可继续用于小程序；建议在微信开放平台把移动应用和当前小程序绑定到同一个开放平台账号下，用 UnionID 打通跨端账号。

## 微信开放平台移动应用审核素材

- 已为创建微信开放平台移动应用生成审核素材到 `tmp/wechat-open-platform/`：`shitan-icon-watermark-28x28.png`、`shitan-icon-hd-108x108.png`、`shitan-app-operation-flow.png`。
- 28x28 水印图与 108x108 高清图均由 `apps/mobile/assets/icon.png` 等比缩放生成，PNG 且小于 300KB；运行流程图为 1600x1000 PNG，约 185KB，小于 5MB。
- 申请说明建议粘贴：`食探是AI饮食与健康管理App，用户可通过手机号验证码或微信登录，拍照/相册上传食物图片后获得营养分析，保存饮食记录，查看健康趋势，并可在圈子分享互动。当前功能无需固定测试账号，可用手机号验证码注册登录。`
- 微信开放平台“填写应用平台信息”页：当前只配置 Android 应用，选择“开发Android应用=是”，应用签名填 release APK 证书 MD5 `720ab81809518f6b54a2a00c493acc07`，应用包名填 `cn.healthymax.foodlink`，应用已上架按当前事实选“否”，开发方式保留“传统模式开发”。

## App 安装显示名称改为智健食探

- 用户确认希望 APK 安装后的桌面名称从 `食探` 改为 `智健食探`。
- 已修改 `apps/mobile/app.json` 的 `expo.name` 和 `apps/mobile/android/app/src/main/res/values/strings.xml` 的 `app_name`；现有已安装 APK 需要重新构建安装后才会显示新名称。
- 验证：`node -e` 读取 `apps/mobile/app.json` 确认为 `智健食探`；`git diff --check -- apps/mobile/app.json apps/mobile/android/app/src/main/res/values/strings.xml` 通过，仅有既有 LF/CRLF 提示。

## 2026-06-19 App 食物识别选项与等待态修复

- 用户反馈 App 食物识别页缺少小程序侧已有的识别选项，进入识别后只显示“正在分析”，且旧等待态用 60 次/60 秒倒计时不合理。
- 已修复：`apps/mobile/src/screens/AnalyzeScreen.tsx` 改为先选图再确认提交，补回快速/普通/精准模式、联网校准、分项模式、多视角辅助、AI 摄入比例、餐次、运动时机、文字补充、日期和识别记录等入口；拍照/相册入口不再立刻提交分析。
- 已修复：`AnalyzeLoadingScreen` 移除 `maxAttempts=60` 与固定等待超时，改为显示“已等待 x 秒/分”、任务状态和“先离开 / 识别记录”按钮；后台继续轮询任务，网络波动才短暂重试，后端明确失败则直接提示。
- 已补充：`packages/core/src/types.ts` 的 `AnalyzeTaskSubmitParams` 增加 `suggest_ratio_enabled`，App 提交时与小程序同源传参。
- 验证：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck` 均通过；scoped `git diff --check` 仅有既有 LF/CRLF 提示；ADB/Expo Go 验证识别页入口、相册选择取消后停留在识别设置页，模式/联网校准可交互，页面下半部分显示运动时机、文字补充、日期和开始按钮。
- 运行时证据截图：`tmp/mobile-record-sheet.png`、`tmp/mobile-analyze-options.png`、`tmp/mobile-analyze-options-toggled.png`、`tmp/mobile-analyze-options-lower.png`。

## 2026-06-19 iOS App 发布流程咨询

- 用户询问当前 Android 可以发 APK，若要发布 Apple iOS App 是否必须上架 App Store、公司需要申请什么、多久、费用，以及 App 内支付是否要给 Apple 分成。
- 结论：普通用户分发 iOS App 默认走 Apple Developer Program 组织账号、App Store Connect、TestFlight 和 App Store 审核；不是 Android APK/COS 侧载模式。
- 公司主体发布需准备组织 Apple 开发者账号、D-U-N-S 编号、公开官网、组织域名邮箱、账号持有人法律授权；Apple Developer Program 官方年费为 99 USD/年或当地货币，中国大陆 Apple Developer App 续订页面显示每年 ¥688。
- 当前 `apps/mobile/app.json` 尚未配置正式 iOS `bundleIdentifier`，`apps/mobile/eas.json` 已有 iOS EAS build 脚本入口但 distribution 仍偏 internal；若落地 iOS 发布，需要补 App Store Connect 应用记录、签名、隐私、截图、TestFlight、IAP 商品和服务端 Apple 交易校验。
- 支付判断：会员、积分、AI 识别次数、数字高级功能等 App 内消费通常需要 Apple IAP；中国大陆 App Store 自 2026-03-15 起标准佣金率为 25%，Small Business Program 等符合条件费率为 12%。实物商品或 App 外消费的线下服务可走非 IAP 支付。
### iOS 中国大陆区 ICP 备案补充
- 若 iOS App 选择中国大陆 App Store 供给范围，App Store Connect 需要填写 ICP 备案号；App 名称、简体中文元数据与工信部备案信息需要一致。此事项应作为 Apple 开发者账号之后、首次提交审核之前的前置清单。
### 完成补充：main/dev 已同步

- 本次本地功能提交为 `d8421124 feat: improve specialty recognition resilience`。
- 推送前远端 `origin/main` 新增到 `dbe18685`，已合并远端更新并解决 `memory/2026-06-18.md` / `memory/2026-06-19.md` 冲突。
- 最终同步提交为 `f33e7ea9 chore: merge remote main updates`。
- 已推送同一个 HEAD 到 `origin/main` 和 `origin/dev`，保持开发分支和主分支一致。

## 用户"小渝儿～"0 热量识别记录排查

- 用户 ID：`e479caa5-e781-4f7b-b0ba-65f7d11fb54f`。
- 异常任务：`90086254-92d5-47f4-98bd-95b72b98cb1b`，`food/done`，创建于 `2026-06-19 01:34:19+08`。
- 任务结果为 `items=[]`、`description=无法获取描述`，对应保存记录 `e999ce68-1da9-4fde-b736-0a6295399fd5` 落为 `total_calories=0`。
- 用同一组三张 CDN 图片调用本机同步 `/api/analyze`：不带用户身份返回 4 个 item，总热量约 `776.15 kcal`；带该用户调试 JWT 时客户端 90 秒超时，但服务端日志显示最终也返回 4 个 item、总热量非 0。
- 判断：原始问题不是当前稳定可复现的图片/数据问题，更像是当时 Gemini 返回空 `items` 后，后端把空识别结果当成功保存。后续应在任务完成和保存饮食记录前拦截空 items。

## 比例生成失败默认 100%

- 用户要求建议比例和可食用比例生成失败时不要直接错误，默认给出 `100%`。
- 已修改 `backend/internal/analyze/service/analyze_service.go`：
  - DeepSeek 可食比例生成失败时，每个 item 默认 `ediblePortionRatio=100`，状态 `edible_portion_status=fallback`、`edible_portion_fallback_reason=generation_failed`。
  - DeepSeek 未配置时也按 100% 可食比例兜底，不再沿用旧推导比例。
  - Gemini 建议比例生成失败时，每个 item 默认 `suggestedRatio=100`，状态 `suggest_ratio_status=fallback`、`suggest_ratio_fallback_reason=generation_failed`。
- 已补单测覆盖失败兜底：`TestAnalyzeService_ApplyEdiblePortionRatiosFailureDefaultsTo100`、`TestAnalyzeService_ApplySuggestedRatiosFailureDefaultsTo100`。
- 验证：定向 `go test ./internal/analyze/service -run ... -count=1` 通过，`git diff --check` 通过。完整 `go test ./internal/analyze/service -count=1` 仍有既有失败，非本次修改引入。

## 2026-06-19 - 识别中页面 UI 布局优化

- 用户要求优化小程序识别中页面：合并「整理任务/任务已提交」与「已等待时间/流程」为单行，并将识别模式标签移到「任务已提交」右侧。
- 已修改 `apps/wechat/src/packageExtra/pages/analyze-loading/index.tsx`：
  - `steps-panel` 改为单行布局，左侧显示「任务已提交/纠错任务已提交」及内联模式标签，右侧显示「已等待 x 秒/分」。
  - 移除原阶段标题与「识别食物/份量 → 匹配营养库 → 整理结果」流程文字。
  - 移除原来位于等待交互卡片下方的独立 `mode-badge`。
  - 清理不再使用的 `currentStep` 状态、阶段标签常量与步骤推进逻辑。
- 已修改 `apps/wechat/src/packageExtra/pages/analyze-loading/index.scss`：
  - 新增 `.stage-summary-left`、`.mode-badge.inline` 样式。
  - 移除已弃用的 `.compact-stage-row` 相关样式。
- 验证：`eslint` 针对本文件通过；`tsc` 无本文件类型错误；`git diff --check` 通过。
- 运行时验证阻塞：当前无 `dev:weapp` 编译进程，端口 9420 未连接；按项目规则未擅自启动本地 watch 进程，需用户启动 `npm run dev:weapp` 后查看效果。

## 2026-06-19 - 首页微量营养卡片增加进度条

- 用户询问首页「微量营养」为什么只有 8 项，并希望在点击更多展开的微量元素单元格里展示和宏量营养素一样的进度条。
- 结论：首页微量营养固定展示 8 项（膳食纤维、钠、钾、钙、铁、维A、维C、维D），且只过滤掉数值为 0 的项。
- 已确认 `backend/internal/health/service/stats_service.go` 中已有这 8 项的每日参考摄入量，但此前未在首页 dashboard 返回。
- 已修改 `backend/internal/home/service/dashboard_service.go`：
  - 新增 `homeMicronutrientReferences` 定义 8 项微量元素每日参考摄入量。
  - `buildHomeMicronutrientPayload` 现在返回 `{ current, target, progress }` 结构。
- 已修改 `apps/wechat/src/utils/api.ts`：
  - 新增 `HomeMicronutrientItem` 类型，更新 `HomeIntakeData.micros` 类型。
- 已修改 `apps/wechat/src/pages/index/components/MicrosSection.tsx`：
  - 解析新结构并兼容旧缓存（数值/Object 两种形态）。
  - 每个微量营养卡片新增 `current/target` 文案与底部线性进度条。
- 已修改 `apps/wechat/src/pages/index/index.scss`：
  - 新增 `.micros-preview-card-target`、`.micros-preview-progress-bg`、`.micros-preview-progress-fill`、`.micros-skeleton--progress` 样式。
- 已更新 `backend/internal/home/service/dashboard_service_test.go` 中断言新结构。
- 验证：`go build ./internal/home/service/` 通过；`npm --workspace apps/wechat run typecheck` 无本次新增文件报错（既有分包类型错误与本次无关）。

## 2026-06-19 - 圈子图片返回 HTTP 而非 HTTPS

- 用户反馈圈子里的图片链接以 `http://` 开头而非 `https://`。
- 根因：后端 `backend/config.yaml` 中 `storage.food_images_cdn_base_url` 配置为 `http://cdn-food-images.coachlink.fit`。
- 圈子里 `user_circle_posts.image_paths` 存的是相对 key（如 `circle-posts/<user>/<uuid>.jpg`），查询时由 `CommunityService.resolveFoodImageURLs` -> `storage.ResolveReferenceURLs("food-images", ...)` 拼成完整 URL，因此直接使用了 HTTP 的 CDN base URL。
- 已修改：
  - `backend/config.yaml`：food-images / user-avatars / health-reports / icon 的 CDN base URL 全部改为 `https://`。
  - `PROJECT_STATE.md`：同步更新 CDN 配置说明。
- 后续：需要重启后端服务；若生产环境使用 Apollo 或环境变量覆盖，还需同步修改 `CDN_FOOD_IMAGES_BASE_URL`。历史数据库里已存为完整 HTTP URL 的字段不会自动变，需要时可清洗。

## 2026-06-19 - 「我的」页公共食物库入口图标改为 icon-foodshop

- 用户要求把「我的」页里的「公共食物库」入口图标改为 iconfont 的 `icon-foodshop`。
- 已修改 `apps/wechat/src/pages/profile/index.tsx`：对应 `iconClass` 从 `icon-shuju` 改为 `icon-foodshop`。
- 验证：`npx eslint apps/wechat/src/pages/profile/index.tsx --max-warnings 0` 通过。
- 运行时验证待启动 `npm run dev:weapp` 后确认。

## 2026-06-19 - 鹅腿/鸭腿/鸡腿专线首页轮播图与详情页添加背景图

- 用户要求给首页“鹅腿还是鸭腿？”轮播图，以及点进去后的鹅腿专线页顶部图片、图片卡片都加上背景图 `https://cdn-food-images.coachlink.fit/ecf8e073-83ca-41b4-bb79-659b17e94c85.png`。
- 已修改：
  - `apps/wechat/src/utils/static-asset-cdn-url.ts`：新增 `GOOSE_DUCK_CHICKEN_BG_URL` 常量。
  - `apps/wechat/src/pages/index/index.tsx`：轮播 banner 数据增加 `bgImage` 字段，鹅腿 banner 配置新背景图，校园 banner 也统一用 `bgImage`，渲染逻辑改为通用 `{banner.bgImage && <Image ... />}`。
  - `apps/wechat/src/packageExtra/pages/goose-duck-chicken/index.tsx`：顶部 hero 与上传占位卡片内各增加背景图 Image。
  - `apps/wechat/src/packageExtra/pages/goose-duck-chicken/index.scss`：新增 hero 背景图、上传卡片背景图与遮罩的层级样式。
- 验证：`npx eslint` 通过；`typecheck` 中本次引入的类型错误已消除，剩余为既有错误。
- 运行时验证待启动 `npm run dev:weapp` 后查看。

## 2026-06-19 - 鹅腿/鸭腿/鸡腿专线背景图透明度调整

- 用户反馈背景图应保留原有橙色背景，并增加透明度。
- 已调整：
  - 首页轮播图 `.home-reward-hint--goose-duck-chicken .home-reward-hint__bg` 设置 `opacity: 0.35`，文字/按钮区域 `z-index: 2`。
  - 鹅腿专线页 hero 背景图 `.goose-hero-bg` 设置 `opacity: 0.35`，保留原有橙色渐变。
- 验证：`npx eslint` 通过；`typecheck` 中本次改动相关错误已消除。

## 2026-06-19 - 鹅腿/鸭腿/鸡腿专线背景图透明度调至 0.65

- 用户认为背景图透明度太低（图片太淡），要求调到 0.65 左右。
- 已调整：
  - 首页轮播图 `.home-reward-hint--goose-duck-chicken .home-reward-hint__bg`：`opacity: 0.65`。
  - 鹅腿专线页 hero `.goose-hero-bg`：`opacity: 0.65`。
- 上传占位卡片的背景图仍保持较淡（opacity 0.18 + 遮罩），如需同步调亮可再调整。

## 2026-06-19 - 鹅腿/鸭腿/鸡腿专线背景图透明度调至 0.2

- 用户要求首页轮播图和活动页顶部背景图透明度都调到 0.2。
- 已调整：
  - 首页轮播图 `.home-reward-hint--goose-duck-chicken .home-reward-hint__bg`：`opacity: 0.2`。
  - 鹅腿专线页 hero `.goose-hero-bg`：`opacity: 0.2`。

## 2026-06-19 - 修复饮食记录保存后首页饮水进度未同步

- 用户提到「这个之前提交过好多次类似修复」，指向新增饮食记录后首页饮水进度未立即刷新的问题。
- 已定位：后端 `food_record_service.go` 保存记录时已把食物含水量写入 `body_water_logs`；删除路径在 `apps/wechat/src/pages/index/index.tsx` 做了乐观扣减；但新增路径的 `result/index.tsx`、`result-text/index.tsx`、`record-manual/index.tsx` 没有触发 force dashboard 刷新。
- 已修复：在上述三个页面保存成功后，先等待 `refreshHomeDashboardLocalSnapshotFromCloud` 完成，再触发 `HOME_INTAKE_DATA_CHANGED_EVENT { date, force: true }`，使首页 `loadDashboard` 重新拉取云端身体指标（含水）。
- `record-manual/index.tsx` 同时补上了事件 `date` 参数，并兼容 `saveResult.already_saved` 的 toast 提示。
- 验证：ESLint 通过；`npm --workspace apps/wechat run typecheck` 未引入新错误；运行时验证待启动 dev:weapp 后确认。

## 2026-06-19 - 饮食记录保存后首页饮水进度未同步（二次修复）

- 用户反馈：从收藏记录咖啡、从实物上传图片记录后，首页「今日餐食」卡片显示含水 200ml / 175ml，但饮水进度仍为 0。
- 根因定位：
  - dashboard 接口直接按食物项计算水，所以餐食卡片能显示。
  - 饮水进度依赖 `/api/body-metrics/summary` 的 `water_daily`（来自 `user_water_logs`）。
  - 之前只有 `analyze-history` 保存后触发 force dashboard 刷新；`result` / `result-text` / `record-manual` 未触发。
  - 缺少前端乐观更新，返回首页前有一致性空窗。
  - `record-manual` 的 `NUTRIENT_SCALE_KEYS` 未包含 `waterMl`/`water_ml`，手动记录会丢掉水。
- 已修复：
  - `home-dashboard-local-cache.ts` 新增 `calculateFoodRecordItemsWaterMl` 与 `addWaterToBodyMetricsStorage`。
  - `index.tsx` 的 `markHomeStale` 从 storage 重新加载 `bodyMetrics`，使乐观更新立即生效。
  - `result/index.tsx`、`recipes/index.tsx`、`result-text/index.tsx`、`record-manual/index.tsx` 保存成功后乐观写入食物含水量。
  - `record-manual/index.tsx` 修复 `NUTRIENT_SCALE_KEYS` 保留水字段。
  - 后端 `foodrecord` / `recipe` / `health repo` 增加饮水日志写入的结构化日志，便于确认 `user_water_logs` 落库情况。
  - `index.tsx` `loadDashboard` 增加 `bodyMetricsRes` 调试打印。
- 验证：ESLint 通过；`npm --workspace apps/wechat run typecheck` 无新错误；`go build ./...` 通过；`gofmt` 通过。
- 运行时验证待用户启动 `npm run dev:weapp` 并确认后端已重启带日志的新版本后测试。

## 2026-06-19 - 饮水未同步根因：时区转换导致 recorded_on 错一天

- 用户要求查数据库判断是数据库写入错误还是后端错误。
- 已查询用户「锦恢」（id=8826bc8d-81ad-40a4-bc42-6cc30506b8c3）最近 3 天数据：
  - 食物记录已正确保存，items 里含水（如冰拿铁 `water_ml: 200`）。
  - 饮水日志已写入 `user_water_logs`，但 `recorded_on` 比预期早一天。
  - 例：食物记录 `record_time=2026-06-19 19:45:13`，对应饮水日志 `created_at=2026-06-19 19:45:13`，但 `recorded_on=2026-06-18`。
- 根因：后端用 `time.Date(..., chinaTZ)` 构造日期，GORM 写入 Postgres `date` 列时按 UTC 会话时区转换，导致日期提前一天。
- 已修复：
  - `food_record_service.go` 的 `foodRecordWaterDate` 改为返回 UTC 午夜。
  - `recipe_service.go` 的 `recordFoodWaterIntake` 中 `recordedDate` 同样改为 UTC 午夜。
- 历史错日期数据可用幂等 SQL 根据 `user_food_records.record_time` 重新校正，待用户确认是否执行。
- 前端乐观更新改动仍保留，确保返回首页立即看到饮水变化。

## 2026-06-19 - 修复目标设置弹窗无法上下滑动

- 用户反馈手机端目标设置弹窗无法上下滑动，电脑模拟器可用滚轮滚动但无法拖动。
- 根因：`TargetEditor.tsx` 外层 `.target-modal` 使用 `catchMove` 拦截了内容区的 `touchmove`；可滚动区是普通 `View` + `overflow-y: auto`，在微信小程序中触摸滑动不可靠。
- 已修复：
  - `apps/wechat/src/pages/index/components/TargetEditor.tsx`：引入 `ScrollView`；把 `catchMove` 从外层移到遮罩；将表单区改为 `ScrollView`（`scrollY` + `enhanced` + `showScrollbar={false}`）；标题和保存按钮固定，中间区域独立滚动。
  - `apps/wechat/src/pages/index/index.scss`：`.target-modal-content` 改为 `overflow: hidden` 避免与 `ScrollView` 冲突；`.target-modal-scroll` 改为 `overflow: hidden` 并加 `padding-bottom: 12rpx`。
- 验证：`npx eslint apps/wechat/src/pages/index/components/TargetEditor.tsx` 通过；`npm --workspace apps/wechat run typecheck` 未引入新错误。
- 运行时验证：微信开发者工具未启动（端口 9420 未连接），待用户启动 `npm run dev:weapp` 并启用自动化后测试。

## 2026-06-19 - 个人主页动态/收藏与圈子食物卡片同步，新增收藏详情页

- 用户要求把圈子板块的食物库/手动来源卡片样式同步到个人主页动态；若记录来自用户自己的收藏（食谱），也做成卡片并标注「收藏」；点击跳转对应收藏详情页；个人主页收藏 tab 也进入同一详情页。
- 已完成后端 + 前端实现：
  - 后端：`user_food_records` 新增 `recipe_id` 列；`food-record/save` 与 recipe `Use` 都保存该字段；feed SQL 返回 `entry_type` / `recipe_id`。
  - 前端：`FoodRecord` / `SaveFoodRecordRequest` 增加 `entry_type` / `recipe_id`。
  - 新建共享组件 `ManualFoodCards`，圈子与个人主页动态共用。
  - 个人主页动态：有 `recipe_id` 显示「收藏」卡片；手动来源显示食物库卡片；点击分别进入 `recipe-detail` / `food-library-detail`。
  - 新建 `packageExtra/pages/recipe-detail`：展示食谱详情、营养、食材，支持一键记录/编辑/删除。
  - 个人主页收藏 tab 与「我的收藏」列表页点击均进入 `recipe-detail`。
  - `app.config.ts` 注册新页面。
- 验证：`go build ./...` 通过；`go test ./internal/foodrecord/handler ./internal/recipe/service ./internal/community/service -count=1` 通过；`npx eslint` 通过；`npm --workspace apps/wechat run typecheck` 无新错误；数据库迁移已执行成功。
- 运行时验证待启动 `npm run dev:weapp` 与微信开发者工具后确认。

## 2026-06-19 - 目标设置弹窗滚动完全失效二次修复

- 用户反馈：上次修复后，目标设置弹窗无论是鼠标滚轮还是点击拖拽都无法向下滚动。
- 根因：`ScrollView` 放在只有 `max-height` 的 flex 容器内，`flex: 1` 拿不到确定高度，导致滚动完全失效。
- 已修复：
  - `apps/wechat/src/pages/index/components/TargetEditor.tsx`：移除 `ScrollView`，改用普通 `View`。
  - `apps/wechat/src/pages/index/index.scss`：`.target-modal-content` 改回 `overflow-y: auto`，保留 `max-height`；`.target-modal-scroll` 去掉 flex/overflow 限制。
  - `catchMove` 继续只在遮罩上，内容区不拦截 touchmove。
- 验证：`npx eslint` 通过；`npm --workspace apps/wechat run typecheck` 未引入新错误；运行时验证待用户在微信开发者工具确认。

## 2026-06-19 - Benchmark 评测名称匹配方案与标注保存 500 修复

- 用户要求：
  1. 给出后台管理「数据集评测」模块中食物名称匹配的鲁棒性方案，使「德芙巧克力」与「德芙牛奶巧克力」这类品牌+品类一致但口味/规格扩展的名称被视为匹配。
  2. 修复 Admin 后台编辑 benchmark 样本标注后保存报 500 的问题。
- 名称匹配方案（未改代码）：
  - 引入 `NameIdentity` 产品身份向量（品牌、品类、口味/规格、净含量）。
  - 用分层打分替代现有字符串 Jaccard：品牌命中 +0.35，品类命中 +0.25，口味一致 +0.15，规格一致 +0.1，字符相似度仅作为 0.4 权重的基础分。
  - 包装食品可用生产环境的 `packaged_food_library`（product_key / product_family_key / aliases）做兜底验证，让评测指标与真实营养库解析逻辑对齐。
  - 分层阈值：普通饭菜 0.8，包装食品 0.75，强品牌锚定可降至 0.7。
- 500 错误修复：
  - 根因：`FoodWeightLabeledSampleDO.Items` 为 `map[string]float64` + `serializer:json` + PostgreSQL `jsonb`，pgx 无法编码具体 value 类型的 map（OID 0）。
  - 修改：
    - `backend/internal/migration/do/schema_do.go`：`Items` 改为 `map[string]any`。
    - `backend/internal/admin/domain/benchmark.go`：Input 的 `Items` 改为 `map[string]any`；DTO 的 `Items` 保持 `map[string]float64`，新增 `sampleItemsToFloat64Map` 从 any 转回 float64。
    - `backend/internal/admin/service/benchmark_service.go`：`normalizeSampleItems` 与 `UpdateSample` 使用 `map[string]any`。
    - `backend/internal/admin/service/benchmark_service_test.go`：测试用例中的 `Items` 字面量改为 `map[string]any`。
    - `backend/cmd/import-food-weight-dataset/main.go`：导入时 `itemsMap` 改为 `map[string]any`。
  - 验证：`go build ./...` 通过；`go test ./internal/admin/...` 因测试使用 `go-sqlite3` 而当前环境 `CGO_ENABLED=0` 且缺少 gcc 无法运行，非本次改动引入。
  - 数据库无需迁移：列类型仍是 `jsonb`，仅 Go 结构体类型变化。
- 任务一方案是否编码由用户后续决定。

## 2026-06-19 - Benchmark 运行结果保存 500 与前端路由拆分
n- 用户反馈 Benchmark 运行一段时间后报新 500：`unable to encode map[string]interface{} ... into text format for unknown type (OID 0)`。
- 根因：`BenchmarkRunDO.Metrics` 等 jsonb 字段通过 `Updates(map[string]any)` 更新时，GORM 未能触发 `serializer:json`，pgx 无法编码裸 `map[string]any`。
- 已修复 `backend/internal/admin/service/benchmark_service.go`：
  - `executeRun` 中 `metrics` 保存前用 `datatypes.JSONMap(metrics.ToMap())`。
  - `executeSample` 中 `prediction`、`stage_outputs`、`metrics` 均用 `datatypes.JSONMap(...)` 包装。
- 用户还要求为「评测记录」和「每个具体评测」设计独立前端路由与跳转。
- 已修改 admin 前端：
  - `admin/src/App.tsx`：把 `/benchmark` 改为 `/benchmark/*` 嵌套路由，注册 `/benchmark/datasets`、`/benchmark/runs`、`/benchmark/runs/:runId`。
  - `admin/src/pages/benchmark-page.tsx`：`BenchmarkPage` 改为布局容器（`<Outlet />`）；导出 `BenchmarkDatasetsSection`、`BenchmarkRunsSection`、`BenchmarkRunDetailSection`；各 section 内部用 `useNavigate` 跳转，`RunDetailSection` 用 `useParams` 读取 `runId`。
  - 跳转关系：数据集页 → 评测记录列表 → 具体评测详情；详情页返回评测记录列表；发起评测后自动跳转到 `/benchmark/runs`。
- 验证：
  - `go build ./...` 通过。
  - `admin` 目录 `npx tsc -b` 通过。
  - 后端测试因环境 `CGO_ENABLED=0` + 缺少 gcc 无法运行 go-sqlite3 测试，与本次改动无关。
- 注意：数据库列类型仍为 `jsonb`，无需 migration。

## 2026-06-19 - 个人主页手动/收藏卡片样式修复与深色主题适配

- 用户反馈个人主页动态里的手动来源食物卡片没有样式，只是图片和文字堆砌。
- 根因：`ManualFoodCards` 的样式写在 `pages/community/index.scss` 里，个人主页在 `packageExtra` 子包未引用，导致类名无样式。
- 已修复：
  - 新建 `apps/wechat/src/pages/community/components/ManualFoodCards.scss`，把手动卡片样式抽到组件级。
  - `ManualFoodCards.tsx` 导入自身 SCSS；`profile-settings/index.scss` 也导入该 SCSS。
  - 从 `pages/community/index.scss` 移除重复的基础样式块，保留相邻选择器。
- 用户还要求首页目标设置弹窗标题/内容、圈子动态卡片、个人主页动态卡片支持深色主题。
- 已修复：
  - `apps/wechat/src/pages/index/index.scss`：在 `.home-page--dark` 下补充 `.target-modal-content`、标题、表单、输入框、按钮、校准提示、体重/喝水快捷选择等深色样式。
  - `apps/wechat/src/styles/fl-color-scheme-dark.scss`：补充圈子动态的手动来源食物卡片、图片占位、展开/收起、校园标签、运动宏营养等深色样式。
  - `apps/wechat/src/packageExtra/pages/profile-settings/index.scss`：在 `.profile-settings-page--dark` 下补充动态里的收藏食谱卡片、手动来源食物卡片、图片占位背景的深色样式。
- 验证：`npx eslint` 针对改动文件通过；`npm --workspace apps/wechat run typecheck` 未引入新错误；`mrc where --port 9420` 仍无法连接，运行时验证待用户启动 `npm run dev:weapp` 与微信开发者工具后确认。

## 2026-06-19 - 首页微量营养与目标设置扩展为 21 项

- 用户要求首页「微量营养」面板和目标设置弹窗默认显示全部 21 项微量营养素（包括零值），与识别结果页「更多营养」列表保持一致。
- 已修改：
  - 后端 `backend/internal/home/service/dashboard_service.go`：`homeMicronutrientMetrics`、`homeMicronutrientReferences`、`homeMicronutrientTargetKeyMap` 从 8 项扩展到 21 项。
  - 后端 `backend/internal/user/service/user_service.go`：`dashboardMicroTargetKeys` 从 8 项扩展到 21 项。
  - 前端 `apps/wechat/src/pages/index/components/MicrosSection.tsx`：扩展为 21 项，移除 `current > 0` 过滤，零值默认显示；loading skeleton 数量跟随配置长度。
  - 前端 `apps/wechat/src/pages/index/components/TargetEditor.tsx`：扩展为 21 项微量元素目标输入。
  - 前端 `apps/wechat/src/pages/index/types/index.ts`：扩展 `TargetFormState` 为 21 项微量目标字段。
  - 前端 `apps/wechat/src/pages/index/utils/helpers.ts`：扩展 `createTargetForm` 从 intake.micros 读取 21 项目标。
  - 前端 `apps/wechat/src/pages/index/index.tsx`：扩展 `buildMicroTargetsFromForm` 字段映射为 21 项。
  - 前端 `apps/wechat/src/utils/api.ts`：扩展 `DashboardTargets` 接口为 21 项微量目标字段。
- 验证：`cd backend && go build ./...` 通过；`go vet ./internal/home/service/... ./internal/user/service/...` 通过；TypeScript 针对改动文件无新增错误；`go test` 因环境 `CGO_ENABLED=0` 无法运行 SQLite 测试，与本次改动无关。
- 运行时验证待用户启动 `npm run dev:weapp` 与微信开发者工具后确认。

## 2026-06-19 - 目标设置默认值、深色标题与保存 500 二次修复

- 用户反馈三个问题：新增微量元素目标在目标设置弹窗中默认值为空；深色模式下「微量元素目标」小标题未变亮；点击保存报 500。
- 已修复：
  - `apps/wechat/src/pages/index/utils/helpers.ts`：新增 `MICRO_TARGET_DEFAULTS`，`createTargetForm` 在后端未返回目标时回退到前端默认参考值，确保 21 项都有初始值。
  - `apps/wechat/src/pages/index/index.scss`：`.home-page--dark` 下覆写 `.target-form-section-title` 为 `#f2f7f4`。
  - `backend/internal/user/service/user_service.go`：`UpdateDashboardTargets` 更新 `health_condition` 时使用 `datatypes.JSONMap(healthCondition)`，解决 pgx 无法编码嵌套 `map[string]interface{}` 导致的 500。
- 验证：`go build ./...`、`go vet ./internal/user/service/...`、ESLint、TypeScript 均无新增错误。
- 后端需重启后修复才会生效；运行时验证待微信开发者工具连接后确认。

## 2026-06-19 - 首页展开更多图标与「我的」列表箭头调整

- 用户要求：首页「营养概览」展开更多右侧使用 `icon-expand` / `icon-collapse`；「我的」页从健康档案到意见反馈的每个列表项右侧箭头去掉。
- 已修复：
  - `apps/wechat/src/pages/index/index.tsx`：在展开 affordance 中增加 `iconfont` 图标，根据展开状态切换 `icon-collapse` / `icon-expand`。
  - `apps/wechat/src/pages/index/index.scss`：新增 `.nutrition-expand-affordance-icon` 样式并同步深色模式颜色。
  - `apps/wechat/src/pages/profile/index.tsx`：移除 `services` 和 `settings` 列表项里的 `<ProfileChevron />` / `.list-arrow`。
- 验证：ESLint、TypeScript 均无新增错误。运行时验证待微信开发者工具连接后确认。

## 2026-06-19 - 比例/重量调节对微量元素的缩放逻辑排查

- 用户要求检查食物识别完成后调节比例/重量是否会同比例调节微量元素，以及所有可修改饮食记录的板块是否一致。
- 排查结果：
  - `result/index.tsx`（图片识别）：重量调节会缩放全部 21 项微量元素；比例调节只改 ratio/intake，最终摄入依赖后端按 ratio 缩放，结果正确但 item.nutrients 未立即反映。
  - `result-text/index.tsx`（文字识别）：重量调节**只缩放热量和三大营养素**，未缩放 `item.nutrients` 中的微量元素；保存 payload 的 nutrients 只包含 calories/protein/carbs/fat/fiber/sugar，**丢失全部微量元素**。这是需要修复的缺口。
  - `record-manual/index.tsx`：已使用包含 21 项的 `NUTRIENT_SCALE_KEYS`，重量变化会同步缩放全部微量元素。
  - `MealRecordEditModal.tsx`（记录详情编辑）：重量调节会缩放全部营养素；比例调节只改 ratio/intake，与 result 页一致。
  - 后端 `food_record_service.go`：`scaleFoodItemNutrients` 已覆盖全部 21 项微量元素；保存/更新存储前端传入的 nutrients，展示时按 ratio 缩放。
- 结论：主要缺口在 `result-text/index.tsx`，需要修复重量调节时的 nutrients 缩放和保存时的 nutrients 完整性。
