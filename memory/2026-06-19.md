# 2026-06-19

## App 首页、图标与图片上传体验修复

- 用户反馈 Android 桌面图标被裁切、App 名称仍是 `Food Link`、首页顶部运营位只有单个不可滑动、首页“今天吃什么”在真机竖排，以及选图提交时报 `Unsupported FormDataPart implementation`。
- 已修复：Android 展示名改为 `食探`；重新生成带安全边距的 launcher/adaptive icon；首页 banner 改为 4 个可横向滑动入口并带分页圆点；记录入口改为稳定 2x2 网格；图片上传从 `fetch(FormData)` 改为 `XMLHttpRequest` multipart。
- 已验证：按用户授权重启本地后端 `3010` 和 Metro `8081`，ADB/Expo Go 打开 `exp://192.168.8.193:8081`，首页轮播和 2x2 记录入口截图通过；选图后 `/api/upload-analyze-image-file` 返回 200，不再出现 FormDataPart 错误。
- 当前上传后的分析提交因测试账号食物分析积分不足返回 402：`食物分析积分不足，本次需要 2 积分，当前可用 0 积分`。这说明 multipart 上传问题已越过，后续如需完整分析成功需给测试账号补足积分或换有积分账号。
- 注意：Expo Go 只能验证 JS 页面和上传链路；Android 桌面图标与 App 名称需要重新构建/安装 APK 后才能在启动器中体现。

## 小程序宠物聊天功能复查

- 用户询问微信小程序宠物是否还有聊天功能，怀疑同事之前写过但后来被覆盖，因为 App 侧有同类功能且当时参考过小程序。
- 复查结论：小程序宠物聊天当前仍在代码和产物中。页面位于 `apps/wechat/src/packageExtra/pages/pet-chat/`，路由在 `apps/wechat/src/app.config.ts` 注册为 `packageExtra/pages/pet-chat/index`，当前 `apps/wechat/dist/app.json` 与 `dist/packageExtra/pages/pet-chat/` 也包含该页面。
- 当前入口：小程序首页悬浮宠物展开态点击进入 `extraPkgUrl('/pages/pet-chat/index')`；宠物主页 `apps/wechat/src/packageExtra/pages/pet-home/index.tsx` 的“去问问它”也进入同一路由。
- 历史证据：`0aec20c feat: add pet chat history and specialty analysis` 同时新增小程序 pet-chat 页面、首页入口、pet-home 入口、路由和 API；该提交已包含在 `origin/main` 与 `origin/dev`。未发现后续提交删除小程序 pet-chat。
- 可能误判原因：`home_pet_companion_hidden_v1` 会让首页悬浮宠物完全隐藏，`home_pet_companion_collapsed_v1` 会让它收起成小气泡；若用户隐藏/收起过，首页会像“宠物聊天入口没了”。当前“我的 -> 清除缓存”只移除 collapsed 与 float position，没有移除 hidden。

## 小程序“我的”页白屏与 7ca325b 回退复查

- 用户反馈小程序点进“我的”页白屏，并提供日志：`ProfilePage` 抛出 `Minified React error #130`，参数为 `undefined`。
- 复查 `7ca325b revert(weapp): 将首页主要代码回退到一天前版本`：该提交只修改 `apps/wechat/src/pages/index/*` 相关文件，删除 `MicrosSection`，回退首页 banner、营养概览展开、首页宠物点击聊天入口等；没有修改 `apps/wechat/src/pages/profile/index.tsx`。`7ca325b^` 与 `7ca325b` 中 profile 页均已存在 `Navigator` 快捷入口，因此它不是“我的”页白屏的直接引入提交。
- `7ca325b` 确实回退/删除了部分新加首页功能：`MicrosSection` 微量营养组件、鹅鸭鸡专线首页 banner、首页宠物点击进入 pet-chat、首页 banner 跳转失败 toast/容错、iconfont CDN canvas 字体加载等。
- 已修复“我的”页一个明确崩溃点：将 `apps/wechat/src/pages/profile/index.tsx` 中登录后快捷入口从 `Navigator` 改为 `View` + 既有 `handleQuickActionClick(extraPkgUrl(...))`，避免当前 Taro/微信运行时渲染 undefined 组件触发 React #130。
- 验证：`npx eslint apps/wechat/src/pages/profile/index.tsx` 通过；`apps/wechat/dist/pages/profile/index.js` 已随 watch 更新且不再包含 `taro.Navigator`；`npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有分包类型错误阻塞，未指向 `profile/index.tsx`。
- weapp-devtools：`cli.bat auto --project K:\code\little-horse\food_link\apps\wechat --auto-port 9420 --trust-project` 成功，`miniprogram-automator` 可连接并切到 `pages/profile/index`；页面 data 中已有用户信息和快捷入口节点。截图仍只显示底部 tabBar，疑似当前 DevTools/Taro 模板刷新会话异常；连续 tab 截图脚本超时，未完成可视层最终确认。本轮未运行 `npm run build:weapp`，也未启动/重启本地前后端。

## 2026-06-19 App 真机布局、验证码倒计时与关于/反馈拆分

- 用户反馈真机上 App 整体字号和卡片偏大、部分文字超宽；同时验证码发送按钮没有倒计时，意见反馈页不应混入关于信息。
- 已修复：App 全局限制 Text/TextInput 最大字体放大倍率为 1.12；移动端 Page/Card/Button/Login/Home/Community/TabBar 做密度收敛；首页统计、banner、记录入口、圈子搜索和动态标题增加行数/自适应约束。
- 已修复：验证码发送成功后优先按后端 `cooldown_seconds` 或 `retry_after_seconds` 倒计时；旧接口未返回时用当前默认节流值 30 秒兜底，倒计时期间按钮禁用。
- 已修复：新增独立 `About` 路由和 `AboutScreen`；“我的”页中“意见反馈”进入纯反馈表单，“关于我们”进入关于页；反馈页不再显示关于卡、隐私设置和协议社群。
- 验证通过：`npm --workspace apps/mobile run typecheck`；scoped `git diff --check` 仅有既有 LF/CRLF 提示。
- ADB/Expo Go 验证通过：登录页密度收敛、首页 2x2 与轮播正常、圈子按钮不溢出、反馈页纯反馈、关于页独立显示且长文本未溢出。截图：`tmp/mobile-layout-after-open.png`、`tmp/mobile-after-login-attempt.png`、`tmp/mobile-community-density.png`、`tmp/mobile-feedback-only.png`、`tmp/mobile-profile-about-visible.png`、`tmp/mobile-about-page.png`。
- 注意：本轮未运行 `npm run build:weapp`；Expo Go 可验证 JS 页面，用户已安装 APK 需重新构建安装后才能看到最新改动。

## 微信登录账号平台配置澄清

- 复核现有后端实现：小程序登录走 `external.appid` / `external.secret`，调用微信小程序 `jscode2session`；App 侧微信登录走 `app_auth.wechat_app_id` / `app_auth.wechat_app_secret`，调用微信开放平台 OAuth `sns/oauth2/access_token`。
- 因此 App 点“微信一键登录”若提示缺少 `app_auth.wechat_app_id / app_auth.wechat_app_secret`，需要配置微信开放平台“移动应用”的 AppID/AppSecret；不能直接拿公众平台小程序 AppID/AppSecret 当作原生 App 登录凭证。
- 现有公众平台小程序账号仍可继续用于小程序；建议在微信开放平台把移动应用和当前小程序绑定到同一个开放平台账号下，用 UnionID 打通跨端账号。

## 微信开放平台移动应用审核素材

- 已为创建微信开放平台移动应用生成审核素材到 `tmp/wechat-open-platform/`：`shitan-icon-watermark-28x28.png`、`shitan-icon-hd-108x108.png`、`shitan-app-operation-flow.png`。
- 28x28 水印图与 108x108 高清图均由 `apps/mobile/assets/icon.png` 等比缩放生成，PNG 且小于 300KB；运行流程图为 1600x1000 PNG，约 185KB，小于 5MB。
- 申请说明建议粘贴：`食探是AI饮食与健康管理App，用户可通过手机号验证码或微信登录，拍照/相册上传食物图片后获得营养分析，保存饮食记录，查看健康趋势，并可在圈子分享互动。当前功能无需固定测试账号，可用手机号验证码注册登录。`
- 微信开放平台“填写应用平台信息”页：当前只配置 Android 应用，选择“开发Android应用=是”，应用签名填 release APK 证书 MD5 `720ab81809518f6b54a2a00c493acc07`，应用包名填 `cn.healthymax.foodlink`，应用已上架按当前事实选“否”，开发方式保留“传统模式开发”。

## App 安装显示名称改为智健食探

- 用户确认希望 APK 安装后的桌面名称从 `食探` 改为 `智健食探`。
- 已修改 `apps/mobile/app.json` 的 `expo.name` 和 `apps/mobile/android/app/src/main/res/values/strings.xml` 的 `app_name`；现有已安装 APK 需要重新构建安装后才会显示新名称。
- 验证：`node -e` 读取 `apps/mobile/app.json` 确认为 `智健食探`；`git diff --check -- apps/mobile/app.json apps/mobile/android/app/src/main/res/values/strings.xml` 通过，仅有既有 LF/CRLF 提示。

## App 记录一餐底部弹层真机布局修复

- 用户提供真机截图：底部 Tab 中间“记录一餐”弹层内四个主入口被排成左侧单列，右侧大面积空白。
- 根因：`apps/mobile/src/components/RecordActionSheet.tsx` 的主入口卡片使用 `width: '48.5%'` 搭配 `gap: 10`，真实窄屏下两列总宽度略微超过容器，导致每个卡片换行。
- 已修复为 `justifyContent: 'space-between'` + `width: '48%'` + `marginBottom: 10` 的稳定两列布局，并给主标题/描述加 `numberOfLines`/缩放约束，避免字体放大撑破卡片。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/components/RecordActionSheet.tsx` 通过，仅有既有 LF/CRLF 提示。`adb devices` 显示 `emulator-5554` 在线，但本轮未主动重启/打开 App 做运行时截图。

## App 底部导航栏安全区空隙修复

- 用户提供真机长屏截图：自定义底部导航栏下方到屏幕最底部露出一段页面背景/灰色区域，视觉上像导航栏没有贴底。
- 当前底部导航由 `apps/mobile/src/navigation/CustomTabBar.tsx` 自定义实现：外层 `wrap` 绝对定位到底部，中间圆形拍照按钮打开 `RecordActionSheet`；原实现把 `paddingBottom: Math.max(insets.bottom, 10)` 加在透明外层 `wrap` 上。
- 根因：底部 safe-area padding 位于透明容器内，白色 `bar` 没有覆盖该区域，长屏/手势导航机型会看到导航栏下方露出页面背景。
- 已修复：将 `bottomInset` 并入白色 `bar` 本体，`bar` 使用 `minHeight: 72 + bottomInset` 和 `paddingBottom: bottomInset`，外层不再加透明 padding，使导航栏白底延伸到屏幕底部。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/navigation/CustomTabBar.tsx` 通过，仅有既有 LF/CRLF 提示。本轮未擅自重启 Metro 或重新安装 APK。

## 2026-06-19 App 食物识别选项与等待态修复

- 用户反馈 App 食物识别页缺少小程序侧已有的识别选项，进入识别后只显示“正在分析”，且旧等待态用 60 次/60 秒倒计时不合理。
- 已修复：`apps/mobile/src/screens/AnalyzeScreen.tsx` 改为先选图再确认提交，补回快速/普通/精准模式、联网校准、分项模式、多视角辅助、AI 摄入比例、餐次、运动时机、文字补充、日期和识别记录等入口；拍照/相册入口不再立刻提交分析。
- 已修复：`AnalyzeLoadingScreen` 移除 `maxAttempts=60` 与固定等待超时，改为显示“已等待 x 秒/分”、任务状态和“先离开 / 识别记录”按钮；后台继续轮询任务，网络波动才短暂重试，后端明确失败则直接提示。
- 已补充：`packages/core/src/types.ts` 的 `AnalyzeTaskSubmitParams` 增加 `suggest_ratio_enabled`，App 提交时与小程序同源传参。
- 验证：`npm --workspace apps/mobile run typecheck`、`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck` 均通过；scoped `git diff --check` 仅有既有 LF/CRLF 提示；ADB/Expo Go 验证识别页入口、相册选择取消后停留在识别设置页，模式/联网校准可交互，页面下半部分显示运动时机、文字补充、日期和开始按钮。
- 运行时证据截图：`tmp/mobile-record-sheet.png`、`tmp/mobile-analyze-options.png`、`tmp/mobile-analyze-options-toggled.png`、`tmp/mobile-analyze-options-lower.png`。

## 2026-06-19 iOS App 发布流程咨询

- 用户询问当前 Android 可以发 APK，若要发布 Apple iOS App 是否必须上架 App Store、公司需要申请什么、多久、费用，以及 App 内支付是否要给 Apple 分成。
- 结论：普通用户分发 iOS App 默认走 Apple Developer Program 组织账号、App Store Connect、TestFlight 和 App Store 审核；不是 Android APK/COS 侧载模式。
- 公司主体发布需准备组织 Apple 开发者账号、D-U-N-S 编号、公开官网、组织域名邮箱、账号持有人法律授权；Apple Developer Program 官方年费为 99 USD/年或当地货币，中国大陆 Apple Developer App 续订页面显示每年 ¥688。
- 当前 `apps/mobile/app.json` 尚未配置正式 iOS `bundleIdentifier`，`apps/mobile/eas.json` 已有 iOS EAS build 脚本入口但 distribution 仍偏 internal；若落地 iOS 发布，需要补 App Store Connect 应用记录、签名、隐私、截图、TestFlight、IAP 商品和服务端 Apple 交易校验。
- 支付判断：会员、积分、AI 识别次数、数字高级功能等 App 内消费通常需要 Apple IAP；中国大陆 App Store 自 2026-03-15 起标准佣金率为 25%，Small Business Program 等符合条件费率为 12%。实物商品或 App 外消费的线下服务可走非 IAP 支付。
### iOS 中国大陆区 ICP 备案补充
- 若 iOS App 选择中国大陆 App Store 供给范围，App Store Connect 需要填写 ICP 备案号；App 名称、简体中文元数据与工信部备案信息需要一致。此事项应作为 Apple 开发者账号之后、首次提交审核之前的前置清单。

## 2026-06-19 小程序首页记录菜单选图后残留修复

- 用户反馈小程序端从“拍照识别/相册上传”选择图片后，页面下方仍停留在记录入口按钮，而不是进入图片预览和识别设置。
- 已修复 `apps/wechat/src/pages/index/components/RecordMenu.tsx`：拍照/相册流程增加 `imagePickInProgress` 状态，点击后立即隐藏首页记录菜单；关闭菜单等待短暂落稳后再调用微信选图，选图成功后写入 `analyzeImagePath(s)` 并跳转分析页。跳转失败时显示“进入识别设置失败，请重试”，避免静默停留在按钮菜单。
- 验证：`npx eslint apps/wechat/src/pages/index/components/RecordMenu.tsx` 通过；`git diff --check -- apps/wechat/src/pages/index/components/RecordMenu.tsx` 通过，仅有既有 LF/CRLF 提示；`npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有分包类型错误阻塞，未指向本次文件。
- weapp-devtools 验证：`cli.bat auto --project K:\code\little-horse\food_link\apps\wechat --auto-port 9420 --trust-project` 成功；用 `miniprogram-automator` mock `chooseImage` 后，点击首页“去记录一餐”->“相册上传”，当前路由进入 `packageExtra/pages/analyze/index?date=2026-06-19`，`.record-menu-grid-v2` 不存在，`.image-grid` 和 `.analyze-guide-quality-zone` 存在。截图接口 `App.captureScreenshot` 仍超时，未取得图片证据；已清理测试写入的 `analyzeImagePath(s)` 并切回首页。

## 2026-06-19 App 小程序视觉一致性第一阶段

- 用户启动长期目标：App MVP 已基本可用，但布局像“大字老年版”，希望直接照小程序页面布局、字号、间距和视觉效果做 App，对平台差异页例外。
- 已完成第一阶段全局基线：`apps/mobile/src/theme/index.ts` 的主色、背景、spacing、radius、shadow 调整到更接近小程序；`Page`、`Card`、`AppButton`、`CustomTabBar`、`RecordActionSheet` 以及首页、分析、圈子、统计、我的页做了字号、卡片、列表行高和弹层密度收敛。
- 已将 `apps/mobile/src/utils/textScaling.ts` 改为 `Text` / `TextInput` 默认不跟随 Android 系统字号放大，`maxFontSizeMultiplier=1`，避免真机系统大字体把迁移页面重新撑大。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；scoped `git diff --check` 通过，仅有既有 LF/CRLF warning；ADB/Expo Go 重新打开 `exp://192.168.8.193:8081` 后，验证分析 Tab、记录一餐弹层、我的页可渲染并交互，弹层保持 2 列，底部导航栏贴底，无明显文字溢出。截图：`tmp/mobile-ui-parity-pass-analyze.png`、`tmp/mobile-ui-parity-pass-record-sheet.png`、`tmp/mobile-ui-parity-pass-after-fixed-font.png`。
- weapp-devtools 9420 可连接并能切换到 `pages/index/index`、`pages/profile/index`，确认页面根节点存在；但 `App.captureScreenshot` 本轮仍超时，未取得小程序参考截图。
- 后续：目标尚未完成到“每一个页面完全一致”。下一步应按注册小程序页面逐页建立 App/小程序截图对照，继续处理 Detail/Migration/Secondary/Tertiary 等长尾迁移页的大字号、大间距和页面结构差异。

## 2026-06-19 App 小程序视觉一致性第二阶段

- 继续围绕 App 向小程序视觉密度靠拢，重点处理第一阶段未覆盖的长尾迁移页面：`DetailScreens`、`MigrationScreens`、`SecondaryMigrationScreens`、`TertiaryMigrationScreens`、`ProfileSettingsScreen`、`CommunitySearchScreen`、`PetChatScreen`、`HealthProfileViewScreen`，并顺手收紧 `StatsScreen`、`ProfileScreen`、`HomeScreen`、`AnalyzeScreen` 与 `RecordActionSheet` 的剩余大字号和固定高度。
- 已压缩大号数字、个人主页头像、私信会话头像、邀请码/会员弹窗、排行榜头像、用户搜索头像、表单 textarea、记录入口卡片、统计页分数、宠物对话头部等样式，使长尾页不再沿用 React Native 默认的大字大间距观感。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；scoped `git diff --check` 通过，仅有既有 LF/CRLF warning。
- ADB/Expo Go 验证：设备 `emulator-5554` 在线，当前运行 `host.exp.exponent`；截图已保存 `tmp/mobile-ui-parity-stage2-current.png`、`tmp/mobile-profile-settings-stage2.png`、`tmp/mobile-record-sheet-stage2.png`、`tmp/mobile-record-sheet-stage2-retry.png`。实际抽查到“我的”“个人主页”“意见反馈”“文字记录”等页面，未见横向溢出，表单/卡片密度较第一阶段收紧。
- weapp-devtools 验证：9420 可连接，可切到 `pages/profile/index` 与 `pages/index/index`，节点读取正常；`miniProgram.screenshot()` / `App.captureScreenshot` 仍会卡住超时，本轮没有取得小程序截图。
- 后续：总目标仍未完成到每一页完全一致；下一步需要继续按小程序页面源码逐页重排 App 页面结构，尤其是个人主页的大封面占位、会员/积分/校园餐/食物库等页面与小程序仍可能存在结构级差异。

## 2026-06-19 App 小程序视觉一致性 Stage 64 - 收藏食谱页

- 继续按小程序 `packageExtra/pages/recipes/index` 对齐 App `RecipesScreen`。
- 已移除 App 收藏食谱页顶部小程序没有的“新建”胶囊入口，保留白色页头、灰底内容区和居中空态。
- 已把卡片内展开式餐次选择改成贴底餐次面板，避免卡片高度被撑开；确认记录仍走 `apiClient.useRecipe(recipe.id, mealType)`。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning。
- weapp-devtools：9420 可连接并 `reLaunch('/packageExtra/pages/recipes/index')`；节点确认 `.recipes-page`、`.page-header` 存在，当前小程序账号收藏为空，`.recipe-card` 为 0；截图接口返回 `fail to capture screenshot`。
- ADB/Expo Go：从“我的”页点击“我的收藏”进入收藏食谱页，截图 `tmp/mobile-recipes-empty-stage64-retry.png`；XML 确认有 `收藏食谱`、`我的收藏`、`还没有收藏餐食`，且 `新建=False`。当前 App 账号也无收藏卡片，本轮未能截图验证“记录”餐次面板。

## 2026-06-19 App 小程序视觉一致性第三阶段

- 继续推进 App 与小程序视觉一致性，第三阶段聚焦个人主页结构级差异。
- `apps/mobile/src/screens/ProfileSettingsScreen.tsx` 已从通用 `Page`/`Card` 外壳改为对齐小程序 `packageExtra/pages/profile-settings` 的结构：深色/封面顶部用户区、头像/ID/统计/座右铭、白色底部抽屉、tab 下划线和动态卡片。
- `apps/mobile/src/screens/ProfileScreen.tsx` 的“个人主页”快捷入口改为直接进入 `ProfileSettings`，避免先进入旧 `PublicProfile` 中转页再点“资料页”。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check` 通过但仍有既有 LF/CRLF warning；ADB/Expo Go 截图 `tmp/mobile-profile-settings-stage3-target-current.png` 与 `tmp/mobile-profile-direct-after-fix.png` 显示个人主页已变为深色顶部 + 白色抽屉结构。
- weapp-devtools 9420 当前可读到小程序 profile-settings 页节点：`.profile-top-section`、`.profile-bottom-drawer`、2 个 tab 和 15 张动态卡；截图接口仍超时，未取得小程序截图。
- 总目标仍未完成到每一页完全一致；后续继续处理会员/积分/食物库/校园餐/公开库等结构级差异页。

## 2026-06-19 App 小程序视觉一致性第四阶段

- 本阶段继续处理结构级差异页，重点对齐积分中心和会员中心。
- `apps/mobile/src/screens/DetailScreens.tsx` 的 `RewardCenterScreen` 已参考小程序 `packageExtra/pages/reward-center` 重排为绿色奖励积分 hero、深色“最快拿分”区、今日进度标题和独立任务卡；移除原 App 侧通用 Card 列表里的进度条/说明文本，使结构更接近小程序。
- `apps/mobile/src/screens/MigrationScreens.tsx` 的 `MembershipCenterScreen` 已参考小程序 `packageExtra/pages/pro-membership` 重排首屏和套餐选择：深绿会员 hero、积分面板、三档卡、三周期卡、已选套餐摘要和支付确认；保留原有 `previewPayment -> Alert -> createPayment` 下单确认链路，避免误触真实支付/订单创建。
- `apps/mobile/src/navigation/RootNavigator.tsx` 中会员页栈标题已从“会员中心”改为小程序一致的“积分充值”。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；scoped `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 通过，仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：`packageExtra/pages/reward-center/index` 可读取 `.reward-center-page`、`.reward-hero`、`.reward-quick-section`、`.reward-section__title`、3 张 `.reward-task-card`；`packageExtra/pages/pro-membership/index` 可读取 `.membership-page`、`.hero-section`、`.hero-credits`、3 张 `.tier-card`、3 个 `.period-tab`、1 张 `.plan-card`。
- ADB/Expo Go 验证：从“我的”页进入“赚积分”后，UI dump 命中“奖励积分 / 当前余额 / 今日已获得 / 最快拿分 / 今日进度 / 去完成”，截图 `tmp/mobile-reward-center-stage4.png`；从“我的”页进入会员页后，UI dump 命中“食探会员 / 选择档位 / 轻度版 / 标准版 / 进阶版 / 选择周期 / 月卡 / 季卡 / 年卡 / 支付确认”，干净截图 `tmp/mobile-membership-stage4-title.png` 显示顶部标题为“积分充值”。
- 总目标仍未完成到每一页完全一致；后续继续处理食物库、校园餐、公开食物详情/分享、自动续费审核等页面。

## 2026-06-19 App 小程序视觉一致性第五阶段

- 本阶段聚焦公共食物库列表页。`apps/mobile/src/screens/MigrationScreens.tsx` 的 `PublicFoodScreen` 已参考小程序 `packageExtra/pages/food-library` 重排为白底四 tab、搜索行、排序/筛选条、筛选浮层、骨架态和 110dp 左右缩略图食物卡片；不再使用旧的通用 `Page title + Card 筛选表单 + Card 列表` 结构。
- `PublicFoodCard` 已改为接近小程序 `food-card` 的结构：左侧图片/标签，右侧标题、描述、商家/校园信息、价格/蛋白标签和右下 kcal，底部作者与赞评藏数据；校园 tab 会隐藏搜索/排序/筛选区，直接展示校园食堂卡片流。
- 保留原有接口、详情跳转、上传入口和数据写入链路；本轮只改列表页视觉结构和筛选交互。筛选区“清除”会同时清空搜索词与减脂筛选。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/food-library/index` 可读取 `.food-library-page`、`.tab-section`、4 个 `.tab-item`、`.filter-section`、`.sort-section`、50 张 `.food-card`、`.food-image-wrap` 和 `.food-footer`。
- ADB/Expo Go 验证：从“我的”进入“公共食物库”，UI dump 命中“全部 / 校园食堂 / 收藏夹 / 我上传的 / 搜索 / 最新 / 最热 / 评分 / 筛选 / kcal”；点击筛选后命中“类型 / 全部 / 适合减脂”；点击“校园食堂”tab 后隐藏搜索筛选并展示校园卡片。截图：`tmp/mobile-public-food-stage5-list.png`、`tmp/mobile-public-food-filter-stage5.png`、`tmp/mobile-public-food-campus-stage5.png`。
- 总目标仍未完成到每一页完全一致；下一步继续处理公共食物详情、公共食物分享/校园食堂分享、校园食堂专区和自动续费审核等页面。

## 2026-06-19 App 小程序视觉一致性第六阶段

- 本阶段聚焦公共食物详情页。`apps/mobile/src/screens/MigrationScreens.tsx` 的 `PublicFoodDetailScreen` 已从旧通用 `Page + Card` 模板改为对齐小程序 `packageExtra/pages/food-library-detail`：首屏大图、负 margin 白色信息卡、热量徽标、四列营养、作者行、校园食堂信息卡、评论卡和底部固定操作条。
- 新增详情页专用骨架态、空态、营养单元格、信息小格、卡片标题和图片列表/发布时间辅助函数；保留原有详情加载、评论、纠错、点赞、收藏、编辑、删除和一键记录链路。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：从小程序 `packageExtra/pages/food-library/index` 点击第一张 `.food-card` 进入 `packageExtra/pages/food-library-detail/index`，可读取 `.food-detail-page`、`.image-section`、`.info-card`、`.nutrients-row`、`.author-row`、`.comments-card`、`.bottom-bar`；小程序 `App.captureScreenshot` 本轮 10 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从 App 公共食物库列表点击第一张食物卡进入详情页，UI dump 命中“食物详情 / 483 kcal / 热量 kcal / 蛋白质 / 碳水 / 脂肪 / 校园食堂 / 评论 / 一键记录”，截图：`tmp/mobile-public-food-detail-stage6.png`。
- 本轮未触发点赞、收藏、评论发布、删除或一键记录，避免污染真实数据。总目标仍未完成到“每一个页面完全一致”；下一步继续处理公共食物分享/校园食堂分享、校园食堂专区、自动续费审核等页面。

## 2026-06-19 App 小程序视觉一致性第七阶段

- 本阶段聚焦公共食物分享/校园食堂分享表单。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `PublicFoodShareScreen` 已从旧通用 `Page title + Card` 表单改为对齐小程序 `food-library-share` / `campus-food-share` 的分段结构：顶部绿色提示卡、餐食来源三选项、图片上传区、营养信息、基础/菜品信息、位置信息、价格信息、标签、补充说明和底部固定提交栏。
- 保留原有图片上传、编辑回填、位置搜索草稿、创建/更新公共食物、校园餐模式切换和提交确认链路；本轮只打开和切换表单，未点击提交，避免写入公共食物库真实数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/food-library-share/index` 可读取 `.share-page`、`.source-section`、`.image-section`、`.nutrition-section`、`.merchant-section`、`.tags-section`、`.submit-bar`；`packageExtra/pages/campus-food-share/index` 可读取 `.campus-share-page`、`.campus-share-hero`、`.campus-share-section`、`.image-upload-area`、`.submit-bar`。两个页面截图接口均 10 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从“圈子 -> 分享食物”进入 App 分享页，UI dump 命中“上传到公共食物库 / 餐食来源 / 外食/堂食 / 食物图片 / 营养信息 / 基础信息 / 发布到公共库”，截图 `tmp/mobile-public-food-share-stage7.png`；点击“校园餐”后命中“分享校园食堂菜品 / 菜品图片 / 菜品信息 / 提交并后台分析”，截图 `tmp/mobile-public-food-share-campus-stage7.png`；下滑后命中“学校 / 食堂 / 价格信息（可选） / 计价方式”，截图 `tmp/mobile-public-food-share-campus-lower-stage7.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理校园食堂专区、自动续费审核和其他尚未结构级对齐的迁移页。

## 2026-06-19 App 小程序视觉一致性第八阶段

- 本阶段聚焦校园食堂专区。`apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `CampusCanteenScreen` 已从旧通用 `Page title + Card` 列表改为对齐小程序 `packageExtra/pages/campus-canteen`：顶部食堂背景图 hero、补充菜品按钮、四个筛选胶囊、搜索行、排序条、横滑推荐卡、低热量/性价比推荐面板、全部校园菜品列表和右下角 FAB。
- 保留原有校园餐数据加载、刷新、排序、筛选、搜索、详情跳转和上传入口；运行时验证只做页面进入和排序切换，未点击“记录”、点赞、评论或提交类动作。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/campus-canteen/index` 可读取 `.campus-canteen-page`、`.campus-hero`、`.campus-header`、4 个 `.filter-chip`、`.search-row`、`.sort-section`、4 张 `.recommend-card`、2 个 `.recommend-panel`、2 张 `.campus-card` 和 `.fab-button`；截图接口 10 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从“圈子 -> 校园餐”进入 App 校园食堂页，UI dump 命中“食探校园食堂计划 / 补充菜品 / 选择大学 / 食堂名称 / 楼层 / 窗口 / 搜索菜名 / 热门 / 高蛋白 / 低热量 / 性价比 / 热门菜品 / 高蛋白推荐 / 全部校园菜品”，截图 `tmp/mobile-campus-canteen-stage8.png`；点击“高蛋白”排序后下划线切换成功，且未因为只切换排序显示“清除筛选”。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理自动续费审核、剩余迁移页和其他尚未结构级对齐的页面。

## 2026-06-19 App 小程序视觉一致性第九阶段

- 本阶段聚焦自动续费审核页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `AutoRenewAuditScreen` 已从旧通用 `Page + Card` 状态页改为对齐小程序 `packageExtra/pages/auto-renew-audit`：浅绿全屏背景、深绿 banner、当前状态面板、服务内容网格、会员权益价格表、签约前说明卡、产品内取消续费路径卡和审核预览弹窗。
- 保留 App 侧真实会员状态与真实套餐数据链路：`getMyMembership()` / `listMembershipPlans()` 继续驱动当前状态、默认选中套餐和价格表；价格表档位固定显示“轻度版 / 标准版 / 进阶版”，价格仍来自后端套餐。签约和取消按钮只打开预览弹窗，不创建订单、不调用支付渠道委托代扣、不执行真实解约。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序分包路径为 `packageExtra/pages/auto-renew-audit/index`，可读取 `.audit-page`、`.audit-banner`、`.service-grid`、`.price-head`、3 个 `.price-row`、`.auto-card`、`.cancel-card`、`.primary-btn`、`.secondary-btn`；点击 `.primary-btn` 后 `.audit-modal` 存在。小程序截图接口 10 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从会员中心进入 App 自动续费审核页，UI dump 命中“食探会员服务 / 食探会员自动续费 / 当前状态 / 服务内容介绍 / 会员权益与价格表 / 自动续费签约前说明”；滚动后命中“确认开通自动续费（审核预览）/ 产品内取消续费路径 / 关闭自动续费”。截图：`tmp/mobile-auto-renew-audit-stage9.png`、`tmp/mobile-auto-renew-audit-bottom-stage9.png`。
- ADB 交互验证：勾选协议后点击确认按钮，只弹出“自动续费签约预览”，截图 `tmp/mobile-auto-renew-audit-modal-stage9.png`；点击“关闭自动续费”只弹出“关闭自动续费路径”，截图 `tmp/mobile-auto-renew-audit-cancel-modal-stage9.png`。两者均为审核预览说明态。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理剩余迁移页和其他尚未结构级对齐的页面。

## 2026-06-19 App 小程序视觉一致性第十阶段

- 本阶段聚焦打卡排行榜页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `CheckinLeaderboardScreen` 已从旧通用 `Page + Card` 列表改为对齐小程序 `packageExtra/pages/checkin-leaderboard`：浅绿顶部 wash、好友本周打卡标题、统计周期、白色排行榜行、左侧排名、圆形头像、昵称/“我”标签和右侧打卡次数。
- 保留原有数据链路与 App 侧行点击能力：排行榜仍来自 `communityGetCheckinLeaderboard()`，下拉刷新仍可重新加载；加载态只显示 spinner，不出现“加载中”文字；错误态用页面内提示与“重试”按钮。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/checkin-leaderboard/index` 可读取 `.checkin-lb-page`、`.checkin-lb-header`、`.checkin-lb-title`、`.checkin-lb-scroll`、`.checkin-lb-list`，当前有 6 个 `.checkin-lb-row`；点击首行后仍停留在排行榜页，说明小程序行本身没有导航行为。截图接口本轮仍超时/不可用，未取得小程序截图。
- ADB/Expo Go 验证：从“圈子 -> 本周打卡排行榜”进入 App 页面，UI dump 命中“好友本周打卡 / 统计周期 / 次打卡”，截图 `tmp/mobile-checkin-leaderboard-stage10-updated.png`；点击排行榜行后进入个人主页，截图 `tmp/mobile-checkin-leaderboard-rowtap-profile-stage10.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理剩余注册小程序页面和 App 中尚未结构级对齐的迁移页。

## 2026-06-19 App 小程序视觉一致性第十一阶段

- 本阶段聚焦好友页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `FriendsScreen` 已从旧通用 `Page + Card` 好友管理页改为对齐小程序 `packageExtra/pages/friends`：浅绿背景、右上刷新胶囊、三段胶囊 Tab、搜索好友行、好友/申请卡片、图标动作按钮和空态卡。
- 保留原有好友数据链路与动作：好友列表、收到申请、我发起的、搜索新好友、发送申请、接受/拒绝、撤销申请、删除好友、私信和进入个人主页仍复用原 API 与导航逻辑；本轮运行时只做进入页面与 Tab 切换，未触发好友关系写入动作。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/DetailScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/friends/index` 可读取 `.friends-page`、`.friends-header`、`.refresh-btn`、`.tabs-wrapper`、`.tabs`、3 个 `.tab-item`、`.search-bar`、`.list-container` 和 5 张 `.card`；点击“收到请求”后仍在好友页，收到请求态有 2 张卡片。
- ADB/Expo Go 验证：从“圈子 -> 好友管理”进入 App 好友页，UI dump 命中“好友 / 刷新 / 好友列表 / 收到请求 / 我发起的 / 搜索好友昵称 / 查找新好友”，截图 `tmp/mobile-friends-stage11.png`；点击“收到请求”后命中“暂无好友请求 / 新的好友申请”，截图 `tmp/mobile-friends-received-stage11.png`。本地图片查看器未能渲染 `tmp/mobile-friends-stage11.png`，但截图文件已生成，运行时判断以 ADB UI dump 为准。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理互动消息、邀请好友、关注列表、隐私设置/协议类或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十二阶段

- 本阶段聚焦互动消息页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `NotificationsScreen` 已从旧通用 `Page + Card` 收件箱改为对齐小程序 `packageExtra/pages/interaction-notifications`：浅灰全屏、白色 header 卡、右侧“全部已读”胶囊、三段白底下划线 Tab、通知卡片、spinner 加载态和图标空态。
- 保留原有通知链路：`listCommunityNotifications()`、`markCommunityNotificationsRead()`、自动/批量已读、Tab 筛选、滚动加载更多、单条通知进入动态详情、头像进入个人主页都继续复用原逻辑；运行时验证未创建互动数据。
- 同步清理：删除 `FriendsScreen` 第十一阶段留下的不可达旧 `Page + Card` 好友模板，避免后续维护时误回退到旧结构。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/DetailScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/interaction-notifications/index` 可读取 `.interaction-notifications-page`、`.notifications-header`、`.mark-read-btn`、`.notifications-tabs`、3 个 `.tab-item`、`.notifications-list` 和 5 张 `.notification-card`；点击“点赞”Tab 后仍在同页且 active tab 正常。小程序 `App.captureScreenshot` 本轮仍超时，未取得小程序截图。
- ADB/Expo Go 验证：从“圈子 -> 互动消息”进入 App 页面，UI dump 命中“互动消息 / 全部已读 / 全部 / 点赞 / 评论 / 暂无互动消息”，截图 `tmp/mobile-notifications-all-stage12.png`；点击“点赞”Tab 后命中“暂无点赞 / 切换到‘全部’查看所有互动”，截图 `tmp/mobile-notifications-like-stage12.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理邀请好友、关注列表、隐私设置/协议类、私信列表/私信会话或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十三阶段

- 本阶段聚焦邀请好友页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `InviteFriendsScreen` 已从旧通用 `Page + Card` 邀请表单改为对齐小程序 `packageExtra/pages/invite-friends`：浅绿背景、邀请 hero、邀请人卡、邀请码 chip、三条规则卡、二维码卡、动作按钮组、手动填写邀请码和平台说明。
- 保留原有邀请链路：进入页读取当前用户和邀请人资料，`fi` / `invite_code` / `inviteCode` deep link 自动解析，`resolveInvite()` / `acceptInvite()`、复制邀请码、复制链接、系统分享、二维码预览和手动解析仍复用原逻辑；运行时验证只触发复制邀请码，不触发加好友或其它关系写入。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/invite-friends/index` 可读取 `.invite-page`、`.invite-hero`、`.invite-card`、`.inviter-card`、`.invite-code-chip`、`.rules-card`、3 个 `.rule-item`、`.qr-card`、`.invite-actions` 和 2 个 `.invite-btn`；截图接口本轮未作为证据使用。
- ADB/Expo Go 验证：通过 `exp://192.168.8.193:8081/--/invite?fi=eff3150a` 进入邀请页，UI dump 命中“邀请有礼 / 邀请码 / 扫码也能加入 / 立即转发邀请 / 复制邀请码 / 复制链接 / 手动填写邀请码”，截图 `tmp/mobile-invite-stage13-fi.png`；点击“复制邀请码”弹出“已复制 / 邀请码已复制，可以发送给朋友。”确认框，截图 `tmp/mobile-invite-copy-dialog-stage13.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理关注列表、隐私设置/协议类、私信列表/私信会话或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十四阶段

- 本阶段聚焦关注列表页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `FollowListScreen` 已从旧通用 `Page + Card`、说明卡和“主页/私信/关注”三按钮操作区，改为对齐小程序 `packageExtra/pages/follow-list` 的浅绿顶部背景、头像、昵称、单个关注胶囊按钮和轻量空态。
- 保留原有数据链路：`getFollowers()` / `getFollowing()`、头像或昵称进入个人主页、关注/取消关注状态切换、下拉刷新和滚动到底加载更多仍复用原逻辑；为了贴近小程序，列表行不再额外暴露“私信”按钮。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/follow-list/index?type=following&user_id=ec6b7cf7-4719-497c-bc58-78e362b16824` 可读取 `.follow-list-page`、`.follow-list-content`、2 个 `.follow-list-item`、`.follow-item-left`、`.follow-item-avatar`、`.follow-item-nickname` 和 `.follow-item-btn`。验证中误点了第一行关注按钮，已立即再次点击同一按钮恢复，恢复后按钮文案均为“已关注”。
- ADB/Expo Go 验证：通过个人主页点击“关注”进入 App 关注列表，UI dump 命中“关注 / 魔法猫咪 / 锦恢 / 已关注”，截图 `tmp/mobile-follow-list-stage14.png`；本轮未点击 App 侧关注/取消关注按钮，避免再次改动关系数据。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理隐私设置/协议类、私信列表/私信会话或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十五阶段

- 本阶段聚焦隐私设置页。`apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `PrivacySettingsScreen` 已从旧通用 `Page title + Card + 说明卡` 改为对齐小程序 `packageExtra/pages/privacy-settings`：浅灰页面背景、白色圆角设置组、两条设置项、右侧 Switch 和 spinner-only 加载态。
- 文案已与小程序保持一致：第一项 brief 为“开启后，其他用户可以通过用户名或手机号搜索到您。”；第二项为“开启后，其他用户在圈子里可以看到您的动态和饮食记录。”。
- 保留原有数据链路：`getPrivacySettings()` / `updatePrivacySettings()`、下拉刷新、单项 optimistic update 与失败回滚逻辑继续复用原实现；运行时验证未点击 App 或小程序 Switch，避免修改真实隐私状态。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/privacy-settings/index` 可读取 `.privacy-settings-page`、`.setting-group`、2 个 `.taroify-cell`、2 个 `.taroify-cell__title`、2 个 `.taroify-cell__brief` 和 2 个 `.taroify-switch`；小程序截图接口 `App.captureScreenshot` / `miniProgram.screenshot()` 仍 12 秒超时，未取得小程序图片证据。
- ADB/Expo Go 验证：从“我的 -> 隐私设置”进入 App 页面，UI dump 命中“隐私设置 / 允许在圈子中被搜索 / 开启后，其他用户可以通过用户名或手机号搜索到您。/ 公开我的饮食记录”，截图 `tmp/mobile-privacy-settings-stage15-settled.png`。初始截图 `tmp/mobile-privacy-settings-stage15.png` 抓到瞬时系统 RefreshControl 蓝条，不作为最终视觉证据。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理协议类、私信列表/私信会话或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十六阶段

- 本阶段聚焦协议类文档页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `AgreementsScreen` / `PrivacyPolicyScreen` 与 `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `MembershipAgreementScreen` 已从旧通用多卡片协议模板改为对齐小程序 `agreement` / `privacy` / `membership-agreement` 的单个白色文档卡结构：浅灰页面、圆角文档容器、居中标题、居中更新时间、虚线分隔线、章节标题和正文段落。
- 保留 App 侧必要平台差异：协议正文继续覆盖 App、小程序、会员、支付、AI 识别、社区、私信等跨端条款，没有为了视觉一致性把正文强行改成只适用于小程序的文案。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中；`rg -n "legalParagraph|function LegalCard"` 对这两个文件无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/agreement/index` 可读取 `.agreement-page`、`.document-content`、`.title`、`.update-time`、10 个 `.section` 和 23 个 `.paragraph`；`packageExtra/pages/privacy/index` 可读取 `.document-page`、`.document-content`、5 个 `.section` 和 9 个 `.paragraph`；`packageExtra/pages/membership-agreement/index` 可读取 `.membership-agreement-page`、`.document-content`、9 个 `.section` 和 27 个 `.paragraph`。补充尝试 `miniProgram.screenshot()` 仍超时，未取得小程序图片证据。
- ADB/Expo Go 验证：从“我的 -> 更多功能”真实入口依次打开“用户协议”“会员协议”“隐私政策”，UI dump 分别命中对应页面标题、文档标题、更新时间和前几节正文；截图为 `tmp/mobile-agreement-stage16.png`、`tmp/mobile-membership-agreement-stage16.png`、`tmp/mobile-privacy-policy-stage16.png`。截图右上角灰色齿轮为 Expo Go 调试浮层，不属于正式 APK UI。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理私信列表/私信会话、账号安全、关于/反馈或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十七阶段

- 本阶段聚焦私信列表和私信会话。`apps/mobile/src/screens/MigrationScreens.tsx` 的 `ConversationsScreen` 已从通用 `Page + Card` 列表改为对齐小程序 `packageExtra/pages/private-conversations`：浅灰页面背景、白色会话卡、头像、昵称、预览、相对时间、未读 badge、spinner-only 加载态和轻量空态。
- `PrivateChatScreen` 已从通用 `Page`、顶部“查看更早消息”按钮和底部输入卡，改为对齐小程序 `packageExtra/pages/private-chat`：全屏浅灰聊天背景、时间分隔、左右头像消息气泡、系统消息气泡、贴底输入条、图片按钮、禁用/激活发送按钮和底部消息操作 sheet。历史消息保留滚动到顶部自动加载，不再占用可见按钮。
- 保留原有链路：会话加载、未读数、进入聊天、轮询、标记已读、发送文字、发送图片、复制消息、删除/举报消息、头像进入个人主页、deep link 进入私信均继续复用原 API；本轮运行时验证未发送消息、未上传图片、未删除或举报消息。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/MigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `private-conversations` 可读取 `.private-conversations-page`、`.conversations-list`、2 张 `.conversation-card`、2 个 `.conversation-avatar` 和 2 个 `.conversation-preview`；点击首张会话进入 `private-chat`，可读取 `.private-chat-page`、`.chat-message-list`、3 条 `.chat-message-row`、3 个 `.chat-avatar`、3 个 `.chat-bubble`、`.chat-input-bar`、`.chat-image-btn` 和 `.chat-send-btn`。补充尝试 `miniProgram.screenshot()` 仍超时，未取得小程序图片证据。
- ADB/Expo Go 验证：从“圈子 -> 私信”真实入口进入 App 会话列表，当前测试账号为空态，UI dump 命中“私信 / 暂无私信 / 有人给你发消息时会出现在这里”，截图 `tmp/mobile-conversations-stage17.png`；通过 `private-chat?user_id=ec6b7cf7-4719-497c-bc58-78e362b16824` deep link 进入聊天页，UI dump 命中“开始聊天吧 / 说点什么... / 发送”，截图 `tmp/mobile-private-chat-stage17.png`。截图右上角灰色齿轮为 Expo Go 调试浮层，不属于正式 APK UI。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理账号安全、关于/反馈、收藏食谱/编辑食谱、AI 助手或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十八阶段

- 本阶段聚焦用户群页。`apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `UserGroupScreen` 已从通用 `Page + Card` 改为对齐小程序 `packageUserGroup/pages/user-group` / `packageExtra/pages/user-group`：浅绿页面背景、食探交流群 hero、白色二维码卡、永久有效标签、二维码大图、打开二维码/复制群名双按钮和加入方式提示卡。
- 保留 App 平台差异：二维码预览继续使用 App 内 Modal；加入方式文案保留截图后在微信识别二维码的 App 可执行路径，而不是照搬小程序长按菜单文案。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序注册路径 `packageUserGroup/pages/user-group/index` 可读取 `.user-group-page`、`.user-group-hero`、`.qr-card`、`.qr-frame`、`.qr-image`、`.action-row`、`.primary-action`、`.secondary-action` 和 `.hint-card`；`miniProgram.screenshot()` 12 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从“我的 -> 加入用户群”真实入口进入 App 页面，UI dump 命中“用户群 / 食探交流群 / 一起把食探做得更好用 / 食探用户群 / 永久有效 / 打开二维码 / 复制群名 / 加入方式”，截图 `tmp/mobile-user-group-stage18.png`；点击“打开二维码”弹出二维码预览 Modal，UI dump 命中“关闭”，截图 `tmp/mobile-user-group-modal-stage18.png`。截图右上角灰色齿轮为 Expo Go 调试浮层，不属于正式 APK UI。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理账号安全、关于/反馈、收藏食谱/编辑食谱、AI 助手或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第十九阶段

- 本阶段聚焦收藏食谱页。`apps/mobile/src/screens/MigrationScreens.tsx` 的 `RecipesScreen` 已从旧通用 `Page + Card + Pill` 列表改为对齐小程序 `packageExtra/pages/recipes`：白色页面 header、我的收藏标题、小程序同款说明文案、纵向食谱卡结构、spinner-only 加载态和收藏空态；当前测试账号无收藏食谱，因此运行时以空态为主要可视证据。
- 保留 App 能力差异：header 右侧保留“新建”胶囊入口；食谱卡在有数据时保留记录这餐、编辑和删除确认链路，其中删除需要二次确认。本轮运行时只点击“新建”验证导航，未保存、记录或删除真实食谱。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/MigrationScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/recipes/index` 可读取 `.recipes-page`、`.page-header`、`.page-title`、`.page-subtitle`、`.recipe-list` 和 `.empty-state`；当前账号 `.recipe-card` 为 0；`miniProgram.screenshot()` 12 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从“我的 -> 我的收藏”真实入口进入 App 收藏食谱页，UI dump 命中“收藏食谱 / 我的收藏 / 这里会显示你收藏过的餐食 / 还没有收藏餐食 / 新建”，截图 `tmp/mobile-recipes-stage19.png`；点击“新建”进入 `RecipeEdit`，UI dump 命中“编辑食谱 / 新建食谱 / 常吃组合可一键写入饮食记录”，截图 `tmp/mobile-recipes-new-stage19.png`。未点击保存。
- 总目标仍未完成到“每一个页面完全一致”；下一步优先处理 `RecipeEditScreen`，它仍保留旧通用表单样式。

## 2026-06-19 App 小程序视觉一致性第二十阶段

- 本阶段聚焦编辑食谱页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `RecipeEditScreen` 已从旧通用 `Page + Card + Field + AppButton` 表单改为对齐小程序 `packageExtra/pages/recipe-edit`：浅绿页面背景、白色圆角表单卡、紧凑输入框、餐次胶囊、营养四栏摘要、轻量食物明细卡和底部保存/删除动作。
- `apps/mobile/src/navigation/RootNavigator.tsx` 已将 `RecipeEdit` 原生 header 改成小程序同款绿色导航栏；新建态标题为“新建食谱”，编辑态标题为“编辑食谱”。
- 保留 App 能力差异：小程序无 id 时提示从识别结果页保存后再编辑，App 仍保留收藏食谱页的手动“新建”能力，并继续保留食物明细手动编辑、图片 URL、标签、保存、更新和删除二次确认链路。本轮运行时只点击餐次胶囊和滚动页面，未点击保存或删除。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/recipe-edit/index?id=visual-reference` 可读取 `.recipe-edit-page`、2 个 `.form-card`、`.input`、`.textarea`、6 个 `.option`、`.summary`、4 个 `.summary-item`、`.action-bar`、`.primary-btn` 和 `.danger-btn`；点击首个餐次 option 成功；`miniProgram.screenshot()` 12 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：当前 App `RecipeEdit` 热更新后 UI dump 命中“新建食谱 / 已收藏 / 基本信息 / 食谱名称 / 描述 / 图片 URL / 标签 / 适合餐次 / 营养摘要 / 食物明细 / 保存”；截图 `tmp/mobile-recipe-edit-stage20-top.png`、`tmp/mobile-recipe-edit-stage20-bottom.png`、`tmp/mobile-recipe-edit-stage20-header-green.png`。点击“早餐”只修改本地餐次状态，未保存真实食谱。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理账号安全、关于/反馈、AI 助手或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第二十一阶段

- 本阶段聚焦意见反馈页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `AboutFeedbackScreen` 已从旧通用 `Page + Card` 单卡表单改为对齐小程序 `packageExtra/pages/feedback`：灰色页面、hero、独立反馈类型卡、内容卡、截图卡、联系方式卡、诊断开关卡和贴底提交栏。
- 保留原有支持链路：`submitFeedback()` 继续附带 `pagePath: app://feedback`、`APP_VERSION`、最近请求 trace、console 日志、截图 URL 和联系方式；图片选择/上传仍走 `uploadFeedbackImageFile()`；运行时验证未提交反馈、未选择相册、未上传图片。
- 同步清理：反馈页不再加载隐藏 about/privacy 分支，也不再为了旧隐私设置请求 `getUserProfile()`；“关于我们”和隐私设置仍由独立页面承接。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/DetailScreens.tsx` 无命中。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/feedback/index` 可读取 `.feedback-page`、`.feedback-hero`、5 个 `.feedback-card`、4 个 `.feedback-category`、`.feedback-textarea`、`.feedback-image-grid`、`.feedback-contact`、`.feedback-diagnostic`、`.feedback-submit-bar` 和 `.feedback-submit`；点击第二个反馈类型卡成功；`miniProgram.screenshot()` 12 秒超时，未取得小程序截图。
- ADB/Expo Go 验证：从“我的 -> 意见反馈”真实入口进入 App 页面，UI dump 命中“意见反馈 / 告诉我们你遇到的问题 / 反馈类型 / 问题反馈 / 功能建议 / 使用体验 / 反馈内容 / 截图（选填） / 联系方式（选填） / 附带请求诊断 / 提交反馈”；点击“功能建议”只修改本地类型状态，截图 `tmp/mobile-feedback-stage21.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理账号安全、AI 助手或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第二十二阶段

- 本阶段聚焦账号安全页。`apps/mobile/src/screens/AccountSecurityScreen.tsx` 是 App 专属能力页，小程序当前没有注册同名页面，因此本轮按小程序设置/表单页视觉密度收敛，而不声称存在小程序逐页母版。
- 已改动：账号安全页从旧通用 `Page + Card + AppButton` 大表单改为灰色页面、手机号与密码 hero、登录方式白卡、紧凑设置手机号密码表单、状态胶囊、说明/注销白卡和贴底保存栏。
- 保留链路：进入页仍通过 `getUserProfile()` 加载手机号、微信登录状态和 `has_password`；保存仍走 `setAccountPassword()`，并保留已设置密码时的当前密码校验。本轮运行时未输入密码、未点击保存。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/AccountSecurityScreen.tsx` 通过，仅有既有 LF/CRLF warning；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/AccountSecurityScreen.tsx` 无命中。
- 小程序对照说明：`rg` 未发现 `apps/wechat/src` 中存在 `account-security` 或“账号安全”注册页；本页属于平台差异页，后续验收以 App 端密度和不溢出为主。
- ADB/Expo Go 验证：从“我的 -> 更多功能 -> 账号安全”真实入口进入页面，UI dump 命中“账号安全 / 手机号与密码 / 登录方式 / 微信登录 / 手机号 / 密码登录 / 设置手机号密码 / 新密码 / 确认新密码 / 至少 8 位 / 保存手机号和密码 / 账号注销”；截图 `tmp/mobile-account-security-stage22.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理 AI 助手或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第二十三阶段

- 本阶段聚焦 AI 助手页。`apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的 `AiAssistantScreen` 是 App 侧统计 AI 助手，本轮以小程序同源 AI 对话体验 `packageExtra/pages/pet-chat` 作为视觉参考，但不替换为 `PetChat` 路由。
- 已改动：AI 助手页从旧通用 `Page + Card + AppButton` 四块大卡改为浅绿页面、顶部近一周/近一月胶囊和刷新胶囊、伙伴头像+气泡舞台、三列指标卡、AI 风险解读卡、关注卡片 chip 和饮食建议卡。
- 保留链路：`getStatsSummary()`、`getPetSummary()`、`generateStatsInsight()` + `saveStatsInsight()`、`generateCustomFocusCard()`、`generateDietRecommendation({ scene: 'mobile_ai_assistant' })` 均保留；运行时验证未点击任何生成按钮，避免触发 AI 生成或写入。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg -n "加载中|上传中|数据加载中|发送中" apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 通过，仅既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/pet-chat/index` 可读取 `.pet-chat-page`、`.pet-chat-topbar`、`.pet-chat-stage`、`.pet-chat-scroll`、`.pet-chat-message`、`.pet-chat-bubble`、`.pet-chat-quick-row`、`.pet-chat-input-bar`、`.pet-chat-send` 和 `.pet-chat-history-button`；点击历史按钮后 `.pet-chat-history-panel` 存在。截图调用仍卡住超时，未取得小程序图片。
- ADB/Expo Go 验证：从“我的 -> 更多功能 -> AI 助手”真实入口进入页面，UI dump 命中“近一周 / 近一月 / 刷新 / 豆豆露露的 AI 助手 / 日均摄入 / TDEE / 连续记录 / AI 风险解读 / 关注卡片 / 饮食建议”；点击“近一月”只做只读范围切换，截图 `tmp/mobile-ai-assistant-stage23.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理伙伴对话 `PetChatScreen` 或其他仍保留通用模板的 App 页面。

## 2026-06-19 App 小程序视觉一致性第二十四阶段

- 本阶段聚焦伙伴对话页。`apps/mobile/src/screens/PetChatScreen.tsx` 已从旧通用 `Page + Card` 聊天页改为对齐小程序 `packageExtra/pages/pet-chat`：浅绿全屏、右上最近/新对话胶囊、伙伴舞台、范围分段、左右聊天气泡、横向快捷问题、贴底输入栏和底部历史抽屉。
- 保留链路：`getPetSummary()`、`getStatsSummary()`、`getLatestPetChatSession()`、`listPetChatSessions()`、`getPetChatSession()`、`generatePetChat()` 均继续复用；运行时验证只打开页面和历史抽屉，未发送问题、未触发 AI 生成、未写入聊天记录。
- 修复运行时发现的密度问题：快捷问题横滑区原本在 React Native 中被撑到大块空白，已通过 `quickScroll` 固定高度与 `quickRow` 居中收敛；历史抽屉改为实白背景，避免底部输入栏和快捷问题透出。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg -n "加载中|上传中|数据加载中|发送中|正在读取" apps/mobile/src/screens/PetChatScreen.tsx` 无命中；`git diff --check -- apps/mobile/src/screens/PetChatScreen.tsx` 通过，仅既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/pet-chat/index` 可读取 `.pet-chat-page`、`.pet-chat-topbar`、`.pet-chat-stage`、`.pet-chat-scroll`、`.pet-chat-messages`、`.pet-chat-message`、`.pet-chat-bubble`、`.pet-chat-quick-row`、`.pet-chat-input-bar`、`.pet-chat-send` 和 `.pet-chat-history-button`；点击历史按钮后 `.pet-chat-history-panel` 存在。截图调用 12 秒超时，未取得小程序图片。
- ADB/Expo Go 验证：从首页悬浮伙伴展开态点击“和伙伴聊天”进入 App 伙伴对话页，UI dump 命中“伙伴对话 / 最近 / 新对话 / 豆豆露露在读你的饮食记录 / 最近 7 天 / 最近 30 天 / 我是豆豆露露 / 问它：训练状态... / 发送”；点击“最近”打开历史抽屉，UI dump 命中“最近对话 / 新对话 / 还没有历史对话”。截图：`tmp/mobile-pet-chat-stage24.png`、`tmp/mobile-pet-chat-history-stage24-fixed.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续扫描剩余仍保留通用模板或未做运行时对照的 App 页面。

## 2026-06-19 App 小程序视觉一致性第二十五阶段

- 本阶段聚焦我的宠物/成长伙伴页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `PetHomeScreen` 已从旧通用 `Page + Card` 改为对齐小程序 `packageExtra/pages/pet-home`：浅绿页面、伙伴 hero、问问宠物卡、为什么是它、候选伙伴、成长进度、今日状态、离线小惊喜和外观换装动作列表。
- 保留链路：`getPetSummary()`、`claimPetOfflineBonus()`、`selectPetCandidate()`、`rerollPetAppearance()`、首页悬浮宠物开关、进入 `PetChat` 与 `PetLab` 都继续复用原逻辑；运行时验证只做页面进入和滚动检查，未领取奖励、未选择候选、未随机换外观、未切换悬浮宠物。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg -n "加载中|上传中|数据加载中|发送中|正在读取" apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 无命中；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 通过，仅既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序 `packageExtra/pages/pet-home/index` 可读取 `.pet-home-page`、`.pet-home-shell`、`.pet-home-hero`、`.pet-home-avatar`、`.pet-home-chat-card`、7 个 `.pet-home-card`、7 个 `.pet-home-card-title`、`.pet-home-progress` 和 `.pet-home-action-list`；截图调用 12 秒超时，未取得小程序图片。
- ADB/Expo Go 验证：从首页悬浮伙伴真实入口点击“打开伙伴主页”进入 App 成长伙伴页，UI dump 命中“成长伙伴 / 豆豆露露 / 问问豆豆露露 / 为什么是它 / 成长进度 / 今日状态 / 离线小惊喜 / 外观换装 / 首页悬浮宠物 / 随机换外观 / 外观实验室”。截图：`tmp/mobile-pet-home-stage25.png`、`tmp/mobile-pet-home-stage25-lower.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续扫描剩余仍保留通用模板或未做运行时对照的 App 页面。

## 2026-06-19 App 小程序视觉一致性第二十六阶段

- 本阶段聚焦外观实验室页。`apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `PetLabScreen` 已从旧通用 `Page + Card` 改为对齐小程序 `packageExtra/pages/pet-lab`：浅绿页面、外观实验室 hero、生成原理、推荐/风险样本横滑、筛选组合、双列外观网格和底部详情弹层。
- 保留链路：伙伴摘要、会员积分、随机换外观、后端候选选择继续复用；运行时验证只打开页面、滚动、点击生成样本详情弹层，未随机换外观、未选择候选、未消耗积分。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped `git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读 pet-lab 关键节点并打开详情弹层，截图接口 12 秒超时。ADB/Expo Go 截图 `tmp/mobile-pet-lab-stage26.png`、`tmp/mobile-pet-lab-stage26-grid.png`、`tmp/mobile-pet-lab-stage26-detail.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续扫描剩余仍保留通用模板或未做运行时对照的 App 页面。

## 2026-06-19 App 小程序视觉一致性第二十七阶段

- 本阶段聚焦关于页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `AboutScreen` 已从旧通用 `Page + Card` 协议/社群混合页改为对齐小程序 `packageAbout/pages/about` / `packageExtra/pages/about`：白色居中 logo header、应用名与版本、关于食探卡、官方邮箱 cell、特别鸣谢卡和底部版权。
- 保留链路：官方邮箱点击继续复制 `jianwen_ma@stu.pku.edu.cn`；用户协议、隐私政策、会员协议和用户群仍通过“我的”页其它入口承接，不再挤在关于页里破坏小程序密度。
- 验证：mobile typecheck 通过；加载中文字扫描无命中；scoped diff check 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读 about 关键节点并点击邮箱 cell，截图接口 12 秒超时。ADB/Expo Go 从“我的 -> 关于我们”真实入口进入，截图 `tmp/mobile-about-stage27.png`，邮箱复制交互截图 `tmp/mobile-about-stage27-copy.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续扫描剩余仍保留通用模板或未做运行时对照的 App 页面。

## 2026-06-19 App 小程序视觉一致性第二十八阶段

- 本阶段聚焦圈子搜索页。`apps/mobile/src/screens/CommunitySearchScreen.tsx` 已从旧通用 `Page + Card + AppButton` 搜索模板改为对齐小程序 `packageExtra/pages/search-results`：白色搜索栏、灰色圆角输入框、绿色搜索动作、搜索记录标签、白底下划线 tab、骨架屏、空态、内容卡、用户卡和底部加载/结束提示。
- 保留链路：搜索历史、本地清除/删除历史、内容/用户 tab、社区搜索 API、自动切换用户结果、内容详情跳转、用户主页跳转、点赞/取消赞和评论入口都继续复用原逻辑；运行时验证只做页面进入和 tab 切换，未主动发起真实搜索或点赞写入。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped diff check 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序搜索页关键节点并点击“用户”tab，截图接口 12 秒超时。ADB/Expo Go 从“圈子 -> 搜索框”真实入口进入，截图 `tmp/mobile-community-search-stage28-empty.png`、`tmp/mobile-community-search-stage28-users.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理仍保留通用模板的记录、识别历史、食物库、健康档案或统计趋势类页面。

## 2026-06-19 App 小程序视觉一致性第二十九阶段

- 本阶段聚焦识别记录页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `AnalyzeHistoryScreen` 已从旧通用 `Page + Card + SmallButton` 历史模板改为对齐小程序 `packageExtra/pages/analyze-history`：浅绿背景、紧凑搜索条、白色任务卡、56dp 缩略图、热量大数字、状态胶囊、餐次/模式标签、右侧更多按钮和“一键删除未记录”胶囊。
- 保留链路：识别历史加载、关键词搜索、下拉刷新、查看图片/文字/包装识别结果、失败任务重试、进入识别进度、批量删除未记录任务都继续复用原有 `apiClient` 与导航；本轮运行时没有点击删除或重试，避免改动真实账号数据。
- 同步修复：文本识别标题展示增加安全 `decodeURIComponent`，历史数据里类似 `rice%20egg%20milk` 的内容会显示为 `rice egg milk`，不影响原始数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped diff check 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序识别记录关键节点，当前账号有 102 张任务卡并可点击首张卡，截图接口 12 秒超时。ADB/Expo Go 从底部“记录一餐 -> 识别记录”真实入口进入，截图 `tmp/mobile-analyze-history-stage29.png`、`tmp/mobile-analyze-history-stage29-decoded.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理仍保留通用模板的食物库、健康档案、趋势/记录细分页或其他长尾页面。

## 2026-06-19 App 小程序视觉一致性第三十阶段

- 本阶段聚焦食物库输入/手动记录页。`apps/mobile/src/screens/DetailScreens.tsx` 的 `ManualRecordScreen` 已从旧通用 `Page + Card + Field + AppButton` 表单改为对齐小程序 `packageExtra/pages/record-manual`：浅绿页面、单餐工作台卡、三列餐次胶囊、紧凑日期行、内嵌搜索条、自定义入口、左侧分类栏、右侧食物列表、选中态和底部保存汇总栏。
- 保留链路：`getManualFoodBrowse()`、`getManualFoodCatalog()`、`searchManualFood()`、`saveManualFoodRecords()`、quickItem 预填、餐次/日期、重量调整、份量预设和食物库自定义入口仍复用原有逻辑；本轮运行时只切换分类和点选一条食物生成本地选中态，未点击保存，未写入真实饮食记录。
- 同步修复：App 手动记录页不再用初始 `RefreshControl refreshing={loading}`，避免 Android/Expo 顶部出现英文 `Refreshing...` 蓝条；加载态只用列表区域 spinner 或按钮内 spinner。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序 record-manual 关键节点并点击第二个餐次，截图接口 12 秒超时。ADB/Expo Go 从底部“记录一餐 -> 食物库输入”真实入口进入，截图 `tmp/mobile-manual-record-stage30-fixed.png`、分类交互截图 `tmp/mobile-manual-record-stage30-campus.png`、本地选中态截图 `tmp/mobile-manual-record-stage30-selected.png`。
- 总目标仍未完成到“每一个页面完全一致”；下一步继续处理食物库、自定义食物、健康档案、趋势/记录细分页或其他仍保留通用模板的页面。

## 2026-06-19 App 小程序视觉一致性第三十一阶段

- 本阶段聚焦 App `FoodLibraryScreen`。`apps/mobile/src/screens/DetailScreens.tsx` 已从旧通用 `Page + Card + Field + AppButton` 表单改为小程序密度：顶部四段 Tab、搜索条、排序条、白色食物卡、图片缩略图、营养 chip、详情/记录动作，以及紧凑自定义食物面板。
- 对照来源：公共食物库列表参考小程序 `packageExtra/pages/food-library` 的 Tab、搜索、排序和卡片结构；自定义食物表单参考小程序 `packageExtra/pages/record-manual` 的 `custom-food-panel`。App 侧仍保留平台差异：食物库本页承载手动记录可用的营养库/自定义食物，不替换成完整社区公共食物库页。
- 保留链路：`getManualFoodBrowse()`、`getCustomFoods()`、`searchManualFood()`、`saveCustomFood()`、进入 `FoodLibraryDetail`、从食物卡跳 `ManualRecord({ quickItem })`、从手动记录“直接自定义”进入食物库新建 Tab 均继续复用原逻辑；运行时未点击“完成自定义”或“记录”，没有写入真实食物或饮食记录。
- 同步改动：`apps/mobile/src/navigation/types.ts` 的 `FoodLibrary` 路由允许 `initialTab`，用于从 `ManualRecordScreen` 直接打开自定义食物面板。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序 `food-library` 节点并点击“校园食堂”Tab，也可打开 `record-manual` 自定义面板；两处截图接口均 12 秒超时。ADB/Expo Go 截图：`tmp/mobile-food-library-create-stage31.png`、`tmp/mobile-food-library-list-stage31.png`、`tmp/mobile-food-library-custom-empty-stage31.png`。
- 下一步：继续处理健康档案、趋势/记录细分页或其他仍保留通用模板的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十二阶段

- 本阶段聚焦 App `HealthProfileScreen`。`apps/mobile/src/screens/DetailScreens.tsx` 已从旧通用 `Page + Card + Field + AppButton` 表单改为对齐小程序 `packageExtra/pages/health-profile`：浅灰页面、顶部进度点、步骤卡、选项卡、数字输入卡、作息/多选/目标字段和贴底上一步/下一步/保存栏。
- 保留链路：`getHealthProfile()`、`updateHealthProfile()`、`getDashboardTargets()`、`updateDashboardTargets()`、生日/年龄换算、枚举中文映射、病史/偏好/过敏多选和健康档案详情入口都继续复用原逻辑；运行时验证只做进入、选项选择和步进，未点击最终保存，未写入真实健康档案。
- 平台差异：小程序问卷包含体检报告上传步骤；App 仍由 `HealthProfileView` 承接体检报告识别和详情字段编辑，本轮在最后补充信息步骤保留进入详情页入口。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/navigation/types.ts` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序健康档案问卷关键节点，并完成性别选项点击与下一步交互；截图接口继续不可用。ADB/Expo Go 从首页“健康档案与目标”真实入口进入，截图 `tmp/mobile-health-profile-stage32-gender.png`；点击“男”后进入 `2 / 11` 年龄页，截图 `tmp/mobile-health-profile-stage32-age.png`。
- 下一步：继续处理健康档案详情、趋势/记录细分页或其他仍保留通用模板的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十三阶段

- 本阶段聚焦 App `HealthProfileViewScreen`。页面已从旧通用 `Page + Card + AppButton` 详情/编辑模板改为对齐小程序 `packageExtra/pages/health-profile-view`：浅灰背景、白色信息块、紧凑行列表、右侧箭头、体检/病例识别结果上传卡、底部字段编辑弹层和“重新填写”动作。
- 保留链路：`getHealthProfile()`、`updateHealthProfile()`、`uploadReportImages()`、`submitReportExtractionTask()`、`getAnalyzeTask()`、体检报告轮询、报告失败重试、枚举中文映射、执行模式会员校验和重新填写导航都继续复用；运行时只打开详情页和性别编辑弹层，未点击“确定”、未上传报告、未保存字段。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载中文字扫描无命中；scoped `git diff --check -- apps/mobile/src/screens/HealthProfileViewScreen.tsx` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序健康档案详情页关键节点：4 个 `.block`、14 个 `.row`、12 个 `.row.editable`、1 个 `.report-upload-trigger`，点击可编辑行后 `.editor-modal` / `.editor-modal-content` / `.editor-header` 出现；截图接口继续 12 秒超时。
- ADB/Expo Go 验证：从健康档案问卷最后一步点击“体检报告与详情在档案详情页继续管理”进入 App 详情页，截图 `tmp/mobile-health-profile-view-stage33.png`；点击“性别”打开底部编辑弹层，截图 `tmp/mobile-health-profile-view-editor-stage33.png`。Expo Go 调试齿轮浮层属于调试容器，不是生产 UI。
- 下一步：继续处理趋势/记录细分页、体重/运动/积分/会员等仍可能保留通用模板或未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十四阶段

- 本阶段聚焦 App `BodyMetricRecordScreen` 的体重、喝水、运动三类记录页，小程序参考页分别为 `packageExtra/pages/weight-record`、`packageExtra/pages/water-record` 和 `packageExtra/pages/exercise-record`。
- 已改动：三类页面从旧通用 `Page + Card` 大模板改为小程序同源密度：体重页使用浅灰背景、顶部日期/趋势、白色大输入卡、今天记录卡和本月概览；喝水页使用蓝色进度卡、快捷加水网格、自定义输入行、今天记录和本月概览；运动页使用统计卡、记录运动卡、横向快捷 chip、图片按钮、贴右发送圆按钮、任务/记录卡和轻量空态。
- 保留链路：`getBodyMetricsSummary()`、体重保存/删除、喝水添加/删除/清空、运动图片上传、运动分析任务提交/轮询/刷新、运动记录删除和趋势跳转均继续复用原逻辑；运行时只进入页面和点运动快捷 chip，未保存体重、未添加/清空喝水、未上传图片、未发送运动记录、未删除真实数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：体重页读取 `.weight-record-page`、`.weight-record-topbar`、`.weight-main-card`、`.weight-save-btn`、`.weight-day-card`；喝水页读取 `.water-record-page`、`.water-record-topbar`、`.water-progress-card`、`.water-action-card`、4 个 `.water-preset`、`.water-day-card`；运动页读取 `.exercise-record-page`、`.header-stats`、`.stats-card`、`.input-section`、`.quick-examples-strip`、`.exercise-send-trigger`、`.records-scroll`，点击 `.example-tag` 后 `.chat-input` 的 value/text 为“跑步30分钟”。小程序截图接口继续 12 秒超时。
- ADB/Expo Go 验证：从首页真实快捷卡进入体重、喝水、运动三页；截图 `tmp/mobile-bodymetric-weight-stage34.png`、`tmp/mobile-bodymetric-water-stage34.png`、`tmp/mobile-bodymetric-exercise-stage34.png`、`tmp/mobile-bodymetric-exercise-preset-stage34.png`。Expo Go 调试齿轮浮层属于调试容器，不是生产 UI。
- 下一步：继续处理趋势详情、奖励积分、会员中心或其他仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十五阶段

- 本阶段聚焦 App `TrendDetailScreen`，小程序参考页为 `packageExtra/pages/weight-trend`、`packageExtra/pages/water-trend` 和 `packageExtra/pages/exercise-trend`。
- 已改动：趋势详情从旧 `Page + Card + 小柱状图` 通用模板改为小程序同源结构：白色 hero、摘要卡片网格、体重折线图、喝水/运动 30 天热力格、历史记录白卡；三类趋势使用各自的绿色/蓝色/橙色页面背景和密度。
- 保留链路：`getBodyMetricsSummary()`、`getExerciseLogs()`、体重删除、喝水单条删除、运动记录删除、下拉刷新和从记录页进入趋势页都继续复用原逻辑；运行时验证只进入页面和点小程序热力格，未删除任何真实记录。
- 同步修复：`SmallButton` 增加 spinner-only `loading` 状态，并把包装食品零食库搜索按钮的“搜索中”文字改为 spinner，避免目标文件继续出现纯加载中文字。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg -n "搜索中|提交中|加载中|上传中|数据加载中|发送中|正在读取|保存中|删除中|清空中|刷新中" apps\\mobile\\src\\screens\\TertiaryMigrationScreens.tsx` 无命中；scoped `git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序体重趋势读取 `.weight-trend-page`、`.trend-hero`、2 个 `.trend-summary-card`、`.weight-line-panel`、`.history-card` 和 1 条 `.weight-history-row`，未点击删除；喝水趋势读取 30 个 `.water-heatmap-cell`、3 条 `.water-history-row` 并点击热力格后 `.water-heatmap-cell.is-selected` 为 1；运动趋势读取 30 个 `.exercise-heatmap-cell` 和 3 个 summary 卡并点击热力格。截图调用仍超时或接口不可用。
- ADB/Expo Go 验证：从首页真实快捷卡进入体重、喝水、运动记录页，再点击“查看趋势”进入三类趋势页；截图 `tmp/mobile-trend-weight-stage35.png`、`tmp/mobile-trend-water-stage35.png`、`tmp/mobile-trend-exercise-stage35.png`。
- 下一步：继续处理奖励积分、会员中心或其他仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十六阶段

- 本阶段聚焦 App `RewardCenterScreen`，小程序参考页为 `packageExtra/pages/reward-center`。
- 已改动：奖励中心继续贴近小程序结构，页面背景和横向内边距按小程序密度收口；任务卡补充进度条，展示每日上限任务的 `today_count / daily_limit`，不限次数任务有提交时显示满格。
- 保留链路：`getRewardCenter()`、最快任务、任务状态、每日上限判断和 `navigateRewardTask()` 跳转继续复用；运行时只做展示与导航验证，未提交包装食品、公共食物或真实分享领取。
- 同步改动：`apps/mobile/src/components/Page.tsx` 增加可选 `style` 与 `contentContainerStyle`，供单页覆盖背景和内容内边距，不改变其它页面默认表现。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`git diff --check -- apps/mobile/src/components/Page.tsx apps/mobile/src/screens/DetailScreens.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序奖励中心读取 `.reward-center-page`、`.reward-hero`、2 个 `.reward-stat`、2 个 `.reward-quick-card`、3 个 `.reward-task-card`、3 个 `.reward-task-card__meta` 和 3 个 `.reward-task-card__button`；点击首个 quick card 后页面栈从 `reward-center` 进入 `day-record`。截图接口继续超时。
- ADB/Expo Go 验证：从首页 banner 横滑到“今日任务 / 赚积分换权益”并点击进入奖励中心，截图 `tmp/mobile-reward-center-stage36.png`、`tmp/mobile-reward-center-stage36-final.png`；点击首个“去完成”后进入单日记录页，截图 `tmp/mobile-reward-nav-stage36.png`，随后已返回奖励中心。
- 下一步：继续处理会员中心或其他仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十七阶段

- 本阶段聚焦 App `MembershipCenterScreen`，小程序参考页为 `packageExtra/pages/pro-membership`。
- 已改动：会员中心从旧通用 `Page + Card` 套餐/支付页改为小程序同序结构：深色会员 hero、三档套餐、三周期卡片、已选价格卡、三档对比表、积分消耗说明、当前状态卡、绿色开通/续费动作区和自动续费/会员协议入口。
- 保留链路：`getMyMembership()`、`listMembershipPlans()`、档位/周期选择、`createMembershipPayment()` 前的确认弹窗、`syncMembershipPayment()`、自动续费审核和会员协议导航继续复用；运行时只做档位/周期切换与展示验证，未点击开通按钮，未创建真实订单。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/components/Page.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序会员页读取 `.membership-page`、`.hero-section`、`.hero-emblem-row`、`.hero-credits`、3 个 `.tier-card`、3 个 `.period-tab`、`.plan-card`、`.features-section`、3 个 `.features-row`、`.credits-hint-card`、`.status-card`、`.action-section`、`.subscribe-btn`；点击档位和周期后 active 状态各为 1。截图接口继续 12 秒超时。
- ADB/Expo Go 验证：从“我的”页真实会员卡进入会员中心，截图 `tmp/mobile-membership-stage37.png`；点击标准版与季卡后价格卡更新为 `标准版 · 季卡` / `¥56.90`，截图 `tmp/mobile-membership-stage37-selected.png`；下滑到底部确认积分消耗、状态卡和 `立即开通 · ¥56.90` 按钮可见，截图 `tmp/mobile-membership-stage37-bottom.png`。Expo Go 调试齿轮浮层属于调试容器，不是生产 UI。
- 下一步：继续扫描其它仍保留通用模板或未做运行时对照的 App 页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十八阶段

- 本阶段聚焦 App `AutoRenewAuditScreen`，小程序参考态为 `packageExtra/pages/pro-membership/index?audit_auto_renew=1` 中的自动续费审核预览。
- 已改动：自动续费审核页补齐小程序同源结构：顶部蓝色审核预览提示、会员服务深色说明、当前会员状态、服务内容、真实套餐价格表、自动续费说明卡、四行签约信息、灰底取消路径、协议勾选、会员协议链接、绿色审核预览按钮，以及蓝色自动续费管理/关闭路径卡。
- 保留链路：`getMyMembership()`、`listMembershipPlans()`、套餐选择、协议勾选、本地签约预览弹窗、关闭路径预览弹窗和会员协议导航继续复用；运行时未创建订单、未调用真实支付或委托代扣、未执行真实解约。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序审核态关键节点并完成 `.auto-renew-check` 勾选，截图接口 12 秒超时。ADB/Expo Go 从会员中心真实入口进入，截图 `tmp/mobile-auto-renew-stage38-top.png`、`tmp/mobile-auto-renew-stage38-check.png`、`tmp/mobile-auto-renew-stage38-sign-modal.png`、`tmp/mobile-auto-renew-stage38-cancel-modal.png`。
- 下一步：继续扫描其它仍保留通用模板或未做运行时对照的 App 页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第三十九阶段

- 本阶段聚焦 App `TextRecordScreen`，小程序参考页为 `packageExtra/pages/record-text`。
- 已改动：文字记录页从旧通用 `Page + Card + Field + AppButton` 表单改为小程序同序结构：浅绿页面、积分配额条、食物大输入卡、常用标签、补充份量输入卡、记录日期、六宫格餐次、饮食目标 chip、运动时机 chip 和贴底提交栏；提交按钮 loading 态只使用 spinner，不显示纯加载中文字。
- 保留链路：`getMyMembership()`、`submitTextTask()`、文本内容、补充上下文、餐次和日期继续复用原有 API；饮食目标与运动时机会被拼入 `additionalContext`，不改变后端接口契约。当前账号积分不足时按钮禁用，运行时验证未点击提交，未创建真实 AI 分析任务，未消耗积分。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可读小程序文字记录页关键节点并完成标签/餐次点击，截图接口 12 秒超时。ADB/Expo Go 从底部“记录一餐 -> 文本输入”真实入口进入，截图 `tmp/mobile-record-text-stage39-interaction.png`；点击“鸡蛋”、午餐、减脂期、日常后输入框和选中态正常，未提交。
- 下一步：继续扫描其它仍保留通用模板或未做运行时对照的 App 页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十阶段

- 本阶段聚焦 App `ResultScreen` / `TextResultScreen`，小程序参考页为 `packageExtra/pages/result`。
- 已改动：两个结果页从旧通用 `Page + Card + AppButton` 改为小程序同源结构：顶部图片/文字 hero、圆角内容抽屉、识别模式行、营养总览卡、宏量营养条、食材卡五格营养条、重量/比例调整、饮食建议和贴底操作栏。
- 保留链路：图片结果页继续走 `buildSaveFoodRecordRequestFromTask()` 与 `saveFoodRecord()`；文字结果页继续保留重量、比例、营养值编辑与确认记录链路；loading 态只用 spinner。文字结果页展示层补充安全 URL 解码，避免历史文本显示 `%20`。
- 验证：mobile typecheck 通过；加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 用本地 fixture 验证 mini result 图片态/文字态关键节点，图片态还成功点击 `.ratio-suggestion-action`，截图接口继续超时。ADB/Expo 从“我的 -> 识别记录”真实入口打开现有文字任务，截图 `tmp/mobile-text-result-stage40.png`、`tmp/mobile-text-result-stage40-ratio.png`、`tmp/mobile-text-result-stage40-decoded.png`；点击 50% 比例 chip 只改变本地临时状态，未保存记录。
- 限制：当前测试账号只有 1 条文字识别历史且积分不足，未能在 App 运行时打开图片 `ResultScreen`；图片结果页已完成静态与小程序 fixture 对照。
- 下一步：继续扫描其它仍保留通用模板或未做运行时对照的 App 页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十一阶段

- 本阶段聚焦 App `DayRecordScreen` / `RecordDetailScreen`，小程序参考页为 `packageExtra/pages/day-record` 与小程序饮食记录详情结构。
- 已改动：`DayRecordScreen` 从旧通用 `Page + Card` 改为浅绿页面、顶部日期、三格汇总、空态、记录卡、餐次徽章、食物明细行、单条食物删除和整条记录删除；`RecordDetailScreen` 改为详情 header、图片/占位 hero、场景标签、营养/建议信息块、食物明细、营养汇总、分享/圈子/删除动作和紧凑编辑面板。
- 保留链路：`getFoodRecordList()`、`getHomeDashboard()`、`updateFoodRecord()`、`deleteFoodRecord()`、系统分享、圈子详情跳转和从空态进入 `ManualRecord` 都继续复用原逻辑；运行时未新增、删除或修改真实饮食记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅有既有 LF/CRLF warning。weapp-devtools 9420 可重启到小程序单日记录页并读取 `.day-record-summary`、`.day-record-empty`、`.day-record-empty-btn`，截图接口继续超时。ADB/Expo Go 截图 `tmp/mobile-day-record-stage41-valid.png`；点击“去记录”进入手动记录页，截图 `tmp/mobile-manual-from-day-record-stage41-valid.png`。
- 限制：当前测试账号 2026-06-19 没有饮食记录，且本轮避免创建或删除真实记录，因此 `RecordDetailScreen` 只完成静态、类型和代码路径验证，未打开真实详情页截图。
- 下一步：继续扫描剩余记录细节、包装/图片结果或其它还未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十二阶段

- 本阶段聚焦 App `ExpiryScreen` / `ExpiryEditScreen`，小程序参考页为 `packageExtra/pages/expiry` 与 `packageExtra/pages/expiry-edit`。
- 已改动：保质期列表从旧通用 `Page + Card` 内联新增表单改为小程序同源结构：绿色 hero、右侧“新增”胶囊、四格统计、预览提醒、按优先处理/保鲜中/已处理分组的食物卡、状态徽章、保存方式/数量/备注/日期信息和安全二次确认动作；新增/编辑改为独立页，包含 hero、常用预设、基础信息、储存方式 chip、状态 chip 和贴底保存栏。
- 保留链路：`getFoodExpiryDashboard()`、`listFoodExpiryItems()`、`updateFoodExpiryItemStatus()`、`deleteFoodExpiryItem()`、`createFoodExpiryItem()`、`updateFoodExpiryItem()` 和编辑路由继续复用；运行时只进入页面、点击新增和点击“牛奶”预设，未点击保存、未删除、未改状态，没有写入真实保质期数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无新增命中，现有命中仅为包装/校园餐已有“分析中”状态；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序保质期列表可读 `.expiry-page`、hero、新增入口和 4 张统计卡；点击新增后进入 `packageExtra/pages/expiry-edit/index`，可读编辑根节点、8 个 block、6 个常用预设和储存/状态 chip；截图接口继续超时。
- ADB/Expo Go 验证：从首页食物保质期卡右侧“管理”进入 App 列表，截图 `tmp/mobile-expiry-stage42.png`；点击顶部“新增”进入编辑页，截图 `tmp/mobile-expiry-edit-stage42.png`；点击“牛奶”预设后名称、分类、到期日、数量和按钮状态正常，截图 `tmp/mobile-expiry-edit-preset-stage42.png`。Expo Go 调试齿轮浮层属于调试容器，不是生产 UI。
- 下一步：继续处理包装食品编辑/任务详情、位置搜索、代谢统计或其它还未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十三阶段

- 本阶段聚焦 App `PackagedFoodEditScreen` / `PackagedFoodTaskDetailScreen`，小程序参考页为 `packageExtra/pages/packaged-food-edit` 与 `packageExtra/pages/packaged-food-task-detail`。
- 已改动：包装食品编辑页从旧通用 `Page + Card + Field` 表单改为小程序同源结构：浅绿背景、零食上传 hero、拍摄示例、零食库搜索、正反面图片槽、AI 识别任务、商品信息两列字段、营养成分字段和贴底保存栏；任务详情页改为任务 hero、状态胶囊、图片卡、识别结果、营养标签、入库状态、配料和 OCR 分块。
- 保留链路：零食库搜索、图片选择/上传、包装识别任务提交、任务详情读取、从任务带回表单、营养识别、保存包装食品和任务详情跳转继续复用原 API；运行时未提交新识别任务、未保存包装食品，避免写入真实包装库或消耗积分。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg -n "加载中|提交中|保存中|删除中|搜索中|Refreshing" apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 无命中；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅有既有 LF/CRLF warning。
- weapp-devtools 9420 验证：小程序编辑页可读 `.packaged-food-edit-page`、`.wizard-section`、`.capture-card`；任务详情页可读 `.packaged-task-detail-page`、`.detail-hero`、3 个 `.detail-card`；截图接口继续超时。
- ADB/Expo Go 验证：从首页真实“包装食品”入口进入编辑页，截图 `tmp/mobile-packaged-edit-stage43.png`；下滑到图片槽、AI 识别任务和商品信息区，截图 `tmp/mobile-packaged-edit-lower-stage43.png`。当前未找到不写入真实数据即可进入 App 任务详情页的现成任务入口，因此 `PackagedFoodTaskDetailScreen` 只完成静态、类型和小程序运行时对照，缺少 App 真实详情截图。
- 下一步：继续处理位置搜索、代谢统计、包装任务详情可验证入口或其它还未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十四阶段

- 本阶段聚焦 App `LocationSearchScreen`，小程序参考页为 `packageExtra/pages/location-search`。
- 已改动：位置搜索页从旧通用 `Page + Card + Field + AppButton` 表单改为小程序同源结构：顶部地图预览区、中心 pin、左下“搜索地址”浮标、白色上浮面板、紧凑搜索行、当前选中卡、搜索结果列表和贴底“使用该位置”按钮。
- 保留链路：`apiClient.searchLocation()`、POI 经纬度解析、结果选择、`LocationSelection` 和 `returnTo: PublicFoodShare` 草稿回填继续复用；运行时只做页面进入和空搜索校验，未提交公共食物分享，未写入真实公共库数据。
- 运行时修复：首轮 App 截图发现远程 OSM tile 显示 `Access blocked`，已移除 `tile.openstreetmap.org` 依赖，改为本地稳定地图预览层；过程中一次 Fast Refresh 混入旧 bundle 触发 `LOCATION_TILE_ZOOM` 红屏，重新打开 Expo URL 后使用完整新 bundle 复测通过。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；旧 tile/helper 与目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序位置搜索关键节点并完成空搜索点击，截图接口继续 12 秒超时。ADB/Expo Go 从“我的 -> 更多功能 -> 定位搜索”真实入口进入，截图 `tmp/mobile-location-search-stage44-final2.png`；空搜索校验弹窗截图 `tmp/mobile-location-search-stage44-empty-alert.png`，标题“请输入关键词”；节点和截图均不含 `Access blocked` / `Render Error`。
- 下一步：继续处理代谢统计、包装任务详情可验证入口或其它仍未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十五阶段

- 本阶段聚焦 App `StatsMetabolicScreen`，小程序参考页为 `packageStatsMetabolic/pages/stats-metabolic`。
- 已改动：代谢分析页从旧 `Page + Card` 月度指标页改为小程序同源结构：独立绿白渐变页、自定义返回头、当日代谢标题、档案未完善门禁骨架、三项指标、SVG 曲线图、图例、红线口径说明、免责声明，以及代谢档案/用户基础数据底部弹层。`RootNavigator` 对 `StatsMetabolic` 隐藏原生 header，避免与自定义头重复。
- 保留链路：健康档案读取/保存、首页 dashboard 当日餐食、饮食记录 fallback、运动消耗和分钟级代谢模拟继续使用真实 API；运行时只打开门禁和档案弹层，未点击保存，未改动真实健康档案。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序代谢页关键节点并打开用户基础数据弹层，截图接口继续 12 秒超时。ADB/Expo Go 从“分析 -> 代谢分析”真实入口进入，当前账号展示档案未完善门禁，截图 `tmp/mobile-stats-metabolic-stage45-gate.png`；点击“点击填写”打开底部档案弹层，截图 `tmp/mobile-stats-metabolic-stage45-profile-sheet.png`，未点击保存。
- 下一步：继续处理包装任务详情可验证入口或其它仍未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十六阶段

- 本阶段聚焦 App `CommunityScreen`，小程序参考页为 `pages/community/index`。
- 已改动：圈子主 Tab 从旧 `Page + Card` 通用外壳改为小程序同源首屏结构：浅绿/灰背景、四宫格好友入口、绿色打卡排行榜、公开动态标题、搜索/筛选/发布栏、校园餐快捷 chip、朋友圈式透明信息流行和白底空态。
- 交互补齐：`更多筛选` 从旧跳转搜索页改为页内底部抽屉，支持排序和内容类型筛选，并继续调用真实 `communityGetFeed` 参数；搜索、发布、食物库、好友/私信/通知、排行榜和动态详情导航仍复用原链路。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序圈子页关键节点并点击筛选抽屉，截图接口继续 12 秒超时。ADB/Expo Go 圈子首屏和筛选抽屉截图 `tmp/mobile-community-stage46-final.png`、`tmp/mobile-community-filter-stage46.png`。
- 下一步：继续处理主 Tab 内仍保留通用模板的统计、首页、我的或其它迁移长尾页；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十七阶段

- 本阶段聚焦 App `StatsScreen`，小程序参考页为 `pages/stats/index`。
- 已改动：统计/分析主 Tab 从旧 `Page + Card` 通用外壳改为小程序同源结构：浅绿背景、固定周期胶囊、健康指数门禁/综合分 hero、三段分析 tab、双列风险卡、AI 风险解读卡、热量趋势、宏量营养、餐次结构、长期健康指标、底部周期选择弹层和风险详情弹层。
- 保留链路：`getStatsSummary(range)`、`generateStatsInsight(range)`、`StatsSummary.body_metrics`、AI 助手、代谢分析和身体趋势导航继续复用原 API 与路由；运行时只做进入、tab 切换和周期弹层检查，未点击生成 AI，未写真实数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序统计页关键节点并完成 AI/结构 tab 与周期胶囊交互，截图接口继续 12 秒超时。ADB/Expo Go 截图 `tmp/mobile-stats-stage47-main.png`、`tmp/mobile-stats-stage47-ai.png`、`tmp/mobile-stats-stage47-structure.png`、`tmp/mobile-stats-stage47-range.png`，UI dump 确认周期弹层文本节点。
- 下一步：继续处理首页、我的、记录入口或其它仍保留通用模板/未做运行时对照的 App 页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十八阶段：我的主 Tab

- 本阶段聚焦 App `ProfileScreen`，小程序参考页为 `pages/profile/index`。
- 已改动：我的主 Tab 从旧 `Page + Card` 通用外壳改为小程序同源结构：浅绿/灰背景、透明用户信息区、三项快捷统计、食探会员双进度卡、单张白色列表卡、独立清缓存/退出登录工具卡和版本号；`更多功能` 作为 App 平台长尾入口保留在同一列表卡内折叠展开，并为底部 TabBar 增加滚动余量。
- 保留链路：用户资料、会员、奖励积分、保质期、记录天数、识别/好友/收藏数量、好友请求红点、个人主页、会员中心、服务列表、清缓存和退出登录继续复用原 API/导航；运行时未点击清缓存、退出登录或会员开通。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序“我的”页关键节点；截图接口/命令层仍会超时，此前 quick action 轻交互进入识别记录页。ADB/Expo Go 截图 `tmp/mobile-profile-stage48-main.png`、`tmp/mobile-profile-stage48-more-expanded.png`，确认主态和“更多功能”展开态正常。
- 下一步：继续处理首页、记录入口、分析等待页、公共食物库详情或其它仍保留通用模板/未做运行时对照的 App 页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第四十九阶段

- 本阶段聚焦 App `AnalyzeLoadingScreen`，小程序参考页为 `packageExtra/pages/analyze-loading`。
- 已改动：分析等待页从旧通用 `Page + Card + ActivityIndicator` 改为小程序同源全屏体验：图片/文字背景、暗色可读遮罩、扫描框、扫描线、健康小贴士、阶段面板、等待问答卡、模式标签、底部离开/识别记录入口和页内失败/违规状态；`RootNavigator` 对 `AnalyzeLoading` 隐藏原生 header。
- 保留链路：真实 `apiClient.getAnalyzeTask()` 轮询继续可用；完成后按任务类型进入 `TextResult` / `Result`，运动任务回 `BodyMetricRecord`；失败、超时、取消、违规和连续网络失败展示为页面内状态，避免大弹窗打断沉浸页。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`AnalyzeLoadingScreen` 不再引用 `Page` / `Card` / `ActivityIndicator`；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 用 `debug-stage49` 验证小程序文字等待态关键节点存在，并确认“换一个”能切换等待卡；截图接口继续 12 秒超时。
- ADB/Expo Go 验证受限：设备在线，尝试通过 Expo deep link 打开等待页，但当前 App 只处理 invite/private-chat/profile 深链，仍停留在“我的”页；本轮没有创建真实 AI 任务、没有改积分、没有污染测试账号。尝试截图 `tmp/mobile-analyze-loading-stage49-attempt.png` 仅证明入口受限，不作为等待页截图。
- 下一步：继续处理首页、记录入口、公共食物库详情或其它仍未完全对照页面；如要补 App 等待页真机截图，需要安全 pending 任务、有积分测试账号，或明确同意加入开发用 QA 深链。

## 2026-06-19 App 小程序视觉一致性第五十阶段
- 本阶段聚焦 App `PublicFoodDetailScreen`，小程序参考页为 `packageExtra/pages/food-library-detail`，校园食堂详情也复用同一路由。
- 已改动：公共食物详情从通用 `Page` 外壳改为独立 `ScrollView + RefreshControl`，去掉通用页面内边距；顶部图片区支持横向分页多图和动态计数器；信息卡、校园卡、评论卡、纠错卡与贴底操作栏按小程序边距/密度收敛。
- 保留链路：`getPublicFood()`、`getCampusFoodDetail()`、评论/回复、删除评论、点赞、收藏、纠错反馈、业主编辑/删除和一键记录继续复用；运行时未点击任何会写入真实数据的动作。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning；weapp-devtools 9420 从小程序公共食物库进入详情并验证关键节点，截图命令不可用；ADB/Expo Go 从圈子「校园餐」进入校园列表再打开 `咖啡汉堡套餐` 详情，截图 `tmp/mobile-campus-list-stage50.png`、`tmp/mobile-public-detail-stage50-top.png`、`tmp/mobile-public-detail-stage50-lower.png`，UI dump 确认图片 hero、营养卡、校园信息、评论、纠错和贴底操作栏。
- 下一步：继续处理首页、记录入口、本地食物详情或其它仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第五十一阶段
- 本阶段聚焦 App `PublicFoodScreen`，小程序参考页为 `packageExtra/pages/food-library`。
- 已改动：公共食物库列表从通用 `Page` 外壳改为固定全屏灰底、顶部四 tab、全部页搜索/排序/筛选、独立列表滚动、公共食物卡片和右下悬浮分享按钮；校园 tab 按小程序逻辑隐藏搜索/排序区，直接展示校园菜品列表。
- 保留链路：全部/校园/收藏/我上传列表读取、商家搜索、排序、减脂筛选、详情跳转、校园食堂入口、公共食物分享入口继续复用；运行时未点击点赞、收藏、删除、发布或分享提交。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；scoped diff check 仅既有 LF/CRLF warning；weapp-devtools 9420 验证小程序公共食物库关键节点、校园 tab 和筛选面板；ADB/Expo Go 从“我的 -> 公共食物库”进入，截图 `tmp/mobile-public-food-stage51-main.png`、`tmp/mobile-public-food-stage51-filter.png`、`tmp/mobile-public-food-stage51-campus.png`，UI dump 确认 tab、搜索、排序、筛选、卡片和 FAB 存在。
- 下一步：继续处理首页、记录入口、本地食物库详情/列表、圈子动态详情或其它仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第五十二阶段

- 本阶段聚焦 App `HomeScreen`，小程序参考页为 `pages/index/index`。
- 已改动：首页从旧 `Page + Card` 通用外壳改为小程序同源结构：问候区、日期 selector、运营 banner、热量主卡、宏量营养环、身体状态条、今日餐食、保质期预览和统计入口；热量/宏量目标编辑改为底部弹层。
- 保留链路：`useHomeDashboard(selectedDate)` 按选中日期读取 dashboard；热量目标更新、DayRecord、Expiry、Stats、AnalyzeHistory/TextRecord/PackagedFoodEdit 等原有导航继续可用。运行时只切换日期和打开弹层，未保存目标，未写真实记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg` 加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/HomeScreen.tsx apps/mobile/src/hooks/useHomeDashboard.ts` 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序首页关键节点并点击日期，截图接口不可用；ADB/Expo Go 首页截图 `tmp/mobile-home-stage52-main.png`，目标弹层截图 `tmp/mobile-home-target-stage52.png`，XML dump 确认核心节点存在且无 Render Error。
- 限制：小程序 `page.screenshot` / 截图命令仍不可用，本阶段以节点、交互和 App ADB 截图作为证据。
- 下一步：继续处理记录入口、本地食物库详情/列表、圈子动态详情或其它仍保留通用模板/未做运行时对照的页面；总目标不要标记完成。

## 2026-06-19 App 小程序视觉一致性第五十三阶段

- 本阶段聚焦 App `AnalyzeScreen`，小程序参考页为 `packageExtra/pages/analyze`。
- 已改动：图片分析设置页从旧 `Page + Card` 大模板改为小程序同源结构：透明积分条、摄影技巧胶囊、图片预览/空占位、识别模式、联网/分项/多视角/AI 摄入比例开关、文字补充、餐次、运动时机和贴底提交/识别记录入口；`RootNavigator` 标题同步为“图片分析”。
- 保留链路：拍照/相册选图、3 张上限、上传图片、图片分析提交、模式传参、餐次/日期/运动时机/文字补充、等待页跳转、识别历史和开发 demo 入口继续复用；本轮未提交真实分析任务、未上传新图片、未消耗积分。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描无命中；`AnalyzeScreen` 不再引用 `Page` / `Card` / `AppButton` / 旧记录快捷组件；scoped diff check 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序分析页关键节点并完成模式/联网选项点击，截图接口继续 12 秒超时。ADB/Expo Go 从首页真实记录入口进入 App 图片分析页，截图 `tmp/mobile-analyze-stage53.png`；模式切换截图 `tmp/mobile-analyze-mode-stage53.png`；摄影技巧弹层截图 `tmp/mobile-analyze-help-stage53.png`。
- 限制：当前测试账号可用积分为 0，所以提交按钮按小程序逻辑显示积分不足禁用态；如需完整提交流，需要可用积分测试账号或临时授权补积分。
- 下一步：继续处理本地食物库详情/列表、圈子动态详情、社区详情或其它仍保留通用模板/未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第五十四阶段

- 本阶段聚焦 App `FoodLibraryDetailScreen`，参考小程序 `packageExtra/pages/record-manual` 的本地/手动食物记录结构。
- 已改动：本地食物库详情从旧 `Page + Card` 通用模板改为独立浅绿页面，包含图片 hero、来源/份量胶囊、四格营养、营养信息卡、记录到今天表单和贴底保存栏；`public_library` 已映射为“真实餐食”，避免暴露后端枚举。
- 保留链路：详情 item 读取、刷新、餐次选择、日期/重量输入和 `addFoodRecord()` 保存逻辑继续可用；运行时只进入页面并切换餐次，未点击保存，没有创建真实饮食记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；加载/提交/保存等纯加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序手动记录关键节点并打开自定义面板，截图接口仍超时。ADB/Expo Go 从“记录一餐 -> 食物库 -> 详情”进入，截图 `tmp/mobile-food-library-detail-stage54.png`、`tmp/mobile-food-library-detail-stage54-localized.png`、`tmp/mobile-food-library-detail-stage54-meal.png`；UI dump 确认来源中文化和餐次切换后的底部摘要。
- 下一步：继续处理 `CommunityFeedDetailScreen`、`PublicProfile`、`BodyTrends`、`RewardCenterScreen`、`CirclePostEditScreen` 或其它仍保留通用模板/未做运行时对照的页面；总目标仍不要标记完成。

## 2026-06-19 App 小程序视觉一致性第五十五阶段

- 本阶段聚焦 App `CommunityFeedDetailScreen`，小程序参考页为 `packageExtra/pages/interaction-feed-detail`。
- 已改动：圈子动态详情从旧 `Page + Card` 通用详情页改为小程序同源朋友圈式结构：浅绿/灰背景、头像列、作者/时间、动态标题/正文、图片网格、营养摘要、点赞/评论/更多操作、评论/回复列表和贴底评论输入栏。
- 保留链路：动态详情读取、刷新、头像主页跳转、点赞/取消点赞、评论/回复、编辑/删除本人圈子帖、举报他人动态继续复用原 API 与路由；运行时只进入详情并打开更多操作抽屉，未点赞、未评论、未举报、未删除。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/提交/保存/删除/搜索/举报文案扫描无命中；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 可进入小程序详情页并读取根节点、动态卡、操作区、评论输入和 action sheet，截图接口继续超时。ADB/Expo Go 从“圈子 -> 搜索 奶油 -> 第一条动态”进入 App 详情，截图 `tmp/mobile-community-detail-stage55.png`；点击更多操作打开底部抽屉，截图 `tmp/mobile-community-detail-action-stage55.png`，UI dump 确认“动态操作 / 举报动态 / 取消”。
- 下一步：继续处理 `PublicProfile`、`BodyTrends`、`RewardCenterScreen`、`CirclePostEditScreen` 或其它仍保留通用模板/未做运行时对照的页面；总目标仍不要标记完成。
## 2026-06-19 App 小程序视觉一致性第五十六阶段

- 本阶段聚焦 App `CirclePostEditScreen`，小程序参考页为 `packageExtra/pages/circle-post-edit`。
- 已改动：圈子发布/编辑页从旧 `Page + Card + Field + AppButton` 通用表单改为小程序同源结构：浅灰页面、图片上传卡、标题/正文编辑卡、营养信息卡、营养开关、两列营养字段和贴底“存草稿 / 发布动态”操作栏。
- 保留链路：`createCirclePost()`、`updateCirclePost()`、编辑态回填、图片上传、草稿保存/清理和营养字段提交继续可用；运行时只进入页面并打开营养信息，没有点击发布或保存真实动态。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/提交/保存/删除/搜索/举报/上传文案扫描无命中；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 可读小程序发布页关键节点，点击营养开关后读取 8 个营养字段，截图 `tmp/weapp-circle-post-edit-stage56.png`。ADB/Expo Go 从圈子真实发布入口进入 App 发布页，初始 XML dump 确认图片卡、编辑卡和 footer；点击营养开关后截图 `tmp/mobile-circle-post-edit-nutrition-stage56.png`，UI dump 确认 8 个营养字段和禁用态发布按钮。
- 限制：App 截图中可见 Expo Go 调试齿轮浮层，这是开发环境覆盖层，不属于生产 UI；本轮没有点击“发布动态”，没有新增或编辑真实圈子数据。
- 下一步：继续处理 `PublicProfile`、`BodyTrends`、`RewardCenterScreen` 或其它仍保留通用模板/未做运行时对照的页面；总目标仍不要标记完成。
## 2026-06-19 App 小程序视觉一致性第五十七阶段

- 本阶段聚焦 App `RewardCenterScreen`，小程序参考页为 `packageExtra/pages/reward-center`。
- 已改动：积分中心从通用 `Page` 外壳改为小程序同源结构：浅灰页面、绿色奖励积分 hero、两项积分统计、深色“最快拿分”任务卡、今日进度标题和白色任务卡列表；移除 App 独有任务进度条，避免偏离小程序母版。
- 保留链路：`getRewardCenter()` 读取、下拉刷新、可用任务筛选、任务跳转、公共食物上传和包装食品/分享等任务导航继续可用；运行时只做页面进入和任务导航，没有提交任务、没有新增公共食物、没有消耗积分。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/提交/保存/删除/搜索/举报/发布/上传文案扫描无命中；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 进入小程序积分中心，读取关键节点和 3 个任务卡，点击任务可导航到 `packageExtra/pages/day-record/index`，截图 `tmp/weapp-reward-center-stage57.png`。ADB/Expo Go 从“我的 -> 赚积分”进入 App 积分中心，截图 `tmp/mobile-reward-center-stage57.png`，XML 确认核心文本节点；点击第一个“去完成”进入“单日记录”，再返回积分中心。
- 限制：App 截图中可见 Expo Go 调试齿轮浮层，这是开发环境覆盖层，不属于生产 UI；本轮没有执行会改变积分或公共库数据的提交动作。
- 下一步：继续处理 `PublicProfile`、`BodyTrends` 或其它仍保留通用模板/未做运行时对照的页面；总目标仍不要标记完成。
## 2026-06-19 App 小程序视觉一致性第五十八阶段

- 本阶段聚焦 App `BodyTrendsScreen`，小程序参考页为 `packageExtra/pages/body-trends` 兼容页，以及其落点 `weight-trend / water-trend / exercise-trend`。
- 已改动：App `BodyTrends` 从旧的 `Page + Card + MacroRow` 聚合模板改为小程序同源兼容跳转页；默认直接 `replace` 到 `TrendDetail` 的体重趋势，支持 `tab=water/exercise` 映射到喝水/运动趋势；`RootNavigator` 对 `BodyTrends` 隐藏 header，避免用户看到 App 独有中间页。
- 保留链路：单项 `TrendDetail` 页继续使用已有体重、饮水、运动趋势数据读取、刷新、删除确认和历史列表逻辑；本轮未创建、删除或修改任何身体记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/提交/保存/删除/搜索/举报/发布/上传文案扫描无命中；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/navigation/types.ts apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 验证小程序 `body-trends?tab=water` 跳到 `water-trend`，`body-trends?tab=exercise` 跳到 `exercise-trend`，且运动趋势页关键节点存在；截图命令继续超时。ADB/Expo Go 从“我的 -> 更多功能 -> 身体趋势”进入，截图 `tmp/mobile-body-trends-stage58.png`，XML 确认落到“体重趋势 / 近 30 天趋势 / 历史记录”，旧 App 聚合页没有出现。
- 限制：App 截图中可见 Expo Go 调试齿轮浮层，这是开发环境覆盖层，不属于生产 UI；本轮未运行 `npm run build:weapp`，未重启本地前后端或 Metro。
- 下一步：继续处理 `PublicProfile` 或其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成到每个页面完全一致。
## 2026-06-19 App 小程序视觉一致性第五十九阶段

- 本阶段聚焦 App `PublicProfile` 兼容入口，小程序参考语义为 `packageExtra/pages/profile-settings/index?user_id=...`。
- 已改动：`PublicProfileScreen` 从旧 `Page + Card` 用户主页模板改为隐藏 header 的兼容重定向页，进入后立即 `replace` 到 `ProfileSettings({ userId })`；圈子主 Tab 的作者入口也直接进入 `ProfileSettings`，避免用户看到 App 独有公共主页中转页。
- 保留链路：个人主页资料、关注/私信、动态、收藏、分享和关注列表仍由已对齐的 `ProfileSettingsScreen` 承接；旧 `PublicProfile` 路由只作为历史兼容存在。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/提交/保存/删除/搜索/举报/发布/上传文案扫描无命中；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/screens/CommunityScreen.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 打开小程序他人主页并验证 profile-settings 关键节点，点击“食物收藏”切换成功；截图命令继续超时。ADB/Expo Go 用 `profile?user_id=ec6b7cf7-4719-497c-bc58-78e362b16824` 打开 App 他人主页，截图 `tmp/mobile-public-profile-stage59.png`，点击“食物收藏”后截图 `tmp/mobile-public-profile-stage59-collections.png`；XML 确认页面标题为“个人主页”，存在关注/私信、最新动态/食物收藏和收藏条目。
- 限制：当前测试账号圈子 feed 为空，未能从真实动态卡作者入口截图；代码调用点已确认圈子作者入口不再导航旧 `PublicProfile`。
- 下一步：继续处理其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成到每个页面完全一致。
## 2026-06-19 App 小程序视觉一致性第六十阶段

- 本阶段聚焦 App `ConversationsScreen` 与 `PrivateChatScreen`，小程序参考页为 `packageExtra/pages/private-conversations` 和 `packageExtra/pages/private-chat`。
- 已改动：会话列表移除 App 独有顶部未读总数胶囊，系统会话头像改为小程序同源品牌图；聊天页动态更新对方昵称标题，聊天头像缩到小程序密度，气泡宽度、图片消息、底部 action sheet 尺寸更贴近小程序；自己消息操作改为“撤回”并补 15 分钟窗口判断。
- 保留链路：会话列表、未读徽标、进入聊天、资料读取、轮询、标记已读、文本/图片发送、复制、撤回/举报、头像进主页继续走原 API/路由；本轮未发送、撤回或举报真实消息。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载/发送/删除/举报文案未新增；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 确认小程序会话页与聊天页关键节点，点击会话成功进入聊天页；直接用 `miniprogram-automator` 调 `.chat-bubble.longpress()` 确认底部操作面板出现并有 2 个动作，截图命令仍超时。ADB/Expo Go 用 `private-chat?user_id=...` 打开 App 私信页，截图 `tmp/mobile-private-chat-stage60.png`、`tmp/mobile-private-chat-stage60-thread.png`，XML 确认动态标题、空会话态、底部图片按钮、输入框和禁用发送按钮。
- 限制：当前 App 测试账号与小程序会话数据不一致，所测对方在 App 中为空会话；为避免污染真实私信，本轮没有发送测试消息，因此没有 App 侧真实消息气泡 action sheet 截图。
- 下一步：继续处理其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十一阶段

- 本阶段聚焦 App `NotificationsScreen`，小程序参考页为 `packageExtra/pages/interaction-notifications`。
- 已改动：互动消息页从整页 ScrollView 收紧为小程序同源收件箱结构：白色头部说明卡、右侧“全部已读”胶囊、三段 tab、独立列表/空态区域；去掉 App 独有的已读图标和空态圆形图标；通知时间改为小程序式相对时间。
- 保留链路：通知列表读取、tab 筛选、自动/手动已读、单条跳转 `CommunityFeedDetail`、头像进 `ProfileSettings` 和分页加载继续复用原 API/导航；运行时只进入页面并切换 tab，没有制造真实点赞/评论/回复数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描未指向本页；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 通过 `miniprogram-automator` 打开小程序互动消息页，确认根节点、头部、已读按钮、tab 和通知卡存在，并点击“点赞”tab 成功；直连 automator 无截图方法。ADB/Expo Go 从圈子主 Tab 自然入口进入 App 互动消息页，截图 `tmp/mobile-notifications-stage61-raw.png`；点击“点赞”tab 后截图 `tmp/mobile-notifications-like-stage61-raw.png`，XML 确认空态、tab 激活和禁用全部已读按钮。
- 限制：App 当前测试账号互动消息为空态，小程序账号有通知卡，数据状态不一致；本轮未主动制造通知数据。
- 下一步：继续处理其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十二阶段

- 本阶段聚焦 App `FriendsScreen`，小程序参考页为 `packageExtra/pages/friends`。
- 已改动：好友页从带 App 自有“查找新好友”大卡和好友卡私信动作的模板，收紧为小程序同源结构：右上刷新胶囊、三段 tab、只在有好友时显示昵称搜索、好友卡删除按钮、收到请求/我发起的请求列表和无外框轻量空态；好友空态按钮“去添加好友”回到圈子发现好友。
- 保留链路：`listFriends()`、`getFriendRequestsOverview()`、好友资料跳转、删除好友、同意/拒绝收到请求、撤销发出请求、下拉刷新和圈子入口继续复用原 API 与路由；运行时只进入页面并切换 tab，没有删除好友、处理请求或撤销真实申请。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标加载文案扫描未指向好友页；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 用 `miniprogram-automator` 打开小程序好友页，确认 3 个 tab、刷新按钮、列表容器和 5 张好友卡，并切换“收到的请求 / 我发起的”；`mrc` 不在 PATH，直连 automator 无截图能力。ADB/Expo Go 从圈子真实“好友管理”入口进入 App 好友页，截图 `tmp/mobile-friends-stage62-raw.png`；切“收到请求”后截图 `tmp/mobile-friends-received-stage62-raw.png`，XML 确认不再出现“查找新好友”或“私信”。
- 限制：当前 App 测试账号好友/请求为空态，小程序账号有好友卡，数据状态不一致；本轮不主动制造好友或请求数据。
- 下一步：继续处理其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十三阶段

- 本阶段聚焦 App `PrivacySettingsScreen` 与 `UserGroupScreen`，小程序参考页分别为 `packageExtra/pages/privacy-settings` 和 `packageUserGroup/pages/user-group` / `packageExtra/pages/user-group`。
- 已改动：隐私设置页补齐小程序 `Cell.Group` 风格的“基础隐私”分组标题、白色圆角设置组和更紧凑的开关尺寸；用户群页补齐小程序同款浅绿到灰渐变背景，保留 hero、白色二维码卡、永久有效标签、二维码框、打开二维码/复制群名按钮和加入方式卡。
- 保留链路：隐私设置读取/更新 `getUserProfile()`、`updateUserProfile()` 逻辑不变；用户群二维码预览与复制群名逻辑不变。本轮没有点击隐私开关，没有改真实账号设置。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标纯加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅既有 LF/CRLF warning。weapp-devtools 9420 验证小程序隐私页有 2 个 cell/2 个 switch，用户群页有 hero、二维码卡、按钮和加入方式卡，并点击“复制群名”。ADB/Expo Go 从“我的 -> 加入用户群”进入 App 用户群页，截图 `tmp/mobile-user-group-stage63.png`；从“我的 -> 隐私设置”进入 App 隐私设置页，截图 `tmp/mobile-privacy-settings-stage63.png`，XML 确认分组标题与 2 个设置项/开关。
- 限制：小程序截图仍未取得，使用节点和交互作为小程序侧证据；App 截图中可见 Expo Go 调试齿轮浮层，非生产 UI。
- 下一步：继续处理其它仍保留通用模板/未做运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十五阶段

- 本阶段聚焦 App `RecipeEditScreen`，小程序参考页为 `packageExtra/pages/recipe-edit`。
- 已改动：食谱编辑页从带页内大标题、收藏开关、图片 URL、标签和食物明细编辑器的 App 独有重表单，收回为小程序同款浅绿背景、白色基本信息卡、餐次胶囊、营养摘要卡和底部删除/保存动作；无 `recipeId` 的入口也改为小程序同款“无法编辑食谱”空态。
- 保留链路：食谱详情读取、名称/描述/餐次编辑、保存、删除继续走原 API；已加载的图片、标签、收藏状态和 items payload 仍在保存时透传，本轮没有点击保存或删除真实食谱。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 验证小程序空态 `.empty-card`，以及带 `id` 表单态 2 张 `.form-card`、6 个 `.option`、`.summary` 和 `.action-bar`；截图接口仍超时。ADB/Expo Go 从个人主页食物收藏入口进入 App 编辑页，截图 `tmp/mobile-recipe-edit-stage65-pull.png`，XML 确认旧“图片 URL / 标签 / 食物明细 / 餐食图片预览 / 新建食谱”不再可见。
- 限制：App 侧从他人收藏进入时详情 API 弹过“获取食谱失败”，关闭后仍可验证当前编辑页结构；本轮未修改该权限/数据问题。截图中可见 Expo Go 齿轮浮层，非生产 UI。
- 下一步：继续处理公共食物分享、会员中心、自动续费审核、宠物实验室或其它仍未逐页运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十六阶段

- 本阶段聚焦 App `PublicFoodShareScreen`，小程序参考页为 `packageExtra/pages/food-library-share` 与 `packageExtra/pages/campus-food-share`。
- 已改动：普通公共食物分享页从 App 独有三来源大卡、顶部 hero、最近公共食物参考和大表单，收回为小程序同款图片上传、营养摘要、基础信息、餐食来源芯片、标签、地址、备注和贴底提交；校园分享页改为小程序同款浅绿 hero、菜品图片、菜品信息、价格、标签、备注和贴底提交。
- 保留链路：图片上传、编辑态回填、位置搜索草稿恢复、普通/校园 submit、tags、价格、评分和公共库保存/创建 API 继续复用原逻辑；本轮没有点击提交，没有上传/发布真实公共库数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 验证普通/校园小程序页面关键节点存在。ADB/Expo Go 从圈子入口进入普通分享和补校园餐页，截图 `tmp/mobile-public-food-share-stage66.png`、`tmp/mobile-public-food-share-campus-stage66.png`、`tmp/mobile-public-food-share-campus-lower-stage66.png`；普通页来源切换已验证，校园页下滑确认价格、标签、备注和贴底提交栏。
- 限制：小程序截图接口继续不可用/超时，本轮以小程序节点和交互作为小程序侧证据；Expo Go 调试齿轮浮层不属于生产 UI。
- 下一步：继续处理会员中心、自动续费审核、宠物实验室或其它仍未逐页运行时对照的页面；整体 `/goal` 仍未完成。
## 2026-06-19 App 小程序视觉一致性第六十七阶段

- 本阶段聚焦 App `MembershipCenterScreen`，小程序参考页为 `packageExtra/pages/pro-membership/index`。
- 已改动：会员中心从偏 App 化的套餐列表收回为小程序同款浅绿背景、深色会员 hero、三档套餐、三周期卡、默认标准版年卡、价格卡、权益对比表、积分消耗说明、当前状态卡、立即开通按钮，以及自动续费审核/会员协议入口；原生导航头也换成小程序同款浅绿背景。
- 保留链路：会员状态读取、套餐列表读取、支付创建、支付同步、自动续费审核和会员协议导航继续走原 API/路由；本轮没有点击最终开通或创建真实支付订单。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 可进入小程序会员中心并读取关键节点，点击档位/周期后 active 节点正确；截图接口仍超时。ADB/Expo Go 从“我的”会员入口进入 App 会员中心，截图 `tmp/mobile-membership-stage67.png`；切换进阶/月卡后截图 `tmp/mobile-membership-stage67-switched.png`；下滑后截图 `tmp/mobile-membership-stage67-lower.png`，XML 确认积分消耗、当前状态、立即开通、自动续费审核和会员协议入口。
- 限制：小程序截图接口继续不可用/超时，本轮以小程序节点和交互作为小程序侧证据；Expo Go 调试齿轮浮层不属于生产 UI。
- 下一步：继续处理自动续费审核、宠物实验室或其它仍未逐页运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十八阶段

- 本阶段聚焦 App `AutoRenewAuditScreen`，小程序参考页为 `packageExtra/pages/auto-renew-audit/index`。
- 已改动：自动续费审核页移除 App 独有蓝色审核预览首屏提示，原生标题改为“自动续费”并使用浅绿 header；页面顺序和文案收紧为小程序同款 hero、服务内容、会员权益与价目表、自动续费签约前说明、产品内取消续费路径、确认/关闭预览弹窗。
- 保留链路：当前会员状态、真实套餐列表、套餐选择、协议勾选、会员协议入口和预览弹窗继续走原 App 数据/导航逻辑；本轮未创建支付订单、未发起代扣、未执行真实关闭自动续费。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 验证小程序自动续费审核页关键节点和确认/关闭两个 modal。ADB/Expo Go 从会员中心自动续费入口进入 App 页面，截图 `tmp/mobile-auto-renew-stage68.png`、`tmp/mobile-auto-renew-stage68-lower.png`、`tmp/mobile-auto-renew-stage68-sign-modal.png`、`tmp/mobile-auto-renew-stage68-cancel-modal.png`，XML 确认签约和关闭 modal 文案。
- 限制：小程序截图接口继续超时，本轮以节点和交互作为小程序侧证据；Expo Go 调试齿轮浮层不属于生产 UI。
- 下一步：继续处理宠物实验室或其它仍未逐页运行时对照的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第六十九阶段：宠物外观试验箱

- 本阶段聚焦 App `PetLabScreen` / `PetHome` 外观试验箱入口，小程序参考页为 `packageExtra/pages/pet-lab`。
- 已改动：App 宠物外观页补齐小程序同款宠物外观试验箱 hero、基础组合统计、当前形象、生成原理公式、画像倾向、推荐/特色/风险样本、质量/画像/颜色/体型/动物/花纹/配饰筛选、当前筛选/已展示计数、两列候选网格和详情弹层；`PetHome` 入口改为“外观试验箱 / 打开试验箱”，原生导航标题为“宠物试验箱”。
- 保留链路：`getPetSummary()`、`getMyMembership()`、`rerollPetAppearance()`、`selectPetAppearance()` 保持真实数据与刷新/选择链路；本轮运行时只进入页面、滚动、筛选和打开详情，没有点击随机刷新或选择候选，未消耗积分、未切换真实外观。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 已验证小程序 `pet-lab` 关键节点、样本详情弹层和筛选交互，截图接口 8s 超时。ADB/Expo Go 从 PetHome 真实入口进入 App 宠物试验箱，截图 `tmp/mobile-pet-lab-stage69-first.png`、`tmp/mobile-pet-lab-stage69-mid.png`、`tmp/mobile-pet-lab-stage69-grid.png`、`tmp/mobile-pet-lab-stage69-modal.png`；XML 确认入口热更新、筛选从 35 种缩到 4 种、详情弹层可打开；可见文本/content-desc 未出现 `surprised`、`pattern-`、`steady_caregiver`、`energetic_buddy`、`gentle_healer`、`protein_guardian`、`light_lifestyle` 等内部枚举。
- 限制：小程序截图接口继续超时，本轮以节点和交互作为小程序侧证据；Expo Go 齿轮浮层非生产 UI；详情中的 `组合 ID：sample-...` 与小程序母版一致。
- 下一步：继续逐页处理其它仍未运行时对照或仍保留通用大模板的 App 页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十阶段：宠物聊天页

- 本阶段聚焦 App `PetChatScreen`，小程序参考页为 `packageExtra/pages/pet-chat`。
- 已改动：宠物聊天页从 App 自有的范围分段与较大头像密度，收回为小程序同款浅绿到米色背景、顶部“最近 / 新对话”、宠物舞台气泡、intro 文案、宠物/用户消息气泡、线索编号、行动 chip、横向快捷问题、贴底输入栏和底部历史对话 sheet；移除 App 独有“最近 7 天 / 最近 30 天”分段控件；标题初始为“问问宠物”，拿到宠物名后动态显示“和{宠物名}聊聊”。
- 保留链路：宠物摘要、统计摘要、最新会话、会话列表、打开历史会话、生成宠物分析和本地新对话逻辑继续复用原 API；本轮运行时只打开页面、打开历史面板和新建本地空对话，没有发送真实聊天问题。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/PetChatScreen.tsx apps/mobile/src/components/PetAvatar.tsx apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描未新增。weapp-devtools/miniprogram-automator 9420 验证小程序宠物聊天页根节点、顶部动作、舞台、消息、快捷问题、输入栏、历史 sheet 与新对话交互，截图接口 8s 超时。ADB/Expo Go 从 PetHome “去问问它”真实入口进入 App，截图 `tmp/mobile-pet-chat-stage70-first.png`、`tmp/mobile-pet-chat-stage70-history.png`；XML 确认旧“伙伴对话 / 发送给伙伴”和范围分段控件不再出现。
- 限制：小程序截图接口继续超时，本轮以节点和交互作为小程序侧证据；Expo Go 齿轮浮层非生产 UI。
- 下一步：继续逐页处理其它仍未运行时对照或仍保留通用大模板的 App 页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十一阶段：打卡排行榜页

- 本阶段聚焦 App `CheckinLeaderboardScreen`，小程序参考页为 `packageExtra/pages/checkin-leaderboard`。
- 已改动：打卡排行榜页改为小程序同款浅绿到白色背景、固定页头、周期文案、白色排行榜行、排名、头像、昵称、“我”标签和右侧打卡次数；移除 App 独有兜底副标题；头像占位改为通用人物占位；“我”的行只保留绿色描边/阴影，不再整行浅绿。
- 交互对齐：小程序排行榜行点击后仍停留在本页；App 榜单行也从 `Pressable` 改为纯展示 `View`，不再点击进入个人主页。
- 保留链路：排行榜读取继续走 `communityGetCheckinLeaderboard()`，错误/空态/刷新拉取逻辑保留；本轮没有写入真实数据。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 验证小程序页根节点、页头、周期、列表、6 行榜单、1 行“我”，点击首行后仍停留在排行榜页；截图接口继续超时。ADB/Expo Go 从“圈子 -> 本周打卡排行榜”真实入口进入 App，截图 `tmp/mobile-checkin-leaderboard-stage71.png`、点击榜单行后 `tmp/mobile-checkin-leaderboard-stage71-aftertap.png`；XML 确认标题、周期、榜单行、“我”和“次打卡”文案。
- 限制：小程序截图接口继续不可用/超时，本轮以节点和交互作为小程序侧证据；App 与小程序当前登录数据集不完全一致，小程序读到 6 行，App 当前账号只读到 1 行，后续若要同数据截图需先统一账号/环境。
- 下一步：继续逐页处理其它仍未运行时对照或仍保留通用大模板的 App 页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十二阶段：邀请好友页

- 本阶段聚焦 App `InviteFriendsScreen`，小程序参考页为 `packageExtra/pages/invite-friends`。
- 已改动：邀请好友页从 App 独有的手动填写邀请码、复制链接、平台说明和额外操作入口，收紧为小程序同款浅绿背景、邀请有礼 hero、邀请人卡、邀请码 chip、三条规则、二维码卡和底部两个动作按钮；分享标题和消息文案也统一为“食探”，不再在邀请分享入口显示 `Food Link`。
- 保留链路：`getUserProfile()`、`getInviteProfile()`、`getInviteProfileByCode()`、`resolveInvite()`、`acceptInvite()`、系统分享、复制邀请码和本地二维码预览继续走原 App 链路；深链 `fi` / `invite_code` / `inviteCode` 进入时会先初始化邀请码。运行时只进入页面和复制邀请码，没有接受真实邀请或创建好友申请。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。weapp-devtools/miniprogram-automator 9420 验证小程序邀请页根节点、hero、邀请人卡、邀请码 chip、3 条规则、二维码卡、2 个按钮，且无手动输入卡/平台说明；截图接口 8s 超时。ADB/Expo Go 从“我的 -> 邀请好友”真实入口进入 App，截图 `tmp/mobile-invite-friends-stage72.png`，复制邀请码后截图 `tmp/mobile-invite-friends-stage72-copy.png`；XML 负面扫描确认无“手动填写 / 复制链接 / 平台说明 / App 侧 / Food Link / 解析邀请码 / 邀请链接”。
- 限制：小程序截图接口继续超时，本轮以小程序节点和交互作为小程序侧证据；App 截图中可见 Expo Go 调试齿轮浮层，非生产 UI。
- 下一步：继续逐页处理其它仍未运行时对照或仍保留通用大模板的 App 页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十三阶段：鹅鸭鸡识别专线

- 本阶段补齐 App 侧此前缺失的小程序 `packageExtra/pages/goose-duck-chicken` 对应页面，确保鹅腿/鸭腿/鸡腿专线不是混入普通食物分析的入口。
- 已改动：新增 `GooseDuckChickenScreen`，按小程序母版实现暖色页面、热点 hero、上传卡、补充线索、单纯识别通道说明、结果卡和底部“开始专线识别”；新增 `GooseDuckChicken` 路由；首页 banner 和底部“记录一餐”弹层均增加“鹅鸭鸡识别”入口。
- 保留链路：图片上传继续复用 `uploadAnalyzeImageFile()`，识别调用新增的 `apiClient.classifyGooseDuckChicken()`，请求后端独立 `POST /api/analyze/goose-duck-chicken`，只返回 `species/label/confidence/reason/evidence`，不进入普通分析 loading、营养回算或饮食记录保存。
- 验证：`npm --workspace apps/mobile run typecheck` 与 `npm --workspace packages/api-client run typecheck` 通过；`git diff --check -- packages/api-client/src/index.ts apps/mobile/src/screens/GooseDuckChickenScreen.tsx apps/mobile/src/navigation/types.ts apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/components/RecordActionSheet.tsx apps/mobile/src/navigation/CustomTabBar.tsx apps/mobile/src/screens/HomeScreen.tsx` 仅既有 LF/CRLF warning；目标加载文案扫描无命中。
- weapp-devtools/miniprogram-automator 9420：小程序专线页可进入，`.goose-page`、`.goose-hero`、`.goose-upload-card`、`.goose-info-card`、`.goose-rule-card`、`.goose-submit` 均为 1；点击提交但未选图后仍停留在专线页，未出现结果卡。截图接口继续卡住超时。
- ADB/Expo Go：首页 banner 已显示“鹅腿阿姨热点识别 / 鹅腿、鸭腿，还是鸡腿？”并可进入 App 专线页；截图 `tmp/mobile-goose-stage73.png`。点击上传卡打开来源选择弹层，显示“选择图片 / 拍照 / 从相册选择”，截图 `tmp/mobile-goose-source-stage73.png`；未继续选图或触发真实上传/识别。
- 下一步：继续排查其它小程序已注册但 App 缺失或仍保留通用大模板的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十四阶段：体重记录页

- 本阶段聚焦 App `BodyMetricRecordScreen(type=weight)`，小程序参考页为 `packageExtra/pages/weight-record`。
- 已改动：体重记录页接收首页传入的 `date`，按小程序母版重排为浅灰/浅绿页面、白色顶部卡、日期 kicker、“查看趋势”胶囊、居中大号体重输入、绿色保存按钮、当天记录卡和本月概览卡；体重记录行日期改为短日期格式，避免继续使用完整时间戳风格。
- 已改动：`HomeBodyStatusStrip` 的体重/喝水/运动入口都会把当前首页日期传给 `BodyMetricRecord`；`RootNavigator` 的记录页原生标题改为随类型显示“记录体重 / 记录喝水 / 记录运动”，不再使用旧的统一“身体记录”。
- 保留链路：体重记录读取、保存、删除继续走原 App API；本轮运行时只进入页面和点击“查看趋势”，没有点击保存或删除真实体重记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/HomeScreen.tsx apps/mobile/src/navigation/types.ts apps/mobile/src/navigation/RootNavigator.tsx` 仅既有 LF/CRLF warning；目标路径未新增纯加载文案，后续第七十五阶段已清理同文件另一块既有任务状态标签“处理中”。
- weapp-devtools/miniprogram-automator 9420：小程序 `weight-record` 可进入并确认页面根、topbar、main card、day card 和 trend link；点击 `.weight-trend-link` 后进入 `packageExtra/pages/weight-trend/index`。截图接口仍超时，因此小程序侧以节点和交互作为证据。
- ADB/Expo Go：从首页体重卡进入 App 体重记录页，XML 确认“记录体重 / 查看趋势 / 2026-06-19 的体重 / 保存体重 / 今天记录 / 本月概览”等节点；点击“查看趋势”进入“体重趋势”。截图 `tmp/mobile-weight-record-stage74.png`、`tmp/mobile-weight-record-stage74-final.png`、`tmp/mobile-weight-trend-from-record-stage74.png`；热更新后截图可见 Expo Go `Refreshing...` 蓝条，属于开发容器浮层，不是生产 UI。
- 下一步：继续处理喝水记录、运动记录及三个趋势页的进一步小程序结构对照；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十五阶段：喝水记录页与运动记录页

- 本阶段继续处理 `BodyMetricRecordScreen(type=water|exercise)`，小程序参考页为 `packageExtra/pages/water-record` 和 `packageExtra/pages/exercise-record`。
- 已改动：喝水记录页总毫升数、单位颜色和字号按小程序收回为黑字/灰色单位，进度卡高度与快捷加水按钮高度按 rpx 折算收紧；运动记录页统计卡、统计数字、记录运动标题、快捷提示居中和运动记录 kcal 数值按小程序密度收紧。
- 已改动：通用任务状态标签中的进行中展示从“排队中/处理中”改为“排队/识别”，避免在运动记录 pending 卡上出现纯加载态文案；接口状态和轮询逻辑不变。
- 保留链路：喝水添加/清空/删除、运动文本或图片提交、任务轮询、刷新结果和记录删除都继续走原 App API；本轮运行时只做页面进入和趋势跳转，没有添加喝水、提交运动或删除真实记录。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；目标文件 `git diff --check` 仅既有 LF/CRLF warning；纯加载文案扫描无命中。
- weapp-devtools/miniprogram-automator 9420：小程序水页确认 `.water-record-page`、topbar、progress/action/day card 和 trend link；小程序运动页确认 `.exercise-record-page`、统计区、输入卡、8 个快捷 chip、输入行和 trend link；点击水/运动趋势分别进入 `water-trend` 与 `exercise-trend`。截图接口仍不稳定，本轮以节点和交互作为小程序证据。
- ADB/Expo Go：从首页中间“喝水”卡进入 App 喝水记录页，截图 `tmp/mobile-water-record-stage75.png`，XML 确认“记录喝水 / 快捷加水 / +150ml / 添加 / 今天记录 / 本月概览”；点击趋势进入“饮水趋势”，截图 `tmp/mobile-water-trend-stage75.png`。从首页右侧“运动”卡进入 App 运动记录页，截图 `tmp/mobile-exercise-record-stage75.png`，XML 确认“记录运动 / 今天运动消耗 / 快捷 chip / 输入框 / 空态”；点击趋势进入“运动趋势”，截图 `tmp/mobile-exercise-trend-stage75.png`。
- 下一步：继续处理体重/喝水/运动三个趋势页本身的视觉结构细节，或继续扫描其它小程序注册页里 App 尚未逐页对照的长尾页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十六阶段：体重/饮水/运动趋势页

- 本阶段聚焦 `TrendDetailScreen(kind=weight|water|exercise)`，小程序参考页为 `packageExtra/pages/weight-trend`、`packageExtra/pages/water-trend` 和 `packageExtra/pages/exercise-trend`。
- 已改动：`BodyMetricRecordScreen` 的体重、喝水、运动“查看趋势”都会把当前记录日期传给 `TrendDetail`；趋势页 hero 使用入口日期展示，近 30 天统计范围改为以今天结尾，贴近小程序 `buildDateRange(30)`。
- 已改动：趋势页外层改为小程序同款顶部主题色渐变到灰底；体重趋势卡/历史卡、饮水热力图/最近喝水、运动热力图/最近运动的卡片高度、padding、字号和行高继续收紧。摘要卡统一收回为 2 列布局，运动第三张摘要卡落到第二行左侧；饮水热力图经 ADB XML 确认首行 10 格。
- 已改动：通用任务状态标签另一处 `processing/running` 文案从“处理中”改成“处理”，避免目标文件加载文案扫描继续命中；状态逻辑不变。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/navigation/types.ts apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 仅既有 LF/CRLF warning；目标纯加载文案扫描无命中。
- weapp-devtools/miniprogram-automator 9420：小程序三个趋势页均可进入，分别确认根节点、hero、摘要卡、趋势图/热力图、历史卡和记录/空态节点；`body-trends?tab=water` 会重定向到 `water-trend?date=2026-06-19`；点击饮水热力格后选中态节点数量变为 1。`page.screenshot` 不存在，`npx mrc screenshot` 也因当前依赖无法确定 executable，因此本轮小程序侧仍以节点和交互为证据。
- ADB/Expo Go：从首页真实体重、喝水、运动卡进入记录页，再点击“查看趋势”进入三个趋势页。截图 `tmp/mobile-weight-trend-stage76.png`、`tmp/mobile-water-trend-stage76.png`、`tmp/mobile-exercise-trend-stage76.png`；XML 确认“体重趋势 / 饮水趋势 / 运动趋势”、入口日期 `2026-06-19`、摘要卡、趋势图/热力图、历史区和对应空态/记录区存在；返回运动记录页链路也验证可用。
- 下一步：继续扫描其它小程序注册页里 App 尚未逐页运行时对照的长尾页面；整体 `/goal` 仍未完成到每个页面完全一致。

## 2026-06-19 App 小程序视觉一致性第七十七阶段：登录页二次收紧

- 本阶段聚焦 App `LoginScreen`，小程序参考页为 `packageExtra/pages/login`；登录方式存在平台差异，App 保留手机号验证码登录和微信登录，但首屏视觉继续向小程序靠拢。
- 已改动：登录页从纯文字品牌头改为小程序同款 logo 容器、应用名“智健食探”和标语“记录饮食，连接健康”；表单顶部、手机号/验证码行、主按钮、微信登录文字按钮、协议勾选框和底部调试区间距进一步收紧，避免继续像 App 大表单。
- 保留链路：短信验证码发送、后端 cooldown 倒计时、短信登录/注册、微信登录、协议入口、invite deep link 参数和 debug 登录逻辑均未改动。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`rg` 目标纯加载文案扫描无命中；`git diff --check -- apps/mobile/src/screens/LoginScreen.tsx` 仅既有 LF/CRLF warning。weapp-devtools/miniprogram-automator 9420 已进入小程序登录页，确认 `.login-page`、`.login-header`、`.app-logo`、`.app-name`、`.app-slogan`、`.wx-login-btn`、`.skip-login-btn`、`.agreement-row` 等节点存在，并点击协议行后 `.agreement-checkbox.checked` 数量为 1。
- 限制：当前 ADB/Expo Go 处于已登录的饮水趋势页，App 没有公开 login deep link；为避免清除用户会话，本阶段未执行 `adb shell pm clear host.exp.exponent`，因此没有新的 App 登录页截图。若后续需要登录页真机截图，需要用户允许临时清 Expo Go 本地登录态或提供未登录设备。
- 下一步：继续做小程序注册页全局补漏扫描，优先确认旧兼容入口、平台差异页和仍可能残留旧模板的页面；整体 `/goal` 仍未完成。

## 2026-06-19 App 小程序视觉一致性第七十八阶段：旧模板残留清理与分享/会员补漏

- 本阶段聚焦全局旧模板残留，以及 `PublicFoodShareScreen` / `MembershipCenterScreen` 的运行时补漏；整体 `/goal` 仍在进行中，不要标记完成。
- 已改动：`DetailScreens.tsx`、`MigrationScreens.tsx`、`SecondaryMigrationScreens.tsx`、`TertiaryMigrationScreens.tsx` 不再从旧通用 `Page` / `Card` 组件导入或渲染；会员中心和公共食物分享页改为页面自有 `View/ScrollView + RefreshControl` 外壳；积分中心、自动续费等旧空态改为本地轻量空态，避免退回大字号通用模板。
- 已改动：`RootNavigator` 中会员中心原生标题从“积分充值”改为“食探会员”；运动任务/健康档案/分析历史里残留的纯处理中类文案收紧为“识别”等短状态或非加载提示。
- 保留链路：会员中心真实 `getMyMembership()`、`listMembershipPlans()`、支付预览、自动续费入口；公共食物/校园食物分享的选图、表单、定位、提交前置能力均保留。本轮运行时没有提交公共库、没有上传图片、没有创建支付订单。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/HealthProfileViewScreen.tsx apps/mobile/src/screens/MigrationScreens.tsx apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx apps/mobile/src/screens/LoginScreen.tsx` 仅既有 LF/CRLF warning；全局 `Page/Card` 旧模板扫描无命中；全局“未迁移/占位/加载中/处理中”等可见遗留文案扫描无命中。
- weapp-devtools/miniprogram-automator 9420：已验证小程序 `food-library-share`、`campus-food-share`、`pro-membership` 关键节点和会员周期点击交互；小程序截图接口仍超时，本轮以节点和交互作为小程序侧证据。
- ADB/Expo Go：App 侧截图 `tmp/mobile-public-food-share-stage78.png`、`tmp/mobile-public-food-share-campus-stage78.png`、`tmp/mobile-membership-title-stage78.png`；XML 确认分享食物页的图片、营养、基础信息、来源和贴底“发布到公共库”，校园分享页的 hero、菜品图片、菜品信息、价格信息和贴底“提交并后台分析”，会员页原生标题已为“食探会员”。
- 限制：Expo Go 右上角齿轮浮层仍会出现在截图里，属于开发容器浮层，不是生产 UI；登录页 App 侧真机截图仍需未登录设备或用户允许清 Expo Go 本地登录态。
- 下一步：继续做长尾逐页截图级复核，优先复查 `search-results` / `CommunitySearch`、`result-text` 独立路径、`food-expiry` / `about` 兼容入口，以及全量页面最终验收清单。
## 2026-06-19 App mini-program visual parity Stage 79: search results / CommunitySearch

- Status: completed the `CommunitySearchScreen` density pass against mini-program `packageExtra/pages/search-results/index`; the overall `/goal` is still active and must not be marked complete.
- Changed: tightened the App search bar and tab strip to match the mini-program rpx density more closely: 32dp search input, 48dp search bar, lighter 13dp search action text, 48dp tab strip, 12dp top tab padding, 14dp search icon, and history title weight aligned to the mini-program style. No API or navigation behavior was changed.
- Preserved: search history, history remove/clear, content/user tab switching, `communitySearch()`, auto-switch to user results when content is empty, content detail navigation, profile navigation, optimistic like/unlike, comment entry and pagination all continue to use the existing App logic.
- Mini evidence: direct `miniprogram-automator` connection to `ws://127.0.0.1:9420` opened `/packageExtra/pages/search-results/index?keyword=%E7%89%9B`; nodes confirmed `.search-results-page`, `.search-bar`, `.search-history`, `.search-tabs`, 2 `.tab-item`, 20 `.content-card`; after tapping the user tab, `.user-list` existed with 4 `.user-card`. `mrc` is not in PATH and direct automator exposes no screenshot method, so mini evidence is node/interaction based.
- App evidence: ADB/Expo Go used the real route `圈子 -> 搜索动态内容或用户... -> CommunitySearch`; screenshots saved as `tmp/mobile-community-search-empty-stage79.png`, `tmp/mobile-community-search-content-stage79.png`, `tmp/mobile-community-search-users-stage79.png`, plus `tmp/mobile-community-search-users-stage79-clean.png`. XML confirmed `圈子搜索`, search bar, `搜索记录`, `动态内容(41)`, content cards for `奶油`, user tab, and user empty state. The screenshots currently include Expo Go `Refreshing...` and gear overlays after hot reload; these are development container overlays, not production UI.
- Verification: `npm --workspace apps/mobile run typecheck` passed; `git diff --check -- apps/mobile/src/screens/CommunitySearchScreen.tsx` only reported the existing LF/CRLF warning; target loading/progress copy scan had no hits.
- Next: continue with standalone `result-text` / App `TextResult`, then compatibility entries `food-expiry` and `about`, and final page-by-page validation checklist.

## 2026-06-19 App 小程序视觉一致性第八十阶段：文字识别结果页

- 状态：已完成 App `TextResultScreen` 对小程序 `packageExtra/pages/result-text/index` 的结构收紧；整体 `/goal` 仍在进行中，不要标记完成。
- 已改动：文字识别结果页移除旧 `Page` / `Card` / `AppButton` 大模板，改为小程序同款灰色 hero、悬浮营养总览卡、AI 饮食透视、包含成分区、食材卡内重量/比例/三大营养素编辑，以及单一贴底“确认记录”按钮；修复 hero 安全区 padding 导致标题被营养卡遮挡的问题。
- 已改动：底部“确认记录”不再直接写入，先弹出小程序同款餐次选择层，6 个餐次选项默认带入入口餐次；只有点击“确认保存”才调用保存，点击“取消”或遮罩只关闭弹层。
- 保留链路：`buildSaveFoodRecordRequestFromTask()`、`apiClient.saveFoodRecord()`、`food_text` entry_type、记录详情跳转、食物名称/重量/比例/PFC 编辑和重新计算逻辑继续使用原 App 真实链路；运行时验证没有点击确认保存，没有创建真实饮食记录。
- 小程序证据：`miniprogram-automator` 直连 `ws://127.0.0.1:9420` 打开 `/packageExtra/pages/result-text/index?date=2026-06-19`，通过测试 storage 注入文字识别结果；确认 `.result-text-page`、`.hero-section`、`.nutrition-overview-card`、3 个 `.macro-item`、`.insight-card`、`.ingredients-section`、2 个 `.ingredient-card`、6 个 `.macro-editor-chip`、`.footer-actions`、`.primary-btn` 存在；点击 `.primary-btn` 后确认 `.meal-selector-overlay.visible`、`.meal-selector-card`、6 个 `.meal-option-item`、`.confirm-btn`、`.cancel-btn` 存在。截图接口仍超时，本轮以节点/交互为证据。
- App 证据：ADB/Expo Go 从首页“记录一餐 -> 识别记录”进入真实文字任务，截图 `tmp/mobile-text-result-stage80-fixed2.png`、`tmp/mobile-text-result-stage80-scroll.png`、`tmp/mobile-text-result-stage80-bottom.png`、`tmp/mobile-text-result-stage80-meal-selector.png`；XML 确认餐次弹层显示“选择餐次 / 早餐 / 早加餐 / 午餐 / 午加餐 / 晚餐 / 晚加餐 / 取消 / 确认保存”，取消后弹层关闭且没有出现“保存成功”。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/screens/TextResultScreen.tsx` 仅既有 LF/CRLF warning；目标页纯加载/旧文字扫描无命中，旧通用组件扫描只剩本地 `nutritionOverviewCard` 等样式名，不是旧组件引用。
- 下一步：继续复核 `food-expiry` 与 `about` 兼容入口，并整理最终全量页面验收清单；登录页 App 截图仍需要未登录设备或用户允许清 Expo Go 本地会话。

## 2026-06-19 App 小程序视觉一致性第八十一阶段：food-expiry / about 兼容入口

- 状态：已完成 `food-expiry` 与 `about` 兼容入口复核与 App 深链补齐；整体 `/goal` 仍在进行中，不要标记完成。
- 已改动：`RootNavigator` 增加静态深链识别，收到 `about` 路径时导航到 `About`，收到 `food-expiry` / `expiry` / `pages/expiry` 路径时导航到 `Expiry`；未登录栈也注册 `About`，但 `Expiry`、邀请、私信和个人主页仍等待登录后再跳。
- 设计取舍：小程序旧 `food-expiry` 是“正在跳转到食材保质期页面...”兼容页；App 不新增跳转中页面，直接落到真实 `Expiry`，避免 App 出现临时跳转文案和额外视觉差异。
- 小程序证据：`miniprogram-automator` 9420 打开 `/packageExtra/pages/food-expiry/index` 后当前路径变为 `packageExtra/pages/expiry/index`，`.expiry-page`、`.expiry-hero-title`、`.expiry-summary-grid` 均为 1，`.legacy-food-expiry-page` 为 0；`/packageAbout/pages/about/index` 与 `/packageExtra/pages/about/index` 均可打开，`.about-page`、`.header-section`、`.app-name`、2 个 `.card`、`.cell-group` 存在。当前运行产物 `dist/app.json` 没有主包 `pages/about/index`，所以该 compat 主包路径报 `page not found`。
- App 证据：ADB/Expo Go 深链 `exp://192.168.8.193:8081/--/about` 打开 `About`，截图 `tmp/mobile-stage81-about.png`，XML 确认“关于 / 智健食探 / 关于食探 / 官方邮箱 / 特别鸣谢”；深链 `exp://192.168.8.193:8081/--/food-expiry` 打开 `Expiry`，截图 `tmp/mobile-stage81-food-expiry.png`，XML 确认“食物保质期 / 我的食物管理 / 保质期提醒 / 今天优先吃 / 即将过期 / 已过期 / 保鲜中”，且未出现“正在跳转”。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/screens/TextResultScreen.tsx` 仅既有 LF/CRLF warning。
- 下一步：整理小程序注册页到 App 路由的最终全量验收清单，标出已截图/节点验证、平台差异和剩余阻塞。

## 2026-06-19 App 小程序视觉一致性最终页表清单

- 已新增 `.local-state/current-task/app-mini-parity-checklist-2026-06-19.md`，按当前 `apps/wechat/dist/app.json` 登记 63 个小程序运行态页面到 App 路由/页面的映射。
- 清单自检：Node 脚本读取 `apps/wechat/dist/app.json` 后比对清单文本，`pageCount=63`、`missing=[]`，没有未登记的小程序页面。
- 当前页面映射结论：63/63 均有 App 承接；`food-expiry` 和重复 about 归为兼容入口，`login` 归为平台差异页。
- 仍需单独补的证据：登录页 App 侧截图需要未登录设备或用户允许清 Expo Go 本地会话；近期小程序截图 API 继续超时，因此小程序侧证据以节点/交互为主。
- 最终检查：`npm --workspace apps/mobile run typecheck` 通过；`git diff --check -- apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/screens/TextResultScreen.tsx .local-state/current-task/app-mini-parity-checklist-2026-06-19.md memory/2026-06-19.md` 仅既有 LF/CRLF warning。
