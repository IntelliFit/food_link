# 2026-04-11

- 已按 `weapp-devtools` 技能执行：`cli auto --project food_link --auto-port 9420`，`mrc pageInfo` 确认当前包为 `pages/index/index`；截图存于本机 `/tmp/food_link_verify_index2.png`（首页正常渲染）。
- 分享海报「较昨同餐 + 三点」为记录详情页 Canvas 绘制，完整视觉验证需在开发者工具内打开含 `id` 的记录详情 → 登录为本人 → 点「生成分享海报」；纯 `relaunch` 无 `id` 时无法稳定进入有效详情态。

## 首页 → 餐食 → 生成分享图（mrc 自动化复现）

- `mrc relaunch /pages/index/index`
- `mrc waitFor ".meal-item--tappable"`（今日餐食加载完成）
- `mrc tap ".meal-item--tappable"` → 已进入 `pages/record-detail/index?id=...&ui=home`
- `mrc waitFor ".poster-btn"` → `mrc tap ".poster-btn"`
- `mrc waitFor ".poster-modal-image"` → 弹层已出现且预览图已挂载（说明 Canvas 导出成功）
- 本轮 `mrc screenshot` 多次返回 `fail to capture screenshot` 或耗时极长，未稳定落盘弹窗截图；若需留证请在模拟器界面手动截图或使用开发者工具自带截图。

## 2026-04-11 晚：skill 复验（关闭 DevTools 后重启）

- `npm run dev:restart` + `cli open` + `cli auto --port 9420`
- 首页无 `.meal-item`（模拟器可能未登录/当日无餐食），改 `relaunch` 带 `id` 进 `record-detail` → `tap .poster-btn` → `waitFor .poster-modal-image` 成功
- `mrc screenshot /tmp/food_link_poster_verify_skill.png` 成功：分享预览中 **135 kcal 右侧可见米色胶囊**，文案为 **「暂无昨日同餐」** + 三空心圆（`has_baseline=false` 占位逻辑生效）；若需「较昨 ±数字」需库中昨日同餐次有记录。
