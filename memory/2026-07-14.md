# 2026-07-14

- 只读定位用户截图中的营养不闭合：昵称“瑞”于 19:05 记录的三层皇堡，截图原始任务为 989 kcal / 蛋白55.9g / 碳水43g / 脂肪51.6g，4/4/9 仅860 kcal；当天截图合计1755.4 kcal与宏量换算1611.8 kcal相差143.7 kcal，因此只剩5 kcal而宏量未满是后端数据真实矛盾，不是前端缓存。
- 根因是未命中库后 Qwen 生成的每100g 230 kcal / 13g / 10g / 12g 被自动持久化为有效 `qwen_generated` 食物；现有归一化只防热量明显低于宏量换算，不防热量明显偏高，写库和记录保存也无双向门禁。该用户后来已把记录改为1173/70/50/77并闭合，但错误库条目仍有效。只读范围审计按“高出至少20 kcal且10%”发现 qwen 1/58、deepseek 9/1739；本轮未改生产数据或业务代码。

- 已提交邀请会员奖励与健康服务优化：奖励延迟启用并区分邀请人7天/受邀人3天；同步奖励中心、消息跳转、关于页独立分包、食物分析/健康文本模型路由、迁移和测试。随后合并远端 `dev` 的账号注销、昵称唯一性、公开收藏菜谱和社区跳转修复；本地及远端 `main`、`dev` 已同步，不纳入终端残留 `wq!`。

- 用户进一步纠正产品结构：邀请会员奖励不属于“赚积分”，应独立命名为“邀请好友得会员”。已在“我的”页新增独立入口，邀请页承载邀请码/邀请链接/二维码、进度、奖励和启用；“赚积分”删除邀请模块并过滤邀请会员奖励。
- 本轮按已向用户明示的规则落地：新朋友 7 天内完成 2 个不同自然日有效记录后，邀请人获得 7 天轻度会员，新朋友获得 3 天轻度会员；两边长期保存并手动启用，邀请积分兼容字段归零。系统消息改跳独立邀请中心，用户协议同步。
- Go 邀请/奖励/app 测试与 vet、前端定向 ESLint、Jest 17 suites/64 tests、diff-check 通过；全量 tsc 仍只有既有 day-record 空值类型错误。weapp-devtools 3001/9420/9421 均未监听，无法取得页面截图和导航/分享交互证据。无数据库结构变化，本轮无需再次迁移。
- 用户纠正邀请奖励产品定义：保留“赚积分”原任务页，去掉独立“我的礼券”；邀请人和被邀请人达标后各获得可长期保存、手动启用才开始计时的 7 天轻度会员，系统消息应能直接前往启用。
- Git 追溯确认 `Kirigaya <1193466151@qq.com>` 的 `aa427217` 引入礼券后端/页面/奖励中心区，`7fef6ac9` 把“我的礼券”加入个人页；原“赚积分”入口来自 `18af916f`。任务消失的直接原因是前端 `Promise.all` 将任务请求和礼券请求绑死，礼券请求失败时整页任务数据不落状态。
- 已移除个人页独立礼券入口，旧礼券页只做兼容跳转；“赚积分”恢复任务独立加载、邀请任务常驻展示，并在有 pending 奖励时显示“可用奖励”和“现在启用”。系统消息支持 action/path，旧消息无 action 时也按内容兼容跳转。
- 后端邀请奖励改为不设过期时间，达标未启用显示“待启用”，手动启用后才调用会员发放；新增迁移用于清理历史邀请奖励过期字段和恢复未使用记录。只读远程库确认截图用户已有 1 条 pending 奖励，消息也带 action/path，后端并非空壳；本轮没有远程写库授权，迁移未执行。
- Go 定向测试/vet、小程序全量 Jest 17 suites/63 tests、定向 ESLint、diff-check 通过；全量 tsc 仍只有既有 day-record 空值类型错误。weapp-devtools 3001/9420/9421 均未监听，未启动用户进程，故没有运行时截图/点击证据。
- 用户随后明确授权迁移当前远程库。2026-07-14 10:06 对 `154.8.205.78:5432 / food-link / public` 成功执行完整幂等迁移。回查未使用过期邀请奖励和仍需规范化记录均为 0；库中当前没有 `invite_light_week`。
- 邀请历史只读审计：`pending_qualified=2`、`reward_active=2`、`reward_blocked=9`、`reward_completed=0`，不存在已达标但既无新奖励又无旧会员 grant 的漏奖。旧逻辑已经生效的 2 条 `reward_active` 保持不变；后端部署后，未来达标邀请才开始生成长期保存、手动启用的 7 天会员奖励。

- 用户反馈小程序本地/体验版 WXSS 报错和线上识别 400。当前 dist 直接证实：`app.json` 将 about 同时列入 `packageExtra`、`packageAbout`，两个目录的 about JS/WXML 存在但 `index.wxss` 均不存在，邻页 WXSS 正常，故是构建分包产物问题、与后端无关。三种 weapp build 脚本当前实际命令相同，API 由运行时 envVersion 选取。
- 线上识别截图 `Invalid JSON payload ... Unknown name \"messages\"` 是 Gemini 原生上游收到 OpenAI body 的服务端协议错误；本地已有 Gemini native 修复但线上仍旧错误，表明生产后端未运行该修复。weapp 自动化端口未监听，未改代码/未启动服务/未执行 build。
- 用户要求快速修复后，已把 about 唯一收敛到 `packageAbout`：从 `packageExtra` 页列表删除，并迁移为本分包直连 TSX+SCSS，删除旧跨分包源文件；路由 helper 保持指向 `packageAbout`。版本文案同步使用 `__APP_VERSION__`。定向 ESLint 与保留原视觉样式的独立 Sass 编译（3061 bytes）通过；weapp 自动化 3001/9420/9421 全部拒绝连接，未启动用户服务，未执行 production build。
- 纠正本轮一次误判：用户明确所有模型实际出口早已统一为万界方舟，只有历史字段名可保留旧供应商名称。后端本地真实配置源是 `app-config.yaml`，生产真实配置源是 Apollo，二者的五组模型 base URL 均为万界；旧 `config.yaml` 中的 Yunwu URL 不参与服务启动。误加的 Yunwu native 支持已撤销，后端文件恢复到 Git 状态。正确的万界 Gemini 原生协议修复已在 `fa63b561`，截图仍出现 `messages` 表明线上镜像/Pod 尚未运行该提交。万界定向测试和 app 装配检查通过，待用户重新部署当前后端。
- 用户 release build 成功且包体均 OK，但开发者工具上传报旧 `packageExtra/pages/about/index.js` 不存在。新 dist 的 app.json/文件完整且旧路径引用为 0，确认是 `ignoreUploadUnusedFiles=true` 的陈旧上传依赖图。已在 public/private project config 同时关闭该优化，并关闭 private 热重载覆盖；JSON 校验通过。需要关闭项目窗口后重新打开以重载配置，无需重 build。

- 后端镜像推送启动卡住已定位在 Docker 前：`push-docker-ccr.mjs` 标题后立即同步执行 `git rev-parse`，该命令在仓库中超过19–34秒不返回。发现 2026-06-23 残留的 `git rebase --continue -> git commit` 进程；未擅自终止。清理经用户确认的残留进程并确认 Git 命令立即返回后，才重试构建。

- 用户反馈宠物对话仍显示 Markdown 星号。根因是后端 prompt 要求 Markdown，但小程序 `PetMarkdown` 只处理双星号，单星号 `*强调*` 会原样显示。已扩展为单/双/三星号、代码、下划线和列表兼容渲染，并要求新模型回复不用星号/下划线强调。
- 新增 `apps/wechat/tests/unit/pet-markdown.test.tsx`，定向 Jest（2 cases）、目标 ESLint、后端 `TestBuildPetChatPromptRequiresGentleTone` 和 `git diff --check` 均通过。`mrc where --port 3001` 被拒绝，未能完成开发者工具截图/交互验证；未启动或重启用户进程。

- 万界方舟 Gemini 兼容性复测：`gemini-3-flash-preview` 与 `gemini-3.5-flash` 在 `/api/v1/chat/completions` 上使用 OpenAI `messages` 会出现 400/500，不是 key 或模型不可用。两模型在 `https://maas-openapi.wanjiedata.com/api/v1beta/models/{model}:generateContent` 使用 Gemini 原生 `contents`、`inlineData`、`generationConfig` 后，文字和 HTTPS 图片请求均返回 HTTP 200。
- 已修改 `backend/internal/analyze/service/llm_client.go`：仅当 base URL 为万界方舟且模型名前缀为 `gemini-` 时使用原生 Gemini endpoint；HTTPS 图片下载后以内联图片上传，限制 10MB。普通与精准模型名保持用户指定值不变。新增单测，`go test ./internal/analyze/service -run '^TestOfoxAIClient_' -count=1`、`go test ./internal/app -run '^$' -count=1` 通过；尚待后端部署。
- 已修改 `backend/internal/analyze/service/analyze_service.go`：`external.llm_provider=doubao` 不再覆盖明确写出的 `qwen3.6-flash`，从而保证快速模式使用 Qwen。快速模式定向测试与全量 `go test ./internal/analyze/service -count=1`（43 秒）均通过；尚待后端部署。
- 已提交并推送 `f1a3abfa feat: 完善 AI 路由与营养审核`；本地和远端 `main`、`dev` 已同步到该提交。根目录未跟踪的 `wq!` 是终端分支列表输出，未纳入提交。
- 线上精准模式牛肉炒饭失败已定位：任务 `911665f2-41f0-4a71-b40e-3f5675d11f1a` 走 `strict_separate`，隐藏规划阶段仍调用旧豆包，万界方舟返回 400/model-not-found。已在本地把精准规划/称重/复核统一为 `gemini-3.5-flash`，瞬时故障回退改为 `qwen3.6-flash`；包装标签 OCR 改为 Qwen 3.6，营养未命中补全改为 `deepseek-v4-pro`。
- 已增加任务错误双层清洗：worker 写入前清洗，TaskService 返回历史任务前再次清洗。相关 4 个后端包测试及 vet 全通过；代码提交 `039580db` 已推送并同步 `main`、`dev`，尚待用户部署。
- 保质期识别截图问题已从生产日志定位：02:02:44 Gemini 走错误 OpenAI `messages` 协议返回 `contents is required`，旧豆包回退又返回 `model not found`。已删除该自建协议/豆包回退；保质期、营养标签 OCR、零食 `packaged_product_extract` 均固定复用万界原生 `gemini-3.5-flash`。Gemini 原生 model/token metadata 也已补齐，定向测试与 vet 通过；修复提交 `fa63b561` 已推送并同步本地/远端 `main`、`dev`，待用户部署验证。
- 体验版更新 dev 后端后仍慢的真实证据：快速任务 `0a115c2b-863e-4e00-b258-b8fa166ccbde` 排队约 1 秒、处理 149.4 秒，但 metadata 确认首轮 `qwen3.6-flash` 仅 5.96 秒。根因是首轮后串行 DeepSeek 可食部/语义复用/营养补全，营养补全还会在 DeepSeek 最多约 78 秒失败后再调用 Qwen。已在本地把用户可见后处理改为 Qwen 优先；快速模式首轮识别含重试共享 25 秒预算，后处理共享 20 秒预算，跳过额外可食部和语义模型，营养补全最多 12 秒，建议比例保留并由 Qwen 生成。分析服务全量测试、定向回归、相关 vet 和 diff-check 通过，尚未提交推送部署。
- 用户进一步决定所有追求实时响应的文本链路都优先 Qwen。已把文字食物分析、统计 AI 解读、宠物普通/流式对话、饮食推荐、自定义关注点统一到 `qwen3.6-flash` 优先配置，并修复显式 Qwen 误用豆包客户端；`AI Usage Pricing` 不再决定宠物实际模型。快速/普通/精准核心视觉映射保持不变，离线管理员审核保留 DeepSeek。分析、健康、工作器、App 四包全量测试及相关 vet/diff-check 通过，尚未提交推送部署。
- 营养别名审核后台已补齐 Admin 页面：独立导航、待审列表、筛选、目标食物搜索、人工候选、Pro 单条/10 条批量预审、人工通过/拒绝及完整审计信息。Pro 高置信度生成的别名仍是 pending；批准时后端再次执行混合菜/单品、形态和主食碳水门禁后才事务写正式表。
- 识别链路的 DeepSeek 语义复用达到 0.95 后会自动进入候选队列，不再只打“跳过永久写入”日志；写候选失败不影响本次识别。Admin build、AI 不自动发布测试、语义命中只入候选测试和牛肉面硬门禁测试通过。候选表迁移尚未执行，因为当前配置指向远程库且本轮未重新确认迁移授权。
# 2026-07-14

- 重新复核近两周 10 个远程分支。当前 `origin/main` 已到 `45b34439`；只有旧 main 圈子修复 `548ea28d` 已包含在当前主线，其余分支 tip 均未进入主线且明显落后。
- `generic-final-weight-presentation` 后端 handler 编译失败（引用包装纠错 service 类型但分支未提供）；包装 OCR/纠错分支新增测试失败且缺 `/api/packaged-food/corrections` 路由注册；exercise/new-user、packaged workflow/OCR 两组分支分别重复同一 HEAD。
- 额外逻辑风险：任意 JWT 请求在结果产生前就触发邀请“有效使用”；4/4/9 公式可能覆盖合法特殊食品热量；健康分支对未记录补剂的营养缺口强制抬分，并用单次体重差直接影响健康分。
- 本轮仅做 fetch、静态审查和沿用未变化 commit HEAD 的隔离测试证据；未修改、合并或关闭任何业务分支/PR。
- 用户提供临时万界 key 并明确授权忽略算力成本，对标准食物库图片全量并行质检。基线为 13,592 条有效食物、11,552 条已有图、2,040 条缺图；无重复图片 key 批量复用。已让图片回填 CLI 支持万界 Gemini 原生视觉协议、已有图只读审计、并发/断点/重试和错配后替代图搜索，并修复缺图批处理 `checkpoint-every` 使用单条 attempts 导致无法按全局进度保存 state 的 bug。
- 200 条并发基准约 114 秒；首批高频扫描发现熟鸡胸肉用了生肉图、土豆烧鸡配虎皮蛋缺少虎皮蛋、抹茶奶油方块面包更像抹茶吐司等候选。缺图已为熟鸡腿肉、红薯找到 0.95 置信无水印候选，ON 巧克力乳清候选均有促销文字/水印未通过。
- 03:12 起在 D 盘临时二进制启动全量后台 dry-run：已有图 24 workers、缺图 24 workers；结果写 `backend/tmp/food-image-audit-20260714/`，不更新数据库/COS。完成后需对 mismatch/uncertain/failed 二次复核并生成安全写入清单。

- 营养库问题全量处理：生产百奇当前已正确显示 `504 kcal/100g`、25g=126kcal；茶叶蛋线上已变为55g/个，但仍被代表历史记录覆盖为约108kcal/个。本地已修复整蛋份量、exact canonical 优先、聚合宏量优先/代表记录只补微量、物理异常高频过滤和包装份量写入门禁。
- 事务修正4条包装 serving 超净重数据并停用1条每100g宏量总和110.65g的“迷你麻薯”；两类 postcheck 均为0，备份位于对应 `tmp/*-applied-20260714/` 目录。
- DeepSeek V4 Pro 以48路并发逐条审完13,641条有效营养记录，最终 `keep=13336/review=253/fix=48/deactivate=4`，ID/字段完整性校验通过、API失败0。模型动作存在明显误判，未自动写库。硬修复后有效条目13,640、规则候选366、severity>=5为48、severity>=8为0。
- Go 测试、vet、Python py_compile 与 diff-check 通过；C盘无可用空间，本次通过把测试进程临时目录定向D盘完成。未提交、推送或部署；部署后需确认茶叶蛋约90kcal/55g。
- 04:10 状态检查：两个 24-worker 进程均存活；已有图报告 6,487 条，其中 match 4,941、mismatch 665、uncertain 431、vision_failed 450，错配已有替代候选 421 条；缺图完成 1,910/2,040，其中合格候选 1,169 条。stderr 无 panic/fatal，预计缺图数分钟内完成、已有图约还需 45–55 分钟。
- 04:17 缺图全量第一轮完成：2,040 条中 1,202 条找到合格 dry-run 候选，770 no_match、56 vision_failed、12 download_failed；未上传 COS、未写数据库。同期已有图报告 7,112/11,552，仍由 24 workers 继续扫描，预计剩余约 40–50 分钟。
- 已有图第一轮随后完成 11,552/11,552：8,348 match、1,400 mismatch、890 uncertain、914 vision_failed，错配已有替代候选 874 条。已用同一只读报告启动第二轮 24-worker 补扫，仅重试 1,804 条 uncertain/vision_failed；仍不写数据库/COS。
- 第二轮补扫完成后：8,704 match、1,580 mismatch、610 uncertain、658 vision_failed，错配已有替代候选 1,001 条；未解决项由 1,804 降至 1,268。随后以 12 workers 启动第三轮低并发补扫，降低并发截断/网络失败。

- 用户指出前序百奇25g结论仍不可信。回查生产原记录和原始图确认：图上只有“净含量50g”，没有25g/两份/营养表；25g是无来源历史值。已撤销“只要不超过整包就有效”的规则。
- 新增包装份量证据审计和运行时门禁：全扫313条有效包装食品，修复后202条整包有证据、41条小份量有证据、28条小份量缺证据、42条缺份量。较小份量必须由每份口径、OCR、规格拆分或单位字段印证，否则写库/搜索回退整包。
- 原图视觉审计第一模型313/313成功，第二模型283/313成功；共同完成项207一致匹配、30一致错配、27一致不确定，其余分歧。模型结论没有自动写库。
- 人工查看原图后事务修复7条：百奇25→50g；纳宝帝30→10g并补 `200克（10克×20）`；5条明确错图/规格错配设为needs_review隔离。备份 `tmp/packaged-evidence-reviewed-applied-20260714/`，postcheck通过。
- 生产 API 实测：百奇504kcal/100g、50g整包252kcal；纳宝帝10g/50kcal；已隔离曼妥思记录不再返回。证据门禁代码的domain/service/utility定向测试和vet通过，仍待后端部署后覆盖未来写入及其它历史无证据份量。
- 完整HTML审计报告位于 `tmp/packaged-serving-quality-20260714/report.html`，同目录保留CSV/JSON和双模型结果。artifact校验/自包含生成通过；增强读者验证进入fallback，语义回退页面经截图检查可读。
- 用户决定近期分支只集成账户注销与记录图片去重，其余分支暂不处理。已在独立工作树复核并清理：记录图片分支夹带的包装 OCR 确认/纠错整套改动被剔除，仅保留手动记录不上传来源封面、后端真实照片优先过滤来源封面的逻辑。
- 账户注销二次修正：非法/缺失 JSON 返回 400、错误确认词不进入 service、修正结构化日志 action、补全 `manual_food_library` 无 user_id 的级联删除回归测试；个人设置页删除中显示 spinner。
- 验证通过：账户 handler 全包，账户 repo 级联删除、图片过滤纯逻辑与数据库集成定向测试，四个相关 Go 包编译检查；前端 scoped ESLint；Jest 14 suites / 54 tests。全量 Go 回归在 3 分钟上限超时但无失败输出；全项目 lint/typecheck 只有既有的另页 lint 与 day-record 类型错误。
- WeApp 自动化端口 `3001/9420/9421` 均未监听，按规则未擅自启动常驻进程，因此本轮没有 UI 截图/点击证据。
- 已推送提交 `330e07bb`（ma-yuqin0603）、`674e9f4a`（peng668-pixel，清理后记录图片改动）、`a447a81f`（二次修正）；远端 `dev` 与 `main` 均已同步到 `a447a81f`。

- 为 2026-07-15 助理录入清华紫荆园/桃李园和 AI远景中心食堂照片，实现独立食堂采集层及 Admin“食堂采集”工作台。批次/条目模型覆盖校园与园区、楼层/窗口、早中晚餐次、固定份/自选/组合/称重/麻辣烫、固定/区间/按重/组合/原文/未知价格、菜品/菜单牌/窗口/价签等图片证据，并允许缺名称、缺图、缺价格后续补全。
- 后端加入管理员图片上传、批次幂等创建、批次/条目查询；图片存 COS key、返回 CDN，限制 20MB 且校验真实 MIME。Admin 支持批量一图一条、同条多图、无图条目、3 并发和失败保留已上传结果。指南为 `docs/campus-food-collection-guide.md`。
- 定向 Go 测试/vet、迁移包编译、Admin production build 和 diff-check 通过；未改小程序，无 weapp UI 变更。因当前配置指向远程 PostgreSQL 且本轮未获明确迁移授权，未运行迁移、未部署后端/Admin；上线前必须先确认目标库并执行迁移。

- 用户随后明确授权迁移远程数据库、自己负责后端部署。已从 `backend/` 成功运行 `go run ./cmd/migration -config-dir . -timeout 5m`，迁移完整退出且无错误。只读复核确认 `campus_food_collection_batches` 为23列、`campus_food_catalog_items` 为40列；批次客户端幂等唯一索引及地点/窗口/状态/时间相关索引均存在。临时核验程序已删除，未部署或重启任何服务。

- 用户继续反馈线上茶叶蛋显示108kcal/1个但不显示克重、茶叶蛋蛋白显示65.83g，且按个数/按克重交互混乱。生产API确认茶叶蛋内部实际是55g，只是小程序主动隐藏；茶叶蛋蛋白是历史AVG重量直接暴露。common/protein各抽前60条分别有46/43条小数克重，属于广类问题。
- 已统一手动食物份量：后端常用食物AVG默认量取整但保持原营养密度；蛋类分别按整蛋55g、蛋白33g、蛋黄17g、鹌鹑蛋10g、鸽子蛋15g、鸭蛋65g、鹅蛋180g归一化；前端所有按个/按份结果与已选卡片同时显示等价克重，并移除piece换算硬编码55g。
- 后端utility全包、Jest 16 suites/60 tests、目标ESLint/go vet/diff-check通过；全量tsc仅有既有day-record空值类型错误。weapp-devtools 3001/9420/9421均未监听，无截图/点击验证。没有再写生产库、没有部署/发布：茶叶蛋热量修复需后端部署，单位显示需小程序重新发布；无数据库迁移。

- 标准食物图片全量任务最终完成。已有图三轮覆盖11,552条，最终8,835 match、1,658 mismatch、521 uncertain、538 vision_failed；缺图2,040条中1,202条找到首轮候选。高置信候选1,366条，排除90条跨食物复用URL后，对1,276条做独立只读复核，得到677条固定白名单。
- 用户明确授权对相对确定项写数据库/COS。白名单正式写入前再次复核，最终621条上传COS并条件更新数据库：缺图补齐517、已有错图替换104；0 upload_failed、0 db_update_failed，旧COS对象未删除。缺图队列2,040→1,523。
- Postcheck：621/621数据库新key一致；CDN首轮24并发有6条瞬时EOF，8并发四次重试后621/621可访问。完整报告 `backend/tmp/food-image-audit-20260714/comprehensive-report.md`，回滚清单 `reviewed-apply-report.json`。剩余不确定/失败/无替代项未写入；网络图片许可证未逐张独立核验。
# 2026-07-14（分支同步提交）

- 用户要求提交当前代码并保持 `main`、`dev` 一致。核验时本地两分支共同在 `fa63b561`，共同落后已一致的 `origin/main` / `origin/dev` 3 个提交；将快进、提交并推送同一最终 SHA。未跟踪 `wq!` 为终端输出残留，不提交。
- 已按用户要求改写为单一详细提交，完整列出食堂采集、营养库治理、标准食物图片审计补图、小程序与服务韧性、迁移、测试和文档；本地及远端 `main`、`dev` 已同步为同一提交。工作区持续明确排除未跟踪终端残留 `wq!`。
## 食物收藏隐私开关

- 实现了 `public_favorite_recipes`：默认公开，关闭后仅隐藏他人对“拍照分析后收藏餐食”（`user_recipes.is_favorite=true`）的访问，本人不受影响。
- 前端隐藏他人主页的食物收藏 Tab 并跳过请求；后端对列表和详情均进行可见性校验。迁移代码已落地但未执行远程数据库迁移。
- 已通过 recipe service 测试和关联包编译；DevTools 自动化 3001 未开启，待运行时验证。按用户要求暂不提交。

## 历史昵称重复与远程迁移

- 远程 `food-link` 库检测到 80 个 `LoadTestUser` 重复昵称。用户说明它们可能是真实账号，决定不删除、不自动改名。
- `ensureNicknameUniqueIndex` 现输出警告并暂缓唯一索引；服务层仍拦截常规重复昵称。2026-07-14 14:49，`go run ./cmd/migration -config-dir .` 已成功完成，未改动历史账号、昵称、收藏或记录。

## 分析结果收藏去重

- 修复分析结果页重复收藏：前端保存后即时显示“已收藏”并在请求中锁定；后端将以 `(user_id, source_task_id)` 查询/复用已有 `user_recipes`，迁移会增加 `source_task_id` 与部分唯一索引。
- 不自动处理已有重复收藏。关联 Go 测试通过；用户已确认并于 2026-07-14 16:16 成功执行远程数据库迁移，未修改或删除既有收藏数据；仍待 DevTools 3001 自动化验证。

# 2026-07-14（AI 营养 4/4/9 闭合修复）

- 用户确认后续 AI 临时生成营养必须满足 calories = protein*4 + carbs*4 + fat*9，必要时可从 Qwen 切换 Gemini。后端已在 AI 输出、营养库读写/缩放和饮食记录新增/编辑三层强制校准；Qwen 失败或无可用结果时依次转 Gemini 3.5、DeepSeek。
- 截图中的汉堡 230/13/10/12 每100g 会变为 200/13/10/12，430g 总热量 989 会变为 860；标签及可信数据库来源不强制改写。
- domain/service/analyze 全包、repo 新增定向用例、app 编译、go vet、diff-check 通过。repo 全包保留两个既有无关失败。未部署、未写生产库，现有坏库行仅在部署后读取时运行时纠正；物理批量回写需用户另行授权。
