# 2026-07-14

- 万界方舟 Gemini 兼容性复测：`gemini-3-flash-preview` 与 `gemini-3.5-flash` 在 `/api/v1/chat/completions` 上使用 OpenAI `messages` 会出现 400/500，不是 key 或模型不可用。两模型在 `https://maas-openapi.wanjiedata.com/api/v1beta/models/{model}:generateContent` 使用 Gemini 原生 `contents`、`inlineData`、`generationConfig` 后，文字和 HTTPS 图片请求均返回 HTTP 200。
- 已修改 `backend/internal/analyze/service/llm_client.go`：仅当 base URL 为万界方舟且模型名前缀为 `gemini-` 时使用原生 Gemini endpoint；HTTPS 图片下载后以内联图片上传，限制 10MB。普通与精准模型名保持用户指定值不变。新增单测，`go test ./internal/analyze/service -run '^TestOfoxAIClient_' -count=1`、`go test ./internal/app -run '^$' -count=1` 通过；尚待后端部署。
- 已修改 `backend/internal/analyze/service/analyze_service.go`：`external.llm_provider=doubao` 不再覆盖明确写出的 `qwen3.6-flash`，从而保证快速模式使用 Qwen。快速模式定向测试与全量 `go test ./internal/analyze/service -count=1`（43 秒）均通过；尚待后端部署。
- 已提交并推送 `f1a3abfa feat: 完善 AI 路由与营养审核`；本地和远端 `main`、`dev` 已同步到该提交。根目录未跟踪的 `wq!` 是终端分支列表输出，未纳入提交。
- 线上精准模式牛肉炒饭失败已定位：任务 `911665f2-41f0-4a71-b40e-3f5675d11f1a` 走 `strict_separate`，隐藏规划阶段仍调用旧豆包，万界方舟返回 400/model-not-found。已在本地把精准规划/称重/复核统一为 `gemini-3.5-flash`，瞬时故障回退改为 `qwen3.6-flash`；包装标签 OCR 改为 Qwen 3.6，营养未命中补全改为 `deepseek-v4-pro`。
- 已增加任务错误双层清洗：worker 写入前清洗，TaskService 返回历史任务前再次清洗。相关 4 个后端包测试及 vet 全通过；代码提交 `039580db` 已推送并同步 `main`、`dev`，尚待用户部署。
