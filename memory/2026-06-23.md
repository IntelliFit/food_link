# 2026-06-23

## WeApp feedback submit button color
- User reported that the orange/beige "Submit feedback" button looked like a disabled/prohibited state.
- Updated `apps/wechat/src/packageExtra/pages/feedback/index.scss`: enabled submit uses brand green `#00bc7d -> #00a86b`; disabled submit uses neutral gray `#d0d5dd`.
- Verification: targeted `git diff --check` passed with existing LF/CRLF warning; single-file Sass parse passed.
- WeApp runtime verification was attempted but blocked because `mrc` is not available in PATH and no DevTools automation port was listening on 3001 or 9420. No `dev:weapp` / Taro watch process was running, and the agent did not start a user-managed long-running dev process.

## Pet chat tone softening
- User feedback: the pet chat response felt like it was scolding the user.
- Fixed in `backend/internal/health/service/stats_service.go`: pet chat prompt now requires warm, cooperative tone and forbids teasing, lecturing, blame, sarcasm, or judging the user.
- Added harsh-tone detection/retry for phrases like `你这坎儿`, `别一口气`, `这么猛`, `管不住嘴`.
- Verification passed: targeted `go test ./internal/health/service -run 'Test(BuildPetChatPromptRequiresGentleTone|StatsService_GeneratePetChatRetriesHarshTone|StatsService_GenerateInsightRetriesForbiddenIdentityClaim)$' -count=1` and targeted `git diff --check`.

## 好友黑名单功能
- 用户确认黑名单规则：拉黑后解除好友并取消双方待处理申请；双方不能私信/加好友/邀请码绕过；圈子、搜索、互动通知、点赞评论回复都互相过滤；旧私信记录保留但禁用输入；不通知对方。
- 后端实现：新增 `user_blocks` 迁移 DO 和 friend block API；把 friend service 作为 block checker 注入 message、community、search、user 模块；公开广场改为 optional JWT 以便已登录用户能过滤黑名单内容。
- 小程序实现：`apps/wechat/src/utils/api.ts` 增加黑名单 API；好友管理增加黑名单 tab；私信、个人主页、圈子动态更多菜单增加拉黑/解除入口与输入禁用状态。
- App 实现：`packages/core` / `packages/api-client` 增加黑名单类型和 API；好友管理、私信页、个人主页、圈子动态卡片均补齐黑名单入口和内容不可见/无法发送状态。
- 验证通过：`go build ./cmd/server`、`go build ./cmd/migration`、`go test ./internal/migration/... -count=1`、`go test ./internal/app ./internal/friend/service ./internal/message/service ./internal/user/service ./internal/search/service -run '^$' -count=1`、`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck`、`git diff --check`。
- WeApp DevTools 验证：自动化服务 9420 可连接，可读到 `packageExtra/pages/friends/index` 并切换到 `pages/community/index`；截图接口超时。
- 未执行数据库迁移：新增 `user_blocks` 表，需要确认目标库后从 `backend/` 执行 `go run ./cmd/migration -config-dir .`。
- 仍有旧验证阻塞：WeApp 全量 typecheck 被 day-record/feedback/food-library 旧错误阻塞；App 全量 typecheck 被 HomeScreen 旧错误阻塞；部分后端旧测试 mock 未同步新接口签名而编译失败，本轮未修改已有测试。

## 好友黑名单功能收尾补充
- 已补齐私信未读过滤：被拉黑双方的旧私信仍保留，但 unread badge 和总未读数不再统计对方未读。
- 已把圈子内容搜索/作者筛选、用户搜索的黑名单过滤下沉到 repo SQL 层，服务层继续保留 blockChecker 二次过滤。
- 最新验证通过：后端 server/migration build、migration/search/app/friend/message/user 轻量测试、core/api-client typecheck、git diff check。
- WeApp DevTools 9420 可导航验证好友页和圈子页，但截图超时；当前 DevTools dist 不是最新改动产物，完整 UI 复验需用户启动 dev:weapp/watch 或重新生成 dist。
- 新增 user_blocks 表尚未迁移；当前配置指向远程 PostgreSQL 154.8.205.78:5432/food-link，需要用户明确确认后再执行 cd backend && go run ./cmd/migration -config-dir .。

## 好友黑名单功能收尾二次审查
- 已补齐邀请码资料绕过口：邀请资料接口改为 optional JWT；已登录用户如果与邀请人存在任意方向黑名单关系，返回不可见/不存在语义。
- 小程序和 App API client 都改为请求邀请资料时有 token 就携带 token，保留未登录分享兼容。
- 复验通过：后端 server/migration build、migration/search/app/friend/message/user 轻量测试、packages/core 与 packages/api-client typecheck、git diff --check。
- 全量小程序/App typecheck 仍被旧错误阻塞；WeApp DevTools 9420 可导航到好友管理与圈子页，截图接口超时，当前 dist 未刷新到最新黑名单 tab。
- 新增 `user_blocks` 表尚未迁移；当前配置指向远程 PostgreSQL，需用户确认后执行 `cd backend && go run ./cmd/migration -config-dir .`。

## 2026-06-23 02:48:07 - 好友黑名单功能最终收口补充
- 已补齐关注关系侧门：拉黑时同步删除双方 follow 关系；关注/粉丝/关注统计接口按 viewer 过滤黑名单用户，互相拉黑时不能重新关注。
- 已补齐个人主页直连接口侧门：公共食物收藏、收藏菜谱在服务层按 viewer 与目标用户黑名单关系返回不可见；公共食物列表/详情/校园相关推荐/评论/点赞/收藏/评论写入也按黑名单过滤或拒绝。
- 小程序与 App 界面均已改：好友管理黑名单 tab、私信禁发、个人主页拉黑/解除、圈子动态更多菜单拉黑入口均已补齐。
- 验证通过：后端 server/migration build，migration/publicfood/recipe/app/follow/friend/message/user/search 轻量测试，publicfood 真实相关测试，packages/core 与 packages/api-client typecheck，git diff --check。
- WeApp DevTools 9420 可连接并可导航到好友管理与圈子页，但当前 dist 未刷新到最新源码，好友页未出现“黑名单”文本；截图接口仍 timeout。需用户刷新 dev:weapp/watch 或重新生成 dist 后再做完整 UI 截图复验。
- 未执行数据库迁移：新增 user_blocks 表仍需在确认当前 backend 配置指向的远程 PostgreSQL 目标后，执行 cd backend && go run ./cmd/migration -config-dir .。
