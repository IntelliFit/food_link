# 2026-06-23

## 邀请注册缓存与补偿绑定收紧
- 检查确认：普通微信一键注册路径 `handleWxLogin -> login(code, undefined, inviteCode) -> POST /api/login` 已支持携带邀请码；后端 `findOrCreateWechatUser` 仅在新建微信用户时调用 `bindInviteReferral`，写 `weapp_user.referred_by_user_id` 与 `user_invite_referrals`。
- 修复缓存风险：新增 `apps/wechat/src/utils/pending-friend-invite.ts`，`pending_friend_invite_code` 改为带 `createdAt` 的 JSON、2 小时有效；旧版纯字符串缓存升级后直接清理。
- 登录页不再无条件使用 storage 里的邀请码：只有 query/redirect 明确携带邀请，或 redirect 明确来自邀请页时，storage 才能作为兜底；普通打开登录页会清掉旧 storage 邀请码，不让它污染非邀请注册。
- 登录完成后的邀请处理无论成功/失败都会清理本次 pending 邀请码。
- 后端收紧 `/api/friend/invite/accept` 补偿绑定：只有创建时间 2 小时内的新账号才允许通过 accept 补写奖励邀请关系；老账号扫码/点邀请页只发好友申请，不写 `referred_by_user_id` / `user_invite_referrals`。
- 验证通过：后端 friend/auth 定向测试、`go build ./cmd/server ./cmd/migration`、前端改动文件 eslint、`git diff --check`。
- `npm --workspace apps/wechat run typecheck` 仍被既有旧错误阻塞：`day-record`、`feedback`、`food-library-share`、`food-library`。

## 体验版重新部署后测试账号邀请写入复查
- 用户重新部署线上并再次用测试账号扫码注册后，执行只读数据库核对。
- dev PostgreSQL 当前测试手机号 `13511679220` 最新用户：
  - `id=e091a7b3-2e5d-4abc-b309-808b539cda04`
  - `nickname=测试用户_2026062322`
  - `registration_invite_code=F5602548`
  - `referred_by_user_id` 仍为空
  - `create_time=2026-06-23 22:52:59+08`
  - `last_login_method=password`
- `user_invite_referrals` 中没有该用户作为 `invitee_user_id` 的记录。
- `friend_requests` 中也没有该用户与邀请人 `8826bc8d-81ad-40a4-bc42-6cc30506b8c3` 之间的申请记录。
- `8826bc8d` 仍解析到邀请人 `8826bc8d-81ad-40a4-bc42-6cc30506b8c3`，昵称 `锦恢`，手机号 `13511679218`。
- 结论：本次注册仍未写入邀请关系；并且未走到登录完成后的 `/api/friend/invite/accept` 兜底。下一步应重点确认体验版前端提交 `/api/app/register/password` 的 request body 是否实际带 `inviteCode`，以及线上后端是否包含最新注册绑定日志。

## 23:12 测试账号邀请写入复查
- 用户再次完成体验版扫码注册测试后，只读查询 dev PostgreSQL。
- 最新测试手机号 `13511679220` 账号：
  - `id=c7f17f93-c21a-4e6b-9fd1-559063fa0829`
  - `nickname=测试用户_2026062323`
  - `registration_invite_code=ACE35727`
  - `create_time=2026-06-23 23:12:45+08`
  - `last_login_method=password`
  - `referred_by_user_id` 仍为空
- `user_invite_referrals` 中没有该用户作为 `invitee_user_id` 的记录。
- `friend_requests` 中没有该用户与邀请人 `8826bc8d-81ad-40a4-bc42-6cc30506b8c3` 的申请记录。
- 最近 30 分钟内没有新的 `user_invite_referrals` 写入；`invite_code=8826bc8d` 的记录仍都是 2026-05 旧数据。
- 结论：这次仍未落邀请关系；若前端新增日志显示 `/api/app/register/password` 请求体已带 `inviteCode=8826bc8d`，则问题在后端部署版本或服务端绑定分支；若请求体日志没有 `inviteCode`，则问题仍在前端提交前。

## 23:19 测试账号邀请写入复查成功
- 用户再次完成体验版扫码注册测试后，只读查询 dev PostgreSQL。
- 最新测试手机号 `13511679220` 账号：
  - `id=b4bfac12-a5d5-4092-8537-d5ad6e6e9400`
  - `nickname=测试用户_2026062323`
  - `registration_invite_code=A10A90AF`
  - `create_time=2026-06-23 23:19:39+08`
  - `referred_by_user_id=8826bc8d-81ad-40a4-bc42-6cc30506b8c3`
- `user_invite_referrals` 已写入：
  - `id=af5cac98-4f70-4010-8451-1b08aa21f1f9`
  - `inviter_user_id=8826bc8d-81ad-40a4-bc42-6cc30506b8c3`
  - `invitee_user_id=b4bfac12-a5d5-4092-8537-d5ad6e6e9400`
  - `invite_code=8826BC8D`
  - `status=pending_qualified`
  - `reward_start_date=2026-06-23`
  - `reward_end_date=2026-06-30`
- `friend_requests` 也已创建：
  - `from_user_id=b4bfac12-a5d5-4092-8537-d5ad6e6e9400`
  - `to_user_id=8826bc8d-81ad-40a4-bc42-6cc30506b8c3`
  - `status=pending`
- 结论：邀请绑定链路已经跑通；注册后立即 accept 的前端修复生效。

## 邀请注册截图分析与新增断点日志
- 用户提供两张测试截图：
  - 注册前登录页日志显示 `inviteCodeFromQuery=8826bc8d`、`inviteCodeFromRedirect=8826bc8d`、`inviteCodeFromStorage=8826bc8d`、`final inviteCode=8826bc8d`，所以扫码/邀请页/登录页参数链路没有丢邀请码。
  - 注册成功后进入 `packageExtra/pages/health-profile/index`，说明当前账号还未完成 onboarding；这会让登录完成后的 `finishLoginFlow -> /api/friend/invite/accept` 兜底延后，不能作为主绑定依据。
- 已新增前端 API 提交断点：`apps/wechat/src/utils/api.ts#registerWithPassword` 在请求 `/api/app/register/password` 前打印 `[invite-debug][api] registerWithPassword 请求体邀请码状态`，包含 `hasInviteCode`、`inviteCode`、`requestKeys`、手机号后四位，不打印密码。
- 已新增后端服务入参断点：`backend/internal/auth/service/login_service.go#RegisterWithPassword` 打印 `App 账号密码测试注册请求已收到`，包含脱敏手机号、`invite_code_present`、`invite_code_length`；已有账号按密码登录分支也记录是否带邀请码。后端不新增邀请码明文日志。
- 验证通过：auth service 定向测试、后端 server/migration build、前端改动文件 eslint、`git diff --check`。微信端全量 typecheck 仍被既有旧错误阻塞。

## 邀请注册断点日志前端化
- 用户要求尽量把断点日志打在前端，因为部署后后端日志查看不方便。
- 已在前端 API 层补齐：
  - `login` 打印 `/api/login` 请求是否带邀请码、响应状态、响应用户摘要。
  - `registerWithPassword` 打印 `/api/app/register/password` 请求是否带邀请码、响应状态、响应用户摘要。
  - `acceptFriendInvite` 打印 `/api/friend/invite/accept` 请求前 token/邀请码状态和响应状态。
- 已在登录页业务流补齐：
  - 测试手机号注册返回后打印用户摘要。
  - 微信一键登录提交前/返回后打印邀请码状态。
  - `handleLoginSuccess` 打印用户资料、onboarding、头像昵称/手机号 gate。
  - `continueAfterAuthGates` 打印是否因为健康档案未完成导致邀请码处理延后。
  - `finishLoginFlow` 打印是否调用 accept、accept 成功/失败和清理 pending 邀请码。
  - 完善头像昵称保存后打印是否继续邀请流程。
- 下一次体验版只看小程序控制台即可定位：登录页变量是否有邀请码 -> API 请求体是否带邀请码 -> 注册响应是否成功 -> 是否被健康档案/手机号/头像昵称 gate 延后 -> accept 是否执行。
- 验证通过：前端相关文件 eslint、auth service 定向测试、后端 server/migration build、`git diff --check`；微信端全量 typecheck 仍被既有旧错误阻塞。

## 邀请 accept 不再等待健康档案完成
- 用户 23:12/23:13 前端日志显示：测试手机号注册接口已 200，返回 `userId=c7f17f93-c21a-4e6b-9fd1-559063fa0829`；登录页仍有 `inviteCode=8826bc8d`；随后 `continueAfterAuthGates` 因 `onboardingCompleted=false` 直接跳转健康档案。
- 结论：之前 `/api/friend/invite/accept` 只在 `finishLoginFlow` 中调用，而健康档案 gate 会让 `finishLoginFlow` 不执行，因此 accept 兜底没有机会触发。
- 已修改登录页：注册/微信登录 API 返回并保存 token 后，立即调用 `acceptInviteAfterAuthenticated`；后续健康档案、头像昵称、手机号绑定 gate 不再阻断邀请关系落库尝试。
- `acceptInviteAfterAuthenticated` 使用 `inviteAcceptHandledRef` 避免同一邀请码后续在 `finishLoginFlow` 重复调用。
- 验证通过：前端相关文件 eslint、friend/auth 定向测试、后端 server/migration build、`git diff --check`；微信端全量 typecheck 仍被既有旧错误阻塞。

## Friend blacklist migration and dev deployment
- User confirmed running the remote PostgreSQL migration. Executed `cd backend && go run ./cmd/migration -config-dir .`; migration completed successfully against Apollo local config, and read-only SQL confirmed `public.user_blocks` exists.
- Rebased the blacklist work on latest `origin/dev`; resolved conflicts by keeping the newer community edit flow and search `items` field while reapplying blacklist filtering. Re-verified `go build ./cmd/server`, `go build ./cmd/migration`, migration tests, selected backend compile tests, `packages/core` typecheck, `packages/api-client` typecheck, and `git diff --check`.
- Pushed dev commit `2886c4b` (`feat: add user blacklist`) to `origin/dev`.
- Ran `npm run push-docker-ccr`; pushed `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev` with manifest digest `sha256:05431841af5dd52adb02867e4489741cbcd4b27f87896219601e122323154417`.
- Verified public dev backend: `/api/health` returned 200; `/api/friend/blocks` returned 401 without auth on all three observed pods `foodlink-dev-578bc5d45f-*`, proving the new blacklist route is live.
- Mini-program and App UI code is pushed, but user-facing rollout still requires the normal mini-program upload/release and App release paths. No `npm run build:weapp` was run by the agent.

## WeApp feedback submit button color
- User reported that the orange/beige "Submit feedback" button looked like a disabled/prohibited state.
- Updated `apps/wechat/src/packageExtra/pages/feedback/index.scss`: enabled submit uses brand green `#00bc7d -> #00a86b`; disabled submit uses neutral gray `#d0d5dd`.
- Verification: targeted `git diff --check` passed with existing LF/CRLF warning; single-file Sass parse passed.
- WeApp runtime verification was attempted but blocked because `mrc` is not available in PATH and no DevTools automation port was listening on 3001 or 9420. No `dev:weapp` / Taro watch process was running, and the agent did not start a user-managed long-running dev process.

## Pet chat tone softening
- User feedback: the pet chat response felt like it was scolding the user.
- Fixed in `backend/internal/health/service/stats_service.go`: pet chat prompt now requires warm, cooperative tone and forbids teasing, lecturing, blame, sarcasm, or judging the user.
- Added harsh-tone detection/retry for phrases like `你这坎儿`, `别一口气`, `这么猛`, `管不住嘴`.
- Verification passed: targeted `go test ./internal/health/service -run 'Test(BuildPetChatPromptRequiresGentleTone|StatsService_GeneratePetChatRetriesHarshTone|StatsService_GenerateInsightRetriesForbiddenIdentityClaim)$' -count=1` and targeted `git diff --check`.

## 好友黑名单功能
- 用户确认黑名单规则：拉黑后解除好友并取消双方待处理申请；双方不能私信/加好友/邀请码绕过；圈子、搜索、互动通知、点赞评论回复都互相过滤；旧私信记录保留但禁用输入；不通知对方。
- 后端实现：新增 `user_blocks` 迁移 DO 和 friend block API；把 friend service 作为 block checker 注入 message、community、search、user 模块；公开广场改为 optional JWT 以便已登录用户能过滤黑名单内容。
- 小程序实现：`apps/wechat/src/utils/api.ts` 增加黑名单 API；好友管理增加黑名单 tab；私信、个人主页、圈子动态更多菜单增加拉黑/解除入口与输入禁用状态。
- App 实现：`packages/core` / `packages/api-client` 增加黑名单类型和 API；好友管理、私信页、个人主页、圈子动态卡片均补齐黑名单入口和内容不可见/无法发送状态。
- 验证通过：`go build ./cmd/server`、`go build ./cmd/migration`、`go test ./internal/migration/... -count=1`、`go test ./internal/app ./internal/friend/service ./internal/message/service ./internal/user/service ./internal/search/service -run '^$' -count=1`、`npm --workspace packages/core run typecheck`、`npm --workspace packages/api-client run typecheck`、`git diff --check`。
- WeApp DevTools 验证：自动化服务 9420 可连接，可读到 `packageExtra/pages/friends/index` 并切换到 `pages/community/index`；截图接口超时。
- 未执行数据库迁移：新增 `user_blocks` 表，需要确认目标库后从 `backend/` 执行 `go run ./cmd/migration -config-dir .`。
- 仍有旧验证阻塞：WeApp 全量 typecheck 被 day-record/feedback/food-library 旧错误阻塞；App 全量 typecheck 被 HomeScreen 旧错误阻塞；部分后端旧测试 mock 未同步新接口签名而编译失败，本轮未修改已有测试。

## 好友黑名单功能收尾补充
- 已补齐私信未读过滤：被拉黑双方的旧私信仍保留，但 unread badge 和总未读数不再统计对方未读。
- 已把圈子内容搜索/作者筛选、用户搜索的黑名单过滤下沉到 repo SQL 层，服务层继续保留 blockChecker 二次过滤。
- 最新验证通过：后端 server/migration build、migration/search/app/friend/message/user 轻量测试、core/api-client typecheck、git diff check。
- WeApp DevTools 9420 可导航验证好友页和圈子页，但截图超时；当前 DevTools dist 不是最新改动产物，完整 UI 复验需用户启动 dev:weapp/watch 或重新生成 dist。
- 新增 user_blocks 表尚未迁移；当前配置指向远程 PostgreSQL 154.8.205.78:5432/food-link，需要用户明确确认后再执行 cd backend && go run ./cmd/migration -config-dir .。

## 好友黑名单功能收尾二次审查
- 已补齐邀请码资料绕过口：邀请资料接口改为 optional JWT；已登录用户如果与邀请人存在任意方向黑名单关系，返回不可见/不存在语义。
- 小程序和 App API client 都改为请求邀请资料时有 token 就携带 token，保留未登录分享兼容。
- 复验通过：后端 server/migration build、migration/search/app/friend/message/user 轻量测试、packages/core 与 packages/api-client typecheck、git diff --check。
- 全量小程序/App typecheck 仍被旧错误阻塞；WeApp DevTools 9420 可导航到好友管理与圈子页，截图接口超时，当前 dist 未刷新到最新黑名单 tab。
- 新增 `user_blocks` 表尚未迁移；当前配置指向远程 PostgreSQL，需用户确认后执行 `cd backend && go run ./cmd/migration -config-dir .`。

## 2026-06-23 02:48:07 - 好友黑名单功能最终收口补充
- 已补齐关注关系侧门：拉黑时同步删除双方 follow 关系；关注/粉丝/关注统计接口按 viewer 过滤黑名单用户，互相拉黑时不能重新关注。
- 已补齐个人主页直连接口侧门：公共食物收藏、收藏菜谱在服务层按 viewer 与目标用户黑名单关系返回不可见；公共食物列表/详情/校园相关推荐/评论/点赞/收藏/评论写入也按黑名单过滤或拒绝。
- 小程序与 App 界面均已改：好友管理黑名单 tab、私信禁发、个人主页拉黑/解除、圈子动态更多菜单拉黑入口均已补齐。
- 验证通过：后端 server/migration build，migration/publicfood/recipe/app/follow/friend/message/user/search 轻量测试，publicfood 真实相关测试，packages/core 与 packages/api-client typecheck，git diff --check。
- WeApp DevTools 9420 可连接并可导航到好友管理与圈子页，但当前 dist 未刷新到最新源码，好友页未出现“黑名单”文本；截图接口仍 timeout。需用户刷新 dev:weapp/watch 或重新生成 dist 后再做完整 UI 截图复验。
- 未执行数据库迁移：新增 user_blocks 表仍需在确认当前 backend 配置指向的远程 PostgreSQL 目标后，执行 cd backend && go run ./cmd/migration -config-dir .。

## 2026-06-23 餐食卡展示语义修复
- 按用户要求先同步到 `dev`：切分支前用 stash 暂存了本地状态，切到 `dev` 并 `git pull --ff-only` 后恢复；仅在 `memory/2026-06-21.md` 上发生 stash 冲突，并已手动消解。
- 修复了三处小程序展示语义：圈子页、搜索结果页、个人主页动态。共享 helper 现在以 `entry_type` 为主判定是否显示逐食材卡，不再把 AI 图片/文本分析记录因为 items 带 `manual_source` 就误判成手动食材卡。
- 当前规则：
  - `food_library` / `public_food_library` 显示逐食材卡。
  - `food_image` / `food_text` / `analyze_history` / `favorite_recipe` 保持餐次主图。
  - 只有缺失 `entry_type` 的旧数据才允许回退 `手动记录：` + `manual_source` 兼容逻辑。
- `ManualFoodCards` 已恢复每个食材一张卡，并缩小了缩略图、gap、padding、文字层级；AI 主图尺寸未改。
- 搜索后端已补回 `entry_type`、`source_task_id`、`recipe_id`、`items`，并复用 `foodmedia.EnrichFoodRecordDisplayFields(...)`，确保搜索结果中的 record 图 / item 图语义与圈子 feed 一致。
- 新增/更新验证：
  - 微信端 Jest：`manual-food-source.test.ts`、`search-results.test.tsx` 通过。
  - 后端：`go test ./internal/search/service -count=1` 通过。
  - 微信端全量 `typecheck` 仍被仓库既有旧错误阻塞，报错不在本次修改文件内。
- WeApp DevTools 验证已尝试：
  - 初始 `9420` 无自动化连接。
  - 使用开发者工具 CLI 打开 `/Users/kirigaya/project/food_link/apps/wechat/dist` 时，`preparing` 阶段报 `需要重新登录 (code 10)`。
  - 因此本轮未能完成页面截图和点击导航复验；需要用户先在微信开发者工具中重新登录并重新开启自动化。

## 2026-06-23 运动长文本卡片展示
- 已为长文本运动记录补齐 `exercise_items` 数据链路：新增 `user_exercise_logs.exercise_items jsonb` 迁移 DO 与幂等迁移语句，`ProcessExerciseTask` 仅从 `mode=long_text_library_met` 的 `items` 中持久化分项运动，短文本和图片识别不误卡片化。
- 社区 feed、互动详情和搜索接口均透出 `exercise_items`；搜索接口同时修复 `circle_post` UNION 字段错位风险，并让 `exercise_log.duration_min` 返回真实列。
- 小程序新增 `ExerciseActivityCards`，使用 `icon-zengji` 肌肉图标占位；圈子页、个人主页动态、搜索结果页、互动详情页在 `exercise_items` 有有效项目时显示逐运动项目卡片，否则保持原文字/图片展示。
- 验证通过：
  - `npm --workspace apps/wechat run test -- --runInBand tests/unit/search-results.test.tsx`
  - `cd backend && go test ./internal/search/repo ./internal/search/service -count=1`
  - `cd backend && go test ./internal/search/service ./internal/health/service ./internal/health/repo -run 'Exercise|exercise|SearchContent' -count=1`
  - `git diff --check`
- 已知验证阻塞：
  - 小程序 `typecheck` 仍被既有旧错误阻塞：`day-record`、`feedback`、`food-library-share`、`food-library`。
  - 后端 `health/service` 全包仍被既有 `TestStatsInsightUsesDeepSeekV4FlashModel` 模型名期望不一致阻塞。
  - 后端 `community/repo` 全包仍被既有 `notification_repo_test` mock 参数签名不匹配阻塞。
  - WeApp DevTools 自动化仍被 `需要重新登录 (code 10)` 阻塞，未取得截图/点击证据。
- 未执行数据库迁移；落库前需确认目标库后在 `backend/` 执行 `go run ./cmd/migration -config-dir .`。

## 2026-06-23 用户工作偏好更新
- 用户明确要求：代理后续只负责“改代码”和“进行类型校验”；具体测试验证由用户自己完成。
- 后续不要主动启动开发服务器、测试服务器或 weapp-devtools/微信开发者工具自动化；除非用户在当前对话明确要求。
- 本轮已停止刚才挂住的 `mrc where` / `mrc switchTab` 命令。此前已尝试过微信开发者工具 CLI 自动化，后续不再主动做这类运行时验证。

## 2026-06-23 动态详情评论空态样式
- 用户要求去掉动态详情页「还没有评论，来抢沙发」空态的背景颜色和下方 border。
- 已修改 `apps/wechat/src/packageExtra/pages/interaction-feed-detail/index.scss`：详情页 `.feed-empty` / `.feed-comments` 保持透明背景，并去除 `border-bottom` 与 `box-shadow`。
- 用户随后在控制台看到 `.fl-d .feed-empty` 仍有 `background` 且带 `!important`；根因是全局深色主题 `apps/wechat/src/styles/fl-color-scheme-dark.scss` 的 `.fl-d .feed-empty` 覆盖了详情页局部样式。
- 已补强：详情页局部覆盖加 `!important`；深色主题中新增 `.fl-d .interaction-feed-detail-page .feed-empty/.feed-comments` 精确透明覆盖，并去除 border/shadow。
- 验证：目标样式文件 `git diff --check` 通过；小程序 typecheck 仍被既有旧错误阻塞。未启动服务器或运行 weapp-devtools。

## 2026-06-23 我的页主题按钮迁移与用户 ID 省略
- 用户要求顶部用户 ID 不换行，超出省略；把“我的”页顶部的主题切换按钮移动到个人主页右上动作区，位于「编辑资料」右侧、「分享」左侧。
- 已移除 `apps/wechat/src/pages/profile/index.tsx` 中用户卡内的主题切换按钮。
- 已在 `apps/wechat/src/packageExtra/pages/profile-settings/index.tsx` 接入 `toggleScheme`，owner 个人主页顶部动作组新增主题切换按钮，顺序为编辑资料、主题切换、分享。
- 已为 `apps/wechat/src/pages/profile/index.scss` 的 ID chip 和 `apps/wechat/src/packageExtra/pages/profile-settings/index.scss` 的顶部 ID 行补单行省略样式。
- 验证：目标文件 `git diff --check` 通过；小程序 typecheck 仍被既有旧错误阻塞。未启动服务器或运行 weapp-devtools。

## 2026-06-23 登录页测试注册 public-config 404
- 用户反馈登录页长按图标 3 秒后用于测试好友邀请的快捷注册入口依赖 `http://127.0.0.1:3010/api/app/public-config`，但当前该接口返回 404。
- 修复：`backend/internal/app/app.go` 新增 `registerPublicConfigRoutes`，同时注册 `/api/app/public-config` 和兼容别名 `/api/public-config`，均返回 `system.PublicConfig` 中的 `allow_debug_register`。
- 防回归：`backend/internal/app/app_test.go` 新增路由测试，覆盖两个 public-config 路径都返回 200 并透传配置值。
- 前端：`apps/wechat/src/utils/api.ts#getPublicConfig` 仍优先请求正式路径；仅 404 时回退兼容路径，其它错误继续抛出。
- 验证：后端定向测试和 app 包 compile 测试通过；目标文件 `git diff --check` 通过；小程序 typecheck 仍被既有旧错误阻塞。
- 注意：当前 3010 若仍是旧后端进程，需用户按自己的流程重启后端后新路由才会生效；本轮未启动或重启服务器。

## 2026-06-23 dev 提交推送与后端部署
- 已提交并推送代码：`4d6c211d fix: restore feed card semantics and sharing utilities` 到 `origin/dev`。
- 提交前验证：`git diff --check`、后端定向测试、`go build ./cmd/server ./cmd/migration` 通过；小程序 typecheck 仍被既有旧错误阻塞。
- 已执行 `npm run push-docker-ccr`，成功推送 `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev`。
- 推送镜像 digest：`sha256:207499c484376116f254475f840e4ebebd928e63eaad11c74be7f55fedb5c4fc`。
- 公开 dev 检查：`/api/health` 返回 200；`/api/app/public-config` 返回 200 且 `allow_debug_register:true`。
- 本机无 `kubectl`，无法直接查看 K8s rollout；按现有部署机制，镜像推送后部署端会自动拉取 `:dev`。
- 本轮未执行数据库迁移；`exercise_items` 新字段迁移远程库前仍需用户明确确认。

## 2026-06-23 个人主页 Taro 事件崩溃修复
- 用户反馈编辑资料/重新登录后进入“我的”页，昵称和 13 会员数据加载不出来，控制台报 `TypeError: Cannot read property '_num' of undefined`，栈在 `TaroElement.removeEventListener`。
- 排查：个人主页 `profile-settings` 中多处 owner-only 点击事件写成 `onClick={isOwner ? handleOpenEdit : undefined}`，Taro 小程序端在事件从函数更新为 `undefined` 时可能触发 `_num` 崩溃，进而中断页面渲染/后续数据加载。
- 修复：`apps/wechat/src/packageExtra/pages/profile-settings/index.tsx` 新增稳定 `noop`，将头像、昵称、ID 行、座右铭等 owner-only 点击改为始终传函数；非 owner 时函数为空操作，不再传 `undefined`。
- 验证：目标文件 `git diff --check` 通过；小程序 typecheck 仍被既有旧错误阻塞，未新增到本次文件。
- 继续修复“我的”页本体：登录态切换时 `.user-name` 文本节点原来会从“未登录有 onClick”变成“已登录无 onClick”，同样可能触发 Taro 移除事件监听崩溃；已在 `apps/wechat/src/pages/profile/index.tsx` 让用户名节点始终绑定稳定 `handleUserNameClick`，登录后 no-op，未登录时跳登录。用户反馈本地验证已恢复正常。

## 2026-06-23 圈子 feed exercise_items 缺列 500 兼容修复
- 用户贴出本地后端日志：`GET /api/community/feed` 返回 500，错误为 `ERROR: column "exercise_items" does not exist (SQLSTATE 42703)`。
- 根因：长文本运动卡片新增字段 `user_exercise_logs.exercise_items` 已在迁移代码中，但当前本地数据库未迁移；社区 feed 和搜索 SQL 直接引用新列，导致未迁移库整体报错。
- 已补兼容代码：
  - `backend/internal/community/repo/feed_repo.go` 在运动 feed 列表和动态详情查询中，按列是否存在动态选择 `COALESCE(exercise_items, '[]')` 或空数组。
  - `backend/internal/search/repo/search_repo.go` 在搜索运动记录分支同样动态选择 `exercise_items`。
  - `backend/internal/health/repo/exercise_repo.go` 创建运动记录时，列不存在则 `Omit("ExerciseItems")`，避免写入缺列失败。
- 行为：未迁移库上长文本运动分项卡片不会显示分项，但圈子/搜索/详情不应再因缺列 500；跑迁移后会自动读取真实分项。
- 验证：`go build ./cmd/server ./cmd/migration` 通过；`search/service` 与 `health/repo` 定向测试通过；`community/repo` 包仍被既有 `notification_repo_test.go` 签名旧错误阻塞；小程序 typecheck 仍被既有旧错误阻塞。

## 2026-06-23 体验版扫码邀请注册链路检查
- 固定测试手机号确认：`13511679220`。
- 体验版 API 运行环境确认：小程序 `trial` 使用 `https://dev.api.healthymax.cn`；`/api/health` 返回 200，`/api/app/public-config` 返回 `allow_debug_register:true`。
- 代码链路确认：
  - 分享/邀请二维码 scene 使用 `fi=<invite_code>`，页面为 `pages/index/index`，二维码 env_version 通过 `getWeappEnvVersion()` 跟随当前小程序环境。
  - `apps/wechat/src/app.ts` 会从扫码 `scene` 或 query 读取 `fi`，写入 `pending_friend_invite_code`，未登录时跳邀请页。
  - 登录页固定测试注册读取 query/storage 中的邀请码，并调用 `/api/app/register/password`，带上 `inviteCode`。
  - 后端注册固定手机号后调用 `bindInviteReferral`，会写 `weapp_user.referred_by_user_id` 和 `user_invite_referrals`，初始状态 `pending_qualified`。
- 只读数据库检查：当前 dev 库中 `telephone='13511679220'` 尚无 `weapp_user` 记录，也无对应 `user_invite_referrals` 记录；适合用于真实扫码注册测试。未主动调用注册接口，避免提前占用测试手机号。

## 2026-06-23 登录完善资料微信头像昵称交互调整
- 用户反馈注册/登录第一个环节不应有单独的“一键使用微信头像和昵称”按钮；应在用户点击头像或昵称时，直接弹微信原生资料授权对话框，确认后一次性复制微信头像和昵称。
- 已修改 `apps/wechat/src/packageExtra/pages/login/index.tsx`：手机号快捷登录不再提前调用 `Taro.getUserProfile`；新增 `handleUseWechatProfile`，点击资料区域后调用微信资料授权并回填昵称与头像。
- 已修改 `apps/wechat/src/components/NewUserOnboardingModals.tsx/.scss`：头像圆区和昵称行作为热区触发微信资料授权；移除原 `openType='chooseAvatar'` 透明按钮和 `type='nickname'` 输入框视觉，避免出现独立授权按钮。
- 验证：目标文件 `git diff --check` 通过；`npm --workspace apps/wechat run typecheck` 仍被既有旧错误阻塞，错误仍集中在 `day-record`、`feedback`、`food-library-share`、`food-library`。

## 2026-06-23 体验版测试账号邀请绑定复查
- 用户已用体验版二维码扫码注册固定测试手机号 `13511679220`。
- 只读查询 dev 库结果：
  - `weapp_user` 已有测试用户 `f4f856ae-95b1-44b7-a230-3d281f3b589c`，昵称 `测试用户_2026062319`，`registration_invite_code=1C87C128`。
  - 该用户 `referred_by_user_id` 为空。
  - `user_invite_referrals` 中没有该用户作为 `invitee_user_id` 的记录。
- 结论：这次注册成功了，但邀请绑定没有在注册阶段写入；不是奖励激活失败，而是邀请码没有成功到达/绑定到后端注册链路。
- 已补前端兜底，降低后续丢参概率：
  - `apps/wechat/src/app.ts` 扫码入口同时识别 scene/query 中的 `fi` 与 `invite_code`。
  - `apps/wechat/src/packageExtra/pages/invite-friends/index.tsx` 一拿到 `inviteCode` 就写入 `pending_friend_invite_code`。
  - `apps/wechat/src/packageExtra/pages/login/index.tsx` 除顶层 `invite_code/fi` 和 storage 外，也会从 `redirect` 参数中的 `invite_code/fi` 兜底解析。
- 已增加调试日志，方便用户无需反复创建账号即可定位邀请码是否丢失：
  - `[invite-debug][app]` 打印扫码 scene/query 解析结果以及写入 storage 的邀请码。
  - `[invite-debug][invite-page]` 打印邀请页 route 参数、资料接口返回的邀请码、写入 storage 的值以及跳登录时透传的邀请码。
  - `[invite-debug][login]` 打印登录页 query/redirect/storage/final 邀请码；测试手机号注册提交前也会打印最终 `finalInviteCode`。
- 注意：当前固定测试手机号已被注册占用；再次完整测试注册链路前，需要换一个测试号或经用户明确确认后清理/重置该测试用户数据。

## 2026-06-23 测试手机号注册接口改为注册或登录
- 用户反馈：固定测试手机号已注册后，再用之前密码进入测试注册流程时，不应提示“手机号已被使用”；应当无账号则用输入密码注册，有账号则校验密码并登录。
- 后端已修改 `backend/internal/auth/service/login_service.go`：`RegisterWithPassword` 在固定手机号已有账号时校验输入密码；密码正确则 `touchLogin` 并签发登录态，密码错误返回 `手机号或密码错误`。已有账号登录不会重新创建邀请关系，避免老用户重复绑定邀请奖励。
- 已补测试 `backend/internal/auth/service/login_service_test.go`：
  - `TestLoginService_RegisterWithPasswordExistingPhoneLogsIn`
  - `TestLoginService_RegisterWithPasswordExistingPhoneRejectsWrongPassword`
- 验证：
  - `cd backend && go test ./internal/auth/service -run 'TestLoginService_RegisterWithPasswordExistingPhone|TestLoginService_RegisterAndLoginWithPhonePassword|TestLoginService_RegisterAndLoginWithPhonePasswordOnly|TestLoginService_LoginWithPassword_InvalidPassword' -count=1` 通过。
  - `cd backend && go build ./cmd/server ./cmd/migration` 通过。
  - `npm --workspace apps/wechat run typecheck` 仍被既有旧错误阻塞。

## 2026-06-23 邀请接受接口补偿写入奖励邀请关系
- 用户提供扫码注册控制台截图，登录页显示 `inviteCodeFromQuery`、`inviteCodeFromRedirect`、`inviteCodeFromStorage` 和 `final inviteCode` 均为 `8826bc8d`。
- 只读数据库确认 `8826bc8d` 可解析到邀请人 `8826bc8d-81ad-40a4-bc42-6cc30506b8c3`，邀请关系表结构/默认值正常。
- 结论：截图说明邀请码已到达登录页；当前更大的潜在 bug 是奖励邀请关系只在注册瞬间绑定，而登录后点击邀请页按钮走的 `/api/friend/invite/accept` 只发好友申请，不会补写 `weapp_user.referred_by_user_id` 或 `user_invite_referrals`。一旦注册瞬间丢参或后端未绑定，后续点击邀请按钮也无法恢复奖励关系。
- 已修复：
  - `backend/internal/friend/service/friend_service.go`：`AcceptInvite` 在黑名单校验后、好友申请前调用补偿绑定；若被邀请人尚无 `referred_by_user_id`，则写入 `referred_by_user_id` 并创建 `user_invite_referrals`，已有绑定不覆盖。
  - `backend/internal/auth/repo/user_repo.go`：`CreateInviteReferralBinding` 显式写入 UUID，减少对 PostgreSQL `gen_random_uuid()` 默认值的依赖，也便于 SQLite 单测覆盖。
  - `backend/internal/friend/service/friend_service_test.go`：`TestFriendService_AcceptInvite` 覆盖接受邀请时补写 `referred_by_user_id` 与 `user_invite_referrals`。
- 验证：
  - `cd backend && go test ./internal/friend/service ./internal/auth/service -run 'TestFriendService_AcceptInvite|TestLoginService_RegisterWithPasswordExistingPhone|TestLoginService_RegisterAndLoginWithPhonePassword' -count=1` 通过。
  - `cd backend && go build ./cmd/server ./cmd/migration` 通过。

## 2026-06-23 会员积分页展示邀请奖励明细
- 用户要求：点进「食探会员」后，如果当前用户在邀请数据库里有对应数据，要显示每一条邀请记录、状态、还差什么动作，以及完成后能加多少积分，方便 debug。
- 后端实现：扩展 `GET /api/membership/invite-reward-status`，保留旧的 `as_invitee` / `as_inviter`，新增 `records`，返回当前用户作为 inviter 或 invitee 的全部 `user_invite_referrals` 明细；新增 `ListInviteReferralsForUser` repo 查询。
- 展示语义：pending 未行动显示还需 2 天；已有首个有效记录显示还需另一个自然日 1 天；过期 pending 仅在响应中显示已过期，不自动改库；completed 显示双方各 +15；blocked 映射 `qualification_window_expired` / `monthly_limit_reached`。
- 前端实现：`apps/wechat/src/utils/api.ts` 新增 `InviteRewardRecord` 类型；`apps/wechat/src/packageExtra/pages/pro-membership/index.tsx` 在套餐卡下方新增「邀请奖励明细」卡片组，显示角色、对方昵称、状态、下一步、奖励积分、剩余天数、短 ID 和邀请码；`index.scss` 补亮色/深色样式。
- 验证：`go test ./internal/membership/service -count=1`、`go build ./cmd/server ./cmd/migration`、目标前端 eslint、`git diff --check` 通过；`npm --workspace apps/wechat run typecheck` 仍被既有旧错误阻塞（day-record、feedback、food-library-share、food-library）。
- 未做：未启动开发服务器、未启动 weapp-devtools、未做运行时验证。

## 2026-06-23 邀请奖励明细迁移到赚积分页
- 用户修正：邀请奖励明细不要放在「食探会员 / 积分充值」页，应放到「赚积分」页，并分别设计邀请人和被邀请人视角。
- 后端实现：`GetRewardCenter` 新增 `invite_reward` 聚合对象，包含 `as_inviter_summary`、`as_invitee_summary`、`records`；`invite-reward-status` 独立接口继续保留。邀请人统计 `invited_count/completed_count/pending_count/estimated_credits/earned_credits`；被邀请人统计 `completed_days/required_days/remaining_days/reward_credits/deadline_text/next_action_text`。
- 前端实现：从 `pro-membership` 删除邀请奖励请求、提示、明细卡片和样式；`reward-center` 新增「邀请奖励」区块，被邀请人卡显示 0/2、1/2、2/2 自然日进度和 +15 积分，邀请人卡显示已邀请、已达标、预计可得与好友状态列表；补暗色主题样式。
- 验证：`go test ./internal/membership/service -count=1`、`go build ./cmd/server ./cmd/migration`、目标前端 eslint、`git diff --check` 通过；小程序全量 typecheck 仍被既有旧错误阻塞（day-record、feedback、food-library-share、food-library）。
- 未做：未启动开发服务器、未启动 weapp-devtools、未做运行时验证。
