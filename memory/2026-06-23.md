# 2026-06-23

## WeApp feedback submit button color
- User reported that the orange/beige "Submit feedback" button looked like a disabled/prohibited state.
- Updated `apps/wechat/src/packageExtra/pages/feedback/index.scss`: enabled submit uses brand green `#00bc7d -> #00a86b`; disabled submit uses neutral gray `#d0d5dd`.
- Verification: targeted `git diff --check` passed with existing LF/CRLF warning; single-file Sass parse passed.
- WeApp runtime verification was attempted but blocked because `mrc` is not available in PATH and no DevTools automation port was listening on 3001 or 9420. No `dev:weapp` / Taro watch process was running, and the agent did not start a user-managed long-running dev process.
