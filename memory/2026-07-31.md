# 2026-07-31

## 切换到开发分支

- 按用户要求将主工作区切换到开发分支 `dev`，并 fast-forward 到 `52208e2a2d6974a029e0813935bbd347c1f46956`，与当时本地跟踪的 `origin/dev` 一致。
- 切换前 `memory/2026-07-23.md` 的未提交修改和未跟踪的 `memory/2026-07-30.md` 会与目标分支冲突，已保存在名为 `pre-switch-dev-2026-07-31` 的 stash；没有强制覆盖、删除或丢弃用户改动。

## 开发人员长期会员与每日 200 积分

- 用户指定昵称 `微信用户_156864` 为开发人员，要求不仅每天 200 积分，还必须成为会员以使用会员专属功能。
- 按 Apollo `main`、`dev`、`local` 实际配置只读核对，三者当前指向同一个 `food-link` 数据库；昵称精确匹配唯一用户 `f456ab6a-c811-4cdd-9645-4249ce5ee957`，原先没有会员记录。
- 已写入并回查 `user_pro_memberships`：会员记录 ID `5af00750-fa38-4361-81bf-ed178d4d20c0`，套餐 `advanced_yearly`（进阶版 · 年卡）、状态 `active`、每日积分 `200`、自动续费关闭、到期时间 `2099-12-31 23:59:59+08:00`。
- 后端门禁回查确认有效会员与精准模式均已解锁；该账号没有历史 paid 会员订单，不会被运行时会员订单对账覆盖。未修改业务代码、未部署服务。

## 最近分支有用功能按能力迁移

- 用户确认旧分支较乱，要求优先迁移宠物、饮食心情、校园采集和聊天页修改中有用的内容。
- 经对照当前 `origin/dev`，饮食心情已有更新版 `eac6dee4`，校园采集已被开发分支包含并继续更新到 `9b82994f`，7 月 27 日聊天页引导/骨架/流式清理也已有当前实现；这些内容不再从旧分支重复合并。
- 本轮只迁移旧提交 `2e825401` 中确实缺失的像素宠物链路，并根据当前开发分支手工解决 API 冲突；未整分支合并。
- 为避免回归，保留当前宠物三候选选择逻辑；`image-rendering: pixelated` 只作用于自定义宠物；选图前增加照片将交给 AI 服务处理、朋友照片需先授权的确认说明。
- 后端宠物、handler 与配置定向测试、微信端 typecheck/目标 ESLint、core typecheck/test 均通过。food_link 的开发者工具 3001 端口未连接，9420 实为“教链”项目，故未执行错误项目上的 UI 操作，也未启动或重启开发服务。
- 迁移内容当前位于 `dev` 暂存区，尚未提交或推送；生产启用前还需配置像素头像 API，并考虑按用户限流与旧 COS 动作帧回收。

## 精确模式把熟米粉按干米粉计算

- 用户 07:10 截图对应生产精确任务 `62ddfc48-94e1-4207-bbf4-69e6493a4a7f`、session `f6807bf1-21d8-48df-8ad9-c9d32a3493f9`。
- 线上最终结果中“米粉”220g 的单位营养为 353.5 kcal、78g 碳水/100g，总热量 777.7 kcal；结果页选择 38% 后显示约 296 kcal、65.2g 碳水，所以滑块计算正确，单位营养错用了干制口径。
- 生产库已有 active 的“米粉(熟)”110 kcal/100g 与“米线”128.5 kcal/100g，但精确分项链路丢失 `foodState/weightBasis`，营养 fallback 为裸名称“米粉”生成了 inactive 的 qwen 行 `bc14b53b-4c8e-4872-9e28-6209b793c146`。
- 代码根因在 `backend/internal/worker/worker.go`：精准估重 JSON 和 `parsePrecisionEstimateItems` 只保留名称与重量，DB First 无法知道这是 `cooked/as_served`；`nutritionResolveName` 的状态修正目前仅覆盖泡发燕麦等少数情形。
- Health Canada 熟米粉数据约 109 kcal/100g，香港食安中心麻辣米线食物部分均值约 120 kcal/100g，与项目熟米粉库 110 kcal/100g相符。按 110 kcal/100g，220g 约 242 kcal，38% 约 92 kcal。
- 本轮只读诊断，没有修改代码、数据库或线上服务；修复待用户明确要求。
- 后续优化推荐分层实施：P0 先修精确模式字段保真和 fallback 契约，不做数据库迁移；P1 为高风险吸水食物增加后端生熟/干湿匹配门禁；P2 再为营养库增加结构化 preparation state / nutrition basis，并在结果页可选展示“按熟重/按干重”标签。不能只写“米粉”特例。
- 用户补充确认提示词也需要优化：四段模型链路都要输出/保留状态字段，并在营养补全提示中明确“每100g按当前食用状态的可食湿重计算”；同时必须修改解析和字段传递，不能只改 prompt 文案。
- 用户进一步确认名称本身也应包含状态信息。本轮已实现“双轨”：页面/结果名称采用“熟米粉、干米粉、泡发木耳”等明确名称，计算链路同时保留 `foodState/weightBasis/basisEvidence`，两者必须一致。
- 精确模式 planner、分项估重、重量复核的提示词和 JSON 示例均已加入状态字段；worker 的计划规范化、估重解析、复核解析及 metadata 回填已同步保真。裸“米粉”在餐食精确模式模型漏填时兜底为 `熟米粉 + cooked/as_served`。
- `nutritionResolveName` 现在将熟米粉查询规范为现有库标准名 `米粉(熟)`；Qwen/DeepSeek 营养补全输入携带状态、重量口径和证据，并明确熟重不得返回干料每100g营养。
- food nutrition repo 新增熟/泡发对干态、干重对熟态的双向不合理匹配拒绝，覆盖米粉、米线、面条、粉条、意面、燕麦、木耳等吸水食物。
- 新增定向回归覆盖状态化名称、字段跨估重/复核保留、熟/干米粉查询、模型提示契约及双向营养形态门禁，三组定向测试全部通过。完整 worker/analyze/repo 包测试受本机 embedded PostgreSQL 缺失 `initdb.exe` 阻塞；`git diff --check` 通过。未改库、未启服务、未部署。

## 拉取并合理合并开发分支

- 用户要求只更新本地 `dev`：拉取远端最新开发代码并与本地内容合并；`main` 不切换、不修改、不推送。
- 本地 `dev` 的技能元数据修复 `a147cc97` 已与远端 `origin/dev` 的 12 个提交成功合并为 `ef934a18`。
- 本地未跟踪的 `memory/2026-07-26.md` 与远端同名文件都含独立有效记录：本地版本已先保护在 `stash@{0}`，并将其核验摘要补入当前工作区的该文件。命名 stash 暂保留，便于恢复原始内容。
- `.codex-remote-attachments/`、根目录微信开发者工具临时配置、视频算法文档及 7 月 28–30 日记忆均保持未跟踪，未纳入本次合并。

## 万界方舟辅助扩充食物数据库

- 用户提出当前食物库只有一万多条，市场食物/商品可能有几十万种，希望评估用万界方舟 API 生成数据或找图匹配。
- 用户进一步希望大规模覆盖尽可能多的真实需求，并要求对应配图。API Key 不应粘贴到聊天；当前项目 Apollo 已有脱敏的万界兼容配置，正式批量调用前需先做模型权限冒烟并确认预算、候选库/生产库写入权限与图片许可策略。
- 代码核对显示项目已有 `nutrition-backfill`、`food_unresolved_logs` 和 `standard-food-image-backfill`；图片脚本已支持 Bing/Google 候选、视觉匹配、COS 及万界 Gemini 原生协议。
- 当前数据库只读统计：标准食物图片 8,886/9,626，覆盖约 92.3%，仅缺 740；更大的短板是名称/SKU 覆盖与营养证据。
- 建议把标准食物、包装 SKU、餐馆菜品分开扩充。万界只做候选、规范化、图片/OCR 复核和 AI 示意图，营养真值优先采用权威成分表或包装标签；模型估算只进低可信候选层。
- 已形成 `docs/food-database-expansion-plan.md`。后续若用户确认实施，P0 为 unresolved Top 1,000 候选流水线和 93 条中文缺图的合规回填；不得直接批量生成几十万条写入生产库。

## 首页分页、视觉模式与卡片个性化

- 用户纠正此前概念图的问题：首页翻页必须承载并保留当前已有信息和功能，不能为了清爽删信息、改入口或发明替代功能。
- 用户提出养生、活力等视觉模式，以及首页卡片可编辑、拖拽和个性化；当前明确要求停止继续生成图片，先基于代码深入做文字构图和产品规则。
- 当前只有浅色/深色主题，尚无首页模式与卡片布局配置；首页已有日期和条件提示、营养总览、建议、身体记录、餐食、保质期、统计、奖励轮播和可拖拽宠物，多层横向 Swiper 与拖拽会有手势冲突。
- 推荐将视觉模式与用户布局分开保存：模式只改变视觉 token、动效强度、宠物语气和可选默认排序；拖拽仅在编辑态启用，跨页移动通过明确菜单完成，核心卡不可隐藏。
- 当前宠物的功能体系已经较完整，但程序化基础形象和首页自由悬浮仍可能限制整体审美；后续应分别评估“功能是否够用”和“形象/构图是否达到品牌标准”。
