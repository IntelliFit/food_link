# 2026-08-07

## files 与 projects 合并

- 用户明确要求合并结果落在 `D:\projects\food_link`。当前 `codex/home-modes-v1` 从 `52208e2a` fast-forward 到 `origin/dev@639f40d7`；拓扑确认 `files/main@65b12843` 是该 dev 的祖先，因此 files 中已提交的校园层级、导入工具、像素宠物等内容已经包含在最新 dev。
- projects 原未提交/未跟踪 WIP 已完整备份为 stash `b3772e54d66158b1d08ce5f93c9dd9179e6af485`，随后重新应用。仅 3 个文件冲突：首页 `index.tsx`、`index.scss` 与 profile `index.tsx`。
- 首页冲突采用 projects 的 V2“养生/均衡”双模式作为主结构，避免与 dev 中另一套 homeMode/悬浮宠物界面重复；校园食堂 profile 入口保留，并补回 dev 的旧缓存容错、餐食收藏与宠物资料返回刷新。
- 备份中 2026-07-31、08-01、08-04、08-05 的独有任务记录已合并进 dev 同名记忆文件。权威工作区路径已从 `D:\files\food_link` 更新为 `D:\projects\food_link`。
- 验证通过：WeChat typecheck/lint、24 suites/87 tests；Mobile typecheck；API Client 39 tests；Core 14 tests；相关 Go 包编译以及短信、会员与校园门禁纯逻辑测试。数据库型 Go 测试仍受 Windows embedded PostgreSQL `initdb.exe 0xc0000135` 阻塞。
- `mrc` 已安装，但微信开发者工具 3001/9420 自动化端口均未监听，未做本次运行态截图/交互验证。未提交、未推送、未部署，未删除 `D:\files\food_link`，备份 stash 保留。
