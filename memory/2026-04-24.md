# 2026-04-24

- 用户明确要求：AI 后续默认不要自动操作本地常驻进程；前后端都由他自己手动启动和关闭，否则会抢占 `3010`，干扰他的调试。
- 已落实：
  - `AGENTS.md` 已改为“默认不自动运行本地常驻进程，除非用户明确要求”
  - `scripts/run-backend.cjs` 已增加端口预检查；`3010` 被占用时直接退出，不再先起大量 worker 再报 `10048`
  - 新增 `npm run stop:backend`，可一键清掉 `backend.pid` 或占用 `3010` 的残留后端进程
  - 本轮已实际清理掉一个残留的 `python.exe backend\\run_backend.py`，释放了 `3010`

- 用户要求整理本地 `D:\创业\healthymax\food_test` 数据集，但不能改原目录；新数据集必须去除文件名中的标签信息，标签改为单独文件，避免数据泄露。
- 已创建脱敏数据集目录：`D:\创业\healthymax\food_test_sanitized_20260424`
  - 图片统一改名为匿名 `sample_XXXX`
  - `labels.txt` 已写入 `33` 个已标注样本
  - `unlabeled_samples.txt` 记录 `4` 个暂未补标签样本
  - `README.txt` 说明了 total/items 两种标签格式
- 已创建上传 ZIP：`D:\创业\healthymax\food_test_sanitized_20260424_upload.zip`
- 已创建私有映射表（不放进上传包）：`D:\创业\healthymax\food_test_sanitized_20260424_private_mapping.csv`
- 用户给出的 `4` 张工作餐细分标签已按 `items` 模式写入：
  - `sample_0014.png`
  - `sample_0015.png`
  - `sample_0016.png`
  - `sample_0017.png`
- 其余带重量文件名样本已按 `total` 模式写入 `labels.txt`
- 已上传到本地测试后台 `POST /api/test-backend/batch/prepare`
  - 批次号：`6536d31ee0ec7103d7ea3ad1`
  - 状态：`pending`
  - 可处理样本：`33`
  - 跳过未标注样本：`4`
- 本轮只做了“上传准备”，没有启动批量分析，避免直接消耗上游模型额度。
- 用户后续明确确认：这 `4` 个未标注样本现在直接忽略，不需要继续补标签。

- 用户随后明确否定“每次重新上传 ZIP”的工作流，要求测试后台支持长期复用的“云端测试集”。
- 已在代码中接入一版“可复用测试集”能力（本轮未做本地运行验证）：
  - 新 SQL：`backend/sql/add_test_backend_datasets.sql`
  - 新后端接口：
    - `GET /api/test-backend/datasets`
    - `POST /api/test-backend/datasets/import-local`
    - `POST /api/test-backend/datasets/{dataset_id}/prepare`
  - 新后台页面入口：
    - `backend/static/test_backend/index.html`
    - `backend/static/test_backend/app.js`
  - 设计口径：从服务器本机目录导入测试集并持久化，之后在后台列表中重复载入为新批次
- 用户最新要求：这轮不需要本地运行验证，只要把代码写好即可。
- 随后已实际完成导入：
  - 测试集：`food_test_20260424`
  - 测试集 ID：`46ccb0ef-0fa2-484a-b06e-595f0c05120f`
  - 批次 ID：`80bc6b6a92fe1b687430698c`
- 用户对 `33/37` 展示不满，已修正：
  - 云端数据改为 `item_count=33`
  - `unlabeled_count=0`
  - 前端表格只显示可测样本数，不再显示源目录总图数
- 用户希望降低测试成本，新增一个随机抽样的小型测试集：
  - 目录：`D:\创业\healthymax\food_test_sanitized_20260424_mini10`
  - 私有映射：`D:\创业\healthymax\food_test_sanitized_20260424_mini10_private_mapping.csv`
  - 抽样方式：从 33 张已标注样本中按固定随机种子 `20260424` 抽取 10 张，可复现
  - 抽中的样本：
    - `sample_0027.jpg`
    - `sample_0028.jpg`
    - `sample_0001.png`
    - `sample_0014.png`
    - `sample_0009.jpg`
    - `sample_0025.jpg`
    - `sample_0013.jpg`
    - `sample_0021.jpg`
    - `sample_0018.jpg`
    - `sample_0020.jpg`
  - 已导入可复用测试集：
    - 名称：`food_test_20260424_mini10`
    - 测试集 ID：`c4b74960-d83a-4f05-a2d4-babfeb7886d3`
    - 批次 ID：`894ecc623c4fd89068fcfdfb`
- 用户进一步要求测试后台只保留两个模型选项：
  - `gemini-3-flash-preview`
  - `gemini-3.1-flash-lite-preview`
- 已修改：
  - `backend/main.py`：测试后台只接受上述两个具体模型名，并统一走 Ofox Gemini 视觉链路
  - `backend/static/test_backend/index.html`
  - `backend/static/test_backend/app.js`
- 用户最新又明确：为了方便做 prompt 实验，测试后台暂时不要再默认走 `backend/worker.py` 那套动态 prompt，而是直接使用“提示词管理”里配置的自定义提示词。
- 已落实代码：
  - `backend/main.py::_run_test_backend_provider_analysis(...)` 现在会优先 `get_active_prompt("gemini")`
  - 若 `model_prompts` 里存在激活的 Gemini 提示词，则直接把该 `prompt_content` 发给模型；仅在其为空时才回退 `backend/worker.py::_build_food_prompt`
  - 单图测试、批量测试、可复用测试集批次都走这条新逻辑
  - `backend/static/test_backend/index.html` 的文案已同步更新为“当前实验链路优先读取 Gemini 激活提示词”，并说明 `standard/strict` 暂时只保留为实验标记
  - 轻量静态检查通过：`python -m py_compile backend/main.py`、`node --check backend/static/test_backend/app.js`

- 会员体系代码复核补充：
  - 已重新读取项目状态文件并检查 git，同步确认当前本地 `dev` 并不是最新：`git fetch origin dev` 后，本地 `HEAD=14b5bea` 落后 `origin/dev=185be02` 共 `54` 个提交；同时工作树有未提交改动，不能直接安全拉最新
  - 当前会员一期改造的两个硬阻塞已确认：
    - `backend/database/membership_plan_config_seed.sql` 在 `ON CONFLICT ... DO UPDATE SET` 处混入脏文本 `我去`，SQL 当时无法执行
    - `backend/main.py` 仍暴露 `/api/dev/toggle-test-membership`，写死 `TEST_MEMBERSHIP_USER_ID` 与旧套餐 `pro_monthly`；前端 `src/pages/pro-membership/index.tsx` 也仍展示 `[DEV] 测试工具`，所以当时不能把它当成真实的“9 档会员测试入口”
  - 同时复核到：`backend/sql/add_tiered_membership_pricing.sql` 迁移本身结构正常；`backend/main.py` 的 `/api/membership/pay/create` 与 `/api/payment/wechat/notify/membership` 已按 `plan_code` 支持不同套餐，并在 notify 时写 `daily_credits` 快照；`python -m py_compile backend/main.py backend/database.py backend/run_backend.py` 通过
  - 关于此前“微信支付签名错误”排查：从当前代码看，签名拼接仍是标准微信支付 V3 口径，更像商户 `serial_no / private_key / public_key / api_v3_key` 环境配置不匹配，而不是 9 档会员改造本身直接引入的签名算法错误
- 会员体系继续收口：
  - 已修复 `backend/database/membership_plan_config_seed.sql`：删除脏文本，补充“仅 seed、不改 schema、可重复执行”的说明
  - 已修复测试会员开通链路：
    - `backend/main.py` 的 `/api/dev/toggle-test-membership` 现在依赖当前登录用户，不再写死测试账号
    - 开通时按请求里的 `plan_code`（为空则默认 `standard_monthly`）读取有效套餐，按套餐 `duration_months` 计算到期时间，并把 `daily_credits` 快照写入 `user_pro_memberships`
    - `src/utils/api.ts` 的 `toggleTestMembership(planCode?)` 已同步改为传套餐编码
    - `src/pages/pro-membership/index.tsx` 的 `[DEV]` 区块现在会显示当前选中的模拟套餐，并按该套餐执行开通/关闭
  - 已修复“运动记录被扣 3 积分”的根因：
    - 不是前端显示问题，而是后端积分试算把 exercise fallback 任务也算进了 `get_today_food_analysis_count`
    - 当数据库未支持 `task_type=exercise` 时，运动提交会回退投到 `food_text/food_text_debug`，同时 `payload.exercise=true`
    - 现在 `backend/database.py:get_today_food_analysis_count` 会显式排除 `payload.exercise=true`，因此运动只会按 `user_exercise_logs` 的 `1` 分/条计费，不再变成 `2+1=3`
  - 已按用户反馈继续收口会员前端：
    - 分析页精准模式不再按 `is_pro` 直接放行，当前轻度版会提示“升级到标准版/进阶版可解锁精准模式”，并导航到会员页的升级套餐
    - 分析页顶部提示条已从旧的“次数”口径改成新积分口径（已用 / 总额 / 剩余）
    - 会员页不再把 `3/8` 这种未标注意义的数字直接展示成“今日积分”，而是明确拆成“今日已用 5/8”和“今日剩余 3”
    - 我的页会员卡和服务入口文案也同步改为“已用 / 剩余”口径，并显示当前档位（轻度/标准/进阶）
    - 会员页套餐对比表只保留真实已上线差异；“计划指导”“强督促”等未开放项已移除承诺文案
- 用户最新明确要求：后续运行和验证由用户自己执行；代理只改代码，不再主动启动项目或做运行态检查
- 继续收口会员体系：
  - `src/utils/execution-mode.ts` 已新增统一权限判断：精准模式只有 `standard / advanced` 付费会员可用；轻度版统一视为“需升级”，不再仅凭 `is_pro` 放行
  - `src/pages/health-profile/index.tsx`、`src/pages/health-profile-edit/index.tsx` 已同步改口径：执行模式卡片写成“标准版及以上可用”；轻度版/非会员选择精准时会跳“去升级/去开通”，并在保存前自动阻止把无权限账号继续保存成 `strict`
  - `src/pages/pro-membership/index.tsx` 已为轻度版会员补上更显眼的升级提示条和“去看标准版”入口；会员页积分展示继续固定为“已用 / 剩余”双语义
  - `backend/database/membership_plan_config_seed.sql` 的 seed 描述已进一步收口为真实已上线能力，移除轻度版“精准识别”以及未上线功能的暗示性描述
- 用户继续反馈“超额了也能继续分析”，已继续收口会员积分 enforcement：
  - 后端 `backend/main.py` 已新增并接入 `_validate_food_analysis_access(...)` 到全部食物分析主入口：`/api/analyze`、`/api/analyze/submit`、`/api/analyze-text`、`/api/analyze-text/submit`、`/api/precision-sessions/{session_id}/continue`
  - 当前正式口径：
    - 食物分析需 `2` 积分/次，积分不足时统一返回 `402`
    - 精准模式权限继续按档位判定：只有 `standard / advanced` 可显式请求 `strict`；`light` 会员会收到“升级到标准版/进阶版”的拦截提示
    - 若用户历史档案默认还是 `strict`，但当前套餐无权限，后端会自动降回 `standard`，避免老状态穿透
  - 前端本地预检查也同步换成新积分口径：
    - `src/pages/analyze/index.tsx`
    - `src/pages/record-text/index.tsx`
    - `src/pages/record/index.tsx`
    - `src/pages/index/components/RecordMenu.tsx`
    - 不再使用旧 `daily_limit / daily_remaining` 文案或判断；统一改成基于 `/api/membership/me` 的 `daily_credits_max / used / remaining`
    - 超额提示统一从“今日次数已用完”改为“积分不足”，并根据会员状态显示“去开通 / 去升级”
  - 这一轮仍然只是改代码，没有替用户运行项目；后续需由用户自行重启后端和小程序验证 `402` 拦截是否在运行态生效
- 用户随后又指出：运动记录在积分耗尽时仍然能继续提交，这与“运动记录 1 积分/次”矛盾。
- 已继续补齐运动链路：
  - 后端 `backend/main.py`
    - 新增 `_raise_if_exercise_credits_insufficient(...)`
    - `POST /api/exercise-logs` 现会在创建任务前按 `daily_credits_remaining` 做硬拦截；剩余积分 `< 1` 时返回 `402`
  - 前端 `src/utils/membership.ts`
    - 新增运动专用判断与文案：`isExerciseLogCreditExhausted / getExerciseLogCreditBlockMessage / getExerciseLogBlockedActionText`
  - 前端 `src/pages/exercise-record/index.tsx`
    - 进入页面时会拉取当前会员状态
    - 提交运动前先按“运动记录 1 积分/次”做本地预检查
    - 如果后端仍回“积分不足”，前端会弹升级/开通提示，而不是继续走 pending 卡片
- 本轮同样只改代码，没有替用户运行项目；用户自行重启后，应重点复测“已用满 8/8 后，运动记录是否还能提交”。
