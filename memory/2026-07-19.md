# 2026-07-19

## 虚拟支付道具图标适配

- 用户上传的 `D:\创业\食探\虚拟支付的图标.png` 原始尺寸为 1254×1254、约 1.30MB，不符合后台要求的 200×200、<200KB。
- 已创建独立输出 `D:\创业\食探\虚拟支付的图标-200x200.png`，尺寸 200×200，体积 57,314 bytes（约 55.97KB）；原图保持不变，可直接用于普通道具封面上传。

## 首个开发版道具已创建

- 用户已创建普通虚拟支付道具 `light_monthly`（食探轻度版月卡），9.9 元、30 天轻度会员，当前在开发版本且“未发布”。
- 下一阶段是 XPay 代码接入与沙箱联调，不应先直接发布现网；发货推送 URL 需在后端实现对应路由后再配置。

## 虚拟支付代码首轮完成

- 会员页已从普通 `requestPayment` 切换为 `wx.requestVirtualPayment`：fresh `wx.login` code 交给后端，后端换取 session_key 并生成 XPay 三段签名；一次性套餐使用后台道具 ID/固定分价。
- 新增服务端下单和发货推送 URL `/api/membership/xpay/create`、`/api/payment/wechat/xpay/notify`；回调按 Token 签名、环境、openid、商品 ID、数量、金额校验，成功后幂等开通会员。
- 待安全配置 `WECHAT_XPAY_OFFER_ID`、`WECHAT_XPAY_APP_KEY`、`WECHAT_XPAY_SANDBOX=true`、`WECHAT_XPAY_MESSAGE_TOKEN` 并部署。随后在公众平台消息推送设置同 Token，沙箱 URL 使用 `https://dev.api.healthymax.cn/api/payment/wechat/xpay/notify`。
- 用户已确认上述 XPay 环境变量配置完成；等待开发后端和开发版小程序部署、消息推送 URL 明文模式验证，以及 `light_monthly` 沙箱支付联调。
- 公网只读检查最初为 HTTP 404；用户部署后复查同一 URL 已为 HTTP 503，并返回“缺少小程序虚拟支付配置：WECHAT_XPAY_OFFER_ID, WECHAT_XPAY_APP_KEY”。路由已在远程开发后端生效，当前阻塞是远程容器/Apollo 没有加载 OfferID 与 AppKey；需把这两个值（以及 Token 等）设在远程开发部署的真实配置来源后再滚动重启。本地后端重启不会影响微信访问公网 URL。
- 已修复 XPay 配置在 Apollo 模式的兼容遗漏：Apollo 启动路径此前未绑定部署环境变量，且 Apollo 的大写属性键别名未包括四个 `WECHAT_XPAY_*` 键，造成已填写配置仍在运行时为空。配置包定向测试通过；需随下一次开发后端镜像部署后生效。
- 用户称重新部署后，公网回调仍返回相同 HTTP 503（缺少 OfferID/AppKey），因此当前未成功。XPay 接入和 Apollo 兼容修复仍是本地未提交改动，需确认部署构建确实包含此工作区改动，并确认变量注入实际运行的开发 Pod。
- 用户按正确 YAML 层级修改后，公网连续 9 次请求覆盖三个 `foodlink-dev` Pod，仍全部 503，排除滚动发布混合版本。当前应核对 Apollo 是否改在 `dev/app-config.yaml` 并已发布；本机 bootstrap 使用的是 `local/app-config.yaml`，修改 local 不会自动影响公网 dev。
- 用户再次调整 Apollo 后，公网连续 9 次请求已稳定返回 401 验签失败而非 503，确认 XPay 配置已被多个开发 Pod 加载。裸请求没有微信签名时 401 属于预期；现在可在微信后台提交消息推送 URL 验证。
- 微信后台提交仍失败的根因已从开发 Pod 启动/请求日志定位：微信验证请求已到达，但 Apollo Token 末尾 `qIu` 与微信后台截图末尾 `qlu` 存在大写 I/小写 l 差异，导致 SHA-1 签名必然不一致。需统一两端 Token，建议换成 32 位小写十六进制避免歧义。
- 用户统一 Token 后，微信后台消息推送服务器配置验证成功。下一步跑 `light_monthly` 沙箱支付与发货闭环；正式提审前为其余套餐创建一一对应的虚拟支付道具，或隐藏尚未配置的套餐，不能让审核员点击到不存在的商品 ID。
- 仅支付成功不能证明 XPay 生效，因为旧会员 JSAPI 创建接口仍保留，旧小程序包可能继续走普通支付。沙箱验收需同时看到 XPay 创建接口、XPAY 订单字段、虚拟支付后台订单和 XPay 发货回调 200；正式提审前建议关闭小程序会员普通 JSAPI 新下单。
- 04:10 真机打开了微信虚拟支付测试环境与支付面板但未付款；开发 Pod 日志确认请求是 `/api/membership/xpay/create`、`light_monthly` 创建成功且 200，同期无旧会员下单接口。因此新 XPay 前端、签名和微信沙箱受理已验证，付款后发货/权益闭环仍待验证。
- 04:18 用户支付沙箱 ¥9.90 后会员生效；开发 Pod 日志确认微信 XPay 发货 POST 到回调、后端成功开通会员并返回 200，完整闭环通过。后续为关闭旧 JSAPI 新下单、补齐/隐藏其余 8 套餐、提交代码与准备正式环境切换。

## 营养匹配覆盖率与Top5小模型延迟分析

- 对线上近30天1,075个已完成分析任务、2,822个食物项执行只读静态回放。当前来源过滤精确覆盖54.75%、候选灰区11.59%、无候选33.66%；任务级小模型调用上限25.77%。
- 当前AI来源过滤漏掉`AI估算（DeepSeek）`1,943行与`deepseek_auto`181行。保守隔离后可信精确覆盖为45.85%、候选灰区12.23%、无候选41.92%，说明库治理比扩大小模型调用更优先。
- Qwen 3.6 Flash复用连接时，单灰区Top5中位3.142秒且5/5在6秒内；三灰区Top5中位5.142秒，仅3/5在6秒内且有1次超时。建议语义门禁与新鲜营养估算并行，多灰区拆成单项或小批限并发。
- 结论：小模型值得保留为灰区安全门，但不能解决无候选问题；先统一AI来源、结构化食物身份，再影子运行并用人工真值集校准真实命中率。分析目录为`.local-state/audits/2026-07-19-nutrition-match-replay/`；未写数据库、未部署、未改生产业务代码。

## 旧版AI营养数据复审与识别链路升级

- 当前有效旧AI行2,124条已全部包含在2026-07-14旧全量审核中；针对旧非keep和审核后变化的111条重新调用DeepSeek V4 Pro，API失败0，得到keep 74、review 18、fix候选14、deactivate候选5。
- 历史/本轮模型keep都不升级为可信数据。运行时代码已把`AI估算（DeepSeek）`和`deepseek_auto`统一识别为AI来源，全部旧AI行从精确命中和Top5候选中隔离；来源分类也改为`llm_generated`。
- Top5小模型门禁与营养补全已改为并行执行，仍只有四项身份全部等价且confidence>=0.97才复用；否则采用新鲜估算，降低6秒门禁与12秒补全串行叠加的延迟。
- 定向Go测试、go vet、Python py_compile和diff检查均通过。审计目录为`.local-state/audits/2026-07-19-legacy-ai-reconcile/`。未写生产数据库、未部署；生产库物理清洗等待用户明确授权并要求证据、回滚和幂等迁移。

## 生产营养库AI数据清洗完成

- 用户明确授权代理在无需逐条人工审核的情况下直接清洗线上数据库。最终没有用DeepSeek建议值覆盖营养，而是对全部AI生成营养采用保留原始数据、统一设为inactive的可回滚策略。
- 新增迁移独立参数`--only-legacy-ai-nutrition`，只执行营养AI隔离，不会附带当前工作区的支付、会员等其它迁移。已在Apollo local当前指向的远程`food-link/public`主库执行，并重复执行验证幂等。
- 本次实际停用4,180条活跃AI营养：2,124条旧漏分类来源加2,056条已有生成来源。写后活跃营养总数9,560、活跃AI 0；AI inactive总数4,189，其中9条此前已经停用。所有原始ID、名称、营养和来源仍保留。
- 最终定向Go测试、go vet和diff检查通过。数据库清洗已经生效；Top5严格门禁、来源统一分类和并行营养补全仍未部署后端。

## 普通模式西瓜可食部35%异常

- 用户提供线上截图：西瓜原始约350g、可食35%、计入123g，模型理由是果皮和瓜籽不可食、按常识可食部约35%。
- 已确认前端只是展示后端字段；后端因名称命中“西瓜”关键词无条件调用二次文本可食部模型，模型未直接看原图，却按整瓜常识泛化比例。后端只做比例范围校验，没有要求照片中明确可见并计入了皮/籽的证据，因此35%被直接应用。
- 这是可食部证据门禁缺失，不是营养库或前端问题。稳定修复方向是让视觉阶段结构化标注不可食部分是否可见、gross重量是否包含不可食部分；无明确证据或已切块/纯果肉时强制100%，拒绝纯“常识”理由的扣减。
- 已在正确的线上集群主机`154.8.205.78`只读确认`foodlink-main`三个Pod正常；未改业务代码、未写数据库、未重启或部署。
- 进一步核对确认普通模式可食部二次模型优先为`qwen3.6-flash`，无thinking、8秒超时；Qwen不可用才回退DeepSeek。模型不看原图，只拿名称、grossWeight和可选文字证据，当前输出schema也没有confidence，后端仅校验1–100后应用。
- 用户明确要求可食比例不再使用二次文本模型，本轮只做方案分析。推荐用视觉形态分类作为路由：纯果肉/去皮去骨/即食形态100%；完整采购态查带来源与版本的权威可食系数；局部带皮带骨用视觉分割/几何估算；高影响不确定项让用户确认。文本模型只可做标准化，不可决定比例。

## AI营养库整批停用错误已按逐条审核方式纠正

- 用户指出此前把4,180条活跃AI营养整批停用是对需求的错误理解；正确要求是全量审核后把可信数据加入可信库。该纠正已作为稳定项目决策记录，旧的“全部隔离即清洗完成”结论不再作为最终状态。
- 全部4,189条AI营养经过双模型身份审核；179条普通食物再做DeepSeek/Qwen/GLM三模型无锚定估算。最终只激活35条三模型一致估值，12条明确异常标记rejected，4,142条不确定项保持inactive。误停用前已inactive的9条通过历史产物精确识别并继续保持inactive。
- 原本建议权威合并的“樱桃”“糙米”因未同时获得同目标精确别名批准，最终也保持停用，避免把泛称绑定到更具体的USDA条目。
- 全部7,435条旧正式别名完成独立双模型审核：approved_exact 2,822、candidate_only 4,171、blocked 442。茶叶蛋到全蛋、油菜到清炒油菜、清炒土豆丝到土豆均为blocked；原味酸奶到无糖酸奶、红烧茄子到茄子为candidate；花椰菜到raw cauliflower为approved。
- 线上 `food-link/public` 已完成专用质量字段迁移、审核产物dry-run、单事务apply、同批次重复apply幂等验证以及逐ID写后核验。活跃营养现在为权威8,248 + 历史可信1,312 + 三模型审核估值35 = 9,595；写后报告 `ok=true`、无错误。
- 预写备份SHA256为`cc310a6c26524dedfc5805bb3c784865c21a92d8ff1e6e8161f777054d014432`，审核产物SHA256为`eac8945e73b20fa47ebff16979b51402a8e2c9efc0f1660e651e91255c9b0230`，均位于`.local-state/audits/2026-07-19-ai-trust-promotion/`。
- 运行时可信过滤代码已完成但未部署；工作区混有XPay/会员/前端等无关未提交改动，不能直接构建上线。旧线上进程在新代码干净部署前仍可能忽略别名三态，因此数据库治理完成不等于运行时阻断已完全上线。

## 审核后精准命中基线与主动补库建议

- 按新质量等级和别名三态回放近30天数据，普通食物2,666项中严格精确命中31.02%、候选灰区28.28%、无候选40.70%。该口径比旧统计严格，因为只有approved_exact别名能直接命中，blocked不参与候选。
- 高频候选缺口为卤鸡腿、卤蛋、香煎鸡胸肉、青菜汤、煎蛋、美式咖啡、牛奶、青菜、茶叶蛋；高频无候选为煎荷包蛋、茶水、爆炒鱿鱼、剁椒蒸鱼、卤豆腐等。
- 口径修正：煎蛋171、面条137、青菜82、牛肉85是静态宽召回全集的唯一食物ID数，不是线上实际返回数量，也不是同义词数量；线上最多返回Top5。煎蛋之所以触达171条，是规范化删除“煎”后用单字“蛋”包含检索，召回了蛋白粉、蛋汤、蛋糕、蛋挞和各种含蛋菜品。应先禁止单字退化并保留加工态，再去重和补首批100–200个高频条目。
- 补库按权威原料/明确烹饪态、常见菜审核平均模板、包装SKU标签三条线进行，并按周从真实未命中与用户纠错滚动更新。前50组高频灰区和无候选全部安全解决时，静态exact理论上限约51.28%，但泛称不能为了数字强行精确绑定。
- 回放产物为`.local-state/audits/2026-07-19-ai-trust-promotion/post_review_match_replay.json`。

## 小程序虚拟支付生产收口完成

- `light_monthly` 沙箱真实支付、XPay发货回调和会员开通闭环已通过；消息推送URL也已验证成功。
- 提交 `18ecfbbc feat: 完善小程序虚拟支付闭环`：新增XPay下单/验签/发货/退款处理和权益快照幂等；普通JSAPI会员新单410阻断；未获准的Papay新签约403阻断；前端只保留 `wx.requestVirtualPayment`，并移除隐藏自动续费审核预览。
- 全额退款只有在当前会员仍匹配本单激活结果时才自动恢复购买前快照；部分退款、旧单缺少快照或已有后续会员变化时仅标记人工核对，避免错误回退。
- Go定向测试、go vet、前端定向ESLint和diff检查通过；全量TS仅有既有day-record错误。WeApp端口9420可连接，但导航/截图命令挂起，未取得新页面截图。
- 未推送、未部署。当前脏工作区有其它营养/登录改动，不能直接从当前目录构建XPay独立镜像；需从干净提交 `18ecfbbc` 部署，或明确一起上线的范围。
- 用户侧剩余：补齐8个开发版普通道具并抽测；全部通过后发布道具现网。正式上线前配置main现网AppKey、sandbox=false，部署main后把消息回调切到正式API域名。

## 营养库全量向量召回完成

- Yunwu实测选择`text-embedding-3-large` 1024维；30条批量3/3成功，中位2.633秒、P95 2.665秒。`qwen3-rerank`虽中位1.134秒但身份Top1仅60%，不用于最终判等。
- 线上专用向量表已经迁移并幂等回填16,553条：9,560个可信canonical、6,993个非blocked别名，共67,801,088字节；写后长度、hash、资格、blocked alias、约束与索引检查全部通过。
- 精确命中和已有词法Top5不调用embedding；只有普通食物词法0候选才调用。预热后5查询批量embedding+全索引扫描+食物取回约1.868秒，并与新鲜营养估算并行。67.8MB冷加载本机跨网络约114秒，已改启动后台预热，未就绪请求立即走旧回退。
- 安全规则：向量只召回候选，Top20后重新做可信来源、干湿态、鲜食/加工态过滤，最多Top5仍过四项身份门禁；包装食品禁止走普通向量召回，防止百奇规格/每份口径被普通饼干污染；“煎蛋”不得退化成单字“蛋”。
- 当前`dev` HEAD为`11bf0a34 feat: 添加营养向量候选召回`。线上向量数据已就绪，但运行时代码尚未从干净提交部署、默认配置仍关闭；工作区混有另一组注册引导改动，不能直接构建。临时API key未持久化，正式启用需Apollo长期密钥和一次干净部署。
- 审计证据：`.local-state/audits/2026-07-19-embedding-retrieval/yunwu_benchmark.json`与`production_postcheck.json`。

## dev 全量合并与虚拟支付发布候选验证

- 用户确认九个开发道具已全部部署，并授权把所有有效本地修改合入远端 dev、部署和抽测。排除 `.codex-remote-attachments/`；未强推、未丢弃远端改动。
- 本地三笔提交 `18ecfbbc`、`dbd1dd3d`、`11bf0a34` 与远端十笔新手引导提交完成合并；修复 migration 双冲突、onboarding 测试表字段、包装食品向量召回边界、向量索引 TTL 刷新和 day-record null 图片类型。merge commit `b450db46` 已推送 `origin/dev`。
- 验证通过：受影响前端 ESLint、全量 TypeScript、XPay/会员/config/app/analyze/worker/migration/user/auth/营养相关 Go 测试与 go vet。全量 Go 测试的仓库既有 `backend/tmp` 多 main 包问题未作为本次阻塞；本次所有相关包均单独通过。
- Apollo local 明确解析为 development 后，两个定向迁移 `--only-onboarding-status`、`--only-nutrition-embeddings` 均成功。开发镜像构建推送成功，CCR manifest digest 为 `sha256:27077c9448c814e017860ff8466fb593ae00390000428f98994554b0d5769cfe`。
- 只读登录服务器 Kubernetes 后确认三个 `foodlink-dev` Pod 均为 Running/Ready，实际 imageID 与上述 digest 完全一致，启动时间约 06:59。12次健康请求全为200，稳态约25–53ms；会员计划200；XPay创建无认证401，XPay回调GET/POST缺签名均401。日志无XPay/启动错误，仅一条无关的保质期worker数据库连接瞬时超时。
- 微信开发者工具已由 CLI 开启9420自动化端口，但 `mrc where` 持续超时；独立9421启动明确报 `INVALID_TOKEN, access_token is invalid or not latest`。因此本轮没有伪造UI截图结论，正式发布仍需用户刷新开发者工具登录凭证后完成会员页/XPay入口截图与交互抽测。
