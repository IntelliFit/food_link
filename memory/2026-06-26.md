# 2026-06-26

## 完成：我的礼券 + 后台用户奖励发放

- 用户要求：
  1. 小程序「赚积分」页新增「我的礼券」版块，注册试用、邀请轻度会员、后台积分奖励统一以礼券形式发放，用户点击使用才到账。
  2. 奖励发放时通过系统消息通知，消息带跳转链接直达「我的礼券」。
  3. Admin 后台新增「用户奖励」页面，可搜索用户、查看基础信息/积分/会员状态，并直接发放积分礼券。

- 关键决策：后台「直接发送奖励积分」也采用礼券形式发放（用户选择）。

- 已执行数据库迁移：154.8.205.78:5432/food-link（用户确认后执行）。

- 验证结果：后端编译通过、核心模块测试通过、Admin typecheck 通过、小程序 typecheck 无新增错误、小程序 debug 构建成功。

- 阻塞/后续：
  - 需要启动后端服务与 `npm run dev:weapp` 做微信开发者工具端到端验证。
  - 后端需重新部署才能线上生效。
  - 已提交并推送到 `origin/dev`（commit `aa427217`）。

## 2026-06-26 校园食堂/公共食物库反馈通道

- 完成校园食堂、公共食物库的结构化反馈入口，复用 `user_feedback` 表。
- 后端新增 `source` / `extra` 字段与迁移，Admin 反馈页增加来源筛选和快捷跳转。
- 关键文件：`backend/internal/feedback`、`backend/internal/admin/repo/feedback_repo.go`、`apps/wechat/src/packageExtra/pages/{campus-canteen,food-library,food-library-detail}`、`admin/src/pages/feedback-page.tsx` 等。
- 验证：`go build ./...`、后端单测、Admin TS、小程序 typecheck（仅原有 day-record 错误）均通过；微信开发者工具自动化连接成功但命令挂起，未拿到运行时截图。
- 已更新 `.local-state/current-task/active.md`。

## 追加：「我的」页新增「我的奖励」入口

- 用户反馈「我的」页赚积分板块下没有「我的奖励」。
- 在 `apps/wechat/src/pages/profile/index.tsx` 服务列表中新增「我的奖励」入口，跳转到 `my-vouchers` 页面，并显示待使用礼券数量 badge。
- 已用 `npm run build:weapp:debug` 编译验证产物包含该入口。

## 追加 2：修复「我的礼券」入口白屏

- 用户反馈新增入口后「我的」页白屏。
- 将入口文案改为「我的礼券」，并对 `getMyVouchers` 调用增加存在性保护，避免旧 bundle 缺少该函数时崩溃。
- 已重新 `npm run build:weapp:debug`。

## 追加 3：「我的礼券」入口图标改为 icon-youhuiquan

- 按用户要求，将 `pages/profile/index.tsx` 中「我的礼券」的 `iconClass` 从 `icon-jiesuo` 改为 `icon-youhuiquan`。
- 已重新 `npm run build:weapp:debug` 验证产物。

## 追加 4：移除 profile 入口的礼券数量请求

- 用户仍报「我的」页白屏。为进一步排除 `getMyVouchers` 相关调用导致崩溃的可能，先移除 profile 页对该 API 的调用和 badge，仅保留纯静态入口。
- 已重新 `npm run build:weapp:debug`。

## 追加 5：Admin 用户奖励页样式与菜单调整

- `admin/src/pages/user-reward-page.tsx`：外层容器改为 `max-w-[1540px]`、居中对齐，与反馈页等其他模块保持一致，避免宽屏撑满。
- `admin/src/components/admin-sidebar.tsx`：
  - 「用户声音」分组改名为「用户管理」。
  - 「用户奖励」从「实验工具」移到「用户管理」下。
- 顺手清理了 `admin/src/pages/feedback-page.tsx` 中未使用的 `useNavigate`。
- `cd admin && npm run build` 通过。

## 追加 6：Admin 用户奖励搜索结果展示头像与间距

- `admin/src/pages/user-reward-page.tsx`：搜索结果卡片增加用户头像（无头像时显示默认图标），卡片之间间距从 `space-y-2` 加大到 `space-y-3`。
- `cd admin && npm run build` 通过。

## 追加 7：修复系统消息样式与「去使用」跳转白屏

- `apps/wechat/src/packageExtra/pages/private-chat/index.scss`：系统消息气泡统一为 `width: 80%; max-width: 640rpx`，文字居中，保证不同内容宽度一致。
- `apps/wechat/src/utils/subpackage-extra.ts`：`extraPkgUrl` 增加对「已经是完整分包路径」的校验，避免 `/packageExtra/...` 被重复拼接。
- `backend/internal/voucher/service/voucher_service.go`：新礼券系统消息 `extra.path` 改为标准路径 `/pages/my-vouchers/index`（不再带分包前缀）。
- `cd backend && go build ./...` 与 `cd apps/wechat && npm run build:weapp:debug` 均通过。
- 后端部署后新发放的礼券会写入标准路径；历史已发的 `/packageExtra/...` 路径现在也会被前端兼容处理。

## 追加 8：修复 user-group 分包自定义组件路径警告

- 问题：`packageUserGroup/pages/user-group/index.tsx` 只是 re-export `packageExtra/pages/user-group/index`，导致 Taro 在 `lazyCodeLoading: requiredComponents` 下解析跨分包组件路径时报警告。
- 修复：把 `packageExtra/pages/user-group/` 下的 `index.tsx`、`index.config.ts`、`index.scss`、`group-config.ts` 全部移到 `packageUserGroup/pages/user-group/`，并删除 `packageExtra/pages/user-group` 目录。
- `cd apps/wechat && npm run build:weapp:debug` 不再输出相关警告。

## 追加 9：系统消息宽度再大一点、文本左对齐

- `apps/wechat/src/packageExtra/pages/private-chat/index.scss`：系统消息气泡宽度从 `80% / 640rpx` 调整为 `92% / 720rpx`，文本对齐从居中改为左对齐。
- `cd apps/wechat && npm run build:weapp:debug` 通过。

## 追加 10：我的礼券页主题色与类型色带

- `apps/wechat/src/packageExtra/pages/my-vouchers/index.scss`：
  - 页面背景改为首页同款的低饱和绿色渐变。
  - 标签页、按钮使用主题绿。
  - 礼券卡片增加 8px 左边框，并按类型着色：
    - `registration_trial`：绿色 `#5cb896`
    - `invite_light_week`：橙色 `#f0985c`
    - `admin_points`：蓝色 `#5c9ed4`
  - 不同类型同时带有对应的极淡背景色。
- `apps/wechat/src/packageExtra/pages/my-vouchers/index.tsx`：卡片 className 增加 `voucher-card--${voucher.voucher_type}`。
- `cd apps/wechat && npm run build:weapp:debug` 通过。
