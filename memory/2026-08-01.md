# 2026-08-01

## 首页字号收敛与主界面美化

- 用户要求主页字号等级限制为 4 种并继续美化主界面。开发者工具基线统计首屏原有 13 种实际字号，最小仅 8px。
- 首页新增四档 Sass 字号 token：72rpx 主热量、44rpx 问候与身体关键数据、28rpx 模块标题/主要正文、22rpx 辅助文字/按钮/标签；图标尺寸不计入文字等级。
- 体重、喝水、运动由三个独立白卡收拢为统一“今日记录”面板，增加明确标题和轻量提示，并为窄屏辅助文字增加 ellipsis 防溢出。
- 微信端 typecheck、首页 ESLint、SCSS 编译和 diff check 通过。开发者工具自动化确认默认态及展开 21 项微量营养态都只有 4 档实际字号（37/22/14/11px），无运行时异常、重叠或裁切；最终停留在浅色折叠首页，watch 保持运行。

## 首页热量卡片突出重点

- 用户反馈首页主界面重点不突出，要求主要修改中间热量/营养卡片。
- 卡片已重构为“今日热量 → 剩余可摄入/已超出目标大数字 → 已摄入/目标与主进度 → 三大营养”的视觉层级；目标设置降为次级入口，三项营养由三个嵌套白卡收拢为一个分栏信息带。
- 正常状态使用低饱和绿色聚焦，超标只突出异常状态和对应数值；同时补齐深色模式样式。
- 微信端 typecheck、首页 ESLint、SCSS 编译和 diff check 通过。微信开发者工具 3002 自动化连接成功并切到首页，但 `dist` 仍是源码修改前的旧产物；遵守不擅自启动或重启本地开发进程的规则，未将旧版截图作为运行时验证证据。
- 用户随后明确要求在开发者工具中显示；已只重启失效的 `dev:weapp` watch，16:47 重新编译成功，新版卡片已在开发者工具首页显示。
- 自动化截图确认卡片无重叠或裁切；“更多营养”展开/收起正常，访客点击“设置目标”会跳转登录页。最终让开发者工具停留在折叠状态的新版首页，watch 保持运行。

## 像素分身超时与首页宠物精简

- 用户日志显示本地上传 731,965 字节 PNG 后，万界 `https://maas-openapi.wanjiedata.com/api/v1/images/edits` 在约 10 秒发生 TLS handshake timeout，最终 `/api/pet/pixel-avatar` 返回 500。
- 像素分身客户端原有 150 秒总超时，但未覆盖 Go 默认 10 秒 TLS 握手限制。已使用独立 HTTP Transport 将握手预算调整为 30 秒，并只对 TLS 握手超时做一次 300ms 延迟重试；定向测试同时验证普通传输错误不会重试。
- 用户反馈宠物展开时整页下移且周边文字过密。根因是展开态为首页问候区增加 224rpx 最小高度和 400rpx 右侧 padding；已删除这组布局联动，宠物保持 fixed，不再触发页面重排。
- 首页宠物界面现在只保留短对话气泡、角色和无文字收起箭头；“健康分身 / 和我聊聊 / 进食状态 / 定制角色”等标签已从首页移除。点击气泡或宠物仍进入聊天。
- 专属像素分身入口迁移到宠物主页“外观换装”，能力未删除。
- 静态验证通过：微信端 typecheck、目标 ESLint、像素分身 HTTP 客户端测试、宠物 handler 编译与 diff check。微信开发者工具专用端口 3001 未开放，无法按规则完成截图和收起/展开交互验证；未自动启动或重启开发进程。

## 包装食品数据库首轮扩容

- 用户授权调用万界方舟/Apollo 中的密钥，首轮预算不超过 500 元，要求至少新增 10,000 条高含金量且带配图的数据；用户明确允许千问、豆包等低价多模态模型，并要求多智能体并发提速。
- 新增 `backend/cmd/openfoodfacts-import`：支持官方全量 CSV、严格质量门禁、Qwen 中文增强、Qwen/豆包视觉提供方、1-4 并发、外部智能体视觉证据、预算检查点、幂等写库和 PostgreSQL advisory lock。
- 扫描 Open Food Facts 3,423,529 行，严格拒绝 3,176,346 行，选出并净入库 10,200 条；最终数据库来源计数、active、图片、条码和唯一条码均为 10,200，重复组为 0，总包装库 10,537 条。
- 质量统计：质量分中位数 0.99，完整度中位数 1.0，营养图 10,186，配料 10,199，中文检索词 10,158，中文分类 10,163；100 条正式等距视觉抽检 96 通过 / 4 失败。
- 计量成功请求保守估算 112.90762 元；将未返回 usage 的慢路由冒烟也计入预留后仍低于 150 元。豆包和 Qwen VL 万界路由各等待超过 5 分钟未成功，因此停止扩大调用，剩余图片由 3 个并行智能体逐图审核。
- 首次命令超时遗留子进程造成 2,274 条临时重复，已在确认无奖励引用且冗余别名可覆盖后精确清理；最终重复为 0，并用数据库 advisory lock 防止再发。
- 相关导入器单测、豆包/OfoxAI 客户端定向测试和 `go vet` 均通过。未提交、未推送、未部署代码；数据已写入当前 Apollo 配置指向的数据库。
- 本轮只有 62 条商品带 China 国家标签，属于全球热门商品冷启动；下一轮应按真实搜索未命中、纠错和国内 SKU 证据补库。

## 黄豆/黑豆问题复核

- 用户追问首轮数据是否解决截图中“搜索黄豆只有熟重、黑豆只有豆浆、很多食物无数据”的问题。结论：没有完全解决，首轮主要解决包装商品、条码和配图覆盖，不是基础食材生熟状态库。
- 只读数据库复核：新增包装库中黄豆/黑豆/豆浆/大豆相关匹配 98 条，但主要是国外包装豆饮、甜品、豆制品；它们不能替代普通黄豆/黑豆的生熟营养。
- 普通营养库已有 USDA 的生黄豆、生黑豆和无盐煮黄豆/黑豆条目，但“生黄豆/生黑豆/熟黑豆”等别名仍为 `candidate_only`；“黄豆/黑豆”通用 exact 别名缺失。豆浆存在项目内置、历史均值和 AI 生成多套数据，需按无糖/含糖/冲调/现磨等口径收敛。
- 后续正确方向：先建设中国常见基础食材的状态化 canonical 数据与审核别名，再补每个状态的合规图片；无状态搜索词返回生重/熟重候选供用户选择，不能默认其中之一。
