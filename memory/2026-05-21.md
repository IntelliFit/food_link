# 2026-05-21

- 零食补库页底部固定按钮遮挡钠输入框值：截图中“每100g营养成分”的钠值 `100` 跑到“保存到零食库”按钮上。已在 `src/packageExtra/pages/packaged-food-edit/index.tsx` 的 `ScrollView` 内容末尾加入 `.edit-footer-spacer`，并在 `index.scss` 用 `height: calc(150rpx + env(safe-area-inset-bottom))` 给 fixed footer 预留稳定空间。验证：页面 eslint 通过，目标 diff-check 仅 CRLF warning；`mrc where --port 3001` 仍因微信开发者工具自动化端口未连接失败，未能截图复验。
- 营养成分表异步识别提交接口出现 `analysis_tasks_task_type_check (SQLSTATE 23514)`，根因是新增 `packaged_nutrition_label` 任务类型后数据库 check constraint 未同步。已修复 `backend/internal/migration/migration.go`，把 `packaged_nutrition_label` 加入 `analysis_tasks_task_type_check` 的允许值和 debug 正则。验证：`go test ./internal/migration ./internal/app ./internal/foodrecord/handler -run "TestSubmitPackagedNutritionLabelTask|^$" -count=1` 通过，目标 diff-check 仅 CRLF warning。当前 `backend/app-config.yaml` 指向远程 `154.8.205.78:5432 / food-link / public`，需用户确认后运行 `cd backend && go run ./cmd/migration -config-dir .`。

- 零食补库入口已从结果页内编辑/提交改为独立页面：结果页“添加零食”写入 `packagedFoodEditDraft` 后跳转 `packageExtra/pages/packaged-food-edit/index`；新页面允许用户修改品牌、名称、重量和每 100g 热量/蛋白质/碳水/脂肪/膳食纤维/糖/钠，再调用 `POST /api/packaged-food` 保存。保存成功后写入 `packagedFoodEditSaved` 并返回结果页，结果页据此把对应 item 标记为 `packaged_food_library`，隐藏补库提示。验证：前端 eslint 与目标 diff-check 通过；`weapp-devtools` 3001/9420 自动化端口仍未连接，未能截图/交互验证。
- 补充零食独立页已新增“更多营养成分”折叠区，默认收起；可填写饱和脂肪、胆固醇、钾钙铁镁锌、维生素 A/C/D/E/K、B1/B2/B6/B12、烟酸、叶酸，并随 `createPackagedFood` payload 保存。验证：目标前端 eslint 与 diff-check 通过；`weapp-devtools` 3001/9420 仍未连接，运行态验证阻塞。
- 补充零食页已增加拍照识别营养成分表：决策为复用现有 Doubao 视觉模型配置，不新增独立 LLM key/config。后端新增 `POST /api/packaged-food/nutrition-label/recognize`，由 `FoodNutritionService.RecognizePackagedNutritionLabel` 调用 Doubao 视觉客户端把图片结构化为每 100g 营养字段；前端新增 `recognizePackagedNutritionLabel`，页面按钮复用隐私授权、图片压缩和 `uploadAnalyzeImageFile`，识别后回填基础和更多营养字段。验证：定向 Go 测试、前端 eslint、diff-check 通过；需重启后端生效，`weapp-devtools` 自动化端口仍未连接。
- 用户指出营养成分表识别会调用 LLM API，同步等待可能很慢；已改为异步 worker 任务。新增 `analysis_tasks.task_type=packaged_nutrition_label`，复用现有 `TaskRepo`、task queue、worker claim/lease/complete/fail 机制；`POST /api/packaged-food/nutrition-label/submit` 创建任务并返回 `task_id`；worker 处理时复用 Doubao 视觉客户端识别并把结果写入 `result.nutrition`；前端上传图片后提交任务并轮询 `getAnalyzeTask`，done 后回填补充零食表单。验证：handler/service/worker/app 定向测试、前端 eslint、diff-check 通过；需重启后端/worker 后生效。

- 零食拍照分析补充后端优先级：提示词要求 item 带 `type`；后端 `db_first` 若判断为零食/预包装食品，先查 `packaged_food_library`，命中返回 `nutrition_source=packaged_food_library`，未命中继续走普通食物库和 DeepSeek fallback。前端只在 snack-like 且未命中 packaged food library 时显示补库提示。

- 实现拍照结果页零食补库入口：后端新增 `POST /api/packaged-food`，把用户提交的零食/预包装食品名称、重量和营养成分按 `normalized_name` 幂等保存到 `packaged_food_library`，并写入 `packaged_food_aliases`；前端结果页保留 item 的 `type/category/nutrition_source/unit_nutrition_per_100g` 元数据，识别到零食且未命中 `packaged_food_library` 时展示“识别为零食 / 添加零食”提示，弹层可编辑品牌、名称、重量、热量、蛋白质、碳水、脂肪、膳食纤维、糖、钠并提交。验证：handler 定向测试、handler/service/app 编译级测试、结果页与 API eslint、目标 diff-check 均通过；`mrc where --port 3001/9420` 仍因微信开发者工具自动化端口未连接失败，未能截图/交互验证。

- 用户调整“用户 ID”展示方案：不再在“我的”页头像昵称区直接显示完整 ID，只保留“复制用户ID”按钮；完整用户 ID 移到“编辑资料”页展示，并继续提供复制按钮。已修改 `src/pages/profile/index.tsx`、`src/pages/profile/index.scss`、`src/packageExtra/pages/profile-settings/index.tsx`、`src/packageExtra/pages/profile-settings/index.scss`。编辑资料保存时保留本地 `userInfo.id`，避免后续展示/复制丢失。验证：`npx eslint src/pages/profile/index.tsx src/packageExtra/pages/profile-settings/index.tsx --max-warnings 0` 通过；目标 `git diff --check` 通过，仅 CRLF warning；`mrc where --port 3001/9420` 均因微信开发者工具自动化端口未连接失败，未能截图/交互验证。

- 用户要求小程序登录后在「我的」页显示自己的用户 ID，并加复制按钮，用于配合 Jaeger 按 `user.id` / `enduser.id` 筛选 trace。已修改 `src/pages/profile/index.tsx`：新增 `userId` 状态，登录后先用本地 `user_id` 兜底，`getUserProfile()` 成功后用 `apiUserInfo.id` 更新；头像昵称区域下方展示 `用户ID`、单行省略的 ID 和 `复制` 按钮；点击复制调用 `Taro.setClipboardData`。已修改 `src/pages/profile/index.scss`：新增浅色/深色样式，避免长 UUID 撑开布局。验证：`npx eslint src/pages/profile/index.tsx --max-warnings 0` 和目标 `git diff --check` 均通过；`mrc where --port 3001/9420` 仍因微信开发者工具自动化端口未连接失败，未能截图/交互验证。

- 用户要求给 trace 加例外：`/api/health` 健康检查不需要生成 trace，因为 Jaeger 里目前几乎全是健康检查。已在 `backend/internal/app/app.go` 将 `otelgin.Middleware(cfg.App.Name)` 改为 `otelgin.Middleware(cfg.App.Name, otelgin.WithFilter(shouldTraceHTTPRequest))`，其中 `shouldTraceHTTPRequest` 对精确路径 `/api/health` 返回 false，其它请求继续 trace。新增 app 单测覆盖健康检查跳过和普通路由保留。验证：`cd backend && go test ./internal/app -run 'TestShouldTraceHTTPRequest|TestAppPackage' -count=1`、`go test ./internal/auth -count=1` 均通过。重启后生效。

- 用户提出：虽然小程序报错时控制台已打印 trace_id，但普通用户无法打开微信小程序控制台，只有体验版能打开，所以希望把用户 ID 关联到 trace，直接用 Jaeger tags 筛选。已确认现状：HTTP 入口通过 `otelgin` 建 span，`RequestID` 返回 `X-Trace-Id`，分析/worker 链路已有 `analysis.user_id` 等 tag，但普通登录请求此前没有把用户写入 span。已修改 `backend/internal/auth/middleware.go`：`RequireJWT` / `OptionalJWT` JWT 解析成功后设置 `user.id` 与 `enduser.id` tag；不写 openid/unionid。验证：`cd backend && go test ./internal/auth -count=1` 与 `go test ./internal/app -run '^$' -count=1` 均通过。重启后新 trace 可用 `user.id=<用户UUID>` / `enduser.id=<用户UUID>` 筛选 HTTP 请求，分析 worker 可继续用 `analysis.user_id=<用户UUID>`。

- 用户执行 `npm run dev:backend` 时，Go 编译报错 `internal/analyze/service/analyze_service.go:27:2: no required module provides package go.uber.org/zap`。已定位为 `backend/internal/analyze/service/analyze_service.go` 残留旧 `zap` 日志写法，而当前项目日志基础设施已统一迁移到 `backend/pkg/logger` + `log/slog`，`backend/go.mod` 中也没有 `zap` 依赖，所以后端在编译阶段直接失败。

- 已将该文件中的 `go.uber.org/zap` 导入和 `zap.String/Int/Bool/Duration/Any/Error` 全部改为 `slog.*` / `logger.Err(...)`，并执行 `gofmt -w backend/internal/analyze/service/analyze_service.go`。验证：`cd backend && go test ./internal/analyze/service ./internal/app -run '^$' -count=1` 通过。当前未替用户启动常驻进程，需用户自行重新运行 `npm run dev:backend` 验证启动恢复。

- 用户随后贴出会员支付 500 日志：`/api/membership/pay/create` 在写表 `pro_membership_payment_records` 时触发 `notify_payload` 非空约束，错误为 `ERROR: null value in column "notify_payload" ... SQLSTATE 23502`。已定位为数据库约束层问题：`MembershipService.CreatePayment` 创建支付单时只填了 `Extra`，没有填 `NotifyPayload`；repo 直接 `Create` 导致 nil map 被写成 NULL。已在 `backend/internal/membership/repo/membership_repo.go` 增加统一兜底，创建支付单时若 `NotifyPayload`/`Extra` 为 nil 自动补成空 map；新增回归测试 `TestMembershipRepo_CreatePaymentInitializesNonNullJSONFields`。验证：`go test ./internal/membership/repo ./internal/membership/service ./internal/app -run '^$' -count=1` 通过；repo 运行型 sqlite 测试仍被本机 `CGO_ENABLED=0` 阻塞。需重启后端后再重试支付创建接口。

## 2026-05-21 - 后端配置接入 Infisical
- Task: 用户希望后端启动时通过 Go SDK 自动从 Infisical 云端拉取配置，本地只保留 Infisical bootstrap 和环境相关配置，不使用 Infisical CLI 或额外环境注入命令。
- Status: implemented_static_verified_restart_required
- Change:
  - `backend/pkg/config` 新增 `infisical` bootstrap 配置，支持 Universal Auth：`enabled/site_url/project_id/project_slug/environment/secret_path/client_id/client_secret/include_imports/recursive/expand_secret_references`。
  - `config.Load()` 现在先读默认值、legacy env、本地 YAML 保护项，再在 `infisical.enabled=true` 时用 `github.com/infisical/go-sdk` 登录并拉取 secrets，最后合并为现有 `Config`。
  - Infisical secret key 支持三种命名：优先使用点号路径式 key 如 `database.host` 或 `external.doubao_api_key`；继续兼容旧环境变量名如 `POSTGRESQL_HOST`、`DOUBAO_API_KEY`、`WORKER_COUNT`，以及双下划线路径式 key 如 `database__host`。
  - `worker.count` 现在可以来自本地 `config.yaml` 或 Infisical；本地 YAML 仍兼容，Infisical 云端值会覆盖本地业务配置。
  - `backend/config-example.yaml` 已补充最小 Infisical bootstrap 示例。
- Verification:
  - `go test ./pkg/config -count=1` passed.
  - `go test ./internal/app -run '^$' -count=1` passed.
  - `go build -o $env:TEMP\food-link-server-infisical.exe ./cmd/server` passed.
  - `git diff --check -- backend/pkg/config/config.go backend/pkg/config/config_test.go backend/config-example.yaml backend/go.mod backend/go.sum` passed with only CRLF warnings.
- Next step:
  - 在真实 `backend/config.yaml` 中只保留 `infisical` bootstrap 和必要本地环境项，真实业务配置迁移到 Infisical 网页后，重启后端生效。

### Follow-up: 配置源改为 local / infisical 互斥
- 用户要求配置源必须显式声明云端或本地，云端模式就是全云端，本地模式就是全本地，不要本地业务配置被云端覆盖或兜底的混合设计。
- 已新增顶层 `config_source`，默认 `local`；可选值仅 `local` / `infisical`。
- `config_source: local`：沿用本地 YAML 业务配置，兼容现有 legacy env 与本地 YAML 保护项。
- `config_source: infisical`：本地 YAML 只读取 `infisical` bootstrap；业务配置全部从 Infisical secrets 构建，本地 `database/worker/external/...` 等业务字段不会参与合并或兜底。
- 新增测试覆盖：Infisical 空 secrets 时即使本地 YAML 有 `worker.count` 也必须失败，证明云端模式不吃本地业务配置。

### Follow-up: 拆分 app-config.yaml 与 infisical-config.yaml
- 用户希望不要再用一个 YAML 同时承载本地和 Infisical 配置。
- 已调整配置加载顺序：优先读取 `backend/infisical-config.yaml`（必须 `config_source: infisical`），其次读取 `backend/app-config.yaml`（必须 `config_source: local`），最后兼容旧 `backend/config.yaml`。
- 云端模式下 `infisical-config.yaml` 只作为 Universal Auth/bootstrap；业务运行配置仍然必须全部来自 Infisical secrets。
- 本地模式下 `app-config.yaml` 承载全部本地业务配置。
- 新增 `backend/infisical-config-example.yaml`，`backend/config-example.yaml` 改为本地 app config 示例。
- Verification: `go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1`、`go build -o $env:TEMP\food-link-server-infisical-split.exe ./cmd/server` 均通过；diff-check 仅 CRLF warnings。

### Follow-up: 用 .env 作为唯一配置源选择入口
- 用户确认最终方案：`.env` 只放 `CONFIG_SOURCE=local|infisical`，没有默认值；`local` 只读 `app-config.yaml`，`infisical` 只读 `infisical-config.yaml` 并从云端拉全部业务配置。
- 已移除旧的按文件存在性自动判断和旧 `config.yaml` 自动兼容路径；`.env` 缺失或 `CONFIG_SOURCE` 缺失/非法会直接启动失败。
- `backend/config-example.yaml` 现在作为 `app-config.yaml` 模板；新增 `backend/infisical-config-example.yaml` 作为 `infisical-config.yaml` 模板；新增 `backend/.env.example`。
- `backend/.gitignore` 新增忽略 `app-config.yaml` 与 `infisical-config.yaml`，避免本地/云端 bootstrap 密钥入库。
- Verification: `go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1`、`go build -o $env:TEMP\food-link-server-env-source.exe ./cmd/server` 均通过；diff-check 仅 CRLF warnings。

- 食物识别链路新增 trace 绑定排障日志：`backend/internal/analyze/service/analyze_service.go` 在视觉模型返回、db_first 营养回算开始/完成、DeepSeek fallback 开始/完成处同时写 `logger.WithTrace(ctx)` 中文结构化日志和 `apm.AddEvent`。item 摘要限制最多 12 条，只包含 name、weight、calories/P/C/F、nutrition_source、resolve_status、matched_food_name、is_unresolved、suggested_ratio/source，不记录图片 URL、完整请求体或 token。验证：analyze service 定向测试、空运行编译级测试、worker sanitize 编译级测试通过；diff-check 仅 CRLF warning。重启后端/worker 后可在 Jaeger trace 事件里查看 `视觉模型识别结果已返回`、`营养库优先回算开始`、`营养库优先回算完成`，用于定位多图热量 0 是视觉结果缺营养、营养库未命中还是 DeepSeek fallback 未生成。

### Follow-up: Kubernetes CONFIG_SOURCE 环境变量不要求 .env 文件
- 用户部署 `foodlink-dev` 时 Pod 已通过 env 设置 `CONFIG_SOURCE=local`，但镜像内无 `.env` 导致启动失败：`load config: open .env: no such file or directory`。
- 已修正配置源读取顺序：优先读取进程环境变量 `CONFIG_SOURCE`；只有未设置时才读取 `backend/.env`；两者都没有才启动失败。
- 这匹配 Kubernetes/容器部署习惯：Deployment env 可以直接声明配置源，`.env` 只是本地开发便利文件。
- 新增测试覆盖无 `.env` 但进程 env 存在的场景。
- Verification: `go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1`、`go build -o $env:TEMP\food-link-server-config-env.exe ./cmd/server` 均通过。

- 2026-05-21: 分析等待页失败态已展示 traceId。`getAnalyzeTask` 会把成功响应头 `X-Trace-Id` / `X-Request-Id` 附加到 `AnalysisTask`，失败页优先展示任务体或 payload 中的 traceId，兜底展示本次轮询请求 traceId；占位值 `no-trace-id/null/undefined` 不展示。静态验证：`npx eslint src/packageExtra/pages/analyze-loading/index.tsx src/utils/api.ts --max-warnings 0` 通过，目标 `git diff --check` 仅 CRLF warning。

- 2026-05-21: 后端 HTTP/应用错误日志已补充 `trace_id/request_id/host_name`。`RequestLogger()` 的 `请求完成/请求失败` 会输出这些字段，`response.Error()` 的 `应用错误`、参数错误、未处理错误也会输出这些字段；`appendTraceAttrs` 已避免重复输出同名 trace 字段。验证：`go test ./internal/common/middleware ./internal/common/response ./pkg/logger ./internal/app -run '^$|TestRequestID|TestLogger' -count=1` 通过，目标 `git diff --check` 仅 CRLF warning。

- 2026-05-21: 分析链路已把 taskId 作为统一关联键。HTTP handler 在提交成功、继续精准任务、GET/PATCH/DELETE `/api/analyze/tasks/:task_id` 时设置 Gin context 和当前 HTTP span tag `analysis.task_id`；`RequestLogger` 和 `response.Error` 会输出 `analysis.task_id` 与兼容字段 `task_id`；提交日志与 worker 关键日志也补齐 `analysis.task_id`。后续可在日志平台按 `analysis.task_id=<taskId>` 查提交、轮询、worker 认领/处理/完成/失败日志，在 Jaeger 按同名 tag 查相关 span。验证：目标 handler/worker/common/logger/app 测试通过，service 编译级通过；service 运行型 sqlite 测试受本机 `CGO_ENABLED=0` 阻塞。

- 2026-05-21: 零食库查询改为完全由模型 `type` 决定。已删除后端 `snackAnalyzeKeywords` 关键词兜底，不再按“薯片/饼干/零食/净含量”等名称或 OCR 关键词推断零食；普通、精准、轻量、分组规划、文字和文字 db_first prompt 均要求 item 输出 `type`，取值口径为 `normal` / `snack` / `packaged`。`db_first` 只有在模型显式输出 `snack/packaged` 或兼容预包装取值时才查 `packaged_food_library`，否则走普通食物库和 DeepSeek fallback。日志 item 摘要新增 `type`。验证：`go test ./internal/analyze/service -run "TestModelDeclaredPackagedFood|TestBuildPrompt|TestBuildStandardImageHybrid|TestBuildAnalyzeResponse" -count=1` 通过；`git diff --check` 通过，仅 CRLF warning；sqlite repo 型测试受本机 cgo/gcc 环境阻塞。
- Infisical 配置迁移建议：当前 Go 后端实现通过 Infisical Go SDK 拉取一组 secrets，再按 secret key 映射成 Viper 配置树；推荐在 Infisical 中维护扁平 key/value（优先 `database.host` / `external.doubao_api_key` 这种点号路径式 key，兼容 `database__host` 和旧环境变量名），不要把整份业务 YAML 放成单个 secret。`wechat_pay.private_key` / `public_key` 可作为多行 secret value 保存；`task_queue.brokers` 可用逗号分隔字符串或后续再改代码支持结构化数组。用户在聊天中贴出的数据库、COS、LLM、微信支付等密钥需要按实际暴露范围评估轮换。
- 已将 `backend/infisical-config.yaml` 与 `backend/infisical-config.example.yaml` 的英文注释改为更详细的中文说明：解释 `project_id` 与 `project_slug` 的区别和获取位置、`environment` 对应 Infisical 环境列、`secret_path`/`include_imports`/`recursive`/`expand_secret_references` 的含义，以及 Universal Auth 机器身份凭证的用途。验证：`cd backend && go test ./pkg/config -count=1` 通过。
- Infisical bootstrap 暂时只保留 key/value 基础拉取能力：已从 `backend/infisical-config.yaml` 和 `backend/infisical-config.example.yaml` 移除 `include_imports`、`recursive`、`expand_secret_references`，并删除代码里 `include_imports=true` 与 `expand_secret_references=true` 的默认值；不写时三者按 Go bool 零值 false 传给 SDK，后续需要 imports、递归目录或 secret 引用展开时再显式加回 YAML。验证：`cd backend && go test ./pkg/config -count=1` 通过。
- Infisical/配置加载排障日志已加入 `backend/pkg/config/config.go`：启动时记录 CONFIG_SOURCE 来源（进程环境变量或 `.env`）、实际读取的配置文件、Infisical login/list URL、project/environment/secret_path、拉取 secret 数量、原始 secret key 到配置 key 的映射，以及最终配置 key 列表；所有 value 统一经 `maskValue` 脱敏，不原样输出。验证：`cd backend && go test ./pkg/config -count=1`、`go test ./pkg/config ./internal/app -run '^$' -count=1` 通过，`git diff --check` 仅 CRLF warning。
- 配置日志字段语义已修正：`Infisical 启动配置文件已读取` 现在只统计真实 YAML 文件内容，字段名为 `config.file.key_count` / `config.file.keys`；合并默认值、Infisical secrets 和运行时配置后的最终快照继续使用 `config.key_count` / `config.keys`；Infisical 网页 secrets 数量对应 `infisical.secret_count`，不要用 bootstrap 文件 key count 对比云端 secrets 数量。新增测试 `TestLogConfigFileSnapshotOnlyLogsFileKeys`。验证：`cd backend && go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1` 通过。

## 2026-05-21 - 后端配置中心替换为 Apollo
- Task: 用户要求弃用 Infisical，删除原有相关代码和文件，改为 Apollo 云端配置，确认 Go SDK 可用。
- Status: implemented_static_verified_restart_required
- Change:
  - `backend/pkg/config` 从 `github.com/infisical/go-sdk` 替换为 `github.com/apolloconfig/agollo/v5`。
  - `CONFIG_SOURCE` 现在只支持 `local` 和 `apollo`；`apollo` 模式读取 `backend/apollo-config.yaml`，再从 Apollo namespaces 拉取全部业务配置。
  - 新增 `backend/apollo-config.example.yaml`；删除 `backend/infisical-config.yaml` 和 `backend/infisical-config.example.yaml`；`.env` 改为 `CONFIG_SOURCE=apollo`；`.gitignore` 改为忽略 `apollo-config.yaml`。
  - Apollo key 映射复用原有扁平 key 方案：点号路径、双下划线路径、旧环境变量式 key 都兼容。
- Verification:
  - `go test ./pkg/config -count=1` passed.
  - `go test ./internal/app -run '^$' -count=1` passed.
  - `go build -o $env:TEMP\food-link-server-apollo.exe ./cmd/server` passed.
- Next step:
  - 填写真实 `backend/apollo-config.yaml` 的 `config_server_url/app_id/cluster/namespaces/access_key_secret` 后，重启后端生效。

- 2026-05-21: Apollo 示例配置已收敛为中文最小模板。
  - `backend/apollo-config.example.yaml` 现在只保留 `apollo.config_server_url` 和可选 `apollo.access_key_secret`。
  - 代码默认值已补齐：`app_id=food-link`、`cluster=dev`、`namespaces=application`、`enable_backup=true`、`backup_config_path=logs/apollo-cache`、`must_start=true`、`sync_timeout_seconds=5`；如需覆盖再写入 YAML。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1` 通过。

- 2026-05-21: Apollo 示例配置调整为“短但可人工核对”。
  - `backend/apollo-config.example.yaml` 保留 `config_server_url`、`app_id`、`cluster`、`namespaces` 和可选 `access_key_secret`；备份路径、must_start、同步超时等低频项继续走代码默认值。
  - 验证：`go test ./pkg/config -count=1` 通过。

- 2026-05-21: Apollo `application` namespace 支持单个 YAML `content` 配置。
  - 现象：用户在 Apollo 上配置整段 YAML，虽然 YAML 内有 `worker.count`，但后端报缺少 `worker.count`。
  - 原因：agollo 只有 namespace 名带 `.yaml/.yml` 时才自动解析 YAML；默认 `application` namespace 返回的单个 `content` key 不会自动展开，后端原先只看到 `content`，看不到 `worker.count`。
  - 修复：`backend/pkg/config` 在 Apollo key 为 `content` 且内容像 YAML 时，主动用 Viper 解析并扁平化合并到配置树，兼容默认 `application` namespace 放整段 YAML 的用法。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1`、`go build -o $env:TEMP\food-link-server-apollo-yaml.exe ./cmd/server` 通过。

- 2026-05-21: Apollo namespace 名未带 `.yaml` 时增加自动后缀探测。
  - 现象：本地启动日志显示 `apollo.namespaces=local-config`、`apollo.config_count=0`，随后缺少 `worker.count`。
  - 判断：Apollo 上很可能创建的是 YAML namespace `local-config.yaml`，但 bootstrap 写成 `local-config`；agollo 读取 `local-config` 返回空，所以没有任何配置可解析。
  - 修复：当 namespace 未带 `.yaml/.yml/.properties` 且读取 0 条配置时，后端会自动尝试 `<namespace>.yaml` 和 `<namespace>.yml`；命中后记录中文 Warn 日志，提示 resolved namespace。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1`、`go build -o $env:TEMP\food-link-server-apollo-namespace.exe ./cmd/server` 通过。

- 2026-05-21: Apollo 空配置错误提示已前置。
  - 用户截图显示 `app-config` YAML namespace 页面右上角仍有“发布”按钮，说明配置可能未发布；Apollo 客户端只能读取已发布配置。
  - 后端现在若 Apollo 拉取 0 条配置，会直接报 `apollo returned no config`，并带上 app_id、cluster、namespaces，提示检查 namespace 是否存在、是否已发布、access_key_secret 是否正确，不再继续落到 `worker.count` 二级错误。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1` 通过。
