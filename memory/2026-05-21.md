# 2026-05-21

- 用户要求给 trace 加例外：`/api/health` 健康检查不需要生成 trace，因为 Jaeger 里目前几乎全是健康检查。已在 `backend/internal/app/app.go` 将 `otelgin.Middleware(cfg.App.Name)` 改为 `otelgin.Middleware(cfg.App.Name, otelgin.WithFilter(shouldTraceHTTPRequest))`，其中 `shouldTraceHTTPRequest` 对精确路径 `/api/health` 返回 false，其它请求继续 trace。新增 app 单测覆盖健康检查跳过和普通路由保留。验证：`cd backend && go test ./internal/app -run 'TestShouldTraceHTTPRequest|TestAppPackage' -count=1`、`go test ./internal/auth -count=1` 均通过。重启后生效。

- 用户提出：虽然小程序报错时控制台已打印 trace_id，但普通用户无法打开微信小程序控制台，只有体验版能打开，所以希望把用户 ID 关联到 trace，直接用 Jaeger tags 筛选。已确认现状：HTTP 入口通过 `otelgin` 建 span，`RequestID` 返回 `X-Trace-Id`，分析/worker 链路已有 `analysis.user_id` 等 tag，但普通登录请求此前没有把用户写入 span。已修改 `backend/internal/auth/middleware.go`：`RequireJWT` / `OptionalJWT` JWT 解析成功后设置 `user.id` 与 `enduser.id` tag；不写 openid/unionid。验证：`cd backend && go test ./internal/auth -count=1` 与 `go test ./internal/app -run '^$' -count=1` 均通过。重启后新 trace 可用 `user.id=<用户UUID>` / `enduser.id=<用户UUID>` 筛选 HTTP 请求，分析 worker 可继续用 `analysis.user_id=<用户UUID>`。

- 用户执行 `npm run dev:backend` 时，Go 编译报错 `internal/analyze/service/analyze_service.go:27:2: no required module provides package go.uber.org/zap`。已定位为 `backend/internal/analyze/service/analyze_service.go` 残留旧 `zap` 日志写法，而当前项目日志基础设施已统一迁移到 `backend/pkg/logger` + `log/slog`，`backend/go.mod` 中也没有 `zap` 依赖，所以后端在编译阶段直接失败。

- 已将该文件中的 `go.uber.org/zap` 导入和 `zap.String/Int/Bool/Duration/Any/Error` 全部改为 `slog.*` / `logger.Err(...)`，并执行 `gofmt -w backend/internal/analyze/service/analyze_service.go`。验证：`cd backend && go test ./internal/analyze/service ./internal/app -run '^$' -count=1` 通过。当前未替用户启动常驻进程，需用户自行重新运行 `npm run dev:backend` 验证启动恢复。

- 用户随后贴出会员支付 500 日志：`/api/membership/pay/create` 在写表 `pro_membership_payment_records` 时触发 `notify_payload` 非空约束，错误为 `ERROR: null value in column "notify_payload" ... SQLSTATE 23502`。已定位为数据库约束层问题：`MembershipService.CreatePayment` 创建支付单时只填了 `Extra`，没有填 `NotifyPayload`；repo 直接 `Create` 导致 nil map 被写成 NULL。已在 `backend/internal/membership/repo/membership_repo.go` 增加统一兜底，创建支付单时若 `NotifyPayload`/`Extra` 为 nil 自动补成空 map；新增回归测试 `TestMembershipRepo_CreatePaymentInitializesNonNullJSONFields`。验证：`go test ./internal/membership/repo ./internal/membership/service ./internal/app -run '^$' -count=1` 通过；repo 运行型 sqlite 测试仍被本机 `CGO_ENABLED=0` 阻塞。需重启后端后再重试支付创建接口。
