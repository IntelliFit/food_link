- 运动热量估算：`exercise_llm.py` 已改为 Instructor + Pydantic；新增 `tests/unit/test_exercise_llm.py`；`backend/requirements.txt` 增加 `instructor`、`openai`。
- 部署后端需重装依赖；可选 `EXERCISE_CALORIES_INSTRUCTOR_MODE=json|md_json|tools`（默认 `md_json`）。
- 分享海报 **2.0.12**：`src/utils/poster.ts` 深色页底 + 米白一体卡、总热量在卡内；`computePosterHeight` 与绘制对齐；`package.json` 已 bump；`npm run build:weapp` 通过。微信体验版上传需在本地执行 `npm run build:weapp:preview` 后开发者工具 `cli upload`（需已登录）。
