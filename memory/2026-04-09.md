- 运动热量估算：`exercise_llm.py` 已改为 Instructor + Pydantic；新增 `tests/unit/test_exercise_llm.py`；`backend/requirements.txt` 增加 `instructor`、`openai`。
- 部署后端需重装依赖；可选 `EXERCISE_CALORIES_INSTRUCTOR_MODE=json|md_json|tools`（默认 `md_json`）。
- 分享海报 **2.0.12**：`src/utils/poster.ts` 深色页底 + 米白一体卡、总热量在卡内；`computePosterHeight` 与绘制对齐；`package.json` 已 bump；`npm run build:weapp` / `build:weapp:preview` 通过；已执行 `cli upload` 上传微信（需在公众平台将对应版本设为体验版）。
- 圈子评论区：乐观插入评论 + 立即清空输入；`communityPostComment` 后台完成后再替换/失败回滚；发送区 `icon-send` + 进行中 spinner；`saveToCache` 不持久化 `_is_pending`。
- 记录详情分享海报：`poster.ts` 画布外透明、米白单圆角卡顶格；日期压印在食物图左下角；下区左上总摄入 + 宏量标签色同首页 `MacrosSection`；`canvasToTempFilePath` 设 `transparent`；预览弹窗内容区透明、海报图无外边距。
- 积分充值写 `pro_membership_payment_records` 若 `23514 duration_months_check`：在 Supabase 执行 `backend/sql/migrate_pro_membership_payment_duration_months_allow_zero.sql`（`duration_months=0` 为 `points_recharge` 非订阅语义）。
- 若 `23503 plan_code_fkey` 或 `23514 membership_plan_config_duration_months_check`：执行 `backend/sql/migrate_membership_plan_config_points_recharge_fk.sql`（先放宽 `membership_plan_config.duration_months` CHECK，再插入 `points_recharge`）。
