# 2026-08-20

- 全国高校食堂目录继续按一手来源、正式校区、唯一物理父食堂、具体正常餐饮楼层与 2024—2026 同父运营双门槛扩充；招采、装修、建筑总层数、监管地址和活动地点不单独激活。
- 永州批次已发布湖南科技学院校本部桃园食堂2F、松园食堂2F、荷园食堂1F/2F；提交 `b1eff803` 已推送并部署。发布后 development 为 865 Active 校区、2424 Active 父食堂、36 Active 窗口、覆盖 473 校，来源 2558 条（2299 approved、226 pending、33 rejected）。
- 怀化严格 0 报告提交 `08c6ae68`、邵阳严格 0 报告提交 `8a67b83f` 均已推送 `origin/dev`，两批无后端数据或代码变更。
- 湘西批次严格 0：完整审计吉首大学和湘西民族职业技术学院，州内 4 个物理教学地点均缺具体楼层正常售餐闭环；未写 development，报告提交 `1a699c2a` 已推送 `origin/dev`。
- 湘西提交曾因共享工作区并发切分支误落本地 `main`，已精确推送到 `dev` 并恢复本地 `main`/`dev` 指针；其他任务未发布提交 `164d0c72` 保存在本地 `refs/keep/dev-unpublished-20260820-201133`，未夹带推送。
- main 后端发布候选为本地 `6748f854`，基于 `origin/dev@1a699c2a` 并修复两项后端发布门禁；相关提交钩子测试通过。候选相对 `origin/main@534aa1c2` 含 197 个提交及前端/Admin/文档等整体变更，远端 main 推送因范围需要明确授权而未执行。候选还新增两张包装食品纠错表，正式镜像部署前必须先获得用户对生产迁移和整体 main 发布的明确确认。
- 用户已授权候选整体发布、生产迁移和后端部署，但发布前两轴审查发现包装食品部分纠错会把遗漏字段以 0/NULL 全量写回的 Blocker，以及重复/并发审批无 pending/锁/版本保护的 High。按门禁停止 main 推送、生产迁移和镜像部署；`origin/main` 保持 `534aa1c2`。需先修复字段 presence、审批幂等并补部分纠错/重复并发审批/迁移回归测试，再重新审查。
- 衡阳批次已严格发布 3 校、4 校区、6 个物理父食堂、7 条楼层关系，并撤回衡师东校区新食堂弱证旧 Active；development 更新为 866 Active 校区、2424 Active 父食堂、36 Active 窗口、覆盖 473 校，来源 2568 条（2306 approved、226 pending、36 rejected）。提交 `5b872efc` 已推送 `origin/dev`，开发后端镜像摘要 `sha256:ed6f903c4b4b06872ada455056bdc9545c805a0188cb6a0e54f76ba6b42847d0` 已推送。
- 衡阳提交前本地 `dev` 上的无关提交链已保存在 `refs/keep/dev-unpublished-hengyang-20260820`，未随衡阳批次推送；本地 `dev` 已对齐 `origin/dev@5b872efc`。
- main 后端发布 Blocker/High 已修复：纠错请求只 patch 显式 JSON 字段；审批事务使用提案/食品行锁、pending 条件更新和字段级旧值校验，重复/并发/过期审批返回 409。提交 `a9868a2e` 已推送 `origin/main`。
- 已先成功执行生产数据库幂等迁移，再从 `a9868a2e` 干净快照推送 linux/amd64 `foodlink:main`；镜像清单 digest `sha256:18b194c1917c400fb20cddf161c156755395ed0bb655618a6a5345ba32c7fa33`。线上 health 200，纠错路由连续 6 次返回预期未认证 401，确认新版本已更新。
