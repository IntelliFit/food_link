# 2026-08-14

## 同事体验版与我方热修复关系

- 我方提交 `315e26ed` 未进入 `dev/main`；同事在 main 独立修复识别记录、互动消息和登录 loading。
- 用户决定放弃我方旧分支，以同事当前版本继续开发。Draft PR #96 已关闭，远端 `codex/login-loading-hotfix` 已删除。

## 动态详情持续转圈修复

- 同事体验版对应代码在 `origin/main@5151fcce`，`origin/dev` 尚未包含相关修复，因此本地切换为基于 main 的 `codex/fix-feed-detail-from-teammate`。
- 原工作区未提交的补剂、食物扩库、精准模式和登录热修内容已备份到 stash `before-feed-detail-sync-20260814-032004`，没有混入当前分支。
- `interaction-feed-detail` 原先串行等待 context 和完整评论，再一次性关闭 spinner/渲染整卡；现改为 context 返回即结束 spinner、80ms 后提交主体，完整评论后台补全且失败不阻塞。
- 新增定向回归测试，模拟完整评论 Promise 永不结束，确认动态主体仍显示。Jest 1/1、TypeScript、目标 ESLint、diff check 全部通过。
- 微信开发者工具自动化端口 9420–9422 未开放，未取得运行时截图/点击证据；未擅自启动进程。
- 根目录未跟踪 `NUL` 是约 129KB 的既有文件，安全检查后未删除，也不属于当前修复。

## 基础代谢与每日热量估算审计

- 只读核查确认：健康档案/数据库/API 没有体脂率、去脂体重或肌肉量字段，因此当前没有对高肌肉量的校正。
- 初始档案的 BMR 是历史体重性别公式与 Mifflin–St Jeor 的平均；体重记录同步则覆写成只用体重和性别的旧公式，且 TDEE 固定按四档日常活动乘数计算。
- 初始热量目标 = TDEE 经减脂/增重调整，写入后不随新体重自动重算；14 天规则只会根据体重趋势给出需确认的 ±100 kcal 建议。建议后续以体脂/去脂体重分支与统一计算入口修复。

## main、dev 与动态详情修复完成集成

- 用户本地测试确认动态详情修复正常，允许合入开发分支并把主分支改动同步到开发分支，同时要求保留 dev 上尚未验收的改动。
- 基于 `origin/dev@fd4afa33` 创建合并提交 `95eb74d3`，纳入 `origin/main@5151fcce`；冲突解决保留 dev 的宠物、错误重试等功能，同时采用 main 的识别记录轻量摘要、渐进加载、worker 退避和存储缩略图修复。
- 追加动态详情提交 `8d65c935`，远端 `dev` 已普通快进推送到该提交；确认旧 `origin/dev` 和 `origin/main` 都是它的祖先，没有 force-push 或丢弃 dev 历史。
- 本地主工作区已切换并快进到 `dev@8d65c935`，用户可直接自行启动 watch 验收；既有 stash `before-feed-detail-sync-20260814-032004`、未跟踪 `NUL` 和本日记均保留。
- 微信端最终 44 suites / 133 tests、TypeScript、ESLint通过；移动端 TypeScript、worker 定向测试、Go compile-only/vet 通过。完整数据库型 Go 测试被本机 embedded PostgreSQL 缺失 `initdb` 阻塞。

## 互动消息点赞/评论数量修复
- 用户截图显示“全部”页角标为 17/3，切换点赞和评论后分别变成 20/6。根因是前端用当前分页列表长度统计角标，且评论请求只筛 `comment_received`。
- 后端 `/api/community/notifications` 新增全量 `like_count` / `comment_count`，评论组包含 `comment_received`、`reply_received`、`comment_rejected`；小程序角标改用服务端统计。
- 仓储嵌入式 PostgreSQL 回归测试、服务/handler 目标测试、微信端 typecheck、目标 ESLint、diff-check 通过。
- WeApp DevTools 运行时验证未完成：本机 `mrc` 不存在，3001/9420 均未监听，且未获授权启动常驻开发进程。
- 发布：代码提交 `ae42d52b` 已推送 `origin/main`；`npm run push-docker-ccr` 成功推送 `ccr.ccs.tencentyun.com/littlehorse/foodlink:main`，manifest digest `sha256:b5d0b53487b8faaf7672dc06259a888846c34f5d429c02340be0a033f335f11d`。生产 `/api/health` 返回 200，部署端按现有自动更新机制拉取。

## 圈子动态详情 loading 修复
- 圈子列表正常、详情 API 完成后持续转圈，确认详情页仍保留与互动消息/登录相同的 Android 真机宿主状态提交风险。
- 详情页统一使用 React namespace hooks；完整动态和评论树先在 spinner 后面完成挂载，再由独立 80ms 宿主任务移除 spinner，避免销毁 loading 与创建重详情树同批提交。
- 新增 `[interaction-feed-detail-debug]` 阶段日志，预期顺序为 `load-start`、`context-resolved`、`detail-committed`、`loading-release-scheduled`、`loading-release-committed` 和 `react-commit`；日志不包含动态内容、令牌或完整请求数据。
- TypeScript、目标 ESLint 和 diff-check 通过。WeApp DevTools 自动化因本机无 `mrc` 且 9420/3001 未监听而未完成运行时复验。
