# 2026-03-31

- 用户明确要求把用户总目标热量的默认底层公式从 Mifflin-St Jeor 改成更适合中国人的毛德倩公式。
- 已完成后端切换：
  - `backend/metabolic.py` 的 `calculate_bmr(...)` 改为毛德倩公式
  - `backend/main.py` 的 `update_health_profile(...)` 不再要求必须有身高/生日/年龄才生成 `BMR/TDEE`，只要具备 `性别 + 体重 + 活动水平` 即可
- 当前 `TDEE` 口径保持不变，仍为 `BMR × 活动系数`

- 用户反馈食物分析每日次数限制口径不对，体感上“拍了 2 次就无法继续拍”；已复核当前统计并非“纯拍照成功次数”，而是按 `analysis_tasks` 当天创建量计数，同时包含 `food` 与 `food_text`，失败/未完成任务也会占额。
- 按用户要求，已先临时取消食物分析每日次数限制：
  - `backend/main.py` 新增环境变量开关 `FOOD_ANALYSIS_DAILY_LIMIT_ENABLED`
  - 默认关闭日限检查，不再因 `3/20` 配额拒绝图片/文字分析提交
  - `/api/membership/me` 在限次关闭时返回 `daily_limit/daily_used/daily_remaining = null`

- 用户询问 Supabase Storage 配额超限的处理策略，并提供截图显示当前项目 Free Plan 的 `Storage Size` 已达 `1.181 / 1 GB`。
- 初步代码审查发现 `food-images`、`health-reports`、`user-avatars` 都采用“每次上传生成新对象”的方式，但仓库内仅存在未被调用的 `delete_image_from_storage(...)`，尚未形成成体系的旧文件回收或生命周期治理。
- 前端对食物分析图片已做上传前压缩，目标约 `760 KB`，说明当前更可能是“历史对象持续累积”而不只是“单张过大”。
- 已直接对真实 Supabase 项目做对象与业务引用交叉统计：
  - `food-images`: `2485` 个文件，约 `1113.13 MB`
    - 真正进入 `user_food_records` 的记录图：`1327` 个，约 `531.42 MB`
    - 仅存在于 `analysis_tasks`、未进入记录/公共库的临时分析图：`1041` 个，约 `460.40 MB`
    - 不被任何业务表引用的孤儿图：`107` 个，约 `119.18 MB`
    - 仅在 `public_food_library`、未进用户记录：`10` 个，约 `2.13 MB`
  - `user-avatars`: `404` 个文件，约 `2.09 MB`
    - 当前仍被 `weapp_user.avatar` 引用：`364` 个，约 `1.96 MB`
    - 旧头像残留：`40` 个，约 `0.13 MB`
  - `health-reports`: `17` 个文件，约 `4.95 MB`
    - 仍被 `user_health_documents.image_url` 引用：`14` 个，约 `4.20 MB`
    - 未引用残留：`3` 个，约 `0.75 MB`
  - `icon`: `1` 个文件，约 `0.79 MB`
- 若仅保留“已进入用户记录 + 公共库”的 `food-images`，理论上可从存储里直接释放约 `579.58 MB`（临时图 + 孤儿图），当前最主要矛盾明确在 `food-images` 而不是头像或报告。
- 用户进一步确认：出于保险考虑，当前阶段优先“先压缩”，暂不先删除图片；后续应先做对象 key/URL 不变的保守二次压缩，再视效果处理临时图与孤儿图。
- 已落地 `backend/compress_food_images.py` 批量压缩脚本，并完成小样本 dry-run：`20` 张被引用图片里有 `11` 张值得替换，样本从 `8.18 MB` 预计降到 `1.50 MB`。
- 已实际执行首批线上压缩：`python backend/compress_food_images.py --execute --limit 200`
  - 处理 `200` 张被引用图片
  - 实际替换 `141` 张
  - 跳过 `59` 张，失败 `0`
  - 报告显示首批约释放 `65.01 MB`
  - 报告文件：`backend/reports/food-images-compress-report-20260331T012028Z.json`
- 已按用户指定对单文件 `156d3b2c5a094fcf81b44f4229aade73.jpg` 做定点压缩验证：
  - 执行命令：`python backend/compress_food_images.py --execute --name "156d3b2c5a094fcf81b44f4229aade73.jpg"`
  - 压缩前：`2245767 bytes`，`3072x4096`
  - 压缩后：`193622 bytes`，`960x1280`
  - 单文件约释放 `1.96 MB`
  - 报告文件：`backend/reports/food-images-compress-report-20260331T014529Z.json`
- 用户反馈微信开发者工具上传失败，截图报错 `80051`，提示 `source size 2060KB exceed max limit 2MB`。
- 已定位到开发者工具上传目录为 `dist/`，且此前配置存在两层问题：
  - `project.config.json` 中 `minified=false`、`uploadWithSourceMap=true`
  - 原始 `dist` 中 `.map` 合计约 `3240.53 KB`；即使排除 `.map`，非 map 产物也约 `2076.31 KB`，仍略超主包上限
- 已按“尽量不动业务逻辑”的原则完成减包配置修复：
  - `project.config.json` 改为 `minified=true`、`uploadWithSourceMap=false`
  - `config/prod.ts` 显式启用 `terser`、`csso`，并关闭 `mini.enableSourceMap`
- 已重新执行生产构建 `npm run build:weapp`，构建通过；新 `dist` 约 `1517.69 KB`，且 `.map` 数量为 `0`，说明此次上传超限问题已从构建侧消除。

- 用户继续反馈圈子推荐的排序体验有明显问题：
  - 无论切 `推荐 / 高赞 / 均衡`，至少都必须符合时间顺序
  - 当前体验中旧动态会被顶到新动态前面，用户感知为“时间顺序乱了”
- 已修复后端排序口径：
  - `backend/database.py`
  - `list_friends_feed_records(...)` 与 `list_public_feed_records(...)` 的自定义排序从“推荐分优先、时间次级”改成“时间倒序优先、推荐分次级”
  - 保留推荐能力，但不再允许老内容整体压过新内容
- 本次按项目默认口径未做构建或运行态验证，等待用户直接在圈子页实测排序是否恢复正常

- 用户反馈图片模式二次纠错仍有一个问题：
  - 原结果若有 `4` 个食物，用户只把其中 `1` 个改名，例如 `橘子 -> 橙子`
  - 最终结果却会变成 `5` 个食物，表现为旧 `橘子` 没被替换，反而额外新增了 `橙子`
- 已定位根因：
  - `backend/worker.py` 的 `_apply_image_correction_items(...)` 只按纠错后的 `name` 匹配
  - 改名场景缺少“这条纠错原本对应哪一项”的稳定身份，因此后端只能把新名字当新增项追加
- 已完成修复：
  - `src/pages/result/index.tsx` 二次纠错提交时新增透传 `sourceItemId / sourceName`
  - `src/utils/api.ts` 为 `correctionItems` 和 `FoodItem` 补充对应字段
  - `backend/worker.py` 改为优先按 `sourceItemId`、其次按 `sourceName` 匹配旧项
  - 更正后的结果会保留原 `itemId`，便于后续继续纠错
- 验证：
  - `python -m py_compile backend/worker.py` 通过
  - 最小复现脚本已确认：`4` 项中把第 `3` 项改名后，结果仍是 `4` 项，不再多出一项
- 运行态验证阻塞：
  - 已按项目要求尝试 `weapp-devtools`
  - `mrc where --port 9420` 失败，当前本机未开启微信开发者工具自动化端口，无法完成截图/点击复测

- 用户随后继续反馈：实际体验里二次纠错仍然会把“改名”当成“新增”，说明上一版修复还不够严格。
- 已进一步定位到更深一层根因：
  - `_apply_image_correction_items(...)` 虽然能把“原项 -> 新项”替换上，但仍会把模型本轮额外生成的旧项/重复项继续保留下来
  - 因此当模型输出 `苹果 / 香蕉 / 橘子 / 橙子 / 梨子` 时，最终结果还是可能残留 `5` 项
- 已补第二层修复：
  - 将图片模式二次纠错的结果口径收紧为“`correctionItems` 就是最终完整列表”
  - Worker 最终不再追加任何模型额外生成、但不在纠错清单中的项
  - 同时修正 `_build_item_from_fallback(...)` 中 `itemId` 回写不可达的小 bug
- 追加验证：
  - 用最小复现脚本模拟模型错误返回 `5` 项，其中同时包含旧 `orange_old` 和新 `orange_new`
  - 修复后最终结果被严格收回到 `4` 项：`apple / banana / orange_new / pear`

- 用户继续反馈：现在又出现了另一种体感问题，不再是“多 1 项”，而是“完全没变化，像把原结果又说了一遍”。
- 进一步定位后确认，这不是同一个点：
  - 用户实际操作里很可能是直接在“补充说明”写 `那不是橘子，是橙子`
  - 但旧前端只把这句当普通自由文本说明，结构化 `correctionItems` 仍是原始 4 项
  - 由于本轮后端已经改成“最终严格以 correctionItems 为准”，所以这种写法会表现成“完全没变化”
- 已补第三层修复（前端兼容自然语言改名）：
  - `src/pages/result/index.tsx` 新增轻量解析：
    - 支持 `不是A，是B`
    - 支持 `把A改成B`
  - 提交图片模式二次纠错前，会先把这些明确改名句式同步到结构化纠错列表，再统一下发
  - placeholder 文案也改成了明确示例：`不是橘子，是橙子`
- 验证：
  - `npm run build:weapp` 通过
  - 已确认微信开发者工具自动化端口可连，但当前连接到的并非 `food_link`，而是另一套小程序页面 `training/today-training`
  - 因此这次仍未能在正确应用里完成结果页二次纠错的运行态点击/截图验证

- 用户提出应直接把“第 1 轮到第 2 轮”的所有输入输出日志打全，再基于真实链路分析，不再靠猜。
- 已按这个方向补日志：
  - `backend/main.py`
    - 新增 `FOOD_ANALYSIS_DEBUG` 控制的提交日志
    - 图片提交会打印 `image_submit_request / image_submit_created`
    - 文字提交会打印 `text_submit_request / text_submit_created`
  - `backend/worker.py`
    - 图片/文字分析在原有 `prompt / raw_model_output / final_result` 之外，新增 `task_input`
    - 这样可以直接看到 worker 实际拿到的 `payload.previousResult / payload.correctionItems / additionalContext`
- 校验：
  - `python -m py_compile backend/main.py backend/worker.py` 通过
- 当前新的调试口径：
  - 若用户再次复现异常，优先查看一整条链路：
    - 第一轮结果
    - 第二轮提交 payload
    - worker task_input
    - prompt
    - raw_model_output
    - final_result

- 用户又补了两个圈子交互体验点：
  - 互动消息目前看不到“点赞了你的动态”
  - 评论里的“回复别人”视觉层级不明显，看起来像两条独立评论
- 已完成修改：
  - 后端互动通知补齐 `like_received`
    - `backend/main.py` 点赞接口会在“新增点赞”成功后，为动态作者创建点赞通知
    - `backend/database.py` 的 `add_feed_like(...)` 改为返回是否真的新增点赞，避免重复点赞重复通知
  - 数据库脚本补齐：
    - 更新 `backend/database/feed_interaction_notifications.sql`
    - 新增 `backend/database/migrate_feed_interaction_notifications_add_like_received.sql`
    - 旧库若要收到点赞通知，需要先执行这份 migration
  - 前端通知页补齐“点赞”文案：
    - `src/utils/api.ts`、`src/pages/interaction-notifications/index.tsx`
  - 圈子评论视觉层级增强：
    - `src/pages/community/index.tsx` 改成“作者 / 回复对象 / 独立内容气泡”的渲染结构
    - `src/pages/community/index.scss` 为回复项增加左侧引导线、回复对象标签和差异化气泡
  - 顺手修正了一个交互细节：
    - 点赞通知回到圈子页时只定位对应动态，不会像评论通知那样自动弹出评论输入框
- 本地验证结果：
  - `python -m py_compile backend/main.py backend/database.py backend/worker.py` 通过
  - `npm run build:weapp` 通过
  - 已尝试 `mrc where --port 9420` 做 DevTools 自动化验证，但当前端口未开启，无法完成运行态截图/点击验证

- 用户指出统计页里的 `AI 营养洞察` 不应该在每次打开页面时都触发分析，更合理的是按天缓存，或者由用户主动点击生成。
- 已按“缓存优先、手动刷新”的方向完成收口：
  - 前端统计页移除了“无缓存就自动走 WebSocket 实时生成”的逻辑
  - 改成默认展示缓存结果，并在卡片里明确标注“默认读取缓存，需要时手动更新”
  - 若当前无缓存，则展示空态说明和“生成本周/本月洞察”按钮
  - 若缓存已不是最新数据，只提示“可手动更新”，不再自动重跑模型
- 后端同步调整：
  - `backend/database.py` 新增最近一条 AI 洞察缓存查询
  - `backend/main.py` 的统计汇总接口改为优先返回最近缓存，并返回 `analysis_summary_generated_date / analysis_summary_needs_refresh`
  - 统计洞察相关日期统一按北京时间计算
  - 顺手修正了统计洞察生成里误传内置 `range` 而非 `stats_range` 的参数问题
- 本次验证：
  - `npm run build:weapp` 通过
  - `python -m py_compile backend/main.py backend/database.py` 通过
  - 已尝试 `mrc where --port 9420`，但当前微信开发者工具自动化端口未开启，仍无法完成运行态截图/点击验证

- 用户要求先暂时关闭精准模式，因为还没有完全改好；若用户点击精准模式，需要直接提示“该功能仍在完善中”。
- 已完成前端收口：
  - 新增 `src/utils/execution-mode.ts` 统一管理执行模式开关
  - 分析页点击“精准”只弹提示，不再切换到 `strict`
  - 健康档案问卷/编辑页里的精准模式改为“完善中，暂时不可选”
  - 若老用户档案或历史缓存里仍是 `strict`，分析页默认执行和结果页重新分析都会自动回落到 `standard`
  - 档案查看页文案同步改为“精准模式（完善中，暂未开放）”
- 本次验证：
  - `npm run build:weapp` 通过
  - 已尝试 `mrc where --port 9420`、`mrc errors 20 --port 9420`
  - 当前阻塞：微信开发者工具自动化端口 `9420` 未开启，无法完成点击“精准模式”后的截图与运行态确认

- 用户反馈首页比例显示失真：
  - 三大营养素里 `蛋白质 92%` 看起来像满圈，`碳水 58%` 的圆环也不够可信
  - 早餐 / 午餐卡片只有进度条，没有明确百分比文本
  - 产品口径要求比例文案支持超过 `100%`
- 已完成首页收口：
  - `src/pages/index/index.tsx`
    - 新增首页数值与百分比归一化函数，兜底 `undefined / NaN / 非数字字符串`
    - 热量总进度、三大营养素进度、餐次进度统一为“真实值显示 + 视觉值裁到 100%”
    - 三大营养素卡片移除旧的伪圆环实现，改为百分比徽标 + 横向进度条
    - 按用户最新确认，去掉“目标达成 / 已超目标”文案，不再重复解释状态
    - 餐次卡片新增明确百分比文本，仍保留 `目标/参考 xxx kcal` 文案
  - `src/pages/index/index.scss`
    - 新增营养素百分比徽标、横向进度条、餐次百分比区块样式
- 本次验证：
  - `npm run build:weapp` 曾通过
  - 在去掉“目标达成 / 已超目标”文案后再次执行 `npm run build:weapp`，命中 `vite:esbuild-transpile` 的 `write ENOMEM`，属于本机构建进程内存不足，不是本次首页逻辑报错
  - 已按项目要求读取 `C:\Users\29454\.codex\skills\weapp-devtools\SKILL.md`
  - 已尝试 `mrc where --port 9420`、`mrc errors 20 --port 9420`
  - 当前阻塞：微信开发者工具自动化端口 `9420` 未开启，无法完成首页截图与点击验证

- 用户要求把社区评论审核从当前较慢的模型切到 `openai/gpt-5.4-nano`，并且和 Gemini 热量识别共用同一套 OfoxAI `base_url + api key`，同时把审核放宽一些。
- 已完成后端切换：
  - `backend/worker.py` 的评论审核请求不再走 `qwen-plus + DashScope`
  - 改为走 `https://api.ofox.ai/v1/chat/completions`
  - 复用 `OFOXAI_API_KEY / ofox_ai_apikey`
  - 默认评论审核模型改为 `openai/gpt-5.4-nano`
  - timeout 默认收紧到 `8s`
- 已同步放宽评论审核口径：
  - prompt 明确要求“默认放行，只有明确违规且高把握才拦截”
  - 普通吐槽、轻微负评、简短回复、食物语境玩梗默认放行
  - 后端额外补了一层本地兜底，避免模型把短句评论误判成 `harassment / spam / inappropriate_text / other / politics`
- 验证：
  - `python -m py_compile backend/worker.py` 通过
- 后续提醒：
  - 需要重启 `backend/run_backend.py` 或评论 worker，线上才会真正开始使用 `gpt-5.4-nano`

- 用户明确指出当前页面职责混乱：
  - 分析页点某一天记录会跳到 `record` 页
  - 但 `record` 页本质上是拍照/录入页，不应该再承担按天历史查看
  - 用户希望“历史全部放到分析页”，而“某一天的记录”进入一个独立页面
- 已按这个方向完成前端拆分：
  - 新增 `src/pages/day-record/*` 独立页面，专门展示某一天的饮食明细、总摄入、目标热量和删除入口
  - `src/pages/stats/index.tsx` 的日历点击与“查看当天记录”改为直接跳新页
  - `src/pages/index/index.tsx` 的“查看全部”和日期热力格入口同步改到新页
  - `src/pages/record/index.tsx` 移除了按天历史/日期选择区块，页面只保留拍照识别与文字记录
  - `src/pages/record/index.config.ts` 标题收口为 `记录饮食`
  - `src/app.config.ts` 已注册 `pages/day-record/index`
- 构建与验证：
  - `npm run build:weapp` 通过，产物中已确认存在 `dist/pages/stats/index.*` 和 `dist/pages/day-record/index.*`
  - `mrc where --port 9420` 成功，当前自动化连接到了 `pages/index/index`
  - `mrc errors 20 --port 9420` 返回 `0`
  - 但继续做运行态跳转时：
    - `mrc switchTab /pages/stats/index --port 9420` 返回 `page "pages/stats/index" is not found`
    - `mrc relaunch /pages/day-record/index?date=2026-03-31 --port 9420` 返回 `page not found`
  - 这说明当前微信开发者工具自动化目标里的运行包仍不是最新 `dist`，需要在 DevTools 内手动重新编译/刷新后再继续截图与点击验证

- 用户继续补充一个明确体验点：
  - “当天饮食记录”页里，希望在点进详情前就能先看到当时照片的大概预览，而不是只有文字
- 已完成补充：
  - `src/pages/day-record/index.tsx`
    - 每条记录卡片新增 `imageUrls / previewImage / hasRealImage`
    - 有实物图时展示首张照片缩略图，并支持点击缩略图直接 `previewImage`
    - 多图时在缩略图角标显示 `N 张`
    - 无图时回退到产品 logo 占位，并明确标记“无照片”
  - `src/pages/day-record/index.scss`
    - 新增缩略图容器、角标、预览提示文案样式
- 验证：
  - `npm run build:weapp` 再次通过
  - `mrc where --port 9420` 仍只显示当前连到 `pages/index/index`
  - 由于 DevTools 运行包还未刷新到最新 `dist`，本轮仍未能在自动化里拿到“当天记录页缩略图”的真实截图

- 用户继续追查图片模式二次纠错“改名后没变化 / 变新增”的问题，并要求打开从提交到最终写回的全链路 debug 日志排查。
- 根据用户提供的日志，已明确定位到当前主根因不是模型没改，而是“前端没有把补充说明中的自然语言改名转成结构化 `correctionItems`”：
  - 本次样本里模型 `raw_model_output` 已经把 `炸肉饼` 改成了 `炸牛肉饼`
  - 但提交给后端的 `correctionItems` 仍然是旧名 `炸肉饼`
  - 在后端“最终以 `correctionItems` 为准”的兜底下，最终结果就会被旧清单压回去，表现成“完全没变化”
- 已继续补前端 `src/pages/result/index.tsx`：
  - 自然语言改名识别新增支持 `X 实际上是 Y / X 其实是 Y / X 应该是 Y`
  - 原项匹配从严格全等改为包含匹配，兼容“这个炸肉饼说得太粗糙了，它实际上是牛肉饼”这类带修饰语的句子
- 这轮未额外跑构建；已用 `ReadLints` 检查 `src/pages/result/index.tsx`，无新增报错

- 用户随后追问文字记录链路的二次纠错是否也需要同步修；已判断“需要”，否则图片模式和文字模式的用户心智会继续割裂。
- 已继续落地文字链路修复：
  - `src/pages/result/index.tsx`
    - 文字模式二次纠错也复用“补充说明 -> 结构化改名”逻辑
    - 提交 `submitTextAnalyzeTask(...)` 时不再把 `correctionItems` 置为 `undefined`
    - 会把用户当前确认后的结构化清单和补充说明一起下发
  - `backend/worker.py`
    - 新增 `_apply_text_correction_items(...)`，当前实现按与图片模式一致的规则收口
    - 文字模式在模型返回后，也会按 `correctionItems` 统一名称、数量、顺序与明确重量
    - 文字模式 prompt 中把结构化纠错清单优先级提升到高于上一轮结果
- 本次验证：
  - `python -m py_compile backend/worker.py` 通过
  - `ReadLints` 检查 `src/pages/result/index.tsx` 无新增报错

- 用户继续指出：当前“补充说明/文字纠错”在产品心智上不该只是次级补充，而应是二次纠错的主要参考信息。
- 结合用户提供的新日志再次确认：
  - 当前图片链路里，`additionalContext` 的包装文案仍把自由文本放成“补充说明”
  - 旧规则即使继续补，也仍会不断漏掉新的口语表达，属于方向性错误
- 用户明确要求停止“正则理解纠错语义”这条路线，并指出这套算法本身有问题；这个判断成立。
- 已按“模型主导 + 字段级锁定”重构：
  - `src/pages/result/index.tsx`
    - 删除前端基于正则的自然语言改名主逻辑
    - 新增 `buildStructuredCorrectionItems(...)`
    - 提交时只发送：
      - 原始自由文本纠错说明（最高优先级）
      - 结构化食物清单
      - `nameEdited / weightEdited` 显式字段锁
  - `src/utils/api.ts`
    - 二次纠错请求类型新增 `nameEdited / weightEdited`
  - `backend/worker.py`
    - 删除/停用“从 `additionalContext` 用规则抽改名语义”的主路线
    - 新增 `_normalize_correction_items(...)`
    - `_apply_image_correction_items(...)` / `_apply_text_correction_items(...)` 改为：
      - 语义理解交给模型
      - 仅当 `nameEdited / weightEdited` 为真时，才锁该字段
      - 未显式修改的旧名称/旧重量，不再压过模型本轮结果
    - prompt 同步改成“自由文本说明最高优先级，结构化清单只说明哪些字段被人工改过”
- 追加验证：
  - `python -m py_compile backend/worker.py` 通过
  - `ReadLints` 检查 `src/pages/result/index.tsx` 无新增报错
  - 直接调用后端收口函数验证两种最小场景：
    - `nameEdited=False` 时，模型输出 `炸牛肉排`，最终结果保留 `炸牛肉排`
    - `nameEdited=True` 时，用户手动改成 `牛肉排`，最终结果锁定为 `牛肉排`

- 用户要求在圈子页为自己发的帖子增加删除动态功能，明确是从圈子中移除而非删除底层饮食记录。
- 已完成全链路实现：数据库新增 hidden_from_feed 字段 + 后端隐藏接口 + 前端移除按钮与确认弹窗
- migration 脚本：backend/database/migrate_user_food_records_add_hidden_from_feed.sql
- 验证：后端编译通过，前端 npm run build:weapp 通过
- 待执行：先在数据库跑 migration，再重启后端
