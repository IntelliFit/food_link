# 2026-06-18

## 2026-06-18 - 提交当前代码并同步 main/dev

- 用户要求提交当前代码、写好 commit 信息，并保持主分支和开发分支一致。
- 操作计划：先提交当前工作区，再合入远端最新提交；最终将同一个 HEAD 推送到 `origin/main` 和 `origin/dev`。
- 当前检查：`origin/main` 与 `origin/dev` 已同为 `5b1a28ac`，本地 `main` 落后一笔并有大量未提交改动。

## 2026-06-18 - 鹅鸭鸡专线纠偏为纯分类接口

- 用户再次明确：鹅腿/鸭腿/鸡腿专线不是把 `gpt-4.1` 接入当前普通食物识别通道，也不是普通分析任务的一种特殊模式，而是一个单纯识别鹅腿、鸭腿、鸡腿或不确定的独立通道。
- 已按该方向改造：
  - 后端新增 `POST /api/analyze/goose-duck-chicken`，只返回 `species`、`label`、`confidence`、`reason`、`evidence`。
  - 专线模型名和 prompt 只放后端；前端页面、首页横幅和 API 层不暴露 `gpt-4.1`、模型名或内部提示词。
  - 小程序 `packageExtra/pages/goose-duck-chicken/index` 上传图片后调用 `classifyGooseDuckChicken`，在本页展示结果。
  - 专线不再调用 `submitAnalyzeTask`，不跳 `analyze-loading`，不进入普通结果页，不做营养回算或饮食记录保存。
  - 首页横幅文案为 `去识别`，入口跳独立专线页。
- 验证：
  - `go test ./internal/analyze/handler -run "TestAnalyzeHandler_ClassifyGooseDuckChicken|TestAnalyzeHandler_Analyze"` 通过。
  - `go test ./internal/analyze/service -run "TestBuildSubmitTaskPayloadPreservesIntegratedPackagedCorrectionFields|TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode|TestResolveModelConfig"` 通过。
  - `git diff --check` 通过（仅 CRLF warning）。
  - 前端敏感字符串扫描确认专线页和首页入口无 `gpt` / `GPT` / `modelName` / prompt / 提示词暴露，专线页无 `submitAnalyzeTask`。
- `npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有类型错误阻塞，未指向本次专线文件。
- `weapp-devtools` 自动化仍无法连接：`mrc where --port 3001` 与 `9420` 均失败，未能取得截图或交互证据。

## 2026-06-18 - 鹅腿/鸭腿/鸡腿专线首版入口落地

- 用户确认先做首页活动横幅和独立专线页，暂不把专线混进现有快速/普通/精准识别链路。
- 已新增首页活动横幅入口：“鹅腿还是鸭腿？”点击进入 `packageExtra/pages/goose-duck-chicken/index`。
- 已新增“鹅鸭鸡专线”页面：上传单图、可补充线索、提交时显式传 `modelName=gpt-4.1:stable`，并注入鹅/鸭/鸡专项判断 prompt。
- 专线复用现有异步分析任务、上传和 `analyze-loading` 结果链路；额外写入 `analyzeSpecialLane=goose_duck_chicken`，便于后续结果页做专线化展示或活动埋点。
- 普通分析默认模型不变：普通 Gemini、快速 Qwen、精准 Gemini 3.5、纠错 Qwen；GPT 只由专线显式调用。
- 静态检查：相关文件 `git diff --check` 通过（仅 CRLF warning）。`npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有页面类型错误阻塞，未指向本次专线入口或新页面。
- 小程序运行时验证仍阻塞：`mrc where --port 3001` / `9420` 均无法连接微信开发者工具自动化服务，暂未取得截图或交互证据。

## 2026-06-18 - 鹅鸭鸡专线文案纠偏与入口点击反馈

- 用户指出首页横幅点“去挑战”没反应，且首版不应叫挑战；当前应先做简单的“上传图片 -> AI 帮用户识别鹅/鸭/鸡”，后续才考虑“用户先选答案再跟 AI 比拼”的玩法。
- 已把首页按钮文案从 `去挑战` 改为 `去识别`。
- 已把专线页文案从挑战口吻改成识别口吻：页面标题为“鹅鸭鸡识别”，上传区为“上传要识别的图片”，说明卡为“当前是 AI 识别模式”。
- 已给首页横幅按钮区域补独立点击处理，并在 `Taro.navigateTo` 失败时显示 toast：`入口加载失败，请重启小程序预览后再试`，避免用户点击无反馈。
- 只读检查发现当前 `apps/wechat/dist/app.json` 还没有 `goose-duck-chicken`，说明开发者工具当前加载的运行产物仍是旧页面列表；由于新增页面写在 `app.config.ts`，需要重启 `npm run dev:weapp` 后重新预览。
- `git diff --check` 通过；`tsc` 仍被既有类型错误阻塞，但未指向本次修改文件。`weapp-devtools` 3001/9420 仍无法连接。

## 2026-06-18 - 鹅鸭鸡专线入口改为分析页参数模式

- 用户贴出真实运行日志：横幅点击已经触发，但微信报 `navigateTo:fail page "packageExtra/pages/goose-duck-chicken/index" is not found`。
- 根因：运行态路由表当时未注册新增的 `goose-duck-chicken` 页面，新增页面在 Taro watch / DevTools 热更新下不一定能立即进入 `app.json`。
- 已把首页横幅入口从直接跳新页面改为跳已有分析页：`/packageExtra/pages/analyze/index?special_lane=goose_duck_chicken`。
- 已在 `apps/wechat/src/packageExtra/pages/analyze/index.tsx` 增加专线态：
  - 识别 `special_lane=goose_duck_chicken`
  - 顶部展示“鹅腿、鸭腿，还是鸡腿？”热点识别卡
  - 单图上传文案改为鹅鸭鸡识别
  - 隐藏普通识别模式、联网校准、多视角和 AI 摄入比例设置
  - 提交时显式传 `modelName=gpt-4.1:stable` 与鹅鸭鸡专项 prompt
  - 成功后写入 `analyzeSpecialLane=goose_duck_chicken`
- 最新只读复查 `dist/app.json` 已包含 `pages/analyze/index` 和 `pages/goose-duck-chicken/index`；但横幅仍保留跳 analyze 参数模式，降低后续新增页面热更新不稳定风险。
- 静态检查通过；`tsc` 仍被既有错误阻塞，未指向本次专线改动。`weapp-devtools` 自动化端口 3001/9420 仍无法连接。

## 接入 `gpt-4.1:stable` 到现有分析主通道

- 用户确认采用 `gpt-4.1:stable`，并要求“接入现有的通道中”。
- 已完成接入：
  - 后端标准图片分析主链路在未显式传 `modelName` 时，默认走 `gpt-4.1:stable`
  - `SubmitAnalyzeTask` / `SubmitInternalAnalyzeTask` 会在未传模型时把默认模型写入 payload，保证 worker 执行和日志链路一致
  - 同步图片分析现在尊重显式传入的 `modelName`，不再被执行模式默认值覆盖
  - `AnalyzeCompareEngines` 在未传模型时也会跟随该默认值
  - 小程序前端同步 API `analyzeFoodImage` / `analyzeFoodImageCompare` 不再硬编码 `gemini-3-flash-preview`
- 代码位置：
  - `backend/internal/analyze/service/analyze_service.go`
  - `backend/internal/analyze/service/task_service.go`
  - `apps/wechat/src/utils/api.ts`
- 单测：
  - `cd backend && go test ./internal/analyze/service -run "TestAnalyzeService_AnalyzeImageStandardUsesGPT41StableDefault|TestAnalyzeService_AnalyzeImageCorrectionUsesGPT41StableDefault|TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode|TestResolveModelConfig"` passed
- 小程序验证阻塞：
  - 已尝试 `mrc where --port 3001` 和 `mrc where --port 9420`
  - 两者都报 `Failed connecting to ws://localhost:<port>`，当前无法完成 `weapp-devtools` 运行时截图与交互验证

## 2026-06-18 - “接入现有通道”需求纠偏为“普通不动，专线显式走 GPT”

- 用户随后明确纠正：只有“鹅腿/鸭腿/鸡腿专线”才需要走 `gpt-4.1:stable`，普通模式不要切 GPT。
- 已据此回撤主通道默认切换，恢复原默认映射：
  - 普通模式：`gemini-3-flash-preview`
  - 快速模式：`qwen3.6-flash`
  - 精准模式：`gemini-3.5-flash`
  - 标准纠错图：`gemini-3.1-flash-lite`
- 保留此前新增的 OpenAI-compatible `gpt-*` / `o*` 路由支持；后续如果要上“鹅腿/鸭腿/鸡腿专线”，只需显式传 `modelName=gpt-4.1:stable`，不会影响普通分析链路。
- 前端 `apps/wechat/src/utils/api.ts` 继续不硬编码 `gemini-3-flash-preview`；因为后端默认已恢复，普通调用仍会自然走原 Gemini 默认，而专线调用仍可显式传模型。
- 局部验证通过：
  - `cd backend && go test ./internal/analyze/service -run "TestAnalyzeService_AnalyzeImageStandardUsesGemini3FlashDefault|TestAnalyzeService_AnalyzeImageCorrectionUsesGemini31LiteDefault|TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode|TestAnalyzeService_AnalyzeCompareEngines|TestResolveModelConfig"` passed
- 小程序运行时验证仍阻塞：
  - 再次尝试 `mrc where --port 3001` 和 `mrc where --port 9420`，仍返回 `Failed connecting to ws://localhost:<port>`

## 2026-06-18 - 纠错模式默认切到 `qwen3.6-flash`

- 用户新增要求：纠错模式默认模型改为 `qwen3.6-flash`，认为这条链路更合适。
- 已按较稳妥口径落地：
  - 普通模式：`gemini-3-flash-preview`
  - 快速模式：`qwen3.6-flash`
  - 纠错模式：`qwen3.6-flash`
  - 精准模式：`gemini-3.5-flash`
  - 精准纠错：`gemini-3.5-flash`
- 也就是说，这次没有把“精准纠错”一起改成 Qwen，仍保留在 Gemini 3.5，避免影响高精路径。
- 已更新 `backend/internal/analyze/service/analyze_service.go` 的默认模型选择逻辑，并补充 / 更新单测：
  - `TestAnalyzeService_AnalyzeImageStandardUsesGemini3FlashDefault`
  - `TestAnalyzeService_AnalyzeImageCorrectionUsesQwen36FlashDefault`
  - `TestAnalyzeService_AnalyzeImagePrecisionCorrectionUsesGemini35Default`
  - `TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode`
- 验证通过：
  - `cd backend && go test ./internal/analyze/service -run "TestAnalyzeService_AnalyzeImageStandardUsesGemini3FlashDefault|TestAnalyzeService_AnalyzeImageCorrectionUsesQwen36FlashDefault|TestAnalyzeService_AnalyzeImagePrecisionCorrectionUsesGemini35Default|TestAnalyzeService_AnalyzeImageRespectsExplicitModelNameInStandardMode|TestResolveModelConfig"` passed

## 2026-06-18 - 宠物对话正式接口与动态用量扣费接入

- 已把宠物对话从“前端 demo 借统计洞察接口”推进为正式后端接口：`POST /api/pet/chat/estimate` 和 `POST /api/pet/chat`。
- 后端生成链路复用统计服务的饮食上下文、DeepSeek 文本调用和统一 AI 用量计费器；生成前按预计积分校验余额，成功后按真实 usage 计算实际积分并扣费，失败不扣。
- 会员服务新增 `ValidateUsageCredits` 支持动态积分成本校验。
- 小程序宠物聊天页已切到新接口：确认弹窗先使用后端估价，生成后展示后端实际 usage 折算积分。
- Go 文件已 `gofmt`；未运行测试、未启动或重启本地服务、未运行 `npm run build:weapp`。
- 微信开发者工具自动化验证仍失败：`3001` 与 `9420` 均无法连接，无法提供截图/交互证据。

## 2026-06-18 - 宠物对话追问不重复扣积分

- 已补充宠物聊天页追问体验：首轮深度分析按真实模型 usage 结算；同一轮后续追问沿用上次分析线索，本地延展，不重新调用模型，不重复扣积分。
- 页面会在已有首轮分析后显示“本轮追问沿用刚才线索，不重复扣积分”；宠物回复也会主动说明本轮追问不重复扣积分。
- 这是前端会话级保护；后续服务端会话缓存仍可继续增强。

## 2026-06-18 - 宠物对话估价失败不进入扣费生成

- 已修正宠物聊天页：后端估价失败时直接提示并停止，不再使用本地估算继续进入生成。
- 产品原则：正式扣费入口必须以后端估价为准，本地估算只做页面展示参考。

## 2026-06-18 - 宠物对话用量计费文档同步为真实接口契约

- 已更新 `docs/pet-chat-usage-pricing.md`，把宠物对话定价从方案描述同步为当前已落地接口契约。
- 文档包含后端估价/生成接口、真实 usage 扣费流程、失败不扣、积分流水 meta 审计字段、同一轮追问不重复扣积分和后续服务端 session/cache 增强方向。

## 2026-06-18 - 宠物对话估价/生成流程防连点

- 宠物聊天页现在从后端估价开始就进入 busy 状态，防止快捷问题或发送按钮连点造成多个计费流程并发。
- 估价失败、用户取消、生成结束都会释放 busy。

## 2026-06-18 - 宠物对话估价阶段前置余额校验

- 已补严估价接口：`/api/pet/chat/estimate` 会按预计积分先校验用户可用积分，积分不足时直接返回错误。
- 这样前端不会在积分不足时弹出“开始分析”确认，也不会继续进入生成和扣费流程。

## 2026-06-18 - 非任务型 AI 消费系统积分落账修复

- 修复宠物对话同步接口不创建 `analysis_tasks` 导致系统每日积分可能不计入已用额度的问题。
- `ConsumeEarnedCreditsAfterSuccess` 现在会为非任务型成功消费写 `delta=0` 系统积分使用 ledger；`CountDailySystemCreditUsage` 会把这类 ledger 计入每日系统积分已用。
- 奖励积分仍按原来的负数 delta 扣余额；系统积分 ledger 只做审计和额度统计。
- 已补测试样例和文档，未运行测试。

## 2026-06-18 - 宠物对话扣费 ledger 关联字段补充

- 同步 AI 消费的系统积分 ledger 和奖励积分扣减 meta 现在都会带 `credit_charge_source_key`，方便把同一次小食探消费的多条流水关联起来。
- 本次只增强审计字段，不改变扣费金额。

## 2026-06-18 - 宠物对话扣费落账顺序修正

- 同步 AI 消费现在先完成奖励积分扣减，再写系统积分使用 ledger，避免扣费失败时提前占用每日系统积分。

## 2026-06-18 - 同步 AI 消费扣费落账事务化

- 新增事务型 repo 方法 `ChangeCreditsWithSystemUsage`，让同步 AI 消费的系统积分使用 ledger 与奖励积分扣减 ledger 同成同败。
- `ConsumeEarnedCreditsAfterSuccess` 已改用该方法处理非任务型 AI 消费，避免宠物对话账目半成功半失败。
- 已同步 mock 和文档，未运行测试。

## 2026-06-18 - 宠物对话扣费结果文案清晰化

- 宠物聊天结果现在会明确展示实际扣除积分、开始前预计积分和后端实际 usage 折算信息，降低用户对动态计费的困惑。

## 2026-06-18 - 宠物对话单次扣费上限护栏

- AI 用量计费器新增单次最高扣费上限，默认 20 积分；超过上限时按上限扣并返回 `capped=true`。
- 前端结果文案会提示“已触发单次扣费封顶”。
- 文档和配置默认值已同步，未运行测试。

## 2026-06-18 - 宠物对话确认弹窗补充单次封顶提示

- 宠物对话开始分析前的确认弹窗已补充“单次最高 20 积分”，让用户在消费前知道动态扣费上限。

## 2026-06-18 - 宠物对话封顶前积分审计字段

- AI 用量计费返回新增 `uncapped_credits_charged`，用于记录封顶前按公式应收积分；`credits_charged` 仍是实际扣除积分。
- 该字段用于后续复盘封顶对毛利的影响。

## 2026-06-18 - 宠物对话接口结构化日志补充

- `POST /api/pet/chat/estimate` 与 `POST /api/pet/chat` 已补充 handler 层中文结构化日志，覆盖请求进入、记录天数、预计/实际扣费、封顶状态和 billing_status。
- 本次只增强可观测性，不改变计费逻辑。

## 2026-06-18 - 扣费 meta 兜底创建以保证账目关联

- `ConsumeEarnedCreditsAfterSuccess` 现在会在 meta 为空时自动创建 meta，并写入 `credit_charge_source_key`，确保系统积分和奖励积分流水可关联。

## 2026-06-18 - 系统积分使用 ledger 要求 source key 幂等锚点

- 同步 AI 消费的 `delta=0` 系统积分 ledger 现在要求有非空 source key，避免不可幂等的 0 流水重复统计。

## 2026-06-18 - 宠物对话前端同步锁防并发触发

- 宠物聊天页新增 `busyRef` 同步锁，在 state 更新前即可阻止极快连点导致的多个估价/生成流程并发。
- 该改动进一步降低重复触发和重复扣费风险。

## 2026-06-18 - 系统积分 ledger 统计限定 ledger_role

- 每日系统积分统计现在只认 `delta=0` 且 `meta.ledger_role=system_credit_usage` 的 ledger，避免其他 0 流水污染系统积分已用额度。

## 2026-06-18 - 宠物对话生成后显示剩余积分反馈

- 小食探分析生成成功后会刷新会员积分，并在回复里显示当前可用积分；刷新失败时不打断结果，只提示稍后刷新。

## 2026-06-18 - 微信开发者工具 `app.json` 缺失报错诊断

- 用户贴出微信开发者工具报错：`app.json: 在项目根目录未找到 app.json`。
- 已核实当前仓库真正的小程序工程根目录是 `D:\files\food_link\apps\wechat`，其 `project.config.json` 配置 `miniprogramRoot: "dist/"`。
- 当前 `app.json` 实际存在于 `D:\files\food_link\apps\wechat\dist\app.json`，说明不是 Taro 产物缺失。
- 仓库根目录 `D:\files\food_link` 没有 `project.config.json`；如果误把根目录导入 DevTools，工具就会直接在根目录找 `app.json` 并报错。
- 结论：这是导入目录错误，正确导入目录应为 `D:\files\food_link\apps\wechat`。

## 2026-06-18 - 宠物对话 UI 与连续对话反馈修正

- 用户指出当前页面不支持明确连续对话/新建对话能力，界面偏丑，“问问小食探”和“饭饭记录”文案不符合已有宠物形象。
- 已修改 `apps/wechat/src/packageExtra/pages/pet-chat/index.tsx` 与 `index.scss`：
  - 页面顶部改成“宠物饮食对话 / 问问{宠物名}”。
  - 新增“新对话”按钮，可清空当前轮次并重新开始。
  - 聊天流展示宠物形象，首轮分析仍正式估价/扣费，后续追问沿用当前轮线索不重复扣费。
  - 扣费结果从正文移到气泡底部小票，减少视觉噪音。
  - 追问逻辑不再重复账单和同一组结论卡，并补充“微量元素”类追问的边界说明。
- 已修改入口：
  - `apps/wechat/src/packageExtra/pages/pet-home/index.tsx` 改为“问问{宠物名} / 去问问它”。
  - `apps/wechat/src/pages/index/index.tsx` 首页宠物浮层改为“问问{宠物名}”。
  - `apps/wechat/src/packageExtra/pages/pet-chat/index.config.ts` 兜底标题改为“问问宠物”。
- weapp-devtools 验证阻塞：`mrc where --port 3001` 和 `mrc where --port 9420` 都无法连接自动化服务，未能截图或交互验证。
- 本轮未运行测试、未启动/重启开发服务、未执行 `npm run build:weapp`。

## 2026-06-18 - 分析页与首页微量营养纳入

- 用户明确追问：统计分析页的 AI 风险解读有没有把微量元素 / 维生素算进去；并希望首页除了蛋白、碳水、脂肪外，也能显示可折叠的微量营养。
- 已完成后端统计改造：
  - `backend/internal/health/service/stats_service.go` 现在会按记录日均把膳食纤维、钠、钾、钙、铁、维生素 A/C/D 作为微量营养证据写入 AI prompt。
  - prompt 要求也已改成必须覆盖“微量营养与维生素/矿物质线索”，不再只看热量和三大宏量。
  - 本地 fallback 统计文案在有明显线索时也会补一句微量营养偏差提示。
- 已完成首页改造：
  - `backend/internal/home/service/dashboard_service.go` 为首页 `intakeData` 新增 `micros` 聚合。
  - 新增 `apps/wechat/src/pages/index/components/MicrosSection.tsx`，默认接入的关键项为：纤维、钠、钾、钙、铁、维A、维C、维D。
  - 首版内嵌在主卡正文里，被用户明确指出“太占空间，会把其他内容挤下去”。
  - 已改版为：首页仅保留一个很轻的“微量”入口，和“目标设置”并排；点击后通过底部浮层展示完整微量营养，不再推挤首页下方内容。
- 新增公用 helper：`backend/internal/nutritionagg/nutrition_aggregate.go`，统一按 `ratio` 或 `intake/weight` 折算 item 内营养素，避免只吃半份却按整份微量营养累计。
- 验证：
  - `go test ./internal/health/service -run "TestBuildNutritionInsightPromptForbidsCertifiedIdentityClaims|TestBuildNutritionInsightPromptIncludesMicronutrientEvidence|TestStatsService_GenerateInsightRequiresFoodRecords"` passed
  - `go test ./internal/home/service -run "TestBuildMealItem$|TestTotalFoodRecordWaterMl$"` passed
  - `TestDashboardService_HomeDashboard` 这类 sqlite 测试在当前环境被 `CGO_ENABLED=0` + `go-sqlite3` stub 阻塞
  - `mrc where --port 3001` / `9420` 仍无法连接，未拿到 weapp-devtools 截图与交互证据

## 2026-06-18 - 首页宠物卡降噪
- 用户反馈首页宠物卡仍然很乱很丑，包含过多没必要信息。
- 已在 `apps/wechat/src/pages/index/index.scss` 追加首页宠物卡降噪样式：保留宠物头像、宠物名、一句状态和主按钮；隐藏成长等级、任务、领奖励等杂讯，把完整宠物档案能力留在宠物主页。
- 交互原则：首页宠物卡只承担“问宠物/进入宠物互动”的轻入口，不再承担完整成长面板。
- 已尝试 weapp-devtools 验证，3001/9420 仍无法连接自动化服务。

## 2026-06-18 - 首页微量营养交互改为横向滑动
- 用户继续否定“目标设置旁小入口 + 底部浮层”的微量营养方案，明确要求不要丑、不要额外挤占默认版面，并建议整块营养区右滑切换到微量营养。
- 已把首页热量卡下方营养区改为 `Swiper` 横向滑动：
  - 第 1 页保留原三大营养素卡。
  - 第 2 页展示微量营养预览卡。
  - 只有在第 2 页点击“展开”时，才在当前页下方展开完整微量营养详情。
- 这样默认首页高度不增加，不再使用独立按钮入口或底部浮层。
- 静态检查：`git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss apps/wechat/src/pages/index/components/MicrosSection.tsx apps/wechat/src/pages/index/components/index.ts` 通过（仅 CRLF warning）。
- `npm --workspace apps/wechat run typecheck` 仍失败，但报错集中在 `day-record`、`feedback`、`food-library`、`pet-home`、`pet-lab`、`private-chat`、`record-manual`、`result-text` 等既有文件，未指向本次首页微量营养改动文件。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` 与 `mrc where --port 9420` 都无法连接微信开发者工具自动化端口。

## 2026-06-18 - 首页营养切换器去悬浮并消除重复展开
- 用户进一步指出上一版“宏量/微量”切换器压住营养卡内容，而且不够像组件；同时微量页展开后出现了“预览一份 + 下方再复制一份完整微量卡”的重复问题。
- 已继续调整首页营养区交互：
  - 去掉右下角悬浮切换器。
  - 改成营养卡内顶部的分段切换组件：`宏量营养 / 微量营养`，同时保留左右滑动切换。
  - 微量页展开后改为原地长开，直接把当前页 4 项预览扩成完整微量营养网格，不再在下面追加一块重复内容。
- 修改文件：
  - `apps/wechat/src/pages/index/index.tsx`
  - `apps/wechat/src/pages/index/index.scss`
  - `apps/wechat/src/pages/index/components/MicrosSection.tsx`
  - `apps/wechat/src/pages/index/components/index.ts`
- 静态检查：`git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss apps/wechat/src/pages/index/components/MicrosSection.tsx apps/wechat/src/pages/index/components/index.ts` 通过（仅 CRLF warning）。
- `npm --workspace apps/wechat run typecheck` 仍失败，但仍是既有类型错误，未指向本次首页营养区文件。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` 和 `mrc where --port 9420` 继续返回 `Failed connecting to ws://localhost:<port>`。

## 2026-06-18 - 首页营养区继续压缩空白并支持内部滚动
- 用户继续指出两点：
  - 微量展开后在这个小区域里不能上下滑动。
  - 默认态中间空白仍然偏多。
- 已继续收敛首页营养区：
  - `apps/wechat/src/pages/index/components/MicrosSection.tsx` 改为在微量展开态使用卡片内部 `ScrollView scrollY`，完整微量营养在这块区域内就能上下滚。
  - `apps/wechat/src/pages/index/index.tsx` 进一步按状态细分 `Swiper` 高度：宏量页更矮、微量预览稍高、微量展开再拉高。
  - `apps/wechat/src/pages/index/index.scss` 同步压缩默认空白：缩短宏量卡顶部预留空槽、减少营养切换器间距、压小微量标题/副文案/卡片留白。
- 静态检查：`git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss apps/wechat/src/pages/index/components/MicrosSection.tsx apps/wechat/src/pages/index/components/index.ts` 通过（仅 CRLF warning）。
- `npm --workspace apps/wechat run typecheck` 仍失败，但依旧是既有类型错误，未指向本次首页营养区文件。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` 和 `mrc where --port 9420` 仍无法连接微信开发者工具自动化端口。

## 2026-06-18 - 宠物对话历史持久化

- 用户指出宠物对话需要保存历史，且需要改数据结构。
- 已确认旧实现没有真正保存聊天历史：页面刷新/退出会丢，后端 ledger 只有问题和 usage 审计。
- 已新增：
  - `backend/internal/health/domain/pet_chat.go`
  - `backend/internal/health/repo/pet_chat_repo.go`
  - `pet_chat_sessions` / `pet_chat_messages` 迁移 DO 与索引/外键迁移
  - `GET /api/pet/chat/latest`
  - `POST /api/pet/chat/messages`
  - `POST /api/pet/chat` 返回并保存 `session_id`
- 前端 `apps/wechat/src/packageExtra/pages/pet-chat/index.tsx` 已接入最近会话恢复、session_id 传递、新对话清空 session、本地追问追加保存。
- `apps/wechat/src/utils/api.ts` 已新增历史读取与消息追加 API。
- 已执行 `gofmt`；未运行测试、未运行数据库迁移、未重启服务、未执行小程序构建。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` / `9420` 均无法连接。

## 2026-06-18 - 宠物对话历史表数据库迁移完成

- 用户确认可以迁移当前远程库：`154.8.205.78:5432 / food-link / public`。
- 迁移过程：
  - 原 `go run ./cmd/migration -config-dir .` 多次在 AutoMigrate 阶段超时。
  - 用户切换网络后，迁移推进到索引阶段并暴露既有 SQL 语法错误。
  - 已修复 `backend/internal/migration/migration.go` 中 `idx_user_circle_posts_search_gin` 表达式 GIN 索引语法。
  - 重新 `go build ./cmd/migration` 后执行 `./migration.exe -config-dir . -timeout 10m` 成功。
- 成功输出：`数据库迁移完成: config_dir=. schema=public`。
- 当前数据库应已具备 `pet_chat_sessions` 与 `pet_chat_messages` 结构及相关索引/外键。
- 未重启后端服务、未运行测试、未执行小程序构建。

## 2026-06-18 - Product Design 重做宠物入口与聊天页简洁化
- 用户通过 `@product-design` 明确要求重做首页宠物入口和宠物对话页：当前深绿按钮突兀、宠物形象不一致、页面乱、7天/30天 tab 没必要。
- Brief：宠物本身应成为自然对话入口，像“宠物想说话/点我问问”，而不是 AI 功能按钮；聊天页应极简、宠物形象一致、隐藏范围切换和报告式 hero。
- 已修改：
  - 首页宠物入口按钮文案改为“点我问问”，并用浅色气泡样式替代深绿色 CTA，增加轻微说话提示动效。
  - 宠物聊天页 `PetFigure` 改为复用宠物主页的 `pet-home-avatar/pet-body` 结构与样式，不再另画一只宠物。
  - 聊天页隐藏范围切换卡，弱化 hero，改成浅色简短提示；新对话按钮和发送按钮都改为更轻的浅绿色体系。
  - 页面不再展示当前可用积分等首屏噪音；积分仍在确认和小票中保留。
- 未运行测试、未构建小程序、未重启服务。
- weapp-devtools 仍需验证；若 3001/9420 不可连接，则无法提供截图证据。

## 2026-06-18 额外记录 - 宠物 UI 重画第二轮
- 用户对上一轮宠物入口和宠物对话页非常不满意，明确要求别再修旧方案，而是整体重做。
- 本轮已把首页宠物浮卡继续收成更轻的单层入口，并把文案压到只剩宠物名、状态一句话和 `点我聊聊`。
- 本轮已把宠物对话页从“hero + 范围卡 + 聊天”改成真正的聊天布局：顶栏、宠物开场气泡、消息流、快捷问题、输入栏。
- 对话页已移除显式 7 天 / 30 天切换器，范围改为默认 7 天、根据提问语义自动放大到 30 天。
- 再次尝试 `weapp-devtools` 校验仍失败：`3001` / `9420` 都连不上自动化 WebSocket，暂时拿不到截图证据。
- 用户明确要求首页宠物入口必须能收起，不能因为重做视觉就把功能删掉。
- 已补回：展开态可点右上角 `收起`，收起态点宠物恢复展开；卡片正文在收起态隐藏。
- 再次尝试 `mrc where --port 3001` / `9420` 仍失败，自动化验证继续阻塞。

## 2026-06-18 - 首页公告横幅与营养区继续压缩

- 用户指出首页顶部滑动横幅和营养展示区仍然过高，已经把“今日餐食/今日饮食”挤到太下面。
- 最新确认的方向不是先做收起逻辑，而是先保留整块公告横幅，把横幅高度和营养区尺寸都再压一版；横幅作为系统公告栏，暂不优先做持久收起。
- 已修改 `apps/wechat/src/pages/index/index.scss`：
  - 公告横幅高度从 `162rpx` 收到 `136rpx`
  - 同步压缩横幅 padding、标题/描述字号、按钮尺寸、圆点尺寸和阴影
  - 营养切换器、宏量卡、微量卡的 padding / gap / 字号 / 留白整体收紧
- 已修改 `apps/wechat/src/pages/index/index.tsx`：
  - `nutrition-swiper` 高度从 `154 / 212 / 362rpx` 调整为 `138 / 188 / 320rpx`
- 静态检查：
  - `git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss` 通过（仅 CRLF warning）
- 运行时验证阻塞：
  - `mrc where --port 3001` 与 `mrc where --port 9420` 仍都无法连接微信开发者工具自动化端口，未拿到截图证据

## 2026-06-18 - 首页营养切换器与横幅再缩一档

- 用户继续指出“宏量营养素 / 微量营养素”按钮还偏大，希望按钮更小、文案更短，横幅字体和空间也继续压。
- 已修改 `apps/wechat/src/pages/index/index.tsx`：
  - 切换文案从 `宏量营养 / 微量营养` 改为 `宏量 / 微量`
  - 右侧提示从 `可左右滑动切换` 改为 `左右滑动`
  - `nutrition-swiper` 高度继续从 `138 / 188 / 320rpx` 调整为 `126 / 170 / 292rpx`
- 已修改 `apps/wechat/src/pages/index/index.scss`：
  - 公告横幅高度从 `136rpx` 继续压到 `122rpx`
  - 横幅标题、描述、按钮和圆点字号/尺寸再降一档
  - 营养切换按钮最小宽度收为 `82rpx`，padding、字号继续减小
  - 微量营养说明、展开按钮、卡片字号和留白继续压缩
- 静态检查：
  - `git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss` 通过（仅 CRLF warning）
- 运行时验证阻塞：
  - 再次尝试 `mrc where --port 3001` 与 `mrc where --port 9420`，仍然都无法连接微信开发者工具自动化端口

## 2026-06-18 - 首页微量营养内容字体纠偏

- 用户指出上一轮误把微量营养具体内容区字体也缩小了，这不是需求本意。
- 已修正：
  - 保留更小的 `宏量 / 微量` 切换按钮
  - 去掉 `左右滑动` 提示文案
  - 恢复 `MicrosSection` 内容区字号：包括标题、副文案、卡片 label/unit/value、空态文案和 footnote
- 静态检查：
  - `git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss` 通过（仅 CRLF warning）
- 运行时验证阻塞：
  - `mrc where --port 3001` 与 `mrc where --port 9420` 仍无法连接微信开发者工具自动化端口
- 用户在微信开发者工具里抓到真实运行错误：宠物对话页因为直接 import `pet-home/index.scss`，触发 `pet-home/index.wxss` 缺失和 `navigateTo timeout`。
- 已把宠物对话页改为样式自包含，不再跨页面依赖 pet-home wxss。
- 本轮再次尝试 `mrc where --port 3001` / `9420` 仍失败，无法自动化截图验证。- 用户明确指出首页宠物收起态背后的淡色圆圈很丑，不需要；已隐藏收起态背景圆晕，只保留宠物本体。
- 用户指出宠物对话页宠物变形严重、页面内“点点我，直接开聊”不合理、对话历史入口缺失。
- 已补：右上角 `历史` 可恢复最近对话，`新对话` 继续保留；去掉开聊文案；对话页宠物增加紧凑比例样式覆盖，避免变形成长条。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。- 用户进一步要求宠物对话页只显示一个宠物、去掉顶部历史/新对话那排，并保持宠物和首页完全一致。
- 已移除消息流重复宠物头像和顶部操作排；对话页宠物改用首页紧凑宠物比例覆盖。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。- 用户指出宠物对话没接入微量元素，且顶部历史/新对话不应被移除。
- 已把统计页已有微量营养 prompt block 接入宠物对话 prompt，并恢复顶部历史/新对话轻量操作。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。

## 2026-06-18 - 首页营养区改成整块展开

- 用户重新提出更清晰的交互方向：不要再设置“宏量 / 微量”按钮，整块营养卡默认展示三大营养素，展开后展示更完整的微量元素；展开后的 UI 要更美观。
- 已修改 `apps/wechat/src/pages/index/index.tsx`：
  - 移除 `nutritionPanelIndex`、Swiper 切换和宏量/微量按钮。
  - 保留单一 `nutritionExpanded` 状态。
  - 默认展示三大营养素，点击“营养概览”整块展开 / 收起。
  - 展开后在同一块营养卡下方渲染 `MicrosSection`。
- 已修改 `apps/wechat/src/pages/index/components/MicrosSection.tsx`：
  - 组件改为纯微量营养展开内容，不再自带展开按钮。
  - 完整微量元素在内部 `ScrollView` 里滚动承载。
- 已修改 `apps/wechat/src/pages/index/index.scss`：
  - 删除旧 `nutrition-switcher` / `nutrition-swiper` 相关视觉模型。
  - 新增 `nutrition-expand-*` 样式，让展开态像同一张营养卡自然长出微量详情。
- 静态检查：`git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss apps/wechat/src/pages/index/components/MicrosSection.tsx apps/wechat/src/pages/index/components/index.ts` 通过（仅 CRLF warning）。
- `npm --workspace apps/wechat run typecheck` 仍被既有小程序类型错误阻塞，错误未指向本次首页营养区改动文件。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` 与 `mrc where --port 9420` 仍无法连接微信开发者工具自动化端口。

## 2026-06-18 - 首页微量营养展开态提高信息密度

- 用户指出微量元素展开后信息密度太低，一行只有两个，而且“这里只做饮食参考...”这类文字属于思考过程，不应该在产品里给用户看。
- 已修改 `apps/wechat/src/pages/index/components/MicrosSection.tsx`：
  - 移除 `ScrollView` 包裹，完整 8 项微量营养直接展示。
  - 删除“这里只做饮食线索参考，不替代医学判断...”文案。
  - 标题旁只保留简短状态，如 `8项`、`同步中`。
- 已修改 `apps/wechat/src/pages/index/index.scss`：
  - 微量营养网格从 2 列改成 4 列。
  - 卡片改为更紧凑的小数据块，数值和单位同行展示，8 项两行可扫完。
- 静态检查：`git diff --check -- apps/wechat/src/pages/index/index.tsx apps/wechat/src/pages/index/index.scss apps/wechat/src/pages/index/components/MicrosSection.tsx apps/wechat/src/pages/index/components/index.ts` 通过（仅 CRLF warning）。
- `npm --workspace apps/wechat run typecheck` 仍被既有小程序类型错误阻塞，未指向本次首页营养区文件。
- weapp-devtools 验证仍阻塞：`mrc where --port 3001` 与 `mrc where --port 9420` 继续无法连接自动化端口。

## 2026-06-18 - 咨询食探 iOS App Store 快速上架与个人开发者资质

- 用户询问如果希望食探快速上架苹果 App Store，最快路径、需要做哪些操作，以及个人开发者账号是否有资质注册并赚钱。
- 已核对 Apple 官方信息：个人开发者账号可加入 Apple Developer Program 并分发 App，但 App Store 卖家名会显示个人法定姓名；组织账号才显示公司/机构法定名称且需要 D-U-N-S。
- 赚钱需要在 App Store Connect 完成 Paid Apps Agreement、税务和银行卡信息；食探会员、积分、AI 功能解锁这类 App 内数字服务原则上需要接 Apple In-App Purchase。
- 若面向中国大陆 App Store，还需要注意 Apple/MIIT 关于 ICP/App 备案信息的要求；中国大陆区可能是快速上架的主要合规阻塞点。
- 用户要求宠物对话积分消耗静默处理，不要在发送前弹窗或在回复里提示积分。
- 已移除 pet-chat 前端估价弹窗、扣费 receipt 和相关积分文案；发送后直接发起分析。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。
- 用户指出连续对话不是真连续：前端本地拼回答，没有把上下文给模型。
- 已删除前端本地追问逻辑；追问现在继续走 `generatePetChat` 并沿用 session_id。后端新增按 session 读取最近消息，prompt 里带最近对话上下文。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。- 用户建议借鉴 ChatGPT 的 Recents / 新建对话设计。
- 已补后端会话列表和按会话读取接口；前端 `最近` 打开底部最近对话抽屉，点击会话切换，新对话保留。
- weapp-devtools 3001/9420 仍连接失败，无法自动截图验证。- 用户追问宠物对话是否带健康档案等重要上下文。
- 已确认原 prompt 带健康档案、身体指标、饮食目标、微量营养、历史上下文；本轮补入用户自定义关注指标，并加强回答要求。
