# 2026-06-18

## 修复：迁移执行到圈子搜索 GIN 索引时报 `||` 语法错误
- 用户在 `backend/` 执行 `go run ./cmd/migration -config-dir .`，Apollo local 指向远程 PostgreSQL 后失败：`apply index statement: ERROR: syntax error at or near "||" (SQLSTATE 42601)`。
- 根因是既有 `idx_user_circle_posts_search_gin` 的 GIN trigram 表达式索引写法缺少整体括号，不是新增的 `weapp_user.telephone` 归一化唯一索引。
- 已修复 `backend/internal/migration/migration.go`：`idx_user_circle_posts_search_gin` 改为 `USING gin ((COALESCE(title, '') || ' ' || COALESCE(body, '')) gin_trgm_ops)`。
- 验证通过：`go test ./internal/migration/... -count=1`、`go build -o $env:TEMP/foodlink-migration-check.exe ./cmd/migration`、`git diff --check -- backend/internal/migration/migration.go backend/internal/migration/do/schema_do.go`；临时编译产物已删除。
- 代理尚未重跑远程迁移命令；需要用户自行重跑或明确授权代理执行。

## 完成：weapp_user.telephone 归一化唯一索引迁移代码
- 用户反馈已删除此前重复手机号；本轮只读复查当前 `backend/app-config.yaml` 指向的远程 PostgreSQL `154.8.205.78:5432/food-link`，`weapp_user.telephone` 原始重复为 0，按 `+86` / 空格 / 横线 / 括号归一化后的重复为 0，非空非法手机号为 0。
- `backend/internal/migration/migration.go` 新增 `weapp_user_telephone_format_check`，允许空手机号或可归一化的中国大陆手机号；新增 `idx_weapp_user_telephone_normalized_unique`，按归一化表达式做唯一索引，防止 `131...` 与 `+86131...` 绑定到不同用户。
- `backend/internal/migration/do/schema_do.go` 在 `Telephone` 字段旁补充注释，说明普通查询索引保留在 DO，归一化唯一索引由幂等迁移 SQL 表达。
- 验证通过：`go test ./internal/migration/... -count=1`、`go build ./cmd/migration`、`git diff --check -- backend/internal/migration/do/schema_do.go backend/internal/migration/migration.go`；仅有既有 LF/CRLF 提示。
- 尚未执行 `go run ./cmd/migration -config-dir .`，因为当前配置指向远程 PostgreSQL；需要用户明确确认后再从 `backend/` 执行迁移。

## Mobile App 全局弹窗按钮布局修复

- 用户反馈登录页协议弹窗的绿色确认按钮视觉上掉出白色弹窗卡片。
- 修复 `apps/mobile/src/providers/DialogProvider.tsx`：将操作区重命名为 `footer`，恢复卡片底部内边距；单按钮不再使用 `flex: 1`，仅双按钮横排时通过 `twoActionItem` 平分宽度，避免按钮把自适应高度弹窗撑到边界外。
- 验证通过：`npm --workspace apps/mobile run typecheck`；`git diff --check -- apps/mobile/src/providers/DialogProvider.tsx` 仅有既有 LF/CRLF 提示。
- MuMu/Expo Go/ADB 验证：复用现有 Metro 和 `127.0.0.1:5555`，未勾选协议点击微信登录后弹出自定义弹窗；截图 `tmp/app-dialog-login-agreement-contained-fixed.png` 显示按钮已在白色卡片内部。UI dump 中卡片底部 `1578`、按钮底部 `1525`，底部留白正常。
- 本轮未启动、停止或重启本地服务，未运行 `npm run build:weapp`，未触碰 admin/backend 既有未提交改动。

## 意见反馈飞书通知失败排查

- 用户反馈提交意见反馈后没有飞书通知，线上日志为 `意见反馈飞书通知失败`，错误 `feishu api error code=19001 msg=param invalid: incoming webhook access token invalid`。
- 只读拉取 Apollo `main/app-config.yaml` 验证飞书配置：`feishu.feedback_webhook_url` 与 `feishu.report_webhook_url` 均存在，二者 token 不同；Apollo `local` 和 `main` 当前两组飞书 token 相同。
- 用 Apollo `main` 配置实际 POST 测试：反馈 webhook 返回 `code=19001 incoming webhook access token invalid`；举报 webhook 返回 `code=0 success`。
- 结论：反馈通知失败根因是 Apollo 中反馈专用飞书机器人 webhook access token 已失效/不属于当前可用机器人，不是后端签名算法、反馈入库、异步通知链路或 Apollo 加载问题。
- 后续处理：在 Apollo 更新 `feishu.feedback_webhook_url` 和 `feishu.feedback_webhook_secret` 为有效机器人；如果反馈和举报可发同一群，可临时复用已验证成功的举报 webhook 配置。更新后重启/滚动后端服务加载新配置。
- 进一步对照用户提供的飞书机器人截图确认：Apollo/粘贴配置里的反馈 webhook 最后一段被写成 `aald61e8ed89`，截图实际是 `aa1d61e80d89`；反馈 secret 中 `w2O8` 的 `O` 被写成了数字 `0`。用截图 URL 加错误 secret 返回 `19021 sign match fail`，改成截图 secret 后返回 `code=0 success`。
- 最终根因确认：同事新增 report 飞书配置时曾用 OCR 录入/改动 feedback 配置，导致原本正确的 feedback webhook URL 和 secret 被相似字符污染。以后更新 webhook/secret 必须直接复制，不要 OCR 或手敲。

## Mobile Android 发布入口与 API 自动分流

- Android 发布入口已收敛为根目录 `npm run release:mobile:android`。
- `scripts/release_mobile_android.py` 会按当前 Git 分支自动选择：`dev` -> 体验版、EAS `preview`、`https://dev.api.healthymax.cn`、`beta` channel；`main` -> 正式版、EAS `production`、`https://api.healthymax.cn`、`stable` channel；其它分支拒绝执行。
- `eas.json` 已给 `development` / `preview` 注入 dev API，给 `production` 注入正式 API；当前 Android `production` profile 也生成 APK，配合同一发布脚本上传 stable channel。
- 已移除根目录多余 Android 构建/发布别名和 `apps/mobile` 内的 `build:android:apk` / `build:android:aab` / `build:android:dev` 别名；保留 iOS 构建脚本。
- 验证通过：`python -m py_compile scripts/release_mobile_android.py`、`python scripts/release_mobile_android.py --help`、JSON 解析校验、`npm run typecheck:mobile`、相关文件 `git diff --check`。本轮未触发 EAS 构建、COS 上传或本地服务启动。

## Mobile Android 发布脚本读取 Expo Token

- `scripts/release_mobile_android.py` 已补充从 Apollo `release-config.yaml` 读取 `expo.token`；环境变量 `EXPO_TOKEN` 仍然优先，Apollo 作为默认配置。
- Apollo 建议配置：`expo.token: <Expo access token>`；COS 发布配置仍沿用 `storage.cos_region`、`storage.cos_secret_id`、`storage.cos_secret_key`、`storage.release_bucket`、`storage.release_cdn_base_url`。
- 验证通过：`python -m py_compile scripts/release_mobile_android.py`、`python scripts/release_mobile_android.py --help`、`git diff --check -- scripts/release_mobile_android.py`。
# WeChat DevTools CLI automation smoke test

- User asked whether the WeChat DevTools CLI at `D:\IDE\微信web开发者工具` can be used for screenshots and clicks.
- Confirmed `D:\IDE\微信web开发者工具\cli.bat` exists and `127.0.0.1:16465` is the active IDE HTTP service port from the Settings screenshot.
- `mrc` is not installed/in PATH, but `miniprogram-automator@0.12.1` is already present in the repo.
- `cli.bat auto --project K:\code\little-horse\food_link\apps\wechat --auto-port 9420 --port 16465 --trust-project` succeeded.
- Connected with `miniprogram-automator` to `ws://127.0.0.1:9420`, captured screenshots, inspected elements, and tapped the bottom `我的` tab.
- Result: current page changed to `pages/profile/index`; evidence screenshots are `tmp/weapp-cli-smoke-before.png`, `tmp/weapp-cli-smoke-after-click.png`, and `tmp/weapp-cli-profile-click.png`.
- No `npm run build:weapp`, no dev server start/restart, and no app code changes were made.

## 2026-06-18 Mobile Android EAS build 4 queue follow-up

- Expo/EAS project initialized as `@healthymax/food-link-mobile`, project id `09e22b2a-2d32-4d4c-bdd8-b663dae88fef`; `apps/mobile/app.json` now has `owner: healthymax` and `extra.eas.projectId`.
- Android build number was bumped to 4 in both `apps/mobile/app.json` (`expo.android.versionCode`) and native `apps/mobile/android/app/build.gradle` (`versionCode 4`). Because `apps/mobile/android/` exists, EAS uses the native Gradle versionCode as authoritative.
- Root `app.json` created by EAS init was removed; `apps/mobile/eas.json` is the EAS config used when the release script runs EAS from `apps/mobile`. Root `eas.json` is kept aligned for documentation/root visibility.
- Added root `.easignore` and `apps/mobile/.easignore` so EAS archive no longer uploads local Android build outputs such as `apps/mobile/android/app/build/` and `.cxx/`. `eas build:inspect --stage archive --output $TEMP/foodlink-eas-inspect` now completes quickly; inspected copy totals about 12 MB.
- Old queued/stale EAS builds were canceled: `f7ca4b3b-a645-45e5-a26f-7a3fec892faf` and `0c76c8f2-91f7-43c7-be44d0f44a4ca596`, both were buildVersion 3.
- Current EAS Android preview build for dev/beta is `965f5a36-4e7c-439f-8bae-01ef983f2dcc`, appBuildVersion 4, buildProfile `preview`, API `https://dev.api.healthymax.cn`, status `IN_QUEUE` after 5 minutes of polling. No local release/EAS process remains running; this is Expo cloud queue wait, not a local Java/Gradle hang.
- Once build `965f5a36-4e7c-439f-8bae-01ef983f2dcc` finishes, download its APK artifact and run `npm run release:mobile:android -- --artifact-apk <apk-path>` from repo root to upload to COS and update the beta channel manifest without starting another EAS build.

### ׷�ӣ�EAS build 3 ��������
- ����һ�� EAS no-wait �����ڱ��ض�ȡ stdout ʱ���� GBK ������󣬵��ƶ������Ѿ��ύ������������ `e77a9186-603f-41e0-9cbd-518aad155185`��appBuildVersion 3�����ù���������ȡ����
- ��ǰ��Ҫ�������ȴ����� build 4��`965f5a36-4e7c-439f-8bae-01ef983f2dcc`��״̬��Ϊ `IN_QUEUE`��
## 完成：main/dev 分支代码同步

- 按用户要求同步主分支和开发分支代码。
- `git fetch origin --prune` 后确认 `origin/main` 与 `origin/dev` 已分叉：`main` 在 `5b95d52`，`dev` 在 `89edca1`。
- 本地 `dev` 先快进到 `origin/dev`，随后在 `main` 合并 `dev`；唯一冲突为 `memory/2026-06-17.md`，已保留双方日记记录后完成合并提交 `c3be7c4`。
- 已推送 `origin/main`，再将 `dev` 快进到同一提交并推送 `origin/dev`。
- 本轮只做 git 同步和状态记录；未启动/停止/重启本地服务，未执行数据库迁移，未运行 `npm run build:weapp`。

## 完成：运动能量长文本识别与运动库

- 用户要求排查运动能量识别长文本失败，并按阈值实现长文本两阶段识别；当前阈值已调整为超过 80 字进入长文本链路；同时参考零食/包装食品库思路，为运动建立可审核的标准库。
- 后端新增 `exercise_energy_library` / `exercise_energy_aliases` 两张表的迁移 DO、约束、外键和 trigram 搜索索引；业务 domain/repo 支持精确名称、别名和模糊匹配。
- `ExerciseService` 保持 80 字以内原算法；超过 80 字时先让大模型抽取运动名称、时长、组数/次数和强度，再优先匹配 active 运动库 MET，按 `MET × 体重kg × 时长小时` 汇总热量。
- 运动库未命中时，由大模型估算粗粒度运动名称、分类、强度和 MET；当前请求使用该估值计算，并写入 `pending` 记录，后续必须在 Admin 审核为 `active` 后才会被自动复用。
- Admin 后台新增「运动库」页面和 `/api/admin/exercise-energy-library` 系列接口，可筛选、查看、编辑 MET、分类、强度、别名、审核状态和启用状态。
- 验证通过：`go test ./internal/health/... -count=1`、`go test ./internal/admin/... -run '^$' -count=1`、`go build ./cmd/server`、`admin npm run build`、`git diff --check`。
- 本轮未执行数据库迁移、未启动/重启本地前后端服务、未运行 `npm run build:weapp`；上线前需确认目标库后运行 `backend/cmd/migration`。

## 补充：运动能量短/长文本分流测试

- 按用户要求新增两个专门测试；后续用户要求删除泛化长文本样例，长文本回归改为使用完整健身房训练文本：
  - `TestExerciseService_EstimateCalories_ShortTextRun40MinutesUsesOriginalLLM`：输入 `我今天跑了40分钟步`，确认短文本使用原 LLM 估算链路，估算 320 kcal。
  - 已删除 `TestExerciseService_EstimateCalories_Over200RunesTrainingTextUsesLibraryMET` 泛化样例；当前完整健身房文本由 `TestExerciseService_ProcessExerciseTask_LongStrengthCardioTextIgnoresInvalidImageURL` 覆盖。

## 补充：运动长文本两阶段模型切到 Gemini35 配置

- 确认现状：运动短文本、图片和长文本两阶段此前都硬编码豆包 `doubao-seed-2-0-lite-260428`。
- 项目里代表更高质量 Gemini 代理的配置组是 `external.gemini35_api_key`、`external.gemini35_base_url`、`external.gemini35_model`，对应环境变量 `GEMINI35_API_KEY`、`GEMINI35_BASE_URL`、`GEMINI35_MODEL`；食物分析已用这组初始化 Gemini 3.5 Flash client。
- 已修改：运动长文本两阶段 JSON 调用（运动项抽取、未命中运动 MET 估算）优先使用 `gemini35_*`；未配置时回退豆包。短文本原算法仍使用豆包。
- 新增测试 `TestExerciseService_EstimateCalories_LongTextUsesGemini35Config`，验证长文本请求走 Gemini 代理 base URL、Bearer key、`gemini-3.5-flash` model，并设置 `response_format=json_object`。
- 验证通过：相关长短文本测试、`go test ./internal/health/service -count=1`、`go build ./cmd/server`、`git diff --check`。

## 修复：长运动文本误入旧 calories_kcal 解析器

- 用户用完整健身房训练文本测试（深蹲、卧推、高位下拉、划船、哑铃推举、弯举、绳索下压、跑步机、卷腹、平板、背伸、拉伸）时，前端显示 `运动分析结果格式解析失败，missing calories_kcal`。
- 根因定位：`missing calories_kcal` 只来自旧的单次运动热量解析器；长文本抽取链路应解析 `items`。后端此前先判断 `imageURL != ""` 再判断长文本，如果前端误传 `image_url="undefined"` / `null` 等非空占位，会误走图片/单次估算链路，导致模型返回 `items` 时被旧解析器按 `calories_kcal` 解析失败。
- 修复：
  - `estimateExerciseCalories` 改为先判断超过 80 字的长文本，再处理图片路径。
  - `resolveFoodImageURL` 将 `undefined`、`null`、`<nil>` 当作空值。
  - 新增 `TestExerciseService_ProcessExerciseTask_LongStrengthCardioTextIgnoresInvalidImageURL`，直接使用用户提供的真实长文本，并模拟 `imageURL="undefined"`。
- 验证通过：新增复现测试、`go test ./internal/health/service -count=1`、`go test ./internal/health/... -count=1`、`go build ./cmd/server`、`git diff --check`。

## 二次修复：小程序 Input 默认 140 字截断运动长文本

- 用户再次提供失败日志后，只读查询任务 `8609c0a1-bd32-4011-8a0c-1c1496c513dc`，确认 `analysis_tasks.text_input` 实际入库只有 140 字，不是用户粘贴的完整文本。
- 真实根因：运动记录页使用小程序单行 `Input`，微信 Input 默认最大输入长度为 140；完整训练文本在前端已经被截断。按当时 150 字阈值，后端收到 140 字后没有进入长文本链路，于是走短文本旧算法；模型按复杂训练内容返回了 `items`，旧解析器要求 `calories_kcal`，最终失败。当前阈值已调整为 80 字，可降低类似截断文本误走短文本链路的概率。
- 修复：
  - `apps/wechat/src/packageExtra/pages/exercise-record/index.tsx`：`Input` 改为 `Textarea`，显式 `maxlength={2000}`，避免长运动文本被截断。
  - `apps/wechat/src/packageExtra/pages/exercise-record/index.scss`：调整输入区为多行样式，限制最大高度，避免撑坏发送按钮布局。
  - 后端旧解析器增加兜底：如果旧链路收到 `items` 且文本长度超过阈值，记录中文 warn 日志并切回长文本两阶段链路。
  - 新增 `TestExerciseService_EstimateSingleLongTextItemsResponseFallsBackToLongTextFlow` 覆盖 `missing calories_kcal` 的兼容兜底。
- 验证：
  - `go test ./internal/health/service -run 'TestExerciseService_(ProcessExerciseTask_LongStrengthCardioTextIgnoresInvalidImageURL|EstimateSingleLongTextItemsResponseFallsBackToLongTextFlow)$' -count=1 -v` 通过。
  - `go test ./internal/health/... -count=1`、`go build ./cmd/server`、`git diff --check` 通过。
  - `npm --workspace apps/wechat run typecheck` 仍失败，但错误均为既有其它页面问题，未指向本次修改的 `exercise-record`。
  - `weapp-devtools` 运行时验证阻塞：`mrc where --port 3001` 与 `mrc where --port 9420` 均连接失败。

## 三次修复：运动长文本 Gemini 空响应导致 JSON 解析失败

- 用户反馈前端仍显示 `unexpected end of JSON input`。
- 定位：该错误来自后端长文本两阶段 JSON 解析链路，不是前端解析接口响应失败。Gemini 兼容代理可能返回 HTTP 200，但 `choices[0].message.content` 为空；后端此前没有校验空 content，直接调用 `json.Unmarshal([]byte(""))`，于是返回 Go 原生错误 `unexpected end of JSON input`。
- 修复：
  - `callExerciseJSONLLM` 改为按配置顺序尝试：Gemini 优先，若空 content、HTTP 异常、响应解析失败或请求失败，则记录中文 warn 并自动尝试豆包备用模型。
  - 空 content 现在被识别为 `运动分析服务返回空内容`，不会继续进入 JSON 解析。
  - `parseExtractedExerciseItems` / `parseExerciseMETEstimate` 增加空响应保护，上层错误明确标注失败阶段：运动项目抽取或 MET 估算。
- 新增测试：
  - `TestExerciseService_EstimateCalories_LongTextEmptyGeminiFallsBackToDoubao`：Gemini 空 content 后回退豆包，跑步机 35 分钟按库中 MET=8.3、体重 80kg 算出 387 kcal。
  - `TestParseExtractedExerciseItems_EmptyResponse`：空响应返回中文阶段错误。
- 验证通过：
  - `go test ./internal/health/service -run 'TestExerciseService_EstimateCalories_LongText(UsesGemini35Config|EmptyGeminiFallsBackToDoubao)|TestParseExtractedExerciseItems_EmptyResponse|TestExerciseService_ProcessExerciseTask_LongStrengthCardioTextIgnoresInvalidImageURL|TestExerciseService_EstimateSingleLongTextItemsResponseFallsBackToLongTextFlow' -count=1 -v`
  - `go test ./internal/health/... -count=1`
  - `go build ./cmd/server`
  - `git diff --check`

## 四次修复：真实 3010 接口复现 Gemini 半截 JSON

- 用户继续反馈前端仍报 `unexpected end of JSON input`，并要求确认前端运动记录访问的后端端口。
- 端口确认：
  - `.env.development` / `.env.production` 的 develop API 均为 `http://127.0.0.1:3010`。
  - `apps/wechat/dist/common.js` 当前注入的 develop URL 是 `http://127.0.0.1:3010`。
  - `apps/wechat/dist/packageExtra/pages/exercise-record/index.js` 已包含 `Textarea` 和 `maxlength: 2000`。
  - 本机 3010 当前由 Go 后端进程监听，3001 是 SSH 监听，不是后端。
- 真实接口复现：通过 3010 的 `/api/test-backend/login` + `/api/test-backend/impersonate-user` 拿真实 JWT，请求 `/api/exercise-logs/estimate-calories` 并提交用户完整健身房训练文本，复现 400：`运动项目抽取结果格式解析失败: unexpected end of JSON input`。
- 复现结论：前端端口没有错；这次不是空 content，而是 Gemini 返回了非空但不完整 JSON（例如半截 `{"items":[`），此前空响应兜底无法覆盖。
- 修复：
  - `callExerciseJSONLLMOnce` 返回内容前增加 JSON 对象完整性校验；不完整 JSON 视为该模型调用失败，记录中文 warn 并回退豆包。
  - 新增 `TestExerciseService_EstimateCalories_LongTextMalformedGeminiFallsBackToDoubao`，覆盖半截 JSON 回退豆包并算出 387 kcal。
  - 按用户要求删除旧的 `TestExerciseService_EstimateCalories_Over200RunesTrainingTextUsesLibraryMET` 泛化样例；保留用户完整健身房文本作为长文本真实回归样例。
- 验证通过：定向 service 测试、`go test ./internal/health/... -count=1`、`go build ./cmd/server`、`git diff --check`。
- 注意：当前 3010 后端进程是在最后这个 malformed JSON 补丁前启动的，尚未加载最新代码；需要用户重启后端后再用前端复测。

## 五次修复：运动库表未迁移导致任务写库失败

- 用户重启后端后复测，长文本分析已成功，但任务处理结束时报错：`ERROR: relation "exercise_energy_library" does not exist (SQLSTATE 42P01)`。
- 定位：当前 `backend/config.yaml` 指向远程 PostgreSQL `154.8.205.78:5432/food-link`，本轮新增的运动库表迁移尚未在该库执行。Go HTTP 服务启动时不执行数据库迁移，必须手动运行 `cmd/migration`。
- 因为目标库是远程库，按项目规则不能擅自执行迁移；需要用户明确确认后，才可在 `backend/` 运行 `go run ./cmd/migration -config-dir .`。
- 为避免迁移前继续影响用户记录，已增加代码降级：
  - `ExerciseRepo` 识别 PostgreSQL `42P01` 缺表错误。
  - 运动库查询缺表时返回 `library_unavailable`/空结果。
  - pending 运动库写入缺表时跳过，不再让本次运动记录失败。
- 影响：迁移前长文本运动记录仍可完成，但不会命中运动库，也不会写入待审核运动库；迁移后完整库匹配、pending 写入和 Admin 审核功能才可用。
- 验证通过：`go test ./internal/health/repo ./internal/health/service -count=1`、`go test ./internal/health/... -count=1`、`go build ./cmd/server`、`git diff --check`。

## 完成：远程库运动库表迁移

- 用户明确授权对当前配置指向的远程库执行迁移。
- 在 `backend/` 运行 `go run ./cmd/migration -config-dir .`；第一次执行在运动库表达式 trigram 索引处失败：`syntax error at or near "||"`。
- 修复 `backend/internal/migration/migration.go` 中表达式 GIN trigram 索引 SQL：对 `COALESCE(...)` 和字符串拼接表达式使用 PostgreSQL 明确接受的表达式索引写法。
- 重跑 `go run ./cmd/migration -config-dir .` 成功，输出 `数据库迁移完成: config_dir=. schema=public`。
- 只读验证确认以下对象已存在：
  - `exercise_energy_library`
  - `exercise_energy_aliases`
  - `idx_exercise_energy_library_search_gin`
  - `idx_exercise_energy_aliases_alias_gin`

## 六次修复：运动长文本 AI 识别超时

- 用户迁移运动库表并重启后，前端报 `AI识别服务响应超时`。结合后端日志，根因是长文本任务仍依赖第一阶段 LLM 抽取；Gemini 兼容接口返回 malformed JSON 后回退豆包，且运动库初始命中不足时还会逐项请求 MET 估算，最终超过任务上下文时间。
- 已在 `backend/internal/health/service/exercise_service.go` 增加本地结构化解析器：识别常见训练动作、`4组10次`、`3组60秒`、`跑35分钟`、`各30秒` 等格式；当解析到至少 3 个运动项目时，直接跳过第一阶段 LLM，进入运动库 MET 汇总。
- 用户提供的完整健身房训练文本回归测试 `TestExerciseService_ProcessExerciseTask_LongStrengthCardioTextIgnoresInvalidImageURL` 已更新：断言本案例 `callCount=0`，估算结果仍为 958 kcal，`ai_response` 包含 `local_exercise_text_parser`。
- 验证通过：`cd backend && go test ./internal/health/... -count=1`、`go build ./cmd/server`、`git diff --check`。
- 本轮没有替用户启动或重启本地后端；需要后端重新加载代码后再做前端真实复测。

## 完成：运动长文本前端自动折叠

- 用户要求运动分析 UI 在文本特别多时自动折叠，范围包括用户自己提交运动的列表，以及动态/圈子里的运动打卡展示。
- 已修改 `apps/wechat/src/packageExtra/pages/exercise-record/index.tsx/scss`：运动记录页的已完成记录和 pending/失败卡片中，运动描述超过 90 字默认折叠，并提供「展开 / 收起」。
- 已修改 `apps/wechat/src/pages/community/index.tsx/scss`：社区动态列表中的运动打卡描述超过 90 字默认折叠，保留描述与图片/营养区间距。
- 已修改 `apps/wechat/src/packageExtra/pages/interaction-feed-detail/index.tsx`：动态详情页中的运动描述和运动 AI 估算原因超过 90 字默认折叠。
- 验证：本次修改文件 ESLint 通过；`git diff --check` 通过。全局 `npm --workspace apps/wechat run typecheck` 仍失败，但错误均在既有其它页面，未指向本次修改文件。
- 微信开发者工具自动化验证阻塞：`mrc where --port 9420` 连接失败（9420 有 wechatweb 监听但 WebSocket 不可用）；`mrc where --port 3001` 连接失败且 3001 当前由 SSH 监听。未运行 `npm run build:weapp`，未启动/重启本地前端服务。
