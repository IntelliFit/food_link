# 2026-06-01

## 拉取 main/dev 最新代码

- 用户要求拉取最新代码，主分支和开发分支都要更新。
- 拉取前当前位于 `dev`，有 3 个未提交本地修改：`backend/cmd/packaged-quality-backfill/main.go`、`backend/cmd/packaged-quality-backfill/main_test.go`、`memory/2026-06-01.md`。
- 已用 stash 临时保护本地修改，执行 `git fetch origin --prune`，然后分别在 `main` 与 `dev` 执行 `git pull --ff-only origin main/dev`。
- 两个分支均已是最新；当前 `main`、`dev`、`origin/main`、`origin/dev` 都在 `936b166956c98ca78519ac55e946b3e0fc733d51`。
- 已恢复 stash，当前回到 `dev`，上述 3 个本地未提交修改仍保留。
- `git diff --quiet origin/main origin/dev` 通过，确认远端主分支和开发分支文件树一致。

## 提交当前代码并同步 main/dev

- 用户要求提交当前代码，写好 commit 信息，并确保开发分支和主分支一致且 push 到远端。
- 已提交当前工作树：`9bd1e7e feat(packaged-food): improve sku quality and analysis flows`。
- 首次 push `main` 时被拒绝，因为远端 `main` 在本地提交期间新增提交；随后 fetch 发现远端 `main` 与 `dev` 均有新提交。
- 已合入 `origin/main` 与 `origin/dev`，避免强推覆盖他人更新；再将最终结果同步到 `main` 和 `dev`。
- 已成功 push：
  - `main`: `31c398b..58c12e0`
  - `dev`: `03fdf79..e7a7ebe`
- 最终校验：`origin/main` 与 `origin/dev` 文件树一致；本地当前在 `dev`，工作区干净。
- Push 输出提示 GitHub 默认分支有 109 个 Dependabot 漏洞告警，本轮未处理。

## 上传前零食库搜索 source 参数修复

- 用户反馈“先搜零食库”搜索 `咖啡` 没有结果，但搜索 `雀巢` 能看到多个标题含咖啡的包装食品。
- 根因：上传前搜索页复用 `/api/manual-food/search` 混合搜索接口，返回普通营养库、最近记录、公共库、包装库等混合结果；`咖啡` 这种泛词会被普通营养库结果占满前 30 条，前端再过滤 `source=packaged_food` 后就变成空。
- 已修复：`/api/manual-food/search` 支持 `source=packaged_food`，命中该参数时只查包装食品库；上传前“先搜零食库”改为调用这个参数，不再在前端过滤混合结果。
- 验证通过：`go test ./internal/utility/handler -run "TestManualFoodSearch|TestManualFoodSearchPackagedOnly" -count=1`、`go test ./internal/utility/service -run '^$' -count=1`、`go test ./internal/app -run '^$' -count=1`、`npx eslint src/utils/api.ts src/packageExtra/pages/packaged-food-edit/index.tsx --max-warnings 0`、相关文件 `git diff --check`。
- 运行时验证被阻塞：`mrc where --port 3001` / `9420` 均无法连接微信开发者工具自动化。
- 重启后检测：访问 `/api/manual-food/search?q=咖啡&limit=30&source=packaged_food` 或在小程序“先搜零食库”搜索 `咖啡`，应返回包装食品来源的咖啡商品。

## 零食数据库质量体检

- 用户要求查看当前 `packaged_food_library` 整体质量是否有问题。
- 已用临时只读脚本 `tmp/packaged_quality_audit.go` 查询当前配置指向的 PostgreSQL（`CONFIG_SOURCE=local`，host 脱敏 `154******.78`，schema=`public`），输出 `tmp/packaged_quality_audit_20260601.json`。
- 当前包装库共 `238` 条，全部 active；来源分布为 `human_photo_batch_20260526=145`、`user_capture_ocr=61`、`web_ai_completed=29`、`user_submitted=3`。
- 召回结构字段质量已改善：`display_name/search_text/product_key/product_family_key` 缺失均为 `0`；`product_key` 重复为 `0`，alias 重复为 `0`。
- 主要问题：
  - `net_weight_g` 和结构化净含量缺失 `36` 条，直接影响包装库作为重量锚点使用。
  - `source_image_urls` 缺失 `32` 条，全部来自 web/user 手动补充，应视为低可信补充数据。
  - `spec_text` 缺失 `51` 条，`ocr_raw_text` 缺失 `151` 条，证据可追溯性不足。
  - 宏量三项全 0 有 `2` 条：盒马瑞士卷、Ritter Sport 巧克力。
  - 多规格 family 仅士力架花生夹心巧克力 `35g/70g`，属于合理歧义，不是脏重复。
- 结论：当前库可以继续支撑零食试验模式和手动搜索，但不能直接视为高可信 SKU 库；下一步优先用 Gemini 3 Flash dry-run/backfill-missing 补缺净含量和宏量字段。
- 补充细查“最近上传”：
  - 最近真正入库记录几乎都来自 `user_capture_ocr`，每次通常 2 张图片，并在 `reward_task_uploads` 中记录为 `packaged_food_upload succeeded`，奖励 `1` 积分。
  - 最新一条为 `2026-05-31 17:09:13`：`大窑 DAYAO 大窑橙诺 橙味果汁汽水 橙味 净含量:520mL`，source_task_id=`f74024be-6c8c-4347-b041-eb7ea21ddc54`，packaged_food_id=`69bc3819-0b65-4e0f-a441-0e46f329569a`，任务 done、2 张图、奖励 succeeded。
  - 该最新记录质量有问题：`spec_text=净含量:520mL`、营养已换算为约 `16.0 kcal/100g`、碳水 `3.5g/100g`，但 `net_weight_g=0` 且 `net_content_value=0`，说明 ml/净含量解析没有正确落入结构化净含量字段。
  - 这暴露奖励/入库门槛过宽：即使净含量结构化失败，也可能创建 packaged_food 并发奖励。后续应把主动上传的质量状态拆成 `created_but_review` 与 `created_verified`，只有达到“品名 + 净含量/容量 + 营养换算闭合 + 图片证据”的记录才视作可直接用于识别权重锚点。

## 零食数据库质量补齐脚本

- 用户进一步要求急需一个脚本，把当前零食库能完善的信息都完善；有原始图片的记录要重新根据原图识别，营养字段问题也要解决。
- 已新增 `backend/cmd/packaged-quality-backfill`：
  - `--mode deterministic`：只从现有文本中明确的 `净含量/净重/规格` 或干净品名尾部规格补结构化净含量。
  - `--mode vision`：对有 `source_image_urls` 的问题记录调用视觉模型重新识别。
  - `--mode all`：先 deterministic，无法补再走 vision。
  - 默认 dry-run，只写 `results.jsonl/review.csv/summary.json`；显式 `--apply` 才会写库。
- 安全规则：
  - 不把 `每100g/每100mL/营养成分表` 口径当作包装净含量。
  - 只补缺失字段；已有非空字段与 Gemini 新结果冲突时，记录 `conflict_fields` 并进入 review，不自动覆盖。
  - 没有原图的 web/user 旧数据不能可靠自动补营养，应重新上传图片或进入人工 review。
- 同步收紧主动上传入库门槛：没有 `net_weight_g` 且没有结构化 `net_content_value` 时，自动入库返回 `blocked/missing_net_content`，避免继续沉淀无法作为重量锚点的 SKU。
- 验证：
  - `go test ./cmd/packaged-quality-backfill -count=1` passed。
  - `go test ./internal/foodrecord/service -run "TestEvaluatePackagedProductExtract|TestConvertPackagedNutrition|TestBuildPackagedProductExtractPrompt|TestSubmitPackagedProductExtractTask" -count=1` passed。
  - deterministic dry-run：`processed=40 deterministic=20 review=20 errors=0`。
  - Gemini all 小批量 dry-run：`processed=5 deterministic=1 vision=4 review=0 errors=0`。
- 实际补库执行：
  - deterministic apply 已执行：`processed=40 applied=20 deterministic=20 review=20 errors=0`，输出 `tmp/packaged-quality-backfill-deterministic-apply`。
  - Gemini all apply 已执行：`processed=39 applied=34 vision=34 review=5 errors=1`，输出 `tmp/packaged-quality-backfill-all-apply`；其中 1 条可口可乐为上游超时，后续因已有 `net_content=330ml` 不再视作必须补 `net_weight_g`。
  - 后续小批量补剩余问题两轮：`remaining-apply-1` 补 1 条，`remaining-apply-2` 补 1 条。
  - 补后审计输出 `tmp/packaged_quality_audit_after_backfill_20260601.json`。
- 补后指标：
  - `missing_structured_net_content` 从 36 降到 9。
  - `missing_ocr_raw_text` 从 151 降到 136。
  - `nutrition_macros_all_zero` 从 2 降到 1。
  - `nutrition_kcal_over_900` 仍为 2，说明 Gemini 没能安全覆盖达利园法式软面包、圣大农场花椒锅巴的热量异常。
  - 剩余 9 条结构化净含量缺失均有图片，但多次 Gemini 复识别未给出可安全落库净含量，需人工 review 或重新上传更清晰净含量图。

## 高校食堂宣传专区与邀请码分成机制讨论

- 用户提出一个增长策略：利用自己“北京大学博士生 + 健身教练”的身份，先录入北大某个或多个食堂的菜品，也可扩展到清华等学校。
- 数据形态：固定单盘菜适合拍照入库并标注学校、食堂、楼层、窗口/位置；麻辣烫等自选组合不适合提前拍完整成品，更适合记录窗口、可选食材和估算规则。
- 用户希望思考是否在软件中开辟专区，候选位置包括“圈子”或“公共食物库/动态浏览”。
- 用户还提出可让其他学校的人发起同类活动，并通过邀请码追踪注册和会员转化，给发起人会员收入分成，未来可能需要提现或兑现模块。
- 本轮明确为产品讨论，不写代码；后续需区分 MVP、增长活动、分销/提现合规风险。
- 用户补充价格也是关键数据；已确认价格应进入第一版结构化模型，并支持价格类型、价格历史、更新时间、来源和置信度。
- 已新增 `docs/校园食堂专区PRD.md`，覆盖背景目标、信息架构、页面、数据模型、价格需求、接口、审核、激励、MVP 分期和验收标准。
- 用户进一步澄清：暂时不需要代理实施，想用 PRD 测试与工程师的协作能力；若工程师能做好，说明工作模式有效。
- 已将 `docs/校园食堂专区PRD.md` 改写为更适合分发给工程师的产品 PRD，保留目标、用户路径、页面、字段、MVP、验收标准，弱化具体数据库/API/迁移等实施方案，并要求工程师基于 PRD 输出技术方案、数据结构、接口、工期、风险和待确认问题。

## 零食系统 SKU 结构化迁移

- 用户明确要求执行零食系统数据库迁移，并希望知道如何检测“零食通道自然融入当前识别通路”。
- 已执行：`cd backend && go run ./cmd/migration -config-dir .`。
- 迁移目标：`CONFIG_SOURCE=local`，当前配置指向的 PostgreSQL，schema=`public`。
- 迁移成功日志：`数据库迁移完成: config_dir=. schema=public`。
- 只读校验结果：
  - `packaged_food_library` 新字段存在：`display_name/search_text/product_family_key/net_content_value/net_content_unit/unit_count/unit_content_value/unit_content_unit/review_status`。
  - 关键索引存在且为非唯一：`idx_packaged_food_library_product_key`、`idx_packaged_food_library_display_name`、`idx_packaged_food_library_family_key`、`idx_packaged_food_library_review_status`、`idx_packaged_food_aliases_normalized_alias`。
  - `normalized_name` 唯一索引数量为 `0`。
  - 样本中 `喜之郎 cici果粒爽 橙汁饮料` 为 `258g`，`桃李 豆沙小饼面包 红豆沙馅` 为 `55g`，士力架存在 `35g` 与 `70g` 规格记录。
  - `FOOD_LINK_PACKAGED_SEARCH_INTEGRATION=1 go test ./internal/foodrecord/repo -run TestPackagedFoodSearchIntegrationCiciOrange -count=1 -v` passed，喜之郎 Cici 橙汁 258g 为第一候选。
- 后续检测路径：
  - 重启 Go 后端。
  - 先测手动搜索：搜索 `喜之郎Cici橙味`、`豆沙小饼面包`、`士力架两条装`，应看到完整展示名和规格。
  - 再测 `standard_packaged_experiment` 图片识别：喜之郎应 258g，桃李应 55g；士力架两条装若识别/规格线索明确应 70g，否则至少返回多规格候选而不是盲套 35g。
