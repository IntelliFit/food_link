# 2026-06-08

- 已完成代码提交并推送：
  - 497eea4 feat: add nutrition quality audit and zero-nutrition repair workflows
  - c3cdf82 test(backend): add zero-nutrition benchmark unit tests
- 已保证 main 与 origin/dev 一致，当前共同提交为 c3cdf82。
- 推送动作执行了 git push origin HEAD:main、git push origin HEAD:dev，均已成功。

## 营养未命中 DeepSeek + Qwen 二级兜底

- 用户要求检测 DeepSeek 营养兜底 API 是否可用，并在 DeepSeek 不稳定时再用快速模式同款 `qwen3.6-flash` 兜底。
- 真实 API 检测：
  - DeepSeek 营养补全接口可用，样例请求约 `3885ms` 返回。
  - DashScope `qwen3.6-flash` 可用，样例请求约 `6430ms` 返回。
  - Qwen 在本次 `香菇油麦菜` 样例中估算偏离较大，因此只作为第二层兜底，不替代 DeepSeek。
- 已新增 `backend/internal/analyze/service/nutrition_fallback.go`，实现 chained fallback 与 `QwenNutritionEstimator`。
- `AnalyzeService` 现在会组合 DeepSeek 与 DashScope/Qwen：DeepSeek 成功标 `deepseek_generated`；DeepSeek 失败或空结果后 Qwen 成功标 `qwen_generated`。
- 相关测试通过：
  - `go test ./internal/analyze/service -run "TestAnalyzeService_FinalizeAnalyzeResponse(GeneratesNutritionWhenPackagedAndNormalMiss|FallsBackToQwenWhenDeepSeekFails)|TestQwenNutritionEstimatorEstimateParsesNutrition" -count=1`
  - `go test ./internal/worker -run "TestRunFoodAnalysisWithAnalyzeServiceFallsBackToAIWhenPackagedFoodMisses" -count=1`
  - `go test ./internal/foodrecord/service -run "TestZeroNutritionGate" -count=1`
- 注意：新后端逻辑需要重启/部署后才会影响运行中请求。
