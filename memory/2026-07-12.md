# 2026-07-12

- 用户要求同时完成两项：整理 Apollo 大模型迁移配置清单，并把 DeepSeek 文本链路的 base URL 配置化。
- 已新增 `external.deepseek_base_url`，支持 Apollo key `external.deepseek_base_url` 和环境变量 `DEEPSEEK_BASE_URL`；本地 YAML 覆盖、云配置别名和首尾空白清理均已补齐。
- 后端运行时已将该配置传给 DeepSeek 分析/营养补全客户端，并统一供统计洞察、宠物对话、自定义关注卡、今天吃什么使用；旧 `https://api.deepseek.com` 只在未配置时作为兼容默认值。
- 已新增 `docs/llm-apollo-migration.md`，记录完整运行时 Apollo 模型配置项和新平台 URL 拼接规则。对新平台 OpenAI-compatible，项目内 `*_base_url` 必须填 `https://maas-openapi.wanjiedata.com/api/v1`。
- 验证通过：`go test ./pkg/config -count=1`（覆盖 Apollo properties 的 `DEEPSEEK_BASE_URL` 映射）、`go test ./internal/health/service -count=1`、定向 DeepSeek 分析服务测试、`go test ./internal/app -run '^$' -count=1`。健康服务中原有模型断言与实际常量漂移（测试断言 flash、代码为 pro）已改为与现行 `deepseek-v4-pro` 默认值一致。
- 待用户在 Apollo 开发 namespace 写入新平台 key/base URL 并重启开发后端，按文档验证后再迁移生产。
