## 2026-04-13

- 拉取远端 `origin/dev` 最新代码（`fac6b14 -> bc4afab`）。
- 完成圈子模块 3 个细节修复：
  - 好友动态标题字号上调（更醒目）。
  - 搜索框下方左侧漏斗改为 `iconfont icon-filter-filling`（修复未渲染）。
  - 互动消息点击动态时优先 `navigateBack` 回圈子页，降低“跳转慢/不跳转”体感。
- 通过 `weapp-devtools` skill 完成本地联调环境拉起：
  - `npm run dev:restart` 启动前后端
  - 微信开发者工具 CLI `auto --project /Users/kirigaya/project/food_link --auto-port 9420` 成功
  - `mrc where --port 9420` 可连接，`pages/community/index` 调试与截图成功（`community-debug.png`）
- 调试中发现旧后端实例曾异常占用 `3010` 且 `/openapi.json` 返回 `500`；清理旧进程后已恢复 `200`。
- 新增首页分享称号能力：
  - 分享按钮换为 `iconfont icon-share`
  - 新建 `src/utils/daily-honor-title.ts` 独立称号系统
  - 今日小结分享弹层新增“显示今日称号”开关（默认开），开关变更后自动重生成海报
  - 海报渲染支持展示「今日称号：xxx」胶囊
- 根据用户截图再做样式修正：
  - 称号胶囊改为绝对定位到中央热量圆上半区
  - 分享弹层开关行增加上边距，解决顶部被遮挡
- 互动消息跳转需求变更：不再回圈子页滚动定位，已改为进入独立页面 `pages/interaction-feed-detail/index`。
  - 新页面仅展示被点击的单条动态（结构对齐圈子列表）
  - 支持点赞、评论信息展示与评论发送
  - `mrc` 已验证路由可直达并无错误日志
- 今日小结海报按用户口径二次调整：
  - 称号去掉“今日称号”前缀并放大，移动到原日期区域
  - 日期移动到热量圆上半区、离中心更近，并移除年份
- 海报三次微调：
  - “显示今日称号”开关行上边距调到 `120rpx`
  - 中央热量圆增加同色边框
  - 圆内改为按进度展示波浪液位（从底部填充到当前百分比）
- 互动消息单动态页补修：
  - 修复缺 `recordId` 时持续 loading 问题（无参立即结束加载并提示）
  - 兼容 `recordId/record_id` 参数读取
  - 移除页面顶部标题与简介
  - 加载态文案改为纯 spinner 动画
- `AGENTS.md` 新增加载态规范：前端 loading 禁止显示“加载中”文字，统一动画表达。
- 互动消息详情页“持续加载中”继续修复：
  - 原因之一为带鉴权 HOC 的首次渲染门控可能导致 query 生命周期丢失
  - 已改为 `public` 挂载 + 页面内鉴权重定向
  - 参数解析兼容 `recordId/record_id/id`
  - 请求链路加入超时兜底，避免长时间等待
