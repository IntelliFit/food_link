# 2026-07-16

## 包装千焦误当大卡修复

- 用户提供“瑞”的截图，包装营养表为每100g能量518kJ、蛋白质21.8g、脂肪2.8g、碳水2.6g，识别重量约60g，但结果页显示311kcal。
- 对共享 PostgreSQL 做只读质量审计，定位任务 `6d213d68-af69-43c2-bc2e-3d8749b64274`：`nutrition_source=ingredient_label`，每100g calories 被保存为518，60g被直接缩放为310.8；宏量营养整份对应73.68kcal，确认是kJ字段误填而非前端显示误差。
- 已修改 Go 分析服务：四类图片 prompt 明确 energyKj/calories 单位与 `/4.184` 换算；标签归一化支持显式energyKj及约4.184倍宏量一致性检测，只对ingredient_label生效；GetTask/ListTasks对历史结果做只读兼容归一化，旧任务重新打开约显示74.28kcal。
- 未直接修库或执行修复性SQL。`go test ./internal/analyze/... -count=1` 全部通过，目标diff-check通过。无前端改动、无需weapp运行时验证；需部署/重启后端后生效。
- 按 build-test-suite 用户确认制未新增或修改测试文件，只运行现有分析模块完整测试；专门的518kJ持久回归用例待用户确认测试设计。
- 用户明确当前节奏：已有生产方法修改即可先部署观察，不急于继续加固；若上线仍复现再优化。误解后短暂新增的独立测试文件已撤回，最终仍只保留已验证的生产修复。

## 今天吃什么动态推理反馈与可靠性加固

- 用户要求推荐推理期间提供动态效果，并尽可能避免推荐失败。
- 前端已把单一 spinner 替换为纯视觉分阶段动画：双轨道旋转、叶片核心呼吸、三节点依次点亮、流动进度光带和三张 shimmer 推荐骨架卡；不显示加载中文字。
- 增加推荐请求序号隔离，切换外食/自做时旧响应不会覆盖最新结果。单次等待改为18秒，只对网络超时/连接错误或5xx安全重试一次；业务错误不重试。积分 source key 幂等，不会因重试重复扣减。
- 后端已有12秒总预算与公共食物库、用户历史记录、标准营养库/规则兜底，模型慢、非2xx、空响应或JSON错误均能正常返回推荐。
- 后端定向测试通过；前端17 suites / 64 tests、目标ESLint、SCSS编译通过。微信开发者工具3001自动化端口未开启，无法提供截图或切换场景交互验证；未启动/重启服务，未执行 production build。
# 2026-07-16（虚拟支付订阅 DAU 准入量级）

- 用户确认：小程序虚拟支付已接入；首次发布已满 90 天且认证备案完成。安卓/鸿蒙与 iOS 的会员订阅分别需要不同签约接口，不能复用当前 Papay；“近 30 天日均 DAU ≥ 1 万”是近期增长目标。
- 2026-07-16 只读核对业务 PostgreSQL 的 2026-06-16 至 2026-07-15（30 个完整自然日）明确操作代理：日均 18.60 位去重登录用户、峰值 32、30 天累计 133、最近登录用户 282、业务库注册用户 1,287。明确操作包含分析、饮食/喝水/体重/运动记录、圈子互动及保质期项目，同一用户同日跨操作去重。
- 该代理漏掉仅浏览、未登录访客和未落库访问，不能替代微信公众平台数据助手的官方 DAU，也不能用于订阅准入；但现有可观测量级远低于 1 万。正式判断必须在公众平台数据助手查看最近 30 天官方 DAU 及日均值。
- 已生成可追溯报告输入 `reports/dau-readout-20260716/artifact.json`；portable report renderer 两次在桌面宽度出现自身横向溢出校验失败，未生成可交付 HTML，失败截图已清理。此为报告渲染器交付阻塞，不影响数据库统计结论。

# 2026-07-16（虚拟支付审核整改具体步骤）

- 用户再次提供微信官方运营指南并要求明确下一步。结论：平台虚拟支付能力虽然可能已经开通，但代码尚未接入；仓库没有 XPay/requestVirtualPayment，仍是普通 JSAPI + Papay。
- 最小合规方案是将现有轻度/标准/进阶的月卡、季卡、年卡配置成 9 个虚拟道具，使用 `short_series_goods`。一次性购买不受 DAU 1 万订阅门槛限制；自动续费、动态补差升级先从提审版本关闭。
- 公众平台操作由用户完成商品配置并获取 offerId/productId；AppKey 放密钥配置。编码部分由项目代理在用户明确要求后实施：fresh code2Session、双 HMAC 签名、requestVirtualPayment、发货通知与查单双保险、幂等会员开通、退款、关闭旧支付入口。
- 现有会员和历史支付记录保持不变，仅迁移新订单；本轮未改业务代码或配置。

# 2026-07-16（临时允许重复昵称）

- 用户因注册被昵称重复限制阻塞，要求立即临时允许重复昵称。
- 后端已移除注册创建与个人资料更新的昵称占用校验；昵称内容格式、长度和敏感词校验保持不变。
- 迁移改为幂等删除 `idx_weapp_user_nickname_normalized_unique`，确保既有数据库索引不再阻塞重复昵称。
- 定向认证、用户服务和迁移模块测试均通过。用户明确授权后，已对 Apollo `local` 实际指向的远程 PostgreSQL `public` schema 成功运行 `go run ./cmd/migration -config-dir .`；服务尚未重启/部署，待用户更新后端。
# 2026-07-16（再次提交并同步 main/dev）

- 用户要求将当前有效工作区修改提交，并让本地与远端 `main`、`dev` 保持同一提交后推送到云端。
- 本次提交范围包括“今天吃什么”推理动态与请求可靠性、包装营养标签 kJ/kcal 归一化、临时允许重复昵称及唯一索引迁移调整，以及对应测试代码和状态记录。
- 会话附件目录 `.codex-remote-attachments/` 不纳入版本库。

# 2026-07-16（恢复注册默认三天试用）

- 用户发现近期新注册用户没有默认三天基础会员。根因是 2026-06-26 礼券体系将注册试用变为待领取 `registration_trial` 礼券，领取前不会创建 `user_trial_entitlements`，会员状态的 `trial_active` 因而为 false。
- 用户决策：当前立即恢复注册即生效的 3 天试用；理想的“告知用户并授权领取”后续单独做，不能在本轮替代默认权益。
- 已移除登录服务对注册试用礼券的依赖，覆盖微信小程序、App 微信、短信及测试密码注册。前端现有 `trial_active` 展示无需变更。认证服务、认证 handler 与 app 装配测试通过。
- 用户明确授权后，已通过 Apollo `local` 实际连接的远程 PostgreSQL `public` schema 运行 `go run ./cmd/migration -config-dir .`，命令成功完成。迁移对缺少权益的旧 `registration_trial` 待领取/已过期礼券，从执行时起补发完整试用期，并标记为已启用；未来新用户仍需部署本轮后端代码才会立即生效。
