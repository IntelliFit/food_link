# 2026-07-23

- 当前任务：排查微信小程序审核驳回的两个问题：iOS 端虚拟支付能力与未登录状态下圈子刷新失败。
- 用户确认此前已成功接入虚拟支付并完成过真实支付，但本次审核明确要求包含 iOS 端内购的全终端虚拟支付能力。
- 待核实：当前代码是否存在 iOS 专用支付适配/后台能力开关；圈子公开动态刷新是否被登录态接口或生产环境接口失败连带阻断。
- 代码核对：前端已使用统一的 `wx.requestVirtualPayment`；官方 iOS 文档说明平台按设备自动路由到 Apple 支付。无需额外 iOS 前端 API，但 Apple 支付只支持现网、不支持沙箱。
- 最后一次生产预检记录显示 Apollo main 当时仍为沙箱 AppKey、`sandbox=true`；此前道具也处于开发版本未发布且苹果 IAP 开关关闭。若之后未切换，这是本次 iOS 审核失败的直接解释。当前回调缺少 `xpay_subscribe_ios_refund_query_notify` 退款问询处理。
- 圈子未登录路径只调用 `/api/community/public-feed`。本次只读实测 main/dev 公开 Feed 与会员套餐接口均 HTTP 200，当前服务正常；审核时失败更可能来自送审包正式域名/版本不一致或当时瞬时故障。需核对微信 request 合法域名包含 `https://api.healthymax.cn`；XPay 消息回调配置不等于前端 request 域名配置。
- 微信开发者工具自动化端口 3001/9420 均关闭，生产 SSH publickey 被拒，本轮无法读取送审包控制台或审核时段服务器日志，未改业务代码。
- 用户截图确认虚拟支付基础配置中“苹果 IAP 支付”已经显示“已开通”。“平台路径”不是官方 iOS 道具直购的必选条件，食探当前无需开启。下一步证明现网接入应以 iPhone 实际唤起 Apple 支付、虚拟支付交易订单出现 Apple 支付渠道、发货回调成功并开通会员为准，而不是只看后台开关。
- 用户要求让虚拟支付接入在会员页更醒目。已在套餐价格卡下新增支付方式说明卡，明确 iOS Apple 支付/其他平台微信支付和不自动续费；按钮、确认弹窗同步显式写“虚拟支付”，弹窗按当前平台提示入口。未改支付接口和补差计算。TypeScript、ESLint、Jest 17/64、diff check 均通过，watch 已编译；开发者工具 3001/9420 自动化端口未开启，运行时截图与点击验证待完成。
- 用户提供的官方流程图实际属于 Windows 客户端支付能力：电脑微信中的短剧外层素材、集数选择和二维码支付链路，不是普通会员道具购买页的标准样式。食探自定义会员页无需照图重做；点击按钮后原生支付面板才是支付能力的可见差异。当前前端确实调用 `requestVirtualPayment`，后端返回 `short_series_goods` 道具直购参数。
- 审核登录网络失败已找到确定性根因：登录页自身根据 `envVersion` 显示“开发版”，审核截图证明审核容器返回 `develop`；production 上传产物又把 `develop` 映射到 `http://127.0.0.1:3010`，导致审核设备请求自身端口而失败。本地因有 3010 服务而正常，体验版因 `trial` 走 `https://dev.api.healthymax.cn` 而正常。dev/main 公网 `/api/public-config` 当前均 200，排除公网后端普遍宕机。后续应调整 production 映射，让审核态可达公网 API，本地 localhost 仅由开发构建或显式 override 使用。
- 用户指出实际环境变量管理不应被简化成修改 `.env`。进一步核对：小程序 `config/index.ts` 当前确实会将根目录/应用目录的 `.env[.<mode>]` 作为缺省来源，但系统或命令行 `process.env` 优先；根因与变量来源无关。最终修复应在构建配置中按 `NODE_ENV` 固化安全边界：production 的 `develop` 使用正式/审核公网 API并阻止 localhost override，development 的 `develop` 才允许本地 3010。
- Apollo 与上述前端构建变量是两条不同配置链：Apollo 仅由 Go 后端读取，微信前端没有 Apollo 客户端；当前 `dist/common.js` 已把 release/trial/develop 地址固化为字符串。审核设备在发出首次请求前无法依赖 Apollo 决定后端地址，因此修复点仍是前端 production/development 路由逻辑。

## Android 端并行工作记录

## Android 识别记录图标对齐

- 五个操作图标改为与小程序同一套 iconfont 字形及颜色，并通过固定字形盒、行高、水平/垂直对齐和关闭 Android font padding 居中。
- Mobile typecheck 与 diff check 通过；weapp-devtools 3001 实际打开小程序识别记录页，操作弹层元素存在且 0 error。
- 安卓前台恢复时停在 `Reloading...`，未取得新版菜单截图，不能宣称安卓运行态视觉已完成验证。

## Android 与小程序我的收藏

- 用户明确补充：Markdown 残留同时存在于小程序和 Android，两端都必须修复；Android 还缺少小程序的微量营养区域。
- 两端收藏卡片均新增展示层 Markdown 纯文本化，覆盖食谱名称、描述和标签，数据库原始内容不改写；用户截图中可见的 `#识别记录` 也已在两端源码改为纯文本 `识别记录`。
- Android 同步小程序的微量营养算法：按 `items[].nutrients × ratio` 汇总，固定顺序最多显示 4 项，并兼容钠、钾等 camelCase/snake_case 字段。
- `npm --workspace apps/wechat run typecheck` 与 `npm --workspace apps/mobile run typecheck` 均通过。
- Android 模拟器真实西瓜收藏显示膳食纤维 8.8g、糖 136g、钠 22mg、钾 2464mg；截图 `.local-state/runtime/android-favorites-fixed.png`。
- 按项目要求使用 weapp-devtools：通过微信开发者工具 CLI 启动 3001 自动化端口，重启到 `packageExtra/pages/recipes/index`，收藏页微量营养显示正常且运行时错误日志为 0。
- 小程序去 `#` 的最终运行时验证被旧构建产物阻塞：当前无 Taro watch 进程，`apps/wechat/dist/packageExtra/pages/recipes/index.js` 时间仍为 2026-07-20 12:18；开发者工具截图仍显示旧 `#识别记录`。按项目规定未擅自启动开发服务器，需用户运行 `npm run dev:weapp` 后再由 weapp-devtools 复验。
- 用户随后发来的“仍未改变”截图实际为 Android（碳水显示 167g 而非小程序的 167.2g）。Android Reload 一度白屏，logcat 明确报 `Cannot connect to Expo CLI`、目标 `10.0.2.2:8081`；恢复 `adb reverse tcp:8081 tcp:8081` 后重新 Reload。
- 最终 Android UI 层级验证：`#识别记录` 不存在，纯文本 `识别记录` 存在，微量营养仍完整；截图 `.local-state/runtime/android-favorites-no-markdown.png`。
- 用户随后明确要求重新运行小程序前端。已按规范启动 `npm run dev:weapp`（未运行生产构建），watch 进程保持运行；首次编译约 55 秒完成，`dist/packageExtra/pages/recipes/index.js` 更新于 18:34:50。
- weapp-devtools 重新载入收藏页后，标签已显示纯文本“识别记录”，下方膳食纤维/糖/钠/钾完整，错误日志 0；截图 `.local-state/runtime/wechat-favorites-no-markdown.png`。

## Android 好友管理标题与页签

- 用户要求 Android 好友页与小程序一致：导航栏显示居中的「好友管理」，页签必须写「收到的请求」。
- `RootNavigator` 的 `Friends` Screen 已改为 `title: '好友管理'`、`headerTitleAlign: 'center'`。
- Android 好友页第二个页签已从「收到请求」改为「收到的请求」。
- `npm --workspace apps/mobile run typecheck` 通过。
- 后续复核确认本地后端 3010 一直在监听，`GET /api/health` 返回 200；Metro 8081 也正常。此前未进入页面是 AVD 焦点跑到系统 Google App，而不是后端未启动。
- 已重新启动 Food Link App 本身并从「我的→好友管理」实际进入页面；截图和 UI 层级确认「好友管理」标题居中、「收到的请求」显示正确，旧「收到请求」不存在。

## Android 识别记录与小程序同步

- 对照小程序 `analyze-history` 当前源码和用户两张截图，确认 Android 不仅视觉不同，还存在批量删除漏掉排队中、识别中和失败任务的功能差异。
- Android 页面标题统一为「识别记录」；搜索框改为 300ms 防抖自动查询并删除独立搜索按钮。
- 卡片信息层级改为小程序口径：`图片/文字记录 · 识别出 N 项食物`、今天/昨天/日期 + 上午/下午时间、状态放在同一时间行；删除 Android 独有餐次标签，普通/快速模式不再额外展示，仅精准模式保留「精准」。
- 一键删除范围同步为 pending、processing、failed、done 且未记录；确认弹窗同步为「确认丢弃」「确定丢弃 N 条未记录的任务吗？丢弃后不可恢复。」，并使用 `Promise.allSettled` 保留成功删除结果、提示失败数量。
- `npm --workspace apps/mobile run typecheck` 通过。
- 模拟器运行时首次进入时遇到 Metro 增量热更新残留导致 Hermes 报 `selectMenuOpenTask doesn't exist`；源码定义存在且类型检查通过。应用级 Reload 清掉错误后回到首页，随后自动化点击被现有首页日期/宠物浮层导航状态干扰，未取得改后页面最终截图；未擅自重启用户 Metro。

## Android 外观试验箱入口

- 用户要求 Android 与小程序的未开放逻辑一致：“外观试验箱”右侧不是“打开试验箱”，而是“即将开放”，点击后不应发生页面跳转。
- Android 宠物页已将该项由可点击 `Pressable` 改为普通 `View`，显示“即将开放”。
- 用户进一步确认“即将开放”应为灰色禁用态；已复用现有 muted 状态样式覆盖原绿色操作文字。
- 已删除 `RootStackParamList.PetLab`、`RootNavigator` 中的 `PetLabScreen` 导入和 `Stack.Screen` 注册，避免保留隐藏导航入口。
- 首次 Mobile TypeScript typecheck 通过；灰色样式补充后复跑遇到当前工作区 `DetailScreens.tsx:1630-1632` 的既有/并行 `stringOrUndefined` 未定义错误，与本次样式行无关。模拟器最终点击验证因桌面工具权限服务拒绝后续界面读取而未完成，代码层已确认没有该导航调用和路由注册。

## Android 宠物奖励领取状态

- 用户明确产品逻辑：点击「离线小惊喜→领取奖励」并成功后，奖励卡片与首页宠物旁的可领奖图标都应消失。
- Android 领取成功后已立即更新本地宠物和事件状态；首页 dashboard 即使命中本地缓存，也会独立刷新宠物摘要，避免沿用旧的可领奖状态。
- 运行时发现原事件确实已领取，但后端 `Summary` 随即返回了 2026-07-07 的另一条历史未领取事件，因此图标看似没有消失。
- 后端已改为只读取请求日期前一日的 `offline_review` 事件，不再调用“任意最新未领取事件”递补旧奖励。
- `npm --workspace apps/mobile run typecheck` 与 `go test ./internal/pet/service -run 'TestSummaryCreatesStablePetAndSingleOfflineEvent|TestClaimEventIsIdempotent' -count=1` 通过。
- 本轮尝试检查 3010 监听进程准备重启时，桌面权限自动审批两次超时；为避免误杀进程，尚未再次重启后端，模拟器最终状态待重启后确认。

## 本机与云端同步

- 已获取 GitHub `origin` 最新引用。
- 当前分支 `daily-2026-07-20（2）` 从 `9433527e` 快进到 `origin/dev` 的 `f9103436`。
- 本地 `dev` 已同步至 `origin/dev`，本地 `main` 已同步至 `origin/main` 的 `f251a12b`。
- `HEAD...origin/dev`、`dev...origin/dev`、`main...origin/main` 均为 0 ahead / 0 behind。
- 同步前工作区包含 7 个已修改文件和 3 个未跟踪补丁；已先保存到 `codex-presync-2026-07-23`，同步后恢复成功，无冲突。
- 为便于回退，安全 stash 暂未删除，当前位于 `stash@{0}`。

## Android 与小程序功能同步

- 用户纠正验收口径：要求每一个功能点完全一致，不能只按页面或路由判断。
- 已静态提取小程序 67 个页面的 API、缓存、事件和 Taro 原生能力调用，并提取 Android 63 个 Screen 的 API 与原生能力调用。
- 已发现图片分析精准模式、结果纠错/反馈/食谱、历史保存、保质期图片识别/订阅、积分券、统计关注项、分享邀请、账号安全等多处功能差异。
- 新增 `docs/android-wechat-functional-parity.md` 作为功能契约矩阵和完成门槛。
- Android 本地构建与运行已打通：Gradle Debug 构建成功，Pixel_7 AVD 上 `cn.healthymax.foodlink` 成功加载 React Native 登录页，协议勾选交互通过。
- 新增 Android 食谱详情页，并补齐食谱一键记录后的首页快照刷新与摄入变化事件；Mobile 类型检查通过。
- 测试集方案已提出；受项目技能的用户确认制约束，新增测试文件前等待用户明确确认。

## Android 头像、更多功能与健康报告修复

- 真实接口核查确认测试用户头像字段非空，Android 也已成功下载；显示的灰色人像就是服务端保存的微信默认头像，并非 RN Image 加载失败。取消了头像/背景选图前不必要的相册权限申请，用户可在个人主页重新上传真实头像。
- 已完整删除 Android「我的→更多功能」入口、导航类型、路由和页面；模拟器滚动到底确认只剩隐私设置、关于我们、清除缓存、退出登录。
- 健康报告上传新增「拍摄 / 从手机相册选择 / 取消」来源选择；相册直接进入 Android Photo Picker，相机只在实际拍摄时请求权限。模拟器已验证交互。
- OCR “AI 识别服务配置异常”根因是万界网关搭配了硬编码豆包模型。后端现按网关选择 `qwen3.6-flash` 或豆包模型。
- 验证：Mobile TypeScript typecheck 通过；`go test ocr_service.go ocr_service_test.go` 通过；Gradle `:app:assembleDebug` 成功。完整 user/worker 包测试被本机 embedded-postgres 的 `initdb 0xc0000135` 阻塞，不是本次断言失败。
- 对用户提供的 `C:\Users\hp\Desktop\测试图片\1.png` 进行运行时核查：图片清晰且上传成功，任务失败原文是 `model not found`；当前 3010 服务仍为 15:31 启动的旧进程，尚未加载按网关选择 Qwen 的修复。需重启本地后端后复点“重新识别当前报告”。
- 用户随后明确授权重启本地后端。已只停止确认过的 food_link 后端进程树并隐藏启动新进程；新 PID 31648，健康检查 200。
- 将 `1.png` 推入模拟器 `/sdcard/Download/foodlink-health-report-1.png`，通过 APP 相册实际选择并点击系统 Photo Picker 的 `Add (1)`。上传和 OCR 全链路成功：任务 `cb4232d8-c29c-4edf-8145-1d7abc0d643d` 约 4.9 秒完成，状态 done、error 为空，识别文本与图片中的张明健康档案内容一致。
- Android 识别记录页已按小程序补齐关键操作与副作用：快速记录、原内容重识别、收藏到我的餐食、分享到公共食物库、删除；批量丢弃范围及部分成功处理同步小程序。复用现有共享 API Client，没有新增后端接口。`npm run typecheck:mobile` 与 `git diff --check` 通过；Mobile 无 lint script。安卓运行态能打开识别记录列表和结果页，但页面栈受宠物悬浮交互干扰，未拿到稳定菜单截图；小程序自动化端口 9420/3001 未开启。

## Android「我的」主页同步

- 当前小程序代码包含“邀请好友得会员”和“账号安全”，Android 原主页缺少这两个直接入口；现已接入已有 `InviteFriends` 与 `AccountSecurity` 页面，没有新增接口。
- 同步服务项说明、宠物奖励积分余额、赚积分任务说明、会员卡“奖励积分（一直持有）”及未知会员档位“会员”回退。
- 页面版本优先使用根 `package.json` 的 `3.0.2`，`apps/mobile/app.json` 同步为 `3.0.2`，不再显示历史 `v0.0.1`。
- `npm run typecheck:mobile` 通过。模拟器实际渲染确认奖励积分文案和邀请入口，UI 层级确认账号安全入口；后续滚动被残留识别结果导航状态抢走，账号安全点击跳转未完成最终实测。
- `weapp-devtools` 连接 9420 与 3001 均失败，原因是微信开发者工具未开启自动化服务；小程序基准来自当前源码和用户截图。

### 用户纠正

- 用户明确要求 Android 主页以其提供的小程序实际页面为准，禁止根据仓库里尚未出现在小程序实际页面的代码提前增加功能。
- 已撤销“邀请好友得会员”主页入口，并移除 Android 独有的列表副标题、右侧箭头及顶部主题切换按钮；“账号安全”因小程序实际页面存在而保留。
- 模拟器截图 `android-profile-strict-2.png` 已确认邀请入口和副标题消失，主页列表恢复为仅图标、标题、徽标的结构。Mobile typecheck 与 diff check 通过。

## Android 个人主页与小程序同步

- 用户继续指出 Android 个人主页存在大量与小程序不一致之处。
- 已通过 `weapp-devtools` 3001 端口打开小程序 `packageExtra/pages/profile-settings/index`，保存实际运行截图，并点击“编辑资料”确认底部编辑弹层存在。
- Android `ProfileSettingsScreen` 已同步顶部操作、底部编辑弹层、收藏隐私与收藏食谱卡、动态紧凑食物卡/营养摘要，以及输入“注销账号”的二次确认。
- `npm run typecheck:mobile` 和补丁格式检查通过；安卓运行实例可热更新且未发现 React Native 错误日志，但自动点击进入目标页时误入其它功能，最终个人主页截图待人工进入该页后复核。

## Android 会员进度条配色

- 用户指出小程序会员“系统可用”进度条为橙色，而 Android 错误显示为白色。
- Android 已按 `membership.is_pro` 区分配色：会员主进度条使用橙色 `#f59e0b`，非会员保持白色；奖励积分分段已填充部分也同步为会员橙色、非会员白色。
- 模拟器热更新后，`食探会员 / 可用 190/200` 的主进度条已实际显示为橙色；最终截图 `.local-state/runtime/android-profile-orange-progress.png`。
- weapp-devtools 3001 已重启到小程序 `pages/profile/index`，运行时错误日志为 0；当前 MRC 截图能力报 `fail to capture screenshot`，但页面导航验证成功。
- `ProfileScreen.tsx` 补丁格式检查通过；全量 Mobile typecheck 当前被并行中的 `TertiaryMigrationScreens.tsx` 多个保质期 AI 样式定义缺失阻塞，与本次进度条改动无关。

## 推送远端 dev

- 用户要求把当前本地成果以 `fix:` 提交并推送到远端 `dev`。
- 已确认提交前 `HEAD` 与 `origin/dev` 同为 `f9103436`，不存在远端领先或本地分叉。
- Mobile/WeChat TypeScript typecheck、宠物 service 定向测试、用户 OCR service 定向测试及 `git diff --check` 均通过。
- 功能提交为 `75e40ec8 fix: 同步安卓端功能与小程序`；已通过 SSH 推送到 `origin/dev`，推送后远端引用与本地提交一致。

## Android 新增保质期与小程序同步

- 用户以小程序截图纠正 Android 原“单条基础表单”实现，要求新增保质期完整保持一致。
- 已加入多图选择、图片上传、`/api/expiry/recognize` AI 预填、识别说明、多食物草稿、AI 置信度/估算提示、完整与自定义分类、模板、追加/删除、批量创建和动态保存数量。
- Android 模拟器实际进入新增页，页面顶部拍照识别区、2 积分标识、模板和食物卡均正常渲染；导航标题已从写死“编辑保质期”修正为新增/编辑动态切换。
- `npm run typecheck:mobile`、`git diff --check`、API Client Jest 29 项测试全部通过。
- 本轮功能同步已提交并推送远端 `dev`：`75e40ec8 fix: 同步安卓端功能与小程序`。
- 用户新增长期约定：每个独立功能完成后直接做一个 `fix` 提交；可以直接创建 PR，不再逐次询问是否提交或建 PR。
