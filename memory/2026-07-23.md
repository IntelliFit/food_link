# 2026-07-23

- 当前任务：排查微信小程序审核驳回的两个问题：iOS 端虚拟支付能力与未登录状态下圈子刷新失败。
- 用户确认此前已成功接入虚拟支付并完成过真实支付，但本次审核明确要求包含 iOS 端内购的全终端虚拟支付能力。
- 待核实：当前代码是否存在 iOS 专用支付适配/后台能力开关；圈子公开动态刷新是否被登录态接口或生产环境接口失败连带阻断。
- 代码核对：前端已使用统一的 `wx.requestVirtualPayment`；官方 iOS 文档说明平台按设备自动路由到 Apple 支付。无需额外 iOS 前端 API，但 Apple 支付只支持现网、不支持沙箱。
- 最后一次生产预检记录显示 Apollo main 当时仍为沙箱 AppKey、`sandbox=true`；此前道具也处于开发版本未发布且苹果 IAP 开关关闭。若之后未切换，这是本次 iOS 审核失败的直接解释。当前回调缺少 `xpay_subscribe_ios_refund_query_notify` 退款问询处理。
- 圈子未登录路径只调用 `/api/community/public-feed`。本次只读实测 main/dev 公开 Feed 与会员套餐接口均 HTTP 200，当前服务正常；审核时失败更可能来自送审包正式域名/版本不一致或当时瞬时故障。需核对微信 request 合法域名包含 `https://api.healthymax.cn`；XPay 消息回调配置不等于前端 request 域名配置。
- 微信开发者工具自动化端口 3001/9420 均关闭，生产 SSH publickey 被拒，本轮无法读取送审包控制台或审核时段服务器日志，未改业务代码。
- 用户截图确认虚拟支付基础配置中“苹果 IAP 支付”已经显示“已开通”。“平台路径”不是官方 iOS 道具直购的必选条件，食探当前无需开启。下一步证明现网接入应以 iPhone 实际唤起 Apple 支付、虚拟支付交易订单出现 Apple 支付渠道、发货回调成功并开通会员为准，而不是只看后台开关。
- 用户要求让虚拟支付接入在会员页更醒目。已在套餐价格卡下新增支付方式说明卡，明确 iOS Apple 支付/其他平台微信支付和不自动续费；按钮、确认弹窗同步显式写“虚拟支付”，弹窗按当前平台提示入口。未改支付接口和补差计算。TypeScript、ESLint、Jest 17/64、diff check 均通过，watch 已编译；开发者工具 3001/9420 自动化端口未开启，运行时截图与点击验证待完成。
- 用户提供的官方流程图实际属于 Windows 客户端支付能力：电脑微信中的短剧外层素材、集数选择和二维码支付链路，不是普通会员道具购买页的标准样式。食探自定义会员页无需照图重做；点击按钮后原生支付面板才是支付能力的可见差异。当前前端确实调用 `requestVirtualPayment`，后端返回 `short_series_goods` 道具直购参数。
- 审核登录网络失败已找到确定性根因：登录页自身根据 `envVersion` 显示“开发版”，审核截图证明审核容器返回 `develop`；production 上传产物又把 `develop` 映射到 `http://127.0.0.1:3010`，导致审核设备请求自身端口而失败。本地因有 3010 服务而正常，体验版因 `trial` 走 `https://dev.api.healthymax.cn` 而正常。dev/main 公网 `/api/public-config` 当前均 200，排除公网后端普遍宕机。后续应调整 production 映射，让审核态可达公网 API，本地 localhost 仅由开发构建或显式 override 使用。
- 用户指出实际环境变量管理不应被简化成修改 `.env`。进一步核对：小程序 `config/index.ts` 当前确实会将根目录/应用目录的 `.env[.<mode>]` 作为缺省来源，但系统或命令行 `process.env` 优先；根因与变量来源无关。最终修复应在构建配置中按 `NODE_ENV` 固化安全边界：production 的 `develop` 使用正式/审核公网 API并阻止 localhost override，development 的 `develop` 才允许本地 3010。
- Apollo 与上述前端构建变量是两条不同配置链：Apollo 仅由 Go 后端读取，微信前端没有 Apollo 客户端；当前 `dist/common.js` 已把 release/trial/develop 地址固化为字符串。审核设备在发出首次请求前无法依赖 Apollo 决定后端地址，因此修复点仍是前端 production/development 路由逻辑。
