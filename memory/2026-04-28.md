# 2026-04-28

- 食物分析链路已补充粗粒度地理位置上下文，采用“已有授权则静默带上”的最小侵入方案：
  - `src/utils/api.ts` 为 `submitAnalyzeTask / submitTextAnalyzeTask / continuePrecisionSession` 统一增加 `province / city / district` 注入
  - 仅当小程序已授权 `scope.userLocation` 时，前端才会自动定位并调用 `/api/location/reverse` 解析地址；未授权则直接跳过，不打断分析
  - 位置结果会短期缓存，避免连续分析时重复逆地理
  - `backend/main.py` 与 `backend/worker.py` 已把位置上下文接到图片/文字分析与精准模式继续链路
  - prompt 口径明确为“位置仅作弱提示，用于辅助理解地域菜名、口味和常见分量；若与图片/文字冲突，仍以图片/文字本身为准”
- 本轮校验：
  - `python -m py_compile backend/main.py backend/worker.py` 通过
  - `npx tsc --noEmit` 仍被项目内大量历史类型错误阻塞，未发现与本次位置改动直接相关的新报错定位
  - 按项目要求尝试 `weapp-devtools` 运行态验证，但 `mrc where --port 9420` 失败，当前机器未开启微信开发者工具 9420 自动化端口，因此没能拿到截图证据

- 食物测试后台继续收口多模型结果展示：
  - 单图页顶部“分析摘要”不再混用某个模型私有的 `估算总重量 / 估算总热量`
  - 顶部只保留全局摘要：图片数、参与模型数、标签模式、标准总重量、最佳综合分模型/最低总重误差模型
  - 每个模型结果卡单独展示自己的 `估重 / 热量 / 回答时长`
  - 批量页顶部新增各模型平均回答时长，方便做生产环境模型/提示词选型
- 食物测试后台 benchmark 进一步改成“只关注食物名称 + 重量”的实验页：
  - 评估器本身不用大改，仍然只做食物名称匹配与重量误差计算
  - `custom` 模式提示词允许只返回 `items[].name + items[].estimatedWeightGrams`
  - 测试后台单图/批量详情移除描述、建议、PFC、吸收率、情境建议、热量等主展示内容，改为突出 `识别结果摘要 / 食物明细 / benchmark / 回答时长`
- 后端测试分析结果不再为缺失的 `description / insight` 注入误导性的默认文案
- benchmark 报告修正：
  - 对于“模型识别到了一个候选食物，但 DeepSeek 判定它与标准标签不匹配”的情况，报告不再直接显示“未识别”
  - 现在会保留该候选识别结果、重量和匹配类型
  - 例如 `炖冬瓜（含虾米少量）` 之后应能看到 `白萝卜` 这类错误候选，而不是只在下方“额外识别”里孤立出现
- 继续修正 DeepSeek evaluator 的 rejected candidate 展示：
  - prompt 明确要求每个 `expected_item` 都返回 assignment；即使 `accepted=false`，也应尽量给出 best candidate 的 `predicted_index`
  - 增加本地“未匹配候选回填”兜底：若 DeepSeek 漏掉 rejected candidate，就用主体食材宽松比较回填展示候选，例如 `炖冬瓜（含虾米少量） -> 清炒冬瓜`
  - 该回填只影响报告展示，不改变正式评分；`status` 仍为 `missing`，`Food F1` 不会被错误抬高
- 修复本地脱敏测试集标签口径：
  - `D:\创业\healthymax\food_test_sanitized_20260424\labels.txt` 与 `...\food_test_sanitized_20260424_mini10\labels.txt` 中，单食物样本已从纯 `total` 改成单项 `items`
  - 已补上的样本包括：德芙巧克力、橙汁、面包、米饭、咸鸭蛋、橙子、黑咖啡、牛肉馅酥脆饼干、牛油拌饭、牛油炒饭、小橘子、芝士夹心饼干
  - `不知名食物 89g`、`空包装 0g`、以及只有整餐总重但没有逐项分重的多食物样本，先继续保留 `total`
  - 私有映射表 `food_test_sanitized_20260424_private_mapping.csv` 已同步改成对应的 `items/total` 口径
- evaluator 继续适配新 prompt 字段：
  - `backend/test_backend/utils.py` 现在会把 `isMixedDish / count / confidence` 透传给 DeepSeek evaluator，并在 prompt 中补充混合菜匹配规则与 expected/predicted 总重量上下文
  - `backend/worker.py::_parse_analysis_result_items(...)` 已补保留 `isMixedDish / count / confidence`，避免主链路在进入评测前把这些字段丢掉
  - 目前主评分仍只基于食物匹配与重量误差；`count / confidence / totalEstimatedWeightGrams` 暂不直接计入 benchmark 分数
- 本轮未替用户启动本地前后端；只完成代码改动与静态校验
- 静态校验结果：
  - `python -m py_compile backend/main.py backend/test_backend/utils.py` 通过（使用 Codex bundled Python）
  - `node --check backend/static/test_backend/app.js` 通过（使用 Codex bundled Node）
  - bundled Python 未内置 `pytest`，因此这轮没有在同一运行时里继续跑 `backend/tests/unit/test_test_backend_utils.py`
- 已废弃 GitHub Actions `deploy-backend`：删除 `\.github\workflows\deploy-backend.yml`，同内容归档为 `deploy-backend.yml.disabled`；恢复时重命名回 `.yml` 即可。
- 测试后台 `custom` 模式已支持多提示词同时测试：
  - 分析体验与批量测试的提示词选择器改为可多选
  - 执行语义是“所选模型 × 所选提示词”同时并跑
  - 结果区、批量汇总和详情都会把“同模型不同提示词”拆开显示，避免实验结果混淆
