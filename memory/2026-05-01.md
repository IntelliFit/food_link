# 2026-05-01

- Discussed product direction for backfilling past food and exercise records.
- Current recommendation:
  - Support backfill.
  - Using homepage date selection as an entry is acceptable, but the chosen date must be very explicit.
  - Prefer keeping today's quick-entry flow simple while letting users enter a clear historical-day context for backfill.
  - Saving should clearly confirm the target date to reduce accidental edits to non-today records.
- User clarified the credit-spend rule for historical backfill:
  - Spend the target day's unused daily system credits first.
  - If that day is exhausted, spend today's unused daily system credits next.
  - If today's daily system credits are also exhausted, spend earned persistent credits last.
- No code change yet; this is still a product discussion, not a final decision.

- Implemented historical backfill for food and exercise records:
  - Backfill is limited to the most recent 3 days.
  - Homepage selected date now flows into food image analysis, text analysis, manual food record, and exercise record entry.
  - Non-today homepage state now shows an explicit backfill banner.
  - Backend now validates the record date window, writes historical `record_time` for saved food records, and stores credit usage snapshots in `analysis_tasks.payload.credit_usage`.
  - Historical credit consumption now follows: target-day system credits -> today's system credits -> earned persistent credits.
- Verification snapshot for the backfill implementation:
  - `python -m py_compile backend\\main.py backend\\database.py` passed.
  - Targeted frontend lint passed for the touched backfill files.
  - `weapp-devtools` runtime verification is still blocked in this environment:
    - `mrc` is not installed / not in PATH.
    - Current WeChat DevTools CLI exposes `auto --port`, not the skill doc's `--auto-port`.
    - Launching `cli auto` opened WeChat DevTools, but neither `3001` nor `9420` started listening, so no automated screenshot/interaction run could be completed.

- Implemented the membership credits split:
  - System credits still refresh the next day.
  - User-earned reward credits from invite/share now accumulate in a persistent balance.
  - Consumption order is now system credits first, then earned-credit balance.
- Added SQL migration draft: `backend/sql/add_persistent_earned_reward_credits.sql`
- Updated membership-related backend/frontend surfaces to expose:
  - `system_credits_remaining`
  - `earned_credits_balance`
  - `total_credits_available`
- Verification snapshot:
  - `python -m py_compile backend/main.py backend/database.py` passed.
  - Relevant frontend lint passed.
  - `npm run typecheck` still fails on pre-existing unrelated files under expiry / food-library / health-profile pages.
  - `weapp-devtools` runtime validation was blocked because `mrc` is not installed / not in PATH and port `9420` is not listening.
- 用户确认要修复“开发机架构影响服务器部署”的风险，要求无论开发机 ARM/AMD64，部署到 `linux/amd64` 服务器都稳定可用。
- 已将 `backend/scripts/push-docker-ccr.mjs` 改为 `docker buildx build --platform ... --push`。
- 当前默认平台固定为 `linux/amd64`，并支持通过 `DOCKER_BUILD_PLATFORM` 覆盖（如多架构构建）。
- 已同步更新 `CURRENT_TASK.md`、`DECISIONS.md`、`PROGRESS.md`。
- 用户要求把“完整部署相关操作”写入 `AGENTS.md`。
- 已完善 `AGENTS.md` 的后端部署章节：补充 Docker/Buildx 前置依赖、默认 `linux/amd64` 策略、PowerShell/Bash 平台覆盖示例、标准部署步骤、常见故障排查。
