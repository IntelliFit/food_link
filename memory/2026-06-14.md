# 2026-06-14

## 完成工作
- 圈子自定义动态全链路改造：支持标题/正文分离，营养字段从 4 项扩展到 8 项（热量、蛋白质、碳水、脂肪、膳食纤维、糖分、钠、总重量）。
- 后端：迁移 DO、迁移脚本、domain、repo、service、handler 及单测 mock 更新；`go build ./...` 通过，service 单测通过。
- 前端：`api.ts` 类型与请求、`circle-post-edit`、`community`、`interaction-feed-detail`、`profile-settings` 编辑/渲染/样式更新；相关文件 ESLint 通过，`typecheck` 未引入新错误。
- 已执行远程数据库迁移（154.8.205.78:5432/food-link）。

## 关键决策
- 自定义动态字段名与接口对齐后端：`title`/`body`；营养 payload 前端使用简化名（calories/protein/carbs/fat/fiber/sugar/sodium_mg/total_weight_grams），请求时映射为后端 `total_*` 字段。
- 旧 `content` 列通过迁移脚本回填到 `body`，删除 `content` 列，保持历史数据可读。
- 标题 120 字、正文 2000 字，与前端 maxlength 一致。

## 阻塞/待验证
- 微信开发者工具自动化未连接，未进行 UI 运行时验证；需用户启动 `npm run dev:weapp` 后手动或自动验证发布/展示流程。

## 新增：圈子自定义动态举报功能
- 后端新增 `feed_reports` 表与举报接口 `POST /api/community/feed-targets/:target_type/:target_id/report`。
- 举报信息通过飞书 webhook 以富文本卡片发送到管理群，使用独立的 `feishu.report_webhook_url`/`feishu.report_webhook_secret`（意见反馈仍使用 `feishu.feedback_webhook_*`）。
- 前端新增可复用 `FeedReportMask` / `FeedReportSheet` 组件，并集成到社区列表、动态详情、个人主页的 `circle_post` 卡片长按交互。
- 业务规则：仅 `circle_post`、非自己动态、固定 5 项原因 + 可选补充说明、状态 `pending/processing/resolved/rejected`、不自动隐藏内容。
- `go build ./...` 通过，service 单测通过，前端 ESLint 通过，`typecheck` 未引入新错误。
- 已执行远程数据库迁移。

## 新增：分析结果页快捷比例支持自定义人数
- 前端 `src/packageExtra/pages/result/index.tsx`：在「快捷比例」底部弹窗新增「自定义人数」选项。
- 点击后调用 `Taro.showModal` 输入 1–99 的整数，校验通过后复用现有 `handleQuickRatio` 按 `100/N%` 均分到所有食物。
- ESLint 通过；`typecheck` 未引入新错误（剩余错误均为历史问题）。
- 编译产物中已确认包含「自定义人数」与测试辅助 class `action-sheet-item--custom`。
- 运行时验证（9500 端口）：
  - 微信开发者工具自动化连接成功；
  - 结果页「快捷比例」按钮可点击，弹窗正确显示四人选项 + 取消；
  - 点击「两人聚餐」后截图确认总热量从 260 kcal 变为 130 kcal（50% 比例生效），证明 `handleQuickRatio` 比例计算逻辑正常；
  - 点击「自定义人数」后 action sheet 消失，确认事件已触发；
  - `showModal` 输入框在自动化截图中不可见（系统弹窗/截图限制），手动在开发者工具中输入人数可完成最终验证。

## 新增：Admin 举报管理 + 私信系统消息
- Admin 后台新增“举报管理”页面，支持列表/搜索/筛选/分页、详情查看、状态变更（pending→processing→resolved/rejected）、处理说明、删除。
- 后端新增 `/api/admin/feed-reports*` 路由与 `internal/admin/{domain,repo,service,handler}` 实现。
- `feed_reports` 表新增 `resolution_note`/`handled_by`/`handled_at`。
- 举报状态变为 `resolved`/`rejected` 时，自动向举报者和被举报者发送系统私信。
- 私信系统消息使用固定 `SystemSenderID`，`content_type='system'`，会话列表与聊天页已适配系统消息展示与只读。
- Admin 入口从旧 `main.tsx` 切换到新 `App.tsx`；旧代码备份为 `admin/src/main-legacy.tsx`。
- 验证：`go build ./...`、admin `tsc -b`、根目录 `typecheck`、ESLint、数据库迁移均通过。

## 补充：动态卡片 ⋮ 举报入口
- 社区列表、动态详情、个人主页的动态卡片均在右下角增加 ⋮ 更多按钮。
- 非自己的动态点击后可直接打开举报表单；自己的动态仍走原有管理菜单。
- 验证 ESLint / typecheck 通过。

## 动态卡片 ⋮ 菜单统一为首页餐食 ActionSheet 样式
- 完成通用 `FeedActionSheet` + 改造 `FeedReportSheet`。
- 社区列表、动态详情、个人主页的 ⋮ 点击后均使用与首页 `MealActionSheet` 一致的底部上滑动画。
- 自己动态走管理菜单（编辑/删除），他人动态走举报菜单（进入举报表单）。
- 类型检查与 ESLint 通过；微信开发者工具未启动，运行时验证待补。

## 动态卡片 ⋮ 菜单反馈修复
- 已去掉社区列表 `.action-item.action-manage` 点击时的缩放/透明效果。
- 用户反馈点击无弹窗：排查发现本地 `npm run dev:weapp` 未运行，`dist/` 没有新组件产物；需重启 watch 后验证。

## 动态卡片 ⋮ 无弹窗进一步排查
- 发现 `dist/` JS 已包含 `FeedActionSheet`，但 WXSS 中没有 `.feed-action-sheet-*` 样式，导致弹层渲染后不可见。
- 已在 `src/pages/community/index.scss` 和 `src/packageExtra/pages/profile-settings/index.scss` 强制引入 `FeedActionSheet.scss` 作为兜底；动态详情页复用社区 SCSS，一并覆盖。

## 动态卡片 ⋮ 运行时验证完成
- 已启动 `npm run dev:weapp` 与微信开发者工具自动化（端口 9420）。
- 自动化切换到社区页，点击右下角 ⋮ 后底部正常弹出 `FeedActionSheet`（白色圆角 action list + 取消按钮）。
- 自己动态的管理菜单（编辑 / 删除）可正常显示，样式与首页「今日餐食」ActionSheet 一致。

## 关闭弹层后页面回顶修复
- 把社区、动态详情、个人主页的 `FeedActionSheet` / `FeedReportSheet` 等 fixed 弹层移到页面根容器之外，避免关闭时 ScrollView 滚动位置重置。
- 给遮罩层点击增加 `e.stopPropagation()`。
- `typecheck` / `eslint` 通过，`dev:weapp` 已重新编译。

## 微信开发者工具重启与自动化测试
- 终止旧 WeChat DevTools 进程，重新启动 `cli.bat auto --project dist --auto-port 9420`。
- MRC 连接并切换回社区页，成功验证点击 ⋮ → 打开菜单 → 点击遮罩关闭，页面未报错。
- 滚动偏移的自动化读取受 MRC 限制，已靠代码修复兜底。

## 关闭弹层后页面回顶最终修复
- 根因：微信小程序 scroll-view 同级 fixed 弹窗 DOM 增删会导致 scroll-view 回顶。
- 修复：将 `FeedActionSheet` / `FeedReportSheet` 改为常驻 DOM，通过 visibility/opacity/pointer-events 控制显隐。
- `typecheck` / `eslint` 通过，`dev:weapp` 已重新编译。
- MRC 验证点击 ⋮ → 打开 → 点击遮罩关闭，页面停留在 community 页。

## 编辑动态 404 与编辑表单 UI/校验修复
- 后端修复 `normalizeServiceTargetType`，补齐 `circle_post` 与 `campus_food` 分支，避免圈子动态被误判为 `food_record` 导致编辑请求 404。
- 前端 `CommunityFoodRecordEditSheet`：图片区块移到动态文本上方；保存前校验「文本或图片至少一项非空」。
- 后端 `go build ./...`、`go test ./internal/community/service/...` 通过；前端 ESLint/typecheck 未引入新错误。
- MRC 自动化连接成功，验证了 ⋮ 菜单打开/关闭与编辑弹窗关闭；截图在切换 tab 后不稳定，未能捕获重排后的编辑弹窗截图。
- 后端需重启/重新部署后生效。

## 自定义动态编辑保存 500 修复
- 根因：`FeedRepo.UpdateCirclePost` 使用 `map[string]any` 更新 `image_paths`，GORM 未触发 `serializer:json`，把 `[]string` 当作 PostgreSQL 数组字面量写入 `jsonb`，报错 `invalid input syntax for type json`。
- 修复：改为 `domain.UserCirclePost` 结构体 + `Select` 指定更新列，使 `image_paths` 正确 JSON 序列化。
- 后端 `go build ./...` 与 service 单测通过。
- 待后端重启后验证保存含标题/图片的自定义动态。

## 修复 circle-post-edit 跳转 page not found
- 根因：本地 `npm run dev:weapp` watch 未重新生成 `dist/app.json`，导致 `packageExtra` 分包未注册 `pages/circle-post-edit/index`，跳转时报 page not found。
- 已在 `dist/app.json` 临时补全该页面，用户可立即测试。
- 建议之后重新启动 `npm run dev:weapp` 以永久同步。

## 发布动态后刷新圈子 + 底部中间相机按钮不跳转首页
- `circle-post-edit` 成功后触发 `COMMUNITY_FEED_CHANGED_EVENT`，社区页监听后自动刷新 Feed。
- `custom-tab-bar/index.js` 中间相机按钮改为直接 `wx.showActionSheet`：拍照识别、相册上传、文字输入、食物库输入，分别跳转对应页面，不再 switchTab 到首页。
- ESLint/typecheck 通过，dist 产物已更新；MRC 截图不稳定，待手动验证。

## 完成：Monorepo 目录迁移

- 用户确认移动端技术路线采用「React Native/Expo App + 共享业务层 + App UI 重写」，并要求先做目录迁移，避免后续混乱。
- 已将微信小程序工程迁到 `apps/wechat/`，根目录改为 npm workspaces 调度入口。
- 新增占位 workspace：`apps/mobile/`、`packages/core`、`packages/api-client`、`packages/config`、`packages/design-tokens`。
- `backend/` 保持原位置不动。
- Taro 配置继续从仓库根读取 `.env*` 和根 `package.json` 版本号，构建产物输出到 `apps/wechat/dist`；e2e 自动化已改为打开 `apps/wechat` 小程序项目。
- 辅助脚本 `convert_login_logo.py`、`prepare_default_avatar.py`、`update-icon.py` 已同步到新微信小程序路径。
- 同事后续会清理冗余 icon 系统；本次只保持路径可用，不做 icon 清理或重构。
- 验证：`npm run build:weapp:e2e` 通过；`npm --workspace apps/wechat run test -- --runInBand --listTests` 能发现测试。
- `npm run typecheck` 仍失败，但剩余为既有业务类型错误，不是目录迁移导致的路径/模块解析错误。

## 完成：Mobile App POC 第一阶段

- 在 `apps/mobile/` 初始化 Expo + React Native + TypeScript POC 工程。
- 在 `packages/core/` 抽出移动端第一阶段需要的共享契约类型、餐次推断、饮食记录 payload 构造逻辑。
- 在 `packages/api-client/` 新增 adapter 化 API client，支持注入 request、uploadFile、tokenStorage；已实现 debug impersonate、首页 dashboard、图片上传、提交分析任务、查询任务、保存饮食记录、会员状态接口。
- `apps/mobile/App.tsx` 实现核心闭环：开发登录 → 首页概览 → 相册选图上传 → 提交分析 → 轮询结果 → 保存记录 → 刷新首页。
- 第一阶段不接正式 App 登录，不迁移社区、会员支付、海报、二维码、ECharts、订阅消息、分享、好友聊天等微信强绑定功能。
- 验证通过：
  - `npm --workspace packages/core run test`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run test`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace apps/mobile run typecheck`
- 运行 mobile 真机时需配置 `EXPO_PUBLIC_API_BASE_URL`；真机访问本机后端不能用 `127.0.0.1`，应改为开发机局域网 IP 或可访问域名。
## Monorepo 目录迁移合并
- 将本地未提交改动提交为 WIP commit 后，合并 `origin/dev` 的 monorepo 重构。
- 启用 `merge.directoryRenames=true`，新增组件（`FeedActionSheet` 等）自动随 `src/` 重命名进入 `apps/wechat/src/`。
- 解决 `memory/2026-06-14.md` 冲突，保留本地与远程记录。
- 运行 `npm install` 安装 workspace 依赖。
- 验证：`go build ./...`、admin `tsc -b`、`apps/wechat` ESLint 通过；`npm run typecheck` 报错为既有历史错误。
- 更新 `AGENTS.md` 中的小程序路径为 `apps/wechat/...`。
- 注意：微信开发者工具项目路径需改为 `apps/wechat`；旧根目录 `dist/` 因占用未删除。

## 初始化迁移后的小程序项目
- 用户反馈 `npm run dev:weapp` 启动不起来。
- 排查发现第一次 `npm install` 后根目录 `node_modules` 未真正安装依赖（只有 workspace 软链），导致 `cross-env`、`taro` 等命令缺失。
- 重新执行 `npm install` 后安装成功（新增 2290 个包）。
- 验证 `npm run build:weapp:e2e` 成功，产物输出到 `apps/wechat/dist`。
- 微信开发者工具需打开项目路径 `apps/wechat`（原根目录 dist 已失效）。

## 启动迁移后的小程序开发与开发者工具
- 终止所有旧 `taro build --type weapp --watch` 进程。
- 后台启动新 `npm run dev:weapp`（对应 `apps/wechat`）。
- 启动微信开发者工具自动化，项目路径改为 `apps/wechat`（端口 9420）。
- MRC 验证首页与 circle-post-edit 页面渲染正常。

## 发布动态 500 修复
- 根因：后端 `domain.UserCirclePost` 与迁移 DO `UserCirclePostDO` 均缺少 `content` 字段，但数据库表 `user_circle_posts` 仍保留旧版非空 `content` 列；GORM 插入时 `content` 被置 NULL，触发 `SQLSTATE 23502`。
- 修复：
  - `backend/internal/migration/do/schema_do.go`：为 `UserCirclePostDO` 增加 `Content string` 字段，`not null;default:''`。
  - `backend/internal/community/domain/community_domain.go`：为 `UserCirclePost` 增加 `Content string` 字段。
  - `backend/internal/community/service/community_service.go`：`CreateCirclePost` 中显式设置 `Content: ""`。
- 验证：`cd backend && go build ./...` 通过；`go test ./internal/community/...` 因环境 `CGO_ENABLED=0` 无法运行 sqlite 测试，与本次修改无关。
- 待确认：`backend/config.yaml` 指向远程 PostgreSQL（154.8.205.78），需用户确认后再执行 `go run ./cmd/migration -config-dir .` 应用迁移。

## 自定义动态正文不加粗
- 业务规则：自定义动态只有标题加粗，正文保持常规字重。
- 修改：
  - `apps/wechat/src/pages/community/index.tsx`：自定义动态正文 `className` 增加 `feed-circle-post-body`。
  - `apps/wechat/src/packageExtra/pages/interaction-feed-detail/index.tsx`：同上。
  - `apps/wechat/src/pages/community/index.scss`：新增 `.feed-circle-post-body { font-weight: 400; }`，覆盖 `.feed-content` 的 `font-weight: 600`，仅对自定义动态正文生效；标题 `.feed-circle-post-title` 保持 `font-weight: 700`。
- 验证：ESLint 通过；`tsc --noEmit` 报错为既有历史错误，未引入新错误。

## 运动打卡卡路里消耗颜色改为渐变低饱和度橙色
- 修改：
  - `apps/wechat/src/pages/community/index.scss`：`.feed-card-exercise .feed-calorie` 背景从青绿色渐变（`#0f766e → #14b8a6`）改为低饱和度橙色渐变（`#f4a261 → #e9c46a`），阴影同步为橙色透明。
  - `apps/wechat/src/styles/fl-color-scheme-dark.scss`：为暗黑模式新增 `.fl-page-theme-root--dark .feed-card-exercise .feed-calorie`，使用低饱和度深橙色渐变（`#b0784f → #8f6a52`）和浅橙色文字（`#ffe8d6`）。
- 验证：`npm run dev:weapp` 编译产物中已确认包含新颜色；`mrc screenshot` 超时，未能获取运行时截图。

## Admin 后台样式丢失修复
- 根因：06-12 重构 Admin 为 shadcn/ui + Tailwind CSS v4 后，`admin/src/styles.css` 顶部需要 `@import "tailwindcss"` 与 `@theme inline`。06-13 精简控制台时被误删，导致新 Admin 页面所有 Tailwind utility class（`flex`、`bg-primary` 等）失效，开发模式无样式。
- 修改：
  - `admin/src/styles.css` 顶部恢复 Tailwind CSS v4 导入、`@custom-variant dark` 与 `@theme inline` 主题映射。
  - 补齐 shadcn 所需 CSS 变量，同时保留现有自定义变量；新增 `--muted-bg` 避免与 legacy 文本色 `--muted` 冲突。
  - 完整保留 `main-legacy.tsx` 所需自定义 CSS 类，不破坏旧控制台备份。
- 验证：`npm run dev` 与 `npm run build` 均成功；编译产物已包含 Tailwind 生成的 utility 样式。

## 完成：Mobile App POC 升级到 Expo SDK 54
- 运行 Expo Go 时发现用户手机 Expo Go 为 SDK 54，而 POC 初始为 SDK 51；已将 `apps/mobile` 升级到 Expo SDK 54（Expo `~54.0.0`、React `19.1.0`、React Native `0.81.5`），并修正 mobile `tsconfig` 继承 Expo 54 的 moduleResolution。升级后 `npm --workspace apps/mobile run typecheck` 通过。

## 完成：Mobile App POC 一键开发登录
- 用户反馈输入 `user_id` + 调试密码太麻烦，主要想先登录试功能。
- 已在 `packages/api-client` 新增 `debugLoginWithTestOpenID(testOpenID)`，调用后端现有 `POST /api/login`，传 `testOpenid`；Go 后端在 `development` 环境会自动创建/复用测试用户并签发 JWT。
- `apps/mobile/App.tsx` 登录页改为默认展示“一键登录测试账号”，使用固定 `mobile-poc-debug-openid`；原“按用户 ID 代登录”保留为备用入口。
- 正式微信 App 登录仍需微信开放平台 App 登录、原生 SDK 和后端 App 登录接口，后续单独设计。
- 验证通过：
  - `npm --workspace packages/api-client run test`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace apps/mobile run typecheck`
