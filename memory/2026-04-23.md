# 2026-04-23

- 食物识别测试后台一期改造完成：
  - 单图与批量测试支持选择 `qwen/gemini`，同一输入可同时跑多模型。
  - 测试后台分析调用当前主链路 `backend/worker.py::_build_food_prompt`，按 `standard/strict` 动态 prompt 分支，不再用 `model_prompts` 作为评测 prompt。
  - 标签标准从“整张总重量”升级为“每种食物 + 标准克重”；推荐 `labels.txt`：`bento01.jpg | 米饭=120g; 鸡胸肉=80g; 西兰花=60g`。
  - 后端返回每个模型的 `modelResults`，含总重量偏差、逐项匹配、缺失项和额外识别项。
  - 后台页面新增模型勾选、模式选择、单图标准标签输入、批量标准测试集格式说明和详情弹窗逐项评测。
- 验证：
  - `python -m py_compile backend/main.py backend/test_backend/utils.py backend/test_backend/batch_processor.py backend/test_backend/single_processor.py` 通过。
  - `node --check backend/static/test_backend/app.js` 通过。
  - 已重启本地后端，`http://127.0.0.1:3010/test-backend/login` 返回 200。
  - 使用测试 ZIP 调 `/api/test-backend/batch/prepare` 成功解析出 `expectedItems`。
- 注意：
  - `npm run dev:restart` 在当前 Windows/WSL 环境因 `scripts/restart-dev.sh` CRLF / `pipefail` 兼容问题失败，本轮改用 PowerShell 精确重启 3010 后端。
  - 未用真实图片触发上游模型，避免消耗额度；本轮验证到后台页面加载与批量解析层。

- 继续补充标准标签口径：
  - `labels.txt` 现在正式支持两种模式，可在同一个 ZIP 中混用。
  - 总重量模式：`total_old.jpg 500g` 或 `total_pipe.jpg | 总重量=620g`，返回 `labelMode=total`，评测只看整餐总重量偏差，不做逐项匹配。
  - 逐项模式：`items.jpg | 米饭=120g; 鸡胸肉=80g; 西兰花=60g`，返回 `labelMode=items`，评测总重量偏差 + 每项匹配/缺失/额外识别/克重偏差。
  - 已修复“总重量样本被当成一个叫 `总重量` 的食物去匹配，导致误报缺失/额外识别”的问题。
  - 验证：`py_compile`、`node --check` 通过；本地 3010 后端已重启；登录后上传混合标签测试 ZIP 到 `/api/test-backend/batch/prepare`，三条样本分别返回 `total / total / items`。
