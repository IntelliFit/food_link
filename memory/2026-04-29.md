# 2026-04-29

- 手动记录页又收了一轮“前台心智”：
  - 用户明确确认：前台真正需要展示给用户理解和选择的，就是两张主库：
    - `food_nutrition_library` -> 标准食物库
    - `public_food_library` -> 真实餐食库
  - `food_nutrition_aliases` 虽然有用，但只作为后台召回，不该在页面上被用户感知成第三张库
  - `food_unresolved_logs` 继续只做后台补词典日志，不进入手动记录展示层
  - 已把 `record-manual` 页文案统一收口成“双库模式：标准食物库 + 真实餐食库”
  - 筛选 chip 收口为：`全部 / 最近常吃 / 收藏餐食 / 真实餐食 / 标准食物`
  - 分组标题也同步改成：
    - `收藏餐食`
    - `真实餐食库`
    - `标准食物库`
  - 默认库说明不再提 `9925` 个 alias，只强调两张主库数量
  - 后端结果 `source_label` 也同步从 `公共库 / 营养词典` 改成 `真实餐食 / 标准食物`
- 本轮校验：
  - `py_compile` 通过
  - `eslint` 定向检查 `record-manual/index.tsx`、`src/utils/api.ts` 通过
  - 已再次尝试 `weapp-devtools`
  - `cmd /c where mrc` 能定位到 `C:\\Users\\29454\\AppData\\Roaming\\npm\\mrc(.cmd)`
  - 但直接执行 `mrc.cmd where --port 9420` 当前报 `Access is denied.`，仍无法拿截图

- 手动记录这轮补强了“大食物库利用率”：
  - 先直接核了当前 Supabase 实库，确认用户提到的“大量食物数据”确实存在，而且主要集中在标准营养库，不是公共库：
    - `food_nutrition_library = 11275`
    - `food_nutrition_aliases = 9925`
    - `public_food_library = 48`
    - `food_unresolved_logs = 40`
  - 结论是：手动记录其实已经接了 `public_food_library + food_nutrition_library + aliases + unresolved_logs` 四层，但过去前端默认只露出少量推荐卡片，且标准营养库额外字段没有被真正利用起来，所以用户体感上像“很多数据没用”
  - 已在 `backend/database.py` 把手动记录搜索/浏览补充读取 `fiber_per_100g / sugar_per_100g / sodium_mg_per_100g`
  - 公共库条目也会从 `items[].nutrients` 聚合 `fiber / sugar / sodium`
  - 浏览接口现会返回库规模统计，前端在 `record-manual` 页会直接提示“已接入 11275 种标准食物、9925 个别名”
  - 手动记录保存时，`items[].nutrients` 不再把 `fiber / sugar` 强制写 0，并新增保留 `sodium_mg`
  - 搜索卡片会优先展示 `纤维 / 糖 / 钠` 营养亮点，避免大词典只剩热量和三大营养素
- 本轮校验：
  - `python -m py_compile backend/main.py backend/database.py` 通过
  - `eslint` 定向检查 `src/packageExtra/pages/record-manual/index.tsx`、`src/utils/api.ts` 通过
  - 已再次尝试 `weapp-devtools`：`mrc where --port 9420`
  - 当前仍报 `Failed connecting to ws://localhost:9420`，说明微信开发者工具自动化端口未开启，没能补截图和运行态交互验证

- 精准模式参考物完成一轮产品化收口：
  - 结果页默认参考物改为“手掌”
  - 图片分析入口页参考物预设同步收口为“手掌 / 常规卡片 / 大卡片 / 自定义”
  - 支持把参考物名称与尺寸保存为用户级默认值，下次精准模式自动回填
  - 预设参考物尺寸按 key 分开记忆，同时支持 1 个自定义参考物
- 技术实现先复用 `health_condition.precision_reference_defaults`：
  - 前端类型：`src/utils/api.ts`
  - 结果页接入：`src/packageExtra/pages/result/index.tsx`
  - 后端保存：`backend/main.py`
- worker 的默认参考物建议文案同步改成“手掌 / 常规卡片 / 大卡片”
- 验证结果：
  - Python 语法编译通过
  - 目标文件定向 TypeScript 检查无新增报错
  - `weapp-devtools` 本轮未能完成运行态连接，当前阻塞是本机 `mrc` shim 调用权限/执行问题，不是业务代码报错

- 保质期提醒完成“多食物拍照识别预填”一期实现：
  - 前端在 `src/packageExtra/pages/expiry-edit/index.tsx` 顶部新增 AI 识别区，支持最多 `5` 张图、识别补充说明、多卡片预填和“手动再加一项”
  - 后端在 `backend/main.py` 新增 `POST /api/expiry/recognize`，复用现有视觉模型输出结构化保质期预填字段
  - 识别支持一个场景里返回多个食物；能识别出的信息自动写入卡片，缺失字段继续让用户补
  - 若缺少明确到期日，允许模型补“建议天数 + 默认到期日”，并在前端提示为估计值
  - 计费沿用现有食物分析 `2` 积分/次，不额外新建积分体系；同时通过 `payload.expiry_recognition=true` 避免混入普通分析历史
- 本轮校验：
  - `backend/main.py` 的 `py_compile` 通过
  - `src/packageExtra/pages/expiry-edit/index.tsx`、`src/packageExtra/pages/analyze-history/index.tsx`、`src/utils/api.ts` 的定向 `eslint` 通过
  - 已尝试按项目要求走 `weapp-devtools` 自动化，但当前会话执行本机 `mrc` shim 仍被权限/调用问题阻塞，没法继续连 `9420` 端口拿截图

- 收藏链路这边继续收口了一层交互：
  - 用户明确指出：首页“记录”弹层里，“我的收藏”入口离记录动作太远，不像快速记录链路的一部分
  - 现已把首页记录弹层底部快捷入口从“只有历史记录”改成“我的收藏 + 历史记录”
  - “我的收藏”文案明确写成“快速记录常吃餐食”，方便直接把历史常吃项带回今天记录
  - 同时新增了 `IconFavorite`，并为底部快捷入口补了标题 + 说明的双行排版
- 本轮校验：
  - `npm run lint -- src/pages/index/components/RecordMenu.tsx src/components/iconfont/index.tsx` 通过
  - 已按项目要求再次尝试 `weapp-devtools`，但 `mrc where --port 9420` 依旧未连通，因此没有新增截图证据

- 对底部导航 `stats` 分析页，已按“疾病风险可视化报告”方向直接改出第一版前端草案：
  - 第一屏改成“疾病预防指数总分 + 4 张风险卡 + 最小改善动作 + AI 风险解读”
  - 原有热力图、热量趋势、宏量占比、餐次结构、体重喝水、连续记录没有删除，而是整体下沉成“支撑证据”
  - 当前 4 张卡先用现有 `stats` 数据做启发式映射：高血压预防、糖尿病预防、心血管保护、体重管理友好度
  - 这版只是产品原型，不是最终科学模型；暂未接入钠、膳食纤维、加工肉、全谷物、含糖饮料等真实风险因子
- 用户随后进一步收口了表现形式：
  - 页面里不应出现“当前版本先基于什么什么估计”这类开发者提示
  - 风险卡应更小，先展示标题 + 分数 + 一句话，点击再展开更多内容
  - 其他证据模块也应改成折叠卡，整体默认不要拉得很长
  - 这些疾病卡都应默认折叠
  - 用户还希望能够按兴趣管理卡片：添加 / 移除自己更关心的疾病方向
- 已根据上述反馈继续调整：
  - 删除了开发者口吻文案
  - 风险卡改为双列小卡片，点击后展开详情
  - `AI 风险解读`、每日记录分布、热量证据、宏量结构、餐次分布、连续记录、长期健康指标都改为可折叠展开
  - 所有疾病卡默认折叠
  - 新增“我的关注”管理卡，支持添加 / 移除疾病方向，并把当前选择持久化到本地 storage
- 用户随后继续收口：
  - “我的关注”不能只是技术味很重的加减列表，要更像正式产品里的卡片管理入口
  - 风险卡折叠态还要再短一轮，默认只适合快速浏览，点击后再展开细节
- 已继续按这个方向收了一轮：
  - `我的关注` 折叠态直接展示“已关注 X 项：血压 / 血糖 / ...”
  - 展开态增加“已关注 X 项”的状态提示，并按“已关注优先”的顺序排列管理卡片
  - 风险卡默认只展示标题、分数和一句短判断；展开后再显示完整说明、依据、最小改善动作和预计提升
- 用户随后又收口了语气：
  - 需要明确补一句“仅供参考”
  - “疾病方向”这类表达太硬，希望换成更委婉的健康管理说法
- 已继续调整：
  - 顶部总分标题改成“饮食健康参考指数”
  - 顶部补充“结果仅供参考，用于观察饮食趋势，不代替医学判断”
  - `高血压预防 / 糖尿病预防 / 心血管保护 / 肠道风险关注 / 健康寿命趋势` 分别柔化为更偏“友好度 / 状态趋势”的表达
  - “我的关注”副标题里的“疾病方向”改成“健康方向”
- 本轮与分析页相关的校验：
  - `npm run lint -- src/pages/stats/index.tsx` 通过
  - `npx tsc --noEmit --pretty false` 仍被项目既有历史类型错误阻塞，未见与本次 `stats` 页直接相关的新报错
  - 已按项目要求尝试 `weapp-devtools`：`mrc where --port 9420`
  - 当前已先后遇到 `Failed connecting to ws://localhost:9420` 和 `Access is denied.`，说明微信开发者工具自动化链路这轮仍不可用，因此没能补分析页截图或运行态交互证据

- 用户再次确认会员治理执行范围没有变化：
  - 现在就取消 `凣凣尜尜 / 草！我要干俄挺 / kk / 条条` 这 4 个
  - 会员补偿仍然不做
  - 可直接执行现成脚本 `backend/sql/membership_governance_fix_20260428_cancel_selected_fake_memberships.sql`
- 用户随后继续收口了一次：
  - 其他 active 假会员先不管
  - 只继续处理“已经过期，但 `status` 还写着 `active`”的脏状态
  - 已新增脚本 `backend/sql/membership_governance_fix_20260429_expire_active_but_past_due.sql`
- 用户继续确认了更具体的数据修正范围：
  - `小马哥 / 锦恢` 先保留不动
  - 其他有真实 `paid` 会员订单的人，都应按支付真相修正会员状态
  - 因此像 `饭饭` 这种“支付表里只有月卡 paid，但会员状态表被挂成了年卡”的记录，需要改回月卡
  - 已新增脚本 `backend/sql/membership_governance_fix_20260429_reconcile_paid_memberships_except_xmj_jh.sql`

- 用户临时调整处理顺序：
  - 先不继续查线上会员充值页报错
  - 先把当前工作区所有现有改动整体提交并推送到远端
  - 发布顺序明确为：`dev` 先提交推送，再合并到 `main` 并推送，之后再复查线上表现
