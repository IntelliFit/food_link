# 2026-06-20

## User group QR actions
- Completed the requested change to remove the user group page's visible "open QR" and "copy group name" buttons and replace them with one "save to local" button.
- App implementation: `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` uses `expo-asset`, `expo-file-system/legacy`, and `expo-media-library` to save the bundled QR image to the photo library, with permission and error handling. Added `expo-media-library` permission config in `apps/mobile/app.json` and dependencies in `apps/mobile/package.json` / `package-lock.json`.
- Weapp implementation: `apps/wechat/src/packageExtra/pages/user-group/index.tsx` uses `Taro.getImageInfo` and `Taro.saveImageToPhotosAlbum`; `index.scss` removed the unused secondary action styles.
- Verification: mobile typecheck passed; scoped weapp ESLint passed; full weapp typecheck still fails on unrelated pre-existing files. weapp-devtools automator verified the user group page has only the "save to local" action and no secondary action; screenshot capture remains unavailable because `mini.screenshot` timed out and `mrc` is not installed. Android Expo Go verification was attempted but stayed on splash while Metro status was running.
- Follow-up: Android bare Manifest was updated directly with `READ_MEDIA_IMAGES`, `READ_MEDIA_VISUAL_USER_SELECTED`, and `requestLegacyExternalStorage`; app plugin config is narrowed to photo-only granular permissions.

## App community detail green strip
- Fixed the pale-green strip under the native `动态详情` header by removing `communityDetailTopWash`, removing redundant top safe-area padding, and setting `communityDetailFeedList.paddingTop` to `0` in `apps/mobile/src/screens/MigrationScreens.tsx`.
- Verification: mobile typecheck passed; target diff check only had LF/CRLF warning. Mini-program community page node check passed via automator; mini screenshot timed out. App runtime verification was blocked by the current Expo Go redbox for `expo-media-library` resolution from the previous user-group dependency change, despite the package existing locally.

## Benchmark 数据集样本 label 编辑保存 500 修复
- 问题：Admin 后台编辑 Benchmark 数据集样本 label 后 PATCH `/api/admin/benchmark/datasets/samples/:sample_id` 返回 500，pgx 无法编码裸 `map[string]any` 到 jsonb（OID 0）。
- 修复：`backend/internal/admin/repo/benchmark_repo.go` 中 `UpdateSample` 对 `items` 和 `metadata` 字段使用 `datatypes.JSONMap(...)` 包装后再 `Updates`。
- 验证：`go build ./...` 通过；运行时验证待后端重启后执行。
- 关联修复：此前 `FoodWeightLabeledSampleDO.Items` 已改为 `map[string]any`，但 `Updates` 路径未做 JSONMap 包装。

## 热量识别核心算法 LLM 温度调整为 0
- 按用户要求，将 `backend/internal/analyze/service/` 下所有硬编码的 LLM temperature 统一改为 0，包括 OfoxAI/Gemini、Doubao、DeepSeek 营养估计、鹅鸭鸡专线、精准模式默认值等。
- 涉及文件：`llm_client.go`、`analyze_service.go`、`deepseek_nutrition.go`、`goose_duck_chicken_service.go`。
- 验证：`go build ./...` 通过；运行时验证待后端重启后执行。

## 橙汁 vs 统一鲜橙多橙汁饮料匹配验证（已执行）
- 连接 PostgreSQL 实际执行算法：
  - `ResolveFood("橙汁")` → exact_canonical, score=1.0, canonical=橙汁
  - `ResolvePackagedFood("统一鲜橙多橙汁饮料")` → unresolved, score=0（包装食品库无记录）
- 确认两个名称不会命中同一条记录。

## Benchmark 名称匹配与重量误差修复
- 修复 `backend/internal/admin/service/benchmark_service.go` 中 `extractItems` 的类型断言 bug：从 JSONB 反序列化的 prediction items 是 `[]map[string]any`，原代码只断言 `[]any` 导致解析为空，预测重量为 0，相对误差显示为 100%。
- 改进 `nameSimilarity`：增加「共同前缀（≥2 rune）+ 都包含食品品类关键词」返回 0.85 的规则，使「统一橙汁饮料」与「统一鲜橙多」能正确匹配。
- 修改 Admin 前端 `admin/src/pages/benchmark-page.tsx`：耗时单位改为秒，分项对比表格重量误差改为只显示相对误差百分比。
- 验证：用数据库实际样本重新计算，结果从 `name_matched=false, total_weight_error_pct=100%` 变为 `name_matched=true, total_weight_error_pct=10%`。

## Benchmark 前端 formatPct 修复
- 发现 `admin/src/pages/benchmark-page.tsx` 的 `formatPct` 直接把 0-1 小数加 `%` 显示，未乘以 100，导致名称匹配率 1 显示为 `1.00%`、0.25 显示为 `0.25%`。
- 修复：`formatPct` 改为 `(value * 100).toFixed(2) + '%'`。
- 验证：`cd admin && npx tsc -b` 通过。

## 小程序手动记录自定义食物小数克数输入修复
- 用户反馈钙片一粒 1.2g，在小程序“手动记录 -> 自定义食物”表单中无法输入小数点；页面定位为 `apps/wechat/src/packageExtra/pages/record-manual/index.tsx` / `packageExtra/pages/record-manual/index`。
- 修复：自定义食物“实际重量 g”、热量、蛋白质、碳水、脂肪以及已选食物重量输入均改为 `Input type='digit'`；克数解析、缓存、展示和已选食物重量更新保留最多两位小数，不再把 `1.2g` 四舍五入为 `1g`。
- 后端补充：`backend/internal/utility/repo/manual_food_repo.go` 的自定义食物默认克数文案改用 `formatManualCompactNumber`，数据库字段与 handler 已支持小数，无需迁移。
- 验证：目标页面 ESLint 通过；`go test ./internal/utility/repo ./internal/utility/service -run '^$' -count=1`、`go test ./internal/utility/domain ./internal/utility/handler -count=1` 通过；全量 weapp typecheck 仍被既有无关分包错误阻塞，失败清单已不包含 `record-manual/index.tsx`。
- 运行时限制：已按 weapp-devtools 约定尝试验证，但 3001/9420 无监听，PATH 和常见安装目录未找到微信开发者工具 `cli.bat`，因此本轮无法截图或交互验证；未运行 `npm run build:weapp`，未启动/重启本地前后端服务。

## App 分享功能现状调研
- 用户询问 App 侧是否像小程序识别记录一样能一点分享到微信。本轮只读代码，未改业务代码。
- 当前 App `AnalyzeHistoryScreen` 识别记录三点菜单只有查看识别结果、重新识别、删除识别记录和取消；没有“分享到公共食物库”，也没有一键分享到微信。
- App 图片/文字识别结果页先保存为饮食记录，保存成功后进入 `RecordDetail`；`RecordDetail` 的“生成分享卡片”实际调用 React Native `Share.share` 分享文字摘要，不生成图片海报，也不直接调用微信专属分享 API。
- App 今日饮食页的“分享今日饮食”同样调用 `Share.share` 分享文字摘要；分享面板完成后调用 `/api/membership/rewards/share-poster/claim` 领取分享奖励。
- 小程序侧差异：识别记录菜单有“分享到公共食物库”；饮食记录详情/今日饮食可通过 canvas 生成海报，再调用 `Taro.showShareImageMenu` 打开微信官方图片分享/保存菜单，并带公开记录路径、二维码和邀请参数。

## App 微信登录开放平台通过后的现状复核
- 用户提供微信开放平台移动应用“智健食探”已通过，AppID 为 `wx62da2262ae3ff06c`，Android 包名 `cn.healthymax.foodlink`，应用签名 `720ab81809518f6b54a2a00c493acc07`，AppSecret 页面仍显示“生成”。
- 只读代码复核结论：后端 `/api/app/login/wechat` 已实现 App 微信登录，使用 `app_auth.wechat_app_id` / `app_auth.wechat_app_secret` 调 `sns/oauth2/access_token`，并用 `app_openid` / `unionid` 找到或创建用户。
- 只读代码复核结论：App 前端正式微信授权未实现，`apps/mobile/src/providers/AuthProvider.tsx` 当前只发送固定 `expo-go-dev-wechat-code`；`apps/mobile/package.json` 没有微信 SDK 依赖，Android 原生目录没有 `wxapi/WXEntryActivity` 或 OpenSDK 调用。
- 判断：不能只改配置中心就完成正式微信登录。需要同时配置 Apollo `app_auth.wechat_app_id=wx62da2262ae3ff06c` 和生成后的 secret，并在 App 侧接入微信 OpenSDK/native module 获取真实授权 code。
- 注意：正式 APK 必须使用与开放平台登记签名一致的 release keystore；当前 Gradle 在缺少 `FOODLINK_ANDROID_KEYSTORE_*` 时会回退 debug keystore，不能拿 debug-signed 包验证正式微信登录。
- 限制：尝试安全读取 Apollo `app-config.yaml` 只输出 app_auth 掩码状态时返回 401，当前本地凭据无法确认线上配置中心是否已经补了 `app_auth.*`。
## 2026-06-20 - App 微信登录配置中心生效验证
- 用户已把微信开放平台移动应用凭据加入配置中心后，本轮做只读验证和无效 code 探测，未修改业务代码。
- Apollo `app-config.yaml` 通过后端同款签名读取逻辑确认：`app_auth` 段存在，`wechat_app_id` 为 `wx62da...f06c` 且匹配用户提供的 `wx62da2262ae3ff06c`，`wechat_app_secret` 非空；未打印 secret。
- 线上 dev/main `/api/health` 均正常；用无效 JSON code 调 `POST /api/app/login/wechat` 时，dev/main 均返回微信侧 `invalid code (40029)`，不再是 `App 微信登录未配置 app_auth.wechat_app_id / app_auth.wechat_app_secret`，说明配置和后端换票据链路已生效。
- 本地后端 `127.0.0.1:3010` 当前未监听；登录相关 Go 单测在当前 Windows 环境被既有 `CGO_ENABLED=0 + go-sqlite3 requires cgo` 阻塞，`pkg/config` Apollo YAML 映射单测通过。
- 仍需改代码：`apps/mobile/src/providers/AuthProvider.tsx` 当前仍用固定 `expo-go-dev-wechat-code` 调后端；App 端尚未接微信 OpenSDK/native module，未实现拉起微信、接收 `SendAuth` 回调 code、再调用现有后端接口。

## 小程序手机号绑定入口现状调研
- 用户询问小程序设置里是否还有绑定手机号入口，以及 App 同时支持微信登录和手机号登录时应如何处理小程序老用户手机号缺失问题。
- 只读代码检查结论：小程序“我的/设置”当前没有主动绑定手机号入口；`apps/wechat/src/pages/profile/index.tsx` 的服务列表没有账号安全/绑定手机号，设置项只有“隐私设置”和“关于我们”。
- 小程序仍有绑定能力，但只在登录链路触发：`apps/wechat/src/packageExtra/pages/login/index.tsx` 登录成功后若 `loginData.purePhoneNumber` 为空，会展示“完善账号”弹窗，`NewUserOnboardingModals` 通过 `openType='getPhoneNumber'` 调用 `bindPhone(phoneCode)`。
- 后端已存在 `POST /api/user/bind-phone`，用微信小程序 `getuserphonenumber` 换取 `purePhoneNumber` 并写入 `weapp_user.telephone`；App “账号安全”通过 `/api/app/account/password` 写入同一个 `telephone` 字段并设置密码。
- 建议方向：补齐小程序“我的 -> 账号安全/绑定手机号”入口，复用现有绑定接口；跨端统一以 `weapp_user.telephone` 做手机号登录/绑定字段；不要一开始强制所有老用户绑定，先做可见入口和温和提醒，在手机号登录、找回账号、好友手机号搜索等场景再引导补绑。
- 补充风险：不要把 `telephone` 当作可随意编辑的资料字段；当前 `PUT /api/user/profile` 的 `UpdateProfileInput` 仍接受 `telephone`，App `/api/app/account/password` 也会写入 `telephone`，后续应收紧为“微信 getPhoneNumber 或短信验证码验证后才能绑定/换绑”，并在写入前检查手机号是否已属于其他用户。

## 小程序账号安全手机号绑定入口实现
- 已完成小程序侧“我的 -> 账号安全”入口和手机号绑定页面；本轮只改小程序前端，复用现有后端 `POST /api/user/bind-phone`。
- `apps/wechat/src/pages/profile/index.tsx` 设置列表新增“账号安全”，点击进入 `packageExtra/pages/account-security/index`；`apps/wechat/src/app.config.ts` 已注册该分包页面。
- 新增 `apps/wechat/src/packageExtra/pages/account-security/index.tsx` / `index.scss` / `index.config.ts`：页面读取 `/api/user/profile`，显示手机号“未绑定/已绑定 138****xxxx”；未绑定时使用微信 `openType='getPhoneNumber'` 授权并调用 `bindPhone(phoneCode)`，成功后更新本地 `phoneNumber` 缓存并刷新资料。
- 验证：目标 ESLint 通过；目标 `git diff --check` 通过，仅有既有 LF/CRLF warning。完整 `npm --workspace apps/wechat run typecheck` 仍失败于既有无关文件，失败清单不包含本次账号安全页或 profile 入口。
- weapp-devtools 验证限制：`mrc` 不在 PATH，`127.0.0.1:9420` 未监听，常见 Windows 微信开发者工具 `cli.bat` 路径均不存在；因此本轮无法截图或做运行时交互验证。

## 2026-06-20 - Android App 微信登录 OpenSDK 接入修复
- 状态：已把 Android App 微信登录从固定 development mock code 改为原生微信 OpenSDK 授权 code 流程；截图里的 `invalid code (40029)` 对应旧 App 仍向后端发送假 code 的问题。
- 后端/配置结论：Apollo `app_auth.wechat_app_id` 匹配 `wx62da2262ae3ff06c` 且 secret 非空；dev/main 用无效 code 探测已进入微信换票据并返回 40029，说明后端配置链路已生效。
- App 修复：新增 Android OpenSDK 依赖、`FoodLinkWechatAuth` React Native 原生模块、`wxapi.WXEntryActivity` 回调入口和 JS wrapper；生产包若缺原生模块会直接提示安装最新正式包，不再把 `expo-go-dev-wechat-code` 发给后端。
- 配置：`WECHAT_APP_ID` 支持 `EXPO_PUBLIC_WECHAT_APP_ID` / Expo extra，并在 `app.json`、App 级和根级 `eas.json` 中写入 `wx62da2262ae3ff06c`。
- 附带修复：补齐 `apps/mobile/src/components/HomeMicrosSection.tsx`，解决当前 mobile typecheck 被缺失组件阻塞的问题。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`apps/mobile/android` 下 `./gradlew.bat :app:assembleDebug` 通过；`python scripts/release_mobile_android.py --dry-run` 能解析 preview/dev 发布配置。真实微信登录仍需重打并安装 release-signed APK，且签名必须匹配开放平台登记的 `720ab81809518f6b54a2a00c493acc07`。
- 限制：debug 包不能证明微信开放平台签名校验通过；本轮未执行真实发布签名 APK 的微信登录回环验证。