# 2026-06-20

## User group QR actions
- Completed the requested change to remove the user group page's visible "open QR" and "copy group name" buttons and replace them with one "save to local" button.
- App implementation: `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` uses `expo-asset`, `expo-file-system/legacy`, and `expo-media-library` to save the bundled QR image to the photo library, with permission and error handling. Added `expo-media-library` permission config in `apps/mobile/app.json` and dependencies in `apps/mobile/package.json` / `package-lock.json`.
- Weapp implementation: `apps/wechat/src/packageExtra/pages/user-group/index.tsx` uses `Taro.getImageInfo` and `Taro.saveImageToPhotosAlbum`; `index.scss` removed the unused secondary action styles.
- Verification: mobile typecheck passed; scoped weapp ESLint passed; full weapp typecheck still fails on unrelated pre-existing files. weapp-devtools automator verified the user group page has only the "save to local" action and no secondary action; screenshot capture remains unavailable because `mini.screenshot` timed out and `mrc` is not installed. Android Expo Go verification was attempted but stayed on splash while Metro status was running.
- Follow-up: Android bare Manifest was updated directly with `READ_MEDIA_IMAGES`, `READ_MEDIA_VISUAL_USER_SELECTED`, and `requestLegacyExternalStorage`; app plugin config is narrowed to photo-only granular permissions.

## App community detail green strip
- Fixed the pale-green strip under the native `动态详情` header by removing `communityDetailTopWash`, removing redundant top safe-area padding, and setting `communityDetailFeedList.paddingTop` to `0` in `apps/mobile/src/screens/MigrationScreens.tsx`.
- Verification: mobile typecheck passed; target diff check only had LF/CRLF warning. Mini-program community page node check passed via automator; mini screenshot timed out. App runtime verification was blocked by the current Expo Go redbox for `expo-media-library` resolution from the previous user-group dependency change, despite the package existing locally.

## Benchmark 数据集样本 label 编辑保存 500 修复
- 问题：Admin 后台编辑 Benchmark 数据集样本 label 后 PATCH `/api/admin/benchmark/datasets/samples/:sample_id` 返回 500，pgx 无法编码裸 `map[string]any` 到 jsonb（OID 0）。
- 修复：`backend/internal/admin/repo/benchmark_repo.go` 中 `UpdateSample` 对 `items` 和 `metadata` 字段使用 `datatypes.JSONMap(...)` 包装后再 `Updates`。
- 验证：`go build ./...` 通过；运行时验证待后端重启后执行。
- 关联修复：此前 `FoodWeightLabeledSampleDO.Items` 已改为 `map[string]any`，但 `Updates` 路径未做 JSONMap 包装。

## 热量识别核心算法 LLM 温度调整为 0
- 按用户要求，将 `backend/internal/analyze/service/` 下所有硬编码的 LLM temperature 统一改为 0，包括 OfoxAI/Gemini、Doubao、DeepSeek 营养估计、鹅鸭鸡专线、精准模式默认值等。
- 涉及文件：`llm_client.go`、`analyze_service.go`、`deepseek_nutrition.go`、`goose_duck_chicken_service.go`。
- 验证：`go build ./...` 通过；运行时验证待后端重启后执行。

## 橙汁 vs 统一鲜橙多橙汁饮料匹配验证（已执行）
- 连接 PostgreSQL 实际执行算法：
  - `ResolveFood("橙汁")` → exact_canonical, score=1.0, canonical=橙汁
  - `ResolvePackagedFood("统一鲜橙多橙汁饮料")` → unresolved, score=0（包装食品库无记录）
- 确认两个名称不会命中同一条记录。

## Benchmark 名称匹配与重量误差修复
- 修复 `backend/internal/admin/service/benchmark_service.go` 中 `extractItems` 的类型断言 bug：从 JSONB 反序列化的 prediction items 是 `[]map[string]any`，原代码只断言 `[]any` 导致解析为空，预测重量为 0，相对误差显示为 100%。
- 改进 `nameSimilarity`：增加「共同前缀（≥2 rune）+ 都包含食品品类关键词」返回 0.85 的规则，使「统一橙汁饮料」与「统一鲜橙多」能正确匹配。
- 修改 Admin 前端 `admin/src/pages/benchmark-page.tsx`：耗时单位改为秒，分项对比表格重量误差改为只显示相对误差百分比。
- 验证：用数据库实际样本重新计算，结果从 `name_matched=false, total_weight_error_pct=100%` 变为 `name_matched=true, total_weight_error_pct=10%`。

## Benchmark 前端 formatPct 修复
- 发现 `admin/src/pages/benchmark-page.tsx` 的 `formatPct` 直接把 0-1 小数加 `%` 显示，未乘以 100，导致名称匹配率 1 显示为 `1.00%`、0.25 显示为 `0.25%`。
- 修复：`formatPct` 改为 `(value * 100).toFixed(2) + '%'`。
- 验证：`cd admin && npx tsc -b` 通过。
