# 2026-07-25

- 当前任务：排查小程序“识别记录”加载慢的问题，确认前后端是否缺少分页；若属实，实现分页并补充验证。
- 排查确认：原实现不是无限全表查询，但小程序每次请求 120 条、App 每次请求 80 条完整分析任务，接口仅支持 `limit`，没有真实分页元数据，页面也没有触底加载。
- 已完成后端 `offset` / `has_more` / `next_offset` 分页和稳定排序，小程序与 App 首屏统一 20 条、触底增量加载；增加 `history_group_key` 防止同一修正/重试链跨页重复。
- 后端 analyze 四个包测试、小程序 74 个 Jest、core 14 个 Jest、api-client 29 个 Jest、core/api-client typecheck、小程序改动 ESLint 和 diff check 均通过。
- 小程序全量 typecheck 因缺少 `@food-link/core` 类型链接、App typecheck 因缺少 `expo/tsconfig.base` 被环境阻塞。`weapp-devtools` 因本机无 `mrc`、3001/9420 未监听且无现有 dist/watch，无法完成截图和交互验证。
- 新收到生产用户“识别记录详情”停在 spinner 的截图。代码定位到该页首屏只等待 `GET /api/food-record/share/:record_id`，超时 10 秒，不是识别记录列表分页问题；旁路会员/海报请求不阻塞首屏。现有 Jaeger 可按 main service、该 route operation、时间和 duration 查根 span，并可用包含记录 UUID 的 `url.path` 精确筛选。Prometheus 没有 user_id/record_id 标签，只适合判断路由和 DB 的聚合异常；目前无 GORM 子 span。
- 用户提供生产候选 trace `2f5e6aaaff2985fccd8cb71567805464`：总耗时 3679ms，`user_food_records` 主键查询 461ms、`weapp_user` 主键查询 3217ms，HTTP 200 且响应 1439B，数据库访问占据近乎全部时间。其 UA 为 Android 16 / PKB110，而原截图明显来自 iPhone，所以不是截图事件的精确 trace，但仍证明生产详情接口存在 DB 延迟。迁移模型确认两表 ID 都是主键。另发现正式 server 启动不调用 `database.Ping()`，而连接池上限/空闲数/lifetime 仅在该函数里配置，线上可能使用默认无上限 max-open、2 个 max-idle 的池，带来连接膨胀/频繁建连风险；待 Prometheus 和生产执行计划进一步确认后再实施修复。
- 恢复生产可观测性账号：Grafana 的 `admin` 管理员密码已通过 `/api/user/password` 重置为随机强密码，公网验证旧密码返回 401、新密码返回 200；Prometheus 公网入口实际由宿主机 Nginx Basic Auth 保护，账号为 `admin`，旧密码仅存哈希，已重置为随机强密码。匿名访问 `/-/ready` 返回 401，新凭据返回 200；旧认证文件备份在 `/etc/nginx/auth/prometheus.bak.20260725T072405Z`。明文密码未写入项目文件。
- Grafana 丢失密码和 Dashboard 的根因已确认：2026-05-20/21 迁移基建目录时两次 `rm -rf` 删除了 Deployment 正在使用的 `/home/ubuntu/littlehorse-deployment/observability` hostPath。进程凭 bind mount 一直使用已删除目录，直至 2026-07-03 13:22 服务器重启；Kubelet 的 `DirectoryOrCreate` 创建空目录，新库于 13:22:51 生成。现磁盘、ext4 删除 inode 与 Git 历史均没有旧库/Dashboard，仅腾讯云控制台若有重启前系统盘快照才可能离线取证。
- 已把 Grafana 主库迁到仓库外 `/var/lib/littlehorse/grafana`，使用 Retain PV/PVC 和 `Directory` hostPath；备份存入 `/var/backups/littlehorse/grafana`，CronJob 每小时执行 SQLite online backup、完整性检查和 SHA-256，保留 7 天小时/30 天日备份。Deployment 改用 Recreate，凭据改由 Secret 注入，权限收紧到目录 750/数据库 640。
- 生产验证：手动备份成功且校验通过；创建临时 Dashboard 后重启 Pod 仍可读取，随后已删除；管理员登录与主库完整性均正常。部署代码已推送 `IntelliFit/littlehorse-deployment` main：`050178f`、`0a50f87`。
- 旧 Grafana Dashboard 无法从当前服务器找回，现有 SQLite、磁盘、ext4 删除 inode 与 Git 历史均无副本；只剩腾讯云若存在删除/重启前系统盘快照这一条离线取证可能。
- 已按 Prometheus 现有指标重建并上线四套代码化 Grafana Dashboard：服务器基础设施、Kubernetes 工作负载、FoodLink 服务、平台服务，共 139 个面板/分组；数据源和 Dashboard 均由 Git provisioning 管理。
- 新增 node-exporter、kube-state-metrics、blackbox-exporter，并采集 K8s API/kubelet/cAdvisor/CoreDNS；Prometheus 现有 27/27 targets 为 up，18/18 HTTP/TCP 探针成功。156 条 Dashboard PromQL 逐条验证为 0 错误、155 条有当前数据，唯一空项是没有近期事件的运动分析结果。
- Prometheus 7.7GB TSDB 已从部署仓库迁到 `/var/lib/littlehorse/prometheus` 的 Retain PV/PVC，保留 30 天、上限 40GB；最早现存时间序列为 2026-07-10 02:00:01 +08:00。旧 TSDB 目录暂保留作为回滚点。
- Grafana 登录、Prometheus 数据源与四个 provisioned Dashboard API 均验证成功；即时备份 `grafana-20260725T084936Z.db` 的 SHA-256 和 SQLite integrity check 均通过。
- 部署仓库 GitHub 与生产服务器 `main` 已同步至 `a925de4ab1097a0ebde6d96f75491c1a7337fbb2`（Dashboard/采集配置 `986c3c0`，kube-state-metrics 修正 `daf1f83`，Grafana provisioning 挂载修正 `a925de4`）。
