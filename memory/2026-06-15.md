# 2026-06-15

## 完成：Mobile 邀请好友深链资料与动作分支
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的邀请好友页已按小程序行为补齐：从 `fi` / `invite_code` / `inviteCode` 打开时会读取邀请人资料，解析关系状态，并显示邀请人昵称、邀请码、手动填写框和“加为好友”动作。
- App 内“我的 → 邀请好友”入口加载当前账号自己的邀请页，显示邀请码、`foodlink://invite?fi=<code>` 链接，以及“分享邀请 / 复制邀请码 / 复制链接”三个动作。
- 无效邀请码不再在页面打开时弹窗挡住用户，而是以页面内提示保留可编辑和重试路径；主动解析或接受失败仍可弹出错误提示。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx`
  - 文案扫描未命中 `暂不可用`、`未迁移`、`占位`、`后续接入`、`NativePlaceholder`
  - MuMu + Expo Go + ADB：有效深链 `exp://192.168.8.193:8081/--/invite?fi=49e9e2fc` 命中后端 `profile-by-code` 与 `resolve` 200，截图 `tmp/expo-invite-profile-by-code-owner-visible.png`、`tmp/expo-invite-resolve-action.png`
  - MuMu + Expo Go + ADB：从“我的”页邀请入口进入自己的邀请页，显示 `eff3150a` 与分享/复制入口，截图 `tmp/expo-invite-owner-actions.png`
- 本轮没有点击“加为好友”，避免创建真实好友申请；没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 保质期储存方式枚举中文化
- `apps/mobile/src/screens/DetailScreens.tsx` 的保质期新增表单把“储存方式”改为中文分段按钮“冷藏 / 常温 / 冷冻”，保存时仍提交后端枚举 `refrigerated`、`room_temp`、`frozen`。
- `apps/mobile/src/screens/TertiaryMigrationScreens.tsx` 的保质期编辑页同步使用同一组选项，避免编辑已有记录时暴露英文枚举。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/HealthProfileViewScreen.tsx apps/mobile/src/screens/TertiaryMigrationScreens.tsx`，仅 `DetailScreens.tsx` LF/CRLF 提示
  - 固定文案扫描未命中 `refrigerated / room_temp / frozen`、健康档案旧枚举提示、`NativePlaceholder`、`未迁移`、`占位`、`后续接入`
  - MuMu + Expo Go + ADB 进入“食物保质期”管理页后，新增表单显示“储存方式：冷藏 / 常温 / 冷冻”，截图 `tmp/expo-expiry-storage-options.png`
- 本轮未点击保存保质期，未新增或修改数据；未执行数据库迁移，未运行 `npm run build:weapp`，未重启后端或 Metro。

## 完成：Mobile 健康档案枚举选项中文化
- `apps/mobile/src/screens/DetailScreens.tsx` 的 `HealthProfileScreen` 把性别、活动水平、饮食目标从英文枚举输入框改为中文分段选项按钮；保存时仍提交后端枚举值。
- `apps/mobile/src/screens/HealthProfileViewScreen.tsx` 的单字段编辑器为 `gender`、`diet_goal`、`daily_life_activity_level`、`execution_mode` 提供中文选项按钮，不再向用户提示 `male / female`、`fat_loss / maintain / muscle_gain`、`sedentary / light / moderate / active`、`fast / standard / strict`。
- 查看态补齐 `other` 性别显示，并把目标显示统一为“减脂 / 保持 / 增肌”。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx apps/mobile/src/screens/HealthProfileViewScreen.tsx`（仅 `DetailScreens.tsx` LF/CRLF 提示）
  - MuMu + Expo Go：进入“健康档案与目标”可见中文选项，截图 `tmp/expo-health-profile-options.png`；进入“健康档案详情”并点击“性别”可见“编辑性别”中文选项卡，截图 `tmp/expo-health-profile-detail-gender-options.png`。
- 本轮没有保存健康档案，未改动测试账号资料；没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 互动消息收件箱入口与空态验证
- `apps/mobile/src/screens/ProfileScreen.tsx` 在“我的”页新增“互动消息 / 通知”快捷卡，并在服务列表新增“互动消息”入口；“打卡排行榜”改放在服务列表中。
- `apps/mobile/src/screens/DetailScreens.tsx` 的互动消息页增强为收件箱式页面：未读数量、收件箱说明、全部已读/暂无未读按钮、消息卡片、头像/首字、内容摘要、时间和未读标记。
- 点击通知时会尝试标记单条已读，再跳转 `CommunityFeedDetail`；缺少目标 ID 时用“未找到对应动态”提示。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/screens/ProfileScreen.tsx apps/mobile/src/screens/DetailScreens.tsx`（仅 LF/CRLF 提示）
  - MuMu + Expo Go：从“我的”页可见互动消息快捷卡和服务入口，截图 `tmp/expo-profile-notifications-entry.png`；进入后显示 `0 条未读`、`互动收件箱`、`暂无未读`、`暂无互动消息`，截图 `tmp/expo-notifications-enhanced-empty.png`。
- 本轮没有创建真实通知数据，没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 私信消息类型与直达会话
- `packages/api-client` 的 `sendPrivateMessage` 从文本字符串扩展为兼容文本/图片消息：
  - 文本仍可直接传字符串。
  - 图片可传 `contentType: "image"` 和 `imageUrl`，请求体使用后端已有 `content_type` / `image_url`。
  - 客户端拦截空文本和空图片地址。
- `apps/mobile/src/screens/MigrationScreens.tsx` 的 `PrivateChatScreen` 补齐私信详情展示：
  - 读取当前用户 ID 区分自己/对方消息。
  - 文本、图片、系统消息分别渲染；系统消息居中，图片消息展示缩略图。
  - 输入区增加“发图片”入口，使用 Expo 相册选择并上传后发送图片消息。
  - 空文本点击发送会弹出“请输入消息内容”，不提交空消息。
- `apps/mobile/src/navigation/RootNavigator.tsx` 增加登录态私信 deep link，支持 `private-chat?user_id=<uuid>&nickname=<name>`；`apps/mobile/src/api.ts` 暴露 `getStoredUserId()`。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run test`（13 tests passed）
  - `git diff --check -- apps/mobile/src/api.ts apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/screens/MigrationScreens.tsx packages/api-client/src/index.ts packages/api-client/tests/client.test.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - “我的 → 私信”进入会话列表，当前账号无会话时显示“暂无私信会话”，截图 `tmp/expo-conversations-screen-enhanced-entry.png`。
  - 使用 `exp://192.168.8.193:8081/--/private-chat?user_id=11111111-1111-1111-1111-111111111111` 直达私信详情，显示“暂无消息”、消息输入框、“发图片”和“发送”，截图 `tmp/expo-private-chat-deeplink-screen.png`。
  - 空内容点击发送弹出“请输入消息内容”，截图 `tmp/expo-private-chat-empty-send-guard.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro；没有发送真实文本或图片消息，避免留下不可清理的私信记录。

## 完成：Mobile 积分中心任务进度与跳转闭环
- `apps/mobile/src/screens/DetailScreens.tsx` 的 `RewardCenterScreen` 已从简化任务列表增强为完整积分中心：
  - 顶部展示奖励积分余额、今日已获得积分和今日任务进度。
  - 增加“最快拿分”区域，仅突出当前可完成任务。
  - 任务卡展示任务状态、奖励积分、进度条、当前进度、每日上限和任务说明。
  - “去完成”根据任务动作跳转到对应 App 页面。
- `packages/core/src/types.ts` 补齐 `RewardCenterTask.action_type` 与 `status`，App 优先按后端动作类型决定任务跳转。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx packages/core/src/types.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 从“我的”页进入“赚积分”，可见奖励积分、当前余额、今日已获得、今日进度、最快拿分和任务卡，截图 `tmp/expo-reward-center-enhanced.png`。
  - 点击每日分享打卡任务跳转到单日记录页，截图 `tmp/expo-reward-center-share-task-navigation.png`。
  - 点击预包装零食/食物上传任务跳转到包装食品表单，截图 `tmp/expo-reward-center-packaged-task-navigation.png`。
  - 点击上传公共食物/校园食堂菜品任务跳转到分享食物表单，截图 `tmp/expo-reward-center-public-task-navigation.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro；未提交包装食品、公共食物或真实分享领取，避免测试账号积分/公共库副作用。

## 完成：Mobile 会员中心状态/套餐/支付确认增强
- `apps/mobile/src/screens/MigrationScreens.tsx` 的 `MembershipCenterScreen` 已从简化套餐列表增强为完整会员中心：
  - 当前会员卡展示试用/Pro 状态、当前可用积分、系统剩余、奖励积分、今日已用、每日额度、当前套餐、有效期、积分重置时间和创始礼遇。
  - 套餐列表使用真实后端配置并按 `sort_order`/价格排序，展示周期、每日系统积分、档位、折扣、省钱金额、现价和原价。
  - 支付确认卡展示选择套餐、周期、每日积分、预计付款、生效方式和支付后同步说明。
  - “创建支付订单”先弹出确认预览，确认后才会调用创建订单接口。
  - 页面提供“自动续费审核”和“会员协议”入口。
- `packages/core/src/types.ts` 补齐 App 会员状态需要的后端字段，包括会员有效期、积分重置、每日奖励、trial 和创始礼遇元数据。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `git diff --check -- apps/mobile/src/screens/MigrationScreens.tsx packages/core/src/types.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 从“我的”页进入会员中心，首屏可见试用状态、8 当前可用积分、系统剩余/奖励积分/今日已用/每日额度、积分重置和真实套餐列表，截图 `tmp/expo-membership-center-enhanced-top.png`。
  - 选择“轻度版 · 季卡”后支付确认区显示 `¥27.90`、每日 8 系统积分和支付成功后即时生效，截图 `tmp/expo-membership-center-payment-confirm.png`。
  - 点击“创建支付订单”出现确认开通会员预览弹窗，截图 `tmp/expo-membership-center-payment-preview-alert.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro；未点击弹窗中的“创建订单”，避免产生支付单。

## 完成：Mobile 成长伙伴首页卡与外观实验室增强
- 新增 `apps/mobile/src/components/PetAvatar.tsx`，统一 App 成长伙伴视觉组件和中文标签转换，支持颜色、形状、纹理、配饰、心情点和状态弱化。
- `apps/mobile/src/hooks/useHomeDashboard.ts` 首页加载时并行读取 `getPetSummary(recordDate)`；伙伴接口失败不会阻塞首页 dashboard。
- `apps/mobile/src/screens/HomeScreen.tsx` 首页顶部新增成长伙伴卡，展示伙伴名称、等级进度、状态、习惯分、可领奖提示和今日任务，点击可进入成长伙伴详情。
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 增强 `PetHomeScreen` 和 `PetLabScreen`：
  - 详情页展示外观档案、匹配原因、今日状态、离线复盘、奖励、成长解锁和外观实验室入口。
  - 外观实验室展示当前形象、候选形象、匹配分、中文外观标签、档案重配状态和刷新消耗。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/components/PetAvatar.tsx apps/mobile/src/hooks/useHomeDashboard.ts apps/mobile/src/screens/HomeScreen.tsx apps/mobile/src/screens/SecondaryMigrationScreens.tsx`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 首页成长伙伴卡截图 `tmp/expo-pet-home-card.png`。
  - 成长伙伴详情页截图 `tmp/expo-pet-detail-enhanced.png`。
  - 外观实验室候选形象页截图 `tmp/expo-pet-lab-enhanced.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro；未点击领取奖励、刷新外观或选择候选，避免改变测试账号状态。

## 完成：Mobile 自动续费审核状态页
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `AutoRenewAuditScreen` 从静态说明页补齐为状态页：读取当前会员状态和真实会员套餐列表，展示服务内容、签约前说明、协议勾选、确认预览和关闭自动续费路径。
- 自动续费确认前必须勾选“我已阅读并同意会员服务协议及自动续费规则”；未勾选时弹出“请先阅读并同意规则”。
- 勾选后点击确认只展示服务、周期、金额和续费说明预览，不触发真实支付或扣费；关闭路径说明覆盖产品内路径、微信支付或应用商店扣费服务路径。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/src/screens/SecondaryMigrationScreens.tsx`
- MuMu + Expo Go + ADB 验证：
  - 从“我的”页进入“自动续费审核”，页面显示“基础账号”“会员状态：试用中”“今日可用积分：8”和真实套餐列表。
  - 截图保存：`tmp/expo-auto-renew-audit-status.png`、`tmp/expo-auto-renew-audit-plans.png`、`tmp/expo-auto-renew-audit-agreement-guard.png`、`tmp/expo-auto-renew-audit-confirm-alert.png`、`tmp/expo-auto-renew-audit-cancel-alert.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。真实自动续费仍需正式 App 包、支付渠道和后端支付回调链路。

## 完成：Mobile 邀请好友分享/复制/深链闭环
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx` 的 `InviteFriendsScreen` 补齐邀请分享闭环：展示邀请码和 `foodlink://invite?fi=<code>` 链接，支持系统分享、复制邀请码、复制链接，并支持从 `fi`、`invite_code`、`inviteCode` 路由参数预填“接受邀请”输入框。
- `apps/mobile/app.json` 增加 `scheme: "foodlink"`；`apps/mobile/src/navigation/RootNavigator.tsx` 增加登录态 invite 深链监听，正式包可承接 `foodlink://invite?...`，Expo Go 可用 `exp://<host>:8081/--/invite?fi=<code>` 验证。
- 登录页和邀请页邀请码输入均改为只 trim，不强制 uppercase，兼容旧用户 ID 前缀邀请码和新注册邀请码。
- `apps/mobile` 增加 `expo-clipboard`，用于复制邀请码和邀请链接。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `git diff --check -- apps/mobile/app.json apps/mobile/package.json package-lock.json apps/mobile/src/screens/SecondaryMigrationScreens.tsx apps/mobile/src/screens/LoginScreen.tsx apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/navigation/types.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 后端 `0.0.0.0:3010` PID `32888`、Mobile Metro `0.0.0.0:8081` PID `48248` 保持运行。
  - 使用 `adb -s 127.0.0.1:16384 shell am start -W -a android.intent.action.VIEW -d "exp://192.168.8.193:8081/--/invite?fi=deeplink16384"` 打开当前 App，返回 `Status: ok`。
  - UI dump 确认进入“邀请好友”页，接受邀请输入框自动显示 `deeplink16384`，截图 `tmp/expo-invite-deeplink-prefill.png`。
  - 复制邀请码截图 `tmp/expo-invite-copy-code-alert.png`；复制链接截图 `tmp/expo-invite-copy-link-alert.png`；系统分享面板截图 `tmp/expo-invite-share-sheet.png`；邀请页整体截图 `tmp/expo-invite-share-copy-actions.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 登录邀请码与 Expo Go 打开验证
- `apps/mobile/src/screens/LoginScreen.tsx` 补充可选邀请码输入框；登录页启动时会解析 deep link 参数 `fi`、`invite_code`、`inviteCode` 并自动填入。
- `apps/mobile/src/providers/AuthProvider.tsx` 将邀请码透传给 App 微信登录和用户名密码注册；密码登录不传邀请码。
- `packages/api-client/tests/client.test.ts` 补充 App 微信登录和密码注册的邀请码请求体断言，覆盖 trim 后的 payload。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`（13 tests passed）
  - `git diff --check -- apps/mobile/src/screens/LoginScreen.tsx apps/mobile/src/providers/AuthProvider.tsx packages/api-client/tests/client.test.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 当前后端监听 `0.0.0.0:3010`，PID `32888`；Mobile Metro 监听 `0.0.0.0:8081`，PID `48248`。
  - 当前联调地址：API `http://192.168.8.193:3010`，Expo `exp://192.168.8.193:8081`。
  - 使用 `adb -s 127.0.0.1:16384 shell am start -W -a android.intent.action.VIEW -d "exp://192.168.8.193:8081/--/?fi=ABCD1234"` 打开当前 App。
  - 登录页邀请码输入框自动显示 `ABCD1234`，截图 `tmp/expo-login-invite-code-filled.png`。
  - 点击“微信一键登录”后进入首页，截图 `tmp/expo-login-invite-wechat-success.png`；后端日志显示 `/api/app/login/wechat` 和 `/api/home/dashboard` 均返回 200。
  - 会话压缩后再次用 `exp://192.168.8.193:8081` 拉起 Expo Go，ADB 返回 `Status: ok`，UI dump 确认停留在 Food Link 首页；截图 `tmp/expo-current-after-adb-launch.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 账号安全与清除缓存入口

- `packages/api-client` 新增 `setAccountPassword`，调用 `POST /api/app/account/password`，支持已登录用户设置/修改用户名密码，并在接口返回新 token 后刷新本地 token。
- `apps/mobile` 新增 `AccountSecurityScreen` 与 `AccountSecurity` 路由：
  - 展示微信登录连接状态、账号密码是否已设置、密码设置时间。
  - 未设置密码的微信登录用户可直接设置用户名和新密码。
  - 已设置密码的用户修改时需要填写当前密码。
  - 表单校验用户名、新密码长度和二次确认密码一致。
- `ProfileScreen` 服务列表新增“账号安全”入口；“我的”页新增可见“清除缓存”工具入口，并给清除缓存和退出登录加二次确认。
- `backend/internal/user/service/user_service.go` 的 profile 响应新增 `username`、`has_password`、`password_set_at`；`packages/core` 的 `UserInfo` 同步补字段。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run test`（13 tests passed）
  - `cd backend && go test ./internal/user/service -run '^$'`
  - `cd backend && go build ./cmd/server`
  - `git diff --check -- apps/mobile/src/screens/AccountSecurityScreen.tsx apps/mobile/src/screens/ProfileScreen.tsx apps/mobile/src/navigation/RootNavigator.tsx apps/mobile/src/navigation/types.ts packages/core/src/types.ts packages/api-client/src/index.ts packages/api-client/tests/client.test.ts backend/internal/user/service/user_service.go`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - “我的”页可见“账号安全”入口，截图 `tmp/expo-profile-account-security-entry.png`。
  - 账号安全页可见登录方式、账号密码状态、用户名、新密码、确认新密码和保存按钮，截图 `tmp/expo-account-security-screen.png`。
  - “我的”页底部可见“清除缓存”和“退出登录”，截图 `tmp/expo-profile-cache-tool-entry.png`。
  - 点击“清除缓存”出现确认弹窗，文案说明登录状态会保留。
- 注意：
  - 未执行数据库迁移，没有重启现有后端或 Metro。
  - 当前运行后端未重启时，profile 新字段不会立即由服务返回；App 已做字段缺失兜底，重启后即可展示真实账号安全状态。
  - `go test ./internal/user/...` 仍受既有问题影响：sqlite 测试需要 CGO，handler 旧测试 `NewUserHandler` 参数未同步；本轮以 user/service 编译检查和 server build 验证后端改动。

## 完成：Mobile 饮食记录分享与奖励入口

- `packages/api-client` 新增 `claimSharePosterReward`，调用 `/api/membership/rewards/share-poster/claim`；单条记录传 `record_id`，单日饮食传 `share_scope: daily_food` 与 `share_date`。
- `apps/mobile/src/screens/DetailScreens.tsx`：
  - `DayRecordScreen` 新增单日营养汇总和“分享今日饮食”入口，使用 React Native 系统分享生成当天饮食摘要，分享成功后调用奖励接口。
  - `RecordDetailScreen` 新增“分享记录”和“圈子详情”，单条分享文本包含餐次、热量、三大营养素和食物明细，圈子详情跳转 `CommunityFeedDetail` 的 `food_record` target。
  - `SmallButton` 支持 disabled 状态，便于无记录时禁用分享入口。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`（12 tests passed）
  - `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx packages/api-client/src/index.ts packages/api-client/tests/client.test.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 使用 `127.0.0.1:16384` 打开 `exp://192.168.8.193:8081`。
  - 单日记录页可见临时记录和“分享今日饮食”，截图 `tmp/expo-day-record-share-action.png`。
  - 记录详情页可见“分享记录 / 圈子详情 / 修改记录 / 删除记录”，截图 `tmp/expo-record-detail-share-actions.png`。
- 清理：
  - 临时饮食记录 `6fb3e27d-ef03-45a5-b4cb-2b6b7dc6946b` 与 `905f6d0e-db36-4980-a962-4ded1edb86bb` 已通过业务 DELETE API 删除。
  - 只读列表接口确认 app wx 与 debug 两个账号在 `2026-06-15` 当天记录数均为 0。
- 注意：
  - 未点击系统分享按钮领取奖励，避免测试账号积分副作用；奖励请求由 API client 单测覆盖。
  - 临时饮食记录的 `entry_type` 需使用数据库允许值，例如 `food_library`；`manual` 会触发 check constraint。
  - 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有重启后端或 Metro。

## 完成：Mobile 公共食物分享补齐与 Expo Go 当前启动确认

- `packages/api-client` 公共食物创建/更新入参补充 `campusLocationText`，请求 payload 写入 `campus_location_text`。
- `apps/mobile` 的公共食物分享表单补齐更多小程序字段：图片 URL、商户/窗口、地址/位置、学校、食堂、楼层、窗口、校园位置描述、价格单位、份量描述、口味评分、减脂适合度、标签和个人备注。
- 公共食物详情页补充展示类型、地点、份量、价格、口味评分、减脂选择、标签和个人备注。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
- MuMu + Expo Go + ADB 验证：
  - 后端仍监听 `0.0.0.0:3010`，Mobile Metro 仍监听 `0.0.0.0:8081`。
  - ADB 设备 `127.0.0.1:16384` 可用，已通过 `adb -s 127.0.0.1:16384 shell am start -W -a android.intent.action.VIEW -d "exp://192.168.8.193:8081"` 重新拉起 Expo Go，返回 `Status: ok`。
  - 当前 Expo Go 页面显示“分享食物”，可见图片 URL、地址、校园餐、学校、食堂、楼层、窗口等字段。
  - 截图保存为 `tmp/expo-current-launch.png`；扩展表单截图保存为 `tmp/expo-public-food-share-expanded.png`。
- 本轮没有执行数据库迁移，也没有重启已有后端或 Metro 进程。

## 完成：Mobile 记录菜单补齐收藏食谱入口

- `apps/mobile/src/components/RecordActionSheet.tsx` 的中间“+”记录菜单新增“收藏食谱”入口，对齐小程序记录菜单里的“我的收藏/常吃餐食”快捷入口。
- 记录菜单改为内部 `ScrollView`，并限制最大高度，避免新增入口后小屏溢出。
- `apps/mobile/src/navigation/CustomTabBar.tsx` 将 `recipes` action 分发到 `Recipes` 路由。
- 验证：
  - `npm --workspace apps/mobile run typecheck` 通过。
  - MuMu + Expo Go + ADB 打开底部中间“+”菜单，UI dump 可见“收藏食谱 / 快速记录常吃餐食组合”。
  - 截图保存为 `tmp/expo-record-menu-recipes.png`。
- 本轮没有重启后端或 Metro。

## 完成：Mobile 文字识别结果闭环
- `packages/core` 的 `buildSaveFoodRecordRequestFromTask` 新增 `entryType` 可选参数，并新增单测覆盖文字识别保存为 `entry_type: food_text`。
- `apps/mobile` 新增 `TextResultScreen` 与 `TextResult` 路由，文字识别任务完成后展示原始描述、营养汇总、明细、比例调整、饮食建议，并支持保存到当天饮食。
- `AnalyzeLoadingScreen` 和 `AnalyzeHistoryScreen` 已按 `food_text` 分流到文字结果页，图片任务仍进入原图片结果页。
- `TextRecordScreen` 新增“份量/补充说明”输入，提交时作为 `additionalContext` 传给后端文字分析接口。
- 验证通过：
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/core run test`
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`
- MuMu + Expo Go + ADB 验证：打开 `exp://192.168.8.193:8081` 后从首页进入“文字记录”，可见新增“份量/补充说明”字段和“提交分析”按钮；截图 `tmp/expo-text-record-form.png`。
- 本轮没有执行数据库迁移，也没有重启现有后端或 Metro。为避免真实 AI 消耗，未提交完整文字分析任务。

## 完成：Mobile 个人主页与健康档案详情迁移补齐

- 新增 `apps/mobile/src/screens/ProfileSettingsScreen.tsx`：统一当前用户与他人用户主页，支持资料展示、资料编辑入口、头像/封面上传、关注/取关、私信、关注列表、动态列表、食谱和食物收藏。
- 新增 `apps/mobile/src/screens/HealthProfileViewScreen.tsx`：补齐健康档案详情查看与编辑，覆盖基础信息、病史/偏好/过敏、作息、执行模式、代谢数据、体检/病例识别结果和报告上传入口。
- `ProfileScreen` 顶部用户卡片可进入个人主页；服务列表新增“健康档案详情”，并保留“健康档案与目标”入口。
- 圈子、关注列表、排行榜等用户入口改为优先进入 `ProfileSettings`。
- `packages/core` 补充 HealthProfile/HealthCondition/HealthReportExtract 等类型；`packages/api-client` 补充头像/封面上传、注销账号、健康报告上传和报告提取任务 API。
- `apps/mobile` 增加 `expo-file-system` 直接依赖和 `readImageAsBase64DataUrl` 工具，用于 Expo Go 图片转 base64 上传。
- 验证通过：
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`
  - `npm --workspace apps/mobile run typecheck`
- MuMu + Expo Go + ADB 验证：
  - `exp://192.168.8.193:8081` 可打开当前 App。
  - 首页、“我的”页、个人主页、健康档案详情页均可渲染。
  - 健康档案字段点击后编辑卡片显示在页面顶部，能看到输入框、保存、取消。
  - 截图保存：`tmp/expo-profile-tab.png`、`tmp/expo-profile-settings.png`、`tmp/expo-health-profile-view.png`、`tmp/expo-health-profile-editor-visible.png`。
- 本轮没有执行数据库迁移。

## 完成：修复 Mobile Metro 无法解析 react

- 用户在 Expo Go 中看到红屏：`Unable to resolve "react" from "apps/mobile/src/providers/AuthProvider.tsx"`；终端显示 Android Bundling 500。
- 根因：为规避 monorepo 中小程序 React 18 与 mobile React 19 的类型冲突，曾在 `apps/mobile/tsconfig.json` 的 `paths` 中把 `react`、`react/jsx-runtime` 指向 `./node_modules/@types/react`。Expo/Metro 会读取该 `paths`，导致运行时把 `react` 解析到类型包，进而无法打包。
- 修复：
  - 从 `apps/mobile/tsconfig.json` 移除 `react` / `react/jsx-runtime` / `react/jsx-dev-runtime` 的运行时 paths。
  - 新增 `apps/mobile/tsconfig.typecheck.json`，只在 `tsc` typecheck 时使用 `@types/react` paths。
  - `apps/mobile/package.json` 的 `typecheck` 脚本改为 `tsc --project tsconfig.typecheck.json --noEmit --pretty false`。
- 验证：`npm --workspace apps/mobile run typecheck` 通过；`ReadLints` 未发现新增诊断。
- 运行提示：正在运行的 Metro 可能仍缓存旧解析结果，需要在 Expo 终端按 `r` reload；若仍显示旧错误，停止当前 Expo 后重新运行 `npm --workspace apps/mobile run start -- --lan --clear`。

## 完成：圈子私信入口与私信列表

- 在圈子顶部快捷区新增「私信」入口（4 列布局），带总未读数角标。
- 新建 `packageExtra/pages/private-conversations` 列表页：会话卡片、未读角标、下拉刷新、滚动触底分页（默认 20 条）、深色主题。
- 后端 `GET /api/messages/conversations` 增加 `offset`/`limit` 分页，返回 `has_more`。
- 数据库：新增 `idx_private_messages_unread` 部分索引；插入测试数据「小马哥」向「锦恢」发送未读消息。
- 修复 `ConversationSummary` JSON 字段名（`user_id`/`nickname` 等），修复系统消息空昵称回退，系统消息头像使用 app logo。
- 修复 `domain.PrivateMessage` JSON 字段名（`content`/`image_url` 等）；私信页移除自定义导航栏改用原生导航栏标题；空头像使用 default_avatar.png。
- 修正测试数据：把私信发送者从空测试用户改为真实「小马哥」用户（带路飞头像），删除空测试用户，消息重置为未读。
- 修复私信图片上传 `path of undefined`、己方头像显示、时间分隔阈值 10 分钟、图片按钮图标改为 `icon-picture`。
- 优化图片消息气泡：去掉背景/边框/内边距，图片满框显示。

## 完成：Mobile 升级到 Expo SDK 56 并接入 MuMu/ADB 验证

- 用户安装 MuMu 模拟器与 SDK 56 版 Expo Go，要求 mobile 项目从 SDK 54 升级到 SDK 56。
- 说明：此前从 SDK 51 升到 SDK 54 是为了匹配当时手机上的 Expo Go；这次不是代码回退，而是新的 MuMu Expo Go 为 SDK 56。
- 升级内容：
  - `apps/mobile/package.json`：`expo` 升到 `~56.0.0`，React 升到 `19.2.3`，React Native 升到 `0.85.3`，并按 `expo install` 对齐 `expo-image-picker`、`expo-status-bar`、`react-native-safe-area-context`、`react-native-screens`、`typescript`、`@types/react`。
  - `apps/mobile/app.json` 由 Expo CLI 添加 `expo-status-bar` config plugin。
  - `apps/mobile/metro.config.js` 恢复为 Expo 56 默认 `getDefaultConfig(projectRoot)`；此前为隔离 React 18/19 的 `disableHierarchicalLookup` 在 SDK 56/RN 0.85 下会导致 RN 内部 default import 转换异常。
  - `apps/mobile/index.ts` 改成 CommonJS 方式兼容 `expo/src/launch/registerRootComponent` 的 CJS/ESM 两种形态。
  - 新增 `patches/expo+56.0.11.patch`：兼容 `expo/src/async-require/hmr.ts` 中 CommonJS HMRClient 在当前 monorepo 下 default import 可能变成 `.default` 的问题。
- MuMu/ADB：
  - MuMu 暴露 `emulator-5554`、`127.0.0.1:5555`、`127.0.0.1:16384`，后续默认使用 `emulator-5554`。
  - 可用 `adb -s emulator-5554 exec-out screencap -p > tmp/mumu-screen.png` 抓图，可用 `adb -s emulator-5554 logcat` 抓原生日志。
- 验证：
  - `npm --workspace apps/mobile run typecheck` 通过。
  - `ReadLints` 未发现新增诊断。
  - `npx expo install --check` 显示依赖已对齐；`expo-doctor` 仍会提示根目录 React 18 与 mobile React 19 重复，这是 monorepo 中小程序和 mobile 共存导致。
  - 使用 MuMu + Expo Go SDK 56 打开 `exp://192.168.8.193:8081`，已进入 mobile 登录页；点击“一键登录测试账号”成功进入首页，底部 Tab 与中间记录按钮显示正常。
- 额外修复：
  - `HomeScreen` 保质期摘要字段缺省时不再显示 `undefined`，改为按 `0` 展示。

## 完成：Mobile 一键局域网启动

- 新增 `scripts/start-mobile-dev.cjs`：启动时自动检测局域网 IPv4，并注入 `REACT_NATIVE_PACKAGER_HOSTNAME`、`EXPO_PUBLIC_API_BASE_URL`。
- 根目录 `npm run dev:mobile` 改为走该脚本；`apps/mobile` 内也可用 `npm run start:lan`。
- 支持手动覆盖：`MOBILE_DEV_LAN_IP`、`MOBILE_DEV_API_PORT`、`MOBILE_DEV_METRO_PORT`。
- 本机检测示例：以太网 `192.168.8.193` → API `http://192.168.8.193:3010`，Expo `exp://192.168.8.193:8081`。

## 完成：Mobile 开发与构建文档

- 新增 `docs/mobile-dev-guide.md`：涵盖 Expo Go 联调、局域网启动、ADB/MuMu、patch 说明、EAS Build / 本地构建 APK·AAB·IPA 命令。
- 根目录补充 `typecheck:mobile`、`build:mobile:android:apk` 等脚本；`apps/mobile` 补充 `prebuild`、`run:android`、`build:android:apk` 等。
- 构建需先 `eas build:configure` 生成 `eas.json`（文档内含 profile 示例）。

## 完成：本机启动 Mobile App 并用 MuMu Expo Go 打开

- 按用户明确要求启动常驻进程：
  - `npm run dev:backend`，后端监听 `0.0.0.0:3010`。
  - `npm run dev:mobile`，Metro 监听 `0.0.0.0:8081`。
- `scripts/start-mobile-dev.cjs` 自动选择局域网 IP `192.168.8.193`，注入：
  - `EXPO_PUBLIC_API_BASE_URL=http://192.168.8.193:3010`
  - Expo URL `exp://192.168.8.193:8081`
- ADB 初始无设备；手动连接 MuMu 常见端口后成功：
  - `adb connect 127.0.0.1:16384`
  - `adb connect 127.0.0.1:5555`
- 使用 `127.0.0.1:16384` 打开 Expo Go：
  - `adb -s 127.0.0.1:16384 shell am start -W -a android.intent.action.VIEW -d "exp://192.168.8.193:8081"`
- Metro 输出 `Android Bundled ... apps\mobile\index.ts`，说明打包成功。
- Expo Go 首次显示开发菜单说明；点击 Continue/关闭后已进入 Food Link 首页。
- 运行时验证：
  - UI dump 可见「首页」「今日概览」「今日餐食」和底部 Tab。
  - 截图保存为 `tmp/expo-app-home.png`。

## 进行中：Mobile App 功能完整迁移扩展

### 后端登录适配
- 新增 App 微信登录、用户名密码登录、用户名密码注册、设置密码接口代码。
- 新增 `app_auth` 配置，支持微信开放平台 AppID/Secret 与开发 mock code。
- 用户迁移 DO 与幂等迁移代码已增加 App 登录字段：`app_openid`、`app_unionid`、`username`、`password_hash`、`password_set_at`、`last_login_method`、`last_login_at`。
- 未执行数据库迁移命令；当前配置可能指向非本地 PostgreSQL。仓储层已为未迁移数据库提供 fallback。

### App 页面迁移
- `apps/mobile` 登录页支持微信一键登录入口、用户名密码登录、用户名密码注册，并保留调试登录。
- 补齐主要小程序页面对应的 RN 路由和入口：AI 助手、代谢分析、身体趋势/体重趋势/饮水趋势/运动趋势、包装食品、包装识别任务、校园食堂、定位搜索、隐私设置、会员协议、用户群等。
- 中间“+”记录菜单新增“包装食品”入口。
- `packages/core` 增加统计洞察、饮食建议、包装食品、定位搜索类型。
- `packages/api-client` 增加统计洞察、饮食建议、包装食品识别/保存、定位搜索等接口封装。

### 验证
- 通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`
  - `cd backend && go test ./internal/auth ./pkg/config`
  - `cd backend && go build ./cmd/server`
- MuMu + Expo Go + ADB 验证：
  - 首页、分析、AI 助手、包装食品、校园食堂、隐私设置、用户群均可打开。
  - 校园食堂页能从后端读取校园餐数据。
  - 隐私设置页能读取用户资料开关。
  - 用户群页能显示本地二维码图。
- 验证截图：
  - `tmp/expo-packaged-food.png`
  - `tmp/expo-campus-canteen.png`
  - `tmp/expo-privacy-settings.png`
  - `tmp/expo-user-group-tertiary.png`

### 注意
- 真实微信 App 登录和微信支付仍需正式 App 包或原生开发构建、微信开放平台配置、微信支付配置后验证。
- 当前开发 mock 账号创建会员支付订单会触发微信支付 `无效的openid`，这是预期限制。
- 意见反馈飞书通知当前返回 invalid token，属于本地后端配置问题。

## 完成：Mobile 食物库详情与保质期编辑补齐

- 补齐 `packages/api-client` 保质期详情与编辑封装：`GET /api/expiry/items/:item_id`、`PUT /api/expiry/items/:item_id`，并新增测试确认字段名按后端 `food_name`、`expire_date` 等格式发送。
- `apps/mobile` 食物库列表卡片支持进入详情页；详情页展示营养字段、默认份量、推荐原因和来源，并提供“记录到今天”表单，调用 `saveManualFoodRecord` 保存饮食记录。
- `apps/mobile` 保质期列表新增“编辑”入口；`ExpiryEdit` 从占位说明改为真实表单，支持编辑名称、分类、到期日期、数量、储存方式、备注和状态。
- 修复保质期列表返回后旧数据不刷新的问题：列表页改为页面重新聚焦时刷新，验证编辑页把测试记录改为“已丢弃”后，返回列表进行中计数恢复为 0。
- 清理 App 页面中用户可见的工程迁移文案，不再出现“App 端”“后续”“占位”“未迁移”等说明。

### 验证

- `npm --workspace apps/mobile run typecheck` 通过。
- `npm --workspace packages/core run typecheck` 通过。
- `npm --workspace packages/api-client run typecheck` 通过。
- `npm --workspace packages/api-client run test` 通过（8 tests passed）。
- MuMu + Expo Go + ADB 运行时验证通过：打开 `exp://192.168.8.193:8081`，进入食物库详情并看到保存记录表单；从首页进入保质期管理，新增测试记录 `codex-milk`，编辑并改为“已丢弃”，后端 `POST/PUT/GET /api/expiry/*` 均返回 200。
- 本轮未执行数据库迁移；临时测试记录 `codex-milk` 已置为 `discarded`，不会留在进行中列表。

## 完成：Mobile 收藏食谱、六餐次和反馈截图上传

- 全局补齐 App 侧常用餐次选择为六餐次：早餐、早加餐、午餐、午加餐、晚餐、晚加餐；旧 `snack` 保留为兼容值并在食谱场景映射到午加餐。
- `apps/mobile/src/screens/MigrationScreens.tsx`：
  - `RecipesScreen` 移除列表内简化快捷新建表单，统一通过 `RecipeEdit` 新建/编辑。
  - 食谱卡展示图片/占位、收藏状态、餐次、使用次数、最近使用时间、营养摘要、标签和食物明细预览。
  - 点击“记录这餐”后在卡片内选择目标餐次，再调用 `apiClient.useRecipe`，保持 `entry_type: favorite_recipe`。
- `apps/mobile/src/screens/SecondaryMigrationScreens.tsx`：
  - `RecipeEditScreen` 支持图片 URL、标签、收藏/普通、多食物明细、自动汇总营养和删除确认。
  - 食谱编辑页在 Expo Go 中可见三列两行六餐次、营养摘要与食物明细。
- `packages/api-client/src/index.ts`：新增 `uploadFeedbackImageFile`，调用 `/api/feedback/upload-image` 上传反馈截图。
- `apps/mobile/src/screens/DetailScreens.tsx`：`AboutFeedbackScreen` 增加截图上传网格，最多 4 张，提交反馈时带 `image_urls`。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`（10 tests passed）
- MuMu + Expo Go + ADB 运行时验证：
  - 收藏食谱入口可打开，截图 `tmp/expo-recipes-screen.png` / `tmp/expo-recipes-screen-updated.png`。
  - 新建食谱页显示六餐次、图片 URL、标签、收藏状态、营养摘要、食物明细，截图 `tmp/expo-recipe-edit-screen.png`。
  - 关于与反馈页显示“截图 0/4”和“添加截图”，截图 `tmp/expo-about-feedback-screen.png`。
- 未执行数据库迁移，未提交真实反馈或新建食谱，未重启已有后端或 Metro。

## 完成：Mobile 圈子自定义动态创建/详情/删除闭环

- 当前本地联调状态：
  - 后端已重启一次加载最新 Go 代码，监听 `0.0.0.0:3010`，服务进程 PID `32888`，日志 `backend-dev.log`。
  - Mobile Metro 监听 `0.0.0.0:8081`，PID `48248`。
  - 局域网 IP `192.168.8.193`；Expo URL `exp://192.168.8.193:8081`；API `http://192.168.8.193:3010`。
  - MuMu ADB 设备使用 `127.0.0.1:16384`，可通过 `adb -s 127.0.0.1:16384 shell am start -W -a android.intent.action.VIEW -d "exp://192.168.8.193:8081"` 打开 Expo Go 当前 App。
- `apps/mobile` 圈子自定义动态补齐发布表单、8 项营养字段、详情标题/正文展示、自己的动态编辑/删除入口和删除确认。
- `apps/mobile/src/screens/CommunityScreen.tsx` 使用页面聚焦刷新 feed，解决发布、删除或外部清理后返回列表仍显示旧动态的问题。
- `backend/internal/community/service/community_service.go` 的 feed item/context 返回 `is_mine`，App 详情页据此展示 owner-only 操作。
- `packages/api-client` 补齐圈子动态 create/update/delete 封装和测试。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run test`（11 tests passed）
  - `cd backend && go test ./internal/community/service/...`
- MuMu + Expo Go + ADB 验证：
  - 发布页截图：`tmp/expo-circle-post-edit-focus-patch.png`、`tmp/expo-circle-post-edit-lower-focus-patch.png`。
  - 成功发布临时动态 `codexe2e2` / `bodye2e2`，列表展示截图：`tmp/expo-circle-post-e2e2-list-after-ok.png`。
  - 详情页展示标题/正文并在后端重启后显示“编辑动态”“删除”，截图：`tmp/expo-circle-post-e2e2-detail-after-backend-restart.png`。
  - 删除确认、删除成功与返回空列表截图：`tmp/expo-circle-post-e2e2-delete-confirm.png`、`tmp/expo-circle-post-e2e2-deleted-alert.png`、`tmp/expo-circle-post-e2e2-after-delete-ok.png`。
- 本轮没有执行数据库迁移；临时动态 `codex-circle-test` / `migration-test` 与 `codexe2e2` / `bodye2e2` 已清理，只读数据库检查数量为 0。
- 观察到 `mobile-dev.log` 里仍保留较早 `NativePlaceholderScreen` 旧错误和 React Navigation navigate 对象参数警告；当前 Expo UI 未复现红屏。

## 完成：Mobile 饮食记录详情编辑闭环

- `apps/mobile/src/screens/DetailScreens.tsx` 的 `RecordDetailScreen` 已补齐饮食记录详情编辑能力：查看摘要和食物明细、进入编辑、修改餐次/说明/食物名称/重量/比例/营养字段、保存修改、删除确认。
- 编辑页支持 25/50/75/100% 比例快捷按钮，并按实际摄入重量实时重算单项和整条记录总营养；保存时调用 `apiClient.updateFoodRecord` 并保留原记录图片与 item 元数据。
- `packages/core/src/types.ts` 补齐 `FoodRecordItemRow` 的来源、包装、手动录入、可食部比例等元数据字段，避免 App 编辑保存后丢字段。
- `packages/api-client/tests/client.test.ts` 增加饮食记录详情 `GET/PUT/DELETE` mock 与更新/删除测试。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`（1 suite，12 tests passed）
  - `git diff --check -- apps/mobile/src/screens/DetailScreens.tsx packages/core/src/types.ts packages/api-client/tests/client.test.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 验证：
  - 从单日详情进入临时记录详情，可见 180 kcal、摘要、食物明细、`修改记录`、`删除记录`，截图 `tmp/expo-record-detail-view-after-edit-patch.png`。
  - 编辑表单可见餐次、说明、食物项、重量、比例和营养字段，截图 `tmp/expo-record-detail-edit-form-after-patch.png`。
  - 点击 50% 后 UI 即时重算为 90 kcal、实际摄入 60g，截图 `tmp/expo-record-detail-edit-ratio-50.png`。
  - 保存后出现 `已保存 / 记录已更新`，后端 `PUT /api/food-record/:record_id` 返回 200，截图 `tmp/expo-record-detail-save-result.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`。临时饮食记录 `c8744c3b-6964-4683-a34f-17ed1ff6427d` 与 `e36fe9d4-4704-44a2-b7a9-c52f558fa8e6` 已通过业务 DELETE API 清理，并用只读列表接口确认对应账号当天记录为空。

## 完成：Mobile 图片识别结果页保存前调整闭环

- `apps/mobile/src/screens/ResultScreen.tsx` 已从只读图片结果页扩展为保存前可编辑结果页：支持食物名称、估算重量、食用比例、营养总量、单项营养和实际摄入重量实时重算。
- 结果页补齐小程序保存前调整体验：25/50/75/100% 快捷比例、按 1/2/3/4 人分摊、自定义人数分摊、AI 建议比例展示与一键应用。
- 图片识别保存 payload 明确使用 `entry_type: food_image`，并继续保留来源任务、图片、食物元数据、PFC 评价、吸收建议和上下文建议。
- 新增 `apps/mobile/src/utils/demoAnalysisTask.ts` 本地示例识别任务；`HomeScreen` 和 `AnalyzeScreen` 在 `SHOW_DEBUG_LOGIN` 开启时提供“示例识别结果”入口，便于 Expo Go 中验证而不触发真实 AI。
- 验证通过：
  - `npm --workspace apps/mobile run typecheck`
  - `npm --workspace packages/core run typecheck`
  - `npm --workspace packages/core run test`
  - `npm --workspace packages/api-client run typecheck`
  - `npm --workspace packages/api-client run test`（12 tests passed）
  - `git diff --check -- apps/mobile/src/screens/ResultScreen.tsx apps/mobile/src/screens/AnalyzeScreen.tsx apps/mobile/src/screens/HomeScreen.tsx apps/mobile/src/utils/demoAnalysisTask.ts`（仅 LF/CRLF 提示）
- MuMu + Expo Go + ADB 运行时验证：
  - 首页开发验证入口“示例识别结果”可进入图片识别结果页。
  - 初始页可见识别图、营养汇总、按人数分摊、AI 建议比例和食物明细，截图 `tmp/expo-result-demo-initial.png`。
  - 点击“2人”后，汇总从 `560 kcal / 420g` 调整为 `280 kcal / 210g`，鸡胸肉沙拉从 `320 kcal / 260g` 调整为 `160 kcal / 130g`，截图 `tmp/expo-result-demo-people-2.png`。
- 本轮没有执行数据库迁移，没有运行 `npm run build:weapp`，没有提交真实保存或触发真实 AI；后端 `0.0.0.0:3010` 与 Metro `0.0.0.0:8081` 保持运行。
## 完成：修复 Admin 数据集评测表格空白

- 现象：数据集评测页只能看到 `共 47 条`，表格里的批次、样本名、类型、状态、标注、图片全部空白。
- 根因：`BenchmarkHandler` 直接把 `do.FoodWeightLabeledSampleDO` 序列化返回，但该 DO 没有 `json` tag，导致 Gin 输出 `ID`、`BatchName`、`ImageURL` 等首字母大写 JSON key；前端 `DatasetSample` 期望 snake_case，所有字段解析为 `undefined`。
- 修复：
  - `backend/internal/admin/domain/benchmark.go`：新增 `DatasetSampleDTO` 与 `ToDatasetSampleDTO`/`ToDatasetSampleDTOList` 转换函数，统一 snake_case JSON 输出。
  - `backend/internal/admin/handler/benchmark_handler.go`：`ListSamples`、`GetSample`、`CreateSample`、`UpdateSample` 统一返回 DTO。
  - `admin/src/pages/benchmark-page.tsx`：表格「图片」列改为缩略图 `<img>`，点击可查看原图；移除不再使用的 `ImageIcon` 导入。
- 验证：`cd backend && go build ./...` 通过；`cd admin && npx tsc -b` 通过。
- 提示：后端需重启后生效，Admin 开发服务器也需重启以拉取最新代码。
