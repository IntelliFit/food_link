# 2026-05-09

## 拉取并运行 dev

- 用户要求拉取最新代码并运行 `dev` 分支。
- 当前仓库已在 `dev`，执行 `git fetch origin dev` 后发现本地落后远端 33 个提交。
- 因本地已有状态文件改动，先临时 stash，再执行 `git pull --ff-only origin dev`，成功快进到 `fcc6b61`。
- 拉取后恢复并合并了本地状态记录：
  - `CURRENT_TASK.md`
  - `memory/2026-05-05.md`
- `npm run dev:restart` 在 Windows 工作区内被 CRLF 阻塞，bash 报 `set: pipefail\r: invalid option name`。
- 已改用隐藏 PowerShell 后台进程启动开发服务：
  - `npm run dev:backend`，日志：`backend-dev.log`
  - `npm run dev:weapp`，日志：`weapp-dev.log`
- 后端已监听 `3010`，`GET http://127.0.0.1:3010/api/health` 返回 `{"status":"healthy"}`。
- 小程序 watch 进程在 `D:\projects\food_link` 下运行，首轮 watch 编译已完成，`dist/` 刷新到约 `2026-05-09 04:14`。
- `weapp-dev.log` 仅出现 iconfont 运行时解析 warning，未见编译失败。
