# 2026-08-21

- 用户反馈 2026-08-20 上传的最新小程序整体很卡，本轮只诊断、未修改业务代码。
- 高概率客户端主因候选有两个：首次首页缓存补齐会并发最多 7 个历史 dashboard，叠加首屏增强请求后总量约 15 个；全局 console 捕获对每条日志立即执行同步 `setStorageSync`，而首页仍打印完整餐次 JSON 和多条 DEBUG 日志。`origin/main`/`origin/dev` 均含这些逻辑，production 默认日志缓冲数仍为 80。
- 正式 API 公网抽样正常：health 约 40ms，public-feed 连续 10 次 110–162ms，未见公共服务整体变慢。生产 SSH 公钥被拒，无法读取已登录请求日志。
- 3001/9420 自动化端口未监听，没有擅自启动微信开发者工具；真机 FPS/CPU/时序尚待运行时取证。
- 用户随后要求“安全地修复”。已保留反馈诊断日志，但将每条 console 同步写 storage 改为 500ms 合并异步写，并清理首页高频/大对象 DEBUG 日志。
- rebase 前发现 `origin/dev@83af2416` 已有并发的首页性能优化：完全删除了 7 天历史 dashboard 补齐，并将统计/身体指标/运动合并改为首屏完成后的后台增强。冲突解决以远端新结构为准，未重新引入本地限并发补齐器，只补远端尚缺的 console 异步合并写与剩余 DEBUG 清理。
- 新增日志密集输出只产生一次异步写入的幂等单测。rebase 前微信端 49 套/141 条 Jest、TypeScript和目标 ESLint 通过；rebase 后需按远端新测试基线重跑。`weapp-devtools` 对 3001/9420/9421 仍无法连接，未进行真机运行时验证。
