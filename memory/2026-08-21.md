# 2026-08-21

- 用户反馈 2026-08-20 上传的最新小程序整体很卡，本轮只诊断、未修改业务代码。
- 高概率客户端主因候选有两个：首次首页缓存补齐会并发最多 7 个历史 dashboard，叠加首屏增强请求后总量约 15 个；全局 console 捕获对每条日志立即执行同步 `setStorageSync`，而首页仍打印完整餐次 JSON 和多条 DEBUG 日志。`origin/main`/`origin/dev` 均含这些逻辑，production 默认日志缓冲数仍为 80。
- 正式 API 公网抽样正常：health 约 40ms，public-feed 连续 10 次 110–162ms，未见公共服务整体变慢。生产 SSH 公钥被拒，无法读取已登录请求日志。
- 3001/9420 自动化端口未监听，没有擅自启动微信开发者工具；真机 FPS/CPU/时序尚待运行时取证。
- 用户随后要求“安全地修复”。已保留反馈诊断日志，但将每条 console 同步写 storage 改为 500ms 合并异步写，并清理首页高频/大对象 DEBUG 日志。
- rebase 前发现 `origin/dev@83af2416` 已有并发的首页性能优化：完全删除了 7 天历史 dashboard 补齐，并将统计/身体指标/运动合并改为首屏完成后的后台增强。冲突解决以远端新结构为准，未重新引入本地限并发补齐器，只补远端尚缺的 console 异步合并写与剩余 DEBUG 清理。
- 新增日志密集输出只产生一次异步写入的幂等单测。rebase 后微信端 55 套/158 条 Jest、TypeScript、目标 ESLint 和 diff check 全部通过；提交 `475118c8` 已推送 `origin/dev`。`weapp-devtools` 对 3001/9420/9421 仍无法连接，未进行真机运行时验证，也未上传新的小程序版本。
- 用户明确要求先拉远端后重建 `dist`。`dev` 已确认为 `origin/dev@327a8e77`，`npm run build:weapp` 已生成 production 产物；主包 1704.7KB、`packageExtra` 1702.0KB、总包 4259.0KB，包体门禁全部通过，source map 为 0。Taro Rust worker 曾打印 macOS `system-configuration` panic 并短暂无输出，但构建最终完成，WXSS 优化与包体检查已独立重跑确认。
- 圈子排行榜已按用户确认的第 3 版效果完成本地代码：原绿色卡片拆为“用户榜 / 食物榜”左右双迷你榜；用户完整榜含打卡榜和单一综合健康指数榜，食物完整榜按标准食物每 100g 的多种营养素排名。健康榜复用分析页现有健康指数，不提供细分指标或计分说明。
- 新增社区健康榜、食物营养素榜后端接口及前端统一排行榜页。微信端 56 套/161 条 Jest、TypeScript、目标 ESLint、SCSS 编译，以及 Go 社区/路由/健康服务目标回归均通过。3001/9420/9421 均无开发者工具监听，未擅自启动，故尚无运行时截图与交互证据。
- 本轮改动仍在本地 `dev`，未提交、未推送或部署；未触碰无关未跟踪文件 `memory/2026-08-20.md`。
- 用户确认食物排行直接从标准食物库抽取。开发环境既有目录接口显示 `nutrition_food_count=9858`；新排行榜查询已对齐标准库 active、正式质量等级、中文名称和有效热量口径，再按每 100g 目标营养素排序。当前开发 API 的新排行榜路由仍为 404，故页面无数据的直接原因是后端尚未部署，而不是标准库为空。
- 用户明确要求部署。排行榜提交 `eb624308` 已推送 `origin/dev`；后端镜像 `foodlink:dev` digest `sha256:d8062f54529cea80062e8fc4e3ad9b92843a88fbd9249ef7be2ca0bed1fd3b7e` 已推送。自动更新未切换旧 digest，随后通过 `foodlink-dev` 精确更新 Deployment，3/3 副本滚动成功。
- 开发 API 已验证蛋白质、膳食纤维、钙三个标准食物榜均返回 HTTP 200 和真实 Top 3 数据，页面无食物排行的后端阻塞已解除。微信小程序前端源码已推送，但未执行小程序上传。
- 用户希望榜单展示金枪鱼、鸡胸肉、牛肉、虾、三文鱼、鸡蛋等日常基础食物，而不是蛋白粉。仓储筛选已改为常见食物候选集并保持每 100g 真实数值排序；明确候选不再因历史质量等级标签遗漏。测试覆盖乳清蛋白粉排除及金枪鱼 29 > 鸡胸肉 28.8 > 鸡蛋 12.7。本轮未修改 `dist`。
- 精确名称版本上线后只返回 4 种食物，确认鸡胸肉/鸡蛋等标准库名称含变体后缀。最终蛋白质查询改为名称族匹配，每个基础食物族仅取真实每 100g 值最高的一个标准库条目并统一展示基础名，避免变体重复占榜；对应 PostgreSQL 回归已通过。
- 食物族最高值版本实测会选中鸡胸肉干、牛肉干等加工变体。最终改为排除干、脯、松、粉、罐头、腌熏炸和脱水名称，优先精确基础名、其次最短普通变体；保留标准库真实每 100g 数值。
