# 2026-06-11

- 小程序 API 改为运行时按 `miniProgram.envVersion` 选择：`release` → `TARO_APP_API_BASE_URL_RELEASE`，`trial` → `TRIAL`，`develop` → `DEVELOP`；域名配置在 `.env.example` / `.env.development` / `.env.production`，不在代码写死。同一份 `build:weapp` 可先体验版再正式版。
- Admin 通过 `VITE_ADMIN_API_BASE_URL` 配置（`admin/.env.example`）；Cloudflare Pages Production → `api.healthymax.cn`，Preview → `dev.api.healthymax.cn`。
- 文档：`docs/api-url-configuration.md`；e2e 改用 `TARO_APP_API_BASE_URL_OVERRIDE`。
- 识别记录详情页修复：
  - 去掉 `.poster-actions` 的 `border-top`（`src/packageExtra/pages/record-detail/index.scss`）
  - 强化 `isOwner` 判定，比较记录 `user_id` 与 URL `from_user_id` 和本地 `user_id`，确保自己查看自己的记录时显示「修改记录」、隐藏「添加好友」邀请卡（`src/packageExtra/pages/record-detail/index.tsx`）
- Feedback 调试面板：当 `ANALYSIS_FEEDBACK_SUBMISSION_ENABLED = false` 时，分析结果页与文字分析结果页右下角的 feedback-dev-panel 不再渲染（`src/utils/api.ts`、`src/packageExtra/pages/result/index.tsx`、`src/packageExtra/pages/result-text/index.tsx`）
