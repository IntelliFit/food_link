# 2026-06-11

- 调研并修复应用内暗黑模式未同步微信系统栏的问题：官方资料确认页面内容变暗不会自动影响状态栏/右上角胶囊；本项目保持 `darkmode: false`，改为在 `AppColorSchemeContext` 和 `theme-navigation-bar` 中运行时调用 `setNavigationBarColor`、`setBackgroundColor`、`setBackgroundTextStyle`。移除主 Tab 页跳过逻辑，因为自定义导航栏页面仍需控制状态栏和胶囊前景色。顺手修正 `expiry` / `expiry-edit` 的 `useAppColorScheme()` 解构类型错误。验证：本次修改文件 `ReadLints` 无错误；`npm run typecheck` 剩余为其它既有错误；`mrc` 连接 9420/3001 失败，微信开发者工具自动化验证待用户开启端口后继续。
- 实现校园食堂评论回复功能：公共食物库评论表新增 `parent_comment_id`、`reply_to_user_id` 字段、索引和外键；后端 `publicfood` 评论接口支持回复树、回复创建、删除联动和评论数统计。
- 前端校园食堂详情页（复用 `food-library-detail`）评论区改为一级评论 + 二级回复展示；点击评论/回复按钮打开"回复 @用户"的底部输入面板；回复不带评分，一级评论仍保留可选评分。
- 验证：`go test ./internal/publicfood/... ./internal/migration/...` 通过；本次修改的前端文件 `ReadLints` 无诊断错误。`npm run typecheck` 仍被其它既有页面类型错误阻塞，但不再包含本次改动文件。
- UI 自动化验证阻塞：`mrc where --port 9420`、`mrc where --port 3001` 均连接失败，微信开发者工具目标项目窗口未开启自动化或端口未开放。

---

## 个人主页改版（仿知乎风格 + 游客模式 + 动态/收藏展示）

### 后端改动
- `backend/internal/user/service/user_service.go`：新增 `GetPublicProfile` 方法
- `backend/internal/user/handler/user_handler.go`：新增 `GetPublicProfile` handler
- `backend/internal/publicfood/handler/public_food_handler.go`：新增 `UserCollections` handler
- `backend/internal/recipe/handler/recipe_handler.go`：新增 `GetUserFavoriteRecipes` handler
- `backend/internal/app/app.go`：注册 3 条新路由
- 后端编译 `go build ./...` 通过

### 前端改动（多轮迭代后最终版）
- `src/packageExtra/pages/profile-settings/index.config.ts`：标题「个人设置」→「个人主页」
- `src/packageExtra/pages/profile-settings/index.tsx`：**重写**：
  - 整体左对齐布局
  - 头像在左，昵称+ID（只展示UUID前段）在头像右侧垂直排列
  - ID 旁有「复制ID」按钮（和「我的」页风格一致）
  - 统计行（记录天数·收藏·收藏内容）左对齐，无圆角卡片包裹
  - 点击头像/昵称/ID → 底部滑出编辑资料弹窗（修改头像、昵称、复制ID、保存、注销）
  - 下方常驻底部抽屉展示「最新动态」和「食物收藏」两个 Tab
  - 「最新动态」：复用 `communityGetFeed` + `author_id` 获取用户动态，支持下拉加载更多
  - 「食物收藏」：展示食谱收藏（原"食谱收藏"改名，接口不变）
  - 游客模式（URL 带 `user_id`）：只读，无编辑弹窗
- `src/packageExtra/pages/profile-settings/index.scss`：重写样式（左对齐、动态卡片、底部抽屉、编辑弹窗、暗色模式）
- `src/pages/profile/index.tsx`：「编辑资料」→「个人主页」
- `src/pages/community/index.tsx`：圈子作者头像点击跳转个人主页
- `src/packageExtra/pages/food-library/index.tsx`：公共食物库作者头像点击跳转个人主页
- `src/packageExtra/pages/campus-canteen/index.tsx`：校园食堂作者头像点击跳转个人主页
- `src/utils/api.ts`：新增 `getPublicUserProfile`、`getUserCollections`、`getUserFavoriteRecipes`
- TypeScript 编译无新增错误

### 阻塞
微信开发者工具自动化端口未开放，待用户启动开发服务器和开发者工具自动化后进行截图/交互验证。
