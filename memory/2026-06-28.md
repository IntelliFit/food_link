# 2026-06-28

## Admin feedback default reward credits
- Changed `admin/src/pages/feedback-page.tsx` so unresolved/open opinion-feedback details default the reward input to `5` credits when the stored value is empty or `0`.
- Kept resolved/closed records display-only against persisted `reward_credits`; this avoids rewriting historical zero-credit outcomes.
- Verification passed: `npm --prefix admin run build`; `git diff --check -- admin/src/pages/feedback-page.tsx` passed with only the repo's LF/CRLF warning.
