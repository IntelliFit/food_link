# 2026-04-26

- 用户最新反馈：当日代谢页面的图标漂移了。
- 本轮已修复：
  - `src/packageExtra/pages/stats-metabolic/metabolic-dynamics-report.scss`
    - 把返回箭头、标题图标、`用户基础数据` 图标、摘要卡图标统一改为固定宽高块级 icon
    - 明确设置 `width / height / line-height / text-align`
    - 避免 iconfont 继续受文本基线影响，在按钮或标签行里出现漂移
- 本轮验证：
  - `mrc where --port 9420`：`packageExtra/pages/stats-metabolic/index`
  - `mrc errors 20 --port 9420`：`0`
  - `mrc logs error 20 --port 9420`：`0`
- 备注：
  - 这轮 `mrc screenshot` 依旧没有稳定产出截图文件，所以没有拿到新的页面截图证据

- 用户继续反馈：我虽然删掉了预览弹层外层的“共几张”，但新角标仍没有出现在分享卡片图片右上角。
- 根因确认：
  - 前端 canvas 绘制逻辑已经加了多图角标
  - 但分享详情 `/api/food-record/share/{id}` 与记录详情 `/api/food-record/{id}` 没有像列表接口一样补全 `image_paths`
  - 导致海报生成时拿到的 `record` 仍是“只有 `image_path` 的单图外形”，所以前端判定 `imageCount <= 1`，角标自然不画
- 本轮已修复：
  - `backend/main.py`
    - 新增 `_hydrate_food_record_image_paths(...)`
    - `/api/food-record/{record_id}` 与 `/api/food-record/share/{record_id}` 都会在缺少 `image_paths` 时，按 `source_task_id` 从分析任务表补回多图
  - `backend/tests/integration/test_food_record_api.py`
    - 新增接口测试，覆盖“记录无 `image_paths`，来源任务有两张图”场景
- 本轮验证：
  - `source backend/venv/bin/activate && pytest backend/tests/integration/test_food_record_api.py -q`：`11 passed`
  - `python -m py_compile backend/main.py`：通过
- 备注：
  - 这次修的是后端数据链，当前本地后端如果没重启，前端依旧会拿到旧响应；重启后分享海报里的右上角多图角标才会真正出现

- 用户最新反馈：分享卡片里多图的“共几张”角标飘到了整张画布右上角，希望它贴在图片区域右上角，并且背景颜色更淡。
- 本轮已修复：
  - `src/utils/poster.ts`
    - 多图角标改为直接画进 `drawRecordPoster(...)` 的顶图区域右上角
    - 角标背景改成更淡的浅绿色半透明底，文字改为深绿色
  - `src/pages/index/components/MealRecordPosterModal.tsx`
  - `src/packageExtra/pages/record-detail/index.tsx`
    - 删除弹层外置 `poster-batch-badge`
  - `src/pages/index/index.scss`
  - `src/pages/index/components/MealRecordPosterModal.scss`
  - `src/packageExtra/pages/record-detail/index.scss`
    - 清理失效的 `poster-batch-badge` 样式
- 本轮验证：
  - `npm run lint -- src/utils/poster.ts src/pages/index/components/MealRecordPosterModal.tsx src/packageExtra/pages/record-detail/index.tsx`：通过
  - `mrc where --port 9420`：`pages/index/index`
  - `mrc errors 20 --port 9420`：`0`
- 备注：
  - 这次没拿到分享弹层的新截图，但从根因上已经把角标改为“海报内绘”，因此导出的分享图与弹层预览会一致

- 用户最新要求：首页 `.meal-item.is-warning` 和相关报错提示做黑色主题适配。
- 本轮已修复：
  - `src/styles/fl-color-scheme-dark.scss`
    - 首页今日餐食 warning 卡片补齐真实类名覆盖：`.meal-desc`、`.meal-time-pill`、`.meal-time-pill-text`、`.meal-type-target`、`.meal-macro-text`、`.meal-progress-percent`、`.meal-thumb-badge`
    - `meal-item.is-warning` 切到深红底 + 深红描边，warning 文案改为高对比浅红
    - `.analysis-status`、`.analysis-status.warning`、`.analysis-error` 改为稳定深色实底，避免黑色主题下发灰
- 本轮验证：
  - `npm run lint -- src/pages/index/index.tsx src/packageExtra/pages/result/index.tsx src/utils/home-dashboard-local-cache.ts`：通过
  - `mrc where --port 9420`：`pages/index/index`
  - `mrc exists .meal-item --port 9420`：存在
  - `mrc click .meal-item --port 9420`：成功
  - `mrc errors 20 --port 9420`：`0`
  - `mrc logs error 20 --port 9420`：`0`
- 备注：
  - `mrc screenshot` 这轮一直挂起，没有稳定产出截图文件；因此这次前端验证主要依赖运行态连接、元素存在、交互成功和错误日志

- 用户反馈：食物分析多图 `POST /api/analyze/batch` 返回 `500`，错误为“所有图片分析均失败，请稍后重试”，要求按现有后端测试口径补齐接口，并兼容单张图片分析结果结构。
- 本轮已修复：
  - `backend/main.py`
    - `batch` 单图调用补 3 次重试
    - 批量调用限并发为 `3`
    - 允许部分图片失败时继续返回成功汇总
    - 汇总结果增加与单张分析兼容的结构化字段
    - 补回 `update_analysis_task_result_sync` import，避免成功路径写任务结果时报 `NameError`
  - `backend/tests/integration/test_food_analysis_batch_api.py`
    - 新增 3 个测试，覆盖成功汇总 / 部分失败 / 全部失败
- 本轮验证：
  - `source backend/venv/bin/activate && pytest backend/tests/integration/test_food_analysis_batch_api.py -q`：`3 passed`
  - `python -m py_compile backend/main.py`：通过
- 备注：
  - 这次没有直接跑真实 DashScope live 调用，主要是先把后端接口行为和容错口径锁进测试

- 用户随后提供了一个真实失败 payload：
  - `image_urls` 为 2 张 Supabase 食物图
  - `modelName: "gemini"`
  - `execution_mode: "standard"`
  - `meal_type: "evening_snack"`
  - `diet_goal: "maintain"`
  - `activity_timing: "none"`
  - `timezone_offset_minutes: -480`
  并要求按这份请求体把 `/api/analyze/batch` 调通。
- 根因确认：
  - `batch` 之前把前端传入的 `modelName: "gemini"` 当成 DashScope 的模型名直接透传，导致 Gemini 请求被错发到 DashScope，最终整批返回“所有图片分析均失败”。
- 已修复：
  - 新增 `_resolve_food_vision_model_config(...)`
  - `/api/analyze` 和 `/api/analyze/batch` 现在都会把：
    - `qwen / qwen-vl-max` 解析到 DashScope
    - `gemini / gemini-*` 解析到 OfoxAI Gemini
  - `batch` 单图子请求在 Gemini 模式下直接调用 `_analyze_with_gemini(...)`
  - 新增 `gemini alias` 覆盖测试
- 本轮验证：
  - `source backend/venv/bin/activate && pytest backend/tests/integration/test_food_analysis_batch_api.py -q`：`4 passed`
  - 使用用户原始 payload，通过 ASGI 接口层 + 真实 Gemini 调用复测成功：
    - `STATUS 200`
    - `image_count = 2`
    - 返回 `4` 个识别 item
  - 修复后本地后端已重启，`3010` 正常监听

- 用户随后要求：重新启动后端。
- 本轮已执行：
  - `npm run stop:backend`
  - 发现 `stop:backend` 没有清掉旧的 `run_backend.py`，随后手动执行 `pkill -f 'run_backend.py'`
  - 尝试 `nohup npm run dev:backend > backend-dev.log 2>&1 &`，但这轮后台方式未稳定驻留
  - 最终以前台 PTY 方式执行 `npm run dev:backend`，后端恢复正常
- 本轮验证：
  - `lsof -iTCP:3010 -sTCP:LISTEN`：可见 Python 进程监听 `3010`
  - `curl -I http://127.0.0.1:3010/`：返回 `HTTP/1.1 404 Not Found`，说明 HTTP 服务已启动

- 用户随后要求：实物分析不要停留在分析页原地 loading；应进入既有 `analyze-loading` 页，由后台继续处理，用户可以直接退出。并要求结果页多图支持左右滑动查看后提交代码。
- 本轮已修复：
  - `src/packageExtra/pages/analyze/index.tsx`
    - 删除多图同步 `/api/analyze/batch` → 直接跳结果页的特殊分支
    - 多图现在和单图一样统一走异步任务提交，再跳 `analyze-loading`
  - `src/packageExtra/pages/result/index.tsx`
    - 保留顶部 `Swiper` 多图左右切换
    - 新增 `imagePaths` 变化时的 `currentImageIndex` 纠正逻辑，避免索引残留
- 本轮验证：
  - `npm run lint -- src/packageExtra/pages/analyze/index.tsx src/packageExtra/pages/result/index.tsx`：通过
  - `mrc where --port 9420`：连接成功
  - `mrc relaunch /packageExtra/pages/result/index --port 9420`：成功
  - `mrc exists .scanner-hero-swiper --port 9420`：存在
  - `mrc exists .image-counter --port 9420`：存在
  - `mrc relaunch /packageExtra/pages/analyze-loading/index?... --port 9420`：成功
  - `mrc exists .btn-leave-v3 --port 9420`：存在
  - `mrc errors 20 --port 9420`：`0`
- 当前阻塞：
  - `dev:weapp` 仍卡在 `transforming...`，未能确认最新源码已被 watch 构建产物实时带入，因此这轮没有完成“真实点击多图提交后跳到 loading 页”的最新产物端到端复演

- 用户随后反馈：分析结果页虽然显示 `1/2` 与“共 2 张”，但模拟左右滑时无法切换图片。
- 根因判断：
  - 结果页顶部头图区是 fixed 层，但整页透明 `ScrollView` 覆盖在上面
  - 导致视觉上能看到 `Swiper`，实际横向手势却被滚动层先接收
- 已修复：
  - `src/packageExtra/pages/result/index.scss`
    - `.scanner-hero-section` `z-index` 提高到 `3`
    - `.scanner-hero-swiper` 增加 `pointer-events: auto`
- 本轮验证：
  - `mrc where --port 9420`：当前页为 `packageExtra/pages/result/index`
  - `mrc errors 20 --port 9420`：`0`
- 仍有限制：
  - 当前 `mrc` 的 `swipe` 命令在这套环境报 `mp.swipe is not a function`
  - `dev:weapp` 仍卡在 `transforming...`
  - 所以这次没有拿到自动化层面的“滑到第 2 张”实证，只能基于页面层级问题完成高置信度修复

- 用户随后要求：分析结果页点击“记录”后，不仅要同步服务器，也要立刻同步当前用户本地缓存，并刷新首页当天的“今日餐食”。
- 已修复：
  - `src/utils/home-dashboard-local-cache.ts`
    - 新增 `applyOptimisticFoodRecordToHomeDashboardSnapshot(...)`
    - 保存饮食记录成功后，立即把当天本地 dashboard 快照里的 `intakeData` 与 `meals` 乐观更新
  - `src/packageExtra/pages/result/index.tsx`
    - 记录成功后先更新今天本地快照，再触发首页 intake 变化事件，最后异步回刷云端 dashboard
  - `src/pages/index/index.tsx`
    - 首页收到 `HOME_INTAKE_DATA_CHANGED_EVENT` 后，如果当前查看的是今天，直接读取本地 dashboard 快照回填 UI，而不再只是标记 stale
- 本轮验证：
  - `npm run lint -- src/packageExtra/pages/result/index.tsx src/pages/index/index.tsx src/utils/home-dashboard-local-cache.ts`：通过
  - `python -m py_compile backend/main.py`：通过

- 用户要求：启动前后端并打开微信开发者工具。
- 本轮已执行：
  - `npm run dev:restart`
  - `/Applications/wechatwebdevtools.app/Contents/MacOS/cli auto --project /Users/kirigaya/project/food_link --auto-port 9420`
- 结果：
  - 微信开发者工具自动化端口 `9420` 已监听
  - `mrc errors 10 --port 9420` 返回 `0`
  - 前端 `npm run dev:weapp` 已进入 watch 构建流程
  - `npm run dev:restart` 本轮没有把 backend 稳定挂住，后续额外以 PTY 方式执行 `npm run dev:backend` 后，`3010` 才恢复监听
  - `curl -I http://127.0.0.1:3010/` 返回 `404`，说明 HTTP 服务已启动
- 当前注意事项：
  - `mrc where --port 9420` 本轮连接成功但页面信息回执挂起，DevTools 自动化不是完全稳定
  - 后端当前依赖本轮额外挂起的 PTY 会话，若会话结束需重新确认服务状态
