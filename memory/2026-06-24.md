# 2026-06-24

## 官网 SEO 对齐 watcha.cn
- 用户要求参照 `https://watcha.cn` 的 SEO 方式，优化 `website/` 官网在 `https://healthymax.cn/` 的 title/meta/canonical/OG/Twitter/JSON-LD/sitemap。
- 已完成：
  - 首页静态 head 补齐为更完整的搜索与分享信息：`keywords`、根域 canonical、首页 `og:image`、`twitter:site/url`、`globalHeadConfig`、`WebSite + Organization + SoftwareApplication` JSON-LD。
  - `PageHead` 运行时同步逻辑补齐了 `keywords/author/application-name/OG/Twitter/canonical`，并统一维护 `globalHeadConfig` 与结构化数据。
  - 新增 `website/scripts/prerender-seo.mjs`，构建后直接生成 `/about`、`/agreement`、`/privacy` 等公开路由的独立 HTML head，避免只靠 SPA 改 meta。
  - `website/src/content/seo.ts` 中把 `/share/food-record/:id` 固定为 `noindex, nofollow`，避免用户饮食分享被搜索引擎收录；空内容 `/blog` 也改为 `noindex` 并从 sitemap 移除。
  - `website/public/sitemap.xml` 补了 `lastmod`，构建产物中的 `dist/sitemap.xml` 会按新策略重写。
- 验证：
  - `cd website && ./node_modules/.bin/eslint src/content/seo.ts src/components/seo/PageHead.tsx src/pages/FoodRecordSharePage.tsx` 通过。
  - `cd website && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/tsc -p tsconfig.app.json --noEmit` 通过。
  - `npm --prefix website run build` 通过；已人工检查 `dist/about/index.html` 与 `dist/index.html` 的 SEO head。
  - `npm --prefix website run lint` 仍被既有 `website/src/components/ui/badge.tsx`、`website/src/components/ui/button.tsx` 的 Fast Refresh 规则阻塞，本轮未处理这两个历史问题。

## App food record share frontend page and QR poster
- User pointed out that current App sharing opened an `api.*` domain backend HTML page and asked to move the final public page to the frontend domain with a nicer UI, backend data lookup, and an App open button.
- Implemented: `packages/api-client` now builds food record share URLs as `https://healthymax.cn/share/food-record/:record_id`; dev/local API environments append `api_env` so the frontend page can fetch the matching backend.
- Implemented: `website/` now has `/share/food-record/:recordId`, fetches `/api/food-record/share/:recordId`, and renders the public record, nutrition metrics, food list, copy-link action, and `foodlink://food-record?record_id=...` App deep link.
- Implemented: backend `/share/food-record/:record_id` no longer renders inline HTML; it redirects to the frontend share page. The data API remains `/api/food-record/share/:record_id`.
- Implemented: App record detail share sheet now includes a QR poster preview. The QR scans to the frontend web page, and the web page can jump to App. Mini-program posters keep the existing mini-program QR behavior.
- Verification passed: API client typecheck/tests, website build, backend foodrecord handler targeted tests, and `git diff --check`. Mobile typecheck is still blocked by existing `HomeScreen.tsx` errors; the new `DetailScreens.tsx` share-poster code has no new typecheck errors.
- Follow-up update: App QR poster sharing was strengthened after audit. Added `react-native-view-shot` and `expo-sharing`; the poster modal now captures the poster card as a PNG and shares the image file through Expo Sharing, while keeping copy-link fallback. Expo Go or other environments without native sharing/capture support will show a fallback message.
- Not done: no local long-running server was started and no weapp-devtools run was attempted.

## 小程序分享卡片二维码固定跳正式版
- 用户要求：无论当前小程序是开发版、体验版还是正式版，分享卡片/海报右下角二维码扫描后都应跳转正式版 `release` 小程序。
- 已新增 `apps/wechat/src/utils/api.ts#getShareQrEnvVersion()`，固定返回 `release`；`getWeappEnvVersion()` 继续表示当前真实运行环境，不改 API 选择和登录页环境标识。
- 已替换所有 `getUnlimitedQRCode(..., getWeappEnvVersion())` 调用点：邀请页二维码、记录详情海报、当天记录海报、首页海报、`MealRecordPosterModal`。
- 验证：目标文件 eslint、`git diff --check` 通过；小程序全量 typecheck 仍被既有旧错误阻塞（day-record、feedback、food-library-share、food-library）。
- 本轮未启动开发服务器、测试服务器或 weapp-devtools。

## 邀请好友申请延后到资料完成后
- 用户指出：新用户扫码注册后立即发送好友申请时，邀请人只能看到默认头像和“用户”昵称。
- 已保留注册/登录时携带 `inviteCode` 的奖励绑定链路，只移动好友申请 `/api/friend/invite/accept` 的触发时机。
- 登录页不再在测试手机号注册或微信登录 token 保存后立即 accept；头像昵称 `updateUserInfo` 成功后才触发好友申请。
- 如果用户资料原本已完整，则在进入健康档案或首页前通过 `continueAfterAuthGates` 发送好友申请。
- App 启动扫码且已登录时不再静默发送好友申请，改为进入邀请页由用户确认。
- 验证：目标文件 eslint、`git diff --check` 通过；小程序全量 typecheck 仍被既有旧错误阻塞（day-record、feedback、food-library-share、food-library）。

## 注销账号后隐藏主页与清理公开内容
- 用户反馈：注销账号后，再进入该用户主页仍显示数据；注销用户的动态和食物记录仍残留。
- 后端修复：`UserRepo.DeleteByID` 改为事务内主动清理账号相关公开内容和互动，再删除用户行；覆盖 `user_food_records`、`user_exercise_logs`、`user_circle_posts`、`public_food_library`、`user_recipes`、feed 点赞/评论/通知/举报、好友/关注/私信、分析任务、健康文档、会员积分等表；清理前判断表是否存在。
- 前端修复：个人主页非本人模式下先请求公开资料；若公开资料不存在，则显示“用户已注销”，不再加载动态/收藏，并隐藏关注、私信、拉黑操作。
- 验证通过：后端 `auth/repo` 级联清理定向测试、`user/service` 资料/注销定向测试、后端 server/migration build、前端目标 eslint、`git diff --check`。
- 小程序全量 typecheck 仍被既有旧错误阻塞：`day-record`、`feedback`、`food-library-share`、`food-library`。

## 指定测试用户数据库数据删除
- 用户要求直接清理 3 个指定用户：`b4bfac12-a5d5-4092-8537-d5ad6e6e9400`、`b24950ae-0c70-4e5a-bf0a-50ad3d04d7ce`、`74af6daa-6efb-434c-b06c-d3a8532cd6f7`。
- 执行前只读统计：3 个 `weapp_user`、1 条 `user_food_records`、6 条 `analysis_tasks`、3 条 `friend_requests`、2 条 `user_friends`、1 条 `user_invite_referrals`，另有 feed 互动等关联。
- 已在当前 `backend/config.yaml` 指向的 PostgreSQL 数据库中用事务清理账号和关联数据；`manual_food_library` 线上是全局基础库且无 `user_id`，未按用户删除。
- 执行后复查：账号、食物/运动/圈子/菜谱/分析任务、好友/邀请、私信、feed 互动、积分会员、健康文档、试用 first_user 关联均为 0。
