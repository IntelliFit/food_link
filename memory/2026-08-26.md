# 2026-08-26 标准食物缺图低成本回填

- development 基线为符合 `active + kcal>0` 口径 11,405 条、已有图 8,963、缺图 2,442；两批薄荷数据启用使缺图数较旧基线增加 1,520 条。
- 普通 Bing 搜索 50 条试跑约 3 分 50 秒，12 条模型初筛候选均因授权来源不清而未写入；库内复用 50 条约 33 秒得到 14 条候选。
- 本地逐图复核后接受 8 条、拒绝 6 条不精确候选。8 条均复用已有 COS key，未生成或上传新图，事务写入成功。
- 写后已有图 8,971、缺图 2,434（中文 1,787、非中文 647），剩余最高 `uses=0`，没有用户实际引用过的缺图项。
- 回填 CLI 新增库内检索、Commons 合规来源元数据和人工 allowlist 安全写入路径；目标 Go 测试、vet、diff 检查通过。运行手册已记录“库内复用 → Commons → AI 生成”的经济顺序。
- 本轮数据库已生效；代码、文档与状态记录因工作区混有其他任务且受下班统一提交约定限制，未提交、未推送、未部署。

# 2026-08-26 退出项目与本地删除收尾

- 用户要求收尾所有改动并完全删除本地项目。已刷新 `origin/dev`，本地 `dev` 基线为 `a612bd87`。
- 提交前验证通过：微信 TypeScript/Lint、全量 Jest 60 套/179 条、USDA Python 3 条、相关 Go test/vet、gofmt 和 diff check。微信自动化端口 9420/3001 未监听，无运行时 UI 证据。
- 用户授权后已修复图片回填 High：通用 provider 禁止直接 apply；网络图专用路径 apply 时强制人工 allowlist，且与 URL、来源、许可证、复用 key 及快照逐字段绑定；库内复用保持专用单事务路径。候选在人工复核后变化会被拒绝。
- 最终 Standards / Spec 双轴复审无 Blocker/High；改动已具备分组提交和推送条件。该交接记录会随代码推送，远端确认后按用户要求删除本地项目。

# 2026-08-26 河南本科院校校区食堂汇总导入

- 用户提供工作簿共 46 行，解析为 24 行已核实、7 行基本核实、15 行待核实；只发布已核实子集，共 14 校、24 校区、49 食堂。
- development dry-run 为复用 7 校区/11 食堂、新建 17 校区/38 食堂；事务发布和最终代码幂等复跑成功，写后 existing 24/49、new 0/0，49 个食堂全部 active。
- 导入使用 D 级用户确认汇总证据并保留原表 SHA-256 与官方 URL，不覆盖既有 A/B/C 强证据元数据；22 行不确定项没有进入用户端。
- 新增河南独立 seed、发布命令和回归测试；完整 migration/命令测试、Go vet、diff check 通过。未修改小程序 UI，无需 weapp-devtools 运行态验证。
- 最终通过开发环境真实用户目录接口鉴权核验：14 校、24 校区、49 食堂全部返回且均为 active，缺失/未激活 0；管理端和用户端均已进入现有可见路径。
- 提交 `a612bd87` 已推送 `origin/dev`；dev 后端镜像已推送，digest `sha256:9c7d4c8ab3315844839582ebcd02b251f9b161da00d8e8ad876f1996a22d2604`。
