# 2026-05-22

- 后端 Docker 推送脚本已优化 Go builder 镜像源重试：`backend/scripts/push-docker-ccr.mjs` 默认按 `docker.io`、`docker.m.daocloud.io`、`docker.xuanyuan.me`、`docker.1ms.run`、`docker.1panel.live` 的 `library/golang:1.26.1-bookworm` 候选列表逐个尝试 `docker buildx build --push`，用于规避 Docker Hub/GFW 导致的 `load metadata` 失败。保留 `DOCKER_GO_BUILDER_IMAGE` 单镜像覆盖，并新增 `DOCKER_GO_BUILDER_IMAGES` 逗号分隔多镜像覆盖。同步更新 `AGENTS.md` 部署说明。

- 线上 Kafka worker 报 `failed to open connection to [[kafka:9092]]:9092 ... missing port in address` 的根因不是用户贴出的 YAML 错，而是 `.yaml` namespace 之前走了 agollo 的已解析 cache，列表值被 `fmt.Sprint` 成 `[kafka:9092]` 字符串。最终修复改为：`backend/pkg/config` 对 `.yaml/.yml` Apollo namespace 直接请求 `/configfiles/{appId}/{cluster}/{namespace}` 原始文本端点，再用 Viper 解析 YAML；properties/key-value namespace 才继续走 agollo cache。验证：`cd backend && go test ./pkg/config ./internal/taskqueue -count=1` 通过。

- Apollo `apollo returned no config` 的一次真实根因：用户已在 Apollo Portal 发布 `food-link / DEV / local / app-config.yaml`，但 `backend/apollo-config.yaml` 的 `apollo.config_server_url` 写成了 Portal 管理后台域名。用 `/configs/food-link/local/app-config.yaml?ip=127.0.0.1` 验证时返回登录页 HTML，而不是 Config Service JSON。改为真实 Apollo Config Service / Meta Service 地址后，用户确认可以启动。

- Apollo namespace 不再自动补 `.yaml/.yml` 后缀。
  - 用户明确要求“没有就是没有”，已删除 `local-config -> local-config.yaml/local-config.yml` 的隐式探测逻辑和对应测试。
  - 当前行为：`apollo.namespaces` 写什么就只读什么；如果 Apollo 返回 0 条配置，直接报 `apollo returned no config` 并提示检查 namespace、发布状态和 access key。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1` 通过。

- 首页健康伙伴（宠物）收起状态优化：
  1. 修复收起后宠物形象变扁问题：`src/pages/index/index.scss` 中 `.pet-companion-float.is-collapsed .pet-companion-card` 尺寸从 108rpx 增大到 116rpx，并添加 `overflow: hidden`；`.pet-companion-float.is-collapsed .pet-companion-avatar` 恢复为 92rpx×92rpx（与展开状态一致），保持宠物比例不变。
  2. `src/pages/index/index.tsx` 中 `getPetFloatMetrics` 同步更新 collapsed 尺寸为 116rpx。
  3. 新增收起后快捷小组件：在 `pet-companion-card` 内绝对定位一个 32rpx 小圆点（`.pet-companion-mini-hint`），位于右上角。有未领取奖励时显示橙色脉冲动画并带 `!` 标记，平时显示绿色状态点。点击小圆点可直接展开宠物（阻止 touch 事件冒泡，避免与拖拽冲突）。
  4. 状态持久化逻辑（`HOME_PET_COLLAPSED_KEY` / `HOME_PET_FLOAT_POSITION_KEY`）已有且工作正常；同步更新 `src/pages/profile/index.tsx` 的 `handleClearCache`，清除缓存时一并重置宠物收起状态和浮窗位置。
  5. dist 产物已验证包含全部修改（`pet-companion-mini-hint`、116rpx、`overflow:hidden` 均正确编译）。微信开发者工具自动化端口因 IDE 重启暂时无法连接，截图验证待后续补充。
