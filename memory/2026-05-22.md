# 2026-05-22

- 后端 Docker 推送脚本已优化 Go builder 镜像源重试：`backend/scripts/push-docker-ccr.mjs` 默认按 `docker.io`、`docker.m.daocloud.io`、`docker.xuanyuan.me`、`docker.1ms.run`、`docker.1panel.live` 的 `library/golang:1.26.1-bookworm` 候选列表逐个尝试 `docker buildx build --push`，用于规避 Docker Hub/GFW 导致的 `load metadata` 失败。保留 `DOCKER_GO_BUILDER_IMAGE` 单镜像覆盖，并新增 `DOCKER_GO_BUILDER_IMAGES` 逗号分隔多镜像覆盖。同步更新 `AGENTS.md` 部署说明。

- Apollo `apollo returned no config` 的一次真实根因：用户已在 Apollo Portal 发布 `food-link / DEV / local / app-config.yaml`，但 `backend/apollo-config.yaml` 的 `apollo.config_server_url` 写成了 Portal 管理后台域名。用 `/configs/food-link/local/app-config.yaml?ip=127.0.0.1` 验证时返回登录页 HTML，而不是 Config Service JSON。改为真实 Apollo Config Service / Meta Service 地址后，用户确认可以启动。

- Apollo namespace 不再自动补 `.yaml/.yml` 后缀。
  - 用户明确要求“没有就是没有”，已删除 `local-config -> local-config.yaml/local-config.yml` 的隐式探测逻辑和对应测试。
  - 当前行为：`apollo.namespaces` 写什么就只读什么；如果 Apollo 返回 0 条配置，直接报 `apollo returned no config` 并提示检查 namespace、发布状态和 access key。
  - 验证：`go test ./pkg/config -count=1`、`go test ./internal/app -run '^$' -count=1` 通过。
