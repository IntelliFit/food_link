# 2026-07-01

## 近两周 bug 清单快速核查与低风险批修
- 用户提供 `C:\Users\29454\Documents\New project\bug临时群_近两周Bug整理_2026-07-01.md`，要求快速核查 `food_link` 相关 bug，判断哪些未修、哪些容易一次性修，并强调改完要验收。
- 本轮采用小批次处理：只改低风险前端状态/展示问题，不碰需要真实数据和端到端复现的大链路。
- 已修：
  - 首页日期栏：从只看 `calories > 0` 改为支持 `hasRecord`，有记录但热量为 0 也能显示已记录态。
  - 首页饮水量：云端 `water_daily` 刷新时如果低于本地乐观水量，保留本地较大值，避免添加餐食后水量被刷新覆盖回旧值。
  - 结果页比例调节：实际摄入区域显示 `约 Xg`，滑动比例时用户能看到克数实时变化。
- 健康档案 500：当前代码已有 JSONB `health_condition` 归一化兜底，标为待线上/真机注册回归，不重复改。
- 不建议本批处理：100g/实际重量营养换算、扫码邀请、校园食堂选项体系、动态重复、公共食物库地理搜索。
- 验证：
  - result converter 单测通过。
  - 改动文件 eslint 通过。
  - git diff check 通过（仅 LF/CRLF warning）。
  - auth repo JSONB 相关测试通过。
  - 微信开发者工具自动化 `3001/9420` 均无法连接，运行时截图/交互验收待补。
  - 整体 wechat typecheck 和部分后端测试仍被既有问题阻塞，详见 active task。

## 数据库/营养异常真实记录审计
- 用户明确：日期入库和健康档案问题已经搞定，重点继续查其它数据库/营养数据 bug。
- 只读扫描近两周远程 `user_food_records` 693 条，筛出 31 条硬异常；典型记录：
  - `8778a314-0ebc-4ddb-a816-849ad570fe12`：西瓜 1200g，water 1840ml。
  - `5bceb62a-e15c-4b8d-9bb7-45e7717032f8`：西瓜 weight 1760g、ratio 90、water 2912ml。
  - `a2e93389-380a-49cd-916a-77e450d1a548`：手动记录 25g 百奇，保存 504kcal/64.5g carbs。
  - `f25bb6ff-31ad-4e12-81c1-328c679c60ba`：手动记录 35g 百奇，保存 459.6kcal/54.9g carbs。
  - `56233d8d-35a1-4f2e-bcd1-f331f6af351b`：用户纠错上下文使每日坚果 30g 变 1500kcal，需下一批继续查 correction context。
- 已修：
  - 包装食品手动记录返回时，把每 100g 营养换算到默认份量总量，不再把每 100g 当 25g/35g 总量。
  - 新保存、首页聚合、本地缓存、记录详情展示都对单 item 水分做不超过重量的上限。
- 验证：相关 Go 单测、weapp 单测、core 单测、core typecheck、scoped eslint、diff check 均通过；WeApp DevTools 3001/9420 仍无法连接，无法截图/交互验收。

## 微信端本地编译缺少 @food-link/core
- 用户反馈小程序开发构建在 `apps/wechat/src/components/PetAvatar.tsx` 处报 `Rollup failed to resolve import "@food-link/core"`。
- 排查结论：这是工作区依赖声明/安装问题，不是 `PetAvatar` 组件逻辑本身错误。
- 已处理：
  - 在 `apps/wechat/package.json` 补上 `@food-link/core: 0.0.0`
  - 根目录重新执行 `npm install`
  - 恢复 `node_modules/@food-link/core -> packages/core` 软链
- 验证：`apps/wechat` 下 `require.resolve('@food-link/core/package.json')` 已成功。
- 提醒：用户需要重启本地 `dev:weapp` watch 进程，旧的失败进程不会自动拾取新装的工作区依赖。

## 同城动态 / 定位失败核查
- 用户追问 `food_link` 现在是否已有“同城动态 / 同城好友 / 同城饮食动态优先推荐”能力，并反馈线上定位报 `位置服务暂时不可用，请稍后尝试`。
- 代码核查结果：
  - 社区 feed 当前没有同城排序/筛选参数，只有 `all / priority / public` 作者范围。
  - 位置错误文案来自后端 `location_service`，不是前端写死。
  - 定位能力当前主要用于地址搜索/逆地理、分析请求省市区补充、公共食物库地理信息录入。
- 2026-07-01 直接从本机请求天地图：
  - geocoder `https://api.tianditu.gov.cn/geocoder` 返回 `403 Forbidden`
  - search `https://api.tianditu.gov.cn/v2/search` 返回 `403 Forbidden`
- 判断：若线上仍使用同一 `TIANDITU_TK`，则线上“位置服务暂时不可用”高概率是第三方 key/配额/禁用问题，而不是小程序 `getLocation` 权限本身。

## 公共食物库搜索地址失败兜底
- 用户截图确认：公共食物库“搜索地址”页搜索 `北京大学` 后弹出 `位置服务暂时不可用，请稍后重试`。
- 已修改：
  - `apps/wechat/src/packageExtra/pages/location-search/index.tsx`：新增 `Taro.chooseLocation` 原生地图选点兜底；后端搜索失败时弹窗引导用户去系统地图选点；页面上也新增 `系统地图选点` 入口。
  - `apps/wechat/src/packageExtra/pages/location-search/index.scss`：补充兜底入口和 spinner 样式。
  - `apps/wechat/src/app.config.ts`：`requiredPrivateInfos` 增加 `chooseLocation`。
  - 同页加载态不再显示 `定位中...` / `搜索中` 文字，改为 spinner。
- 验证：
  - location-search 与 app.config eslint 通过。
  - 改动范围 `git diff --check` 通过（仅 LF/CRLF warning）。
  - `npx tsc --noEmit --pretty false --project apps/wechat/tsconfig.json` 仍被既有无关 TS 错误阻塞。
  - WeApp 自动化端口 `3001/9420` 都无法连接，运行时截图和交互验证待补。
