# 2026-05-15

- 继续调整分析页：营养证据/结构指标面板内折叠卡片的标题和简介统一左对齐；健康指数 6 个友好度卡片去掉底部横线和“查看更多”，改为右下角 `icon-right-arrow + 更多`，整卡点击仍打开详情；风险详情弹层与 AI 风险解读弹层的层级、遮罩、最大高度和安全区留白对齐“我的关注”底部浮层。验证：stats TSX eslint、stats SCSS stylelint、目标文件 `git diff --check` 均通过；全量 typecheck 仍失败于既有未触碰问题；weapp-devtools 3001/9420 未开启自动化，无法截图。

- 用户要求左上角 `近一周/近一个月` 的下拉箭头使用图标系统而非字符。已将 `src/pages/stats/index.tsx` 中的 `⌄` 替换为 `iconfont icon-right-arrow`，并在 `src/pages/stats/index.scss` 里旋转 90 度作为向下箭头。验证：stats TSX eslint、目标文件 `git diff --check` 通过，`rg` 确认 `stats` 页无 `⌄`；weapp-devtools 3001/9420 未开启自动化，无法截图。

- 继续调整分析页布局：三段 Tab 下方主体增加上侧间距；健康指数里的“我的关注”从整张卡片改成健康指数卡右侧的小胶囊按钮，点击后打开底部浮层，浮层内可定制 6 个关注方向并保持至少 1 项；弹层打开时复用 `stats_risk_detail_visible` 隐藏自定义 tabBar，浮层 `z-index: 10000` 盖过底部导航。营养/结构面板中的热量、宏量、餐次、长期指标卡片改为默认展开；标题改为 `热量摄入趋势`、`宏量营养结构`、`餐次热量分布`、`长期健康指标`，不再使用“证据”命名。验证：stats TSX eslint 和目标文件 `git diff --check` 通过；全量 typecheck / stats stylelint 仍失败于既有未触碰问题；weapp-devtools 3001/9420 未开启自动化，无法截图。

- 用户要求调整分析页布局：`近一周/近一个月` 移到左上角并改为带下拉箭头的周期选择，页面顶部间距保持/缩小为 `182rpx`；原周期切换位置改为面板 Tab，默认健康指数，第二块热量证据+宏量结构证据，第三块餐次分布+长期健康指标。已修改 `src/pages/stats/index.tsx` 和 `src/pages/stats/index.scss`：新增左上固定周期下拉（`Taro.showActionSheet` 切换），新增 `健康指数/营养证据/结构指标` 三段内容面板，移除当前渲染中的记录分布和连续记录卡片以减少堆叠。验证：`npx eslint src/pages/stats/index.tsx --max-warnings 0`、目标文件 `git diff --check` 通过；全量 typecheck 和 stats stylelint 仍失败于既有未触碰问题；weapp-devtools 3001/9420 均未开启自动化，无法截图。

- 用户反馈健康档案基础信息中的出生日期显示成 `2001-01-01T00:00:00Z`，只需要年月日。已修改 `src/packageExtra/pages/health-profile-view/index.tsx`：新增 `formatProfileDateOnly()`，展示时从生日字符串提取 `YYYY-MM-DD`；验证 `npx eslint src/packageExtra/pages/health-profile-view/index.tsx --max-warnings 0` 和目标文件 `git diff --check` 通过。按项目规则尝试 `weapp-devtools` 的 3001/9420 端口均因微信开发者工具自动化未开启连接失败，未能截图。

- 用户要求写脚本获取“重新智能分析”后录入的纠错反馈样本最新 10 条，用于 debug。已新增 `scripts/query-analysis-feedback-samples.sh`，默认读取 `backend/config.yaml` 的 PostgreSQL 配置，也支持 `POSTGRESQL_*` 环境变量覆盖；查询 `analysis_feedback_samples`，输出任务链路 ID、模型信息、错误信息和 `before_result.items/user_correction_items/after_result.items` pretty JSON。`package.json` 新增 `npm run debug:feedback-samples`。验证：`bash -n`、`npm pkg get scripts.debug:feedback-samples`、目标文件 `git diff --check` 通过。未直接拉取生产样本内容到对话中，避免泄露用户记录。
