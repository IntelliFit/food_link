# 2026-06-12

## 完成：圈子筛选抽屉与私信页深色主题适配

- 让圈子页（`src/pages/community/index.tsx`）接入 `FlPageThemeRoot` 与 `applyThemeNavigationBar`，使其进入项目统一的主题系统。
- 在 `src/styles/fl-color-scheme-dark.scss` 补充圈子筛选抽屉的深色覆盖：漏斗按钮、搜索图标、抽屉背景/把手/标题/完成按钮、chip 激活态。
- 在 `src/styles/fl-color-scheme-dark.scss` 补充私信页完整深色覆盖，并在 `private-chat/index.tsx` 增加 `placeholderClass` 以支持 placeholder 颜色切换。
- 已用 `npm run typecheck` 与微信开发者工具自动化截图验证深色/浅色两种主题。

## 完成：首页轮播图新增意见反馈入口

- 在 `src/pages/index/index.tsx` 的 `rewardHintBanners` 中新增 `feedback` 项，标题「意见反馈」，描述「提交宝贵建议，最高可得 +5 积分」，点击跳转 `/packageExtra/pages/feedback/index`。
- 在 `src/pages/index/index.scss` 中新增 `.home-reward-hint--feedback` 蓝色渐变样式。
- 已用 `npm run typecheck` 与微信开发者工具自动化验证 banner 渲染和目标页跳转。

## 完成：Admin Benchmark 模块

- 为 admin 后台新增「数据集评测」模块，入口在 `AdminSidebar`，`App.tsx` 按菜单切换页面。
- 后端新增 `benchmark_runs`、`benchmark_run_samples` 表与 DDD 分层（domain/repo/service/handler），注册 `/api/admin/benchmark/*` 路由。
- 前端新增 `admin/src/pages/benchmark-page.tsx`，覆盖数据集 CRUD、筛选、发起评测、评测记录列表、运行详情与逐样本阶段输出展示。
- 验证：`go build ./...` 与 admin `npm run build` 均通过，数据库迁移已执行。
