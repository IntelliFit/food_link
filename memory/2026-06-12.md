# 2026-06-12

- 本地开发首页提示“获取首页数据失败”。按依赖顺序排查后确认后端 `/api/health` 在 `http://127.0.0.1:3010` 返回 200，后端本身已启动；`mrc where --port 3001/9420` 均连接失败，微信开发者工具自动化未开启，无法截图/读运行时日志。
- 根因高度疑似小程序 API 环境注入缺失：仓库根目录存在 `.env.production`，但缺少 `.env.development`；当前代码按 `miniProgram.envVersion=develop` 选择 `TARO_APP_API_BASE_URL_DEVELOP`，缺失时 `resolveApiBaseUrl()` 会返回空字符串，首页请求无法打到本地后端。
- 已补回本地 `.env.development`，内容与本地开发约定一致：`develop -> http://127.0.0.1:3010`，`trial -> https://dev.api.healthymax.cn`，`release -> https://api.healthymax.cn`。需要用户重启 `npm run dev:weapp` 或让现有 watch 重新加载配置后再刷新开发者工具。
- 继续排查发现 `config/index.ts` 原本只读 `process.env`，没有显式加载 `.env.development/.env.production`；因此即使执行 `npm run build:weapp`，也可能继续把 API URL 编译为空。已修改 Taro config，内置轻量 env loader：先读 `.env`，再按 `NODE_ENV` 读 `.env.development` 或 `.env.production`，且不覆盖已有 shell 环境变量。
- 修改后 `dist/common.js` 已出现正确注入值：`release=https://api.healthymax.cn`、`trial=https://dev.api.healthymax.cn`、`develop=http://127.0.0.1:3010`。但随后本机 `http://127.0.0.1:3010/api/health` 连接失败，`3010` 无监听，说明当前后端进程已不在；`build:weapp` 只编译前端，不会启动后端。本地开发需要用户启动 `npm run dev:backend`，小程序侧启动/保持 `npm run dev:weapp`。

- 用户提出希望把分散的管理后台、临时后台、测试集管理、行为统计、用户反馈等整合成统一后台。当前盘点：`admin/` 已有独立 Vite Admin 与反馈页，`/snack-admin` 是后端同源零食临时后台，`/test-backend` 是测试后台；建议以 `admin/` 为长期统一 Admin Console，逐步迁入零食库、反馈、测试集/benchmark、质量审计与行为统计。
- 用户确认“操作吧”后，已完成统一管理后台第一阶段：`admin/` 改为多模块 Admin Console，包含总览、意见反馈、包装食品库、质量审计、测试后台、行为统计；包装食品库迁入旧 `/snack-admin` 的搜索、筛选、图片预览和编辑能力；质量审计/测试后台先保留统一入口。`cd admin && npm run build` 已通过。

- 用户反馈“今天吃什么”推荐弹层本地可滑动、线上只能看到前几个推荐。定位到前端全量渲染 `recommendations`，后端也要求返回 5 个，问题更像线上运行时滚动容器被祖先 `catchMove` 或高度计算拦住。已修改：`DietRecommendationSheet` 根节点移除 `catchMove`，遮罩层保留；首页 `PageMeta` 在弹层打开时锁背景滚动；`.diet-rec-body` 改为 `flex:1/min-height:0/height:0` 并固定 header/handle 不收缩。`eslint` 与 `git diff --check` 通过；`mrc where --port 3001/9420` 仍连接失败，未能做 weapp-devtools 截图/滑动验证。

- 已按用户要求启动本地统一管理台：后端 `3010` 原本已运行且 `/api/health` 返回 200；新增 `admin/.env.development` 指向本地后端；Admin Vite 已后台运行在 `http://127.0.0.1:5173/`，日志为 `admin-dev.log` / `admin-dev.err.log`。

- 已按用户要求重置本地管理后台管理员账号 `admin`，并通过本地 `/api/admin/login` 验证成功；临时密码未写入 memory。
- 体验版上传再次失败：微信开发者工具报 `main package source size 5010KB exceed max limit 2048KB`。已按构建依赖排查，确认不是微信限制变化，而是静态资源复制范围回退导致主包重新带入大图和无用资源。已修复 copy 白名单、压缩餐厅图并放入 `packageExtra`、移除首页主包大图引用、清理 tabbar/png/重复字体。`npm run build:weapp:preview` 通过，main `1969.8KB`，packageExtra `1898.2KB`；weapp-devtools 自动化因 3001/9420 未开启连接失败，未截图验证。
