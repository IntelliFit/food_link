# 2026-08-05

- 用户要求将开发分支提交并推送到 GitHub，再合并到主分支，最终保持 `dev` 与 `main` 完全同步。
- 提交范围只包含应纳入仓库的项目记录；根目录 `project.config.json`、`project.private.config.json` 与 `.codex-remote-attachments/` 属于本地配置或附件，不得提交。
- 同步前需先获取远端更新并验证分支关系；合并提交使用清晰的中文 Conventional Commit 信息。


## 校园食堂主内容从第二屏开始的最终修复

- PR #62 上线后用户仍需下滑。重新排查确认真正原因是新层级页把 `AdminSidebar` 与 `<main>` 作为上下两个普通块级元素，侧栏高度接近 `100vh`，所以主内容天然排到下一屏；启动层并非最终根因。
- 页面已改为 `256px + 自适应主列` 的同屏 Grid，删除主内容 `ml-[260px]`。提交 `f3eb63a9`，PR #65 merge `9d25b471`，Cloudflare 两项 dev 部署成功。
- 不可变线上 bundle `index-veKwzCEJ.js` 已确认旧 margin 类不存在、新两栏类存在；dev 别名缓存也已切换。用户浏览器原先是本地 `file://` 地址，已引导使用正确 dev URL。

- 18:xx 宠物“常用形象”本地新增华佗与太极小子：从用户角色卡截图提取透明 384×384 素材，接入既有 `PetAvatar` 和 `select-appearance` 持久化链路，候选版本升至 4，常用形象改三列。TypeScript、ESLint、78 条 Jest、宠物 Go 服务测试通过。微信开发者工具当前未开放 automator WebSocket，运行时截图/点击验证 blocked；未提交、未推送、未部署。

## 管理端校园食堂顶部空白修复并合入 dev

- 截图中的大块空白来自此前防白屏启动层与 React 页面并存：启动层占 `100vh`，真实页面被推到下一屏。
- 修复为 React 挂载前清空启动 DOM，同时把启动层设为 fixed 覆盖层。提交 `258ffd9d`，dev merge `faa96beb`，PR #62；Cloudflare 两项部署成功，线上资源回读包含修复。
- 生产构建和 diff check 通过。自动浏览器被安全策略阻止打开 Cloudflare 临时预览域名，未取得运行时截图，需用户刷新 dev 管理端确认首屏。

- 校园菜品 AI 历史回填续查：将一次性启动回填改为启动 5 秒后执行并每 10 分钟重试，提交 `e5ebbdbc`、PR #61 合入 `dev`（merge `6a66be19`）；新 dev 镜像 digest 为 `sha256:cd6af23510d019a6c6f8488c7a6732de56de2563d11bc5b7b00da6ba1e34bec4`。16:24 只读复查仍有 1 条“柠檬金汤大锅（酸辣）”published、无任务、0 营养，证明仅推送镜像不等于集群已使用新代码。当前需用户在已登录的 `root@VM-24-14-ubuntu` OrcaTerm 中通过 ubuntu kubeconfig 将 `foodlink-dev` Deployment 固定到新 digest；本机 SSH 公钥不被服务器接受，无法代执行 rollout。
- 用户随后完成不可变 digest rollout，新 ReplicaSet `foodlink-dev-9f5f5cf85` 三副本 2/2 Running；但 16:39 数据审计仍有相同 1 条营养异常。已在本机拉取该 digest 并从后端二进制确认包含持续回填日志字符串，排除镜像内容错误。下一步需收集新 Pod 回填日志定位运行时失败或 dev/local Apollo 数据环境差异，当前不能宣称 AI 全链路完成。
- 后续日志显示三个 Pod 仅记录“维护已启动”，无扫描/提交/失败；Apollo dev 与 local 都指向同一数据库，线上配置直接调用仓库查询可稳定命中 1 条，确认不是数据环境或 SQL 问题。已将首次执行改为协程启动后立即调用，回归测试由旧实现 1 秒超时变为通过，并新增 `candidate_count` 日志。提交 `63e3d757`，PR #63 检查通过；因 GitHub merge API TLS 连续超时，合并最新 dev 后按授权直接推送 `dev@e145e869`。新镜像 digest `sha256:a01f43643840e597403767db3366c0cd34dbda29de4172d8ddfa2e00c99e32f1`，等待最后一次集群 rollout 与数据验收。
- 用户完成 `sha256:a01f4364...` rollout 后，17:15 数据仍无变化。当前需要新 Pod 的 `扫描完成 candidate_count`、内部用户解析和任务提交日志继续定位；任务仍未完成，停止继续用部署替代诊断。
- 17:11 后新 Pod 日志表明三个副本均扫描到 `candidate_count=1`，但没有提交任务。查询数据库约束后确认根因：`public_food_library.status` 不接受代码写入的 `analysis_pending`，合法值包含 `pending`；因此回填在复用原 AI API 前已失败。
- 已把客户端隐藏状态改为合法 `pending`，目录状态机及 `SubmitInternalAnalyzeTask(strict_separate)` 保持不变。提交 `239321b9`，PR #64 merge `8e222bc1`；Cloudflare 检查、目标 Go 测试、repo 编译和 diff check 通过。
- 最新 dev linux/amd64 镜像 digest 为 `sha256:c06cf364d85a10c4a2cb514487f2518a1f2a2cb907889d2e8916a0a60205e4eb`。仍需服务器 rollout 与数据验收，不能只凭镜像推送宣称 AI 回填完成。
- 用户完成该 digest rollout，新 RS `foodlink-dev-799469df9f` 三副本正常。开发库精确回查：目标菜品已生成原 AI 任务 `a82ad02c-8f42-4d30-bc60-9440169e673c`，但上游 AI 返回临时服务端错误；目录为 `analysis_failed`，客户端记录安全保持 `pending`，营养为 0。说明提交路径已修好且失败隔离正确，但最终营养发布尚未完成，需要在管理端用既有重试按钮再次提交并验收。
- 用户随后在管理端编辑显示为“荔园食堂”的食堂仍遇到 500。截图字段直接在 dev 数据库回滚事务中可正常 Updates，故不是这些可见字段本身；截图无法确定真实记录 ID。下一步需用户从新 Kubernetes Pod 导出“管理员保存校园目录失败”日志中的 `resource_id` 与 PostgreSQL 错误，再按真实请求建立红/绿回放并修复。

- 校园目录“冠军餐厅”保存 500 已定位、修复并部署 dev：开发库回滚事务复现为历史 JSONB `aliases` 空值被写成 SQL NULL、违反 NOT NULL；前端现只提交可编辑字段，后端兼容校区/食堂/窗口的 null/空 JSON 数组。相同真实数据回放由 500 变为 200，事务未落库。提交 `5caa7e73`，PR #60 已合入 `dev`（merge `efa1f4a9`），Cloudflare 合并部署成功；后端镜像 digest `sha256:80d2313ec2fac092ea9c5093f1b5da6d10a0147dc4f8d5ab98f670630bb19cfd` 已推送并由开发服务器新实例 `foodlink-dev-9bb7bb7b6-lttdv` 启动，健康检查 200。

- 用户反馈 dev 校园食堂页完全空白。分层排查先复现纯白画面，随后确认控制台无业务错误、API/登录正常，页面能回读 3196 所学校并进入学校→校区→食堂；根因是冷启动时 HTML 根节点为空，bundle 与会话检查前没有任何可视壳。已增加原生明暗主题 spinner 和 15 秒加载错误提示，提交 `fd15619b`，PR #59 merge `4b20c6c3`，Cloudflare 检查和线上 HTML 回读通过。最终浏览器看到用户已正常打开“编辑食堂”弹窗，未刷新打断其输入。

- 校园食堂管理端层级改版已完成并发布到 dev：学校成为主入口，右侧按校区→食堂→窗口→菜品逐级导航，显示层级数量、中文状态、父级自动关联和安全删除；菜品页展示价格/分量/营养/AI/客户端状态，支持单条与批量分析上线、失败重试及自动刷新。提交 `770702f8`，PR #58 merge `d7008603`。Cloudflare bundle 回读到新版文案；dev API 健康 200，新摘要路由未登录返回 401。后端镜像 digest `sha256:50ac1b05703eb68dd72af0a0f0427293aa971f5a573f73ef7a42f7fcf22d7648`。目标测试和 Admin build 通过；API contract fixture 仍有既有重复主键阻塞，浏览器连接超时导致本轮无实际页面截图。

- 已清洗用户提供的北京高校食堂人工审核表。文件名虽称 92 所高校，但内容只是原始 653 条中的 274 条复核子集，覆盖 47 所学校；另 379 条已确认数据未包含。开发库只读去重后得到 153 条可安全更新的现有食堂记录（均有 school_id/campus_id/canteen_id，无新增），80 条因校区缺失、同名冲突、拆分/合并歧义、疑问文字或不可访问证据进入人工复核。输出工作簿位于 `outputs/campus-canteens-beijing-92-20260805/北京高校食堂_管理端清洗导入版_2026-08-05.xlsx`，5 张表均完成公式与视觉检查。本轮未写开发数据库。

- 校园菜品 AI 分析后上线已完成并合入 `dev`：功能提交 `cd75048b`，PR #57 merge commit `a4bb3617`。实现严格复用现有 `SubmitInternalAnalyzeTask + strict_separate`，没有新增 AI API；首次菜品 AI 成功前客户端不可见，旧版更新期间保留有效旧数据，失败可重试，成功事务写入营养并自动发布。
- 管理端批量 AI 上线、状态/失败原因/自动刷新、价格与分量分栏已通过构建并随 Cloudflare Pages dev 部署。开发库迁移完成；2,107 条数据事务清理 1,192 条，复查价格可自动修复异常为 0，保留 20 条真实无独立价格的套餐说明。
- 后端 dev 镜像 `ccr.ccs.tencentyun.com/littlehorse/foodlink:dev` 已推送，digest `sha256:cd87ae282d8fde9d0bac3dade0be1f0b8c67261edc7e359f7695805f0e4a1afc`。截至 13:09 服务器仍未切换新镜像：健康接口 200，但开发库仍有 1 条历史零营养 published 记录，启动回填未运行。SSH `root@coachlink.fit` 缺少被服务器接受的公钥，仓库无其他部署 webhook；需维护者拉取 dev 镜像并重启服务后再做后台→AI→客户端→今日饮食全链路验收。

- 已核对校园食堂管理端逻辑：用户收集的学校食堂名称和位置属于学校/校区/食堂目录层，不进入菜品采集层。当前管理端只能逐条创建和审核，没有 Excel/CSV 上传入口；采集批次、证据来源只是元数据。开发库只读审计为：学校 active 3196；校区 active 349、待审 123；食堂 active 1277、待审 167、inactive 12，覆盖 172 所学校；窗口 active 2、待审 10；来源 approved 1606、待审 233。active 食堂会进入小程序目录选择器，但没有已发布菜品时对应校园列表仍为空。建议用户提供包含 school_name、campus_name、canteen_name、location_text、building_or_floor、source_url、source_org 的表格，先 dry-run 去重和父级匹配，再导入开发库。

- 用户明确调整个人开发节奏：每天的零散改动先只保存在本地，不逐项提交或推送；下班后由用户明确指示，再统一整理、提交、推送到当天的汇总分支和 PR。该规则优先于自动发布偏好。

- 宠物主页本地代码已按最新要求精简：删除“为什么是它”“今日状态”“外观试验箱”，保留“常用形象”“成长进度”“离线小惊喜”“外观换装”；首卡总经验并入与等级一致的泡泡，名称箭头对话入口右对齐，首卡高度收紧。TypeScript、目标 ESLint、78 条 Jest 与 diff check 通过。用户启动 watch 后，微信开发者工具实测四个保留模块完整、三个删除区域不存在、经验泡泡为 20；点击名称箭头进入宠物对话成功，运行时异常 0。截图 `/private/tmp/foodlink-pet-home-local-top.png` 与 `/private/tmp/foodlink-pet-home-local-bottom.png`。HEAD 与远端一致，改动尚未提交或推送。

- 用户明确离线小惊喜本质仅为领取经验，因此独立卡已删除，领取按钮融合到首卡“水滴汤圆”名称下方。开发者工具实际点击“领取 +16 经验”后，总经验由 20 增至 36，成长进度同步为 36/100，按钮消失且异常 0；截图 `/private/tmp/foodlink-pet-home-reward-before.png`、`/private/tmp/foodlink-pet-home-reward-after.png`。改动继续只保存在本地。

- 宠物首卡继续按新参考图调整：等级和经验移到顶部横向经验条，实测显示“Lv.1 36/100 经验值”；头像旁旧经验泡泡删除，只保留积分。按用户要求不添加草地/装饰背景，也不添加卡片内部三点菜单。开发者工具截图 `/private/tmp/foodlink-pet-home-level-bar.png`，运行时异常 0；改动继续仅保存在本地。

- 首卡又按用户反馈改为左右两栏：宠物由 166rpx 放大至 216rpx，贴近左侧顶部占满主要空间；等级/经验条缩进右栏，文字精简为“Lv.1 36/100”，删除“经验值”。开发者工具实测头像 112×112 CSS px、经验栏 202px，无溢出和运行时异常；截图 `/private/tmp/foodlink-pet-home-large-avatar.png`。仍未提交或推送。

- Lv.1 最终从右侧经验栏移到人物左上方，复用与“积分 42”相同的白色状态泡泡；右侧只剩经验进度条和 36/100。开发者工具实测两个泡泡样式一致、经验栏不再包含等级胶囊，异常 0；截图 `/private/tmp/foodlink-pet-home-level-bubble.png`。仍仅保存在本地。

- 首卡名称与经验条已交换为名称在上、经验条在下。专属像素分身流程新增可编辑名字弹窗，默认当前名字，必填且最多 12 字；前端上传 multipart 携带 name，Go handler/service 与像素头像同时保存名称，旧客户端空 name 保留原名。开发者工具实测取名弹窗成功打开并取消，异常 0；未触发图像生成。前端 TypeScript/ESLint/78 条 Jest、Go pet 全模块与新增名称长度测试通过。截图 `/private/tmp/foodlink-pet-home-name-above-exp.png`，全部改动仍只在本地。

- 成长进度已完全融合进宠物首卡：独立成长卡删除，陪伴天数变成人物左下泡泡；首卡底部显示“升级到 Lv.2”、36/100 和全宽进度条，明确进度条用于升级。开发者工具确认成长卡不存在、泡泡和升级栏正常、异常 0；截图 `/private/tmp/foodlink-pet-home-growth-merged.png`。仍未提交或推送。

- 用户实测像素宠物取名后首卡仍为旧名，确认原因是本地前端/Go 后端的 name 链路尚未推送部署，当前 dev 旧接口忽略 multipart name；代码本身已有服务测试证明返回新名字。未做刷新即丢的前端假改名，等下班统一部署后生效。另已删除外观换装最后的“挑选外观”占位项，开发者工具确认只剩 3 个真实入口、异常 0；截图 `/private/tmp/foodlink-pet-home-outfit-three-items.png`。

- 后续进一步确认本地展示未改名的完整根因：本地 dist 虽更新，但 `.env.development` 的 develop API 指向远端 dev，且本机 3010 后端进程启动于名字保存代码之前。已把本地 develop API 改回 `http://127.0.0.1:3010`；需要重启前端 watch 与后端后才能真正走本地新代码。按进程管理规则尚未擅自重启。

- 用户授权后已运行 `npm run dev:restart`，新 Go 后端与 Taro watch 均成功启动。微信运行时为 develop，本地后端日志明确收到开发者工具从 127.0.0.1 发来的 `/api/pet/summary` 并返回 200，当前已真正走本地新后端。旧名字仍显示是因为上一次远端旧接口没有保存输入值，那次输入无法恢复；需重新输入并完成生成验证。未提交或推送。

- 用户反馈本机微信开发者工具中的 Foodlink 小程序显示异常。依赖分层排查确认 3001/3002 端口均未监听、开发者工具处于旧/失联会话；重新通过 CLI 建立 Foodlink 项目自动化会话后恢复。首页重载关键接口全为 200、运行时异常 0；“我的”页 12 个入口正常，切回首页成功。最终截图 `/private/tmp/foodlink-recovered-final.png`。本轮未改代码、未启动前端 watch 或 3010 后端；仅有 Base64 图片过大的非阻断 warning。
# 13:59 首页新增养生模式（本地）

- 按用户参考图完成首页双模式：默认既有界面为均衡模式，新增养生模式与右上切换按钮，选择会本地记忆。
- 养生模式包含热量/三大营养半环卡、今日三餐、拍照识别、节气水墨建议、宜食/运动/作息、养生音乐和八段锦；真实数据和已有入口全部复用，空餐不伪造用户记录。
- 微信开发者工具完成均衡回归、两次切换、拍照菜单、运动页导航与上下半屏截图，异常 0；静态检查和 78 条测试通过。
- 养生音乐目前没有后端或播放器，只显示“即将上线”；节气建议当前为静态内容，后续可接日期/天气服务。
- 今日改动继续只留本地，未提交、未推送。
- 14:22 追加：养生热量主卡下方增加真实活动 Banner，复用均衡模式活动配置；模式按钮去掉叶子/天平图标，改为 `⇄ + 目标模式文字`。微信开发者工具确认 Banner 可见、按钮可双向切换、异常 0。
- 14:38 追加：按用户反馈删除养生模式“今日食物记录”与三餐列表，换成体重、喝水、运动三张卡；两种模式共用同一份三卡 JSX 和原交互。最终开发者工具截图已生成，后续自动化导航回归遇到工具响应超时；代码静态检查与 78 条测试通过。
- 15:04 追加：均衡模式“剩余可摄入”主卡已去掉浅色和深色样式中的绿色边框及外发光，只保留普通中性阴影。微信开发者工具实测边框为 0、运行时异常 0，截图 `/private/tmp/foodlink-home-balanced-no-glow-final.png`；继续只保存在本地。
- 15:33 追加：养生模式删除对均衡活动 Banner 的复用，改为热量概览后展示“今日养生建议”和独立 AI 食物识别养生 Banner；状态三卡及后续功能未改。按用户纠正，节气水墨图已从左侧缩略块改为整张建议卡背景。开发者工具确认新 Banner 可打开记录菜单、旧 Swiper/独立节气节点不存在、异常 0；截图 `/private/tmp/foodlink-home-wellness-advice-background-final.png`，改动继续只在本地。
- 16:03 追加：按用户要求删除养生音乐、养生小运动两张卡及点击功能和专用样式；保留体重/喝水/运动日常记录三卡。TypeScript、ESLint、78 条 Jest、diff check 与 dist 文案扫描通过。微信开发者工具自动化本次连续出现 `pageNotFound`/响应超时，未擅自重启开发进程，也未取得新截图；改动继续只在本地。
- 17:03 追加：养生模式按参考图建立墨绿/米白/金色体系并新增动态底部导航配色；用户认为第一轮整页墨绿过深后，最终把宠物/问候和主体背景改为暖白留白，仅底部导航、AI 识别 Banner 与少量选中态保留深绿。开发者工具完成养生/均衡双向回归，异常 0；最终截图 `/private/tmp/foodlink-home-wellness-light-body-green-nav-final.png`，仍只保存在本地。
- 17:38 追加：养生热量卡复用均衡模式的 `MicrosSection`，增加“营养概览 / 展开更多”入口，展开可显示 21 项微量营养。开发者工具实测 21 项 → 收起 0 项 → 重新展开 21 项，异常 0；截图 `/private/tmp/foodlink-home-wellness-micros-expanded-final.png`。静态检查和 78 条测试通过，仍只在本地保存。
- 17:50 追加：按 UI 视觉审计删除养生日期栏的金色描边和外圈高光，改成暖白单表面、极淡森林绿边界与轻阴影。开发者工具截图 `/private/tmp/foodlink-home-wellness-date-no-gold-ring-final.png`，异常 0；改动继续只保存在本地。
- 18:04 追加：用户要求养生模式导航跨页面保持绿色。已删除自定义 TabBar 的首页路由限制，改为全局读取 `home_display_mode_v1`；开发者工具实测分析、圈子、我的三个 Tab 均保持深墨绿导航，异常 0。页面主体色系不变，只有切回均衡模式才恢复白色导航；仍只在本地保存。
- 22:45 汇总发布：用户明确要求把今天所有代码改动 PR 到 dev。已选择性暂存 21 个业务文件（首页养生模式、宠物主页、命名保存后端、内置养生形象与 QA），排除 minitest、memory、outputs、project 配置和 dist；提交 `6f922164` 已推送并更新 PR #55：`https://github.com/IntelliFit/food_link/pull/55`。PR 目标 `dev`，OPEN、CLEAN、Ready，两项 Cloudflare 检查均成功；main 未修改。

# 14:12 北京高校食堂数据导入 dev

- 从清洗工作簿的“可导入更新”区提取 153 条现有食堂更新，覆盖 27 所学校；80 条人工复核项完全排除，没有新建候选。
- 导入前完成 ID 覆盖、学校/校区层级、状态、名称冲突、空值覆盖和数据库目标校验。发现 5 条旧食堂缺少 `campus_id`，只在同学校区 ID/名称精确匹配且无名称冲突时允许补齐；已有非空校区不允许更换。
- 通过单事务写入 dev 数据库 `154.8.205.78/food-link/public`，153/153 条更新成功。字段统计：楼层 99、校区关联 5、可信度 140、位置 42、食堂名称 1、审核备注 153、证据 URL 141。未新建、未改状态、未清空字段。
- 导入后第二次独立 dry-run 复查：待修改 0，无需修改 153，证明全部回读一致且导入幂等。执行报告与复查报告已保存到 `outputs/campus-canteens-beijing-92-20260805/`。
- 数据会随 dev API 从数据库实时读取，无需代码提交或后端镜像部署；用户可在 dev 管理端“校园食堂 → 食堂”刷新搜索检查。

# 15:xx 原生 App 下载与编译环境确认

- 用户询问 GitHub 上的 Food Link App 版本、本地下载与后续改功能/界面所需编译平台。确认本机工作区已是 `IntelliFit/food_link` 完整克隆，原生源码在 `apps/mobile`，与最新 `origin/dev` 无差异，未重复克隆以避免覆盖当天未提交改动。
- App 技术栈为 Expo/React Native，版本 `3.0.2`。当前 Mac 可用 Expo Go 做日常热更新预览；本地 Android 出包还需 Android Studio/JDK/SDK，iOS 出包还需完整 Xcode，也可选 EAS 云构建。

# 18:xx 校园食堂新建保存 500 最终修复

- Kubernetes 真实请求日志确认 POST `/api/admin/campus-directory/canteens` 因 `school_canteens_confidence_level_check` 失败：缺省置信等级被写成空字符串，而数据库只接受 NULL 或 A/B/C/D。
- 用真实 Gin handler 和 dev PostgreSQL 回滚事务先复现 HTTP 500，再将业务模型置信等级改为可空指针，并在 handler 中把空白转 NULL、合法值转大写、非法值提前返回 400；同一回放变为 HTTP 200，测试事务未落库。
- 提交 `8d278850`、PR #66 已合入 `dev@64bcc3e2`；无需迁移。linux/amd64 镜像已推送，index digest `sha256:f3c374984dff3cb2f6caba16fb12030ea370a1b09fc91c5a051349420200c324`。待服务器 rollout 后由管理端重新创建“荔园食堂”确认。
- 校园菜品 AI 主链路仍待最终验收：失败任务 `a82ad02c-8f42-4d30-bc60-9440169e673c` 需在管理端重试，直至任务 done、目录/客户端 published 且营养非零。
