# 2026-08-05

- 用户要求将开发分支提交并推送到 GitHub，再合并到主分支，最终保持 `dev` 与 `main` 完全同步。
- 提交范围只包含应纳入仓库的项目记录；根目录 `project.config.json`、`project.private.config.json` 与 `.codex-remote-attachments/` 属于本地配置或附件，不得提交。
- 同步前需先获取远端更新并验证分支关系；合并提交使用清晰的中文 Conventional Commit 信息。
