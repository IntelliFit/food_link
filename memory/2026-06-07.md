# 历史 0 营养 109 正式记录与 42 任务历史重跑回填

- 用户追问为什么剩余 109 个明显非 0 历史 item 不全部改完，并指出数据库未命中时应走 DeepSeek/Gemini/当前算法重新生成营养，而不是停在模糊匹配或保存 0。
- 结论：用户判断是对的。之前只做"确定性安全库修复"是为了避免弱 fuzzy 错配，但剩余明显非 0 的历史债务应进入隔离重跑回填，而不是继续卡住。
- 已用隔离测试用户对 109 个正式 `user_food_records` suspicious item 逐个调用当前 `/api/analyze-text/submit`：
  - `submitted=109`、`resolved=109`、`applied_records=70`、`applied_items=109`。
  - 来源分布：`library_exact_canonical=75`、`library_fuzzy=26`、`deepseek_generated=6`、`library_exact_alias=2`。
- 三得利纤漾饮最后单独修复：包装 SKU `919a444b-8084-4643-9ed3-7682d51a9957` 为 `18 kcal/100g`，所以 1000g 应回填 `180 kcal`，不是 verified-zero 0 kcal；已同步关联 task。
- 额外把总体 benchmark 剩余 42 个 `analysis_tasks.result` 历史可疑 item 也重跑回填：
  - `submitted=42`、`resolved=42`、`applied_records=24`、`applied_items=42`。
  - 来源分布：`library_exact_canonical=34`、`library_fuzzy=4`、`deepseek_generated=3`、`library_exact_alias=1`。
- 工具变化：`backend/cmd/zero-nutrition-history-rerun` 支持同时处理 benchmark CSV 里的 `user_food_records` 和 `analysis_tasks`；对 `茶饮` query 归一成 `茶饮料`，首轮仍 0 时使用 Gemini fallback 明确要求按常见食物估算非 0 营养。
- 最终 benchmark：
  - records-only 30 天：`records=1139`、`meaningful_items=2863`、`suspicious_zero=0`、rate `0`、`--fail-on-threshold` 通过。
  - records+tasks 30 天：`records=1139`、`tasks=1182`、`meaningful_items=6149`、`suspicious_zero=0`、rate `0`、`--fail-on-threshold` 通过。
- 报告路径：
  - `tmp/zero-nutrition-history-rerun-all-apply.json`
  - `tmp/zero-nutrition-suntory-repair-apply.json`
  - `tmp/zero-nutrition-task-rerun-apply.json`
  - `tmp/zero-nutrition-benchmark-records-final/`
  - `tmp/zero-nutrition-benchmark-30d-final/`
- 验证：`go test ./cmd/zero-nutrition-history-rerun ./cmd/zero-nutrition-benchmark ./cmd/food-zero-nutrition-repair -count=1` passed；`go test ./internal/foodrecord/service -run "TestZeroNutritionGate" -count=1` passed；核心 repo 回归测试 passed；`git diff --check` passed，仅 LF/CRLF warning。

## 全量识别记录质量审计第一版

- 用户继续追问 109 个重跑时是否再次出现 0，并提出应复查所有识别记录，找出 0 营养之外的潜在错误类型。
- 复核 109 个正式记录最终 apply 报告：`resolved=109`、`still_zero=0`、`not_resolved=0`、`fallback=0`，最终写回时没有再出现 0。
- 中间调试阶段确实暴露过"首轮重跑仍 0"和"茶饮被无糖茶截胡"的风险；最终通过 fallback 与 `茶饮->茶饮料` query 归一解决。
- 新增只读命令 `backend/cmd/food-analysis-quality-audit`：
  - 扫描正式 `user_food_records` 与 `analysis_tasks.result`。
  - issue 类型包括 `suspicious_zero_nutrition`、`extreme_item_calories`、`extreme_energy_density`、`macro_exceeds_weight`、`negative_nutrition`、`unresolved_with_nutrition`、`missing_or_zero_weight`、`packaged_candidate_pending`、`macro_energy_mismatch`、`very_low_energy_density`。
  - 默认不输出缺 `nutrition_source`，避免历史旧字段造成 3 万条低优先级噪音；需要字段治理时再加 `--include-missing-source`。
- 全量审计发现今天新产生一条正式记录 0 营养：`香菇油麦菜 100g = 0 kcal`，记录 `14bf4de3-a3fc-4484-b8a0-f72695c194bb`，关联任务 `e1a34a2e-5f44-489d-99fe-bb8fdd679697`。
  - 已按标准库 `油麦菜` 回填：`14 kcal / P 1.4 / C 2.1 / F 0.2`，同步关联 task。
  - 这说明运行态后端可能还没加载保存门禁新代码；代码层面新增测试覆盖 `香菇油麦菜 100g` 0 营养会被拒绝。
  - 标准库 normalizer 增加 `油麦菜` 安全兜底；修复工具允许 `香菇油麦菜 -> 油麦菜` 这种低风险蔬菜基底修复。
- 修复后 30 天 0 营养 benchmark：`records=1155`、`tasks=1216`、`meaningful_items=6240`、`suspicious_zero=0`、rate `0`，万分之一 gate 通过。
- 全历史 hard issue 审计结果：`records=5148`、`tasks=6327`、`items=33809`、`issue_items=988`、`issue_rows=1003`、high `61`、medium `942`。
  - high 分布：`suspicious_zero_nutrition=32`、`extreme_item_calories=12`、`macro_exceeds_weight=9`、`extreme_energy_density=7`、`negative_nutrition=1`。
  - medium 分布：`unresolved_with_nutrition=855`、`missing_or_zero_weight=48`、`high_energy_density_review=17`、`packaged_candidate_pending=13`、`macro_energy_mismatch=5`、`very_low_energy_density=4`。
- 输出：
  - `tmp/zero-nutrition-xianggu-youmaicai-apply.json`
  - `tmp/zero-nutrition-benchmark-30d-after-xianggu-fix/`
  - `tmp/food-analysis-quality-audit-all-hard-issues-v2/`
- 验证：
  - `go test ./cmd/food-analysis-quality-audit ./cmd/food-zero-nutrition-repair ./cmd/zero-nutrition-benchmark -count=1` passed。
  - `go test ./internal/foodrecord/service -run "TestZeroNutritionGate" -count=1` passed。
  - `go test ./internal/foodrecord/repo -run "TestNutritionQueryVariants|TestIsUnsafeNutritionAliasMatch|TestIsGenericPackagedResolveQuery" -count=1` passed。
  - DB-backed repo test含新油麦菜 case 在本机 `CGO_ENABLED=0` sqlite 环境下仍会被 stub 阻塞；真实库 dry-run/apply 已验证。

## 会议转录：foodlink/食探相关摘取

- 用户提供今天会议转录，要求从中提取和当前 `food_link` / 食探相关的工作与讨论；转录中"食探"出现多种误转写，包括"实探/试探/时态/石炭/实战"。
- 已识别核心相关主题：
  - 食物识别主链路：入口包含拍照、相册、文字、手动记录、历史/收藏复用；重点仍在拍照识别。
  - 算法与数据：包装库与标准营养库分流、包装食品规格选择、标准库 exact/alias/fuzzy、DeepSeek/LLM 兜底、0 营养问题、API/配置失败排查、benchmark 与测试数据集建设。
  - 产品/运营：自有/AI 生成图片应考虑水印和版权保护；平台用户图片是否加水印需要用户协议支持；健康分析空态样式需优化；AI 对话暂缓；保健品/健康推荐执照问题待确认。
  - 数据闭环：识别不准、加减号、比例滑块、编辑行为等用户交互可作为 bad case/反馈信号；识别记录可补充行为字段，用于后续算法优化。
  - 后台：现有测试后台/临时后台建议合并为食探后台，并和意见反馈、数据查看/修正结合；后台重在可用，不追求复杂 UI。
- 本轮仅整理会议内容，没有修改业务代码。

## 打卡分享优先使用正式版

- 用户发现：如果在体验版里分析打卡，别人扫码进入的也是体验版；体验版名额有限，刚刚突然增加的 3 个体验名额很可能来自打卡被他人扫码。
- 产品/运营口径：以后分析打卡和对外分享尽量使用正式版；也应建议用户尽量使用正式版打卡和分享，否则体验版名额限制会不利于传播。
- 体验版保留给内部验收、真机预览、灰度调试和少量指定测试，不作为普通用户传播入口。
- 技术判断：官方体验版二维码本身不能变成正式版入口；但我们通过后端生成的小程序码可以传 `env_version=release`，即使用户当前运行在体验版，也可以生成指向正式版的小程序码。
- 后续建议：打卡海报、整天分享、邀请好友等用户传播二维码应固定请求 `release`，不要 fallback 到 `trial/develop`；体验版里生成分享海报时可提示"当前为体验版，请勿扩散体验版入口"。
- 已修改：`src/pages/index/components/MealRecordPosterModal.tsx`、`src/pages/index/index.tsx`、`src/packageExtra/pages/record-detail/index.tsx`、`src/packageExtra/pages/day-record/index.tsx`、`src/packageExtra/pages/invite-friends/index.tsx` 中小程序内传播二维码统一只请求 `env_version=release`，不再 fallback 到 `trial/develop`。
- 验证：相关 5 个文件 eslint 通过，`git diff --check` 通过；weapp-devtools 端口 `3001/9420` 均连接失败，未能运行时截图验证。`npm run typecheck` 仍受既有无关 TS 问题阻塞。

## 零食上传通道连续拍摄多张

- 用户反馈：零食上传通道如果有多张照片，基本只能走手机相册上传；直接手机拍摄似乎只能拍 1 张，并且默认一张建立一个任务。
- 根因定位：`src/packageExtra/pages/packaged-food-edit/index.tsx` 的多图选择使用 `sourceType: ['camera', 'album']`；微信拍照入口单次通常只返回 1 张，原逻辑没有相机连续补拍流程。
- 已修改：
  - 多图上传入口先让用户选择"从相册选择"或"连续拍摄"。
  - 相册路径仍支持一次选择最多 3 张。
  - 相机路径改成循环：每拍 1 张后询问"继续拍 / 完成"，最多 3 张。
  - 已拍至少 1 张后，如果后续相机取消，保留已拍图片并继续走同一组上传。
  - 奖励上传和识别后补充图片都复用该逻辑，提交仍只建立一个 `packaged_product_extract` 任务。
  - 页面文案改为"相册多选或连续拍摄"。
- 验证：
  - `npx eslint src/packageExtra/pages/packaged-food-edit/index.tsx --max-warnings 0` passed。
  - `git diff --check -- src/packageExtra/pages/packaged-food-edit/index.tsx` passed，仅 LF/CRLF warning。
  - `npm run typecheck -- --pretty false 2>&1 | Select-String "src/packageExtra/pages/packaged-food-edit/index.tsx"` 无本文件相关错误输出；PowerShell 因无匹配返回非零码。
  - weapp-devtools 自动化阻塞：`mrc where --port 3001` 和 `mrc where --port 9420` 均连接失败，未能截图或点击验证。

## 饭饭 0 卡事故原图复测准备

- 用户要求把饭饭 2026-06-06 午餐 0 卡事故的原图下载下来，再复测一遍，且不要破坏当时数据库状态。
- 已从正式记录 `316ccf20-a4e8-4373-af30-9dbf00cac91c` 找到原图：`https://cdn-food-images.coachlink.fit/bd4d90e4-78ca-4fd1-8e0d-116e1e1b8633.jpg`。
- 已下载到本地：`tmp/fanfan-zero-retest/original-bd4d90e4.jpg`，确认图片内容为每日坚果、如实酸奶、双汇玉米热狗肠。
- 旧错误状态保存在备份：`tmp/food_zero_nutrition_backup_316ccf20-a4e8-4373-af30-9dbf00cac91c_20260606_194548.json`，其中如实酸奶和双汇玉米热狗肠均为 0 营养。
- 当前只读 DB-first 复算已确认：正式记录已修复为 `303.67 kcal`，`如实酸奶` 能解析到标准库 `无糖酸奶`，`双汇玉米热狗肠` 能解析到包装库 40g SKU。
- 完整图片识别复测需要运行中的后端；当前 `http://127.0.0.1:3010` 未连接。按项目规则，代理未在没有明确授权时启动本地后端。

## 饭饭 0 卡事故原图复测结果

- 用户启动本地后端后，已用原始 CDN 图 `https://cdn-food-images.coachlink.fit/bd4d90e4-78ca-4fd1-8e0d-116e1e1b8633.jpg` 跑完整灰度复测。
- 为避免污染饭饭账号，临时创建隔离测试用户 `00000000-0000-0000-0000-00000000fa0f` 并给测试积分；复测后已删除该用户、积分流水和测试 `analysis_tasks`。
- `standard` 模式输出目录：`tmp/fanfan-zero-retest/gray-standard-20260607-013804`。
  - task：`66a84c8e-38ee-4a8e-b665-f2c0819f0d90`，状态 `done`。
  - 结果：3 个 item，`unresolved_like_items=0`，`missing_nutrition_items=0`。
  - 每日坚果 `165 kcal`，如实原味发酵乳/酸奶 `83.7 kcal`，双汇玉米热狗肠 `80 kcal`。
  - 酸奶命中标准库 `无糖酸奶`；热狗肠包装候选未自动确认，但回退普通库/LLM 后有非 0 营养，没有复现 0 卡。
- `fast` 模式输出目录：`tmp/fanfan-zero-retest/gray-fast-20260607-014113`。
  - task：`09b0fddb-d039-4bc4-a774-cbd799dc4cfa`，状态 `done`。
  - 结果：3 个 item，`unresolved_like_items=0`，`missing_nutrition_items=0`。
  - 每日坚果 `165 kcal`，如实原味酸奶 `83.7 kcal`，双汇玉米热狗肠 `80 kcal`。
- 复测副作用清理：`standard` 复测里 DeepSeek 兜底曾向 `food_nutrition_library` 持久化 `Member's Mark 每日坚果`（`1a72c0b0-9df0-44f6-844b-c41156eec754`）和 `双汇玉米热狗肠`（`53a75eda-c3cb-47e0-9e1d-549765f2c817`）两条普通库记录；已在复测后删除，尽量恢复数据库状态。
- 结论：当前后端在 fast/standard 两条主链路下不会再把这张图里的酸奶和热狗肠保存成 0；但包装库自动确认仍偏保守，热狗肠本次走 `candidates_only -> 普通库/LLM兜底`，后续可继续优化包装 SKU 自动确认/多规格策略。

## 0 营养风险 benchmark 与保存门禁

- 用户希望把类似饭饭酸奶/热狗肠 0 卡事故的概率压到万分之一甚至更低，要求建立 benchmark。
- 已新增只读命令 `backend/cmd/zero-nutrition-benchmark`：
  - 扫描 `user_food_records.items` 和正式食物相关 `analysis_tasks.result`。
  - 默认目标可疑率 `0.0001`，支持 `--fail-on-threshold` 作为巡检/CI gate。
  - 支持 `--since` 区分历史债务和某次修复/部署后的新结果。
  - 输出 JSON/CSV/Markdown，记录可疑 item、用户、记录/任务 ID、营养来源、原因和严重度。
- 已加后端保存门禁：分析任务保存成正式饮食记录时，若某个有名字、有重量的非真实零卡食物核心营养全 0，则 service 层拒绝保存并提示"营养信息缺失，请先重新识别或手动补充热量"。白开水、纯净水、无糖茶、黑咖啡、美式、无糖可乐等明确真实零卡例外放行。
- benchmark 结果：
  - 30 天历史：`meaningful_items=6190`，`suspicious_zero=189`，可疑率 `0.03053312`，未达万分之一目标；这是历史债务，说明饭饭不是孤例。
  - 近 2 天：`meaningful_items=474`，`suspicious_zero=7`，可疑率 `0.01476793`，未达目标；可疑样本主要集中在 2026-06-05 至 2026-06-06 修复前。
  - 2026-06-06 20:00 之后：`meaningful_items=26`，`suspicious_zero=0`，可疑率 `0`，`--fail-on-threshold` 通过；样本量仍小，不能宣称统计上已证明万分之一，但当前新窗口没有复现。
- 输出报告：
  - `tmp/zero-nutrition-benchmark-30d-current-v2/`
  - `tmp/zero-nutrition-benchmark-2d-current-v2/`
  - `tmp/zero-nutrition-benchmark-since-fix-gated-v2/`
- 验证：
  - `go test ./cmd/zero-nutrition-benchmark -count=1` passed。
  - `go test ./internal/foodrecord/service -run "TestZeroNutritionGate" -count=1` passed。
  - service 全包 DB 型测试本机仍受 `CGO_ENABLED=0` / sqlite stub 阻塞。
  - 本轮没有前端 UI 改动，未触发 weapp-devtools。

## 历史正式饮食记录 0 营养二次确认与安全回填

- 用户要求二次确认历史饮食记录中 0 营养问题：哪些是真没问题，哪些按食物常识明显有问题；对能安全修的直接改，再重新跑 benchmark。
- 已按数据依赖优先排查：records-only 30 天正式饮食记录修复前为 `records=1141`、`meaningful_items=2871`、`suspicious_zero=129`、rate `0.04493208`。
- 分类判断：
  - `温水` 是真实 0/可接受，已加入 benchmark 与保存门禁白名单。
  - `三得利纤漾饮荷叶茉莉花味` 可能是无糖/低能饮料，但历史 item 没有营养标签证据，暂不自动放行为 verified-zero。
  - 其它大部分按常识明显不应为 0，包括水果、肉菜、主食、甜品、饮料和复合菜；但很多不能靠相似库名安全回填。
- 已安全回填 15 条记录 / 18 个 item，并同步关联 `analysis_tasks.result`：清蒸鱼片、清炒佛手瓜、炒小白菜、牛奶饮品、兰州牛肉凉面、冰拿铁咖啡、蓝莓紫米爆珠益生菌风味发酵乳、果冻、鸡蛋（煮）、桃子、酱鸭、红薯块、海苔片、五香猪心、老冰棍棒冰、豆角炒土豆丁、炒杂菜、炸肉条。
- 修复工具强化：
  - 新增 `--from-benchmark-csv`、`--skip-names`、`--output-json`。
  - 按历史 `intake/ratio` 计算总热量，避免把用户只吃一部分的 item 算成整份。
  - 标准库 fuzzy 只允许安全同义/包含关系；拒绝 `山竹->冻干山竹`、`麻辣豆腐->麻辣豆腐烧肉`、`青椒豆腐（裹鸡蛋液）->鸡蛋`、`西兰花与花椰菜->西兰花` 等危险映射。
- 复测：
  - 修复后 records-only：`records=1140`、`meaningful_items=2865`、`suspicious_zero=110`、rate `0.03839442`。
  - 修复后 30 天总体：`records=1140`、`tasks=1184`、`meaningful_items=6162`、`suspicious_zero=173`、rate `0.02807530`。
  - 2026-06-06 20:00 之后新窗口：`meaningful_items=26`、`suspicious_zero=0`，`--fail-on-threshold` 通过。
  - post-apply dry-run：`changed_records=0`、`changed_items=0`，说明安全确定性自动修复已用尽。
- 输出文件：
  - `tmp/zero-nutrition-history-repair-dryrun-safe-v2.json`
  - `tmp/zero-nutrition-history-repair-apply-safe-v2.json`
  - `tmp/zero-nutrition-history-repair-postapply-dryrun.json`
  - `tmp/zero-nutrition-benchmark-records-after-history-repair-v2/`
  - `tmp/zero-nutrition-benchmark-30d-after-history-repair-v2/`
  - `tmp/zero-nutrition-benchmark-since-fix-after-history-repair-v2/`
  - `tmp/zero-nutrition-history-remaining-records-inspect.json`
- 验证：`go test ./cmd/food-zero-nutrition-repair ./cmd/zero-nutrition-benchmark -count=1` passed；`go test ./internal/foodrecord/service -run "TestZeroNutritionGate" -count=1` passed；`git diff --check` 通过，仅 LF/CRLF warning。
- 剩余：109 个正式记录 item 按常识明显不应为 0，但不能靠相似库名安全自动修。剩余 71 条记录中 67 条有 `image_path`，71 条都有 `source_task_id`；下一步可以批量拉原图用隔离测试用户重跑识别，或进入人工纠错队列。
