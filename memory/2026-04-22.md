# 2026-04-22

- 完成对象存储切换首轮代码改造：后端上传/删除、健康报告 OCR、压缩与巡检脚本统一切到腾讯云 COS。
- 当前口径：
  - `food-images` -> `https://cdn-images.coachlink.fit/<key>`
  - `user-avatars` -> `https://cdn-food-user-avatars.coachlink.fit/<key>`
  - `icon` -> `https://cdn-food-icon.coachlink.fit/<key>`
  - `health-reports` -> 私有 key + 短期签名 URL
- 用户进一步确认：`health-reports` 的私有属性不再由环境变量控制，直接在代码里写死为私有桶。
- 健康报告上传接口现返回 `imageUrl + storageKey`；前端保存预览 URL 展示，并优先把 `storageKey` 提交给后台异步 OCR，避免使用会过期的临时 URL。
- 兼容要求已固化到代码：旧 Supabase URL、新 CDN URL、原始 key 都能解析为对象 key，用于删除、压缩、清理与 OCR。
- 尚未做的是真实业务联调：需要用户在当前线上环境验证新上传图片、旧图回显、后台 OCR、海报加载与任务删除清理。
