# 2026-04-30

- 本地后端启动时出现：
  - `ModuleNotFoundError: No module named 'opentelemetry'`
  - 影响范围包括 `backend/main.py`、`backend/database.py`、`backend/worker.py` 和 `run_backend.py` 拉起的多 worker 进程
- 根因确认：
  - OTel 相关 import 被直接写成硬依赖
  - 当前本地 Anaconda Python 环境未安装 `opentelemetry-*` 依赖
- 已修复方式：
  - 新增 `backend/otel_compat.py` 作为可选依赖兼容层
  - `main.py` 与 `database.py` 改为统一从该兼容层导入 OTel 相关符号
  - 未安装 OTel 时自动降级为 no-op，并打印：
    - `OpenTelemetry packages are not installed in the current environment; observability is disabled.`
- 校验：
  - `python -m py_compile backend\\otel_compat.py backend\\database.py backend\\main.py backend\\worker.py` 通过
  - 进程内 `import main` 成功，不再因 OTel 缺失崩溃
- 用户继续在本地真机/本地后端联调时发现：
  - worker 持续报：`'_NoOpSpan' object has no attribute 'set_attribute'`
  - `GET /api/home/dashboard`、`GET /api/stats/summary` 也因此返回 `500`
  - 前端错误弹窗复制结果只有 `no-trace-id`
- 当前判断：
  - 这是 `backend/otel_compat.py` 的 no-op 兼容层还没补完整，不是首页/统计业务本身先坏了
  - 本地环境未安装真实 OTel 依赖时，代码会退回 `_NoOpSpan`
  - 但 `_NoOpSpan` 目前缺 `set_attribute(...)`
  - 而 `backend/database.py` 中很多 tracing 代码直接在 no-op span 上调用 `set_attribute(...)`
  - 结果是“为了兼容无 OTel 环境而写的 no-op span”反而把 worker、首页、统计等接口打挂
  - 由于没有真实有效 span，上层响应头也不会注入 `x-trace-id`，前端最终只能复制到 `no-trace-id`
- 已补修：
  - `backend/otel_compat.py` 的 `_NoOpSpan` 现补齐：
    - `set_attribute(...)`
    - `set_attributes(...)`
    - `is_recording()`
  - `python -m py_compile backend\\otel_compat.py backend\\database.py backend\\main.py backend\\worker.py` 通过
  - 进程内最小 no-op span 调用验证通过，不再因 `set_attribute` 缺失而崩
- 当前剩余动作：
  - 需要重启本地后端进程，让修复后的兼容层生效

- 用户随后要求不要只走 no-op 降级，直接把真实 OpenTelemetry 依赖装进本地后端环境。
- 已安装到当前后端实际使用的解释器：
  - `D:\software\anaconda\python.exe`
  - 包版本：
    - `opentelemetry-api==1.33.1`
    - `opentelemetry-sdk==1.33.1`
    - `opentelemetry-exporter-otlp-proto-http==1.33.1`
    - `opentelemetry-instrumentation-fastapi==0.54b1`
    - `opentelemetry-instrumentation-httpx==0.54b1`
    - `opentelemetry-instrumentation-logging==0.54b1`
- 校验：
  - `OTEL_AVAILABLE=True`
  - `import main` 成功
  - `OpenTelemetry logs exporter enabled, endpoint=http://localhost:4318/v1/logs, service=food-link-backend`
- 新结论：
  - 现在“缺 OTel 依赖”这件事已经解决
  - 后续若还看到 `ConnectionRefused`，说明是本机 `localhost:4318` 没有 collector / 没有开 SSH 隧道，不是 Python 包没装
  - 具体表现是：
    - `opentelemetry.exporter.otlp.proto.http._log_exporter`
    - 向 `http://localhost:4318/v1/logs` 发请求时报 `WinError 10061`
  - 这表示“日志/trace 上报目标没人监听”，不是业务接口本身抛出的应用异常
  - 用户补充的可观测使用手册进一步确认了这一点：
    - 本地开发若启用 `OTEL_ENABLED=1` 且 `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`
    - 就必须保持 SSH 隧道：
      - `ssh -N -L 4318:127.0.0.1:30318 ubuntu@154.8.205.78`
    - 否则本地 `4318` 端口无人监听，OTel exporter 就会持续报 `ConnectionRefused`

- 用户明确要求把发布版本号改为 `2.1.1`，并确保「我的」页底部显示的是真实构建生效值，不接受只改表面文案。
- 已按项目发版规则执行：
  - `npm version 2.1.1 --no-git-tag-version`
  - 确认 `package.json` 与 `package-lock.json` 根级版本都已同步为 `2.1.1`
  - 确认 `config/index.ts` 继续从根目录 `package.json` 读取版本并注入 `__APP_VERSION__`
  - 确认 `src/pages/profile/index.tsx` 版本展示继续使用 `版本号 v${__APP_VERSION__}`
- 结论：这次版本号变更属于“源头版本正确 + 页面展示链路本来就正确”，不需要手改 profile 页硬编码字符串。

- 用户要求后端先接 OpenTelemetry，目标包含三件事：
  - 关键链路可上报 trace
  - 日志可通过 OTel 上报
  - 把 trace id 回传到 HTTP 响应头，便于前端反馈问题时快速定位
- 已在 `backend/main.py` 落地：
  - OTel trace：`TracerProvider + OTLPSpanExporter + FastAPIInstrumentor + HTTPXClientInstrumentor`
  - OTel logs：`LoggerProvider + OTLPLogExporter + LoggingHandler`
  - 响应头注入：`x-trace-id`、`traceparent`，并新增 `x-instance-id`
- 实例标识头规则：
  - 默认响应头名：`x-instance-id`（可用 `INSTANCE_HEADER_NAME` 覆盖）
  - 值优先取 `POD_NAME`，回退 `HOSTNAME`，最后兜底 `unknown-instance`
  - 可通过 `INSTANCE_HEADER_ENABLED` 开关控制
- 已在 `backend/.env` 增加默认配置：
  - `OTEL_ENABLED=1`
  - `OTEL_LOGS_ENABLED=1`
  - `OTEL_SERVICE_NAME=food-link-backend`
  - `OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector.observability.svc.cluster.local:4318`
  - `INSTANCE_HEADER_ENABLED=1`
  - `INSTANCE_HEADER_NAME=x-instance-id`
- 已在 `backend/requirements.txt` 增加 OTel 依赖；`python -m py_compile backend/main.py` 校验通过。

- 为排查前端“网络错误，稍后重试”补充了数据库侧 trace 埋点：
  - 文件：`backend/database.py`
  - 新增 `_safe_add_span_event` / `_record_db_exception`，把数据库异常作为 span event + exception 挂到当前 trace
  - `claim_next_pending_task_sync` 现会明确记录 `db.claim.empty / db.claim.success / db.claim.lost_race / db.error`
  - `create_analysis_task_sync`、`get_supabase_client`、`check_supabase_configured` 也补了关键事件
  - 语法与静态检查通过（`py_compile` + lints）

- 同日继续扩展埋点覆盖面到高频读写链路（仍在 `backend/database.py`）：
  - 记录查询：`list_food_records`、`list_food_records_by_range`
  - 洞察缓存：`get_cached_insight`、`get_latest_cached_insight`、`upsert_insight_cache`
  - 任务查询：`get_analysis_task_by_id_sync`、`get_analysis_tasks_by_ids`、`list_analysis_tasks_by_user_sync`
  - 今日计数：`get_today_food_analysis_count`、`get_today_exercise_log_count`
  - 统一事件关键词可在 Jaeger 搜索：`db.query.success / db.cache.hit / db.cache.miss / db.count.success / db.error / db.table_not_ready`

- 按用户要求继续补了业务层（`backend/main.py`）埋点，不只看数据库：
  - 新增通用业务埋点 helper：`_trace_add_event`、`_trace_record_error`
  - 在以下接口增加业务阶段事件与异常挂载：
    - `/api/analyze/submit`（收到请求、参数校验失败、strict 分支、任务创建成功、提交失败）
    - `/api/analyze/tasks/{task_id}`（请求、not_found、forbidden、success、unknown error）
    - `/api/food-record/save`（请求、成功、后台刷新洞察失败、保存失败）
    - `/api/food-record/list`（请求、成功条数、失败）
    - `/api/food-record/{record_id}`（请求、not_found、forbidden、success、失败）
  - 语法与静态检查通过：`py_compile` + lints

- 小程序错误处理现状与收口进展：
  - 现状确认：前端错误提示目前并非统一入口，存在大量页面内 `Taro.showToast(...e.message...)` 分散处理
  - 已在 `src/utils/api.ts` 新增统一错误基座：
    - `showUnifiedApiError(error, fallback)`（统一弹窗 + 复制 traceId）
    - `getTraceIdFromError(error)`（从 error 对象/消息提取）
    - 升级 `throwHttpErrorWithStatus` 支持从响应头提取 `x-trace-id`，并把 traceId 拼进用户可见文案
  - 已先接入高频接口（登录、上传/分析、会员套餐、authenticatedRequest 非 2xx）
  - 已先把保质期页面切到统一弹窗：
    - `src/packageExtra/pages/expiry/index.tsx`
    - `src/packageExtra/pages/expiry-edit/index.tsx`
  - 后续可继续将其它页面分散 `showToast(e.message)` 收口到 `showUnifiedApiError`

- 继续按用户反馈修正统一错误弹窗硬约束（2026-04-30 凌晨）：
  - `showUnifiedApiError` 现在改为“必须点击确认”的单按钮弹窗（`showCancel=false`）
  - 确认按钮固定文案：`复制traceId并确认`
  - 有 `traceId` 时复制真实值；无 `traceId` 时复制占位值 `no-trace-id`（避免没有复制入口）
  - 取消“showModal 失败降级 toast”的路径；首次弹窗异常时改为第二次极简弹窗重试，仍要求用户确认并提供复制动作
  - 修复微信弹窗参数限制：`showModal.confirmText` 不能超过 4 个中文字符，统一改为 `复制`，解决“转圈后无反应 + showModal:fail confirmText length should not larger than 4 Chinese characters”

- 同日继续修正登录链路漏网点：
  - `src/packageExtra/pages/login/index.tsx` 中登录失败、绑定手机号失败从 `showToast(error.message)` 改为 `showUnifiedApiError(...)`
  - `src/utils/api.ts` 的 `login()` catch 改为优先透传已包装错误（保留 traceId），并把底层网络错误文案收口为用户可读短文案，避免把后端原始 JSON 直接展示给用户
  - 继续补齐登录页其余错误出口：头像上传失败、资料保存失败均改为 `showUnifiedApiError(...)`
  - `parseFastApiDetail()` 新增“嵌套 JSON detail 提取 message”逻辑，避免把 Supabase/PostgREST 原始 JSON 整串展示给用户
  - 修复登录页“转圈后无弹窗”的回归：`showLoginErrorModal` 改为双层兜底（完整弹窗失败后立刻走极简弹窗），并加 `setTimeout(0)` 避免同帧状态更新导致弹窗偶发不弹出

- 为避免仍有页面遗漏 `showToast(error...)`，新增全局失败 toast 兜底（`src/app.ts`）：
  - 启动时安装 `Taro.showToast` 拦截器
  - 命中失败关键词（失败/错误/异常/重试/超时/无效/无法）且非 success icon 的 toast，不再弹黑色提示，统一改为“请求失败”确认弹窗
  - 弹窗确认按钮固定“复制traceId并确认”，若文本无 traceId 则复制 `no-trace-id`
  - 后续回滚：该全局拦截在微信运行时触发 `showToast` 递归（`Maximum call stack size exceeded`），已从 `src/app.ts` 完全移除，改回页面内显式错误弹窗策略

- 同日继续按用户要求执行“静态替换而非拦截”：
  - 已把 `src` 下高频页面中“失败类 `showToast`”大范围改为 `showUnifiedApiError(...)`（社区、首页、分析历史、食物库、登录相关页、记录详情/分享、会员、健康档案等）
  - `rg` 结果确认：`title: ...message... + icon:'none'` 的失败 toast 已清零
  - 目前仅保留登录页“复制失败，请手动记录”toast，作为复制动作反馈，不属于 API 失败提示链路
