# 2026-04-07

- 清理了旧食物保质期链路：删除 `pages/food-expiry/*`、前端旧 `/api/food-expiry/*` 封装、后端旧 `/api/food-expiry/*` 路由、`user_food_expiry_items` 数据访问与建表 SQL。
- 首页“快到期食物”改为复用新表 `food_expiry_items` 的摘要数据，入口统一跳转到 `pages/expiry/index`。
- `pages/expiry/index` 与 `pages/expiry-edit/index` 在状态变更/保存后会触发 `FOOD_EXPIRY_CHANGED_EVENT`，首页返回后可刷新保质期摘要。
- 本轮执行了 `npm run dev:weapp`，编译通过；未做微信开发者工具截图验证，阻塞在自动化握手未恢复。
- 定位到微信订阅消息发送报错 `argument invalid! data.character_string5.value` 的直接原因：后端把中文/空白 `quantity_note`（如 `未填写`）塞进了模板的 `character_string5`。
- 已在 `backend/main.py` 和 `backend/worker.py` 增加 `character_string` 字段清洗；新建任务写入快照时会转成 ASCII 安全值，旧队列快照在 worker 实际发送前也会再兜底清洗，默认降级值为 `NA`。
- 圈子页 `pages/community`：去掉「健康圈子」标题区；好友区入口去掉「>」；排行榜右侧改为圆形半透明底 + SVG 箭头；动态搜索框用放大镜 SVG；页面背景渐变对齐首页。本地 `mrc screenshot` 仍因 9420 未连接失败，需开发者工具开启自动化后补截图。
- `pages/analyze-loading` 已从 `5e7eee0` 恢复（全屏底图、`loading-bottom-readability`、640 取景框与扫描线等）；`pages/result` 顶部去掉取景框与图上文案，仅全幅图 + 底黑渐变，健康贴士仍在下方卡片区。
- 首页日期切换：`mrc relaunch` 后截图会卡住；用 `/Applications/wechatwebdevtools.app/Contents/MacOS/cli auto --project <repo> --auto-port 9420` 重连自动化后 `mrc screenshot` 恢复。已在 `debug/weapp-verify/` 存 04–08：连接后首页、点第 2/5 个日期、切「分析」再回「首页」。
- 记运动：`POST /api/exercise-logs` 改为表单后，本地 `curl` 无 Token 返回 **401**（非 422），说明 body 已解析；`mrc` 跑通记运动页输入爬坡样例并截图 `debug/mrc-exercise-verify/`，模拟器未登录故界面仍为 0 条记录。
