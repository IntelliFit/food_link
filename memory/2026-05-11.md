# 2026-05-11

## Backend line count snapshot
- User asked how many lines of backend code there are in total.
- Counted git-tracked files under `backend/`, excluding untracked/cache/temp files.
- Physical line counts:
  - Python files: 73 files, 37908 lines.
  - Production Python excluding `backend/tests/` and `backend/test_backend/`: 50 files, 33066 lines.
  - Source-like backend files (`.py`, `.sql`, `.mjs`, `.js`, `.css`, `.html`, `.sh`, `Dockerfile`): 158 files, 45165 lines.
  - All git-tracked text files under `backend/`: 179 files, 95497 lines.
  - Skipped binary files: 3 test images under `backend/tests/test_images/`.
- No code changes were made for the count.

## Go backend branch analysis
- User said there is a Go backend branch and asked to analyze it.
- After `git fetch --all --prune`, no branch literally named `go` existed; the Go backend was found on `origin/backend-refactor-sync-migrate-tencent`.
- Static line counts from that branch:
  - Go files: 212 files, 46893 lines.
  - Production Go excluding `_test.go`: 117 files, 29581 lines.
  - Go tests: 95 files, 17312 lines.
  - All backend text files: 446 files, 68571 lines.
- Architecture snapshot:
  - Gin server under `cmd/server` / `internal/app`.
  - Worker under `cmd/worker` / `internal/worker`.
  - Business modules use `domain/handler/service/repo` style under `internal/`.
  - Config uses Viper and binds many legacy Python env names.
  - Dockerfile builds server and worker binaries with Go 1.26.1 image and runs from scratch.
- Route snapshot:
  - 147 direct Gin routes found.
  - `docs/backend-api-prd/ROUTE_MAP.md` has 143 specs.
  - Static comparison showed route-map coverage is effectively explicit; extras include poster calorie compare, comment delete, diet recommendations, and public-food delete.
- Verification blocker:
  - Local environment has no `go` executable, so compile/tests could not be run.
  - Temporary worktree `D:\projects\food_link_go_analysis` was created for analysis and removed afterward.
