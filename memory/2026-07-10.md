# 2026-07-10

- 用户咨询：Apollo 中维护了多家大模型 key，现有新的算力服务中心，想把项目里的 API key/请求地址都切过去，确认是只改环境变量还是必须改代码。
- 已读新平台接入说明：其提供两类协议
  - OpenAI 兼容：`https://maas-openapi.wanjiedata.com/api`
  - Anthropic 兼容：`https://maas-openapi.wanjiedata.com/api/anthropic`
- 代码排查结论：
  - `food_link` 当前主链路大多走 OpenAI 兼容接口，很多 provider 已支持 Apollo 配置 `api_key + base_url + model`。
  - 但并非全量都可只改配置；`DeepSeek` 文本链路多个位置仍写死 `https://api.deepseek.com` 或以内置默认值回退。
  - 当前仓库未见正式 Anthropic `/v1/messages` 客户端；若要接 Claude 协议，需要改代码，不是只换 key/base url。
  - 若新平台用于承接 OpenAI-compatible 流量，则 Doubao / Ofox / Gemini35 / DashScope 类链路大概率可只改 Apollo；若要“全部统一迁移”，需补 `deepseek_base_url` 配置化并清理硬编码 URL。
