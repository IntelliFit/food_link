# 2026-07-24

## 快速同步 main/dev

- 用户要求再次快速提交当前有效修改，并让本地与远端 `main`、`dev` 保持同一提交。
- 本次本地范围包含会员页强化“微信小程序虚拟支付”与 iOS Apple 支付识别，以及 React Native Gradle 插件 Foojay resolver 兼容补丁；远端 `dev` 另有 4 个待合并提交。
- 会话附件和根目录微信开发者工具临时配置不纳入版本库；被明确拒绝的 `bb1c9a6b` 不在目标分支祖先链中。

## 微信审核态错误访问 localhost 修复

- 已修改小程序 API 路由：development 构建的 `develop` 继续走本地 3010；production 上传包收到审核态 `develop` 时改走正式 API，正式地址缺失才退回 trial API。
- production 构建会忽略非 HTTPS、localhost 和 loopback override，防止本地覆盖地址误入上传包；安全的公网 HTTPS override 仍受支持。
- 登录页仅在 development 构建显示“开发版”，production 审核态不再展示该误导标识；体验版仍显示“体验版”。
- 新增 production 审核态、缺省回退和 override 安全回归用例。定向 Jest 8/8、全量 Jest 17 suites / 68 tests、TypeScript、目标 ESLint、diff check 全通过。
- 微信开发者工具自动化端口 3001/9420 未开启，运行时截图/交互验证被阻塞；未运行完整 production build、未上传，需新上传包并重新提审后才会生效。
- 追溯 Git 历史确认：6 月 11 日 `a8eb0756` 将此前 production 单一公网 `TARO_APP_API_BASE_URL` 重构成 release/trial/develop 三地址，并把 develop 设为本地 3010，漏掉审核默认使用 develop 的规则；提交元数据作者/提交者为 `li1553770945 <1553770945@qq.com>`。6 月 23 日 `d3bb57bd` 增加登录页环境标识，提交元数据作者/提交者为 `Kirigaya <1193466151@qq.com>`，它只是让问题可见。不能仅凭 Git author 判断当时是本人、代理还是共用环境实际操作。
