# 2026-07-24

- 当前任务：修复 iPhone Apple 支付面板尚未结束时，会员页提前轮询并弹出“支付结果确认中”的交互竞态。
- 真机照片证明 App Store 面板仍显示“购买/取消”时，业务层 8 秒订单轮询已超时并在后面叠出提示。根因是未知于当前 Taro 类型的 `requestVirtualPayment` 被直接当 Promise `await`，实际只同步拉起面板就继续执行。
- 已改为显式等待微信原生 `success/fail`：成功后才轮询，`errCode=-2` 或 cancel/取消直接显示“已取消支付”，不轮询；同步延迟弹窗文案已精简。新增 3 个时序单测。TypeScript、ESLint、定向 3/3、全量 Jest 19/74、diff check 通过，watch 已编译。开发者工具自动化端口关闭且模拟器不能验证 Apple IAP，待真实 iPhone 新包复测。

## 后台小程序 watch 覆盖上传构建

- 定位到 2026-07-22 16:21 启动的 `npm run dev:weapp` 一直未退出。它在用户 production build 完成后继续监视源码，并于 2026-07-24 05:23 把 `apps/wechat/dist` 重新覆盖成 development 产物：`develop` 指向未监听的 `127.0.0.1:3010`，这是本地测试持续网络失败的直接原因。
- 经用户明确要求，已精确停止该 watch 的 PID 1900 及全部 Taro/cross-env/esbuild 子进程，并验证无残留。当前 dist 仍是最后写入的 development 产物，需要在 watch 已停止的前提下重新执行一次 `npm run build:weapp` 才能用于上传。
- 同时确认远端圈子 Feed 存在并发延迟，20 并发正式接口均返回 200，但耗时约 2.383–8.158 秒。已将前端 Feed 单次超时从会误伤的 8 秒纠正为 15 秒，并错峰次要首屏请求；静态与全量前端测试通过，改动待提交。
