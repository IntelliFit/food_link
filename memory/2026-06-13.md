# 2026-06-13

- Admin Console 本地登录 CORS 已修复：根因是 `admin/.env.development` 写死 `http://127.0.0.1:3010`，导致 `5173` 跨域直连后端；已改成本地 API base 留空并走 Vite `/api` 代理。`admin/.env.example` 与 README 已同步说明。`create-admin` 新增 `-skip-migration`，绕开无关 AutoMigrate 约束后已重置 admin 并通过 `5173/api/admin/login` 验证 200；临时密码未写入 memory。

- Admin Console 旧测试后台路由已修复：Vite SPA 原本把 `/test-backend` fallback 到新 Admin，导致 URL 变了但内容不变；已在 `admin/vite.config.ts` 把 `/test-backend` 和 `/static/test_backend` 代理到 Go 后端 `3010`。验证 `5173/test-backend` 返回旧“测试后台登录”HTML，`static/test_backend/app.js` 也返回 200。

- 小程序资源治理确认：普通大图/背景图/运营图/用户或包装食品图片应优先外置到 COS/CDN，通过 HTTPS URL 加载，避免占用 2048KB 主包/分包额度；包内只保留首屏必要小资源和必须本地资源。需要注意 CDN 域名要配置到微信合法域名/下载域名，且 SCSS/CSS 不要相对引用大图以免被内联到 wxss。iconfont 不是普通图片，不能简单依赖本地 `/assets/iconfont/iconfont.ttf`，本次已因该路径导致图标变方框，修复方向是页面和 custom-tab-bar 使用 data URI 字体并去掉重复引入。

## 完成：修复首页轮播图无法进入第三张的问题

- 定位到 `src/pages/index/index.tsx` 中轮播定时器硬编码 `% 2`，导致新增第三张意见反馈 banner 后无法被自动轮播到。
- 修复为 `% rewardHintBanners.length`，并调整 useEffect 位置以避免 TypeScript 闭包报错。
- 已用 `npm run typecheck` 与微信开发者工具自动化截图验证三张 banner 均可正常轮播。

## 完成：food_test 脱敏数据集入库

- 在 `backend/internal/migration/do/schema_do.go` 新增 `FoodWeightLabeledSampleDO`，表名 `food_weight_labeled_samples`。
- 在 `backend/internal/migration/migration.go` 补充该表的 check constraint 与索引。
- 编写 `backend/cmd/import-food-weight-dataset/main.go`，支持解析 `labels.txt` / `unlabeled_samples.txt`、上传 COS、写入数据库、dry-run、统计查询。
- 成功导入两个批次：
  - `food_test_sanitized_20260424`：37 张（33 已标注 + 4 未标注）
  - `food_test_sanitized_20260424_mini10`：10 张（全部已标注）
- 图片上传到现有 COS bucket `food-images-1370036754`，路径前缀 `datasets/food_test/20260424/<batch_name>/`。
- 已通过数据库统计与 COS URL 抽样访问验证。

## 完成：修复首页空白与颜色调整

### 修复首页空白

- 上轮将轮播图自动播放 `useEffect` 移到 `rewardHintBanners` 之后，导致 React Hook 顺序改变，在微信开发者工具热更新时组件无法渲染，首页空白。
- 修复方式：
  - 将 `useEffect` 移回原始 Hook 顺序位置（`loadRewardHintData` 之后、餐食卡片状态之前）。
  - 新增常量 `REWARD_HINT_BANNER_COUNT = 3`，避免 `useEffect` 引用后面定义的 `rewardHintBanners.length`。
  - `currentRewardHint`、`switchRewardHint` 同步改用该常量。
- 验证：微信开发者工具自动化确认首页正常渲染，轮播图 `今日可赚积分` → `食探校园食堂计划` → `意见反馈` → 回到第一张，循环正常。

### 颜色调整

- 用户反馈意见反馈 banner 的蓝色突兀，要求改为与品牌绿色形成对比的特殊黄色。
- 调整内容：
  - `src/pages/index/index.scss`：`.home-reward-hint--feedback` 改为琥珀色渐变 `#f59e0b → #fbbf24`，按钮文字 `#b45309`。
  - `src/packageExtra/pages/feedback/index.scss`：反馈页整体主色调从绿色改为琥珀色（页面背景、选中分类、添加图片 + 号、提交按钮、深色主题下卡片/输入框/图片添加区/提交栏对应色）。
  - `src/packageExtra/pages/feedback/index.tsx`：Switch 开启色从 `#00bc7d` 改为 `#f59e0b`。
- 验证：`npm run typecheck` 无新增错误；编译产物 `dist/pages/index/index.wxss` 与 `dist/packageExtra/pages/feedback/index.wxss` 均已生成对应琥珀色样式；从首页点击「去反馈」可正常跳转反馈页。

## 完成：降低意见反馈黄色饱和度并保留绿色按钮边框

- 用户反馈黄色饱和度过高，要求与首页低饱和度绿色一致；同时要求反馈页提交按钮保留绿色边框。
- 调整内容：
  - 首页 banner 与反馈页主色从 `#f59e0b / #fbbf24` 改为低饱和琥珀色 `#d4a574 / #e0c495`，按钮/文字对应改为 `#7d5f33`，选中态背景改为 `#faf5ed`，禁用态改为 `#e8d5b3`，Switch 开启色改为 `#d4a574`。
  - 深色主题下卡片/输入框/图片添加区/提交栏同步改为低饱和琥珀色调（如 `#252219` / `#2c261c` / `#372d24`）。
  - 反馈页提交大按钮增加 `border: 2rpx solid #00bc7d`，保留品牌绿色边框。
- 验证：`npm run typecheck` 无新增错误；编译产物检查确认首页 banner、反馈页按钮渐变、边框颜色均已正确生成；从首页点击「去反馈」可正常进入反馈页。

## 完成：提交按钮边框改为同色系黄色

- 用户进一步要求提交按钮边框不再保留绿色，而是与按钮本身一致的低饱和黄色。
- 将 `.feedback-submit` 的 `border-color` 从 `#00bc7d` 改为 `#d4a574`。

## 完成：热量识别第一阶段增加配料表输出

- 在 4 个图像识别 prompt 中增加可选 `ingredients` 字段：原始配料文本 `ingredientsText`、每份规格 `servingSize`、每100g关键营养 `nutritionPer100g`。
- `parseItems` 新增 `normalizeItemIngredients`，仅在有实际内容时保留配料表，避免占位空数据污染下游。
- `FoodItem` domain 新增 `Ingredients *FoodItemIngredients`，支持保存到 `user_food_records.items` JSON。
- 营养回算链路中，`ingredients.nutritionPer100g` 作为最高优先级营养来源，直接覆盖包装库/营养库/AI 生成结果；未检测到配料表时保持原有逻辑。
- 新增单元测试覆盖 prompt 关键字、解析保留/空值过滤、domain JSON 序列化、配料表优先覆盖与无配料表回退。
- `go build ./...` 通过；新增测试通过。现存部分测试失败与本次改动无关（packaged_food_library 测试表 schema、CGO sqlite3 环境）。

## 完成：首页校园食堂轮播图增加背景图与绿色渐变 mask

- 改动：
  - `src/pages/index/index.tsx`：campus banner 条件渲染 `<Image className='home-reward-hint__bg' src='/assets/bg/cafeteria-hero.jpg' mode='aspectFill' />`。
  - `src/pages/index/index.scss`：背景图 absolute 铺满、pointer-events none；复用 `.home-reward-hint--campus` 的 `::after` 绿色渐变 mask，内容层 z-index 高于 mask。
  - `config/index.ts`：新增 copy 规则，把 `assets/bg/cafeteria-hero.jpg` 复制到主包 `dist/assets/bg/`，供首页引用。
- 验证：DevTools 自动化确认 campus banner 与背景图元素正常渲染、点击跳转校园食堂页正常；`mrc screenshot` 当前环境超时，未保存截图。

## 完成：首页轮播图支持左右滑动切换与丝滑过渡

- 改动：
  - `src/pages/index/index.tsx`：用 Taro `<Swiper>` + `<SwiperItem>` 重构首页奖励提示轮播；`current` 绑定 `rewardHintIndex`，`onChange` 回写索引；保留 5 秒自动轮播 timer；移除手写 touch/swipe 逻辑与 `rewardHintTouchRef`；点击单张 banner 直接跳转对应页面。
  - `src/pages/index/index.scss`：新增 swiper 容器负责圆角/阴影/溢出裁剪；轨道高度固定 162rpx 匹配 banner 实际内容；指示点提到容器底部并 `z-index: 2`，避免被 campus 绿色 mask 盖住。
- 验证：DevTools 自动化确认 3 个 banner 正常布局、自动轮播位移生效、点击 campus banner 跳转正常；截图工具当前环境超时未保存截图。

## 完成：意见反馈飞书 Webhook 通知

- 新增 `backend/pkg/feishu` 与 `internal/feedback/service/feishu_notifier.go`。
- 用户通过 `POST /api/feedback` 提交成功后，后端异步向飞书机器人推送摘要（反馈 ID、用户、分类、页面、版本、联系方式、内容摘要、TraceID 等）。
- 配置：`feishu.feedback_webhook_url` / `feishu.feedback_webhook_secret`（环境变量 `FEISHU_FEEDBACK_WEBHOOK_*`）；留空则关闭通知。

## 完成：关闭应用启动时自动数据库迁移

- 决策：所有环境（本地/K8s dev/main）均不在 HTTP 服务启动时跑 `migration.AutoMigrate`；结构变更仅通过 `go run ./cmd/migration -config-dir .` 手动执行。
- 代码：`app.go` 移除启动迁移；`create-admin` 移除默认迁移与 `-skip-migration`。
- e2e 临时库初始化仍保留进程内迁移。

## 完成：优化意见反馈飞书通知样式

- 用户反馈飞书群里的意见反馈通知只有纯文字、可读性差；已查飞书官方自定义机器人文档，确认消息卡片使用 `msg_type=interactive` 且内容放在 `card` 字段，Markdown 文本使用 `lark_md`。
- `backend/pkg/feishu` 新增 `SendInteractiveCard`，保留原有签名、超时和 webhook 错误处理，并修正飞书返回 `code != 0` 但 `StatusCode == 0` 时未报错的问题。
- `backend/internal/feedback/service/feishu_notifier.go` 将新反馈通知从纯文本改为卡片：橙色标题、关键字段 Markdown、最近失败请求引用块、反馈内容引用块。
- 验证：`go test ./pkg/feishu ./internal/feedback/service` 通过；未自动重启/部署后端，线上需发版后生效。
