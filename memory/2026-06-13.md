# 2026-06-13

- Admin Console 本地登录 CORS 已修复：根因是 `admin/.env.development` 写死 `http://127.0.0.1:3010`，导致 `5173` 跨域直连后端；已改成本地 API base 留空并走 Vite `/api` 代理。`admin/.env.example` 与 README 已同步说明。`create-admin` 新增 `-skip-migration`，绕开无关 AutoMigrate 约束后已重置 admin 并通过 `5173/api/admin/login` 验证 200；临时密码未写入 memory。

- Admin Console 旧测试后台路由已修复：Vite SPA 原本把 `/test-backend` fallback 到新 Admin，导致 URL 变了但内容不变；已在 `admin/vite.config.ts` 把 `/test-backend` 和 `/static/test_backend` 代理到 Go 后端 `3010`。验证 `5173/test-backend` 返回旧“测试后台登录”HTML，`static/test_backend/app.js` 也返回 200。

- 小程序资源治理确认：普通大图/背景图/运营图/用户或包装食品图片应优先外置到 COS/CDN，通过 HTTPS URL 加载，避免占用 2048KB 主包/分包额度；包内只保留首屏必要小资源和必须本地资源。需要注意 CDN 域名要配置到微信合法域名/下载域名，且 SCSS/CSS 不要相对引用大图以免被内联到 wxss。iconfont 不是普通图片，不能简单依赖本地 `/assets/iconfont/iconfont.ttf`，本次已因该路径导致图标变方框，修复方向是页面和 custom-tab-bar 使用 data URI 字体并去掉重复引入。

## 完成：修复首页轮播图无法进入第三张的问题

- 定位到 `src/pages/index/index.tsx` 中轮播定时器硬编码 `% 2`，导致新增第三张意见反馈 banner 后无法被自动轮播到。
- 修复为 `% rewardHintBanners.length`，并调整 useEffect 位置以避免 TypeScript 闭包报错。
- 已用 `npm run typecheck` 与微信开发者工具自动化截图验证三张 banner 均可正常轮播。

## 完成：food_test 脱敏数据集入库

- 在 `backend/internal/migration/do/schema_do.go` 新增 `FoodWeightLabeledSampleDO`，表名 `food_weight_labeled_samples`。
- 在 `backend/internal/migration/migration.go` 补充该表的 check constraint 与索引。
- 编写 `backend/cmd/import-food-weight-dataset/main.go`，支持解析 `labels.txt` / `unlabeled_samples.txt`、上传 COS、写入数据库、dry-run、统计查询。
- 成功导入两个批次：
  - `food_test_sanitized_20260424`：37 张（33 已标注 + 4 未标注）
  - `food_test_sanitized_20260424_mini10`：10 张（全部已标注）
- 图片上传到现有 COS bucket `food-images-1370036754`，路径前缀 `datasets/food_test/20260424/<batch_name>/`。
- 已通过数据库统计与 COS URL 抽样访问验证。

## 完成：修复首页空白与颜色调整

### 修复首页空白

- 上轮将轮播图自动播放 `useEffect` 移到 `rewardHintBanners` 之后，导致 React Hook 顺序改变，在微信开发者工具热更新时组件无法渲染，首页空白。
- 修复方式：
  - 将 `useEffect` 移回原始 Hook 顺序位置（`loadRewardHintData` 之后、餐食卡片状态之前）。
  - 新增常量 `REWARD_HINT_BANNER_COUNT = 3`，避免 `useEffect` 引用后面定义的 `rewardHintBanners.length`。
  - `currentRewardHint`、`switchRewardHint` 同步改用该常量。
- 验证：微信开发者工具自动化确认首页正常渲染，轮播图 `今日可赚积分` → `食探校园食堂计划` → `意见反馈` → 回到第一张，循环正常。

### 颜色调整

- 用户反馈意见反馈 banner 的蓝色突兀，要求改为与品牌绿色形成对比的特殊黄色。
- 调整内容：
  - `src/pages/index/index.scss`：`.home-reward-hint--feedback` 改为琥珀色渐变 `#f59e0b → #fbbf24`，按钮文字 `#b45309`。
  - `src/packageExtra/pages/feedback/index.scss`：反馈页整体主色调从绿色改为琥珀色（页面背景、选中分类、添加图片 + 号、提交按钮、深色主题下卡片/输入框/图片添加区/提交栏对应色）。
  - `src/packageExtra/pages/feedback/index.tsx`：Switch 开启色从 `#00bc7d` 改为 `#f59e0b`。
- 验证：`npm run typecheck` 无新增错误；编译产物 `dist/pages/index/index.wxss` 与 `dist/packageExtra/pages/feedback/index.wxss` 均已生成对应琥珀色样式；从首页点击「去反馈」可正常跳转反馈页。

## 完成：降低意见反馈黄色饱和度并保留绿色按钮边框

- 用户反馈黄色饱和度过高，要求与首页低饱和度绿色一致；同时要求反馈页提交按钮保留绿色边框。
- 调整内容：
  - 首页 banner 与反馈页主色从 `#f59e0b / #fbbf24` 改为低饱和琥珀色 `#d4a574 / #e0c495`，按钮/文字对应改为 `#7d5f33`，选中态背景改为 `#faf5ed`，禁用态改为 `#e8d5b3`，Switch 开启色改为 `#d4a574`。
  - 深色主题下卡片/输入框/图片添加区/提交栏同步改为低饱和琥珀色调（如 `#252219` / `#2c261c` / `#372d24`）。
  - 反馈页提交大按钮增加 `border: 2rpx solid #00bc7d`，保留品牌绿色边框。
- 验证：`npm run typecheck` 无新增错误；编译产物检查确认首页 banner、反馈页按钮渐变、边框颜色均已正确生成；从首页点击「去反馈」可正常进入反馈页。

## 完成：提交按钮边框改为同色系黄色

- 用户进一步要求提交按钮边框不再保留绿色，而是与按钮本身一致的低饱和黄色。
- 将 `.feedback-submit` 的 `border-color` 从 `#00bc7d` 改为 `#d4a574`。

## 完成：热量识别第一阶段增加配料表输出

- 在 4 个图像识别 prompt 中增加可选 `ingredients` 字段：原始配料文本 `ingredientsText`、每份规格 `servingSize`、每100g关键营养 `nutritionPer100g`。
- `parseItems` 新增 `normalizeItemIngredients`，仅在有实际内容时保留配料表，避免占位空数据污染下游。
- `FoodItem` domain 新增 `Ingredients *FoodItemIngredients`，支持保存到 `user_food_records.items` JSON。
- 营养回算链路中，`ingredients.nutritionPer100g` 作为最高优先级营养来源，直接覆盖包装库/营养库/AI 生成结果；未检测到配料表时保持原有逻辑。
- 新增单元测试覆盖 prompt 关键字、解析保留/空值过滤、domain JSON 序列化、配料表优先覆盖与无配料表回退。
- `go build ./...` 通过；新增测试通过。现存部分测试失败与本次改动无关（packaged_food_library 测试表 schema、CGO sqlite3 环境）。

## 完成：首页校园食堂轮播图增加背景图与绿色渐变 mask

- 改动：
  - `src/pages/index/index.tsx`：campus banner 条件渲染 `<Image className='home-reward-hint__bg' src='/assets/bg/cafeteria-hero.jpg' mode='aspectFill' />`。
  - `src/pages/index/index.scss`：背景图 absolute 铺满、pointer-events none；复用 `.home-reward-hint--campus` 的 `::after` 绿色渐变 mask，内容层 z-index 高于 mask。
  - `config/index.ts`：新增 copy 规则，把 `assets/bg/cafeteria-hero.jpg` 复制到主包 `dist/assets/bg/`，供首页引用。
- 验证：DevTools 自动化确认 campus banner 与背景图元素正常渲染、点击跳转校园食堂页正常；`mrc screenshot` 当前环境超时，未保存截图。

## 完成：首页轮播图支持左右滑动切换与丝滑过渡

- 改动：
  - `src/pages/index/index.tsx`：用 Taro `<Swiper>` + `<SwiperItem>` 重构首页奖励提示轮播；`current` 绑定 `rewardHintIndex`，`onChange` 回写索引；保留 5 秒自动轮播 timer；移除手写 touch/swipe 逻辑与 `rewardHintTouchRef`；点击单张 banner 直接跳转对应页面。
  - `src/pages/index/index.scss`：新增 swiper 容器负责圆角/阴影/溢出裁剪；轨道高度固定 162rpx 匹配 banner 实际内容；指示点提到容器底部并 `z-index: 2`，避免被 campus 绿色 mask 盖住。
- 验证：DevTools 自动化确认 3 个 banner 正常布局、自动轮播位移生效、点击 campus banner 跳转正常；截图工具当前环境超时未保存截图。

## 完成：意见反馈飞书 Webhook 通知

- 新增 `backend/pkg/feishu` 与 `internal/feedback/service/feishu_notifier.go`。
- 用户通过 `POST /api/feedback` 提交成功后，后端异步向飞书机器人推送摘要（反馈 ID、用户、分类、页面、版本、联系方式、内容摘要、TraceID 等）。
- 配置：`feishu.feedback_webhook_url` / `feishu.feedback_webhook_secret`（环境变量 `FEISHU_FEEDBACK_WEBHOOK_*`）；留空则关闭通知。

## 完成：关闭应用启动时自动数据库迁移

- 决策：所有环境（本地/K8s dev/main）均不在 HTTP 服务启动时跑 `migration.AutoMigrate`；结构变更仅通过 `go run ./cmd/migration -config-dir .` 手动执行。
- 代码：`app.go` 移除启动迁移；`create-admin` 移除默认迁移与 `-skip-migration`。
- e2e 临时库初始化仍保留进程内迁移。

## 完成：优化意见反馈飞书通知样式

- 用户反馈飞书群里的意见反馈通知只有纯文字、可读性差；已查飞书官方自定义机器人文档，确认消息卡片使用 `msg_type=interactive` 且内容放在 `card` 字段，Markdown 文本使用 `lark_md`。
- `backend/pkg/feishu` 新增 `SendInteractiveCard`，保留原有签名、超时和 webhook 错误处理，并修正飞书返回 `code != 0` 但 `StatusCode == 0` 时未报错的问题。
- `backend/internal/feedback/service/feishu_notifier.go` 将新反馈通知从纯文本改为卡片：橙色标题、关键字段 Markdown、最近失败请求引用块、反馈内容引用块。
- 验证：`go test ./pkg/feishu ./internal/feedback/service` 通过；未自动重启/部署后端，线上需发版后生效。

## 进行中：修复用户 CZ 食物记录未进入圈子【全部公开】

- 现象：CZ 今日有 2 条食物记录（早餐、午餐），但在圈子【全部公开】看不到。
- 根因：CZ 的 `weapp_user.public_records = NULL`；圈子公开 Feed 查询 `public_records = TRUE`，`NULL` 不匹配。
- 影响：数据库中 262 个用户 `public_records = NULL`，均为注册时未显式赋值导致（`login_service.go` 创建用户未设置该字段）。
- 修复代码：
  - `backend/internal/auth/service/login_service.go`：新用户注册显式设置 `PublicRecords = true`。
  - `backend/internal/community/repo/feed_repo.go`：公开 Feed 查询改为 `COALESCE(public_records, TRUE) = TRUE`。
  - `backend/internal/community/service/community_service.go`：单条 Feed 上下文权限兼容 `NULL` 按默认公开处理。
  - `backend/internal/migration/migration.go`：新增幂等迁移 `ensurePublicRecordsDefault`，把 `public_records IS NULL` 更新为 `TRUE`。
- 验证：`go build ./...` 与 `go test ./internal/community/service/...` 通过；只读查询验证修复后今日【全部公开】从 19 条变为 30 条，CZ 的 2 条记录已出现。
- 已运行迁移 `cd backend && go run ./cmd/migration -config-dir .`：NULL 用户从 262 人修正为 0 人，CZ 的 `public_records` 已变为 `TRUE`。
- 前端同步：`src/packageExtra/pages/privacy-settings/index.tsx` 已正确读取并回写 `public_records`，迁移后数据库字段全部非 NULL，开关状态与数据库一致。
- 已提交并推送至 `origin/dev`，commit `fc81a86`。由于 pre-commit hook 中的 `git add -u`，本次 commit 同时包含了工作区中其他已跟踪但未单独提交的改动（entry_type、iconfont、RecordMenu 文案等）。

## 完成：热量识别配料表营养前置与来源分类

- 将 `backend/internal/analyze/service/analyze_service.go` 中 `applyDBFirstNutritionWithOptions` 的 ingredient label 处理前置到 `s.nutrition == nil` 早退之前；配料表营养不再依赖营养库即可按重量缩放。
- 新增 `nutritionSourceCategory` 函数，把内部 `nutrition_source` 映射为五类用户可感知来源：`user_image_label` / `user_text` / `llm_generated` / `database` / `web_search`。
- 每个 item 输出新增 `nutrition_source_category` 字段；前端类型链路与转换函数同步透传该字段（`src/utils/api.ts`、`src/utils/food-record-item-payload.ts`、`src/packageExtra/pages/result/result-item-converter.ts`、`src/packageExtra/pages/result/correction-payload.ts`、`src/utils/dev-record-preview.ts`、`src/pages/index/components/MealRecordEditModal.tsx`）。
- 新增真实图片测试 `backend/internal/analyze/service/ingredient_label_real_image_test.go`（build tag `real_llm`），使用 `http://cdn-food-images.coachlink.fit/3638910d-422b-4772-a9b7-30086007ad1a.jpg` 验证标准模式下：
  - 识别项包含 `ingredients` 与 `nutritionPer100g`。
  - `nutrition_source == "ingredient_label"`。
  - `nutrition_source_category == "user_image_label"`。
  - 80g 估算重量下各宏量营养素比例与标签 100g 值一致（calories 299.2、protein 9.2、fat 12.4、carbs 37.52）。
- 验证：`go build ./...`、相关 Go 单元测试、前端 3 个单元测试、`npm run typecheck` 均无新增错误。

## 完成：圈子自定义图文动态 + 全动态发布者增删改查

- 用户要求：在圈子顶部增加按钮，允许用户发布自定义图文动态（文本 + 最多 3 张图，类似小红书）；兼容现有圈子动态格式；将自定义动态与现有记录生成的动态融合；所有动态支持发布者自己的增删改查。
- 后端：
  - `backend/internal/migration/do/schema_do.go` 新增 `UserCirclePostDO`（表 `user_circle_posts`）。
  - `backend/internal/migration/migration.go` 同步 `feed_likes`/`feed_comments`/`feed_interaction_notifications` 的 `target_type` check 约束，新增 `user_circle_posts` 的索引、外键、触发器。
  - `backend/internal/community/domain/community_domain.go` 新增 `UserCirclePost`。
  - `backend/internal/community/repo/feed_repo.go` 扩展 `circle_post` 查询、CRUD、`applyTargetFilter`、`normalizeTargetType`。
  - `backend/internal/community/service/community_service.go` 新增 `CreateCirclePost`、`UpdateCirclePost`、`DeleteCirclePost`、`UploadCirclePostImage`；图片 URL 转 COS key 存储。
  - `backend/internal/community/handler/community_handler.go` 新增 `POST /api/community/posts/upload-image`、`POST /api/community/posts`、`PUT /api/community/posts/:post_id`、`DELETE /api/community/posts/:post_id`。
  - `backend/internal/app/app.go` 注册新路由。
  - `backend/internal/health/service/exercise_service.go`、`repo/exercise_repo.go`、`handler/health_handler.go` 新增 `PUT /api/exercise-logs/:log_id`（运动记录编辑）。
- 前端：
  - `src/app.config.ts` 注册 `pages/circle-post-edit/index`。
  - `src/utils/api.ts` 扩展 `CommunityFeedItem` 类型与 `createCirclePost`、`updateCirclePost`、`deleteCirclePost`、`uploadCirclePostImage`、`updateExerciseLog` 等接口。
  - 新增 `src/packageExtra/pages/circle-post-edit/index.tsx/.scss/.config.ts` 发布/编辑页：文本 2000 字、最多 3 张图、编辑态从上下文加载。
  - `src/pages/community/index.tsx/.scss`：顶部新增「发布」按钮、`circle_post` 卡片渲染、多图预览、发布者管理入口（编辑/删除）。
  - `src/packageExtra/pages/interaction-feed-detail/index.tsx`：详情页 `circle_post` 渲染与管理入口。
- 验证：
  - 后端 `cd backend && go build ./...` 通过。
  - 数据库迁移 `cd backend && go run ./cmd/migration -config-dir .` 已在远程 PostgreSQL 成功执行。
  - 前端 `npm run typecheck` 未因本次改动新增错误（剩余错误均为历史问题）。
  - 后端 community/health 相关 mock 已更新以匹配新接口。
- 阻塞：微信开发者工具当前环境未登录，`mrc screenshot` 与 `mrc mock` 均不可用，运行时验证需用户登录后手动完成。
- 后续：用户重启后端、运行 `npm run dev:weapp`，手动验证发布/查看/编辑/删除/混合 Feed 展示。

## 完成：修复圈子饮食动态「管理 → 编辑」报「记录不存在」

- 现象：从收藏选择餐食并记录后，圈子动态中点击「管理 → 编辑」提示「记录不存在」。
- 根因：圈子 Feed（`src/pages/community/index.tsx`）和互动详情页（`src/packageExtra/pages/interaction-feed-detail/index.tsx`）对 `food_record` 的编辑跳转使用 URL 参数 `recordId`，但 `record-detail` 页面读取的是 `router.params?.id`，导致页面获取不到记录 ID，进入 storage 回退分支后提示不存在。
- 修复：将两处的 `record-detail/index?recordId=...` 改为 `record-detail/index?id=...`，与页面解析逻辑及卡片点击入口保持一致。
- 验证：`npm run typecheck` 无新增错误；全局搜索确认已无 `record-detail/index?recordId` 残留。
- 待验证：微信开发者工具运行时验证（当前环境未登录）。

## 完成：圈子饮食动态竖三点管理 + 底部编辑表单

- 用户要求：圈子动态管理按钮改为竖三点（参考公共食物库详情页底部更多按钮）；点击编辑不跳转传统分析页，而是从底部弹出表单，允许编辑动态文本、图片、热量、蛋白质/碳水/脂肪。
- 后端：
  - `backend/internal/foodrecord/service/food_record_service.go`：
    - `UpdateFoodRecordInput` 新增 `Description *string`、`ImagePath *string`、`ImagePaths []string`。
    - `Update` 方法将上述字段写入 updates map。
  - `backend/internal/foodrecord/handler/food_record_handler.go`：
    - `UpdateFoodRecord` body 增加对应字段绑定，并传给 service。
- 前端 API：
  - `src/utils/api.ts`：`UpdateFoodRecordRequest` 新增 `description?`、`image_path?`、`image_paths?`。
- 新增组件：
  - `src/pages/community/components/CommunityFoodRecordEditSheet.tsx/.scss`
  - 表单字段：多行文本、图片列表（最多 3 张，支持上传/删除）、热量、蛋白质、碳水、脂肪。
  - 提交调用 `updateFoodRecord`。
  - 支持深色主题。
- 入口改造：
  - `src/pages/community/index.tsx`：卡片右上角与底部管理按钮图标改为 `⋮`，底部移除「管理」文字；food_record 编辑改为打开底部表单；保存成功后更新本地 Feed 列表与缓存。
  - `src/packageExtra/pages/interaction-feed-detail/index.tsx`：底部管理按钮同样改为竖三点；food_record 编辑改为打开底部表单；保存成功后更新当前详情数据。
- 样式：
  - `src/pages/community/index.scss`：`.feed-card-more-icon`、`.action-manage-icon` 改为竖三点样式（font-size 44rpx、font-weight 700、line-height 1、color #64748b），移除 `.action-manage-text`。
- 验证：`npm run typecheck` 未引入新错误；后端 `go build ./...` 通过。
- 阻塞：微信开发者工具当前环境未登录，运行时验证需用户登录后手动完成。

## 完成：修复圈子饮食动态编辑表单 UI 问题

- 用户反馈：编辑动态的营养信息超出屏幕宽度导致单位看不到；保存按钮在底部被底部导航栏遮住，滚动也找不到。
- 修复内容（`src/pages/community/components/CommunityFoodRecordEditSheet.tsx/.scss`）：
  - 营养信息行改为 `flex-shrink: 0` 的 label/unit + `min-width: 0` 的 input，避免水平溢出。
  - 保存按钮从固定 footer 移入 `ScrollView` 内容区底部，变成可滚动的一部分。
  - 新增 140rpx 底部 spacer，确保保存按钮上方有足够空白，不被底部 tab bar 遮挡。
  - 表单容器 `max-height` 从 `82vh` 改为 `calc(100vh - 120rpx)`，预留 tab bar 高度。
  - 为 `ScrollView` 显式设置 `style={{ height: '100%' }}`，确保在 flex 容器内可滚动。
- 验证：`npm run typecheck` 无新增错误。

## 完成：编辑对话框内部改用普通 View

- 用户反馈：对话框内不要使用 ScrollView，宽度全烂了。
- 修改：`src/pages/community/components/CommunityFoodRecordEditSheet.tsx`
  - 移除 `ScrollView` 导入与使用。
  - 改用普通 `<View className='cfs-body'>` 承载表单内容。
  - 样式 `.cfs-body` 保留 `overflow-y: auto` + `overflow-x: hidden`，并增加 `width: 100%`、`box-sizing: border-box`，确保宽度正确。
  - `.cfs-nutrients` 增加 `max-width: 100%` 与 `box-sizing: border-box`。
- 验证：`npm run typecheck` 无新增错误。

## 完成：调整管理按钮位置与识别记录详情返回逻辑

- 用户要求：
  1. 管理按钮只放在评论/操作行最右侧，昵称行右侧不放三点。
  2. 三点按钮需要圆角矩形背景容器，与背景有区分度。
  3. 从圈子点击自己的分享记录进入识别记录详情后，左上角返回应回到圈子，使用微信小程序自带顶部导航栏返回按钮。
- 修改：
  - `src/pages/community/index.tsx`：
    - 移除昵称行右侧的 `feed-card-more` 三点按钮。
    - 底部操作行管理按钮外增加 `action-manage-box` 容器。
  - `src/pages/community/index.scss`：
    - 新增 `.action-manage-box` 样式：圆角矩形、浅灰背景 `#f3f4f6`、尺寸 56rpx×44rpx、圆角 12rpx。
    - 移除不再使用的 `.feed-card-more` / `.feed-card-more-icon` 样式。
  - `src/packageExtra/pages/interaction-feed-detail/index.tsx`：
    - 同样为底部管理按钮增加 `action-manage-box` 容器（样式复用 community/index.scss）。
  - `src/packageExtra/pages/record-detail/index.config.ts`：
    - 移除 `navigationStyle: 'custom'`，改为微信默认导航栏。
  - `src/packageExtra/pages/record-detail/index.tsx`：
    - 移除 `CustomNavBar` 导入与两处使用。
    - 移除 `getNavBarHeight` 调用。
    - `ScrollView` 高度从 `calc(100vh - navBarHeight)` 改为 `100vh`。
- 验证：`npm run typecheck` 无新增错误。

## 完成：操作行两端对齐布局

- 用户要求：点赞和评论作为一组，三点「更多」作为另一组，两部分分居屏幕两侧，三个点到最右侧。
- 修改：
  - `src/pages/community/index.tsx` 与 `src/packageExtra/pages/interaction-feed-detail/index.tsx`：
    - 将点赞、评论用 `<View className='feed-actions-left'>` 包裹。
    - 三点「更多」单独作为同层兄弟元素。
  - `src/pages/community/index.scss`：
    - `.feed-actions` 增加 `justify-content: space-between`，三点自然贴到最右侧。
    - 新增 `.feed-actions-left` 维持点赞/评论之间的原有间距。
- 验证：`npm run typecheck` 无新增错误。

## 完成：圈子自定义图文动态支持可选营养信息（小红书式编辑器）

- 用户要求：自定义图文动态发布页改为小红书风格，并支持选填营养信息（热量、蛋白质、碳水、脂肪、膳食纤维、糖、钠、重量），填写后在圈子 Feed 卡片展示。
- 后端：
  - `backend/internal/migration/do/schema_do.go`：`UserCirclePostDO` 新增 `Calories/Protein/Carbs/Fat/Fiber/Sugar/SodiumMg/TotalWeightGrams` 字段。
  - `backend/internal/migration/migration.go`：对应幂等迁移逻辑。
  - `backend/internal/community/domain/community_domain.go`：`UserCirclePost` domain 扩展同名字段。
  - `backend/internal/community/service/community_service.go`：`CreateCirclePostInput` / `UpdateCirclePostInput` 与 service 方法保存营养字段；创建时按给定值计算 total_calories/total_protein/total_carbs/total_fat。
  - `backend/internal/community/handler/community_handler.go`：请求 body 绑定营养字段。
  - `backend/internal/community/repo/feed_repo.go`：Feed 查询与单条上下文查询映射营养字段到 `CommunityFeedRecord`。
- 前端：
  - `src/utils/api.ts`：新增 `CirclePostNutritionInput`；`createCirclePost` / `updateCirclePost` 支持营养 payload；`CommunityFeedRecord` 扩展 `fiber` / `sugar` / `sodium_mg`。
  - `src/packageExtra/pages/circle-post-edit/index.tsx/.scss`：重写为小红书式编辑器；顶部大文本输入、图片网格、可开关的营养信息区（8 项数字输入）；编辑态从 `communityGetFeedContext` 回显营养字段。
  - `src/pages/community/index.tsx/.scss`：`circle_post` 卡片新增可选营养汇总展示；未填写时不展示。
- 验证：
  - 后端 `cd backend && go build ./...` 通过。
  - 后端 `go test ./internal/community/service/... ./internal/community/domain/...` 通过。
  - 数据库迁移 `cd backend && go run ./cmd/migration -config-dir .` 已在远程 PostgreSQL 成功执行。
  - 前端 `npm run typecheck` 未在本次改动文件中引入新错误；剩余错误为历史问题。
  - 顺手修复 `src/pages/community/index.tsx` 中 `handleViewDetail` 的类型收窄，消除该函数 5 处 TypeScript 报错。
- 阻塞：微信开发者工具当前环境未登录，`mrc screenshot` / `mrc mock` 不可用；按项目规则代理不主动启动本地 dev 服务器，运行时验证需用户登录并运行 `npm run dev:weapp` 后手动完成。
- 后续：用户启动 `npm run dev:weapp` 与后端服务后，验证发布/编辑/展示营养信息的完整链路。

## 完成：发布页 UI 调整（图片在上、主题色按钮、深色主题、rpx 字号）

- 用户反馈：发布页图片应在文字上方、按钮需为主题色、支持深色主题、手机端文本太小。
- 修改 `src/packageExtra/pages/circle-post-edit/index.tsx/.scss`：
  - 重新排序：图片区 → 文本区 → 营养信息区 → 发布按钮。
  - 发布/保存按钮改为品牌绿渐变（`#00bc7d → #10b981`），无 border，圆角胶囊，高度 96rpx，字号 32rpx。
  - 全页面尺寸改用 `rpx`：页面/卡片间距 24rpx，卡片内边距 28rpx，图片格 200rpx，标题 32rpx，正文 30rpx，辅助文案 24rpx。
  - 增加 `.fl-page-theme-root--dark` 适配：页面背景、卡片背景、文本、输入区、图片添加区、开关底色均切换为暗色变量。
- 验证：`npm run typecheck` 在 `circle-post-edit/index.tsx` 无新增错误。
- 阻塞：微信开发者工具当前环境未登录，运行时验证需用户登录后手动完成。

## 完成：发布页新增本地草稿功能

- 用户要求：发布页左侧增加「存草稿」按钮，发布按钮文本改为「发布动态」；草稿基于本地缓存保存；首次点击时提示仅本地设备可见。
- 修改 `src/packageExtra/pages/circle-post-edit/index.tsx/.scss`：
  - 底部改为左右双按钮布局：左侧「存草稿」（灰底），右侧「发布动态」（品牌绿渐变）。
  - 新增 `handleSaveDraft`：将当前文本、图片（过滤上传中项）、营养开关与数值写入 `Taro.setStorageSync('circle_post_draft_v1')`。
  - 首次保存时通过 `Taro.showModal` 提示「草稿仅保存在本设备，更换设备或清理缓存后将无法查看」，确认按钮为「知道了」；确认后写入 `circle_post_draft_tip_shown_v1`，后续不再弹窗。
  - 新建动态页面进入时自动调用 `loadDraftFromStorage` 回显草稿；编辑已有动态时不回显草稿，仍走服务端数据。
  - 深色主题下「存草稿」按钮同步适配暗色表面。
- 验证：`npm run typecheck` 在 `circle-post-edit/index.tsx` 无新增错误。

## 完成：营养信息字段精简为热量/蛋白质/碳水/脂肪

- 用户要求：营养信息先只展示热量到脂肪 4 项。
- 修改：
  - `src/packageExtra/pages/circle-post-edit/index.tsx`：`NUTRITION_FIELDS` 从 8 项精简为热量、蛋白质、碳水、脂肪。
  - `src/pages/community/index.tsx`：`circle_post` 卡片营养区仅展示热量、蛋白质、碳水、脂肪；`hasNutrition` 判断也同步精简。
- 后端仍保留原有字段存储能力，后续如需扩展无需改接口。
- 验证：`npm run typecheck` 无新增错误。

## GitHub 分支同步与包体积治理补充

- 已提交 `5bb35a0 feat: consolidate admin console and reduce miniapp package assets`。
- 已合并 `origin/dev` 并生成 `73c60e2 chore: sync main with latest dev before push`。
- 已推送 `origin/main` 与 `origin/dev`，并确认两者一致。

## 小程序上传包体积通用治理

- 用户反馈体验版/正式版上传经常超过微信单包 `2048KB` 限制，希望有长期方案。已新增 `scripts/check-weapp-package-size.mjs` 并接入 `build:weapp`、`build:weapp:preview`、`build:weapp:release`，构建后自动按主包/分包检查体积；超过 `2048KB` 失败，超过 `1900KB` 预警并输出最大文件排行。
- 同步优化上传配置和资源策略：微信开发者工具配置开启压缩、忽略未使用文件和 `.map`；Taro 不再复制静态 `assets/iconfont/iconfont.ttf`；移除页面中的 `@taroify/icons`/Vant icon 组件与 style 导入，改用项目 iconfont Text class，避免 base64 字体撑大 `vendors.wxss`。
- 验证：`npm run lint` 通过，`npm run build:weapp:preview` 通过；当前体积为 main `1962.9KB`、packageExtra `1939.4KB`、packageStatsMetabolic `880.7KB`、packageUserGroup `126.9KB`、total `4910.3KB`。`WARN` 是 `1900KB` 早期预警，不是上传失败。
- 微信开发者工具自动化仍未能连接：`mrc where --port 3001/9420` 连接失败，未完成截图和交互验证。
## 食探/教链品牌商标风险

- 用户确认当前软件名为“食探”，全称“智健食探”；另一个小程序名为“教链”，全称“启能教链”。
- 用户担心这两个名称是否已被注册商标，认为存在品牌风险。
- 初步建议：应尽快做正式商标检索和申请，不只查完全同名，还要查同类/近似类别里的近似商标。食探优先关注第 9、42、44 类，视业务补第 35、38、43 类；教链优先关注第 9、41、42 类，视业务补第 35、38 类。

## 微信支付委托代扣/自动续费接入咨询

- 用户已在微信支付商户平台开通“委托代扣”，但不清楚如何接入。判断：商户平台“签约关系管理”页面用于查询已签约关系，不是配置入口；真正接入需要小程序发起签约、后端保存签约关系、处理签约/扣款/解约回调，并定时发起续费扣款。项目已有一次性会员支付、`user_pro_memberships.auto_renew` 字段和自动续费审核预览页，但正式链路尚未实现，且会员协议/会员页目前仍表达“到期后手动续费”。后续落地需确认委托代扣模板 ID、扣费周期/金额、回调 URL 和是否先只支持月卡自动续费。
