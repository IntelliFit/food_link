# 2026-05-23

- 用户反馈当天饮食记录中“光明12mm大粒草莓酸奶”与午加餐出现 0 kcal。已做只读数据库排查，确认不是前端当天页展示错误，而是记录保存时 item 级营养已经是 0。酸奶对应记录 `a01edbb4-7eb5-4202-90f8-24d55a2e8373`，source task `59b20ed6-243e-4c87-b662-e71b2d126e38`，任务结果里酸奶 `nutrition_source=unresolved`、`unit_nutrition_per_100g` 和 `nutrients` 全 0；16:00 午加餐记录 `61bc9982-82c9-4ef1-a482-afcb4d480a06`，source task `4e69a761-9ab4-4e1e-a895-c3e2dd55a45e`，咖啡被标记为 `deepseek_generated` 但单位营养仍全 0。核心根因是 `db_first` 营养库未命中后，DeepSeek 补全失败/漏项/返回全 0 时，后端仍用 `zeroUnitNutritionPer100g()` 继续输出并允许保存；缺少对“非零重量真实食物四大营养全 0”的有效性校验、重试、待补全提示和历史修复机制。

- 新一轮 0 kcal 排查定位到另一个更具体的饮料 bug：任务 `09e4e976-35f6-46ce-b85a-a7de770b047f` 中，“可乐”已经 `library_exact_alias` 命中，但 VLM 输出 `waterMl=100`、重量为 0；`db_first` 只用 `estimatedWeightGrams/originalWeightGrams` 做缩放，所以 42 kcal/100g 的库数据被乘以 0，结果仍是 0 kcal。“哈尔滨啤酒清爽”同理因为只有 ml、没有 g，连 DeepSeek 补全候选都没进去。已修复 `backend/internal/analyze/service/analyze_service.go`：营养回算重量取值顺序改为估算重量、原始重量、`waterMl`，饮料容量按近似克重用于营养缩放和 DeepSeek 候选判断。验证：纯逻辑测试 `TestNutritionWeightFromItemFallsBackToWaterMl` 通过；DB 链路定向测试仍被当前环境 `CGO_ENABLED=0` / `go-sqlite3 requires cgo` 阻塞。

- 健康档案页的自定义病史 / 自定义过敏源已修成可二次编辑。
  - 之前的问题不是单纯“UI 没按钮”，而是重新加载档案时没有把非预设值拆回 `customMedicalList` / `customAllergyList`，所以用户只能看到选中态，不能进入自定义编辑流。
  - 现已在 `src/packageExtra/pages/health-profile/index.tsx` 中补齐：
    - 从 `health_condition.medical_history` / `allergies` 里拆分预设项和自定义项；
    - 自定义项默认回填并选中；
    - 每项新增显式“编辑 / 删除”；
    - 输入框支持“新增”与“编辑保存”两种模式；
    - 过敏源自定义输入改为独立 `customAllergy`，不再误复用 `customMedical`。
  - 验证：`npx eslint src/packageExtra/pages/health-profile/index.tsx --max-warnings 0` 通过。
  - 运行时验证仍受阻：`mrc where --port 9420` / `3001` 无法连接，没法出微信开发者工具截图。

- 首页热量目标模型已按用户确认收敛为稳定基础目标。
  - 新口径：注册/健康档案生成长期基础目标；当天运动不再改变首页可摄入目标；14 天后只提示校准建议，用户确认后才更新。
  - 后端变更：
    - `backend/internal/user/service/metabolic.go` 新增 Mifflin BMR、双公式平均 BMR、稳定目标与宏量分配。
    - `backend/internal/user/service/user_service.go` 在健康档案保存时首次固化 `health_condition.dashboard_targets`，`PUT /api/user/dashboard-targets` 改为只保存长期基础目标，不再写 `user_daily_nutrition_targets`。
    - `backend/internal/home/service/dashboard_service.go` 不再读取每日目标表，不再做运动补偿；首页返回校准建议字段。
    - `backend/internal/home/repo/home_repo.go` 增加 14 天饮食记录天数和体重记录查询，用于校准建议。
  - 前端变更：
    - 首页目标设置改为“基础目标设置”，不再传 `target_date`。
    - 目标说明去掉运动补偿展示。
    - 14 天校准建议在目标设置弹层展示，点击“应用建议”后直接保存长期目标。
  - 验证：
    - 后端纯逻辑定向测试通过。
    - 首页相关 ESLint 通过。
    - `git diff --check` 通过，仅 CRLF warning。
    - 完整 sqlite 测试受 `CGO_ENABLED=0` 阻塞；微信开发者工具自动化端口 9420/3001 均连接失败，未能截图验证。

- 体检报告上传链路已先修主故障：多图上传现在不会再“传了三张但没识别结果”。
  - 根因确认：前端原先把多张报告图 URL 逗号拼接成一个 `imageUrl` 传给 `/api/user/health-profile/submit-report-extraction-task`，而 `backend/internal/user/service/analysis_task_service.go` 创建 `health_report` 任务时只落单个 `image_url`，没有把多图写入 `analysis_tasks.image_paths`。
  - 结果是 worker 虽然具备多图 merge 逻辑，但健康报告任务实际没有走上规范的多图输入链路；同时提交后也没有“识别中”占位写回，用户回看健康档案时像是“啥都没发生”。
  - 已改动：
    - `src/packageExtra/pages/health-profile/index.tsx`
    - `src/packageExtra/pages/health-profile-view/index.tsx`
    - `src/utils/api.ts`
    - `backend/internal/user/domain/user_domain.go`
    - `backend/internal/user/service/analysis_task_service.go`
    - `backend/internal/user/handler/user_handler.go`
  - 现在体检报告图片上限已从 3 张提到 9 张；前后端统一走 `imageUrls` 数组；提交任务时会先把 `_image_urls` 写回 `health_condition.report_extract`，前端可显示“后台识别中”。
  - 验证：
    - `npx eslint src/packageExtra/pages/health-profile/index.tsx src/packageExtra/pages/health-profile-view/index.tsx src/utils/api.ts --max-warnings 0` 通过。
    - `cd backend && go test ./internal/user/handler -count=1` 通过。
    - `cd backend && go test ./internal/user/service -count=1` 仍被当前环境 `CGO_ENABLED=0` / `go-sqlite3 requires cgo` 阻塞。
    - `mrc where --port 9420` / `3001` 仍无法连接，未能做微信开发者工具截图验证。
  - 额外结论：PDF/Word 目前不只是前端没入口，后端 `backend/internal/user/service/ocr_service.go` 也仍是“把 URL 当图片送给视觉模型识别”，还没有真实文档解析能力；后续若要支持 PDF/Word，需要单独补后端文档解析链路。

- 用户提出想要“按 user_id 直接进入某个用户界面”以便 debug，而不是每次都走真实微信登录。
  - 已先完成方案判断：技术上能做，但不建议把公开 `/api/login` 做成通用 user_id 登录后门。
  - 现有项目已经有两块可复用基础：
    - `backend/internal/auth/service/login_service.go` 的 `testOpenid` 开发态模拟登录；
    - `/test-backend` + `/api/test-backend/login` + `RequireTestBackendCookie()` 的内部调试链路。
  - 推荐落地方式是：仅开发环境开放、先经过 test-backend 内部认证、再按 `user_id` 代签 JWT 进入目标用户。
  - 这样既满足排障效率，也把误开到线上或被任意人滥用的风险压低。

- 已按上面的方案完成首版“调试代登录”实现。
  - 后端：
    - `backend/internal/testbackend/service/testbackend_service.go` 新增 `ImpersonateUser(...)`，按 `user_id` 查询 `weapp_user`，仅开发环境允许，成功后签发该用户 JWT。
    - `backend/internal/testbackend/handler/testbackend_handler.go` 新增 `POST /api/test-backend/impersonate-user`。
    - `backend/internal/app/app.go` 把该接口挂到 `RequireTestBackendCookie()` 保护下。
  - 前端：
    - `src/packageExtra/pages/login/index.tsx` 开发环境新增“调试代登录”入口与弹层。
    - `src/utils/api.ts` 新增 `debugImpersonateUser(userId, password)`，内部先登录 `test-backend`，再发起代登录。
  - 当前使用方式：
    - 在开发环境登录页点“调试代登录”；
    - 输入目标 `user_id`；
    - 输入 `TEST_BACKEND_PASSWORD` 对应密码（若未设置环境变量，后端逻辑仍兼容默认测试密码）。
  - 静态验证已过；运行时截图验证仍被微信开发者工具自动化端口未开启阻塞。

- 用户询问“怎么快速下载某个用户某个时间段的饮食记录图片”。
  - 已对齐当前仓库真实模型：主记录表是 `user_food_records`，任务表是 `analysis_tasks`，饮食图片字段是 `image_path` / `image_paths`，时间字段优先用 `record_time`，建档时间可参考 `created_at`。
  - 当前后端会把 food 图片统一按 `food-images` bucket 解析为 CDN URL；批量下载时可直接查询 `user_food_records`，并在 `image_paths` 为空但 `source_task_id` 存在时回查 `analysis_tasks.image_paths` / `image_url` 兜底。
  - 适合的无代码最快路径是：SQL 导出 URL 列表 -> `aria2c` / `wget` 并发下载。

- 已按用户提供的 `openid=oe4xm19ESWlWXHP2A2GC4Xxyi0JE` / `user_id=6f0b67e5-f3b1-4b38-b3fc-081d931d8ef1` 确认目标账号为昵称“饭饭”。
  - 直接连接当前 `backend/config.yaml` 指向的 PostgreSQL，查询该用户最近 10 天的 `user_food_records`，并用 `analysis_tasks` 做图片兜底回填。
  - 共命中 48 条记录，汇总出 60 个唯一图片 URL，已全部成功下载到 `D:\files\downloads\foodlink_fanfan_last10days`。
  - 同目录生成了 `meta.json`（记录、时间、URL、下载结果明细）和 `urls.txt`（一行一个 URL），便于后续复核或再次批量处理。
