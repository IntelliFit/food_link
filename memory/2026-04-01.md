# 2026-04-01

- 已完成精准模式这一轮核心重构，旧的单次 `strict` 已被替换成“精准会话”链路：
  - 新增数据库结构：
    - `backend/database/precision_sessions.sql`
    - `backend/sql/add_precision_sessions.sql`
    - `precision_sessions / precision_session_rounds / precision_item_estimates`
  - 后端主链路：
    - `backend/main.py` 的图片/文字 `strict` 提交不再直接走最终识别，而是先创建/续用精准会话并投递 `precision_plan`
    - 新增 `/api/precision-sessions/{session_id}/continue`
    - `backend/worker.py` 新增 `precision_plan / precision_item_estimate / precision_aggregate`
    - 支持 `needs_user_input / needs_retake / estimating / done`
    - 多主体会拆成子任务并行估计，再聚合为最终结果
  - 前端：
    - `src/pages/analyze/index.tsx` 增加参考物录入和继续精准拍照
    - `src/pages/analyze-loading/index.tsx` 支持跟踪 `redirectTaskId`
    - `src/pages/result/index.tsx` 支持展示追问/重拍/待补充项，并可直接继续精准模式
    - `src/utils/api.ts` 增加精准会话与参考物 API 类型
- 本轮本地校验：
  - `ReadLints` 无新增错误
  - `python -m py_compile backend/main.py backend/worker.py backend/run_backend.py` 通过
  - `npm run build:weapp` 通过
- 运行态验证阻塞：
  - 已按要求尝试 `weapp-devtools`
  - `mrc where --port 9420` / `mrc errors 20 --port 9420` 都因 `ws://localhost:9420` 无法连接失败
  - 所以下次若要继续验证精准模式 UI，前置条件仍是先打开微信开发者工具自动化端口

- 用户继续追打首页体重/喝水的产品逻辑，不只是 UI：
  - 质疑当前体重是否被强行限制为“每天只能记录 1 次”
  - 追问喝水记录的跨天逻辑是否会到第二天自动清零
  - 指出喝水弹层里快捷量是“自动执行但页面不关闭”，而自定义又需要点保存，交互不统一
  - 继续指出体重/喝水弹层底部按钮仍有一点被自定义 tabBar 挡住
- 已完成本轮收口：
  - 后端与数据库：
    - `backend/database.py` 里的体重写入已从“按日期 upsert 覆盖”改成“允许同日多条插入”
    - 新增 `client_record_id` 幂等方案，避免本地同步重复导入
    - `backend/main.py` 的 `body metrics` 汇总已改成：
      - `latest_weight / previous_weight` 取真实最近两条记录
      - `weight_entries` 用于趋势时按“每天最后一次”聚合
    - `backend/database/user_body_metrics.sql` / `backend/sql/add_user_body_metrics.sql` 已同步移除 `(user_id, recorded_on)` 唯一约束
  - 前端首页：
    - `src/pages/index/index.tsx`
      - 体重本地结构补 `clientId / recordedAt`
      - 本地保存体重不再覆盖当天旧值，而是新增一条记录
      - 首页体重摘要改为“最近一次 / 上一次”，不再走“今天 vs 昨天”
      - 体重按钮改成 `去记 / 再记`
      - 喝水弹层快捷量点完会立即记录并关闭；自定义输入仍点保存
      - 体重弹层文案改成“可随时补记，趋势默认取当天最后一次”
    - `src/pages/index/index.scss`
      - 体重/喝水弹层改成浮起式卡片，整体抬高到 tabBar 上方
      - `清空今天` 按钮加深可见性
- 本轮校验：
  - `python -m py_compile backend/main.py backend/database.py` 通过
  - `npm run build:weapp` 通过
  - 已再次尝试 `weapp-devtools`：
    - `mrc where --port 9420`
    - `mrc errors 20 --port 9420`
  - 当前阻塞：仍无法连接 `ws://localhost:9420`，未完成运行态截图与点击确认
- 后续落地提醒：
  - 上线前需要执行 `backend/sql/add_user_body_metrics.sql`
  - 然后重启后端
  - 真机/开发者工具里应重点复测“同日多次体重、快捷喝水自动关闭、弹层底部按钮不再被挡”

- 用户继续收口首页信息表达：希望首页 `今日餐食` 卡片也能直接看到当餐照片预览，而不是只在独立“当天记录”页里才有缩略图。
- 已完成落地：
  - `backend/main.py` 的首页接口 `/api/home/dashboard` 现会为每个餐次聚合代表图与 `image_paths`
  - `src/utils/api.ts` 的 `HomeMealItem` 已补 `image_path / image_paths`
  - `src/pages/index/index.tsx` / `src/pages/index/index.scss` 已把首页餐次卡片左侧改成“缩略图优先、图标兜底”，并支持多图角标与点击预览
- 本轮校验：
  - `python -m py_compile backend/main.py` 通过
  - `npm run build:weapp` 通过
  - 已尝试 `mrc where --port 9420`、`mrc errors 20 --port 9420`
  - 当前阻塞：仍无法连接 `ws://localhost:9420`，未拿到微信开发者工具里的首页运行态截图

- 用户已明确确认新的记录商业化方向：
  - AI 识别改为积分制，而不是继续按“每日次数限制”思路推进
  - 当前拟定口径：`标准分析 1 积分/次`、`精准分析 3 积分/次`、`新用户赠送 20 积分`
  - 计费范围仅限 `拍照记录` 与 `文字记录`
  - 用户同时明确要求新增 `手动记录` 模式，并且该模式必须免费
- 已结合实库报告确认：`public_food_library`、`food_nutrition_library`、`food_nutrition_aliases`、`food_unresolved_logs` 已经为“免费手动记录”提供了较好的现成数据基础。
- 当前产品判断：
  - 手动记录应走“公共食物库优先，标准食物营养词典兜底”的结构化搜索链路
  - 手动记录不应调用 AI，也不应消耗积分

- 用户提出新功能方向：希望支持“快速便捷记录食物保质期并提醒用户吃掉”。当前进一步要求输出更偏工程落地的方案，而不只是产品脑暴。
- 用户当前关注点：
  - 录入方式至少要支持手动录入
  - 需要评估包装食物识别究竟应以 OCR、用户自己打字，还是大模型为主
- 用户已进一步明确产品入口位置判断：该功能不应放首页，当前更适合放在 `我的` 页，且入口位置以“食探会员”卡片附近为优先参考。
- 用户继续明确收口：入口不应做成一整块摘要卡，而应改成和“健康档案 / 收藏餐食 / 饮食记录 / 附近美食”同层级的小图标入口；该小图标允许带右上角红色数字角标，表示快要过期或待处理食物数量。
- 本轮已先结合现有代码架构做评估：
  - 前端现有 `record` 页、首页摘要、轻量通知与后端 API 骨架可部分复用
  - 但“保质期库存/提醒”语义与 `user_food_records` 不同，工程上更适合新增独立表与提醒扫描任务，而不是直接塞进现有饮食记录表
- 已开始实现第一版手动录入 MVP：
  - `我的` 页会员卡下方新增“保质期提醒”摘要卡，点击进入独立管理页
  - 新增 `pages/expiry/index` 列表页，支持查看摘要、按紧急程度排序、标记“已吃完 / 已丢弃 / 恢复提醒”
  - 新增 `pages/expiry-edit/index` 编辑页，支持手动新增/编辑，内置牛奶/酸奶/水果/剩菜等快捷模板
  - 后端新增 `/api/expiry/dashboard`、`/api/expiry/items`、`/api/expiry/items/{id}`、更新状态接口
  - 新增数据库脚本：
    - `backend/database/food_expiry_items.sql`
    - `backend/sql/add_food_expiry_items.sql`
  - 已完成本地校验：
    - `python -m py_compile backend/main.py backend/database.py` 通过
    - `npm run build:weapp` 通过
  - 运行态验证阻塞：
    - 已尝试 `mrc where --port 9420`
    - 当前微信开发者工具自动化端口未开启，无法完成截图与点击验证

- 首页三大营养素：超额（>100%）时大数字与右侧百分比徽标同一行挤压重叠；已改为第一行只显示克数、第二行显示「当前/目标」与徽标，并略缩小字号；去掉 `overflow: hidden` 避免小数显示被裁断。
- 用户明确要求先把评论审核去掉，因为当前整体仍偏严格；已改为评论直接发布：
  - `backend/main.py` 的圈子评论与公共食物库评论接口不再创建 `comment_tasks`
  - 圈子评论改为接口层直接入库并同步写 `comment_received / reply_received` 通知
  - `src/pages/community/index.tsx`、`src/pages/food-library-detail/index.tsx` 去掉“已提交审核/审核中”的提交主链路
  - `src/utils/api.ts` 评论提交返回结构从 `{ task_id, temp_comment }` 改为 `{ comment }`
  - 校验：`python -m py_compile backend/main.py` 通过，相关前端文件 `ReadLints` 无新增报错

- 已将全局 Cursor 的 `supabase` MCP 改为固定 scope 到 `food_link` 当前项目 `ocijuywmkalfmfxquzzf`：
  - 配置文件：`C:\Users\29454\.cursor\mcp.json`
  - 启动参数新增 `--project-ref ocijuywmkalfmfxquzzf`
  - 目的：避免 MCP 默认列出/读取到旧的其他 Supabase 项目，后续数据库结构排查默认只针对 `food_link`

- 已直接基于 `food_link` 实库 `ocijuywmkalfmfxquzzf` 的 `rest/v1` OpenAPI schema 输出正式数据库报告：
  - 文档：`docs/数据库实库Schema分析报告.md`
  - 真实 `public` schema 共确认 `32` 张表
  - 当前最清晰的现行主链为：`weapp_user -> analysis_tasks -> user_food_records -> public_food_library`
  - 已明确识别的高疑似旧表包括：`food_analysis_records`、`meals`、`meal_items`、`staple_meals`、`critical_samples`
  - 已确认线上存在但仓库迁移缺失的表：`public_food_library_likes`

- 已核对“多视角辅助模式”当前状态：
  - 前端分析页仍保留入口：`src/pages/analyze/index.tsx` 支持最多 3 张图，并提交 `is_multi_view`
  - 后端异步分析提交接口 `backend/main.py /api/analyze/submit` 会透传 `is_multi_view`
  - Worker `backend/worker.py` 会在 prompt 中明确告诉模型“这些图片是同一份食物的不同视角，不要当成多份”
  - `npm run build:weapp` 于 `2026-04-01` 本地通过
  - 本轮未完成 DevTools 运行态验证：当前 shell 环境里 `mrc` 命令不可用，无法直接连微信开发者工具做截图/点击确认

- 用户继续指出一个关键产品语义问题：当前无论是否开启“多视角辅助模式”都能上传多张图片，这会让“多张图 = 多份食物”与“多张图 = 同一食物不同角度”两种语义混在一起。
- 已复核代码确认：
  - 正式接口 `/api/analyze/submit` 当前没有限制“多张图必须开启多视角”
  - `is_multi_view` 在正式链路里只影响 worker prompt 的一句提示词，不是硬校验
  - 只有测试后台专用接口才会在“多图但未勾多视角”时直接返回 `400`
- 当前结论：
  - 你看到的现象不是误会，而是实现上的真实行为
  - 现在系统区分“同一食物多角度”与“不同食物多张图”的主要方式，实际上还是靠用户是否勾选开关 + 模型自行理解画面，并没有稳定的规则层约束

- 用户确认采用“严格方案”：
  - 未开启多视角模式时，只允许上传 `1` 张图片
  - 多张图片仅用于“同一份食物的不同视角”
  - 页面需要明确提醒“如果要拍多视角，请先开启多视角模式”
- 已完成落地：
  - `src/pages/analyze/index.tsx`
    - 单图模式下继续加图会弹提醒，不再允许直接选第 2 张
    - 已有多图时尝试关闭多视角，也会被拦住并提示
    - 分析前再次兜底校验，避免旧状态绕过
    - 占位文案与开关说明已同步改成严格口径
  - `backend/main.py`
    - 正式接口 `/api/analyze/submit` 新增硬校验：`image_urls > 1 && !is_multi_view` 时直接返回 `400`
- 本次验证：
  - `npm run build:weapp` 通过
  - `python -m py_compile backend/main.py` 通过
  - 已找到本机 `mrc`，并尝试 `mrc where --port 9420` / `mrc errors 20 --port 9420`
  - 当前仍无法连到微信开发者工具：`ws://localhost:9420` 连接失败，说明 DevTools 未开启自动化端口或未打开目标项目

- 用户新提出体重记录、喝水记录的信息架构诉求：
  - 不建议放进“健康档案/健康板块”这类二级入口
  - 更希望做成用户打开就能直接看到、顺手可记的首页级入口
  - 这意味着该功能后续若落地，应更偏“今日状态/快捷记录”而不是“档案设置项”

- 用户反馈圈子评论当前有明显交互 bug：
  - 点击回复时输入框不好找
  - 回复态页面会出现上下波动、很乱
- 已定位到两层前端问题：
  - 回复提示条 `comment-reply-tip` 和输入框之前被塞在同一个横向 flex 行里，回复态下很容易把输入框挤没
  - 底部评论栏使用了 `Textarea + fixed + autoHeight` 组合，在小程序里很容易和键盘顶起产生抖动
- 已完成修复：
  - `src/pages/community/index.tsx`
    - 底部评论输入改为 `Input`
    - 结构改成“上方回复提示条 + 下方输入框/发送按钮”
    - 打开/关闭评论栏时显式重置 `commentInputFocus`
  - `src/pages/community/index.scss`
    - 起初已把评论栏改成更稳定的 composer 结构
    - 随后根据用户截图继续确认：项目是 `custom tabBar`，评论栏实际被 tabBar 挡住，只露出一条上沿
    - 因此又把评论栏和蒙层整体上移到 tabBar 上方
    - 输入框改为单行圆角输入，不再依赖 `Textarea autoHeight`
- 验证：
  - `npm run build:weapp` 三次通过
  - 已尝试 `mrc where --port 9420`、`mrc errors 20 --port 9420`
  - 当前仍无法连接到微信开发者工具自动化端口 `9420`，所以这轮没有拿到“点回复”的运行态截图

- 已完成首页第一版“今日身体状态”实现：
  - 位置：`src/pages/index/index.tsx` 日期条下方、热量卡上方
  - 结构：并列两张首页一级卡片
    - `今日体重`：显示今日/最近一次体重、较昨日变化、最近 7 天迷你趋势，点击卡片拉起记录弹层
    - `今日喝水`：显示今日累计和目标进度，支持 `+250 / +500` 快捷加水，以及“更多”弹层中的自定义输入和清空今日
  - 数据层：当前先用本地 storage 持久化；若健康档案已有体重，会作为体重弹层默认值来源
  - 样式：沿用首页现有白卡、圆角、轻阴影、绿色主色语言，没有另起一套视觉
  - 构建：`npm run build:weapp` 于 `2026-04-01` 本地通过
  - 运行态验证阻塞：
    - 已尝试 `mrc where --port 9420`
    - 已尝试 `mrc errors 20 --port 9420`
    - 两者都因无法连接 `ws://localhost:9420` 失败，说明当前微信开发者工具未开启自动化端口或未打开可连接目标，暂未拿到截图和点击验证结果

- 用户继续明确指出：上一版即使缩小了，方向仍然不对；问题不是“卡片再小一点”，而是“不要再做成大模块”，按钮要非常非常小，接近甚至小于三大营养素卡里的小徽标量级。
- 已据此再次重做首页：
  - `src/pages/index/index.tsx`
    - 删除体重/喝水双卡式结构
    - 改成热量卡下方的一块极轻量快捷摘要条，两行分别是体重与喝水
    - 体重只保留状态文本 + 很小的 `去记/修改` 按钮
    - 喝水只保留累计文本 + 很小的 `+250 / +500 / 更多` 按钮
    - 首页不再展示体重趋势图、喝水进度条、大面积留白
  - `src/pages/index/index.scss`
    - 整体压成更薄的容器和更小的胶囊按钮，尽量避免喧宾夺主
  - 构建：`npm run build:weapp` 再次通过

- 用户反馈：记录体重和记录水还是太大了，希望做得更精简、位置更小。
- 已将快捷条改为并排胶囊卡片：
  - `src/pages/index/index.tsx`
    - 移除原本上下两行的快捷条结构，改为一行内两列并排的小卡片（体重、喝水并排同行）。
    - 喝水快捷操作只保留 `+250ml`，以节省横向空间，点击面板其余区域依旧可调起全量菜单。
  - `src/pages/index/index.scss`
    - 将容器调整为极简的 padding 和字号，进一步压缩了整体所占用的垂直面积。
  - 构建：即将验证 `npm run build:weapp`。

- 用户明确追问：体重/喝水不只要首页 UI，还必须支持跨设备同步（上云）并纳入统计页长期分析；指出此前这部分没有真正实现，这个判断成立。
- 已完成本轮补齐：
  - 后端新增身体指标数据层：
    - `backend/database.py` 新增 `user_weight_records / user_water_logs / user_body_metric_settings` 的读写函数
    - 新增数据库脚本：
      - `backend/database/user_body_metrics.sql`
      - `backend/sql/add_user_body_metrics.sql`
  - 后端新增接口：
    - `GET /api/body-metrics/summary`
    - `POST /api/body-metrics/weight`
    - `POST /api/body-metrics/water`
    - `POST /api/body-metrics/water/reset`
    - `POST /api/body-metrics/sync-local`
    - `GET /api/stats/summary` 已返回 `body_metrics`
  - 前端首页：
    - `src/pages/index/index.tsx` 现在优先读写云端
    - 若云端接口不可用，则回退到原本 local storage，保证当前版本不崩
    - 登录后会尝试把旧的本地体重/喝水快照同步到云端
  - 前端统计页：
    - `src/pages/stats/index.tsx`
    - `src/pages/stats/index.scss`
    - 新增“体重与喝水”长期趋势区块
- 本轮校验：
  - `python -m py_compile backend/main.py backend/database.py` 通过
  - `npm run build:weapp` 通过
  - 已尝试 `mrc where --port 9420`
  - 当前阻塞：仍无法连接 `ws://localhost:9420`，未完成 DevTools 截图与点击验证

- 用户认为体重/喝水放在热量与营养素之间会打断「摄入」叙事，希望放在蛋白质等三大营养素下面，让热量与营养素靠在一起。
- 已调整首页顺序：`src/pages/index/index.tsx` 中 `body-status-section`（体重/喝水并排卡）已移到 `macros-section`（蛋白质/碳水/脂肪）之后、「今日餐食」之前。

- Supabase Storage 清理与优化：
  - 确认当前实际对象总大小约 307 MB（API 精确统计），但 Dashboard Usage 显示 1.07 GB
  - 差值原因：之前批量 upsert 压缩的旧大文件版本可能在 S3 层形成孤儿，需等 GC 或联系 Supabase 支持
  - 已通过 `backend/cleanup_orphans.py` 识别并删除 110 个孤儿文件（13.51 MB），全部通过 Storage API 正确删除
  - 已新增 `backend/image_compressor.py`：独立的图片压缩模块（max_edge=1280, JPEG q=62, WebP q=58）
  - 已在 `backend/worker.py` 的 `process_one_food_task` 中集成：分析完成后自动压缩原图，失败不影响主流程
  - 这样后续新增图片在分析完成后会立刻被压缩，不再累积大文件

## 手动记录模式 MVP 实现

- 已完成"手动记录"功能的全栈 MVP 开发
- 核心改动：
  - `backend/database.py`：新增 `search_manual_food()` 统一搜索公共食物库 + 标准食物营养词典 + 别名表
  - `backend/database.py`：新增 `log_unresolved_food()` 记录未命中搜索词到 `food_unresolved_logs`
  - `backend/main.py`：新增 `GET /api/manual-food/search?q=&limit=`（无需登录）
  - `src/utils/api.ts`：新增 `ManualFoodSearchResult` 接口 + `searchManualFood()` 函数
  - `src/pages/record/index.tsx`：记录页第三种方式"手动记录"，含搜索栏、结果列表、已选食物重量调整、餐次选择、一键保存
  - `src/pages/record/index.scss`：橙色渐变主题 + "永久免费"徽标
- 搜索逻辑：先搜 `public_food_library`（优先），再兜底 `food_nutrition_library + food_nutrition_aliases`
- 营养计算：词典来源按 per_100g 等比缩放，公共库来源按总量等比缩放
- 保存复用现有 `saveFoodRecord` API，description 标注"手动记录"
- TypeScript 类型检查通过，无新增错误

## 首页喝水弹层

- 快捷量（+100/250/500/750）改为累加到「自定义饮水量」输入框，与手输一致；主按钮为「确认记录」，点后才写入今日饮水并关弹层。`npm run build:weapp` 已通过。
