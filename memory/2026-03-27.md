# 2026-03-27

- 修复“文字记录模式二次纠错会误带上历史照片”的前端链路：
  - `analyze-loading` 在 `food_text` 完成时同时清空 `analyzeImagePath` 和 `analyzeImagePaths`
  - 新增 `analyzeTaskType` 缓存，`result` 页据此区分纯文字/图片任务，避免旧图残留影响重提与保存
  - `result` 页无实物图时展示产品 logo，占位文案为“未提供实物照片”
  - `record-detail` 无实物图时也展示产品 logo，避免记录详情误像上一餐照片
- 本次未完成运行验证：
  - shell 内无 `mrc`
  - shell 内无 `npm`
- 建议下次优先在微信开发者工具自动化环境中验证链路：文字记录 -> 二次纠错两次 -> 保存记录 -> 打开记录详情
- 新增编译修复：
  - 处理 `friendRemove is not exported by src/utils/api.ts` 报错
  - 在 `src/utils/api.ts` 增加兼容导出 `friendRemove = friendDelete`
  - 结论：这是接口命名重构后的兼容缺口，非业务逻辑错误
- 新增编译修复（第二处）：
  - 处理 `requestFriendByInviteCode is not exported by src/utils/api.ts` 报错
  - 在 `src/utils/api.ts` 增加兼容函数 `requestFriendByInviteCode(code)`，内部调用 `acceptFriendInvite(code)`
  - 兼容层将返回状态从 `request_sent` 映射为历史登录页需要的 `requested`
- 新增运行时崩溃修复：
  - 处理小程序启动报错 `ReferenceError: process is not defined`（`api.ts:7`）
  - 修改 `src/utils/api.ts`、`src/pages/location-search/index.tsx`、`src/pages/record-detail/index.tsx` 的环境变量读取，统一加 `typeof process !== 'undefined'` 守卫
  - 验证结果：
    - `npm run build:weapp` 成功，`dist/app.json` 正常生成
    - `mrc errors 50 --port 9420` 为 0
    - `mrc` 导航检查通过（`switchTab/where/stack`）
    - 截图保存：`verify.png`
- 新增二次纠错运行时错误修复：
  - 用户复现场景：文字模式下“二次分析纠正”点击“重新智能分析”时报错 `Cannot access 'taskType2' before initialization`
  - 根因：`src/pages/result/index.tsx` 中 `handleSubmitCorrection` 内部对 `taskType` 发生同作用域重复声明，触发 TDZ
  - 修复：将跳转参数局部变量改名为 `nextTaskType`
  - 验证：
    - `npm run build:weapp` 通过
    - `mrc where --port 9420` 显示 `pages/result/index`
    - `mrc errors 30 --port 9420` 为 0
- 用户偏好更新：
  - 之后优先“只改代码”，截图类自动化验证由用户自行执行
- 新增首页运行时错误修复：
  - 用户报错：`TypeError: taro.useState is not a function or its return value is not iterable`，定位到首页 `src/pages/index/index.tsx`
  - 处理方式：
    - 首页 hooks 改为 `React.useState/useEffect/useCallback`
    - `Taro.useDidShow` 改为命名导入 `useDidShow`
    - `src/components/CustomNavBar/index.tsx` 的 `useState` 也改为 `React.useState`
  - 本次未做自动化验证，按用户要求由用户自行验证
