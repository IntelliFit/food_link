# 2026-04-05

- 移除个人页服务网格中的测试入口 `Pro会员`，仅保留顶部“食探会员”卡片展示。
- 修改文件：`src/pages/profile/index.tsx`
- 验证情况：
  - `npm run build:weapp` 未拿到明确成功结果，当前仅看到 Taro/Rust `system-configuration` panic 输出
  - 尝试使用 `mrc` 做微信开发者工具自动化验证，但当前环境缺少 `mrc` 命令

- 新增食物保质期管理功能 V1：
  - 我的页新增 `食物保质期` 入口
  - 首页新增“快到期食物”摘要卡片
  - 新增 `pages/food-expiry` 列表页和 `pages/food-expiry-edit` 编辑页
  - 后端新增 `user_food_expiry_items` 表、CRUD 接口和首页 `expirySummary` 聚合字段
- 关键文件：
  - `backend/database/user_food_expiry_items.sql`
  - `backend/database.py`
  - `backend/main.py`
  - `src/utils/api.ts`
  - `src/pages/profile/index.tsx`
  - `src/pages/index/index.tsx`
  - `src/pages/index/index.scss`
  - `src/pages/food-expiry/index.tsx`
  - `src/pages/food-expiry-edit/index.tsx`
  - `src/app.config.ts`
- 验证情况：
  - `python -m py_compile backend/main.py backend/database.py` 通过
  - 目标文件 `eslint` 通过
  - `npm run dev:weapp` 仍被 Taro/Rust `system-configuration` panic 阻塞，暂未拿到正常 watch 编译完成结果

- 修正 `pages/food-expiry-edit` 编辑页输入框占位文字被遮挡：
  - 原因倾向于小程序原生 `Input` 直接承载 `padding/border` 样式导致占位文本裁切
  - 已改为外层 `form-input-wrap` 承载边框与内边距，`Input` 本体只保留文字尺寸与行高
  - 并补充 `placeholderClass='food-expiry-input-placeholder'`

- 调整首页“快到期食物”展示规则：
  - 只有 `expirySummary.pendingCount > 0` 时才显示该卡片
  - 若用户未设置任何待处理食物，则首页不展示保质期模块，也不展示空状态文案

- 修复食物保质期修改后首页不即时更新：
  - 新增前端事件 `food-expiry:changed`
  - 新增/编辑/删除/标记已吃完成功后主动触发该事件
  - 首页 `pages/index/index` 监听事件并立即重新拉取 `loadDashboard()`

- 调整新增交互：
  - 在 `pages/food-expiry-edit` 新增模式下，点击 `添加食物` 后先弹确认框
  - 用户确认后才真正创建保质期项；编辑模式保持直接保存
