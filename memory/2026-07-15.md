# 2026-07-15

- 17:30 本地后端诊断：`npm run dev:backend` 实际通过 `backend/apollo-config.yaml` 拉取 Apollo `main/app-config.yaml`，并非本地开发配置。main 配置中的 Kafka broker `kafka:9092` 只在部署网络可解析；Windows 本机累计 416 次 DNS 失败。拍照图片上传与数据库任务创建均成功，但 Kafka 入队 2 秒超时，`/api/analyze/submit` 返回 500，模型尚未被调用。同步 API 多数为 200，因此本地“普通功能可用、异步 AI/拍照不可用”由队列环境错配解释。Apollo main 同时带来 `app.env=production`、16 workers、OTel exporter 超时及连接 main 数据库/COS/Redis 的数据污染风险。体验版走 `dev.api.healthymax.cn` 的部署后端，故可使用部署内 Kafka。
- Apollo 只读复核确认已有 `local/app-config.yaml`：development、1 worker、memory queue、无 brokers、OTel 关闭，并具备数据库/COS/LLM/微信配置；数据库与存储仍和 main/dev 相同，切换后测试数据仍会进入共享资源。当前加载器忽略 `.env` 内 `APOLLO_*`，实际 cluster 只取 `apollo-config.yaml`。最小修复是本机 YAML 改 `cluster: local` 且使用最终 HTTPS Apollo URL；长期可按 CoachLink 把 Apollo bootstrap 统一迁到 `.env`。本轮未改运行配置。
- Apollo main/dev 键集合复核：dev 少旧 Supabase URL/key（Go 运行时已无消费者）和微信代扣 V2 key、签约/扣款回调 3 项；AI、DB、COS、基础微信配置未漏。体验版若缺自动续费可由 Apollo dev 配置解释，其他功能需按具体接口和 dev 日志排查。当前 Apollo HTTP bootstrap 会 301 到 HTTPS，也应改用最终 HTTPS 地址。
- 用户切到 Apollo local 后，memory queue 和本地 worker 已正常：上传、入队、认领全部成功。新拍照任务在 Gemini 请求前约 1ms 失败，报图片必须为有效 HTTPS。只读核对 Apollo local 发现 `storage.food_images_cdn_base_url=http://cdn-food-images.coachlink.fit`，BuildAccessURL 因而返回 HTTP；Gemini 客户端明确拒绝非 HTTPS。需在 Apollo local YAML 把 food-images CDN 改成 `https://...`，并建议同步修正 avatar/icon CDN。Apollo namespace 使用 YAML 的 `key: value`；冒号必需、字符串引号建议使用但不是此次根因；`.env` 使用 `KEY=value`。本轮未改 Apollo 或代码。

- 已实现并提交 Gemini 视觉性能优化：共享 HTTP Transport、4 秒 TLS 建连上限、Gemini 重试复用已下载/编码的图片请求体、一次短重试后 12 秒 Qwen 回退；可食部和建议比例后处理仅在需要时调用，并关闭该短文本调用的思考模式。已同步本地及远端 main/dev；会话附件未提交。

- 已提交并同步保质期识别修复：任务完成结果改用 `datatypes.JSONMap` 写 PostgreSQL JSONB；识别器改复用精准分析编排，避免 Gemini 重试重复下载/编码图片，并统一 Gemini 重试/Qwen 回退。本地及远端 main/dev 已一致；会话附件目录 `.codex-remote-attachments/` 不提交。

- 用户确认 AI 营养热量先采用简化规则：以蛋白质、碳水、脂肪为最终依据，按 4/4/9 单向重算 calories；暂不做热量与营养素双向靠拢，也不因为差异较大调用 Gemini 重新估算。现有代码已经符合，无需继续改动。
- 只读核验 main/dev 一致性：fetch 后本地 main/dev 与 origin/main 同为 `fd896ef5`，但 origin/dev 已到 `c4f410e7`，比 main 多隐私收藏可见性、分析收藏去重 2 个提交；当前工作区还有 11 个已跟踪修改和 4 个未跟踪入口。因此当前没有保证主分支与开发分支一致。本轮未合并、提交、切换或推送。
- 进一步确认两个远端新增提交由 ma-yuqin0603 直接提交到 dev，未关联 PR；main 是 dev 的祖先，可无冲突快进。fetch 只刷新 origin/dev，不会自动更新本地 dev。仓库当前套餐不能启用私有仓库分支保护/规则集，且没有启用自动同步 workflow，所以远端 main/dev 的一致性目前完全依赖人工流程。
- 用户确认采纳远端 dev 的收藏隐私与分析收藏去重修改。已保护并恢复当前未提交营养代码，将本地/远端 main、dev 全部纯快进同步到 `c4f410e7`；`git ls-remote` 与 divergence 复核均一致。恢复时只合并了 memory 双方追加内容，10 个业务修改文件与保护前 stash 完全一致，仍保持未提交/未暂存。
# 2026-07-15（体验版首页 timeout 诊断）

- 用户反馈体验版只有首页数据卡在占位 spinner，其他页面正常，本地开发者工具偶见 `Error: timeout`。
- 本地后端日志证明首页相关接口均 200 且在 0.6 秒内完成；trial 构建地址、运行时 envVersion 选择、构建产物和 api-base-url 单测均正常，dev API 公网健康检查也正常。
- 根因定位到首页 `loadDashboard` 的强耦合 `Promise.all`：主 dashboard 被 stats、body-metrics、exercise 辅助请求共同阻塞，任一路 timeout 都使整页继续 loading；body-metrics 30 秒超时后还重试一次，最坏约 60 秒。其他页面不走这组并发聚合，所以呈现为首页独有。
- 当前超时堆栈没有请求 URL，无法仅凭现有材料精确点名；home-dashboard/stats 超时会让聚合失败，body-metrics 超时会因重试长期阻塞。需体验版请求诊断或 dev 后端对应时刻日志。weapp-devtools 9420 在尝试截图/导航时关闭，未擅自重启工具，故无新运行时截图。此次仅诊断，未改业务代码。
- 用户随后要求立即修复以便当天上线。已将首页主 dashboard 与 stats/body-metrics/exercise 解耦：主接口成功后立即写状态、缓存并关闭 loading，三个辅助请求继续后台合并，单路超时不再卡住主卡片。定向 ESLint、全量 Jest 17/17 suites（64/64 tests）和 diff-check 通过；全量 tsc 仍只有既有 day-record `image_path: null` 错误。9420 实际连接到其他小程序，food_link 专用 3001 未开启，故无法取得本项目运行时截图；未运行开发阶段禁止的 production build。
- 用户再次提醒必须保持 `main/dev` 一致。只读核验：当前分支 `dev`；本地 main/dev、origin 跟踪引用和远端实时 main/dev 均为 `c4f410e7`，divergence 0/0。当前首页及后端营养等修改仍是未提交工作区内容，后续发布时须让两条分支落到同一最终提交并分别推送核验。
- 用户切到 main 做上传前构建后报 `body-trends/index.wxss` ENOENT 和 navigateTo timeout。确认不是首页修复引起：02:53 的 dist 在 65 个页面中仅缺 body-trends、my-vouchers 两份 WXSS，根因是这两个兼容跳转页虽有 index.scss，却没有在 TSX 显式导入。已为两页补 `import './index.scss'`；定向 ESLint、Sass、全量 Jest 17/17（64/64）及 diff-check 通过。旧 dist 仍需用户重新 `npm run build:weapp` 生成；food_link 自动化 3001 未开，运行时验证阻塞。
- 用户 03:10 在开发者工具中调试微信登录报 timeout。日志明确 envVersion=develop，Taro.login 已取得 code，public-config 和 `/api/login` 一起超时；本机 3010 无监听/后端进程，curl 为 connection refused，而 dev.api health 为 200。根因是 develop 按约定走 `127.0.0.1:3010` 且本地后端未启动，不是登录代码或 WXSS 修复。用户需自行 `npm run dev:backend` 后本地测试，或上传体验版在 trial 环境走 dev.api。03:06 新 dist 已确认两份缺失 WXSS 均生成成功。

- 保质期识别 03:29 的 502 已定位为万界 Gemini 原生 endpoint 的一次 TLS 握手超时，不是再次使用了错误 `messages` 协议。日志 URL 已是 `/api/v1beta/models/gemini-3.5-flash:generateContent`；代码组包为 `contents/parts/inlineData/generationConfig`。Go 默认 TLS 握手上限 10 秒，当前保质期路径无重试，所以瞬时网络抖动直接返回 502。后续 5 次连通探测 TLS 均约 0.07-0.10 秒并收到 HTTP 400，网络已恢复；DNS 为 `198.18.0.94` Fake-IP 风格，可能经本机代理/TUN，无法仅凭单次错误区分本机代理与万界入口责任。原生协议和 expiry 定向测试通过，本轮未改业务代码。
- 用户希望不重新发小程序，询问是否可只改/发 Go 后端。已确认可以：现有前端会解析标准响应 `message`，TLS 重试及友好错误映射无需前端改动。字面 `Internal Server Error` 不符合当前 expiry `AppError` 应返的“保质期识别服务暂时不可用”，说明对应线上请求可能经过旧后端镜像、panic 或网关层 500。计划仅在 expiry 链路对可重试网络错误短退避重试 1 次，两次失败仍返标准中文 AppError；待用户明确要求实施。
- 用户明确要求立即实施，且只改保质期核心后端。已在 expiry Recognizer 对 TLS handshake timeout、EOF/连接中断、network timeout 和 Gemini 500/502/503/504 加 1 次、300ms 短退避重试；400 协议错误和 context cancel 不重试。只修改 `backend/internal/expiry/service/recognizer.go` 及其测试，前端、共享 LLM 客户端、其他模型链路均未改。expiry 全包、原生 Gemini 定向、app 编译、vet 与 diff-check 通过，待部署后端。已向用户明确：两试只能修复瞬时抖动，若每次都是 TLS 超时，则是持续出站网络/代理故障，不能保证靠重试解决。
- 04:32 新日志确认两次 Gemini 3.5 原生请求均 TLS handshake timeout，总耗时 22.93s，因此单纯重试方案不足。用户原要求切换千问，随后要求改前先解释拍照营养为何可用，故 Qwen 切换暂未实施。实际差异：快速拍照用 Qwen 3.6，普通拍照用 Gemini 3 Flash Preview，精准拍照才用 Gemini 3.5；精准分析有通用瞬时错误重试且部分阶段会回退 Qwen，保质期则是独立同步直连 Gemini 3.5，所以界面显示的“Gemini 拍照可用”不等于保质期所走的同一实际请求成功。
- 应用当前万界 key 对 Gemini 3.5 做实时验证：原生 curl 连续 3 次 HTTP 200，返回 `modelVersion=gemini-3.5-flash`，TLS 0.076-0.104s，总耗时 1.45-1.86s；项目真实 `NewOfoxAIClient`/Go `net/http` 连续 3 次也均完成网络调用，2 次约 1.9s 完整解析成功，1 次是模型 JSON 输出不规范导致 parse error，不是 TLS。证明 Gemini 3.5/密钥/原生协议当前可用，先前双 TLS 失败为短时网络路径问题。建议保质期继续使用 Gemini 3.5 并补 JSON/瞬时错误容错；是否将快速 Qwen、普通 Gemini 3 也全部改成 Gemini 3.5 需用户明确，因为会改变现有速度与成本策略。
- 用户纠正：从未要求所有通路统一 Gemini 3.5；真实要求是保质期主用 Gemini 3.5，实在失败才 Qwen 3.6 兜底。用户同期实测拍照精准 3.5 成功、保质期 3.5 失败，因此“同一时间窗口整体网络不通”诊断已撤回。代码核对确认本地两者同进程并共用 `gemini35Client`，真实差异是 expiry 绕过了拍照精准的 `AnalyzeService` 容错编排。已将 expiry Recognizer 改为复用 `RunPrecisionJSONWithImagesTemperature`，主模型仍 `gemini-3.5-flash`，它多次瞬时/解析失败后才回退 `qwen3.6-flash`；删除 expiry 自建重试代码，不改前端或其他识别通路。保质期定向测试、app 编译和 diff-check 通过，待重新部署后端。
# 2026-07-15（提交修改并同步 main/dev）

- 用户进一步明确：必须提交当前有效修改，再合并/同步主开发分支，并且不要运行任何测试。
- 已清理根目录误生成的 `ddd`、`wq!` 分支列表文件；会话附件目录 `.codex-remote-attachments/` 不纳入版本库。
- 营养热量校准、保质期识别、首页加载解耦、兼容页面样式导入和相关测试代码已提交为 `b992089a fix: 完善营养校准与首页加载`。
- 本地 `dev` 已快进到该提交，并使用 `git push --atomic origin main:main dev:dev` 同时更新远端 `main`、`dev`。按用户要求本轮未运行测试。
# 2026-07-15（保质期回退落库修复）

- 05:06 保质期日志证明主模型 `gemini-3.5-flash` 的前两轮失败在后端下载 CDN 图片 TLS，第三轮失败在万界 `generateContent` TLS；`qwen3.6-flash` 随后回退成功并生成“青番茄”。
- 页面仍报失败是独立的数据库持久化 bug：保质期 `CompleteTask` 直接把 Go map 写入 PostgreSQL JSONB，触发 `cannot find encode plan`，接口最终返回 500。
- 已把保质期任务结果包装为 `datatypes.JSONMap` 后落库，只改后端保质期仓储；定向 repo 测试与 diff-check 通过，待后端重启/部署验证。
- 完整 `backend/logs/food-link-backend.log` 补充定案：04:59 的实物照片 Gemini 首次请求同样发生万界 TLS handshake timeout，第二次成功；05:07:53 的另一条保质期 Gemini 又直接成功。两条通路共用原生协议和 CDN URL，区别只是各次 TLS 连接结果，不存在保质期专属调用格式错误。持续的前端失败由 JSONB 落库 bug 造成，而非 Gemini 是否最终识别成功。

# 2026-07-15（Gemini 3.5 完整耗时拆解）

- 新完整日志确认当前保质期已经成功：图片上传约0.60秒；识别请求先后在 CDN GET、万界 POST 各等待约10.4秒并发生 TLS 握手超时，第三轮 Gemini 约11.0秒成功，`/api/expiry/recognize` 最终 HTTP 200/31.84秒，未回退 Qwen。这也证明 JSONMap 落库修复已在当前运行后端生效。
- 精准实物任务同样先后发生 CDN 与万界 TLS 超时，第三轮 Gemini 成功，视觉阶段约33.81秒且 `fallback_used=false`。Gemini 后又串行调用 Qwen 3.6 做可食部比例约12.46秒、营养库精确查询约29ms、Qwen 3.6 建议比例约11.21秒；从上传到前端轮询见到完成约62.82秒。
- 因此“Flash 为什么一分钟”答案是：成功的 Gemini+图片准备单轮约11-13秒，约20.8秒来自两次 TLS 超时，实物额外约23.7秒来自两次串行 Qwen 后处理；不能把一分钟当作纯 Gemini 推理时间。
- 代码确认 Gemini 原生请求在每次重试内重新下载 CDN 图片并构造 inlineData，默认 Transport 的约10秒 TLS 握手窗口与日志一致。可选后续性能改造优先复用单次下载结果，再考虑一次 Gemini 重试后早回退和减少串行后处理；本轮仅诊断，未实施新的性能修改。

# 2026-07-15（视觉识别后端提速实施）

- 用户明确要求大幅缩短不必要等待。已只改Go后端分析服务：共享LLM HTTP连接池，TLS握手上限4秒；Gemini单次逻辑调用复用已下载/编码图片；实时Gemini最多重试一次后在12秒内回退Qwen；原生Gemini 408/429/5xx纳入瞬时错误。
- 实物图片主链路此前没有真正的Qwen视觉回退，本次已补齐；保质期继续复用同一precision编排，所以两处都获得相同快速失败策略，核心模型选择仍是Gemini 3.5优先、Qwen兜底。
- Qwen后处理关闭thinking，可食部/建议比例上限缩至8秒。普通完全可食食物直接使用100%确定性比例；无剩余热量或本餐未超预算时建议比例直接100%，不发模型请求。05:28绿圣女果样例原约23.7秒串行Qwen等待会被完全短路。
- 新增和更新定向测试后，分析service全包、expiry service/repo、app装配编译、相关vet及diff-check全部通过。没有前端修改，无需小程序重新发布；未重启/部署后端，实际线上耗时待部署后用同阶段日志核验。
# 2026-07-15（付费用户统计）

- 18:17 通过 Apollo local 配置所连接的共享 PostgreSQL 只读核对：当前有效真实付费会员为 24 人；历史累计真实付费用户为 35 人。口径排除测试套餐，当前有效需 `active`、未过期且有 `last_paid_at`；累计需存在金额大于 0 的 `paid` 支付记录。
- 18:21 按更严格的支付记录口径复核：`status=paid`、`paid_at` 非空、微信交易流水号非空，共 44 笔支付订单、36 位不重复付费用户，故有 8 笔为续费/重复购买。此前 35 人额外剔除了 1 位测试套餐用户（其有 2 笔实际支付），与“所有实际支付”的统计口径不同。

- 用户随后要求按“支付完成记录”而非用户去重重新复核。当前库严格以 `status='paid'`、`paid_at` 非空、微信 `wx_transaction_id` 非空统计：**44 条**；记录数与去重后的微信交易流水数均为 44，退款完成记录为 0。原始 `paid` 状态共45条，排除其中一条 `manual_admin / MANUAL_GRANT` 的0元赠送记录（无交易流水）。实付累计1262.45元，最近完成时间2026-07-15 11:33:38 +08:00。仅执行只读查询。

# 2026-07-15（今天吃什么推荐容错与来源说明）

- 定位到推荐接口同步查询公共食物库、用户历史记录、标准营养库候选后再调用 Qwen；前端只等45秒，原接口无独立小预算，模型/查询抖动会表现为 spinner 后失败。
- 已将推荐核心链路设为12秒总预算。模型请求超时、上游非2xx、空响应或JSON非法均自动用真实候选/规则兜底返回，避免把AI临时失败暴露给用户；新增结构化日志记录兜底原因。
- 页面增加“候选数据 + AI仅组合”的来源说明，并将 `food_nutrition_library` 翻译为“标准营养库”。后端定向测试和前端目标 ESLint 通过；开发者工具3001自动化端口未开启，未能截图验证。待后端部署及小程序运行时复测。

# 2026-07-15（小程序虚拟支付审核失败分析）

- 用户提供微信审核失败截图和官方文档。官方运营指南明确将“会员与特权服务”“工具类增加使用次数/额度”“AI 服务购买次数/token/生成结果”列为典型虚拟支付；食探会员的每日 8/20/40 积分和精准模式解锁同时命中，审核员判断有充分依据，不建议申诉误判。
- 代码只读确认：会员一次性购买当前由前端 `Taro.requestPayment` + 后端微信支付 V3 JSAPI 完成；自动续费为普通 Papay 委托代扣。这两条都不是小程序虚拟支付 XPay。
- 推荐迁移为 9 个会员虚拟道具（3 档 × 3 周期），客户端使用 `wx.requestVirtualPayment(mode=short_series_goods)`；后端用虚拟支付 offerId/AppKey 和 fresh session_key 分别生成 paySig/signature，并以 `xpay_goods_deliver_notify` + `query_order` 幂等开通权益。现有支付历史无需改写，新订单切换通道即可。
- 自动续费先下线再提审。官方虚拟会员订阅要求已接 XPay、首发满 90 天、认证备案、近 30 天日均 DAU >= 1 万；单次会员购买不需要该能力。满足准入后再接安卓/鸿蒙订阅与 iOS Apple 订阅。
- 本轮未修改业务代码、配置、数据库或支付数据。
