# 2026-08-09

- 完成 App 应用商店上架准备盘点。Mobile 功能覆盖已较完整，TypeScript typecheck 通过；Android 有原生工程和实际 APK 发布/验证历史，但发布工程尚未达到商店提交标准。
- 主要阻塞：production EAS 仍输出 internal APK；Android `versionName=0.0.1` 与产品版本 `3.0.2` 不一致；release 签名缺失会回退 debug keystore；iOS 缺 bundle identifier 和提交配置；商店素材、隐私 URL/问卷、资质和权限最小化仍待补齐。
- App 内支付尚未开放，自动续费仅为审核预览。后续需先确认首发渠道和版本号，再执行平台化上架清单与正式安装包回归。
- `.local-state/current-task/active.md` 当前不是有效 UTF-8，`apply_patch` 无法安全追加，本次未重写该文件以免破坏历史状态。

## Android vivo 商店构建准备

- Mobile/root/Expo 用户可见版本已统一为 `3.0.2`，Android `versionCode=4`；Gradle 改为直接读取根 `package.json` 与 `apps/mobile/app.json`，避免原生版本继续停留在 `0.0.1`。
- 新增 `npm run build:mobile:android:store`，固定生产 API，离线生成正式签名 APK 与 AAB，不上传 COS。
- release 构建缺少正式签名时现在会直接失败，不再静默回退到 debug keystore。
- 有效正式签名是 `.local-state/android-signing/foodlink-vivo-release-v2.keystore`，凭据备份是同目录 `SIGNING-BACKUP-V2-IMPORTANT.txt`；必须离线双重备份。首次随机数实现失败留下的非 V2 文件不得使用。
- 最终产物：`dist/mobile-store/foodlink-3.0.2-4-release.apk`（86,279,828 bytes，SHA-256 `114E3925219BF8B7E1760C39BD555B73DD7A9C52D84062B4ED77BE1B4553A64D`）和对应 AAB（56,851,797 bytes，SHA-256 `751E13D586352CC2E07F180D5382D647F6B343DC277AD94EBDB5737066FE591C`）。
- APK 包名 `cn.healthymax.foodlink`、应用名“智健食探”、版本 `3.0.2 (4)`；证书 SHA-256 `b96a6ccafa23341dccf0489e88f82d816004d984c9dbfdf7414a9147788bffc6`，APK Signature Scheme v2 验证通过。
- 已从最终 manifest 移除未使用的 `RECORD_AUDIO` 与 `SYSTEM_ALERT_WINDOW`。当前只连接到已有旧签名 App 的模拟器，未连接用户 vivo 真机，因此未卸载旧 App，也尚未完成 vivo 真机安装/交互回归。
