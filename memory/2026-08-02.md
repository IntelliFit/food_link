# 2026-08-02

## 线上像素分身不可用与首个内置真人像素伙伴

- 用户最新正式版在宠物页生成像素分身时提示“像素分身生成服务暂不可用”。该文案只会在 Go 后端判断像素生成凭证为空时返回；生产 `/api/pet/pixel-avatar` 路由本身已存在，未鉴权探测返回 401，排除前端路径或旧生产路由 404。
- 小程序环境路由确认：本地 develop 使用 `127.0.0.1:3010`，体验版 trial 使用 `dev.api.healthymax.cn`，正式版 release 使用 `api.healthymax.cn`。上传/审核小程序前端不会同步部署 Go 后端或补齐 Apollo 后端配置。
- 后端现优先使用专用 `pixel_avatar_api_key`；若该项缺失且像素模型与 OfoxAI 都明确指向万界 `maas-openapi.wanjiedata.com`，安全复用同一万界账号的 `ofoxai_api_key`，不跨提供商兜底，也不记录密钥值。启动日志会记录“复用万界大模型凭证”或“未配置凭证”。
- 用户授权将其提供的证件照转成项目内置常用宠物。使用内置 image generation 工具生成保留短黑发、黑框眼镜、红色运动外套特征的 2x2 四动作像素精灵表，去除绿幕并拆分为 idle/blink/squash/jump 四张 384x384 透明 PNG，保存于 `apps/wechat/src/assets/pets/jianwen-01-*.png`；未把原证件照提交到项目。
- 后端画像匹配版本升级到 3，为所有账号追加 `builtin:jianwen-01` 常用候选；选择结果以 `avatar_type=builtin_person` 和 `builtin_avatar_id=jianwen-01` 写入宠物 meta，因此同一账号跨设备同步，且选择内置形象不消耗免费画像重匹配机会。
- 小程序宠物页新增始终可见的“常用形象”卡片，内置形象直接使用随包四帧资产，不依赖在线模型；首页、宠物主页、聊天页继续复用统一 `PetAvatar` 组件。
- 验证：Go `internal/app`、`internal/pet/service`、`pkg/config` 测试通过；微信端和 core TypeScript 通过；core 14 个测试通过；目标 ESLint 与 `git diff --check` 通过。微信开发者工具 3001 自动化端口仍未开放，运行时截图与点击验证被阻塞。
