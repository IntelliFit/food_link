/** 与 `pages/day-record/index` 相同：微信默认导航栏（继承 app window 白底黑字），仅标题不同 */
export default definePageConfig({
  navigationBarTitleText: '识别记录详情',
  enableShareAppMessage: true,
  enableShareTimeline: true
})
