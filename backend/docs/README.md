# Go Backend Docs

- `backend-api-prd/` is copied from the repo root migration PRD set.
- The Go service reads `docs/backend-api-prd/ROUTE_MAP.md` to auto-register stub routes for unmigrated endpoints.

