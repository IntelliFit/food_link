# Required. No default is applied.
# local: read all runtime config from app-config.yaml.
# infisical: read Infisical bootstrap from infisical-config.yaml, then fetch all
# runtime business config from Infisical.
CONFIG_SOURCE=local
